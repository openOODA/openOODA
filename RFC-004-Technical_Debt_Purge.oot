# RFC-004: Technical Debt Purge

> **Honesty (F8 / 2026-08-12):** This RFC is **CLOSED / superseded**.
> It was a 22-line stub that claimed **884 unresolved stubs** with
> empty 100-stub checkboxes and **no inventory**. The 884 figure is
> **unproven** (RULES §1.1). Live debt is already owned by the SPRINT
> audit umbrella + `audit/audit_2026-08-12_run.oot`. Activating this
> RFC would **duplicate** that umbrella. Darwin Path B. Ledger:
> `audit/f8_rfc004.oot`.

## Status
**CLOSED / superseded.** Do not start a second debt-purge swarm here.

**Superseded by:**
- `SPRINT.oot` §3 — Audit Findings — openOODA/audit run 2026-08-12
- `audit/audit_2026-08-12_run.oot`

**Live owners (do not copy their inventory here):**
- **F26 CRIT** — `p7_check_main_smoke.sh` still FAIL. `audit/f26_cheap_scan.oot`
- **F27 CRIT** — oodac files over the 256-line cap. Peel there.
- **F20 HIGH** — product `ooda/std` `.oo` count vs ~1,030 triage. Classify there.

## What this RFC was
A draft that said "track 884 stubs." Ten unchecked boxes. No file
list. No real / partial / stub / phantom class. No repro.

## What this RFC is not
- Not a live inventory.
- Not a claim that debt is gone.
- Not a claim that 884 stubs exist or were burned down.
- Not a product-path change. No stubs shipped or deleted here.

## Darwin

| Path | What | Result |
|------|------|--------|
| A | Activate with a short inventory that points at F26 / F27 / F20 | **LOST** — would copy SPRINT |
| B | Close / supersede by SPRINT umbrella + audit run | **WIN** — 884 is unproven; no unique debt off SPRINT |

## Residual
Debt remains **open** on SPRINT (F26, F27, F20, and the rest of the
2026-08-12 rank list). Named-gap args / walk still open (F32 / F33).
**Alpha only — beta not claimed.**

## Conclusion
RFC-004 is closed by honesty, not by a purge. Do not import an
884-stub burndown. Work F26, then F27, then F20. Detail:
`audit/f8_rfc004.oot`.
