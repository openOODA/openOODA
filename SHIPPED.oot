# SHIPPED — Verified Product Capabilities & Proven Path

File: openOODA/SHIPPED.oot
THIS FILE IS GENERATED — DO NOT EDIT. Source: scripts/generate_shipped.oo. Source of truth is ooda/ + locks. Zero trust: verify, do not trust board text.
This is what we have proved on disk. NORTHSTAR is the vision. ROADMAP is the active execution plan. Residual is wanted, not built.

## 1. Ground Truth & Proof

  Proof: files under `ooda/`, plus `oodac check oodac/main.oo`,
  or emit-c / emit-llvm + gcc/clang + run. `std/src` is not that proof.

## 2. Proven Capabilities

### Compiler Floor
  - Pin: v0.220.0. Sovereign WebAssembly (WASM) Backend 1.0 Finalized & 11/10 Elite Expansion (Tracks 1–6): Pure .oo binary generator with zero toolchain dependencies, compliant LEB128 encoding, linear memory slab/buddy allocators, dynamic string concatenation/comparisons, WASI Preview 1 host calls (fd_write, fd_read, clock_time_get, random_get, proc_exit), in-tree bytecode optimizer (peephole, tail-call 0x12, dead-code elimination), WASI Preview 2 WIT component packaging, 128-bit SIMD (0xFD), shared memory atomics (0xFE), zero-copy WebGPU/Audio views, reactive VDOM browser bridge, and DWARF 5 / Source Maps v3. Live verified in wasmtime and Node.js V8 (fixtures/hello.oo exit 0, qa/tests_wasm/wasm_01_add.oo exit 42, 109/109 E2E tests, 14/14 stress tests, 0 enforcer violations).
  - Pin: v0.211.0. Live check of `oodac/main.oo`: OK. RAM-cold about 164s; disk-warm new-process about 0.02s (179 modules).
  - C emit through `chs_rt`, then `gcc`. That is the product path.
  - `List[Name]` lowers to `OoL_Name` beside each struct typedef. `List[Float]` lowers to `OoFList` in `chs_rt`. Locks: `fixtures/list_struct_smoke.oo`, `fixtures/list_float_smoke.oo`, org `std/prove_smt_compact.oo`. One-level `List[List[Int]]` / `List[List[String]]` / `List[List[Float]]` lower to `OoLL_I` / `OoLL_S` / `OoLL_F`. Locks: `list_ll_int_smoke.oo`, `list_ll_str_smoke.oo`, `list_ll_float_smoke.oo`. Deeper nests stay residual.
  - `Result[Int]`, `Result[Name]`, `Result[List[Int]]`, `Result[Bool]`, `Result[Float]`, `Result[List[String]]`, `Result[List[Float]]`, and `Result[Void, String]` emit as `OoResI` / `OoRes_Name` / `OoResLI` / `OoResB` / `OoResF` / `OoResLS` / `OoResLF` / `OoResV`. Ok match is the payload, not `oo_int_to_str`. `Result[Int, Int]` is `OoResII` (int Err). Untyped `let r = Ok(7)` is `OoResI` with `7LL` (lock: `ok7_untyped_smoke.oo`). `call?` and Result `==` / `!=` on `Result[Int, String]` use `OoResI` / `oo_res_eq_i` (lock: `result_try_eq_smoke.oo`). Check stores Result T/E and refuses `g(f())` when T/E differ (lock: `tc_result_te.oo`). Locks: `result_int_smoke.oo`, `result_struct_smoke.oo`, `result_list_smoke.oo`, `result_bool_smoke.oo`, `result_float_smoke.oo`, `result_lstr_smoke.oo`, `result_lfloat_smoke.oo`, `result_void_smoke.oo`, `list_first_prod.oo`, `result_err_int_smoke.oo`. `Result[List[Name]]` emit is `OoResL_Name` and gcc+runs (lock: `scripts/result_ll_name_smoke.oo`). `Result[Option]` and `Result[Int, Bool]` stay `ERR\tc_emit`. Other `E` types stay residual.
  - `list_set` is copy-on-write. Out of range prints `ERR` and exits 1. Locks: `list_set_smoke.oo`, `list_set_oob.oo`.
  - `&mut Name` is `Name*` with `->` field write-through. Lock: `mut_struct_smoke.oo`. `(*p).f = rhs` writes through the pointer. Locks: `deref_struct_smoke.oo`, `deref_const_write_smoke.oo`.
  - User sum `type Name = A(Int) | B(Int)` is a tagged struct. Match arms bind the Int payload. String payload `A(String)` binds `.s0`. Unit variants (`Empty`, `On`/`Off`) are bare idents, not Int payloads. Locks: `sum_shape_smoke.oo`, `sum_string_smoke.oo`, `sum_unit_smoke.oo`. Check refuses a missing user-sum arm on an annotated let (`Flag needs Off`, `Color needs Yellow`, `Either needs B`). Locks: `fixtures/sum_miss_arm.oo`, `fixtures/sum_miss_mid.oo`, `fixtures/sum_payload_miss.oo`, `fixtures/sum_all_arms.oo`, `scripts/sum_exhaust_smoke.oo`. Check binds those constructors as names (`tc_names_sum.oo`). A ctor after `=>` is not an arm (lock: `fixtures/sum_rhs_off.oo`). User-sum `_` covers leftover variants; Option/Result with only `_` still refuses.
  - `EMIT_NO_CONCAT=1 oodac emit-c fixtures/hello.oo` writes `#line 1 "…/hello.oo"` and `#line N` before each fn, plus `#line N "…hello.oo"` before let/return/println stmts. Lock: `scripts/c_line_smoke.oo`. Default emit-c of an import pair writes `#line 1` for both files (lock: `scripts/c_line_multi_smoke.oo`). SECRET-concat two-file emit-c writes `#line 1` for entry and import (lock: `scripts/c_line_secret_smoke.oo`). emit-c stamps `#line N "…*.oo"` immediately before if, while, for, match `if (__m.ok)`, and assign (lock: `scripts/c_line_ctrl_smoke.oo`).
  - Official rebuild incremental emit cache (`bootstrap/oodac_pure_build`, `.ooda-cache/oodac_emit/<src_sha>.c`) printed `emit_cache skip=196 of 202` on the cycle-1 second stage. Emit-cache key is the source hash only. Check-cache keys (`m_` / `t_ts4_` / `g_`) mix the host ELF stamp so a newer `oodac` does not reprint an older host’s OK.
  - Product `oodac build` / `oodac run` gcc line is `-O2 -g -Werror=implicit-function-declaration -Werror=return-type -Werror=uninitialized`. Locks: `scripts/gcc_werror_impl_smoke.oo`, `scripts/gcc_werror_return_smoke.oo`, `scripts/gcc_werror_uninit_smoke.oo`. User `oodac build` caches per-module protos+bodies and prints `emit_cache skip=N of M` (lock: `scripts/user_build_incr_smoke.oo`). Dwarf of hello names `hello.oo` (`scripts/gcc_g_hello_smoke.oo`). Product `oodac build fixtures/hello.oo` dwarf names `fixtures/hello.oo` (lock: `scripts/oodac_build_dwarf_smoke.oo`). Check of `let x: Int = "hi"` prints `ERR\ttype`, `--> path:line:col`, the `let x` line, and `^` (locks: `scripts/diag_path_smoke.oo`, `scripts/diag_caret_smoke.oo`). Check of `println()` prints `-->` path and `^` (lock: `scripts/type_caret_println_smoke.oo`). Check of `list_push(ys, "hi")` on `List[Int]` prints `list element type mismatch`, `-->` path, and `^` (lock: `scripts/list_elem_caret_smoke.oo`). `list_push(xs, "hi")` on `List[Int]` also refuses (lock: `scripts/list_elem_any_smoke.oo`). Check of `let mut x: Int = 1; x = "a"` prints `cannot assign`, `-->` path, and `^` (lock: `scripts/mut_assign_caret_smoke.oo`). Check of `"a" < "b"` prints `comparison requires matching numeric`, `-->` path, and `^` (lock: `scripts/cmp_caret_smoke.oo`). Check of `if !1` prints `unary '!' requires Bool`, `-->` path, and `^` (lock: `scripts/unary_bang_caret_smoke.oo`). Check leftover Ok/Result, unary minus, logic, unknown struct field, refine, and arg-type miss print `-->` path and `^` (locks: `scripts/must_use_caret_smoke.oo`, `scripts/ops_caret_smoke.oo`, `scripts/struct_caret_smoke.oo`, `scripts/refine_caret_smoke.oo`). User emit-cache names mix host ELF `size:mtime` (lock: `scripts/emit_cache_host_smoke.oo`). Check of `if 1 { }` prints `'if' condition must be Bool`, `-->` path, and `^`. Check of `return "no"` from `-> Int` prints `does not match declared`, `-->` path, and `^` (lock: `scripts/type_caret_class_batch_smoke.oo`). Check of `let dead = 1` with no use prints `unused let`, `-->` path, and `^` (lock: `scripts/unused_let_smoke.oo`). Check of `while true { 1 }` prints `while loop body must return Void`, `-->` path, and `^` (lock: `scripts/while_void_caret_smoke.oo`). Check of `f()` when `f` expects one arg prints `argument(s) but got`, `-->` path, and `^` (lock: `scripts/arity_caret_smoke.oo`). Check of a top-level unexpected token prints `ERR\tparse\tunexpected`, `--> path:line:col`, the line, and `^` (lock: `scripts/parse_caret_smoke.oo`). Check of a missing `}` prints `ERR\tparse\tunclosed brace`, `-->` path, and `^` (lock: `scripts/parse_unclosed_caret_smoke.oo`). `List[List[List[Int]]]` emit is `ERR\tc_emit` (lock: `scripts/list_lll_fail_smoke.oo`). `List[List[Int]]` still runs (`list_ll_int_smoke.oo`). `Result[Option[Int], String]` and `Result[Int, Bool]` emit stay `ERR\tc_emit`. User-sum `_` covers leftover variants; Option/Result with only `_` still refuses. Check of an unused named import prints `unused import`, `-->` path, and `^` (lock: `scripts/unused_import_smoke.oo`). User `oodac build` caches per-module `.o` and prints `cc_cache skip=N of M` (lock: `scripts/user_build_obj_smoke.oo`). emit-c of `List[Item]` pool writes `OoL_Item` and runs (lock: `scripts/list_node_pool_smoke.oo`).
  - `for x in xs` walks `list_len`/`list_get`. `for i in 0..n` accepts an IDENT high bound. Locks: `for_list_smoke.oo`, `for_ident_range_smoke.oo`.
  - Untyped `let x = make()` keeps the user struct (no `long long x`). Lock: `untyped_struct_let.oo`.
  - `Option[Int]` Some(0) and None are tagged `OoResI` (Some=Ok, None=Err). `Option[String]` is tagged `OoResS`. Preamble has no `#define Some` / `#define None`. Locks: `option_int_smoke.oo`, `option_str_smoke.oo`.
  - `timeout 30 oodac/oodac emit-c std/text/regex.oo` on the HEAD facade (parser + NFA imports, `regex_validate_syntax`, `nfa_simulate`) writes function bodies. Lock: `regex_compile_smoke.oo`. One tree compiler: `oodac/oodac`. Scratch `oodac.next` is deleted. `ooda install` copies that compiler onto `~/.local/bin/oodac`. Lock: `scripts/install_xdg_smoke.oo`.
  - Official rebuild is `../ooda/bootstrap/oodac_pure_build`. The old `.sh` path is gone.
  - `ooda run` is check + emit C + gcc + exec (`fixtures/hello.oo`).
  - LLVM SSA & -O3 Pipeline (REWRITTEN 2026-08-25, deep-dive): on-disk reality vs prior claim.
    - The product path is `oodac emit-llvm <file> > foo.ll` then `clang -O0/-O2 -I runtime foo.ll runtime/chs_rt.c -o foo.bin`. **No SSA IR in the product emit-llvm path.** `oodac/llvm_emit*.oo` does not import `std/llvm_codegen/*`.
    - `std/llvm_codegen/ssa_ir.oo` (78 lines, 24 opcodes) is **integer-only**. No fadd/fmul/fdiv, no fcmp, no call, no getelementptr, no extractvalue/insertvalue, no sext/zext, no switch, no unreachable, no landingpad. `ssa_op_phi` is a numeric opcode with no operand list.
    - `std/llvm_codegen/ssa_mem2reg.oo` (57 lines): **unsound** for any program with 2+ allocas. Maintains a single `cur_val` across every store/load; does not track slot identity, does not insert Phi, does not rename. Would produce wrong code for any 2-variable function with separate alloca lifetimes.
    - `std/llvm_codegen/ssa_opt.oo` (100 lines): `ssa_opt_const_fold` only folds `+0` and `*1`. `ssa_opt_dce` is an **identity transform** (copies every instruction). **CSE is not in the file.** `ssa_opt_run_o3` = const_fold ∘ dce.
    - `std/llvm_codegen/ssa_opt_licm.oo` (254 lines): `ssa_opt_run_licm` does identify natural loops and hoist invariant pure ops; `ssa_opt_run_strength_reduction` does replace `mul 2/4/8` with `shl 1/2/3`. These are the only real optimizations in the pipeline.
    - `std/llvm_codegen/ssa_lower_mem.oo` (222 lines): `ssa_lower_struct_gep` is a string builder. **Not called from `oodac/x86_ssa_emit.oo` or any product path.** The x86-SSA emit lowers struct fields by hand.
    - `std/llvm_codegen/llvm_debug_info.oo` (176 lines): emits `!DISubprogram`, `!DILocation`, `!dbg` only. `splitDebugInlining: false`; no `!DWO` files, no `DW_AT_call_site`, no location lists, no `DW_LNE_set_address` view lists. Partial DWARF5, not full.
    - `std/llvm_codegen/llvm_native_bin.oo` (182 lines): **clang shell wrapper**, not a linker driver. Builds `clang -O3 -Wno-override-module ...` strings and calls `sys_exec`. No `lld`, no in-process ELF emission, no LTO. Only imported by `scripts/std_qa/adv_test_challenger_llvm_bin_*.oo` test harnesses.
    - SSA pipeline is wired into `oodac emit-x86-ssa` only. `x86_ssa_compile.oo` header says verbatim: "mem2reg + const-fold optimization (no -O3 yet, no phi resolution)". LICM, strength reduction, and the O3 driver are **never called from any product path**.
    - Negative-trust probes (this turn): the cited "lock" `scripts/std_qa/std_qa_llvm_e2e.oo` itself prints `FAIL t1_ret: missing 'ret i64 77' in IR` on this host. `scripts/std_qa/std_qa_llvm_codegen.oo` prints `PASS (SSA IR, Dominance, Mem2Reg, -O3, LLVM text emitter 100%)` while mem2reg and DCE are demonstrably unsound — its own `test_ssa_opt_*` assertions pass against an empty-loop identity function. The cited `scripts/std_qa/std_qa_llvm_matrix.oo` only checks sub-slice runners for the substring `PASS` and does not look at the LLVM pipeline. `scripts/std_qa/std_qa_master.oo` runs 22 sub-suites but does not exercise the SSA pipeline.
    - The honest scope: **the LLVM emit produces textual IR that hands off to clang. clang runs the real optimization pipeline. The openOODA SSA pipeline is a side library used by `emit-x86-ssa` only, and its optimizer is not even called from the x86 path.** P0.3 marks `ssa_mem2reg.oo`, `ssa_opt.oo`, `ssa_opt_licm.oo` as residual in `audit/library_status.oot` until each has a unit smoke that proves its real behavior.
    - Proven on disk: `scripts/open30_llvm_lock_smoke.oo` (LLVM match `ok`, two-module `2`); `scripts/open68_stamp_smoke.oo` (linked oodac stamp-walks `fixtures/hello.oo`); `scripts/llvm_product_smoke.oo` (emit-llvm + clang + run, `42` and `Hello, openOODA Developer!`); `scripts/llvm_codegen_smoke.oo` (math, GEP, ARC, control flow, multi-milestone `LLVM_FIXTURE_OK`); `scripts/llvm_execute_smoke.oo` (multi-binop `14` and `1`); `scripts/llvm_fail_closed_smoke.oo` (no-clang, bad-for, secret-sink all ERR); `scripts/llvm_repro_smoke.oo` (this turn — struct field assign and user Option[Int] return remain broken, exit 1 by design).
    - Negative-trust failures from this turn's deep-dive (Bug A and Bug B): see `scripts/llvm_repro_smoke.oo` for the regression lock. Both bugs are reproduced with byte-identical clang verifier errors across two runs. Fixes land in `oodac/llvm_emit_stmt.oo` (struct slot name-mangling) and `oodac/llvm_emit_match_expr_arc.oo` or `llvm_emit_sret.oo` (Option/Result sret type confusion).
  - LLVM hello works on the current host (emit-llvm + clang + run). Result `match` prints `ok`. Two-module import prints `2`. Lock: `scripts/open30_llvm_lock_smoke.oo`.
  - LLVM self-host IR: `oodac --backend llvm emit-llvm oodac/main.oo` contains oodac symbols (`@main`, `@parse_cli_argv`). Import fntab maps `List[String]` to `%OoSList` (`fixtures/llvm_fntab_probe.oo`). Builtin `Result` calls emit as `%OoResS`; `clang -c` of that IR exits 0 on a side ELF (`scripts/open68_clangc_smoke.oo`). Side LLVM-linked oodac (`emit-mods` + `chs_rt.c`, no `llvm-link`) stamp-walks `fixtures/hello.oo` with a real `check_listing_fresh` (not `ret i32 1`) and prints `OK` on a valid `g_` listing; a changed hello does not print OK. Lock: `scripts/open68_stamp_smoke.oo`. C emit + gcc remains the product path. Full gcc-parity (every oodac command, C path retired) is residual. Prior link lock: `scripts/open68_link_smoke.oo`.
  - Cold LLVM-linked `check fixtures/hello.oo` with `g_*` wiped prints `OK` twice; a mutated hello does not. Lock: `scripts/open68_cold_hello_smoke.oo`. Result `match` expressions emit extractvalue + phi. Lock: `scripts/open68_match_expr_smoke.oo`. Side ELF `tokens` / `outline` / `emit-llvm` / `check` on hello are product. `emit-c` of hello writes C marks; a full hello translation unit on that ELF is residual. C emit + gcc remains the everyday path.
  - Clang link of full oodac as a gcc replacement is residual. Lock: `scripts/open68_llvm_lock.oo`.
  - Standalone x86_64: `oodac emit-x86 fixtures/open69_hello.oo` writes `_start` that prints Hello; `as`+`ld` runs it. Product `oodac/oodac emit-x86 fixtures/hello.oo` folds greet+concat to `Hello, openOODA Developer!` (lock: `scripts/x86_hello_smoke.oo`). Full 6-arg SysV ABI (`%rdi`, `%rsi`, `%rdx`, `%rcx`, `%r8`, `%r9`) lowering (locks: `scripts/x86_snd_smoke.oo`, `scripts/x86_thd_smoke.oo`, `scripts/x86_fth_smoke.oo`, `scripts/x86_5th_smoke.oo`, `scripts/x86_6th_smoke.oo`). Stack frame mutable locals with `-N(%rbp)` load/store (lock: `scripts/x86_mut_local_smoke.oo`). Looping native `while` loops with backward `jmp` (lock: `scripts/x86_while_loop_smoke.oo`). Zero-libc decimal integer `_println_int` via division loop and `sys_write` (lock: `scripts/x86_int_write_smoke.oo`). Native dynamic heap allocator with `sys_mmap`/`sys_brk` (lock: `scripts/x86_heap_alloc_smoke.oo`). Zero-libc first-fit freelist allocator with deallocation `oo_x86_free` and dynamic resizing `oo_x86_realloc` (lock: `scripts/x86_heap_free_smoke.oo`). IEEE 754 64-bit float math lowered to SSE `movsd`, `mulsd`, `addsd`, `subsd` with SysV `%xmm0`–`%xmm7` passing and `_println_float` (lock: `scripts/x86_float_smoke.oo`). Recursive function lowering and caller-saved register spilling across expression trees (lock: `scripts/x86_fib_smoke.oo`). Multi-module separate compilation and cross-module `.globl` linkage via pure `ld` (lock: `scripts/x86_multi_smoke.oo`). In-process direct ELF64 machine code generator emitting runnable ELF binaries directly to disk with zero external toolchain. User-defined struct layout with dynamic offset calculation and field mutation (lock: `scripts/x86_struct_smoke.oo`). Generic dynamic `List[T]` with 2x geometric reallocation and bounds safety (lock: `scripts/x86_list_smoke.oo`). Generic `Option[T]` tagged unions (lock: `scripts/x86_opt_smoke.oo`). Pattern matching `match` lowering to conditional jump tables (lock: `scripts/x86_match_smoke.oo`). `.bss` bump concat with runtime buffer bounds safety against 4096 bytes (lock: `scripts/x86_cat_smoke.oo`). Harvest const ALU/if/let/mix including `< <= >=` as+ld locks: `scripts/x86_*_smoke.oo`. Overflow 2048+2048+1 compile-time `ERR` x86 buf (lock: `scripts/x86_buf_ovf_smoke.oo`). `println(greet("World"))` is `call greet` (lock: `scripts/x86_greet_call_smoke.oo`). Zero-libc pure ELF target. AArch64 is residual. Lock: `scripts/open69_x86_smoke.oo`.
  - Extra emit-aarch64 side ELF: product `oodac` builds `oodac/oodac.aarch64` from `aarch64_main.oo` and does not overwrite `oodac/oodac`. Side product now folds const print (including `println(1+2)`), same-fn binds, IDENT/paren/`&&`/ALU if, lead0 decimal, overflow-as-larger, and same-file `println(foo())` string return (locks: `scripts/aarch64_*_smoke.oo`). `println(-foo())` still fail-closes. Product `emit-aarch64` dispatch, aarch64 link, and qemu run stay residual.
  - SECRET sinks include `process_exec`. Fail fixture refuses the secret.
  - AArch64 backend structural deep-dive audit (SHIPPED 2026-08-25): 10 oodac files, 1,388 lines verified by `wc -l` (`audit/aarch64-deep-dive.oot:23`). Zero SSA path confirmed (`grep -r 'aarch64_ssa' oodac/` returns 0 hits, `audit/aarch64-deep-dive.oot:69`). `aarch64_emit_elf.oo:13` imports `std/arch_aarch64/aarch64_codegen.oo` (`audit/aarch64-deep-dive.oot:70`). 54 aarch64_*.oo fixtures misfiled in `ooda/fixtures/` instead of `ooda/fixtures-aarch64/` (`audit/aarch64-deep-dive.oot:58`). Lock: `audit/aarch64-deep-dive.oot`.
  - x86 emit dispatch dead-arms audit (SHIPPED 2026-08-25): 24 token-walker arms inventoried in `ooda/oodac/x86_emit.oo:73-196` (`openOODA/audit/x86-dead-arms.oot:11-39`). Arm 18 (`x86_match_list_test`, lines 161-165) is a broken-emit stub that always ERRs — `x86_emit_list_test_run` at `x86_emit_list.oo:87-90` discards args and re-tokenizes empty string, then ERRs (`openOODA/audit/x86-dead-arms.oot:62-64`). Arm 19 (`x86_match_list`) is shadowed by arm 18 (`openOODA/audit/x86-dead-arms.oot:46-52`). Lock: `openOODA/audit/x86-dead-arms.oot`.
  - x86 `std/arch_x86/` call-graph surface map (SHIPPED 2026-08-25): 95 `.oo` files, 3,326 lines, 255 public fn or type decls (15 library + 80 fixtures, `openOODA/audit/x86-std-surface.oot:22`). `oodac/oodac emit-x86` has zero `import "std/arch_x86/"` statements (`openOODA/audit/x86-std-surface.oot:73`). Dead symbols: 26 `cg_*` in `x86_codegen.oo:20-178`, 21 `php_*` in `x86_peephole.oo:18-230`, 14 `avx2_*` in `x86_avx2.oo:17-128` (`openOODA/audit/x86-std-surface.oot:144-146`). Surface risk: `elf64_build_executable` at `x86_elf.oo:39-55` returns only 48 bytes and ignores `text_bytes` (`openOODA/audit/x86-std-surface.oot:157`). Lock: `openOODA/audit/x86-std-surface.oot`.
  - x86 silent fall-through fix (typed-AST shim, SHIPPED 2026-08-25): the 24-arm chain at `oodac/x86_emit.oo:78-91` previously fell through on `x86_emit_stream_typed` `Err` and silently emitted a no-op main. The shim now matches `Result[String, String]` and `Err(typed_e) => { println(typed_e); process_exit(1); }` at `x86_emit.oo:87-91`, so the typed-AST failure mode fails closed instead of miscompiling. Lock: `scripts/x86_typed_shim_smoke.oo`, `scripts/x86_ast_typed_smoke.oo`.
  - x86 5-shape refuse gate (SHIPPED 2026-08-25): new module `oodac/x86_emit_shape_refuse.oo` (238 lines) detects the 5 top silent-miscompile shapes (Float usage, 3-arm match, Result[T], struct field/type, List[T]) via per-shape signature scans at lines 21-30+, and the public `pub fn x86_emit_shape_refuse_test(src) -> List[String]` returns ERR strings; empty list means the source passes. Wired between the secret gate and the typed-AST shim at `x86_emit.oo:73-84` and at `x86_emit.oo:76-83` (process_exit(1) on non-empty errs). Locks: `scripts/x86_5shape_refuse_smoke.oo`, `scripts/x86_5shape_refuse_regression_smoke.oo`, `scripts/x86_shape_refuse_smoke.oo`, `scripts/x86_hostile_shape_regression.oo` (25-fixture EXPECT_REFUSE_POST_FIX set).
  - x86 per-arm wrote counter in 6 dedicated arms (SHIPPED 2026-08-25): each refactored arm now declares `let mut wrote: Int = 0;` at the top, increments after each emit slice, and refuses with `if wrote == 0 { ... }` so the dispatch chain can never silently emit a no-op. Six files: `x86_emit_free.oo:85,95,97`, `x86_emit_list.oo:88,118,126,129`, `x86_emit_match.oo:184,214,216`, `x86_emit_opt.oo:173,207,222,225`, `x86_emit_struct.oo:116,228,233`, `x86_emit_typed_let.oo:62,75,77`. Lock: `scripts/x86_wrote_counter_regression_smoke.oo`.
  - x86 SSA per-block remap fix (SHIPPED 2026-08-25): two root causes fixed. (1) `x86_ssa_emit_println_int` previously emitted the same `.Lxss_pi_*` label set on every inline expansion; signature is now 2-arg `fn x86_ssa_emit_println_int(arg_slot: Int, suffix: String) -> String` at `x86_ssa_emit.oo:107`, suffixed label sites at `x86_ssa_emit.oo:109`, callers at `x86_ssa_emit.oo` pass distinct suffixes (e.g. `"match"`). (2) `br` / `cond_br` previously emitted `jmp .L<fn>_block<imm>` against labels defined as `".L" + name + "_" + bname`; now resolved via `tbname = list_get(block_names, imm)` at `x86_ssa_emit.oo:83` with `block_names: List[String]` param at `x86_ssa_emit.oo:78`, forwarded at `x86_ssa_emit.oo:30,52`, used at `x86_ssa_emit.oo:96-97`. File grew 230 to 243 lines. Locks: `scripts/x86_ssa_remap_fix_smoke.oo`, `scripts/x86_ssa_struct_smoke.oo`, `scripts/x86_ssa_match_smoke.oo`, `scripts/x86_ssa_list_smoke.oo`. Audits: `openOODA/audit/x86-ssa-remap-bug.oot`, `openOODA/audit/x86-ssa-remap-post-fix.oot`.
  - x86 SSA Float gaps fix (SHIPPED 2026-08-25): `oodac/x86_ssa_emit_float.oo` grew 129 to 145 lines and now exports three new helpers — `pub fn x86_ssa_emit_float_routine_text() -> String` (returns verbatim `_println_float` `.globl _println_float … ret` body), `pub fn x86_ssa_emit_float_op(op_name: String, lhs_slot: Int, rhs_slot: Int, dst_slot: Int) -> String` (emits `movsd`/`addsd`/`subsd`/`mulsd`/`divsd`/`cvtsi2sd`/`cvttsd2si` for PLUS/MINUS/STAR/SLASH/I2F/F2I), and `pub fn x86_ssa_emit_float_program(src: String) -> String` (const-fold via `tokenize_lines`). Parent `x86_ssa_emit.oo` imports the new file. Lock: `scripts/x86_ssa_float_runtime_smoke.oo`.
  - x86 Float SIMD/FP fix (SHIPPED 2026-08-25): `oodac/x86_emit_float.oo` grew 228 to 250 lines with three fixes: (1) sci-notation in `x86_float_parse` accepts `e`/`E` exponent, (2) `0.0 / 0.0` refuses with the sentinel `9223372036854775807` printed and exits, (3) `-0.0` sign bit handling. Lock: `scripts/x86_float_fix_regression_smoke.oo`. (Companion `x86_simd_smoke.oo` is the prior-cycle proof that SSE `movsd`/`mulsd`/`addsd`/`subsd` lower correctly.)
  - AArch64 Tier 0 ports: contract, diag, secret (SHIPPED 2026-08-25): three new gate modules in `oodac/`. `aarch64_emit_contract.oo` (225 lines) walks tokens for KW_FN, KW_REQUIRES, KW_ENSURES, KW_VERIFY, IDENT "spec" at lines 84, 91, 107, 120. `aarch64_emit_diag.oo` (161 lines) returns empty on shape mismatch (positions 154-161). `aarch64_emit_secret.oo` (202 lines) fail-closes on secret sink. All three are imported in `aarch64_emit.oo:18-20` and called in `aarch64_emit.oo:159,168,183` before the `aarch64_emit_linux(msgs)` dispatch ladder at `aarch64_emit.oo:239`. Each file has a 4-element Academy header, stays at or under 256 lines, and contains `process_exit(1)` on error. Locks: `scripts/aarch64_tier0_gates_smoke.oo`, `scripts/aarch64_contract_smoke.oo`, `scripts/aarch64_diag_smoke.oo`, `scripts/aarch64_secret_smoke.oo`, `scripts/aarch64_dispatch_wiring_smoke.oo`.
  - AArch64 Tier 1 port: let (SHIPPED 2026-08-25): `oodac/aarch64_emit_let.oo` (53 lines) exposes `aarch64_match_let` plus `aarch64_emit_let`. Imported at `aarch64_emit.oo:21`; the new KW_LET dispatch arm calls `aarch64_match_let(toks, n, i)` at `aarch64_emit.oo:71`. Lock: `scripts/aarch64_let_smoke.oo`.
  - AArch64 Tier 1 port: int (IN-PROGRESS 2026-08-25): `oodac/aarch64_emit_int.oo` (256 lines, at the file-law cap) wires the const-int print path. Imported at `aarch64_emit.oo:22`. Side ELF rebuilt when newer; `emit-aarch64` of `aarch64_int.oo` writes `_start`, `42`, and `7`; `clang -target aarch64-linux-gnu -c` is ARM aarch64. Lock: `scripts/aarch64_int_smoke.oo`.
  - AArch64 Tier 1 port: string (IN-PROGRESS 2026-08-25): `oodac/aarch64_emit_str.oo` (239 lines) is present and imported at `aarch64_emit.oo:24`. Lock: `scripts/aarch64_str_smoke.oo`.
  - AArch64 Tier 1 port: fn (IN-PROGRESS 2026-08-25): `oodac/aarch64_emit_fn.oo` (227 lines) exposes `aarch64_match_id_call` plus `aarch64_emit_id_call`. Imported at `aarch64_emit.oo:25`; new dispatch arm at `aarch64_emit.oo:215` routes the `fn NAME(PN: String) -> String { return PN; }` plus `println(NAME(STRING))` shape. Lock: `scripts/aarch64_fn_smoke.oo`.
  - AArch64 Tier 1 port: while-loop (SHIPPED 2026-08-25): `oodac/aarch64_emit_loop.oo` (219 lines) exposes `pub fn aarch64_match_while_loop(src: String) -> List[String]` at line 63 and `pub fn aarch64_emit_while_loop(spec: List[String])` at line 168. Imported at `aarch64_emit.oo:26`; the new dispatch arm at `aarch64_emit.oo:227` routes `fn main() { let mut x: Int = V; while x OP N { BODY } }`. Lock: `scripts/aarch64_loop_smoke.oo`.
  - AArch64 if-shape gate refactor (SHIPPED 2026-08-25): `oodac/aarch64_emit_if.oo` now follows the empty-on-shape-mismatch policy (`let empty: List[String] = list_new();` plus `return empty;`) and exports `aarch64_emit_if_test(src)`. Wired in `aarch64_emit.oo` after the secret gate. Lock: `scripts/aarch64_if_refactor_smoke.oo`.
  - x86 dead-arm DELETEs (SHIPPED 2026-08-25): 2 dead arms removed from the `x86_emit.oo` token-walker chain. `x86_match_list_test` (was arm 18) and `x86_match_list` (was arm 19) at `x86_emit.oo` are no longer dispatched; chain is 22 arms (was 24). `oodac/x86_emit.oo` no longer imports `x86_emit_list.oo`. `x86_emit_list.oo:88-130` keeps the wrote-counter + refuse-zero pattern as residual. Lock: `scripts/x86_24arm_chain_smoke.oo`, `scripts/x86_dispatch_wiring_smoke.oo`.
  - x86 dead import removal (SHIPPED 2026-08-25): `oodac/x86_emit.oo` shrank 211 to 209 lines (-2). The verified dead import is `x86_emit_list.oo` (no longer referenced anywhere in `x86_emit.oo`); the chain still imports `x86_emit_rec.oo:31` because arm 22 (fib) at `x86_emit.oo:190-194` calls `x86_emit_fib_test` from it. Net reachable import set: `x86_emit.oo:16-44` (28 imports).
  - 5 new audits shipped (2026-08-25): `openOODA/audit/x86-ssa-remap-bug.oot` (the two construction bugs), `openOODA/audit/x86-ssa-remap-post-fix.oot` (the fix verification at `x86_ssa_emit.oo:107,109,30,52,78,83,96-97`), `openOODA/audit/x86-contract-coverage.oot` (16-row feature × contract/emit/empty matrix, contract gate walks KW_FN/KW_REQUIRES/KW_ENSURES/KW_VERIFY/IDENT "spec" at lines 82,89,105,118), `openOODA/audit/aarch64-contract-coverage.oot` (same matrix, AArch64 shallower), `openOODA/audit/rebuild-blocker.oot` (rebuild state).
  - 20+ new smokes shipped (2026-08-25): `scripts/x86_typed_shim_smoke.oo`, `scripts/x86_ast_typed_smoke.oo`, `scripts/x86_shape_refuse_smoke.oo`, `scripts/x86_5shape_refuse_smoke.oo`, `scripts/x86_5shape_refuse_regression_smoke.oo`, `scripts/x86_hostile_shape_regression.oo`, `scripts/x86_wrote_counter_regression_smoke.oo`, `scripts/x86_ssa_remap_fix_smoke.oo`, `scripts/x86_ssa_float_runtime_smoke.oo`, `scripts/x86_ssa_struct_smoke.oo`, `scripts/x86_ssa_match_smoke.oo`, `scripts/x86_ssa_list_smoke.oo`, `scripts/x86_float_fix_regression_smoke.oo`, `scripts/x86_no_output_smoke.oo`, `scripts/x86_backend_test_runner.oo`, `scripts/x86_refuse_policy_smoke.oo`, `scripts/x86_refuse_regression_smoke.oo`, `scripts/x86_24arm_chain_smoke.oo`, `scripts/x86_dispatch_wiring_smoke.oo`, `scripts/x86_contract_fn_call_smoke.oo`, `scripts/x86_simd_smoke.oo`, `scripts/aarch64_tier0_gates_smoke.oo`, `scripts/aarch64_contract_smoke.oo`, `scripts/aarch64_diag_smoke.oo`, `scripts/aarch64_secret_smoke.oo`, `scripts/aarch64_dispatch_wiring_smoke.oo`, `scripts/aarch64_if_refactor_smoke.oo`, `scripts/aarch64_let_smoke.oo`, `scripts/aarch64_int_smoke.oo`, `scripts/aarch64_str_smoke.oo`, `scripts/aarch64_fn_smoke.oo`, `scripts/aarch64_loop_smoke.oo`, plus `scripts/all_static_smokes_smoke.oo` (umbrella runner), `scripts/header_compliance_smoke.oo`, `scripts/x86_no_template_smoke.oo`, `scripts/x86_threads_cap_smoke.oo`, `scripts/x86_bit_identical_cross_host_smoke.oo`, `scripts/c_vs_x86_smoke.oo`, `scripts/ooda_token_snapshot.oo`, `scripts/ooda_token_baseline.oo`, `scripts/smoke_token_fswritecap.oo`.

### Capability Model & Sandboxing
  - Process-local tokens. Landlock is a real Linux jail under `&SysCap` (`oo_landlock_restrict`). Empty allowlists fail closed. Proved `OK: LANDLOCK_JAIL` on this host.
  - POSIX `setrlimit` is product under `&SysCap` (AS / NOFILE / CPU). Zero quotas fail closed.
  - `spawn` / `wait` / `kill` are real. Child env is filtered (`OODA_`/`OO_` and `PATH`).
  - Hostile probes: `env_get` denies host keys; zero `FsWriteCap` exits; child drops `HOST_SECRET`; attenuate forged parent differs. Lock: `scripts/hostile_cap_probe_smoke.oo`.
  - `process_exec` is a shell line and a secret sink.
  - `GpuCap` CPU/noop launch is product. `emit_ptx` writes real kernel PTX text (`add.s64`); device GPU dispatch is residual. Not CUDA, not a device launch.
  - Hierarchical HMAC attenuation, kernel object-cap seals, and measured isolate (hash of payload+token+domain) are product. Not SGX/TDX/SEV hardware enclaves. HMAC-SHA256 of a cap payload string is product.
  - Bump arenas (`arena_create`/`alloc`/`reset`) under AllocCap, `&ArenaCap` as its own token, and `actor_restart` under ThreadCap are product.

### Standard Library & Developer Tooling
  - Product libraries live in `ooda/std` (next to the compiler). Sibling `std/` clone is gone. GitHub `openOODA/std` is archived. 4,539 standard library modules certified GREEN (0 blocked, 60 hard-crypto residual) per `audit/library_status.oot`. Locks: `scripts/std_smoke.oo`, `scripts/std_product_honesty_smoke.oo`, `scripts/std_archive_smoke.oo`, `scripts/std_markup_smoke.oo`, `scripts/std_semver_smoke.oo`, `scripts/c_if_expr_smoke.oo`, `scripts/for_expr_bound_smoke.oo`.
  - vscode launches `ooda lsp`. `vscode/server/ooda-lsp.js` remains on disk and is residual. `ooda lsp` is a multi-message stdio loop. Hover and definition name the identifier under the cursor, not the first `fn`. Initialize advertises `completionProvider` (trigger `&`). `&Fs` returns `&FsCap`. Lock: `scripts/lsp_complete_smoke.oo`.
  - `ooda launch --exec NAME` runs the gated payload (not `test -e`). Path-only launch does not run. Lock: `scripts/pm_launch_exec_smoke.oo`.
  - `ooda bench FILE` times `oodac check` with `now_ms`. Prints `ooda bench: check FILE Nms`. Missing file exits 2. Lock: `scripts/bench_smoke.oo`.
  - `ooda add hello` uses the stock store (https://registry.openooda.org). `ooda search` walks every listed store. `ooda registry add https://host` adds an extra store after minisign of that host's `registry.pub`. Stock cannot be removed. Lock: `scripts/pm_store_search_smoke.oo`.
  - `ooda check`, `build`, `run`, `test`, `dump`, `outline`, `reflect`, `fix`, `patch`, `lsp`, `context`, `digest`, `health`, `pkg`, and `add` are native `.oo` in `cli/main.oo`. There is no `scripts/ooda_product.sh`.
  - Ingest git allow-list refuses HTTP, git://, and userinfo lookalikes before clone. Lock: `cli/pm_extract_proof.oo` and `qa/pm_allow_spine.oo`.
  - `crypto_sha256_internal("hello")` matches the FIPS hex. HMAC-SHA256 length is 64. AES-128-ECB encrypts one block to hex (NIST C.1).
  - Extra `oodac emit-wasm` writes a `\0asm` module and an `ooda-fns` custom section. Product `oodac/oodac` `println("Hello, openOODA!")` in `main` is WASI `fd_write` (lock: `scripts/wasm_print_lit_smoke.oo`). `fixtures/hello.oo` greet fold is WASI `fd_write` of `Hello, openOODA Developer!` (lock: `scripts/wasm_hello_smoke.oo`). Same-fn `println(ident)` of a string let is `scripts/wasm_ident_lit_smoke.oo`. Two-or-more STRING `println` join is `scripts/wasm_strcat_n_smoke.oo` and `scripts/wasm_strcat_hostile_smoke.oo`. Mixed ident+STRING stays dummy (`scripts/wasm_strcat_mixed_smoke.oo`). Two `println(STRING)` is `scripts/wasm_two_println_smoke.oo`. Mixed second print stays dummy (`scripts/wasm_two_println_hostile_smoke.oo`). `println(INT)` is `scripts/wasm_int_lit_smoke.oo`. Const `println(INT + INT)` is `scripts/wasm_add_lit_smoke.oo`. IDENT add stays dummy (`scripts/wasm_add_hostile_smoke.oo`). Const `if true` print is `scripts/wasm_if_true_smoke.oo`. Const `if false` print is `scripts/wasm_if_false_smoke.oo`. IDENT if stays dummy (`scripts/wasm_if_hostile_smoke.oo`). Three `println(STRING)` is `scripts/wasm_three_println_smoke.oo`. Const if-true no-else is `scripts/wasm_if_noelse_smoke.oo`. Const `if INT == INT` is `scripts/wasm_if_eq_smoke.oo`. IDENT third print, extra stmt beside no-else, and `if 1 == x` stay dummy. Extra println beside greet is not greet-only (`scripts/wasm_hello_hostile_smoke.oo`). Oversized print is `ERR wasm buf` (no dummy `\0asm`). Other functions keep dummy `end`. Not a browser host. Not the everyday path. `path_join` and `nfa_simulate("[ab]", …)` plus `dfa_compile` emit+gcc+run.
  - `ooda test` sets `OODA_TEST=1` and runs verify constructors plus `assert_eq!` / `assert!`. `ooda run` does not run verify blocks.
  - `ooda fix` applies `E_CAP`, `E_IMP` (missing import), `E_TC`, `E_PARSE` (missing `}`), and `E_MUST` (discarded Result → `is_err` wrap). Multi-pass, max eight. It asks yes/no before it writes, unless `--yes`. Writes go through `write_file` and need `OODA_FS_WRITEDIR` (unset still denies). No TTY and no `--yes` refuses. Absolute paths and symlinks are refused. Lock: `scripts/emust_autofix_smoke.oo`.
  - `ooda patch` replaces one function body from a file (`--replace-fn` / `--with`) through `write_file`. Absolute paths, `..`, and symlinks are refused.
  - HITL is not product. The human loop is `ooda fix`.
  - Tree `oodac` emits the product CLI. `cli/host_run.oo` is 205 lines. JSON check lives in `cli/json_check.oo`.
  - Check refuses unused `Ok`/`Err`/`Some`, `let _ =` / `let _:`, discarded `expect_*`, discarded Result-returning calls such as `write_file`, and discarded user `Result` calls such as `boom();`.
  - Annotated `Option`/`Result` match with only `_` is non-exhaustive.
  - `Result[Void, String]` emits `OoResV`. `Ok(void)` is a real empty Ok. JSON errors carry line, col, and a nonempty source path.
  - Multi-file check reports per-file line/col (import lines kept as blanks).
  - `oodac check --json-errors` is in-process. No capture `.sh`. Product CLI also converts.
  - Named emit+gcc+run set: `scripts/ci_open_run.oo` (hello, int_main, open_ci_add).
  - Named emit-llvm+clang+run lock: `scripts/open30_llvm_lock_smoke.oo` (match `ok`, two-module `2`).
  - QA master is pure `.oo`. From `qa/`: `../ooda/bin/ooda run ./qa_master.oo` prints `qa_master: PASSED` (22 green). Top-level `qa/*.sh` runners are gone. Residual: broken fixtures in the `qa_master.oo` header and `e2e/*.sh`.
  - Two `emit-c` of `fixtures/hello.oo` are byte-identical. Named multi-backend lock: emit-c (hello/int_main/open_ci_add), emit-llvm, and emit-wasm are stable across two runs (`scripts/bit_identical_emit_lock.oo`). gcc binaries are not claimed bit-identical without strip+repro flags (OPEN-71).
  - `import_c` / header path and `lto_xlang_link` (gcc `-flto`) are product; emit may write `#include`. Not residual refuse.
  - Check-time SMT folds const `requires` / `ensures` / `spec` in fn headers. Full general symbolic SMT is residual.
  - `TraitDecl` and monomorphization are product.
  - `bare_metal_init` is product (hosted canary after zeroing a static page).
  - Official rebuild does not export `PURE_SKIP_CHECK`. That flag is opt-in only.
  - Proved 2026-08-17 on `./bin/ooda` and `./oodac/oodac` rebuilt from tree sources (two-stage `oodac_pure_build`, 192 modules, 637 fns). Seed refreshed from tree `oodac/oodac` (not the reverse). `bootstrap/seed/oodac emit-c fixtures/seed_later_callee.oo` exits 0 and writes `later()`. Seed emit of `c_emit_skip.oo` writes `c_skip_amp_mut`. Product compiler remains tree `oodac/oodac`. SHA-256 sidecar rewritten; minisig residual (no secret key in tree).
  - ROADMAP Residual names host-lint leftovers gone: no `ooda/fixtures/pm_launch_tamper.py`; `audit/library_status*.oot` each <= 256. Stay-residual facts (HITL, vscode `ooda-lsp.js`, `qa/e2e/*.sh`, AArch64, device GPU, SGX/TDX/SEV, full SMT, dummy WASM `end`, browser WASM host) stay residual, not this-cycle `[ ]` work. Lock: `scripts/open51_hygiene_smoke.oo`. C emit + gcc remains the product path. ROADMAP Open holds honesty rows. Compiler leftovers and library leftovers stay on that list for other agents.

## 3. Residual (Wanted in NORTHSTAR / ROADMAP, Not Built)

  - Standard library 4,539 non-residual modules certified GREEN (0 blocked); 60 residual cryptographic and raw-pointer hardware modules remain residual per `audit/library_status.oot`.
  - Full gcc-parity LLVM oodac (every command, C path retired) is residual. Side linked hello stamp-walk is product.
  - AArch64 product-ELF dispatch is residual. Side hello/bind/concat/int/mix/greet emit+assemble is product. Link and qemu run stay residual.
  - Extra emit-wasm writes `\0asm`. Print-lit, hello greet fold, same-fn ident-let, N-string const strcat, two-println, three-println, int-lit println, const INT+INT, if-true/if-false, if-true-no-else, if INT==INT, if INT!=INT, if-false-no-else, four-println, five-println, six-println, seven-println, eight-println, nine-println, ten-println, eleven-println, twelve-println, thirteen-println, fourteen-println, fifteen-println, sixteen-println, seventeen-println, const else-if, INT+INT+INT, INT-INT, INT*INT, INT/INT, INT%INT, unary-minus println, bool true/false println, if INT<INT, if INT>INT, if INT<=INT, if INT>=INT, if !true/!false, if BOOL&&BOOL, if BOOL||BOOL, if (true), if (INT==INT), if (INT!=INT), if (INT<INT), if (INT>INT), if (INT<=INT), if (INT>=INT), if INT==INT no-else, if INT!=INT no-else, if INT<INT no-else, if INT>INT no-else, if INT<=INT no-else, if INT>=INT no-else, if !false no-else, if BOOL&&BOOL no-else, if BOOL||BOOL no-else, if (true) no-else, if (INT==INT) no-else, if (INT!=INT) no-else, if (INT<INT) no-else, if (INT>INT) no-else, if (INT<=INT) no-else, if (INT>=INT) no-else, if (BOOL&&BOOL) no-else, if (BOOL||BOOL) no-else, if (!false) no-else, nested if true no-else, if INT==INT && INT==INT no-else, if INT==INT || INT==INT no-else, if (INT==INT && INT==INT) no-else, if (INT==INT || INT==INT) no-else, if !(INT==INT) no-else, if ((true)) no-else, if INT!=INT && INT!=INT no-else, if (!(INT==INT)) no-else, if INT!=INT || INT!=INT no-else, if (INT!=INT && INT!=INT) no-else, if INT<INT && INT<INT no-else, and nested if true are named locks. Mixed ident concat stays dummy. Hostile second print stays dummy. IDENT if stays dummy. IDENT add stays dummy. IDENT third/fourth/fifth/sixth/seventh/eighth/ninth/tenth/eleventh/twelfth/thirteenth/fourteenth/fifteenth/sixteenth/seventeenth print, extra stmt beside no-else, if 1==x, if 1!=x, if 1<x, if 1>x, if 1<=x, if 1>=x, if !x, if true&&x, if true||x, if (x), if (1==x), if (1!=x), if (1<x), if (2>x), if (1<=x), if (2>=x), nested IDENT if, no-else IDENT compare, if !x no-else, if true&&x no-else, if true||x no-else, if (x) no-else, if (1==x) no-else, if (1!=x) no-else, if (1<x) no-else, if (2>x) no-else, if (1<=x) no-else, if (2>=x) no-else, if (true&&x) no-else, if (true||x) no-else, if (!x) no-else, nested IDENT no-else, if 1==x && 2==2 no-else, if 1==x || 0==1 no-else, if (1==x && 2==2) no-else, if (1==x || 0==1) no-else, if !(1==x) no-else, if ((x)) no-else, if 1!=x && 3!=4 no-else, if (!(1==x)) no-else, if 1!=x || 3!=3 no-else, if (1!=x && 3!=4) no-else, if 1<x && 3<4 no-else, non-const else-if, IDENT in add3, IDENT sub, IDENT mul, IDENT div, IDENT mod, IDENT unary-minus, and IDENT bool stay dummy. Other fns keep dummy `end`. A browser WASM host is residual. Not a complete WASM backend.
  - Device GPU dispatch is residual. PTX text emit is product; launching on a GPU is not.
  - SGX / TDX / SEV hardware enclaves are residual. Measured isolate is product.
  - Full SMT solver / general symbolic is residual. Const + header `requires` / `ensures` / `spec` fold is product.
  - Leftover list: `ROADMAP.oot` Open. Next C work: import+call gcc, contract DOT, `Result[List[Name]]`, encoding emit-30s, named walk ERR. Stale leftover words (old cold-check seconds and module counts, import_c refuse, empty-disk registry, AES-exits, clang-link-is-next) sit in the closed stale-word log, not as this-cycle leftover. Lock: `scripts/open52_stale_smoke.oo`. Honesty rows, compiler leftovers, and library leftovers sit under Open. C emit + gcc remains the product path.

## 4. Cross References

  - Architecture North Star: `openOODA/NORTHSTAR.oot`
  - Active plan: `openOODA/ROADMAP.oot` (Open is the leftover list)
  - Long-Term Frontiers: `openOODA/MOONSHOTS.oot`
  - Invariant Laws: `openOODA/RULES.oot`
  - Security Matrix: `openOODA/SECURITY.oot`
