SPRINT — this week

File: openOODA/SPRINT.oot
What to do next. DESIGN is the want. SHIPPED is what exists. PM is the pin.

Handoff
  Read RULES.oot. Then SHIPPED.oot and PM.oot.
  Proof is `oodac check oodac/main.oo` or emit + gcc + run.
  Rebuild from ooda/:
    bash bootstrap/oodac_pure_build oodac/main.oo oodac/oodac

Pin
  Release v0.198.0-alpha
  Live check (this machine): OK in about 25 seconds, about 0.32 GiB RAM.
  That is not under 10 seconds. Do not claim 10 seconds.
  Rebuild: bash bootstrap/oodac_pure_build

Open
  None this week. Last week is closed.
  If you add a job, put it here. Keep this list short.

Later (do not start)
  Faster check. Smaller RAM.
  Real .wasm file (today we print .wat text).
  Bigger std, real OS sandbox, moonshots in MOONSHOTS.oot.

Closed (this pin)
  Compiler rebuilds itself. Check of oodac/main.oo finishes OK.
  Run, spawn, secret sinks, LLVM hello on the current host.
  Hash map, JSON, time, HTTP, init/test/doc, error carets, channels.
