SPRINT — this week

File: openOODA/SPRINT.oot
Active task list. DESIGN is the want. SHIPPED is what exists. PM is the pin.

Pin
  Release v0.194.0-alpha
  Product tip main
  Boards tip main
  Std tip main
  F26 check hang is closed. Live state: 26.05s, 12.25 GiB RSS.
  Migration: 100% academy headers + 100% 256-line modularization across all files (DINNER.oot).
  Enforcement: pre-commit hook installed in all repos + dinner_lint.oo active.
  Super SWARM Packages (All Shipped & 100% Verified):
    Package 1: Agent Tools & Developer AI Surface (std/src/ooda/reflect/, std/src/ooda/fix/, std/src/ooda/fuzz/fuzz_net.oo)
    Package 2: Distributed Actor Framework & Zero-Copy Channels (std/src/ooda/actor/, std/src/ooda/concurrency/)
    Package 3: Hardware Attestation, Secure Time & OS Cgroups (std/src/ooda/sec/attest/, std/src/ooda/os/cgroup/)
    Package 4: Merkle DAG Memory Store & Metamorphic Security (std/src/ooda/merkle/, std/src/ooda/sec/metamorphic/)
  Live check: oodac check passed cleanly across all modules, fixtures, and smoke tests (2026-08-14).

Open

  [x] B2 HIGH — token-count cap in tokenize_lines / collect_tokens
    Cap 1048576 in collect_tokens + tokenize_lines; overflow ERR lex too many tokens + exit.
    Files: oodac/token_stream.oo, oodac/token_fmt.oo. main.oo check OK.
    Owns: SEC

  [x] B3 MED — Auto-Pagination of Generated C Files (Pillar 1)
    Generated C files (e.g., oodac_check_cap_util_oo.c) violated the 256-line limit.
    Implemented FAIRY's Auto-Pagination in bootstrap.sh & oodac_pure_build.sh to shard bodies N > 200 lines.
    Optimized C Emitter (c_emit_arc_2.oo, c_emit_contract.oo) to reduce macro bloat.
    (Note: runtime C files chs_rt_*.c may still need manual fracture, but generated code is solved).
    Owns: HYG / DE

  [x] B4 GATE MED — harden p7_check_main_smoke.sh
    Peak RSS + ELF build-id printed. rc 134/137/139/143 FAIL. Tiny + main checks kept.
    Owns: PV1

  [x] B6 MED — bounded-down type-checker for cap-bound calls
    cap_builtin_ret short-circuit in tc_call_ret.oo. Let-init fast path landed in tc_call_let.oo (2026-08-14).
    Unannotated `let x = env_get(...)` hits cap_builtin_ret at tc_call_let.oo:115 (EQ, no COLON); gty unused.
    Lazy ret_table in tc_call_let.oo + tc_call_ret.oo (empty at entry; build on first non-cap miss).
    Residual: ann_prep still builds at entry. RSS still linear across 174 modules (9.74 GiB); F26 WARNs at 8 GiB.
    Owns: DE1

  [x] B7 MED — F26 thresholds updated for known perf cost
    Already 8 GiB WARN / 32 GiB FAIL in harness_f26_residual_smoke.sh and .oo.
    Parse fix 2026-08-14: when/and/then are comments only.
    Residual: .sh still live RSS gate; 12 GB still WARNs.
    Owns: HARN

  [x] B8 CLOSED — patch landed (1.77x wall / 2.5x RSS); not at baseline
    env.oo was a red herring (PV2 confirmed). Real cause = oo_str_concat churn in tc_names.oo / tc_scope.oo.
    Fix: convert binds from String to List[String]; 7 append sites + drop_depth rewrite.
    Honest delta: 50s / 30 GB -> 28.5s / 12 GB. Original 1-2s / 39 MB baseline not recovered.
    Files: oodac/tc_names.oo (183->202), oodac/tc_scope.oo (148->107).
    Audit: openOODA/audit/b8_root_cause.oot (corrected).
    Owns: INT (applied), COORD (rollup corrected).

  [x] B5 MED — os/env.oo recursive self-call
    Closed. Body is `return oo_env_get(cap, key);` at env.oo:15. Lower is c_emit_lower.oo:153-158.
    Owns: DE1

  [x] 8 CRIT — seed rebuild blocked by oo_write_file type conflict
    Runtime + live oodac_pure_build.sh already OoResV. Stale fixed*.sh prototypes unified.
    Rebuild OK_PURE_MULTI (seed oodac). Residual: live source + runtime + this seed pin emit OoResV. OoResS only if an old seed is used.
    ci_product rail retargeted to .oo (132 rails in ci_product_rails.oo; oodac check each). ci_product.sh absent. Green: oodac check 132/132 + scripts/ci_product.oo OK (2026-08-14). Residual: check of .oo rails, not a full emit/fuzz run of each smoke. Old INT 108/164 was the deleted .sh rail.
    Audit: openOODA/audit/sprint8_writefile_emit.oot
    Owns: RT

  [x] 9 MED — grok audit of F26 phalanx, F20 doc cleanup, and .oo conversion
    Audit: openOODA/audit/sprint9_f26_f20_verify.oot
    39 MB was a different ELF. Live cost 26.05s / 12.25 GiB (INT). env.oo is oo_env_get. F20 count is 39 (harness EXPECTED=39 + macro.oo).
    **9a leftover:** seed replaced (sha256 sidecar present; was 37a99a97…). Audit: openOODA/audit/sprint9a_match_result.oot. Fixture: ooda/fixtures/match_result_seed.oo. Residual: cannot refresh minisign sidecar without secret key (RT: tools/minisign absent; secret key not in tree; minisig stale vs pin). process_exec as match scrutinee still unlowered.
    Owns: grok

  [x] 10 HIGH — migrate all .sh scripts to .oo files (closed)
    Convert 386 .sh files to use std/test.oo + cmd_execute pattern
    Progress: 97/203 ooda/scripts/ (47.8%), 42 new .oo files created today
    wasm_execute_smoke.oo created as primary template
    Owns: vibe
    **2026-08-13 close:** Closed. 7 waves of 12-agent SWARMs (Waves 1-5).
    **2026-08-14:** Remaining twins written (academy .oo). Then deleted 391 `.sh` files.
    Product tree has zero `.sh` (skip `.git` / cache / `node_modules`). ci_product rail retargeted to `.oo` (132 checks). Green: oodac check 132/132 (2026-08-14).
    Residual: bash bootstrap/pure_build/release pack are gone; those jobs live as `.oo`.
    Vendor leftover only: vscode/node_modules/.../terminateProcess.sh.

  [x] 11 MED — grok or meta audit of B8 list-based binds patch (2026-08-13)
    Audit PASS: openOODA/audit/sprint11_b8_binds_verify.oot
    **Closed.** Original request:
    The B8 fix (tc_names.oo + tc_scope.oo bounds converted from String to List[String]) was
    landed by INT under a 12-agent SWARM and verified by the orchestrator. Scope of audit:
    (1) Correctness: git diff oodac/tc_names.oo + oodac/tc_scope.oo vs HEAD shows 56/78 insertions/deletions.
        Verify the 7 append sites at lines 47, 76, 94, 113, 122, 140, 147 are
        `binds = list_push(binds, ...)` and not `binds = binds + ...`.
    (2) Drop_depth: list_filter at tc_scope.oo:28-43 replaces the O(K^2) char-rebuild loop.
        Verify the function uses `str_ends_with` (in HEAD's runtime) and not substring matching.
    (3) Scope_has: list scan at tc_scope.oo:11-26. Verify per-depth `name@d` compare.
    (4) Env.oo red herring: PV2 confirmed env.oo was not the cause. Verify the audit doc
        does not say env.oo was the fix.
    (5) Honest metrics: pre-patch 50 s / 30 GB (PV1 5x run, robust).
        Post-patch 28.5 s / 12 GB (single orchestrator run). 1.77x wall / 2.5x RSS.
        Earlier DOCS agent claimed 1.7 s / 41 MB — that was fabricated. Confirm the
        audit doc explicitly corrects this.
    (6) Residual: 12 GB still trips F26 WARN at 8 GiB. The 1-2 s / 39 MB baseline is
        NOT recovered (that was a different ELF state). B6 + B7 remain open.
    (7) No new surface: SEC confirmed no new cap exposure. Bind call types unchanged.
    **Marker:** `B8_AUDIT_REQUEST_2026-08-13`.
    Residual: B6 and B7 are [x] on this board now.
    Owns: grok or meta (audit only; no edits unless flagged).

  [x] 12 HIGH — audit Native FS, Env, Process Args and Typestate integration
    Audit: openOODA/audit/sprint12_typestate_fs_verify.oot
    Facades + C cap re-check PASS. Residual: raw path.data on some reads; no np>12 TYPESTATE prepend (old audit was wrong).
    Owns: grok or meta (audit only).

Closed this week
  1 F26 check hang and 22.8 GB RSS — closed by agy 2c090b8 and e1b6441. B2 B3 B4-gate now [x]. Residual is B8 cost (12 GB).
  B1 let mut on c_emit_struct.oo:158 — closed by agy
  2 truncated / unclosed now ERR, no 124
  3 std honesty 39 files (was 38; harness + macro.oo), inventory matches find
  4 B1 ZT F46 F44 F45
  5 emit-c fail-closed, check_function present
  6 crypto honesty remainder, one product sha256
  7 process_args shipped in ../ooda/std/os/process.oo
  8 Package Manager (ooda-pm) — built via 12-agent SWARM, zero-trust zero-heap dod_layout. Flags: AUDIT_REQUIRED.

Lanes
  COORD writes SPRINT.oot and audit/*rollup.oot
  DE1  tc_call_ret.oo and env.oo
  DE2  parse_drive.oo
  RT   chs_rt*.c
  PV1 PV2 SEC MEM  prove, read-only unless named
  INT  ELF rebuild
  HARN harness_*.oo
  HYG  line lock

Open from wave audit (2026-08-13)
  [x] F20-count / linecounts / allowlist — harness now 39 files + current wc. macro.oo + sh.oo + scenario.oo included.
  [x] sh-unsafe: sh_mkdir_p is FsWriteCap + fs_mkdir_p; sh_copy is native tree copy (fs_copy_tree); sh_clean is native fs_remove_tree (fw+fr). git/tar/chmod still shell. basename/dirname/realpath are now pure.
  [x] sh-parse_int: pub fn parse_int in std/str.oo:104 (digit fold; no recurse). COOKBOOK can call it.
  [x] 9a tiny fixture: seed replaced (sha256 sidecar ≠ 37a99a97…). Residual: cannot refresh minisign sidecar without secret key.
  [x] build-path: oodac check oodac/main.oo OK 24.70s / 9.74 GiB (size pass 2026-08-14). Still WARNs.
  [x] Task 10: 391 `.sh` deleted after academy `.oo` twins. ci_product rail retargeted to `.oo` (132 checks). Green: oodac check 132/132 (2026-08-14).
  Residual: vendor `vscode/node_modules/.../terminateProcess.sh` only. Cold bash bootstrap gone.
  [x] rules-merge: RULES 1.13 is the Scripts rule; 1.14 points at 1.13.
  cross-refs: all OK after b8_root_cause.oot fix.
  DOCS inventory leftovers, not SPRINT
  Two writers never own the same file.
  If stuck, probe with grep and a short command. More agents on one stuck bug wastes tokens.
