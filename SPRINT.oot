# openOODA sprint

Closed work is **out of this file**. History lives in git commits and `audit/*.oot`.
Do not re-open M176–M178, ZT-1..ZT-6, path-A security/CT ships, or pure dual-green at `ffb9a03`.
Do not re-open mono next-5 (`crypto/sha256`, `sec/crypto/blake3`, `auto/occupancy`, `aero/tvc`, `sec/sys/ima`) — all M172 shims ≤120.

## Pin

| Item | Value |
|------|--------|
| Release | `v0.184.0-alpha` (stay alpha) |
| Tip (`ooda`) | `89d6e9b` — dual-green **path-A PASS** (seed_pure_multi + bitwise_ops_smoke green after BYTE_STR.md + c_emit_ops split; see `audit/tip_pure_prove_89d6e9b.oot`) |
| Focus | Residual monofiles (live **8** >600); std commit batches; product depth residual |
| Beta | **Not claimed** |

## Open queue (power law ≤5 this turn)

1. **Monofile residuals** — 8 files still >600. Next batch ≤5: `sec/sys/cfi`, `sec/sys/audit`, `sec/crypto/chacha20`, `sec/crypto/poly1305`, `physics/rigid`. Pattern: M172 shim + parts ≤256. (Prior next-5 all shims ≤120 — closed.)
2. **std commit / honesty** — triage: `audit/std_triage_2026-08-11.oot`. Land BATCH A–C; 884-stub honesty not closed.
3. **Product depth residuals** — full C-import codegen, unrestricted dlopen, full `new`/`malloc` / OS rlimit heap. (dlsym/dlclose path-A = floor.)
4. **Merge hygiene residual** — continue landing dirty trees; never commit `.keys-local/`.

## Live monofiles (>600 lines)

*Regenerate: `find std/src/ooda -name '*.oo' | while read f; do n=$(wc -l <"$f"); [ "$n" -gt 600 ] && echo $n $f; done | sort -rn`*  
*Tree: `/home/jeryd/Projects/openOODA/std` (live count **8** as of 2026-08-11 close)*

| Lines | Path |
|------:|------|
| 627 | `std/src/ooda/sec/sys/cfi.oo` |
| 625 | `std/src/ooda/sec/sys/audit.oo` |
| 617 | `std/src/ooda/sec/crypto/chacha20.oo` |
| 614 | `std/src/ooda/sec/crypto/poly1305.oo` |
| 614 | `std/src/ooda/physics/rigid.oo` |
| 614 | `std/src/ooda/aero/propulsion.oo` |
| 613 | `std/src/ooda/auto/lanelet.oo` |
| 610 | `std/src/ooda/bio/protein.oo` |

## Named residual (not this queue — do not fake-close)

Full SMT · free-form multi-code · interactive HITL harness · actor supervision · real GPU · native `&str` · nested `List[Struct]` · product `&mut T` · object-cap crypto · SOCK_RAW · full async I/O · pkg/LSP/registry · beta.

## Path-A floor (assumed; not open work)

Runtime caps, FFI allowlist + realpath/O_NOFOLLOW dlopen, **registered-handle dlsym/dlclose** (`ooda/runtime/chs_rt_ffi.c`), TLS host verify when OpenSSL linked, concurrency publish-order, free_bytes clamp, seed SHA+minisign (`SEED_PUBKEY_FP=645069A34E6058B7`), CT import reject, `--hitl-allowed`, secret sinks path-A, cap type seal F02. Re-open only if a prove fails.
