# openOODA sprint

Closed work is **out of this file**. History lives in git commits and `audit/*.oot`.
Do not re-open M176–M178, ZT-1..ZT-6, path-A security/CT ships, pure dual-green (seed_pure_multi + bitwise_ops_smoke), monofile >600 queue, mono next-5, or monofile residual batch (cfi/audit/chacha20/poly1305/rigid/propulsion/lanelet/protein — all M172 shims).
Do not re-open path-A product depth floor (chs_rt_alloc raise cap + free_bytes clamp, load_import relative-only, registered-handle dlsym/dlclose).

## Pin

| Item | Value |
|------|--------|
| Release | `v0.184.0-alpha` (stay alpha) |
| Tip (`ooda`) | `206af08` — path-A product depth (alloc + load_import); dual-green path-A prove on `89d6e9b` + bitwise residual closed at `cf5b121` (see `audit/tip_pure_prove_89d6e9b.oot`, `audit/product_depth_path_a.oot`) |
| Focus | Residual DESIGN long-horizon; std honesty surface; product depth moonshots |
| Beta | **Not claimed** |

## Open

- residual named (DESIGN long-horizon): full SMT · free-form multi-code · interactive HITL harness · actor supervision · real GPU · native `&str` · nested `List[Struct]` · product `&mut T` · object-cap crypto · SOCK_RAW · full async I/O · pkg/LSP/registry · beta
- std honesty residual: untracked surface still large (~617 untracked `.oo`, ~215 modified; BATCH A–C monofile/hash/README landed — see `audit/std_batches_landed.oot`; 884-stub honesty not closed)
- product depth residual: full import-C / unrestricted dlopen / OS rlimit (path-A floor shipped — not these)

## Path-A floor

Assumed done: runtime caps; FFI allowlist + realpath/O_NOFOLLOW dlopen; registered-handle dlsym/dlclose (`chs_rt_ffi.c`); TLS host verify when OpenSSL linked; concurrency publish-order; free_bytes clamp + per-raise 1GiB alloc ceiling (`chs_rt_alloc.c` @ `206af08`); seed SHA+minisign; CT import reject; `--hitl-allowed`; secret sinks path-A; cap type seal F02; load_import relative-only probes + bad-name reject (no abs `/home`); monofile count under `std/src/ooda` **>600 = 0**. Re-open only if a prove fails.
