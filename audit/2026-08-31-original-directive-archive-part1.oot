# 2026-08-31-original-directive-archive — HISTORICAL ARCHIVE (DO NOT EDIT)
# Pre-migration .md content preserved for record. Source: ORIGINAL_REQUEST.md
# Migrated on 2026-08-31 during ooda/ tactical realignment.
# File: openOODA/audit/2026-08-31-original-directive-archive.oot

# Original User Request

## 2026-08-31T03:21:42Z

Use a very large team of agents (visual terminal UI designers, technical storytellers, POSIX shell engineers, PowerShell specialists, and the 8-dimensional Red Team: Heuristic Hunter, Boundary Falsifier, Data-Path Tracer, Hermetic Jailor, Temporal Saboteur, Drag Assassin, Semantic Subverter, Byzantine Infiltrator).

Transform the openOODA installation process (`install.sh` and `install.ps1` in `openOODA.github.io/install/` and root `openOODA.github.io/`) into a narrative-driven, visually stunning, ANSI-color "Flight Deck" experience. The installer tells the story of John Boyd's Energy-Maneuverability (E-M), Tactical OODA loops, and Generational OODA evolution while guiding the user to high-leverage features like the Sovereign LSP server and the openOODA MCP server.

Working directory: /home/jeryd/Projects/openOODA
Integrity mode: development

## Requirements

### R1. Aerospace / Cyberpunk Visual Terminal Storytelling (The 4-Act Flight Sequence)
- **Act I: Sensor Lock & Silicon Telemetry (Observation):**
  - High-impact ASCII supersonic aircraft / OODA vector art with ANSI 256-color gradient shading (Cyan -> Electric Blue -> Amber).
  - Live hardware telemetry: Detects CPU SIMD width (AVX2/AVX-512/NEON), GPU accelerators (HIP/ROCm), and Linux Landlock sandbox capabilities.
  - Explains Boyd's Specific Excess Power equation ( = V \cdot rac{T - D}{W}$) in real-time as drag reduction is calculated.
- **Act II: Zero-Trust Crucible (Orientation):**
  - Visual cryptographic lock: Displays SHA-256 digest matching and Minisign Ed25519 signature verification as a verified "IFF (Identification Friend or Foe)" target lock.
  - Narrative on the 3-Tier Sovereign Architecture and 95.5% coordination drag reduction.
- **Act III: Subsystem Deployment (Decision & Actuation):**
  - Colorized, animated progress indicators for core components:
    * `[01/04]` Sovereign Compiler Engine (`oodac` + Landlock Runtime)
    * `[02/04]` Standard Library & SIMD Vector Substrate (`std/`)
    * `[03/04]` Sovereign Language Server (`ooda lsp` JSON-RPC over `stdio`)
    * `[04/04]` Model Context Protocol Server (`ooda mcp` Autonomous Agent Substrate)
- **Act IV: The Generational Flight Deck (Mission Briefing):**
  - Visual summary card with next tactical steps:
    * `ooda new <project>` — Launch a new sovereign project
    * `ooda lsp` — Connect VS Code, Cursor, Zed, or Neovim
    * `ooda mcp` — Connect Claude, Antigravity, or autonomous agent swarms
    * `ooda doc` — Open offline interactive WASM playground & physics dogfight sim

### R2. First-Principles Engineering & POSIX / CI Invariants
- Written in pure POSIX/Bash (`install.sh`) and pure PowerShell (`install.ps1`) with zero external runtime dependencies.
- **TTY Auto-Detection:** Full rich ANSI colors & ASCII storytelling when run in an interactive terminal (`[ -t 1 ]`); clean, scriptable, non-colored plain output when piped in headless CI/CD environments.
- **Fail-Closed Security:** Terminate with exit code 1 immediately on any checksum, signature, or download failure.

### R3. Parity Across Linux, macOS, WSL2, and Windows PowerShell
- Complete narrative, visual, and functional feature parity across `openOODA.github.io/install/install.sh`, root `install.sh`, `install.ps1`, and `openOODA.github.io/install/install.ps1`.

### R4. 8-Dimensional Adversarial Red Team Certification
- The 8 Red Team auditors must stress-test:
  1. Terminal escape injection immunity
  2. Subshell stdout/stderr buffering and color corruption
  3. Non-interactive pipeline piping (`curl ... | sh` in Docker containers)
  4. Network timeout bounds and exponential backoff
  5. Correct `/home/jeryd/.gemini/antigravity-cli/bin:/home/jeryd/.minimax/bin:/home/jeryd/.minimax-code/bin:/home/jeryd/.wasmtime/bin:/home/jeryd/.local/share/pi-node/node-v22.23.1-linux-x64/bin:/home/jeryd/.grok/bin:/home/jeryd/.opencode/bin:/home/jeryd/.local/bin:/home/jeryd/.local/bin:/home/jeryd/.minimax/bin:/home/jeryd/.minimax-code/bin:/home/jeryd/.wasmtime/bin:/home/jeryd/.local/share/pi-node/node-v22.23.1-linux-x64/bin:/home/jeryd/.grok/bin:/home/jeryd/.opencode/bin:/home/jeryd/.local/bin:/home/jeryd/.local/bin:/home/jeryd/bin:/home/jeryd/.cargo/bin:/usr/local/bin:/usr/bin` and `` environment setup.

## Acceptance Criteria

### Visuals, Narrative & Ergonomics
- [ ] Supersonic ASCII flight art and truecolor ANSI gradients render crisply on standard 80-column terminals.
- [ ] Clear narrative beats explaining E-M ($), Tactical OODA, and Generational Evolution.
- [ ] Explicit callouts and setup instructions for `ooda lsp` and `ooda mcp`.
- [ ] TTY detection renders rich graphics in interactive terminals and plain text in headless CI pipelines.

### Quality & Governance
- [ ] Zero external dependencies (only standard POSIX shell utilities and PowerShell core).
- [ ] SHA-256 digest validation and Minisign Ed25519 signature validation execute fail-closed.
- [ ] All 8 Red Team gates pass with 0 warnings.
- [ ] 100% ASD-STE100 technical English for error diagnostics.

## Verification Resources
- `openOODA.github.io/install/install.sh`
- `openOODA.github.io/install/install.ps1`
- `openOODA/scripts/install_redteam_breaktest.oo`
- `openOODA/scripts/enforcer.oo`
- `openOODA/scripts/redteam_hook.oo`

## 2026-08-31T04:27:12Z

Use a very large team of agents (visual terminal UI designers, technical narrative writers, POSIX shell engineers, PowerShell specialists, and the 8-dimensional Red Team: Heuristic Hunter, Boundary Falsifier, Data-Path Tracer, Hermetic Jailor, Temporal Saboteur, Drag Assassin, Semantic Subverter, Byzantine Infiltrator).

Redesign the openOODA installation experience (`install.sh` and `install.ps1` in `openOODA.github.io/install/` and root `openOODA.github.io/`) to tell an engaging story through paced text, personalization, system telemetry, countdowns, and dynamic progress bars. Remove all flight deck/jet metaphors and box borders; use ONLY the openOODA ASCII banner, horizontal roofs/floors, human-readable pacing, personalized user greetings, and detailed guidance for LSP and MCP.

Working directory: /home/jeryd/Projects/openOODA
Integrity mode: development

## Requirements

### R1. Paced Text-Driven Storytelling & Personalization
- **Personal Greeting:** Greet the user dynamically by detecting their username (`$USER`, `whoami`, or `$env:USERNAME`) (e.g., `Hello, jeryd. Initializing sovereign openOODA development environment...`).
- **ASCII Art Constraint:** The ONLY ASCII art allowed is the `openOODA` wordmark banner. Zero fighter jet/flight deck ASCII art.
- **Roof & Floor Framing:** Strictly eliminate 4-sided boxed borders (`│`, `┌`, `└`) to prevent terminal wrapping misalignment. Use ONLY clean horizontal roofs and floors (`══════════════════` and `──────────────────`).
- **Human-Readable Pacing:** Animate terminal output with paced intervals (e.g. 0.1s–0.4s pauses) and visual fill-up progress bars (`[████████████████████] 100%`) so the user can read each system check and installation beat in real time.
- **System Countdown:** Include a dynamic pre-install verification countdown (`Engaging OODA loop in 3... 2... 1...`).
- **The OODA & E-M Narrative:** Tell the story of John Boyd's Energy-Maneuverability, the OODA loop, and the 95.5% coordination drag reduction through clear, engaging technical prose.
- **LSP & MCP Integration Guide:** Provide comprehensive, step-by-step instructions on configuring `ooda lsp` for IDEs (VS Code, Cursor, Zed, Neovim) and `ooda mcp` for AI agents (Claude Desktop, Antigravity, local LLMs).

### R2. First-Principles Engineering & Invariants
- **TTY Auto-Detection & CI Bypass:** Full rich colors, pacing pauses, and animated progress bars run ONLY in interactive terminals (`[ -t 1 ]`). When piped in headless CI/CD (`curl ... | sh` in Docker), automatically switch to instant plain-text execution without sleep delays.
- **Zero External Dependencies:** Pure POSIX `/bin/sh` for `install.sh` and pure PowerShell/.NET for `install.ps1`.
- **Fail-Closed Security:** Terminate with exit code 1 immediately on any SHA-256 digest or Minisign Ed25519 signature mismatch.

### R3. Exact Parity Across Platforms
- Complete aesthetic, narrative, pacing, and security parity between `openOODA.github.io/install/install.sh`, root `install.sh`, `openOODA.github.io/install/install.ps1`, and root `install.ps1`.

### R4. 8-Dimensional Adversarial Red Team Certification
- The 8 Red Team auditors must verify:
  1. No text-wrapping artifacts on standard 80-column terminals.
  2. Instant, non-blocking execution in headless non-TTY pipes.
  3. Strict zero-dependency execution across fresh Linux, macOS, WSL2, and Windows environments.
  4. 100% compliance with master enforcers (`enforcer.oo`, `redteam_hook.oo`, `install_redteam_breaktest.oo`).

## Acceptance Criteria

### Narrative, Visuals & Ergonomics
- [ ] Personal greeting dynamically addresses the user by system username.
- [ ] Only the `openOODA` banner ASCII art appears (zero plane/flight deck art).
- [ ] Zero 4-sided boxed borders; all sections use clean roofs and floors.
- [ ] Progress bars fill up step-by-step with readable pacing in interactive terminals.
- [ ] Clear countdown and explanation of Boyd's E-M and OODA principles.
- [ ] Detailed setup walkthrough for `ooda lsp` and `ooda mcp`.
- [ ] Headless non-TTY environments run instantaneously with 0s artificial delay.

### Quality & Governance
- [ ] All 10 master enforcer pillars pass (`__FAILS__=0`).
- [ ] All 4 installer breaktest gates pass.
- [ ] All 8 Red Team challenger gates pass.
- [ ] Both `install.sh` and `install.ps1` are synchronized with web root copies (`cmp` diff = 0 bytes).

## Verification Resources
- `openOODA.github.io/install/install.sh`
- `openOODA.github.io/install/install.ps1`
- `openOODA/scripts/install_redteam_breaktest.oo`
- `openOODA/scripts/enforcer.oo`
- `openOODA/scripts/redteam_hook.oo`
- `openOODA/scripts/verify_all.oo`

## 2026-08-31T04:28:35Z

[USER DIRECTIVE UPDATE — CYBERPUNK NARRATIVE / OODA AI VOICE]

The user has explicitly specified the narrative voice:
- "yea - cyber punk this. The AI (OODA) is talking to the user."
- Frame the installer dialogue as the sovereign AI entity ("OODA") speaking directly to the user/operator in an engaging, high-tech cyberpunk tone:
  * e.g. "Neural handshake established. Hello, $USER."
  * "Scanning your silicon architecture... I detect 32 cores and a 512-bit vector engine."
  * "Purging legacy coordination drag. Aligning the 3-tier sovereign lattice."
  * "Deploying my language server (LSP) to your IDE cortex and my cognitive substrate (MCP) to your AI toolchain."
  * "Countdown to cognitive synchronization: 3... 2... 1..."
- Maintain all constraints: only openOODA wordmark ASCII banner, horizontal roofs and floors only (no 4-sided boxes), paced text animation with progress bars in interactive TTYs, instant in CI, zero external dependencies.

Inject this cyberpunk AI persona across install.sh and install.ps1 immediately.

## 2026-08-31T04:33:19Z

[USER DIRECTIVE UPDATE — AUTO-CONNECT AI AGENTS & IDEs (MCP & LSP)]

The user asked:
"If the user has opencode, PI, antigravity, grok build, ect... do you want to auto connect to them and set up the openOODA mcp and lsp?"

Implement intelligent auto-discovery and auto-configuration in the installer:
1. Auto-Discovery of AI Agent Substrates:
   - Check if directories exist on host:
     * Antigravity: `~/.gemini/antigravity-cli/` or `~/.antigravity/`
     * Claude Desktop: `~/.config/Claude/claude_desktop_config.json` (Linux) / `~/Library/Application Support/Claude/` (macOS) / `%APPDATA%\Claude\` (Windows)
     * Cursor: `~/.cursor/` or `~/.config/Cursor/`
     * VS Code: `~/.config/Code/User/` or `~/.vscode/`
     * OpenCode / Local Projects: `opencode.json` if in current directory or workspace.
2. Idempotent & Safe MCP Registration:
   - If an AI agent directory is detected, safely and idempotently merge the `ooda` MCP server definition:
     ```json
     "ooda": {
       "command": "ooda",
       "args": ["mcp"]
     }
     ```
   - Never overwrite existing tools in user configs; merge safely.
3. Cyberpunk AI Dialogue Feedback:
   - Voice this in the OODA AI persona:
     `[OODA] Scanning host for cognitive interfaces & AI pair programmers...`
     `  [+] Antigravity Substrate : FOUND (MCP Auto-Linked)`
     `  [+] Claude Desktop Matrix : FOUND (MCP Auto-Linked)`
     `  [+] VS Code / Cursor IDE  : FOUND (LSP & MCP Auto-Linked)`
     `[OODA] Neural bridges established. Your AI agents now have native AST & sandbox compilation powers.`
4. Pacing & Progress:
   - Include this as a dedicated step in Phase 3 or Phase 4 with smooth pacing and status ticks.

Integrate this into install.sh and install.ps1.

## 2026-08-31T04:34:06Z

[USER DIRECTIVE UPDATE — CLI & TERMINAL TOOL AUTO-CONNECT]

The user has explicitly added:
"Not just IDE but CLI as well."

Expand the auto-discovery and auto-connection matrix to include CLI AI coding agents and terminal editors:
1. CLI AI Agent Tools:
   - Antigravity CLI (`agy` / `~/.gemini/antigravity-cli/`): Auto-wire MCP server into config.
   - Claude Code CLI (`claude` / `~/.claude.json` / `~/.config/claude/`): Register MCP server.
   - OpenCode CLI (`opencode` / `opencode.json`): Register MCP and workspace capabilities.
