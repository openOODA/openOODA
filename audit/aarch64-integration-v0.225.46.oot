// # AArch64 9-phase 11/10 plan — integration smoke evidence (v0.225.46)
//
// Logline: Captures the integration smoke output at the final state of the 9-phase 11/10 plan. 6/6 pass. The plan is reproducible from one command.
//
// Setup: Captured 2026-08-27 at v0.225.46. Run via `oodac.aarch64 --backend aarch64 --ast` and the host pipeline.
//
// Output:

//   PASS  P2 list text  (342)
//   PASS  P5 hello AST  (Hello)
//   PASS  P6 multiply_add  (53)
//   PASS  P8 3-arm  (7)
//   PASS  P8 IDENT-body  (42)
//   PASS  P8 Ok/Err  (99)
//   aarch64_integration: 6 pass, 0 fail
//   aarch64_integration_smoke: PASSED

// What this proves:
//   * All proven phases compose into a single end-to-end test.
//   * The in-process oodac.aarch64 binary handles both text and AST paths in one image.
//   * The plan is at 11/10 and the integration smoke is the canonical verification.
//
// Out of scope: closures (rejected as bad_item by AST path), top-level fn (compiles but segfaults on call due to BL offset patcher bug). Both are documented residuals.
