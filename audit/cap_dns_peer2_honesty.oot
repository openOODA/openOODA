# cap_dns_peer2_honesty.oot
# QA Peer 2 — DNS residual honesty
# Date: 2026-08-12T14:18Z
# Role: QA Peer Verifier 2 (DNS residual honesty)
# Tree: /home/jeryd/Projects/openOODA/ooda/std/src/net/dns.oo
# OWN: prove non-localhost still Err residual; localhost Ok; no greenwash "real DNS".
#      Write this audit only. No product/std edits. No push.
# Mandate: residual path-A honesty after CAP-G6 UdpCap migrate (Integration 2).

========================================================================
SUMMARY
========================================================================

| # | Claim | Expected | Actual | VERDICT |
|---|--------|----------|--------|---------|
| 1 | **localhost → Ok** | exact `"localhost"` returns `Ok("127.0.0.1")` | pure body + verify `assert_eq!(ok_res, Ok("127.0.0.1"))` | **PASS** |
| 2 | **non-localhost → Err residual** | any other hostname returns residual Err string | pure body single residual `return Err(...)`; verify `openooda.org` + branch matrix | **PASS** |
| 3 | **no greenwash "real DNS"** | no wire resolver; cap formal ≠ product DNS | no socket/getaddrinfo/bind_udp in dns.oo; UdpCap unused in body; CAPS residual wording; header honesty | **PASS** |
| 4 | Preferred formal still UdpCap | `resolve_ipv4(udp: &UdpCap, …)` | reflect `"type":"&UdpCap"` | **PASS** |
| 5 | Product `oodac check` dns.oo | OK rc=0 (dual) | **OK** ×2 | **PASS** |
| 6 | `cap_dns_udp_smoke.oo` | exit 0 (dual) | **PASSED** ×2 | **PASS** |
| 7 | Push | none | none | **PASS** |

**OVERALL: PASS** — DNS residual is honest: localhost pure-string Ok; non-localhost residual Err; UdpCap preferred formal does **not** imply a live resolver.
**Do not claim:** product UDP socket DNS, dig path, getaddrinfo facade, or full std NetCap purge. No peer code fix required. No push.

========================================================================
METHOD
========================================================================

1. Static read of live WT `ooda/std/src/net/dns.oo`.
2. Control-flow / branch matrix: exact `"localhost"` only Ok path; single residual Err.
3. Anti-greenwash scan: no wire DNS APIs in dns.oo; runtime getaddrinfo only on tcp/tls floors.
4. Product host: `oodac check` dual-repro + `oodac reflect` signature.
5. Smoke dual-repro: `scripts/cap_dns_udp_smoke.oo`.
6. Cross-doc honesty: CAPS.oot §CAP-G6 / UdpCap rows name residual, not product DNS.

========================================================================
HOST / TIP / WT CONTEXT
========================================================================

| Item | Value |
|------|--------|
| When (UTC) | 2026-08-12T14:18Z |
| ooda tip | `2a0d10f` — `feat(caps): ProcessCap product facades for process/async` |
| Product host | `ooda/oodac/oodac` **962680** B @ 2026-08-12 06:41:59 -0700 |
| Host md5 | `7e1ce7df9bd366b607f35724e0efe80b` |
| Surface | `ooda/std/src/net/dns.oo` **30** lines / **1145** B |
| Peer OWN | audit only |

========================================================================
SURFACE (live source — residual contract)
========================================================================

```
fn resolve_ipv4_pure(hostname: String) -> Result[String, String]
    requires hostname != ""
{
    if hostname == "localhost" {
        return Ok("127.0.0.1");
    }
    return Err("Residual: UDP socket DNS not implemented");
}

fn resolve_ipv4(udp: &UdpCap, hostname: String) -> Result[String, String]
    requires hostname != ""
{
    return resolve_ipv4_pure(hostname);   // cap formal unused for wire
}
```

========================================================================
1) CLAIM — localhost Ok
========================================================================

- Body branch: `if hostname == "localhost" { return Ok("127.0.0.1"); }`
- Return Ok count in pure: **1**.
- Verify: `assert_eq!(resolve_ipv4_pure("localhost"), Ok("127.0.0.1"))`.
- Exact match only: `"LOCALHOST"` and `"localhost."` fall to residual Err.
**VERDICT: PASS** — localhost pure-string Ok is the only success path.

========================================================================
2) CLAIM — non-localhost still Err residual
========================================================================

- Residual string: `Err("Residual: UDP socket DNS not implemented")`.
- `openooda.org`, `example.com`, `google.com`, `127.0.0.1` all fall through to Err.
- Wire / sealed call sites: 0.
- `resolve_ipv4(&UdpCap, host)` delegates directly to pure helper without wire ops.
**VERDICT: PASS** — non-localhost remains residual Err.

========================================================================
3) CLAIM — no greenwash "real DNS"
========================================================================

- `dns.oo` has no `getaddrinfo`, `socket`, `bind_udp`, or `udp_recv` calls.
- No synthetic A-record generation.
- No Cap(0) forge in verify.
- `CAPS.oot` explicitly notes: "Real UDP socket DNS not implemented (localhost hardcode + Err residual) — cap preferred ≠ resolver product."
- Runtime `getaddrinfo` is isolated to tcp/tls floors (`chs_rt_netfloor.c`, `chs_rt_tls.c`), not this facade.
**VERDICT: PASS** — no greenwash "real DNS"; residual named and fail-closed for non-localhost.

========================================================================
4) LIVE HOST PROVE
========================================================================

- `oodac check std/src/net/dns.oo` → **OK** rc=0 (dual-repro).
- `cap_dns_udp_smoke.oo`:
  - Static resolve_ipv4 uses `&UdpCap` OK.
  - Residual honesty strings present OK.
  - `cap_dns_udp_smoke`: **PASSED** (exit 0 ×2).
- Soft-lock: `ooda/std/src/net/dns.oo` is 30 lines (≤256 monofile).

========================================================================
5) RESIDUAL INDEX (named — not peer fail)
========================================================================

| Residual | Status |
|----------|--------|
| Real UDP socket DNS (bind/send/recv under UdpCap) | **unimplemented** — Err residual |
| Product dig / getaddrinfo facade via resolve_ipv4 | **absent** (by design residual) |
| Case-insensitive / FQDN / IPv6 / CNAME / multi-A | **absent** |
| NetCap supersede of UdpCap (CAP-G1 one-wave) | still soft OK for legacy callers |

========================================================================
VERDICT & CLOSE
========================================================================

| Criterion | Result |
|-----------|--------|
| localhost Ok proved (body + verify + matrix) | **PASS** |
| non-localhost Err residual proved (body + verify) | **PASS** |
| no greenwash "real DNS" (surface + CAPS + no wire) | **PASS** |
| UdpCap formal retained; cap unused for wire | **PASS** |
| product check + dns smoke dual green | **PASS** |
| No push | **PASS** |

**QA Peer 2 (DNS residual honesty): PASS**
- **localhost Ok** — exact string hardcode only.
- **non-localhost still Err residual** — single residual string; no alternate Ok.
- **no greenwash "real DNS"** — preferred UdpCap is API intent; pure residual body; docs refuse product claim.
No product/std edits. **No push.**

end of cap_dns_peer2_honesty.oot
