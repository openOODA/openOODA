# cap_dns_peer3_regression.oot
# Peer Verifier 3 (QA) — Floor Regression Gate (CAP-G1..G6 + ProcessCap facades)
# Date: 2026-08-12T14:16:19Z
# Role: QA Peer 3 — Floor regression
# Tree: /home/jeryd/Projects/openOODA/ooda
# OWN: write this audit only; no product feature work; no push
# Scope: re-prove CAP-G1 through CAP-G6 + cap_process_facade smokes must-stay-green
#        on tip product host (path-A floors for net/fs/grant-inject/ProcessCap/
#        seccomp honesty / std net+fs facade migration / ProcessCap product facades)

========================================================================
SUMMARY
========================================================================

| Gate | Command | Result |
|------|---------|--------|
| CAP-G1 net granular | `OODAC_BIN=./oodac/oodac bash scripts/cap_g1_net_granular_smoke.sh` | **PASS** exit 0 |
| CAP-G2 fs granular | `OODAC_BIN=./oodac/oodac bash scripts/cap_g2_fs_granular_smoke.sh` | **PASS** exit 0 |
| CAP-G3 grant inject | `OODAC_BIN=./oodac/oodac bash scripts/cap_g3_grant_inject_smoke.sh` | **PASS** exit 0 |
| CAP-G4 ProcessCap | `OODAC_BIN=./oodac/oodac bash scripts/cap_g4_process_smoke.sh` | **PASS** exit 0 |
| CAP-G5 seccomp honesty | `OODAC_BIN=./oodac/oodac bash scripts/cap_g5_seccomp_honesty_smoke.sh` | **PASS** exit 0 |
| CAP-G6 std migrate | `OODAC_BIN=./oodac/oodac bash scripts/cap_g6_std_migrate_smoke.sh` | **PASS** exit 0 |
| ProcessCap product facades | `OODAC_BIN=./oodac/oodac bash scripts/cap_process_facade_smoke.sh` | **PASS** exit 0 |

**OVERALL: PASS**

All seven assigned floor smokes exit 0 on tip product host. No assigned
script soft-skipped. CAP-G1–G5 enforce maps + CAP-G6 path-A net/fs facade
wave + ProcessCap product facades remain green under current tip.

========================================================================
HOST / TIP / FLOOR CONTEXT
========================================================================

| Item | Value |
|------|--------|
| Cwd | `/home/jeryd/Projects/openOODA/ooda` |
| Tip (`git rev-parse --short HEAD`) | `2a0d10f` |
| Tip subject | `feat(caps): ProcessCap product facades for process/async` |
| Full tip | `2a0d10fd3e3eaf91d2047b72b6f11a2752c076b5` |
| Prior floors | CAP-G1–G5 FIXED; CAP-G6 net+fs FIXED; ProcessCap product facades FIXED this tip lineage |
| Gate product host | `oodac/oodac` **962680** B @ 2026-08-12 06:41:59 -0700 |
| Gate host md5 | `7e1ce7df9bd366b607f35724e0efe80b` |
| Gate host BuildID | `78d775e7817c08f55e7376aa7f0358d15e27da98` |
| Bak (gitignored — T4 OK) | 1 file: `oodac/oodac.bak.pre_cap_g4` |
| Branch (ooda) | `main...origin/main` **ahead 0** (no push this peer) |
| openOODA tip | `a5643ea` `docs(caps): FIXED ProcessCap product facades residual` |
| openOODA branch | `main...origin/main` **ahead 0** |
| Mandate | cap_g1–cap_g6 + cap_process_facade smokes must stay green; audit only; no product fix; no push |

### WT observed at peer run (context only; not edited)

ooda: large deleted-doc porcelain + concurrent dirt on tip (branch still
ahead 0 of origin). openOODA: tracked doc/audit dirt from concurrent lanes.
This peer **did not** edit product, runtime, scripts, std, or hygiene —
audit only. Floors re-proved against live gate binary `oodac/oodac`.

### Residual surface (observed, not owned)

Per CAPS/SPRINT / prior rollups (context only):
- CAP-G1–G5 path-A enforce maps FIXED; this peer re-proves smokes only.
- CAP-G6 FIXED net+fs product facade wave; SysCap bulk residual still named
  (~571 under `std/src` — honesty, not a gate fail).
- ProcessCap product facades FIXED on tip (`process.oo` spawn/exec/wait/kill;
  async spawn ProcessCap / join SysCap; **python stays SysCap**).
- Domain SysCap / soft-legacy NetCap leftovers (e.g. dns UDP residual) are
  **not** claimed FIXED by this peer — floors only.

========================================================================
1) cap_g1_net_granular_smoke.sh — PASS (must stay green)
========================================================================

```
OODAC_BIN=./oodac/oodac bash scripts/cap_g1_net_granular_smoke.sh
# exit 0
```

Live transcript:

```
OK static sealed_net_kind_of map (tcp_bind→BindCap)
OK static runtime oo_cap_require_http
OK check deny HttpCap+tcp_bind
OK check deny TcpCap+fetch
OK check allow NetCap+fetch+tcp_connect
OK check allow TcpCap+tcp_connect
OK check allow HttpCap+fetch
OK check allow NetCap+fetch (legacy)
OK ci_product soft-wire present
cap_g1_net_granular_smoke: PASSED
```

**CAP-G1 granular net enforce still green.** Wrong-granular deny + preferred
Http/Tcp/Bind + legacy NetCap allow paths intact under tip host.

========================================================================
2) cap_g2_fs_granular_smoke.sh — PASS (must stay green)
========================================================================

```
OODAC_BIN=./oodac/oodac bash scripts/cap_g2_fs_granular_smoke.sh
# exit 0
```

Live transcript:

```
OK static sealed_fs_kind_of map (read_file→FsReadCap)
OK static sealed_fs_kind_of map (write_file→FsWriteCap)
OK static runtime require_fsread / oo_cap_require_fsread
OK check deny FsReadCap+write_file
OK check deny FsWriteCap+read_file
OK check allow FsCap+read+write
OK check allow FsReadCap+read_file
OK check allow FsWriteCap+write_file
OK check allow FsCap+read_file (legacy)
OK check allow FsCap+write_file (legacy)
OK ci_product soft-wire present
cap_g2_fs_granular_smoke: PASSED
```

**CAP-G2 granular fs enforce still green.** Wrong-granular deny
(FsRead↛write; FsWrite↛read) + preferred FsRead/FsWrite + legacy FsCap allow
paths intact.

========================================================================
3) cap_g3_grant_inject_smoke.sh — PASS (must stay green)
========================================================================

```
OODAC_BIN=./oodac/oodac bash scripts/cap_g3_grant_inject_smoke.sh
# exit 0
```

Live transcript:

```
OK static c_emit_fn markers (grant_http/fsread/tcp)
OK static c_emit_preamble decls (grant_http/fsread)
OK emit-c main(http: &HttpCap) inject (oo_cap_grant_http)
OK emit-c main(fsread: &FsReadCap) inject (oo_cap_grant_fsread)
OK emit-c main(tcp: &TcpCap) inject (oo_cap_grant_tcp)
OK emit-c multi HttpCap+FsReadCap+TcpCap grants
OK ci_product soft-wire present
cap_g3_grant_inject_smoke: PASSED
```

**CAP-G3 type-driven main-param grant inject still green** (http/fsread/tcp
markers + multi-grant emit-c path).

========================================================================
4) cap_g4_process_smoke.sh — PASS (must stay green — ProcessCap floor)
========================================================================

```
OODAC_BIN=./oodac/oodac bash scripts/cap_g4_process_smoke.sh
# exit 0
```

Live transcript:

```
OK static CAP-G4 markers
OK deny process_prctl (rc=1)
OK allow process_exec
OK allow sys_exec
OK allow sys_prctl
OK corpus ok_process_cap_sys_exec.oo
OK corpus ok_sys_cap_sys_exec.oo
OK corpus deny wrong_granular_process_for_prctl.oo
OK ci_product soft-wire present
cap_g4_process_smoke: PASSED
```

**CAP-G4 ProcessCap path-A still green.** ProcessCap preferred for exec;
ProcessCap↛prctl (wrong-granular deny); SysCap supersede for exec + hard Sys
ops; corpus fixtures OK.

========================================================================
5) cap_g5_seccomp_honesty_smoke.sh — PASS (must stay green)
========================================================================

```
OODAC_BIN=./oodac/oodac bash scripts/cap_g5_seccomp_honesty_smoke.sh
# exit 0
```

Live transcript:

```
OK preamble residual honesty markers
OK emit-c free of seccomp-bpf filter (process-local caps only)
OK ci_product soft-wire present
cap_g5_seccomp_honesty_smoke: PASSED
```

**CAP-G5 seccomp residual honesty still green.** Product floor remains
process-local tokens only; no sock_filter/BPF product claim in emit-c.

========================================================================
6) cap_g6_std_migrate_smoke.sh — PASS (must stay green)
========================================================================

```
OODAC_BIN=./oodac/oodac bash scripts/cap_g6_std_migrate_smoke.sh
# exit 0
```

Live transcript:

```
REPORT std/src bare-cap residual counts:
  &NetCap     = 17
  &FsCap      = 6
  &HttpCap    = 8
  &FsReadCap  = 6
  &SysCap     = 571
OK reported bare &NetCap=17 &FsCap=6 in std/src
OK http_get uses &HttpCap (/home/jeryd/Projects/openOODA/std/src/net.oo)
OK http_get uses &HttpCap (/home/jeryd/Projects/openOODA/ooda/std/os/net.oo)
OK fs_read_file uses &FsReadCap (/home/jeryd/Projects/openOODA/std/src/fs.oo)
OK fs_read_file uses &FsReadCap (/home/jeryd/Projects/openOODA/ooda/std/os/fs.oo)
NOTE residual: &SysCap bulk remains in std/src (count=571) — CAP-G6 path-A does not require full SysCap purge
OK SysCap bulk residual noted (not a gate)
OK ci_product soft-wire present
cap_g6_std_migrate_smoke: PASSED (path-A wave criteria met)
```

**CAP-G6 net+fs product facade wave still green.** HttpCap/FsReadCap on
product + external facades; SysCap bulk residual **named** (571) — honesty
holds, not a gate fail.

========================================================================
7) cap_process_facade_smoke.sh — PASS (must stay green)
========================================================================

```
OODAC_BIN=./oodac/oodac bash scripts/cap_process_facade_smoke.sh
# exit 0
```

Live transcript:

```
OK static process_exec uses &ProcessCap
OK static process_prctl uses &SysCap
OK deny process_prctl_shape (rc=1)
OK allow process_exec_sealed
OK check std/os/process.oo
OK python load_model SysCap honesty (/home/jeryd/Projects/openOODA/ooda/std/os/python.oo)
OK python load_model SysCap honesty (/home/jeryd/Projects/openOODA/ooda/../std/python.oo)
OK deny process_python_embed (rc=1)
OK async spawn_task uses &ProcessCap (/home/jeryd/Projects/openOODA/ooda/std/os/async.oo)
OK async join_task stays &SysCap (/home/jeryd/Projects/openOODA/ooda/std/os/async.oo)
OK async spawn_task uses &ProcessCap (/home/jeryd/Projects/openOODA/ooda/../std/async.oo)
OK async join_task stays &SysCap (/home/jeryd/Projects/openOODA/ooda/../std/async.oo)
OK ci_product wire after cap_g6
cap_process_facade_smoke: PASSED
```

**ProcessCap product facades still green.** `process_exec` → ProcessCap;
`process_prctl` stays SysCap; python embed hard SysCap honesty; async spawn
ProcessCap / join SysCap on product + external twins.

========================================================================
VERDICT
========================================================================

| Item | Status |
|------|--------|
| `cap_g1_net_granular_smoke.sh` | **PASS** (exit 0) — must-stay-green **holds** |
| `cap_g2_fs_granular_smoke.sh` | **PASS** (exit 0) — must-stay-green **holds** |
| `cap_g3_grant_inject_smoke.sh` | **PASS** (exit 0) — must-stay-green **holds** |
| `cap_g4_process_smoke.sh` | **PASS** (exit 0) — ProcessCap path-A **holds** |
| `cap_g5_seccomp_honesty_smoke.sh` | **PASS** (exit 0) — residual honesty **holds** |
| `cap_g6_std_migrate_smoke.sh` | **PASS** (exit 0) — net+fs facade wave **holds** |
| `cap_process_facade_smoke.sh` | **PASS** (exit 0) — ProcessCap product facades **hold** |
| Soft-skip of assigned hard gates | **none** |
| Peer product fix | **none** (OWN: audit only) |
| Push | **none** |
| Domain DNS/NetCap residual FIXED claim | **none** (floor re-prove only) |
| **OVERALL** | **PASS** |

### Leave-off (≤15 lines)

- Floor regression: CAP-G1–G6 + `cap_process_facade` **all PASS** on tip host
  `oodac/oodac` (962680 B, BuildID 78d775e7…, tip `2a0d10f`).
- CAP-G1: wrong-granular deny + preferred Http/Tcp/Bind + legacy NetCap.
- CAP-G2: wrong-granular deny + preferred FsRead/FsWrite + legacy FsCap.
- CAP-G3: grant inject http/fsread/tcp + multi-grant emit-c.
- CAP-G4: ProcessCap exec allow; ProcessCap↛prctl; SysCap supersede + corpus.
- CAP-G5: emit-c free of seccomp-bpf; process-local only honesty.
- CAP-G6: HttpCap/FsReadCap facades; SysCap bulk residual named (571).
- ProcessCap facades: process/async ProcessCap; prctl/join/python SysCap.
- Residual open outside this gate: domain SysCap bulk; soft-legacy NetCap
  (e.g. dns UDP) — not this peer's product work.
- Branch ahead 0 of origin — **no push** per OWN. No product edits by this peer.

end of cap_dns_peer3_regression.oot
