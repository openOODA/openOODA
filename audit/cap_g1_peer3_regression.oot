# cap_g1_peer3_regression.oot
# Peer Verifier 3 (QA) — Floor Regression Gate for CAP-G1
# Date: 2026-08-12T11:36:50Z
# Role: QA Peer 3 — Floor Regression on CAP-G1 (granular net enforce wave)
# Tree: /home/jeryd/Projects/openOODA/ooda
# OWN: write this audit only; no product feature work; no push
# Scope: re-prove path-A floors still green under tip product host while
#        CAP-G1 meta net work proceeds:
#        T1 secret return, T2 secret depth, T3 runtime ZT, T4 hygiene
#        + caps_matrix / existing net cap smokes (libfloor_net)

========================================================================
SUMMARY
========================================================================

| Gate | Command | Result |
|------|---------|--------|
| T1 secret return | `OODAC_BIN=./oodac/oodac bash scripts/secret_t1_return_smoke.oo` | **PASS** exit 0 |
| T2 secret depth | `OODAC_BIN=./oodac/oodac bash scripts/secret_t2_depth_smoke.oo` | **FAIL** exit 1 |
| T3 runtime ZT rollup | `OODAC_BIN=./oodac/oodac bash scripts/runtime_zt_t3_smoke.oo` | **PASS** exit 0 |
| T4 hygiene | `bash scripts/hygiene_t4_smoke.oo` | **PASS** exit 0 |
| caps_matrix (net + family rails) | `OODAC_BIN=./oodac/oodac bash scripts/caps_matrix_smoke.oo` | **FAIL** exit 1 |
| libfloor_net (existing net cap smoke) | `OODAC_BIN=./oodac/oodac bash scripts/libfloor_net_smoke.oo` | **PASS** exit 0 |

**OVERALL: FAIL**

CAP-G1 constraint (meta net work must not break ooda floors) does not hold on
tip product host: hard-req T2 secret depth smoke exits 1 (host abort on
refuse fixtures). T1 / T3 / T4 remain green. Net-specific libfloor rail stays
green (legacy `NetCap`); full `caps_matrix` fails on Env runtime + subsequent
sys_exec emit corruption.

========================================================================
HOST / TIP / CAP-G1 SURFACE
========================================================================

| Item | Value |
|------|--------|
| Cwd | `/home/jeryd/Projects/openOODA/ooda` |
| Tip (`git rev-parse --short HEAD`) | `01254d4` |
| Tip subject | `docs(caps): point CAPS_MATRIX at process CAPS.oot catalog` |
| Gate product host | `oodac/oodac` **956688** B @ 2026-08-12 04:27:50 -0700 |
| Gate host md5 | `96da54c682e3349b52ed40d10fcf10c4` |
| Gate host BuildID | `8eb98d1fb115528420a4acdce9d2d201713e0e2b` |
| Bak files | gitignored backups (`.bak`, `.bak.t3pre`, etc.) |
| Branch | `main...origin/main` **ahead 25** (no push this peer) |
| Mandate | granular net enforce must not break T1–T4 floors; prove matrix/net smokes |

### CAP-G1 runtime surface (observed, not owned)
`runtime/chs_rt_sys.c`:
- `g_tok_http` / `g_tok_tcp` / `g_tok_udp` / `g_tok_bind` + grant helpers
- Granular net accepts `g_tok_*` OR full `NetCap` (`g_tok_net`)
- `oo_cap_require_http` used from fetch path

========================================================================
1) secret_t1_return_smoke.oo — PASS
========================================================================

```
OODAC_BIN=./oodac/oodac bash scripts/secret_t1_return_smoke.oo # exit 0
OK secret_function_return_fail emit refuses (rc=1); OK secret_function_return_pass emit OK
OK secret_alias_rebind_pass emit refuses; OK secret_if_taint_fail emit refuses
OK ci_product wires secret_t1_return_smoke; secret_t1_return_smoke: PASSED
```
**No T1 regression under tip host.**

========================================================================
2) secret_t2_depth_smoke.oo — FAIL
========================================================================

```
OODAC_BIN=./oodac/oodac bash scripts/secret_t2_depth_smoke.oo # exit 1
FAIL secret_field_assign_fail missing ERR secret out=…
malloc(): unaligned tcache chunk detected (rc=134)
```
Smoke expects non-zero rc AND `ERR	secret`. Host aborts mid-emit with malloc unaligned tcache on `secret_field_assign_fail`, `secret_call_arg_fail`, `secret_dlopen_arg_fail`, and `secret_sys_exec_argv_fail`.
**T2 regression:** refuse fixtures crash product host instead of clean secret refuse.

========================================================================
3) runtime_zt_t3_smoke.oo — PASS
========================================================================

```
OODAC_BIN=./oodac/oodac bash scripts/runtime_zt_t3_smoke.oo # exit 0
OK 1.8 static/emit+gcc: OODA_/OO_ prefix filter; no dangerous env observed
OK 1.10 static: FFI handle table mutex; OK 4.5 static: nested dlopen refuse
OK soft cap_ffi_runtime_smoke.oo exit 0 / ffi_dlopen_path_a_smoke.oo exit=1
OK 6.4 static/emit+gcc: OODA_FS_WRITEDIR fail-closed
OK ci_product wires runtime_zt_t3_smoke: PASSED
```
**T3 runtime ZT floors still green under tip product host.**

========================================================================
4) hygiene_t4_smoke.oo — PASS
========================================================================

```
bash scripts/hygiene_t4_smoke.oo # exit 0
OK oodac.bak* present and gitignored (3 file(s))
OK no patch_awk / boot_dbg / cap-strip scripts; OK no DBG_/DEBUG println
hygiene_t4_smoke: PASSED
```
**T4 hygiene floor still green.**

========================================================================
5) caps_matrix_smoke.oo — FAIL
========================================================================

```
OODAC_BIN=./oodac/oodac bash scripts/caps_matrix_smoke.oo # exit 1
OK check deny/allow corpus fixtures (Fs/Sys/Env/Net/Time/Rand/Alloc)
FAIL runtime Env env_get out=
```
- `oo_env_get` calls `oo_process_policy_getenv`, which fail-closes non `OODA_`/`OO_` keys. Reading `"PATH"` returns Err, breaking EnvCap `env_get` for ordinary keys.
- Follow-on `sys_exec` emit produces corrupted C (`oo_st_lit("tue")`), causing gcc errors.

========================================================================
6) libfloor_net_smoke.oo — PASS (existing net cap smoke)
========================================================================

```
OODAC_BIN=./oodac/oodac bash scripts/libfloor_net_smoke.oo # exit 0
OK check refuse bare tcp_connect; OK check tcp_connect/tls_connect with NetCap
OK emit-c / lowers / grant_net / honesty; OK runtime residual + zero/magic forge deny
OK TLS split to chs_rt_tls.c; libfloor_net_smoke: PASSED
```
**Legacy NetCap libfloor rail still green.**

========================================================================
VERDICT
========================================================================

| Item | Status |
|------|--------|
| `secret_t1_return_smoke.oo` | **PASS** (exit 0) |
| `secret_t2_depth_smoke.oo` | **FAIL** (exit 1 — host abort, no clean ERR secret) |
| `runtime_zt_t3_smoke.oo` | **PASS** (exit 0) |
| `hygiene_t4_smoke.oo` | **PASS** (exit 0) |
| `caps_matrix_smoke.oo` | **FAIL** (Env env_get + host emit corruption) |
| `libfloor_net_smoke.oo` | **PASS** (exit 0) |
| Soft-skip of assigned gates | **none** |
| Peer product fix | **none** (OWN: audit only) |
| Push | **none** |
| **OVERALL** | **FAIL** |

### Leave-off (≤15 lines)

- Floor regression for CAP-G1: T1 + T3 + T4 **PASS**; **T2 FAIL** on tip host
  `oodac/oodac` (956688 B, BuildID 8eb98d1f…, tip `01254d4`).
- T2: `secret_field_assign_fail` aborts with `malloc(): unaligned tcache chunk detected`
  (rc=134) instead of clean `ERR	secret` refuse.
- Net: `libfloor_net_smoke` **PASS** (legacy NetCap).
- `caps_matrix` **FAIL**: `oo_env_get` drops `PATH`; plus `sys_exec` emit corruption.
- CAP-G1 must not ship over a broken T2 host; restore clean secret refuse +
  EnvCap PATH semantics before claiming matrix green.
- Branch ahead 25 of origin — **no push** per OWN. No product edits by this peer.

end of cap_g1_peer3_regression.oot
