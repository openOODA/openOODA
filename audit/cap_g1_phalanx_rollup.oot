# cap_g1_phalanx_rollup.oot — Close Coordinator rollup (CAP-G1)
# Date: 2026-08-12T11:41:00Z
# Role: Close Coordinator — CAP-G1 (granular net enforce)
# Decision: **FAIL — leave CAP-G1 OPEN** (do not mark FIXED; no greenwash)

## Honest close criteria (all required)

| # | Criterion | Result | Evidence |
|---|-----------|--------|----------|
| 1 | peer1 + peer2 + security + integration **green** | **FAIL** | peer2 runtime **PASS** (C-host tokens); peer3 floors **FAIL**; hygiene residual. **Missing green:** peer1, security, integration |
| 2 | `cap_g1` smoke exit **0** | **FAIL** | `scripts/cap_g1_net_granular_smoke.sh` exit **1** — check **accepts** wrong-granular deny fixtures |
| 3 | T1–T4 floors green | **FAIL** (live host) | Live tip host T2 **exit 1**; T1/T3/T4 exit 0. Pin host `oodac_new` md5 `0208bab4…` T1–T4 all exit 0 |
| 4 | Least-privilege: **HttpCap does not tcp_bind** | **FAIL** (live host) | Live `oodac check` of HttpCap+`tcp_bind` → **OK rc=0** (must refuse). Smoke: `FAIL check accepted deny HttpCap+tcp_bind` |
| 5 | No remote push | **PASS** | Coordinator did not push |

**Close note:** CAP-G1 is **not** FIXED. Catalog/docs WIP over-claims path-A check map; live product host fails least-privilege + T2 floor. Residual named below — do **not** ship as closed.

---

## Inputs merged (what exists)

| Peer audit | Role | Verdict (self) | Notes |
|------------|------|----------------|-------|
| `audit/cap_g1_peer1_*.oot` | Peer 1 (check map) | **ABSENT** | required for close |
| `audit/cap_g1_peer2_runtime.oot` | Peer 2 runtime tokens | **PASS** (runtime C-host) | 32/32 require wrong-granular DENY; NetCap supersede; emit grant Http/Udp/Bind residual |
| `audit/cap_g1_peer3_regression.oot` | Peer 3 floors | **FAIL** | T2 exit 1 on live host `96da54c6…`; caps_matrix also red |
| `audit/cap_g1_security.oot` | Security | **ABSENT** | required for close |
| `audit/cap_g1_integration.oot` | Integration | **ABSENT** | required for close |
| `audit/cap_g1_hygiene.oot` | Hygiene | **HONEST RESIDUAL PASS** | soft/pack line residual; hard >600 = 0; not a close gate alone |

Coordinator does **not** invent missing peer1/security/integration PASS papers. Runtime peer green **does not** close check-layer least privilege.

---

## Coordinator live re-prove (close gate)

| Item | Value |
|------|--------|
| ooda tip commit | `01254d4` — docs(caps) CAPS_MATRIX pointer (WT **DIRTY** CAP-G1 WIP) |
| openOODA tip (pre-this) | ahead origin; CAPS/SPRINT dirty mid-flight |
| **Live product host** | `ooda/oodac/oodac` **956688** B @ 2026-08-12 04:27 — md5 **`96da54c682e3349b52ed40d10fcf10c4`** |
| Live BuildID | `8eb98d1fb115528420a4acdce9d2d201713e0e2b` |
| Pin host (T5-proven) | `ooda/oodac_new` **956688** B md5 **`0208bab43c9cd49718d2b859a84ef94c`** |
| CAP-G1 smoke | `scripts/cap_g1_net_granular_smoke.sh` |

### Floor matrix

| Smoke | Live host `oodac/oodac` `96da54c6…` | Pin `oodac_new` `0208bab4…` |
|-------|-------------------------------------|------------------------------|
| `secret_t1_return_smoke` | **PASS** exit 0 | **PASS** exit 0 |
| `secret_t2_depth_smoke` | **FAIL** exit 1 | **PASS** exit 0 |
| `runtime_zt_t3_smoke` | **PASS** exit 0 | **PASS** exit 0 |
| `hygiene_t4_smoke` | **PASS** exit 0 | **PASS** exit 0 |
| `cap_g1_net_granular_smoke` | **FAIL** exit 1 | **FAIL** exit 1 |

Live T2 first hard fail (matches peer3):

```
FAIL secret_field_assign_fail missing ERR secret out=… (partial C)
… BPF_JUMP(BPF_JMPmalloc(): unaligned tcache chunk detected
```

CAP-G1 smoke (live, post-fixture restore) transcript:

```
FAIL check accepted deny HttpCap+tcp_bind (wrong_granular_http_for_tcp.oo)
FAIL check accepted deny TcpCap+fetch (wrong_granular_tcp_for_fetch.oo)
OK check allow NetCap+fetch+tcp_connect
OK check allow TcpCap+tcp_connect
OK check allow HttpCap+fetch
OK check allow NetCap+fetch (legacy)
cap_g1_net_granular_smoke: FAILED
```

Deny fixtures **present** but check **accepts** them → least-privilege fail at check layer (not missing-file soft fail).

### Least-privilege probe (coordinator, not greenwashed)

Ad-hoc fixtures (corpus fail files **absent** from tree at close):

| Probe | Live `96da54c6…` | Pin `0208bab4…` | Required for CAP-G1 |
|-------|------------------|-----------------|---------------------|
| `&HttpCap` + `tcp_bind` | **OK rc=0** (accept) | **deny** rc≠0 (NetCap required) | **must deny** |
| `&TcpCap` + `fetch` | **OK rc=0** (accept) | (not re-run; pin expects NetCap) | **must deny** |
| `&HttpCap` + `fetch` | **OK** | pin: deny (NetCap-only sealed) | allow once preferred map lands |
| `&TcpCap` + `tcp_connect` | **OK** | pin: deny (NetCap-only) | allow once preferred map lands |

**Verdict:** live tip host **fails** “HttpCap does not tcp_bind”. Runtime source WIP (`oo_cap_require_bind` ≠ http token) is **not** the live check gate; check-time least privilege is the close criterion and is red.

---

## Product surface honesty (WT at close)

| Piece | State | Truth |
|-------|-------|-------|
| `sealed_kind_of` net | still returns **`NetCap`** for all `is_sealed_net` | preferred map **not** in `sealed_kind_of`; `sealed_net_kind_of` **absent** from util |
| `check_caps.oo` WIP | dirty: NetCap **supersede** for granular kinds *if* kind were preferred | cannot least-privilege while kind stays NetCap; live host body ≠ this WIP (accepts wrong granular) |
| `runtime/chs_rt_sys.c` WIP | dirty: `oo_cap_require_{http,tcp,udp,bind}` + fetch→require_http | source-level runtime least-priv **aspirational** until rebuilt + proved |
| `runtime/chs_rt_netfloor.c` WIP | dirty: bind/tcp/udp per-op require | same |
| Corpus fail fixtures | present `wrong_granular_*` | check **accepts** (must deny) → smoke FAIL |
| Corpus pass fixtures | untracked `ok_http_cap_fetch` / `ok_tcp_cap_connect` / `ok_net_cap_fetch_tcp` | present; not a close alone |
| `scripts/cap_g1_net_granular_smoke.sh` | untracked | **FAILED** exit 1 |
| Peer2 runtime | **PASS** C-host require map | emit grant Http/Udp/Bind residual named |
| `CAPS.oot` / `CAPS_MATRIX.md` | claim CAP-G1 check path-A map | **greenwash vs live prove** — coordinator reverts catalog honesty |

**Do not land** dirty check/runtime as FIXED. **Do not** treat matrix prose as smoke green.

---

## Cohort outcomes

| # | Agent / input | Outcome | Notes |
|---|----------------|---------|-------|
| 1 | Peer1 check | **ABSENT** | block close |
| 2 | Peer2 runtime | **PASS** | C-host tokens; emit residual |
| 3 | Peer3 regression | **FAIL** | T2 red live |
| 4 | Security | **ABSENT** | block close |
| 5 | Integration | **ABSENT** | block close |
| 6 | Hygiene | residual PASS | not sufficient |
| 7 | CAP-G1 smoke | **FAIL** | check accepts wrong granular |
| 8 | Close Coordinator | **FAIL leave open** | this rollup |

**Cohort summary:** Runtime peer green · Check/smoke red · Floors live T2 red · Least-privilege live red · Command **FAIL leave open**.

---

## Residual (honest next wave — meta DE)

1. **Restore / land check preferred map:** implement `sealed_net_kind_of` (or equivalent) so `tcp_bind`→`BindCap`, `fetch`→`HttpCap`, etc.; NetCap supersede only; **no** reverse “any granular satisfies NetCap.”
2. **Rebuild product host** from proven sources; discard host `96da54c6…` if it embeds fail-open granular-compat.
3. **Restore deny fixtures** + prove smoke exit 0: HttpCap↛tcp_bind, TcpCap↛fetch; pass HttpCap+fetch, TcpCap+tcp_connect, NetCap supersede multi-op.
4. **T2 floor green on live host** before CAP-G1 close (secret_field_assign_fail clean refuse, no malloc abort).
5. **Peers:** peer1 (check map), peer2 (runtime require), security (least-priv / forge), integration (matrix+smoke) — all green audits required.
6. **Docs:** CAPS.oot / CAPS_MATRIX must not claim CAP-G1 FIXED until criteria 1–4 green (catalog honesty restored this close).
7. **Emit grant/require** matrix residual if still NetCap-message-only in `c_emit_netio.oo`.
8. CAP-G2+ (fs/sys granular) remains separate backlog — do not thrash.

---

## FIXED IDs (this rollup)

**None.** CAP-G1 remains **OPEN**.

---

## Close decision

| Decision | **FAIL — leave open** |
|----------|------------------------|
| SPRINT | CAP-G1 row stays open; pointer to this rollup |
| Product land | **no** CAP-G1 FIXED commit of check/runtime as closed |
| Push | **none** (human-gated residual elsewhere) |

*Honest residual beats green paint. Re-open close only when peers+security+integration green, smoke exit 0, T1–T4 live green, and HttpCap↛tcp_bind is proved deny on the tip host.*
