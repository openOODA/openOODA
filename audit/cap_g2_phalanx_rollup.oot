# cap_g2_phalanx_rollup.oot — CAP-G2 granular fs enforce
# Date: 2026-08-12
# Role: Close Coordinator CAP-G2
# Decision: **FIXED** path-A (check + runtime + smoke + docs)
# No push.

## Close criteria

| # | Criterion | Result |
|---|-----------|--------|
| 1 | Peer least-privilege: FsReadCap ↛ write_file | **PASS** (check rc≠0 + capability ERR) |
| 2 | Peer least-privilege: FsWriteCap ↛ read_file / path_exists | **PASS** |
| 3 | Exact granular allow + FsCap legacy supersede | **PASS** |
| 4 | Runtime `oo_cap_require_fsread` / `fswrite` (granular **or** FsCap) | **PASS** (C probe R/W/F/G ok; X/Y fail-closed) |
| 5 | `OODA_FS_WRITEDIR` write overlay retained | **PASS** (static + T3/fs_writedir green) |
| 6 | `cap_g2_fs_granular_smoke.sh` | **PASS** exit 0 |
| 7 | CAP-G1 still green | **PASS** `cap_g1_net_granular_smoke.sh` exit 0 |
| 8 | Runtime + docs present | **PASS** (chs_rt_fs/sys + CAPS.oot + CAPS_MATRIX + this rollup) |
| 9 | No push | **PASS** |

## Product land (ooda)

| Repo | Commit | What |
|------|--------|------|
| ooda | `0cbc4aa` | check map + check_caps helpers + runtime require + smoke + fixtures + CAPS_MATRIX |

| Surface | What |
|---------|------|
| `oodac/check_cap_util.oo` | `sealed_fs_kind_of` → FsReadCap/FsWriteCap; `cap_fs_granted_ok` / `cap_fs_argflow_ok`; `sealed_kind_of` prefers fs map |
| `oodac/check_caps.oo` | Grant + arg-flow via cap_net_* / cap_fs_* helpers (no inverted any-granular fail-open) |
| `runtime/chs_rt_sys.c` | `oo_cap_require_fsread` / `oo_cap_require_fswrite` (exact **or** `g_tok_fs`) |
| `runtime/chs_rt_fs.c` | read/path/size → require_fsread; write → require_fswrite; writedir overlay kept |
| Corpus | fail: `wrong_granular_fsread_for_write` / `fswrite_for_read` / `fswrite_for_path`; pass: `ok_fs_read_cap_read` / `ok_fs_write_cap_write` / `ok_fs_cap_read_write` (+ path_size extras) |
| Smoke | `scripts/cap_g2_fs_granular_smoke.sh` (ci_product rail listed) |
| Docs | `bootstrap/CAPS_MATRIX.md` CAP-G2 path-A map |

## Host prove (Close Coordinator)

| Item | Value |
|------|--------|
| Product host | `ooda/oodac/oodac` **949456** B |
| md5 | `0de4edd3f5b7f0d9f4acfc8a0c7902f9` |
| Build path | pure multi `scripts/oodac_pure_build.sh` → `~/.cache/ooda-tmp/oodac_from_seed_capg2` (also `/tmp/oodac_cap_g2`) |
| CAP-G2 smoke | exit **0** |
| CAP-G1 smoke | exit **0** |
| Runtime C probe | R/W exact ok; F/G FsCap supersede ok; X (fsread token for fswrite) + Y (fswrite token for fsread) exit 1 fail-closed |
| Peer1 matrix | 4/4 PASS (FsRead↛write, FsWrite↛read, FsCap both, exact allows) — `audit/cap_g2_peer1_check.oot` |
| Peer3 floors | CAP-G1/T1/T3/T4/fs_writedir green; T2 soft residual named — `audit/cap_g2_peer3_regression.oot` |

## Map

| Preferred | Ops | Runtime |
|-----------|-----|---------|
| FsReadCap | read_file, fs_read, path_exists, file_size | require_fsread \| FsCap |
| FsWriteCap | write_file, fs_write | require_fswrite \| FsCap + OODA_FS_WRITEDIR |
| FsCap | supersede soft-granular only | g_tok_fs |

## Residual (not CAP-G2 fail)

- Emit: main-param grant inject for every granular name still partial (`fsread` today; full matrix **CAP-G3**)
- `fs_read` / `fs_write` aliases sealed in check but not product-lowered (type residual / link fail-closed)
- OS FS isolation / landlock / seccomp on open/write (**CAP-G5**)
- Std migration off legacy `&FsCap` (**CAP-G6**)
- Soft pack-line residual on `check_cap_util.oo` / `chs_rt_sys.c` (hygiene; hard ban >600 still green)
- T2 secret depth soft residual on dirty pure multi of unrelated oodac WIP — re-prove on clean tree
- CAP-G3 unused V2 grants; CAP-G4 ProcessCap/sys

## Probe note

Prior dual-accept inverted path (kind always `FsCap`, any matching granular for op) already blocked cross-grant fail-open at check. CAP-G2 lands **preferred-kind** map (`sealed_fs_kind_of`) matching CAP-G1 shape, runtime soft-granular require, corpus fixtures, and smoke. Wrong preferred fails closed; FsCap supersedes only.
