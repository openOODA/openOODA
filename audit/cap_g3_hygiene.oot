# cap_g3_hygiene.oot — Hygiene Enforcer (CAP-G3 unused grants / emit inject)
# Date: 2026-08-12T12:03:24Z
# Role: Hygiene Enforcer — CAP-G3
# Tree: /home/jeryd/Projects/openOODA  (ooda/ + openOODA/)
# Tip (ooda): 0cbc4aa (working tree dirty — CAP-G3 WIP on emit preamble/fn + new smoke)
# Tip (openOODA): 20fd50d
# OWN: line-lock c_emit_fn / c_emit_preamble / c_emit_skip;
#      bash -n new CAP-G3 smoke + related caps smokes; no /tmp secrets; no mass deletes; no push
# WRITE: this audit only
# POLICY: RULES §1.7 / PROBE MAX_LINES = 256 (soft); pack watch 300; hard ban >600
#         No mass peels/deletes this pass (DE mid-flight on CAP-G3)

========================================================================
SUMMARY
========================================================================

| Check | VERDICT | Notes |
|-------|---------|-------|
| Soft lock ≤256 (OWN three) | **PASS** | fn **200**, preamble **144**, skip **222** — all under soft |
| Pack pressure ≤300 | **PASS** | 0/3 over 300; worst skip at 222 (−78 headroom) |
| Hard ban >600 | **PASS** | 0 OWN files >600 |
| Split this pass | **NOT needed** | all under soft; no peel |
| `bash -n` new CAP-G3 smoke | **PASS** | `cap_g3_grant_inject_smoke.sh` syntax clean (130 lines) |
| `bash -n` CAP-G3-related smokes | **PASS** | 15/15 syntax clean (incl. G1/G2 + caps floor) |
| `/tmp` secrets in OWN + smokes | **PASS** | ZERO secrets; OWN free of `/tmp`; new smoke uses `$HOME/.cache/ooda-tmp` |
| New smoke present | **yes (untracked)** | `scripts/cap_g3_grant_inject_smoke.sh` ?? |
| Mass deletes | **none** | charter |
| Push | **no** | charter |

OVERALL: **HONEST PASS** (OWN line-lock green; new smoke bash -n green; CAP-G3 product residual named — not closed)

========================================================================
SCOPE + METHOD
========================================================================

OWN units (CAP-G3 surface — SPRINT: wire unused V2 grants / emit main-param inject):

  `ooda/oodac/c_emit_fn.oo`        — main inject grant matrix
  `ooda/oodac/c_emit_preamble.oo`  — C decls for `oo_cap_grant_*` (legacy + V2)
  `ooda/oodac/c_emit_skip.oo`      — skip helpers used by fn emit (contracts/braces/types)

Related (observe / bash -n only):

  `scripts/cap_g3_grant_inject_smoke.sh`  — **NEW** (untracked) grant-inject harness
  `scripts/cap_g1_net_granular_smoke.sh`  — sibling granular (M dirty)
  `scripts/cap_g2_fs_granular_smoke.sh`   — sibling granular
  `scripts/caps_matrix_smoke.sh`
  `scripts/caps_product_floor_smoke.sh`
  `scripts/cap_forge_path_a_smoke.sh`
  `scripts/alloc_cap_smoke.sh`
  `scripts/biometric_caps_residual_smoke.sh`
  `scripts/cap_ffi_runtime_smoke.sh` / `cap_ffi_product_floor_smoke.sh` / `cap_ffi_residual_smoke.sh`
  `scripts/sys_exec_env_filter_smoke.sh` / `sys_syscall_path_a_smoke.sh`
  `scripts/runtime_zt_t3_smoke.sh`
  `scripts/wasm_caps_smoke.sh`
  `scripts/fs_writedir_smoke.sh`
  host `/tmp/cap_g3*` + `/tmp/cap_g3_peer1/*` (DE probe; untracked)

Commands:

  `wc -l` on OWN three
  `bash scripts/check_file_lines.sh` (repo MAX_LINES=256; O=17 whole tree; OWN three not listed)
  `bash -n` on new CAP-G3 smoke + related caps smokes
  `grep` secret-pattern + `/tmp` scan on OWN / new smoke
  observe host `/tmp/cap_g3*` probe debris (not committed)

Thresholds:

| Threshold | Meaning |
|-----------|---------|
| **256** | RULES §1.7 / PROBE MAX_LINES — soft lock |
| **300** | Pack monofile pressure (flag for DE peel) |
| **600** | Path-A hard ban — still green for OWN |

========================================================================
LINE LOCK — OWN THREE (live)
========================================================================

Measured under `ooda/` at tip `0cbc4aa` + dirty CAP-G3 WIP (2026-08-12T12:03Z).

| File | Lines | ≤256 | ≤300 | ≤600 | Headroom 256 | Headroom 300 | Split? |
|------|------:|:----:|:----:|:----:|-------------:|-------------:|:------:|
| `oodac/c_emit_fn.oo` | **200** | PASS | PASS | PASS | +56 | +100 | NO |
| `oodac/c_emit_preamble.oo` | **144** | PASS | PASS | PASS | +112 | +156 | NO |
| `oodac/c_emit_skip.oo` | **222** | PASS | PASS | PASS | +34 | +78 | freeze growth prefer |
| **OWN total** | **566** | — | — | — | — | — | — |

Committed baselines (HEAD only, for delta):

| File | HEAD lines | Working | Δ |
|------|----------:|--------:|--:|
| `c_emit_fn.oo` | 195 | 200 | **+5** |
| `c_emit_preamble.oo` | 132 | 144 | **+12** |
| `c_emit_skip.oo` | 222 | 222 | 0 |

Delta notes (CAP-G3 land pressure on dirty tree):

- **fn +5**: CAP-G3 main inject block — fixed-name
  `oo_cap_grant_{http,tcp,udp,bind,fsread,fswrite,process}` after legacy
  fs/sys/env/net/time/rand/alloc/ffi/thread/gpu set. Also incidental
  type-annots on locals (`String` / `List[String]`) — zero line cost
  net of inject only (+5). Soft-lock still **+56** headroom.
- **preamble +12**: CAP-G3 comment + 8 println lines declaring granular net/fs
  + OS (process/sync/mem/sign) + HW/UI (audio/camera/usb/hid/window/frame)
  grant prototypes. audit/hitl remain header-only residual (no T inject).
- **skip +0**: no CAP-G3 touch; skip stays pack-safe at 222.

Repo-wide `check_file_lines.sh` (MAX=256): **O=17** violators; **none** of OWN three
appear in OVERSIZE. Soft-lock residual elsewhere (util 321, sys 375, c_emit.oo 459, …)
is CAP-G1/G2 / monofile debt — not this hygiene OWN set.

Observe (not OWN): `oodac/c_emit_preamble.bin.tmp.c` **398** lines shows in
OVERSIZE list — build debris (`*.c` / `*.tmp.c` gitignored per T4 policy), not
a product `.oo` peel candidate.

========================================================================
SPLIT GATE (not exercised this pass)
========================================================================

Rule: if OWN unit over soft lock and peel is **trivial + DE-authorized**,
split at functional seams. Charter this pass: **no mass deletes / no mass peel**.

| Unit | Over? | Trivial peel? | Action |
|------|:-----:|:-------------:|--------|
| `c_emit_fn.oo` 200 | no | n/a | none — inject growth cheap; type-driven map residual is logic not size |
| `c_emit_preamble.oo` 144 | no | n/a | none — grant decl block thin |
| `c_emit_skip.oo` 222 | no | n/a | **freeze growth** prefer; already closest to soft among OWN |

Suggested DE peel (only if inject matrix later blows soft — **not blocking**):

1. `c_emit_fn.oo` → if type-driven grant map grows large, peel main-grant
   table to `c_emit_cap_grant.oo` (param-name or type → `oo_cap_grant_*`).
2. `c_emit_preamble.oo` → peel cap-grant prototype block only if decls exceed
   soft after more families; today +112 headroom.
3. Do **not** peel skip for CAP-G3 — untouched and under soft.

========================================================================
CAP-G3 SURFACE SNAPSHOT (hygiene observe — not prove)
========================================================================

Emit main inject (working tree `c_emit_fn.oo`):

- **Legacy fixed names:** `fs` `sys` `env` `net` `time` `rand` `alloc` `ffi`
  `thread` `gpu`
- **CAP-G3 fixed names (new):** `http` `tcp` `udp` `bind` `fsread` `fswrite`
  `process`
- **Still not injected (V2 residual / CAP-G3 remainder):** `sync` `mem` `sign`
  `audio` `camera` `usb` `hid` `window` `frame` (+ header-only `audit`/`hitl`)
- Inject is **fixed-name**, not type-driven: `main(fs: &FsReadCap)` still gets
  legacy `fs = grant_fs()` only if param is named `fs`; pure
  `main(fsread: &FsReadCap)` needs the new `fsread` inject (now present).
  Param named `fs` with type `FsReadCap` remains honesty residual
  (type→grant map not done — same class CAP-G2 security H7).

Preamble decls (working tree):

- Legacy 10 + CAP-G3 block: net granular, fs granular, process/sync/mem/sign,
  HW/UI six — matches runtime `chs_rt.h` / `chs_rt_sys.c` grant bodies for
  those names (decls ready for DE inject expansion).

Skip (`c_emit_skip.oo`):

- Helpers only (`c_peek_list_elem`, `c_emit_skip_type` / braces / contracts /
  verify). No grant logic; line-lock observe only.

New smoke (`scripts/cap_g3_grant_inject_smoke.sh`, untracked):

- Static markers: `oo_cap_grant_{http,fsread,tcp}` in `c_emit_fn.oo`
- Soft: preamble decls present
- Live emit-c fixtures under `$TMPDIR` (`~/.cache/ooda-tmp`): HttpCap /
  FsReadCap / TcpCap / multi-param — expect matching grant strings in C
- Soft ci_product wire residual (pass either way)
- **bash -n: PASS** (this hygiene); full product prove is peer/integration lane
  (needs pure multi host with CAP-G3 emit landed)

Host probe debris (not committed):

  - `/tmp/cap_g3_http_fetch.oo`, `cap_g3_net_fetch.oo`, `cap_g3_http_for_tcp.oo`,
    `cap_g3_tcp_for_fetch.oo`
  - `/tmp/cap_g3_peer1/` — emit-c probes (`1_http_h`, `2_fsread`, `3_net`, …)

**No secret material.** Hygiene does **not** mass-delete host `/tmp`.

SPRINT honesty residual (not closed by this hygiene pass):

- CAP-G3 = wire or remove unused V2 grants (ProcessCap/SyncCap/MemCap/HW/UI…)
  + full main-param grant matrix. Partial inject (net granular + fs granular +
  process) is **WIP progress**, not SPRINT FIXED.
- Product require path for ProcessCap vs SysCap remains **CAP-G4**.
- Type-driven inject (param type → grant_*) still residual.

========================================================================
bash -n (new CAP-G3 smoke + related)
========================================================================

```
# NEW CAP-G3 smoke
bash -n scripts/cap_g3_grant_inject_smoke.sh  # OK  130 lines  (?? untracked)

# Related CAP-G / caps / runtime floor smokes
bash -n scripts/cap_g1_net_granular_smoke.sh  # OK  105  (M dirty)
bash -n scripts/cap_g2_fs_granular_smoke.sh   # OK  112
bash -n scripts/caps_matrix_smoke.sh          # OK  240
bash -n scripts/caps_product_floor_smoke.sh   # OK   29
bash -n scripts/cap_forge_path_a_smoke.sh     # OK   80
bash -n scripts/alloc_cap_smoke.sh            # OK   82
bash -n scripts/biometric_caps_residual_smoke.sh  # OK  14
bash -n scripts/cap_ffi_runtime_smoke.sh      # OK   84
bash -n scripts/cap_ffi_product_floor_smoke.sh # OK 124
bash -n scripts/cap_ffi_residual_smoke.sh     # OK  139
bash -n scripts/sys_exec_env_filter_smoke.sh  # OK  125
bash -n scripts/sys_syscall_path_a_smoke.sh   # OK  133
bash -n scripts/runtime_zt_t3_smoke.sh        # OK  284
bash -n scripts/wasm_caps_smoke.sh            # OK   20
bash -n scripts/fs_writedir_smoke.sh          # OK  142
# all present: exit 0
```

ALL_BASHN_OK = true
NEW_CAP_G3_SMOKE_SH = `scripts/cap_g3_grant_inject_smoke.sh` (untracked; bash -n PASS)

========================================================================
/tmp + SECRETS
========================================================================

### OWN units

Scan secret-like patterns (`password`, `BEGIN *PRIVATE`, api keys) + `/tmp`:

  - `oodac/c_emit_fn.oo` — **clean**
  - `oodac/c_emit_preamble.oo` — **clean**
  - `oodac/c_emit_skip.oo` — **clean**

### New CAP-G3 smoke

`cap_g3_grant_inject_smoke.sh` prefers non-/tmp temps:

  `export TMPDIR="${TMPDIR:-$HOME/.cache/ooda-tmp}"`
  `mkdir -p "$TMPDIR"`

Fixtures written under `$TMPDIR/cap_g3_*.oo` / `.c` / `.err` — **not** host `/tmp`.
No password/key/token material in script body.

### Host `/tmp` CAP-G3 probe debris (not committed)

Observed:

  - `/tmp/cap_g3_*.oo` (4 tiny fixtures) + emit err empties
  - `/tmp/cap_g3_peer1/` — peer emit-c matrix (`.oo` + generated `.c`)

**No secret material** (no keys/passwords/tokens). Hygiene does **not** mass-delete
host `/tmp` (charter: no mass deletes); operator may `rm -rf /tmp/cap_g3*` when done.
Recommend DE keep product prove in `scripts/cap_g3_grant_inject_smoke.sh` +
`$HOME/.cache/ooda-tmp` (already path-A) rather than host `/tmp`.

VERDICT `/tmp` secrets: **PASS**

========================================================================
ACTIONS / NON-ACTIONS THIS PASS
========================================================================

| Action | Result |
|--------|--------|
| Measure OWN three line locks | **done** — fn 200, preamble 144, skip 222 |
| Flag soft/pack residual | **none** (OWN all under soft+pack) |
| Split oversize units | **none** (not over; no peel) |
| Mass deletes (bak, /tmp probes, WIP) | **none** |
| `bash -n` new CAP-G3 smoke | **OK** (`cap_g3_grant_inject_smoke.sh`) |
| `bash -n` related smokes | **15/15 OK** |
| Purge `/tmp` secrets from committed surface | already clean; no edit |
| Product / runtime / emit source edits | **none** (hygiene audit only) |
| Mark SPRINT CAP-G3 closed | **no** (partial inject WIP; unused V2 residual) |
| Push | **no** |
| Audit write | **this file only** |

========================================================================
RETURN — COMPACT
========================================================================

```
SCOPE   CAP-G3 OWN three  tip ooda=0cbc4aa+WIP  openOODA=20fd50d
OWN     fn=200  preamble=144  skip=222  total=566
ΔHEAD   fn=+5  preamble=+12  skip=0  (CAP-G3 inject+decls dirty)
>600    0   PASS hard ban
>300    0   PASS pack
>256    0   PASS soft  (fn +56, preamble +112, skip +34)
BASHN   NEW smoke OK (cap_g3_grant_inject_smoke.sh 130); related 15/15 OK
/tmp    PASS  no secrets; smoke uses ~/.cache/ooda-tmp; peer debris host-only
SPLIT   none (under soft)
PUSH    no
CAP-G3  residual — partial fixed-name inject (http/tcp/udp/bind/fsread/fswrite/process);
        sync/mem/sign/HW/UI + type-driven map still open; hygiene measure only
OVERALL HONEST PASS
```

end of cap_g3_hygiene.oot
