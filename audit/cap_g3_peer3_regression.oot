# cap_g3_peer3_regression.oot
# Peer Verifier 3 (QA) — G1/G2 Floor Regression Gate for CAP-G3
# Date: 2026-08-12T12:01:37Z
# Role: QA Peer 3 — G1/G2 Regression CAP-G3
# Tree: /home/jeryd/Projects/openOODA/ooda
# OWN: write this audit only; no product feature work; no push
# Scope: re-prove CAP-G1 net granular + CAP-G2 fs granular must-stay-green
#        under tip product host while CAP-G3 (unused V2 grants / full grant
#        inject) meta work proceeds; plus T3 runtime ZT + T4 hygiene floors

========================================================================
SUMMARY
========================================================================

| Gate | Command | Result |
|------|---------|--------|
| CAP-G1 net granular | `OODAC_BIN=./oodac/oodac bash scripts/cap_g1_net_granular_smoke.sh` | **PASS** exit 0 |
| CAP-G2 fs granular | `OODAC_BIN=./oodac/oodac bash scripts/cap_g2_fs_granular_smoke.sh` | **PASS** exit 0 |
| T3 runtime ZT rollup | `OODAC_BIN=./oodac/oodac bash scripts/runtime_zt_t3_smoke.sh` | **PASS** exit 0 |
| T4 hygiene | `bash scripts/hygiene_t4_smoke.sh` | **PASS** exit 0 |

**OVERALL: PASS**

Assigned G1/G2 + T3/T4 rails all exit 0 on tip product host. CAP-G1 and CAP-G2
must-stay-green hold under CAP-G3 residual window (unused V2 grants / full
main-param grant inject not yet wired — peer does **not** claim CAP-G3 FIXED).

Assigned scripts run to completion. Soft residual inside T3 rollup
(`cap_ffi_runtime` / `ffi_dlopen_path_a` exit 1) is unchanged policy, not a
peer soft-skip.

========================================================================
HOST / TIP / CAP-G3 CONTEXT
========================================================================

| Item | Value |
|------|--------|
| Cwd | `/home/jeryd/Projects/openOODA/ooda` |
| Tip (`git rev-parse --short HEAD`) | `0cbc4aa` |
| Tip subject | `fix(cap-g2): granular fs enforce — preferred FsRead/FsWrite + runtime require` |
| Full tip | `0cbc4aa1c0bff24a29e06f7d2c7deadb6de36264` |
| Prior floors | CAP-G2 FIXED this tip; CAP-G1 `c2da895` lineage; T4 `0bfd752`; T3 `344defe` |
| Gate product host | `oodac/oodac` **949456** B @ 2026-08-12 04:55:18 -0700 |
| Gate host md5 | `0de4edd3f5b7f0d9f4acfc8a0c7902f9` |
| Gate host BuildID | `750e069af15830c43b0d063eae48ad421cb44318` |
| Bak (gitignored — T4 OK) | 6 files: `.bak`, `.bak.pre_cap_g1`, `.bak.pre_capg1`, `.bak.pre_cap_g2`, `.bak.t3pre`, `.bak.t5live` |
| Branch | `main...origin/main` **ahead 28** (no push this peer) |
| CAP-G3 mandate | unused V2 grants wire/remove; must not break CAP-G1/G2 path-A or T3/T4 |

### WT observed at peer run (context only; not edited)

~104 porcelain lines; large tracked `oodac/*` dirty surface (emit/check/parse/tc
WIP under concurrent CAP meta lane). This peer **did not** edit product,
runtime, scripts, or hygiene — audit only.

### CAP-G3 residual surface (observed, not owned)

Per `CAPS.oot` / `SPRINT.oot`:
- Emit injects legacy grant set + **partial** granular (`tcp` / `fsread` /
  `process` today); full main-param grant matrix for every V2 name residual
  (**CAP-G3**).
- Unused V2 grants (`ProcessCap` / `SyncCap` / `MemCap` / HW / UI…) — wire
  product require path or honest residual-only (closes **4.3** when done).
- CAP-G1/G2 path-A maps already FIXED; this peer only re-proves their smokes.

========================================================================
1) cap_g1_net_granular_smoke.sh — PASS (must stay green)
========================================================================

```
OODAC_BIN=./oodac/oodac bash scripts/cap_g1_net_granular_smoke.sh
# exit 0
```

Live transcript:

```
OK static sealed_net_kind_of map (tcp_bind→BindCap)
OK static runtime oo_cap_require_http
OK check deny HttpCap+tcp_bind
OK check deny TcpCap+fetch
OK check allow NetCap+fetch+tcp_connect
OK check allow TcpCap+tcp_connect
OK check allow HttpCap+fetch
OK check allow NetCap+fetch (legacy)
OK ci_product soft-wire present
cap_g1_net_granular_smoke: PASSED
```

**CAP-G1 granular net enforce still green under tip host while CAP-G3 residual
window is open.** Wrong-granular deny + preferred Http/Tcp + legacy NetCap
allow paths intact.

========================================================================
2) cap_g2_fs_granular_smoke.sh — PASS (must stay green)
========================================================================

```
OODAC_BIN=./oodac/oodac bash scripts/cap_g2_fs_granular_smoke.sh
# exit 0
```

Live transcript:

```
OK static sealed_fs_kind_of map (read_file→FsReadCap)
OK static sealed_fs_kind_of map (write_file→FsWriteCap)
OK static runtime require_fsread / oo_cap_require_fsread
OK check deny FsReadCap+write_file
OK check deny FsWriteCap+read_file
OK check allow FsCap+read+write
OK check allow FsReadCap+read_file
OK check allow FsWriteCap+write_file
OK check allow FsCap+read_file (legacy)
OK check allow FsCap+write_file (legacy)
OK ci_product soft-wire present
cap_g2_fs_granular_smoke: PASSED
```

**CAP-G2 granular fs enforce still green under tip host.** Wrong-granular deny
(FsRead↛write; FsWrite↛read) + preferred FsRead/FsWrite + legacy FsCap allow
paths intact. Soft-wire in `ci_product` present.

========================================================================
3) runtime_zt_t3_smoke.sh — PASS
========================================================================

```
OODAC_BIN=./oodac/oodac bash scripts/runtime_zt_t3_smoke.sh
# exit 0
```

Live transcript:

```
OK 1.8 static: DE1.8 + OODA_/OO_ prefix filter markers in chs_rt_sys.c
OK 1.8 runtime C harness (emit path unclean/unavailable)
OK 1.8 product: sys_exec child lacks dangerous env (ENV_CLEAN)
OK 1.10 static: FFI handle table mutex present (g_ffi_handles_mu)
OK 4.5 static: nested dlopen refuse residual present
OK soft cap_ffi_runtime_smoke.sh exit=1 (residual/host; not T3 hard fail)
OK soft ffi_dlopen_path_a_smoke.sh exit=1 (residual/host; not T3 hard fail)
OK 6.4 static: OODA_FS_WRITEDIR fail-closed markers in chs_rt_fs.c
OK 6.4 runtime C harness (emit path unclean/unavailable)
OK 6.4 product: WRITEDIR unset fail-closed (IN_ERR/OUT_ERR)
OK 6.4 product: inside WRITEDIR OK / outside refused
OK ci_product wires runtime_zt_t3_smoke
runtime_zt_t3_smoke: PASSED
```

**T3 runtime ZT floors still green under tip product host.**

### Soft residual note (not a peer soft-skip)

| Sub-rail | Result | Policy |
|----------|--------|--------|
| `cap_ffi_runtime_smoke.sh` | exit 1 soft-OK | T3 rollup soft rail |
| `ffi_dlopen_path_a_smoke.sh` | exit 1 | residual/host; rollup does **not** hard-fail |
| Hard claims 1.8 / 1.10 / 4.5 / 6.4 + ci wire | all OK | hard path |
| Note | emit path unclean/unavailable | product harness path used for 1.8/6.4 |

========================================================================
4) hygiene_t4_smoke.sh — PASS
========================================================================

```
bash scripts/hygiene_t4_smoke.sh
# exit 0
```

Live transcript:

```
OK bash -n hygiene_t4_smoke.sh
OK oodac.bak* present and gitignored (6 file(s))
OK no patch_awk / boot_dbg / cap-strip scripts
OK tc_control_cond.oo: no DBG_/DEBUG println (ERR type OK)
hygiene_t4_smoke: PASSED
```

**T4 hygiene floor still green** (6 bak files gitignored, no cap-strip debris,
DEBUG strip on `tc_control_cond.oo`).

========================================================================
VERDICT
========================================================================

| Item | Status |
|------|--------|
| `cap_g1_net_granular_smoke.sh` | **PASS** (exit 0) — must-stay-green **holds** |
| `cap_g2_fs_granular_smoke.sh` | **PASS** (exit 0) — must-stay-green **holds** |
| `runtime_zt_t3_smoke.sh` | **PASS** (exit 0; soft ffi residuals) |
| `hygiene_t4_smoke.sh` | **PASS** (exit 0) |
| Soft-skip of assigned hard gates | **none** |
| Peer product fix | **none** (OWN: audit only) |
| Push | **none** |
| CAP-G3 FIXED claim | **none** (residual only; regression re-prove) |
| **OVERALL** | **PASS** |

### Leave-off (≤15 lines)

- Floor regression for CAP-G3 window: CAP-G1 + CAP-G2 + T3 + T4 **PASS** on
  tip host `oodac/oodac` (949456 B, BuildID 750e069a…, tip `0cbc4aa`).
- CAP-G1 must-stay-green: wrong-granular deny + preferred Http/Tcp + legacy NetCap.
- CAP-G2 must-stay-green: wrong-granular deny + preferred FsRead/FsWrite + legacy FsCap.
- T3 soft: `cap_ffi_runtime` + `ffi_dlopen_path_a` exit 1 (rollup soft-OK policy).
- T4: 6 bak files gitignored; no strip debris; DEBUG clean.
- CAP-G3 residual unchanged: full main-param grant inject + unused V2 grants
  (Process/Sync/Mem/HW/UI) still open — peer did not claim FIXED.
- Branch ahead 28 of origin — **no push** per OWN. No product edits by this peer.

end of cap_g3_peer3_regression.oot
