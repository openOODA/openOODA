# cap_g6_peer3_regression.oot — G1–G5 floor regression peer
# Date: 2026-08-12
# Role: Peer3 (do not regress prior CAP-G* path-A)

## Floors

| Smoke | Expect | Result |
|-------|--------|--------|
| `cap_g1_net_granular_smoke.sh` | exit 0 | **PASS** (via cap_g6 harness) |
| `cap_g2_fs_granular_smoke.sh` | exit 0 | **PASS** |
| `cap_g3_grant_inject_smoke.sh` | exit 0 | **PASS** |
| `cap_g4_process_smoke.sh` | exit 0 | **PASS** |
| `cap_g5_seccomp_honesty_smoke.sh` | exit 0 | **PASS** |
| `cap_g6_std_migrate_smoke.sh` | exit 0 | **PASS** |

## Honesty

CAP-G6 is **std facade migration**, not re-open of G1–G5 enforce maps.
Wrong-granular deny remains check path-A. ProcessCap/SysCap hard split intact.
Seccomp still residual-only (no product BPF claim).
