# cap_g6_phalanx_rollup.oot — CAP-G6 std migration (net+fs wave)
# Date: 2026-08-12
# Role: Close Coordinator CAP-G6
# Decision: **FIXED** for **net+fs product facade wave**; SysCap residual bulk **named**

## Close criteria

| # | Criterion | Result |
|---|-----------|--------|
| 1 | Net facade `http_get` is `&HttpCap` (not NetCap-only) | **PASS** — `ooda/std/os/net.oo` + `std/src/net.oo` |
| 2 | FS read/write facades granular (FsReadCap / FsWriteCap) | **PASS** — `ooda/std/os/fs.oo` + `std/src/fs.oo` |
| 3 | Peer audits present | **PASS** — `cap_g6_peer1_facades.oot`, `cap_g6_peer3_regression.oot` |
| 4 | G1–G5 floors green | **PASS** — smokes exit 0 |
| 5 | Inventory documents SysCap residual bulk | **PASS** — `cap_g6_syscap_inventory.oot` (~571 std/src hits; ooda process/async/python facades) |
| 6 | `cap_g6_std_migrate_smoke.sh` | **PASS** exit 0 |
| 7 | Push after green | **YES** (user allowed this session) |

## Decision honesty

**FIXED path-A for the net+fs facade wave.** Full std SysCap purge is **not** claimed.
Domain stubs and process facades remain SysCap (inventory). No greenwash of residual
crypto/VPN as product because a cap token moved (`wireguard` stays fail-closed residual).

## Product land

| Repo | Surface | What |
|------|---------|------|
| **ooda** | `std/os/net.oo` | `http_get`/`net_get` → HttpCap; tcp* → TcpCap; bind → BindCap; udp → UdpCap; sock_raw → NetCap |
| **ooda** | `std/os/fs.oo` | read/path/size → FsReadCap; write → FsWriteCap |
| **ooda** | `scripts/cap_g6_std_migrate_smoke.sh` | path-A gate + residual counts |
| **ooda** | `scripts/ci_product.sh` | soft-wire G6 smoke |
| **std** | `src/net.oo` | http_get/net_get → HttpCap |
| **std** | `src/fs.oo` | fs_read → FsReadCap; fs_write → FsWriteCap |
| **std** | domain wave | wireguard → UdpCap residual; mTLS session → TcpCap; related cps/ai honesty notes |
| **openOODA** | CAPS.oot / SPRINT.oot | CAP-G6 FIXED net+fs; residual named |
| **openOODA** | `audit/cap_g6_*` | rollup + inventory + peers |

## Prove (Close Coordinator host)

| Gate | Result |
|------|--------|
| `cap_g1_net_granular_smoke.sh` | exit **0** |
| `cap_g2_fs_granular_smoke.sh` | exit **0** |
| `cap_g3_grant_inject_smoke.sh` | exit **0** |
| `cap_g4_process_smoke.sh` | exit **0** |
| `cap_g5_seccomp_honesty_smoke.sh` | exit **0** |
| `cap_g6_std_migrate_smoke.sh` | exit **0** — HttpCap/FsReadCap facades + SysCap bulk note |

## Residual (not CAP-G6 fail)

| Item | Status |
|------|--------|
| `ooda/std/os/process.oo` still `&SysCap` (ProcessCap facade next wave) | named |
| `async.oo` / `python.oo` hard Sys residual | named |
| ~571 `&SysCap` lines under `std/src` domain stubs | inventory |
| Remaining `&NetCap` / `&FsCap` outside product facades | report in smoke |
| OS isolation / landlock / seccomp product | CAP-G5 residual |
| CAP-G7 CAPS_MATRIX → .oot format | optional |

## Probe note

CAP-G1–G5 enforce maps already path-A. CAP-G6 moves **std call-site contracts** onto preferred tokens so new code defaults least-privilege. Legacy NetCap/FsCap/SysCap supersede remains for one-wave compat at check+runtime.
