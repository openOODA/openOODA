# cap_g6_std_honesty.oot — Docs Cartographer CAP-G6 residual ledger
# Date: 2026-08-12
# Role: Docs Cartographer (CAP-G6)
# Tree: openOODA/CAPS.oot + ooda/bootstrap/CAPS_MATRIX.md + STATIC_CAPS.md
# OWN: docs honesty only; no product feature work; **no push** (C2 / human gate)
# WRITE: this audit + CAPS.oot CAP-G6 status + CAPS_MATRIX/STATIC_CAPS std notes

========================================================================
SUMMARY
========================================================================

| Topic | VERDICT | Notes |
|-------|---------|-------|
| CAP-G6 net+fs product facade wave | **FIXED path-A** | per `cap_g6_phalanx_rollup.oot`; smoke exit 0 |
| SysCap bulk residual | **NAMED honesty** | ~571 `&SysCap` under `std/src`; process/async/python facades |
| ProcessCap in std | **0** | residual next wave — not this close |
| Full std least-privilege | **NOT claimed** | do not greenwash domain stubs |
| CAPS.oot CAP-G6 status | **UPDATED** | FIXED net+fs wave + SysCap bulk residual |
| CAPS_MATRIX M166 std note | **UPDATED** | product table + residual honesty |
| STATIC_CAPS M166 | **UPDATED** | preferred facades + inventory link |
| SPRINT CAP-G6 | **already green** (C2/phalanx) | Docs Cartographer did not re-close; matched status |
| Push | **none this pass** | user: no push until C2 |

OVERALL: **HONEST RESIDUAL PASS** — docs match FIXED net+fs wave + SysCap bulk residual.

========================================================================
1. LIVE RECHECK (2026-08-12)
========================================================================

```
OODAC_BIN=./oodac/oodac bash scripts/cap_g6_std_migrate_smoke.sh
# exit 0
# REPORT: &NetCap=17 &FsCap=6 &HttpCap=8 &FsReadCap=6 &SysCap=571 (std/src)
# OK http_get uses &HttpCap (std/src/net.oo + ooda/std/os/net.oo)
# OK fs_read_file uses &FsReadCap (std/src/fs.oo + ooda/std/os/fs.oo)
# NOTE residual: &SysCap bulk remains (count=571) — not a gate
# cap_g6_std_migrate_smoke: PASSED
```

Product facades (signatures):

| File | Preferred |
|------|-----------|
| `ooda/std/os/net.oo` | Http/Tcp/Udp/Bind; sock_raw NetCap |
| `ooda/std/os/fs.oo` | FsRead / FsWrite |
| `std/src/net.oo` | HttpCap on http_get/net_get |
| `std/src/fs.oo` | FsRead / FsWrite |

Still SysCap (honest residual):

| File | Why |
|------|-----|
| `ooda/std/os/process.oo` | spawn/wait/kill/exec facade not ProcessCap yet |
| `ooda/std/os/async.oo` | join hard SysCap-only; facade unsplit |
| `ooda/std/os/python.oo` | python_embed hard SysCap-only |

========================================================================
2. DOCS TOUCHED THIS PASS
========================================================================

| Doc | Change |
|-----|--------|
| `openOODA/CAPS.oot` | STATUS banner + Path-A CAP-G6 row + SysCap/ProcessCap catalog notes + residual links + footer |
| `ooda/bootstrap/CAPS_MATRIX.md` | M166 std note → CAP-G6 product table + SysCap bulk honesty |
| `ooda/bootstrap/STATIC_CAPS.md` | M166 sample table updated for preferred facades + inventory link |
| This audit | residual ledger |

SPRINT CAP-G6 already **FIXED net+fs wave** with SysCap residual bulk inventoried
(`SPRINT.oot` pin + table row). C2 / Close Coordinator owns SPRINT close; Docs
Cartographer **aligned** CAPS/MATRIX only — no second SPRINT thrash.

========================================================================
3. ANTI-GREENWASH
========================================================================

| Claim | Truth |
|-------|-------|
| "CAP-G6 full std migration done" | **FALSE** — SysCap bulk residual ~571; ProcessCap = 0 |
| "CAP-G6 net+fs product facades FIXED path-A" | **TRUE** — smoke + peers + rollup |
| "process.oo is ProcessCap" | **FALSE** — still SysCap (next residual wave) |
| "domain SysCap stubs are least-privilege complete" | **FALSE** — inventory default-deny gates, not ProcessCap map |

========================================================================
4. LEAVE-OFF
========================================================================

- CAPS.oot: CAP-G6 FIXED net+fs facade wave; **SysCap bulk residual** named.
- CAPS_MATRIX + STATIC_CAPS M166: product preferred tokens; residual honesty.
- SPRINT already green (C2); Docs Cartographer did not re-open/close.
- Smoke `cap_g6_std_migrate_smoke` re-proved exit 0; SysCap=571 NOTE.
- **No push** this Docs Cartographer pass.

end of cap_g6_std_honesty.oot
