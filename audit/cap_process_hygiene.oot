# cap_process_hygiene.oot — Hygiene Enforcer (ProcessCap std facades)
# Date: 2026-08-12T14:05:49Z
# Role: Hygiene Enforcer — RULES §1.7 (cap_process wave)
# Tree: /home/jeryd/Projects/openOODA  (ooda/ + std/ + openOODA/)
# Tip (ooda): 2c7ce49 (working tree dirty — process/async/python facades + new smoke)
# Tip (std):  b99aa66 (working tree dirty — async.oo + python.oo twins)
# Tip (openOODA): 70d1e10
# OWN: line-lock process.oo / async.oo / python.oo (product + external twins);
#      bash -n new cap_process_facade_smoke + related process smokes;
#      no /tmp secrets; no mass deletes; no push
# WRITE: this audit only
# POLICY: RULES §1.7 / PROBE MAX_LINES = 256 (soft); pack watch 300; hard ban >600
#         No mass peels/deletes this pass (DE mid-flight on ProcessCap facades)

========================================================================
SUMMARY
========================================================================

| Check | VERDICT | Notes |
|-------|---------|-------|
| Soft lock ≤256 (OWN five) | **PASS** | worst `process.oo` **55** (−201 headroom) |
| Pack pressure ≤300 | **PASS** | 0/5 over 300 |
| Hard ban >600 | **PASS** | 0 OWN files >600 |
| Split this pass | **NOT needed** | all far under soft; no peel |
| `bash -n` **new** `cap_process_facade_smoke.sh` | **PASS** | 129 lines; syntax clean; untracked |
| `bash -n` related process smokes | **PASS** | 3/3 syntax clean (g4/libfloor/g6) |
| `/tmp` secrets in OWN + smokes | **PASS** | ZERO secrets; OWN free of `/tmp`; smokes use `$HOME/.cache/ooda-tmp` |
| Mass deletes | **none** | charter |
| Push | **no** | charter |

OVERALL: **PASS** (OWN line-lock green; new smoke bash -n green; secrets clean;
ProcessCap facade WIP named — not SPRINT-closed by hygiene alone)

========================================================================
SCOPE + METHOD
========================================================================

OWN units (process facade surface — CAP-G4 path-A + CAP-G6 residual wave):

  Dirty this wave (DE WIP — measured live):

  `ooda/std/os/process.oo`   — ProcessCap preferred spawn/wait/kill/exec;
                               SysCap hard residual epoll/inotify/prctl
  `ooda/std/os/async.oo`     — spawn → ProcessCap; join stays SysCap hard
  `ooda/std/os/python.oo`    — load_model stays SysCap hard (python_embed)
  `std/async.oo`             — external twin of product async
  `std/python.oo`            — external twin of product python

  Note: no external `std/**/process.oo` twin (product-only process facade).

Related (bash -n):

  **NEW** `ooda/scripts/cap_process_facade_smoke.sh` (?? untracked)
  `ooda/scripts/cap_g4_process_smoke.sh`
  `ooda/scripts/libfloor_process_smoke.sh`
  `ooda/scripts/cap_g6_std_migrate_smoke.sh` (adjacent std migrate floor)

Commands:

  `wc -l` + formal cap-token inventory on OWN five
  `git diff` HEAD vs working for migration honesty
  `bash -n` on new facade smoke + 3 related process/std smokes
  `grep` secret-pattern + `/tmp` scan on OWN + smokes
  no mass delete; no push; no product source edits this pass

Thresholds:

| Threshold | Meaning |
|-----------|---------|
| **256** | RULES §1.7 / PROBE MAX_LINES — soft lock |
| **300** | Pack monofile pressure (flag for DE peel) |
| **600** | Path-A hard ban — still green for OWN |

========================================================================
LINE LOCK — OWN FIVE (live)
========================================================================

Measured under monorepo at tips ooda=`2c7ce49`+WIP, std=`b99aa66`+WIP
(2026-08-12T14:05Z).

| File | Lines | ≤256 | ≤300 | ≤600 | Headroom 256 | Headroom 300 | Split? |
|------|------:|:----:|:----:|:----:|-------------:|-------------:|:------:|
| `ooda/std/os/process.oo` | **55** | PASS | PASS | PASS | +201 | +245 | NO |
| `ooda/std/os/async.oo` | **30** | PASS | PASS | PASS | +226 | +270 | NO |
| `ooda/std/os/python.oo` | **24** | PASS | PASS | PASS | +232 | +276 | NO |
| `std/async.oo` | **30** | PASS | PASS | PASS | +226 | +270 | NO |
| `std/python.oo` | **24** | PASS | PASS | PASS | +232 | +276 | NO |
| **OWN total** | **163** | — | — | — | — | — | — |

Committed baselines (HEAD only, for delta):

| File | HEAD lines | Working | Δ | Cap migration (work tree) |
|------|----------:|--------:|--:|---------------------------|
| `ooda/std/os/process.oo` | 54 | 55 | **+1** | spawn/wait/kill/exec → `&ProcessCap`; epoll/inotify/prctl stay `&SysCap` |
| `ooda/std/os/async.oo` | 26 | 30 | **+4** | spawn → `&ProcessCap`; join hard `&SysCap` (honest split) |
| `ooda/std/os/python.oo` | 19 | 24 | **+5** | stays `&SysCap` (banner honesty only; no ProcessCap migrate) |
| `std/async.oo` | 26 | 30 | **+4** | mirrors product async split |
| `std/python.oo` | 19 | 24 | **+5** | mirrors product python hard-Sys residual |

Delta notes (ProcessCap facade land pressure):

- **process +1**: dual-cap formals + short honesty banner. Soft-lock trivial.
  Preferred: `process_spawn` / `process_wait` / `process_kill` / `process_exec`
  take `&ProcessCap`. Hard residual: `process_epoll_create` / `process_inotify_init`
  / `process_prctl` stay `&SysCap`. `main` dual-cap probe (ProcessCap + SysCap).
- **async +4**: spawn/join honesty split — ProcessCap preferred for
  `async_spawn_internal`; join remains hard SysCap-only (ProcessCap alone must
  not unlock join). Growth is banner + param rename only.
- **python +5**: **correct non-migrate** — `python_embed_internal` is hard Sys
  residual per CAP-G4 map. Extra lines are honesty comments (do not claim
  ProcessCap for AI bridge). Alpha still fail-closed Err (no CPython).
- External twins (`std/async.oo`, `std/python.oo`) match product line counts
  and cap formals (parity holds this wave).
- Diffstat (ooda product): process/async/python dirty; new smoke **??**.

========================================================================
FORMAL CAP MAP (hygiene observe — not prove)
========================================================================

Product `ooda/std/os/process.oo` (live):

| Facade | Formal | Class |
|--------|--------|-------|
| `process_spawn` | `&ProcessCap` | preferred (SysCap supersedes at check/runtime) |
| `process_wait` | `&ProcessCap` | preferred |
| `process_kill` | `&ProcessCap` | preferred |
| `process_exec` | `&ProcessCap` | preferred (real fork+execvp+waitpid path) |
| `process_epoll_create` | `&SysCap` | hard residual |
| `process_inotify_init` | `&SysCap` | hard residual |
| `process_prctl` | `&SysCap` | hard residual |
| `main` | `&ProcessCap` + `&SysCap` | dual honesty probe |

Product `ooda/std/os/async.oo` (live):

| Facade | Formal | Class |
|--------|--------|-------|
| `spawn_task` | `&ProcessCap` | preferred (→ async_spawn_internal) |
| `join_task` | `&SysCap` | hard residual (→ async_join_internal) |
| `main` | `&ProcessCap` + `&SysCap` | multi-cap honesty probe |

Product `ooda/std/os/python.oo` (live):

| Facade | Formal | Class |
|--------|--------|-------|
| `load_model` | `&SysCap` | hard residual only (python_embed) |
| `main` | `&SysCap` | fail-closed Err probe |

Honesty (do not greenwash):

1. ProcessCap on process spawn/exec facades is **WIP progress** on dirty tree —
   not a hygiene claim that SPRINT CAP-process is FIXED/shipped.
2. `python.oo` remaining on SysCap is **correct residual**, not debt to force-migrate.
3. async join on SysCap is **correct residual** (ProcessCap↛join by design).
4. External `std/` has **no** `process.oo` twin — product-only process facade.
5. CAP-G4 sealed-op map (sys_exec ProcessCap preferred; prctl SysCap-only)
   remains the runtime truth; facades now align formals to that map on WIP.

========================================================================
SPLIT GATE (not exercised this pass)
========================================================================

Rule: if OWN unit over soft lock and peel is **trivial + DE-authorized**,
split at functional seams. Charter this pass: **no mass deletes / no mass peel**.

| Unit | Over? | Trivial peel? | Action |
|------|:-----:|:-------------:|--------|
| all OWN five | no | n/a | none — max 55 lines |
| process dual-cap | no | optional later | keep single file; seams already sectioned by formal type |

Suggested DE (when authorized, not this hygiene pass):

1. Land `cap_process_facade_smoke.sh` + wire after `cap_g6_std_migrate_smoke`
   in `ci_product.sh` (smoke already soft-checks wire).
2. Prove product `process.oo` check + ProcessCap-only exec path (peer/integration).
3. Do **not** migrate `python_embed` or async join to ProcessCap (would be dishonest).

========================================================================
bash -n (new smoke + related)
========================================================================

```
# NEW process facade smoke (untracked on ooda tip 2c7ce49)
bash -n scripts/cap_process_facade_smoke.sh   # OK  129

# Related process / std floors
bash -n scripts/cap_g4_process_smoke.sh       # OK   66
bash -n scripts/libfloor_process_smoke.sh     # OK  158
bash -n scripts/cap_g6_std_migrate_smoke.sh   # OK  156
# all present: exit 0
```

ALL_BASHN_OK = true (4/4)
NEW_CAP_PROCESS_SMOKE_SH = **present** (`cap_process_facade_smoke.sh`, ??)

New smoke intent (static + check, not executed this hygiene lane):

  1. Static: `process_exec` formal is `&ProcessCap`
  2. Static: `process_prctl` formal stays `&SysCap`
  3. Check deny: ProcessCap-only + `sys_prctl`
  4. Check allow: ProcessCap + `sys_exec`
  Soft: `oodac check` on product `std/os/process.oo`; optional ci_product wire

Temps: `TMP="${TMPDIR:-$HOME/.cache/ooda-tmp}/cap_process_facade_smoke_$$"`
+ `trap 'rm -rf "$TMP"' EXIT` — hygiene bar for temps satisfied in script shape.

========================================================================
/tmp + SECRETS
========================================================================

### OWN five

Scan secret-like patterns (`password=`, `BEGIN *PRIVATE`, api keys, token
material) + literal `/tmp`:

  - `ooda/std/os/process.oo` — **clean**
  - `ooda/std/os/async.oo` — **clean**
  - `ooda/std/os/python.oo` — **clean**
  - `std/async.oo` — **clean**
  - `std/python.oo` — **clean**

### New + related smokes

  - `cap_process_facade_smoke.sh` — **clean**; TMPDIR → `~/.cache/ooda-tmp`
  - `cap_g4_process_smoke.sh` — **clean**; same TMPDIR pattern
  - `libfloor_process_smoke.sh` — **clean**; `export TMPDIR=…/ooda-tmp`
  - No password/key/token material in script bodies

VERDICT `/tmp` secrets: **PASS**

========================================================================
ACTIONS / NON-ACTIONS THIS PASS
========================================================================

| Action | Result |
|--------|--------|
| Measure OWN five line locks | **done** — 55/30/24/30/24 all under soft |
| Flag soft/pack residual on OWN | **none** (OWN all under soft+pack) |
| Split oversize OWN units | **none** (not over; no peel) |
| Mass deletes (bak, /tmp, monofiles) | **none** |
| `bash -n` new facade smoke | **PASS** (129L) |
| `bash -n` related process smokes | **3/3 OK** |
| Purge `/tmp` secrets from committed surface | already clean; no edit |
| Product / std source edits | **none** (hygiene audit only) |
| Mark SPRINT ProcessCap facades closed | **no** (WIP measure; peers prove) |
| Push | **no** |
| Audit write | **this file only** |

========================================================================
RETURN — COMPACT
========================================================================

```
SCOPE   cap_process OWN five  ooda=2c7ce49+WIP  std=b99aa66+WIP  openOODA=70d1e10
OWN     process=55  async=30  python=24  std/async=30  std/python=24  total=163
ΔHEAD   process=+1  async=+4  python=+5  std twins match product Δ
>600    0 OWN   PASS hard ban
>300    0 OWN   PASS pack
>256    0 OWN   PASS soft  (worst process +201 headroom)
BASHN   NEW cap_process_facade_smoke.sh OK (129); related 3/3 OK
/tmp    PASS  no secrets; smokes use ~/.cache/ooda-tmp + trap EXIT
SPLIT   none (under soft)
PUSH    no
CAPS    process: exec/spawn/wait/kill → ProcessCap; prctl/epoll/inotify → SysCap
        async: spawn → ProcessCap; join → SysCap hard
        python: load_model → SysCap hard (correct residual)
OVERALL PASS
```

end of cap_process_hygiene.oot
