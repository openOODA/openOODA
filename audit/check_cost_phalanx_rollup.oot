# check_cost phalanx rollup — COORD
# Date: 2026-08-13
# Role: COORD. This file only. Does not edit SPRINT.oot.
# Axiom: RULES 1.6 prove. 1.9 zero trust. 1.10 short words. 1.11 compete.
# Goal: keep cutting oodac check cost.
# Biology: Immune 1.6, Darwin 1.11, Apoptosis SWARM §3, Zero trust 1.9.

## Lane map (fixed)

DE1 `check_drive.oo` | DE2 `check_mod.oo` | RT `chs_rt_str2.c`
PV1 PV2 SEC MEM read-only | INT run | HARN `fixtures/check_cost_*`
HYG read | DOCS `audit/check_cost_fast.oot` | COORD this file

Two writers never shared a file.

## What COORD checked (1.9)

| Role | Own | Live |
|------|-----|------|
| DE1 | `oodac/check_drive.oo` | I17 in: one `tokenize_lines`. No second `lex_all`. 156 lines. |
| DE2 | `oodac/check_mod.oo` | `np>12` still N times full check. `names = names +`. 101 lines. |
| RT | `runtime/chs_rt_str2.c` | ASCII intern. Kind intern. Slice intern. 203 lines. |
| HARN | `fixtures/check_cost_*` | Missing. Immune 1.6 not met. |
| DOCS | `audit/check_cost_fast.oot` | Wrote. Clock not re-run here. |
| HYG | line lock | Three owned files under 256. |
| PV SEC MEM INT | read / run | No this-swarm `.oot`. COORD did not run check. |

List in-place push is in `chs_rt_list.c` (not RT this lane). `field_at` walks bytes in `token_fmt.oo`.

## Clock

COORD did not time `oodac check oodac/main.oo`. No live wall. No live RAM.

DOCS table: ~30 s / ~17.5 GiB then ~20 s / ~7 GiB after intern + field_at + list.
That is DOCS. Not this clock.

SPRINT pin: 28 s / 12 GB after B8. Also not this clock.

False: 1.7 s / 41 MB. False: 39 MB as the live floor.

## B6 — do not [x] as cheap

B6 is a cap-name short path in `tc_call_util.oo`. Not DE1/DE2 this lane.
No timed check. Immune 1.6 fails. Darwin 1.11: "cheap" dies. Intern and list stay.

## What changed / why it matters / not done / next

Faster source is on disk (intern, I17, list push). Check is still long and fat.
Do not close cheap. Do not copy a clock you did not run.

Residual: `check_mod` N-way tokenize. Name concat. `tok_line` char concat.
B2 B4 B6 B7 Task 8 open.

SPRINT recommend (this file does not edit the board): do not [x] B6.
Leave the pin. Time the live ELF if you want a number.
Do not spawn more agents on one stuck clock (Apoptosis SWARM §3).
Land pass + fail `fixtures/check_cost_*` before any cheap claim.

Marker: `CHECK_COST_PHALANX_2026-08-13`
