# crush_docs.oot — Documentation Writer deletion log
# Date: 2026-08-12
# Role: Documentation Writer (crush swarm)
# OWN: SPRINT deletions of proved-closed items. PM residual on RFC-002 only.
#      Write this ledger. **No push.** Do not restore the old 368-line SPRINT.

========================================================================
PLAIN ENGLISH (RULES 1.10)
========================================================================

**What changed**
Closed findings were taken off `SPRINT.oot`. Open CRITs stayed.

**Why it matters**
A sprint list that still names closed bugs is pride. Agents would
re-fix honest docs or chase holes that are not holes.

**What is not done**
F26 (p7 check still times out). F27 (compiler files over 256 lines).
RFC-003 still overclaims args/walk (F19). No push.

========================================================================
SESSION
========================================================================

| Field | Value |
|-------|-------|
| ROOT | `/home/jeryd/Projects/openOODA` |
| HAS_DESIGN | yes (`openOODA/DESIGN.oot`) |
| SWARM plan | solo — SPRINT + PM residual + this ledger |
| Hygiene | this file + SPRINT + PM ≤256 |
| Smokes (suspects) | `p7_check_main_smoke.sh` **exit 1** (F26 kept) |
| Push | **none** |

========================================================================
DELETED FROM SPRINT.oot
========================================================================

Compared to the 135-line crush-start board (7 CRIT + F10–F12 HIGH).
Compact format kept (now **127** lines). Not the old 368-line file.

| ID | Was | Why deleted | Proof |
|----|-----|-------------|-------|
| **F14** | CRIT phantom net paths | **CLOSED.** Five files absent. False ledger deleted. | `audit/f14_phantom_net_close.oot` + `audit/f14_domain_a.oot`. Live: `dns.oo` + `wireguard.oo` only. |
| **F31** | CRIT 8 crypto files phantom | **RESTATED.** Pack no longer cites missing paths as gates. | `bootstrap/STDLIB_CRYPTO_MATH.oot` + `audit/f31_domain_b.oot` (3 exist / 5 phantom). |
| **F38** | CRIT gate-shape drift | **RESTATED.** Shapes A–D + two-tree scope named. | same pack + `f31_domain_b.oot`. F38b `sha256` name collision **residual**. |
| **F32** | CRIT Process.Args phantom | **RESTATED.** RFC says **not shipped**. Named-gap **open**. | `RFC-002-StdLib_Completion.oot` + `audit/f32_rfc002_honesty.oot`. |
| **F33** | CRIT FS.Walk phantom | **RESTATED.** RFC says **not shipped**. Named-gap **open**. | same. |
| **F10** | HIGH FsReadCap→write | **NOT_A_BUG.** check deny rc=1 ×2 | `audit/crush_peer2.oot` + `audit/f10_cap_stale.oot` |
| **F11** | HIGH FsWriteCap→read | **NOT_A_BUG.** check deny rc=1 ×2 | same |
| **F12** | HIGH HttpCap→sock_raw | **NOT_A_BUG.** check deny rc=1 ×2 | same |
| F32/F33 + F31/F38 "CRIT closed" note | honesty subsection | User: **remove** restated bullets. Ledgers exist. | this file |
| F10–F12 "HIGH closed" note | closed subsection | User: **remove** NOT_A_BUG bullets. | this file |

Also gone from the CRIT list (already absent when Docs took the board,
or removed this turn): the F14 bullet itself.

**Not a product ship.** F32/F33 close is honesty only. Args/walk still
do not exist.

========================================================================
KEPT ON SPRINT (still open)
========================================================================

| ID | Why kept |
|----|----------|
| **F26** CRIT | Docs re-ran `ooda/scripts/p7_check_main_smoke.sh` at 10:10. `OK tiny main.oo`. `FAIL oodac/main.oo check timeout 45s`. Exit **1**. Host `ooda/oodac/oodac` 963080 B. User: remove only if smoke 0. |
| **F27** CRIT | `wc -l ooda/oodac/*.oo` still has 7 files >256. Open. |
| AST Deadlock `[x]` | User: remove only if p7 smoke 0. Smoke is **not** 0. Build-parallel note stays; F26 owns the check timeout. |
| F19 HIGH | RFC-003 still says Args/Walk are usable. RFC-002 only was restated. |
| F15 F16 F20 F23 F34 F39 F36 | HIGH residual. Not in this OWN. |
| MED/LOW list | Not proved closed this turn. |

========================================================================
PM.oot (residual only)
========================================================================

| Edit | Why |
|------|-----|
| Open row: RFC-002 named-gap **residual** | Honest: modules not shipped |
| §5.4 note | Same residual on std philosophy |
| Residual index | `f32_rfc002_honesty.oot` + restated crypto pack |
| Dropped "T1–T5" pointer | Compact SPRINT has no T1–T5 |
| §3.1 | **not rewritten** — Domain A already named F14 absent |

Did **not** flip R2/R3/R5 `done`. Did **not** expand PM.

========================================================================
ANTI-GREENWASH
========================================================================

| Claim | Truth |
|-------|-------|
| "F26 closed because AST is [x]" | **FALSE** — p7 still exit 1 |
| "F32/F33 closed means args/walk exist" | **FALSE** — restatement only |
| "F31 closed means 8 gates exist" | **FALSE** — 5 phantom; 3 are helpers |
| "F10–F12 were fail-open" | **FALSE** — live check denies |
| "368-line SPRINT restored" | **FALSE** — 127 lines |

========================================================================
NEXT
========================================================================

1. Compiler: F26 rebuild so `p7_check_main_smoke.sh` exit 0, then
   Docs may drop F26 + the AST `[x]` row.
2. Compiler: F27 split files over 256.
3. F19: restate RFC-003 args/walk bullets.
4. Human: no auto-push.

end of crush_docs.oot
