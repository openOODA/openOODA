# crush_hygiene.oot — Hygiene Enforcer (F15/F16/F17/F18 MED crush)
# Date: 2026-08-12T20:09:54Z
# Role: Dependency/Hygiene Enforcer — RULES §1.7 (audit-2026-08-12 crush)
# Tree: /home/jeryd/Projects/openOODA  (ooda/ + openOODA/ + std/)
# Tip (super):    eaa555d
# Tip (ooda):     52cc4b5 + WIP (secret/2.10 family; not this pass)
# Tip (openOODA): 81f56fc + WIP boards/audits; this file new
# Tip (std):      77fb876
# OWN: delete empty `issue_2.10.patch`; crush F15–F18 disk sludge;
#      do **not** rm useful trees; hard monofile >600 = 0 on product;
#      write this audit; **no push**
# WRITE: this audit + hygiene-only untracked/ignored debris deletes
# POLICY: RULES §1.7 / PROBE MAX_LINES = 256 (soft); pack watch 300;
#         hard ban >600. No mass peels. No product logic rewrite.
# Ledger: `audit/audit_2026-08-12_run.oot` + `SPRINT.oot` F15–F18/F28/F30
# Severity this wave: **MED** (lifecycle / hygiene debt). F15/F16 were
# HIGH on the ledger then F30-downgraded (already gitignored). Still
# crushed as named disk clutter.

========================================================================
SUMMARY
========================================================================

| Check | VERDICT | Notes |
|-------|---------|-------|
| F17 empty `issue_2.10.patch` | **PASS** (deleted) | 0 B leftover of `issue_2_10.patch` (3888 B, kept) |
| F17 twin `ooda/issue2.10.patch` | **PASS** (deleted) | also 0 B; same typo class |
| F15 root emit-C | **PASS** (deleted) | `abs_out.c` 12293 B + `check_caps_out.c` 15526 B |
| F16 root generator | **PASS** (deleted) | `load_import_temp.oo` 8126 B + `make_load_import.py` 8110 B |
| F18 root `tmp_*.oo` | **PASS** (deleted) | **11** files (1097 B). Not 14 — see trees |
| Useful trees kept | **PASS** | `tmp_test/` tracked; `scratch/`; `std/src/`; `ooda/` |
| Hard ban >600 (oodac `.oo`) | **PASS** | **0** over 600 |
| Hard ban >600 (runtime C/H) | **PASS** | **0** over 600 |
| Hard ban >600 (this OWN) | **PASS** | deletes only; audit ≤256 |
| std/src catalog >600 | **residual** | **12** pre-existing. **Not** peeled. **Not** rm'd |
| Secrets in crushed files | **PASS** | ZERO key/token/PEM hits before delete |
| Product source edit | **none** | |
| Mass `rm -rf` of trees | **none** | file-only `rm -f` of named debris |
| Push | **no** | charter |

OVERALL: **PASS**
  (empty patch gone; F15–F18 sludge gone; useful trees intact;
   product monofile >600 = 0; catalog-12 named residual; no push)

========================================================================
SCOPE + METHOD
========================================================================

Crush wave = SPRINT F15 + F16 + F17 + F18 (MED hygiene debt).
F28 is the same empty-duplicate fact as F17. F30 is the gitignore
refinement (clutter, not a git leak).

Commands:

  `ls -la` + `wc -c -l` on named debris (before)
  `git check-ignore -v` + `git ls-files` (tracked vs ignored)
  `find` empty `*.patch` (only the two 0 B files)
  `rg` secret-pattern on debris (zero hits)
  `find … | xargs wc -l | awk '$1>600'` on oodac / runtime / std/src
  `rm -f --` **named files only** (no directory wipe)
  re-`ls` / re-`find` after
  no `git add`; no commit; no push; no SPRINT flip

Thresholds:

| Threshold | Meaning |
|-----------|---------|
| **256** | RULES §1.7 / PROBE MAX_LINES — soft lock |
| **300** | Pack monofile pressure (flag for DE peel) |
| **600** | Path-A hard ban — force-split only if DE blew it |

========================================================================
F17 — empty patch (charter delete)
========================================================================

Live **before** (2026-08-12T20:09:13Z):

| Path | Bytes | Lines | Ignore | Act |
|------|------:|------:|--------|-----|
| `issue_2.10.patch` | **0** | 0 | `.gitignore:33:/*.patch` | **delete** |
| `ooda/issue2.10.patch` | **0** | 0 | `ooda/.gitignore:166:/*.patch` | **delete** |
| `issue_2_10.patch` | **3888** | 77 | same root `/*.patch` | **keep** |
| `ooda/sec_sink.patch` | **2155** | 47 | same ooda `/*.patch` | **keep** |

`issue_2_10.patch` is a real 2.10 `__sec_sink__` + guard-comment diff
(sha256 `3da1a3de0ae1e8468bddd623db86e286909c26cdacff5f5532d6a714e0c66962`).
F17/F28 say delete the **empty** duplicate, not the useful hunk.

Honesty: `audit_2026-08-12_run.oot` F17 text said `issue_2_10.patch = 0
bytes`. That was swapped. SPRINT F17 was right (`issue_2.10.patch` = 0).
Live `ls` proved the dot-name empty and the underscore-name 3888 B.

After: `find` empty `*.patch` → **none**. Both 0 B files gone.

========================================================================
F15 / F16 — gitignored generator dumps
========================================================================

| Path | Bytes | Lines | Ignore | What it was |
|------|------:|------:|--------|-------------|
| `abs_out.c` | 12293 | 241 | `/*.c` | emit-C dump (`OoStr` preamble) |
| `check_caps_out.c` | 15526 | 370 | `/*.c` | emit-C dump (seccomp includes) |
| `load_import_temp.oo` | 8126 | 272 | `/*.oo` | stale dump of `oodac/load_import.oo` (live **287**) |
| `make_load_import.py` | 8110 | 268 | `/*.py` | one-shot writer of that dump |

No product path referenced these four (`rg` over `.oo/.sh/.py/.oot`
except the ledger). Product import loader is `ooda/oodac/load_import.oo`.
All four untracked (never in git index). Deleted.

========================================================================
F18 — root `tmp_*.oo` (not the tree)
========================================================================

Ledger said **14** `tmp_*.oo` at repo root. Live count was **11** root
files + **3** tracked files under `tmp_test/`. Crushing the tree would
have been the blind `rm` this charter forbids.

Deleted (all ignored by `/*.oo`, untracked, 1–22 lines, generator toys):

`tmp_a.oo` `tmp_b.oo` `tmp_dup.oo` `tmp_main.oo` `tmp_outline.oo`
`tmp_test.oo` `tmp_test_1.oo` `tmp_test_2.oo` `tmp_test_contains.oo`
`tmp_test_set_has.oo` `tmp_test_str.oo`  — **1097** B total.

After: `ls tmp_*.oo` at super root → no such file.

========================================================================
TREES KEPT (do not rm blindly)
========================================================================

| Tree | Why kept |
|------|----------|
| `tmp_test/` | **tracked** fixture (`git ls-files`; commit `eaa555d` R2 multi-import). `a/foo.oo` + `b/bar.oo` + `main.oo` still on disk |
| `scratch/` | gitignored lab tree (F5/F29). Out of F15–F18. SCRIPTS calls `scratch/*.oot` debris — **not** a wipe this pass |
| `std/` + `std/src/` | product catalog. 12 files >600. Cohesion / RFC residual. **Not** a phantom dir |
| `ooda/` `ooda/oodac/` `openOODA/` | product + board |
| `issue_2_10.patch` + `ooda/sec_sink.patch` | non-empty ignored patches; useful hunks |

Not this charter (left on disk, still gitignored): `test.c`,
`test.c.tmp.c`, `test_pthread.c`, `gen.py`, `rewrite.py` (F5-adjacent).

========================================================================
MONOFILE — hard >600 = 0 (product)
========================================================================

Live `wc -l` 2026-08-12T20:09:54Z. No product file touched this pass.

`ooda/oodac/**/*.oo` over 600: **0**
`ooda/runtime/*.{c,h}` over 600: **0**

Soft/pack residual (pre-existing; F27 CRIT — not this MED crush):

| File | Lines | ≤256 | ≤300 | ≤600 |
|------|------:|:----:|:----:|:----:|
| `oodac/check_cap_util.oo` | 357 | OVER | OVER | PASS |
| `oodac/c_emit_skip.oo` | 329 | OVER | OVER | PASS |
| `oodac/main.oo` | 307 | OVER | OVER | PASS |
| `oodac/load_import.oo` | 287 | OVER | PASS | PASS |
| `oodac/lex.oo` | 277 | OVER | PASS | PASS |
| `oodac/c_emit_let_ext.oo` | 263 | OVER | PASS | PASS |
| `oodac/c_emit_secret_prop.oo` | 262 | OVER | PASS | PASS |
| `runtime/chs_rt_sys.c` | 438 | OVER | OVER | PASS |
| `runtime/chs_rt_ffi.c` | 359 | OVER | OVER | PASS |
| `runtime/chs_rt.h` | 264 | OVER | PASS | PASS |

std/src catalog **>600 = 12** (observe only; no peel, no delete):

`tss.oo` 964 · `tpm.oo` 931 · `zkstark.oo` 854 · `kmac.oo` 852 ·
`quantum/gates.oo` 831 · `ed25519.oo` 723 · `fin/credit.oo` 714 ·
`groth16.oo` 709 · `dilithium.oo` 684 · `double_ratchet.oo` 677 ·
`robot/slam.oo` 660 · `sha3.oo` 652

Split gate: **none**. Hard ban holds on compiler + runtime.
Catalog-12 is RFC/cohesion residual, not crush debris.

========================================================================
BYTES / COUNTS
========================================================================

| Metric | Value |
|--------|------:|
| Files deleted | **17** |
| Bytes removed | **45152** |
| Empty patches remaining | **0** |
| Root `tmp_*.oo` remaining | **0** |
| Product `.oo` / `.c` edited | **0** |
| Commits / pushes | **0** |

========================================================================
RESIDUAL
========================================================================

1. F27 — 7 oodac `.oo` still over soft 256 (all <600). DE peel, not hygiene.
2. 12 `std/src` catalog monofiles >600. Do not wipe the tree.
3. F5/F29 `scratch/` + root `test.c*` still sit (gitignored).
4. Non-empty ignored patches kept on purpose.
5. SPRINT F15–F18 checkboxes not flipped here (coordinator).

end of crush_hygiene.oot
