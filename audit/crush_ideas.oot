# crush_ideas.oot — Idea Fairy 🧚 remaining CRIT after honesty wave
# Date: 2026-08-12
# Role: creative but **shippable** residual ideas — **no product code** this pass
# Audience: Domain Experts (F26 name-scan) · Docs (F27 residual + F19) · peers
# Scope: rank leftover CRIT; **F27 split vs F26 rebuild vs leave residual**.
#        Not M1–M20. Not Args/Walk product. Not 8 crypto gates. Not new TC.

## Problem (tree truth after honesty)

Honesty wave **closed claims**, not the check hang:

| ID | After honesty | Proof |
|----|---------------|-------|
| F14 | **CLOSED** (absent + ledger deleted) | `f14_phantom_net_close.oot` |
| F31/F38 | **RESTATED** (3 helpers / 5 phantom) | `f31_domain_b.oot` + pack |
| F32/F33 | **RESTATED** on RFC-002 only | `f32_rfc002_honesty.oot` |
| F10–F12 | **NOT_A_BUG** (check deny rc=1 ×2) | `crush_peer2.oot` |
| F15–F18 | **HYGIENE GONE** | `crush_hygiene.oot` |
| **F26** | **STILL CRIT** p7 exit 1 | `f26_p7_integration.oot` |
| **F27** | **STILL CRIT** on board | live `wc` below |
| F19 | HIGH leftover | RFC-003 L15–L18 still ships Args/Walk (`crush_peer1.oot` **REJECT**) |

**F26 live (not a missing rebuild):** product `oodac` 963080 B, md5
`35505fc…`, BuildID `abf4360f…`. ELF **has** `np>12` (`cmp $0xc` +
`scan_fn_names`). p7: tiny OK; `main.oo` timeout 45s ×2. Isolated
`timeout 120 check oodac/main.oo` → rc=124, **0 bytes**, RSS **1.8 GiB**.
Stack at 58s: `tokenize_lines` ← `scan_fn_names` ← `run_check_path`.
Graph: **150** modules / 21 717 lines. Smoke text “needs rebuilt host
with modular check” is **stale**. Fairy 180s check also rc=124.

**F27 live (SPRINT 8-file list is stale):** `check_cap_util.oo` is
**218** (under cap). Over 256 today:

| File | Lines | Seam? |
|------|------:|-------|
| `c_emit_skip.oo` | 329 | 11 fns — peel grants vs skip |
| `main.oo` | 307 | **118 imports** + CLI |
| `load_import.oo` | 287 | `load_expand` already sliced |
| `lex.oo` | 277 | **one** `lex_all` |
| `c_emit_let_ext.oo` | 263 | **one** fn |
| `c_emit_secret_prop.oo` | 262 | 3 fns |

O=**6**, all <330. Hard >600 = 0 (`crush_hygiene.oot`).

**Not this wave:** invent Args/Walk, mint phantom `_ops`, 8-file peel
campaign, new IR/TC, tree `oodac build main.oo`, auto-delete >256.

## Constraints (hard)

1. **Path A only** — residual honesty + one cheap F26 thicken.
2. **Do not greenwash F26** from “modular is in the ELF” or AST `[x]`.
3. **Do not split F27 to close Pride** if the split grows the 150-mod
   graph that F26 cannot finish.
4. **No mid-fn chops** (SPLIT_PLAN). `lex_all` / `c_emit_let_untyped_ty`
   are one function each.
5. **Memory:** no new `name = name + c` / `line = line + c`. Use
   `str_slice` (load_import already). P7 `names = names + scan_fn_names`
   stays named (`crush_memory.oot` CRUSH-6).
6. **Fairy writes ideas only.** DE owns land + prove.

## Darwin scorecard

Axes 0–10. Sum/5 ≈ rank. Losers pruned. 1–2 winners this turn.

| Axis | Meaning |
|------|---------|
| **Close** | Moves live F26 smoke and/or honest F27 |
| **Honest** | No rebuild lie; no “8 files”; no Args/Walk product |
| **Smoke** | Cheap: p7 + `wc -l` + existing cap/s29 rails |
| **Entropy** | Lowers \(S\) (stale cause, fake CRIT, split theater) |
| **Ship** | ≤ few files; no new architecture |

---

## Remaining CRIT rank (after honesty)

| Rank | ID | Why this order |
|-----:|----|----------------|
| **1** | **F26** | Live product smoke red. Users hit `oodac check`. 1.8 GiB / 120s. |
| **2** | **F27** | Doctrine only. Compiler works. Split now **hurts** F26. |
| **3** | **F19** | HIGH on board; Peer1: same shipped-lie as F32/F33. Docs, not ELF. |

Do **not** promote F20/F34/F39/F23 to CRIT. Do **not** re-open F10–F12.

---

## Decide card — F27 split vs F26 rebuild vs leave residual

| | F27 split 6 files | F26 rebuild host | Leave residual |
|--|-------------------|------------------|----------------|
| F26 smoke | **Worse** (more mods to tokenize) | **No-op** (already in ELF) | F26 stays red unless I2/I4 |
| F27 doctrine | Closes O if seams hold | Unchanged | **Honest** if named O=6 + ratchet |
| Prove cost | Needs working check + relink | Integration already recopy | Docs + `wc` |
| Moonshot risk | Mid-fn chops / import boom | “just rebuild” Pride | None |
| This turn | **REJECT** | **REJECT as close** | **WINNER for F27** |

**Ship-now decision:** **leave F27 residual. Do not rebuild for F26.
Do not peel six files.** F26 close is a **light name-scan**, not a
new host.

---

## Ideas evaluated

### I1 — Leave F27 residual (name O=6, ratchet, fix the 8-file lie) ⭐
**Shape:** Docs only. SPRINT F27: **6** files, drop `check_cap_util`
357. PM/AUDIT_RESIDUAL: O=6, all <330, hard >600=0. `check_file_lines
--ratchet` must not grow. No peel this turn.
**Darwin:** Close 6 · Honest 10 · Smoke 9 · Entropy 9 · Ship 10 → **8.8**

### I2 — F26 light `scan_fn_names` (no `tokenize_lines`) ⭐
**Shape:** Integration residual #1. Walk `pub fn` / `fn` idents with
`str_slice` lines (same as `load_import`). Do **not** lex 150 files
just to build `extra_binds`. Keep `run_check_core_binds` second loop
**only after** name-scan is cheap. No new TC.
**Smoke:** p7 exit 0 **or** finish <45s with type/parse rc (script
already accepts non-hang). Dual-repro isolated. s29 + p6 stay 0.
**Darwin:** Close 10 · Honest 10 · Smoke 9 · Entropy 10 · Ship 8 → **9.4**

### I3 — One tokenize reused for names + check
**Shape:** Tokenize each body once; names from toks; then
`run_check_core_binds` on same toks. Bigger than I2.
**Darwin:** Close 9 · Honest 8 · Smoke 8 · Entropy 8 · Ship 5 → **7.6**
**Later** if I2 greens name-scan but second loop still >45s.

### I4 — p7 mid-size rail + name full-`main` as R4
**Shape:** Fixture with **np>12** that **must** finish <45s. Full
`oodac/main.oo` stays named R4/R5 until I2/I3. Demote F26 only if
Docs accept “hang class” vs “150-mod cost.”
**Darwin:** Close 5 · Honest 9 · Smoke 9 · Entropy 8 · Ship 9 → **8.0**
**Belt with I2**, not a solo CRIT close (Pride if p7 still red).

### I5 — Rebuild / recopy host again
**Darwin:** Close 1 · Honest 2 · Smoke 1 · Entropy 0 · Ship 8 → **2.4 — REJECT**
Integration already installed p7b. Symptom unchanged.

### I6 — Split the 6 oversize files this turn
**Darwin:** Close 3 · Honest 4 · Smoke 2 · Entropy 1 · Ship 2 → **2.4 — REJECT**
Grows 150-mod graph. `lex_all` / let_ext are one fn. F26 worse.

### I7 — Raise p7 timeout to 10 min
**Darwin:** Close 2 · Honest 3 · Smoke 3 · Entropy 1 · Ship 8 → **3.4 — REJECT**
120s already dead at 1.8 GiB. Not “almost.”

### I8 — Invent Args/Walk or 8 crypto `_ops` to “close” honesty
**Darwin:** avg **<2 — REJECT** (moonshot; F32/F31 already restated).

### I9 — Doc-only F26 FIXED
**Darwin:** Close 1 · Honest 0 · Smoke 0 · Entropy 0 · Ship 9 → **2.0 — REJECT**

### I10 — Restate RFC-003 L15–L18 (F19) to match RFC-002
**Shape:** Same words as RFC-002: Args/Walk **NOT SHIPPED**. Drop dead
`t5_std_honesty.oot` cites. No product modules.
**Darwin:** Close 4 · Honest 10 · Smoke 8 · Entropy 9 · Ship 10 → **8.2**
**Docs companion.** Unblocks Peer1. Not an F26 close.

---

## Rank (path-A survivors)

| Rank | ID | Avg | Verdict |
|-----:|----|----:|---------|
| **1** | **I2** | 9.4 | **WINNER — F26** light name-scan |
| **2** | **I1** | 8.8 | **WINNER — F27** leave residual |
| **3** | I10 | 8.2 | Docs same turn (F19 / Peer1) |
| **4** | I4 | 8.0 | Belt after I2; not solo close |
| **5** | I3 | 7.6 | Fallback if second loop still hangs |
| — | I7 | 3.4 | Reject timeout theater |
| — | I5 / I6 | 2.4 | Reject rebuild / split |
| — | I9 / I8 | ≤2 | Reject greenwash / invent |

## Top recommendation

### **Ship: I2 (cheap name-scan) + I1 (leave F27 residual). Not rebuild. Not split.**

### DE winner advice (plain English)

1. **Do not** `run_build.sh` / recopy p7b / “rebuild modular” as the close.
2. **Do not** split the six oversize files this turn.
3. **Do not** mint `args.oo` / `fs_walk.oo` / phantom `_ops`.
4. **Land I2** in `check_collect.oo` `scan_fn_names`: no
   `tokenize_lines`. Line `str_slice` + `fn` / `pub fn` IDENT.
5. **Prove:** isolated `p7_check_main_smoke.sh` exit **0** twice.
   Keep s29 + p6 green. If name-scan dies but second loop hangs: **I3**,
   not I5/I6/I7.
6. **Docs I1:** F27 = 6 files, residual, ratchet. Drop 357 / “8 files.”
   Focus line “rebuild so P7 uses per-module path” is **done and still
   red** — rewrite to “light name-scan so p7 exits 0.”
7. **Docs I10:** RFC-003 L15–L18 = RFC-002 NOT SHIPPED.
8. **Out of scope:** I5 rebuild, I6 peel, I7 long timeout, I8 invent,
   I9 doc-FIXED, full TC, auto-delete >256.

### Explicit non-winners

- F26 rebuild / host recopy (I5)
- F27 six-file split this turn (I6)
- Raise timeout and call it closed (I7)
- Invent Args/Walk/crypto (I8)
- SPRINT F26 FIXED without p7=0 (I9)

## Entropy report

`S: ↓` — rebuild lie → named tokenize cost; F27 8-file lie → O=6
residual. `U` holds: 150-mod second-loop cost, emit-c concat
(`tokens main` still 30s hang), F38b sha256 name collision, catalog-12
>600 in `std/src`. `O` does **not** rise (no peel). `W` does not rise
if I2 uses slice, not `+ c`.

## Leave-off

- Fairy: this file only. Probes: p7 45s fail; 180s rc=124; `check_cap_util` 218.
- DE owns I2; Docs own I1+I10. Peers: ACCEPT F26 only on p7=0.
- Memory: I2 must not add per-char concat. CRUSH-6 P7 join still named.
- Hygiene: no ELF commit. No SPRINT F26 flip until smoke 0.

## Pocket card

```
After honesty: CRIT left = F26 then F27. F19 is docs leftover.

  KEEP  F26 CRIT until p7 exit 0 (modular ELF is not the close)
  KEEP  F27 as named residual O=6 (all <330; cap_util 218)
  STOP  rebuild / recopy host as F26 close          (I5)
  STOP  split 6 compiler files this turn            (I6)
  STOP  invent Args/Walk / 8 _ops                    (I8)
  LAND  scan_fn_names without tokenize_lines        (I2)
  LAND  SPRINT/PM F27 count + ratchet               (I1)
  LAND  RFC-003 L15–L18 = NOT SHIPPED               (I10)
  PROVE p7_check_main_smoke exit 0 ×2
        s29_println_guard + p6 still 0
  THEN  Docs may drop F26. Not the AST [x] line.

FALLBACK if name-scan cheap but second loop hangs: I3 (reuse toks).
NOT a close: I4 mid-size rail alone; I7 long timeout.
```

Path-A callout: crush leftover = **stop tokenizing to collect names**,
and **leave the six oversize files named**. Not a rebuild. Not a split
campaign. Not residual-as-FIXED on a red p7.

end of crush_ideas.oot
