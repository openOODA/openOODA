# crush_ideas3.oot — Idea Fairy 🧚 close F26 this hour
# Date: 2026-08-12
# Role: ideas only. No product. No SPRINT. No push.
# Scope: how to close F26 in 60 min. Cheap scan already WON.

## 12-line summary

1. Cheap `scan_fn_names` WON. `/tmp/oodac_f26` **finishes** check in **56.7s** (type ERR, not hang).
2. p7 inner timeout is **45s** → still FAIL. Hang class is dead; this is **cost vs clock**.
3. ELF without live 2.9 (`f26`) fails **s29**. Do not install it.
4. Live F27 lex peel (`f26b`) fails seed gcc: `lex_all` **Int vs List ARC**.
5. `secret.oo` without live `alias.oo` (`f26c`) fails `c_secret_is_user_sink` (2.10 half-land).
6. Parent **f26d** = f26src + print/secret/guard/alias. No lex peel. Right mix.
7. f26d emit **RED**: `chs_rt_sys.c:437` extra `}` in `oo_fetch` (gcc). Not a 100-wide problem.
8. **Reject** another 100-wide live emit. **Reject** skip-typecheck / skip loop2.
9. Peer1 live `check main.oo` on f26: **76.1s finish**, peak RSS **2.4GiB** (loop2, not scan hang).
10. Close = isolated **p7=0 ×2** AND **s29 + p6 still 0**. ELF exists ≠ close.
11. This hour: unblock runtime → retry **this** f26d → measure **live** wall → raise p7 to measured×1.2.
12. Optional same-emit fold: drop probe `lex_all` (I17). Not a second emit. F27 wc is later.

## Constraints

1. Path A. This file only. DE owns land + prove + install.
2. Do **not** start `/tmp/oodac_f26e` from live 168-mod peel tree.
3. Do **not** clobber `ooda/oodac/oodac` until isolated p7+s29+p6.
4. Do **not** skip the 25 TC walks or `check_secret_println_sinks`.
5. Do **not** reuse 56.7s as the raise number — measure **live** `oodac/main.oo` uncontended.
6. p7 already accepts finished rc≠0. Gate is **finish**, not OK.

## Darwin axes (0–10). Sum/5 ≈ rank.

| Axis | Meaning |
|------|---------|
| **Close** | Moves live p7 to exit 0 **and** keeps s29 |
| **Honest** | No skip-TC; no peel emit; no install-f26; no doc-FIXED |
| **Smoke** | p7 + s29 + p6 + `scan_fn_light` |
| **Entropy** | Ends rebuild roulette (f26/b/c) |
| **Ship** | ≤ this snapshot + 1 smoke number; no new arch |

---

## Required Darwin pairs

### P1 — which ELF mix

| Mix | 2.9 | alias/`is_user_sink` | lex peel | Result |
|-----|-----|----------------------|----------|--------|
| **f26** | no | no | no | check **56.7s** / s29 **RED** |
| **f26b** | live | — | **yes** | seed gcc **Int vs List** |
| **f26c** | secret yes | **no** (alias 157) | no | gcc implicit `c_secret_is_user_sink` |
| **f26d** | print+secret+guard | **yes** (alias 237) | **no** | **WINNER mix**; gcc blocked by **runtime `}`** |

**Winner: f26d.** Losers stay dead. Do not revive b/c.

### P2 — beat 45s

| Path | Close | Hon | Smoke | Ent | Ship | Avg | Verdict |
|------|------:|----:|------:|----:|-----:|----:|---------|
| **B raise to measured×1.2** | 9 | 9 | 10 | 9 | 10 | **9.4** | **WINNER** iff live **finishes** |
| **I17 drop probe lex** | 8 | 9 | 8 | 8 | 9 | **8.4** | Fold into **same** f26d retry |
| **C skip typecheck / loop2** | 6 | 0 | 2 | 0 | 7 | **3.0** | **FAIL-OPEN / REJECT** |

C still prints OK without 25 TC + secret + residual free-calls. SCIENCE: fail-closed.

Raise rejected only if live check **does not finish** or RSS grows **without bound** (old hang). Peak 2.4GiB **after a finish** is loop2 residual, not a raise veto.

### P3 — this emit vs another 100-wide

| Path | Verdict |
|------|---------|
| Retry **f26d_src** after chs_rt pin/one-`}` | **WINNER** — same 151-mod snapshot |
| New emit of **live** 168 + lex_scan/load_parse/let_crt/cli | **REJECT** — f26b class |
| Overlay `.oo` into `/tmp/oodac_f26` | **REJECT** — cannot |

### P4 — install now vs isolated first

Install f26 now = s29 die. Clobber product before prove = un-bisectable.
**Winner: OODAC_BIN=/tmp/oodac_f26d** p7+s29+p6, then atomic install.

### P5 — whose `main.oo`

p7 checks **live** `$ROOT/oodac/main.oo` (168 mods, +`cli_parse` +`skip_grant`).
56.7s was snapshot-class. Peer1 live = **76s**. Measure live. Do not raise from memory.

---

## Five options (this hour)

### 1. I16 — Finish f26d + measure + raise ⭐
**Pair:** raise vs keep 45s vs skip-TC (P2).
Unblock `chs_rt_sys.c` (delete extra `}` **or** pin the runtime that linked `/tmp/oodac_f26`). Retry **this** `oodac_pure_build` on `f26d_src`. Then:

```
timeout 120 /usr/bin/time -v /tmp/oodac_f26d check ooda/oodac/main.oo
# write wall + maxrss. p7 inner = ceil(wall*1.2)  (≈68 if 57s; ≈92 if 76s)
OODAC_BIN=/tmp/oodac_f26d bash ooda/scripts/p7_check_main_smoke.oo   # twice
OODAC_BIN=/tmp/oodac_f26d bash ooda/scripts/s29_println_guard_smoke.oo
OODAC_BIN=/tmp/oodac_f26d bash ooda/scripts/p6_c_runtime_qa_smoke.oo
```

Install only if all three 0. **Score 9.**

### 2. I17 — Fold drop probe `lex_all` into the same retry
**Pair:** fold vs raise-only vs second emit.
`run_check_core_binds` lexes, then `tokenize_lines` lexes again. Comment in `check_mod` already says tokenize fail-closes. Delete the probe block in `f26d_src/check_drive.oo` **before** the retry. May land under 45s (no smoke edit). If still >45s → I16 raise. **Not** a new 100-wide. **Score 8.**

### 3. I18 — Isolated-first install (belt)
**Pair:** isolated vs clobber-now.
Never copy over product until I16 prove. Keep tokenize-era ELF as rollback. Dual-repro p7 ×2 on the new bin. **Score 8.** Required with I16; not a solo close.

### 4. I19 — Honesty stop
**Pair:** close vs name-cost-and-wait.
If uncontended live check **rc=124** or RSS climbs until kill: **do not raise, do not close.** Name cost. Then I17 (if not folded) — still **this** snapshot, not live peels. **Score 7.** Gate, not a close.

### 5. I20 — Mid-size np>12 rail
**Pair:** extra rail vs CRIT close.
A 13–20 file fixture that **must** finish <45s. Full `main.oo` stays named cost. **Pride** if p7 on live main still red. Belt only. **Score 5.**

---

## Rank

| Rank | ID | /10 | Verdict |
|-----:|----|----:|---------|
| **1** | **I16** | 9 | **WINNER** — retry f26d, measure live, raise, prove, install |
| **2** | **I17** | 8 | **WINNER fold** — drop probe lex in the same retry |
| **3** | **I18** | 8 | Isolated install belt |
| **4** | I19 | 7 | Stop if live still hangs |
| **5** | I20 | 5 | Extra rail; not CRIT close |
| — | C skip TC | 1 | **FAIL-OPEN / REJECT** |
| — | 100-wide live emit / f26b | 2 | **REJECT** (Int vs List) |
| — | install f26 / f26c | 2 | **REJECT** (s29 / half-2.10) |
| — | blind raise on product ELF | 2 | **REJECT** (still tokenize hang) |
| — | I9 doc-FIXED | 1 | **REJECT** |

## Pocket card

```
Close F26 this hour:

  KEEP  f26d mix (print+secret+guard+alias). No lex peel.
  FIX   chs_rt extra }  OR pin last-good runtime (f26 linked).
  RETRY this f26d_src emit.  STOP second 100-wide. STOP live peels.
  FOLD  drop probe lex_all (I17) if DE can land before retry.
  MEASURE live oodac/main.oo wall+RSS (not 56.7s memory).
  RAISE p7 inner to measured×1.2 IFF it FINISHES.
  NEVER skip typecheck / skip loop2.
  PROVE  OODAC_BIN=/tmp/oodac_f26d  p7×2 + s29 + p6
  THEN   atomic install. Rollback = current 963080 B tokenize ELF.
  ELSE   I19 — do not close.

  DEAD: f26 (s29), f26b (ARC gcc), f26c (is_user_sink), I9, C.
```

Path-A: **one runtime pin + one f26d retry + one measured clock.**
Not another emit. Not a peel. Not skip-TC.

end of crush_ideas3.oot
