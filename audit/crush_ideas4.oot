# crush_ideas4.oot — Idea Fairy 🧚 NEXT 5 after F26 install
# Date: 2026-08-12
# Role: ideas only. No product. No SPRINT. No push. Ignore moonshots.
# Scope: rank after f26e install (p7=0 s29=0 p6=0). F27 wc empty. F46 brace gone.

## 10-line summary

1. F26 close class is **finish**, not OK. f26e check **56.86–58.7s** rc=1 (Bool/String 36:5). Hang dead.
2. F27 **oodac wc empty**. F46 extra `}` gone. F44 **3 cap smokes already .oot** (CLOSED).
3. `cli/main.oo` is **191** (build_args peel). **262 is stale** — not a next land.
4. 2.10 full `mark_user_sinks` walk is **STRIPPED** (f26d >180s). Prefix opt-in stays. Hole is real.
5. loop2 RSS **2.4GiB** is named leftover, **not** a close veto (`f26e_rss.oot`).
6. **Next 1:** I17 drop probe `lex_all` — tokenize already fail-closes. Protects p7 75s slack.
7. **Next 2:** F45 **prove** on new host. C+emit+`is_known_name` exist. Do **not** invent Args/Walk.
8. **Next 3:** isolate Bool/String 36:5 (per-module). p7-accepts-rc1 ≠ self-check green.
9. **Next 4:** 2.10 **cheap fold** into existing sink walk. **Never** re-add full-tree mark.
10. Off: RSS campaign, F44 40-smoke sweep, skip-TC, I9 doc-FIXED, peel-all.

## Constraints

1. Path A. This file only. DE owns land + prove.
2. Do **not** re-add `c_secret_mark_user_sinks` as a second full walk (reopens F26).
3. Do **not** skip typecheck / loop2. Do **not** invent Process.Args / FS.Walk.
4. p7 75s = ceil(56.86×1.2) slack. New cost must **finish** under that clock.
5. F27 surface is `oodac/*.oo` only. `cli/` is out of F27.

## Darwin axes (0–10). Sum/5 ≈ rank.

| Axis | Meaning |
|------|---------|
| **Close** | Next live smoke / keeps F26 closed |
| **Honest** | No skip-TC; no re-hang walk; no stale 262; no doc-FIXED |
| **Smoke** | p7 + s29 + p6 + existing ZT rails |
| **Entropy** | Ends double-lex / ZT-doc theater / half-2.10 |
| **Ship** | ≤ few files; no new arch |

---

## Candidates (scored)

### 1. I17 — Drop probe `lex_all` ⭐
**Pair:** drop vs keep double-lex vs skip-TC.
`run_check_core_binds` (`check_drive.oo` L27) lexes, then `tokenize_lines` lexes again. Comment already says tokenize fail-closes. Delete the probe block. ×~153. Same snapshot. May cut wall+one List.
**Darwin:** Close 8 · Honest 9 · Smoke 9 · Entropy 8 · Ship 10 → **8.8**
**Score 9.** Winner. Keep-both = waste.

### 2. F45 — `sys_args` prove (not invent)
**Pair:** prove-on-f26e vs mint Args module vs ignore HIGH.
C `sys_args` + emit + `is_known_name` + `get_builtin_ret_type` **already in source**. `process.oo` L57–59 wraps it. B1 fail was **old ELF**. After install:
`OODAC_BIN=<new> bash scripts/cap_process_facade_smoke.oo` + `check std/os/process.oo`.
Green → closed for free. Red → tiny bind only. **Not** RFC-002 Args/Walk.
**Darwin:** Close 8 · Honest 9 · Smoke 10 · Entropy 8 · Ship 8 → **8.6**
**Score 8.**

### 3. Bool/String type ERR — isolate 36:5
**Pair:** isolate+fix vs live-with-rc1 vs skip-TC.
First real self-check error now that hang is dead. `return type String does not match declared Bool` @ 36:5. Modular path: first `process_exit` wins. Isolate: check modules in `collect_module_paths` order (deps first = `lex_scan.oo` …) until hit. Then **one** mismatch or one TC false-positive. Do not campaign the 25 walks.
**Darwin:** Close 7 · Honest 10 · Smoke 7 · Entropy 8 · Ship 6 → **7.6**
**Score 8.** Isolate first. Size unknown until then.

### 4. 2.10 cheap marker (fold, not walk)
**Pair:** fold-into-existing-walk vs re-add `mark_user_sinks` vs prefix-only.
`s210_residual.oot`: zero callers of mark. `fn leak(s){println(s)}; leak(tok)` ALLOW. Prefix `__sec_sink__*` still refuses. Cheap = one extra state in the **existing** `check_secret_println_sinks` tok walk (already N). Second full-tree walk = F26d class. After I17.
**Darwin:** Close 7 · Honest 9 · Smoke 8 · Entropy 6 · Ship 6 → **7.2**
**Score 7.**

### 5. F44 leftover — 2 B1 scripts only
**Pair:** 2 leftovers vs F44-reopen vs 40 residual_*.md.
OWN F44 **CLOSED** (`f44_md_grep.oot`): forge / ffi_residual / biometric → `.oot`. Still `.md`: `sys_syscall_path_a_smoke.oo`, `m167_residual_deepen_smoke.oo` (`STATIC_CAPS.md`). Same one-string edit. **Reject** sweeping 40 residual smokes (campaign).
**Darwin:** Close 5 · Honest 9 · Smoke 8 · Entropy 7 · Ship 9 → **7.6** as belt; **6** as “next HIGH” (OWN already done).
**Score 6.**

---

## Also-rans (not top 5)

| ID | /10 | Why not |
|----|----:|---------|
| F41 K256 | 6 | One C word `K256[62]` `0xbef4a9f7`→`0xbef9a3f7`, then pin hello hex. MED. After ZT/type. Header already honest. |
| loop2 RSS 2.4GiB | 4 | Named leftover. Campaign ≠ next. **I17 is the cut.** |
| cli/main.oo 262 | 2 | **STALE.** Live **191**. F27 surface empty. |
| re-add mark walk | 1 | Reopens F26. **REJECT** |
| skip TC / loop2 | 1 | FAIL-OPEN. **REJECT** |
| F44 all residual_*.md | 3 | Moonshot-adjacent campaign. **REJECT** |

## Rank

| Rank | ID | /10 | Verdict |
|-----:|----|----:|---------|
| **1** | **I17** | 9 | **WINNER** — drop probe lex; keep F26 slack |
| **2** | **F45** | 8 | **WINNER** — prove; don't invent Args |
| **3** | **Bool/String** | 8 | isolate 36:5; then one fix |
| **4** | **2.10 cheap** | 7 | fold into existing walk only |
| **5** | **F44 leftover** | 6 | 2 scripts; OWN F44 already closed |
| 6 | F41 K256 | 6 | honor; not this rotation |
| — | RSS / cli 262 / skip-TC / mark-walk | ≤4 | REJECT / stale |

## Pocket card

```
After F26 install (f26e p7=0 s29=0 p6=0):

  KEEP  p7 75s slack. F27 empty. F46 gone. F44 OWN closed.
  LAND  I17 drop probe lex_all in check_drive (same ELF mix).
  PROVE F45 on new host. Tiny bind only if still undefined.
  THEN  isolate Bool/String 36:5 (per-module). One file.
  THEN  2.10 cheap: fold mark into existing sink walk.
        NEVER re-add full mark_user_sinks.
  BELT  F44 leftover: sys_syscall + m167 .md→.oot only.
  LATER F41 K[62] one word + pin hex.
  DEAD  cli 262 (now 191). RSS campaign. skip-TC. Args/Walk.
        40 residual_*.md sweep. I9 doc-FIXED.
```

Path-A: **keep F26 closed, then prove F45, then name the type ERR.**
Not another emit. Not a walk. Not a peel.

end of crush_ideas4.oot
