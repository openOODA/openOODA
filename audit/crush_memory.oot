# crush_memory.oot — Memory Auditor / honesty-wave crush
# Date: 2026-08-12T20:11:14Z
# Role: Memory Auditor — prove-not-leak; **no new concat** from this
#       honesty wave. **PASS if no product walker changes.**
# Tip: super `eaa555d` / ooda `52cc4b5` / openOODA `81f56fc`
# Binary: `/home/jeryd/Projects/openOODA/ooda/oodac/oodac`
#   963080 B, mtime 2026-08-12 10:04:33 -1000
#   md5 `35505fc520e997a9421089f11bdffd92`
#   BuildID `abf4360f72e58b5b12366e1322428f01f808dad4`
# Walker source (unchanged vs s29c pin):
#   `c_emit_secret.oo` (9688 B, 231 L, 09:39, md5 `b96bfc0ead0543120c71e34a6d9f372b`)
#   `c_emit_print.oo` (3339 B, 97 L, 09:35, md5 `4a993a5519c13b08a400c088810468e1`)
#   `c_emit_secret_guard.oo` (9779 B, 228 L, 08:46, md5 `ef9029995324d31aeb26748afe21e75b`)
# OWN: this audit only. No product `.oo`. No ELF overwrite. No push.

## Scope

Prove or reject the **crush honesty wave** memory floor:

> Hunt: **no new concat**. Write **PASS** if product walkers did not change.

Crush wave (2026-08-12T20:08–20:11Z) = SPRINT audit-2026-08-12 honesty:

| Lane | What landed | Product walker? |
|------|-------------|-----------------|
| F14 | delete phantom net claim; PM/RFC-003 cite | **no** |
| F31/F38 | restate `STDLIB_CRYPTO_MATH.oot` | **no** |
| F32/F33 | restate RFC-002 Args/Walk **not shipped** | **no** |
| F10–F12 | Peer2 live `oodac check` prove | **no** (read only) |
| F15–F18 | Hygiene `rm -f` named debris | **no** |
| Build | `oodac_pure_build_fixed.sh` parallel emit + gcc | **no** (script) |
| Relink | product `oodac` 10:04 (same walker sources) | **relink only** |

Not this wave: 2.9 Int depth (`s29`/`s29b`/`s29c_memory.oot`), 2.10
`__sec_sink__`, 7.10 slice, P7 `check_mod` bind concat, 6.1 `env_set`.

## Verdict — path-A memory floor

| Result | Meaning |
|--------|---------|
| **PASS** | Honesty wave added **zero** product walker hunks. Zero `name = name + c`. Zero `args = args + …` in any crush-owned file. SECRET walks are still Int `depth` / Int `d` + `field_at` (s29c pin, md5 match). Relink did not rewrite those sources. Live grouped/bare/eprintln refuse stays the ~4.4–4.6 MB sibling class. |

**Not claimed:** O(n) lex time. Helper unify (I5/I9). P7 `names = names + …`
gone. F26 P7 hang closed. Crypto math implemented.

| Slice | Result |
|-------|--------|
| H1 no new concat from this wave | **PASS** |
| H1 product walker source delta | **EMPTY** (md5 = s29c) |
| H2 live heap vs eprintln class | **PASS** (~4.4–4.6 MB) |
| H2 long-ident / nest **time** | **RESIDUAL** Θ(n²) `char_at` (not crush-new) |
| H3 mechanism | **PASS** — docs/hygiene/prove; no new walk |
| Functional rails (`s29_println_guard_smoke`) | **PASS** ALL OK |
| I5 / I9 unify | **FORBIDDEN** (not landed) |

---

## H1 — No new concat / no walker change

### Crush-owned files (live 20:11Z)

| Site | Form | Alloc? |
|------|------|--------|
| `audit/f14_phantom_net_close.oot` | honesty close | none (doc) |
| `audit/f31_domain_b.oot` + `bootstrap/STDLIB_CRYPTO_MATH.oot` | pack restatement | none (doc) |
| `audit/f32_rfc002_honesty.oot` + `RFC-002-*.oot` | named-gap restatement | none (doc) |
| `audit/crush_peer2.oot` | F10–F12 check prove | none (audit) |
| `audit/crush_hygiene.oot` | 17 debris deletes | none (hygiene) |
| `SPRINT.oot` / `PM.oot` / ledger | board honesty | none (doc) |
| `scripts/oodac_pure_build_fixed.sh` | parallel emit; gcc uncomment | none (host script) |
| `oodac/c_emit_secret.oo` / `c_emit_print.oo` | — | **not touched** |

`find ooda/oodac -name '*.oo' -newermt '2026-08-12 09:58:00'` → **empty**.
Walker mtimes: secret 09:39, print 09:35, guard 08:46.

Forbidden patterns **absent** from crush-owned files **and** from the
three SECRET walks (re-read this pass):

| Pattern | In crush wave? | In SECRET walks? |
|---------|----------------|------------------|
| `name = name + c` / `out + char_at` | **no** | **no** |
| `args = args + ", " + a` / `joined =` | **no** | **no** |
| new `oo_str_concat` / new `str_slice` in IDENT walk | **no** | **no** |
| new walker fn (`c_secret_*` / `c_crush_*`) | **no** | — |
| `let mut depth` / `let mut d` | not added here | **pre-existing Int** |

Every `+` in the three walks is still **Int** (`p+1`, `sp+1`, `depth+1`,
`d+1`). IDENT text is still `field_at(..., 3)`.

Adjacent (not crush-owned):

| Site | Expression | Class |
|------|------------|-------|
| `check_mod.oo` L57 | `names = names + scan_fn_names(...)` | **P7** (09:49) |
| `c_emit_call.oo` L40 | `args = args + ", " + a` | pre-existing free-call join |
| `c_emit_print.oo` L36 | `"__sec__" + sn` | once-per-ident env key (existing) |
| secret L65 / L222 | `str_slice` | **7.10** / **2.10** |

**H1: PASS.** No walker change. No new concat.

---

## H2 — Heap class on the 10:04 host

`nm -S` this ELF (depth **is** linked — unlike s29c 04:49 host):

| Sym | Addr | Size |
|-----|------|-----:|
| `c_emit_println` | `0x465720` | 2615 |
| `check_secret_println_sinks` | `0x4761e0` | 4157 |
| `c_secret_scan_call` | `0x45e200` | 793 |
| `c_secret_emit_guard` | `0x461270` | 5638 |
| `c_guard_scan_idents` | `0x45fd80` | 5354 |

s29c 04:49 sizes were 2583 / 5316 / 783 — that ELF was first-`)`.
Size delta is the **already-landed** 2.9 Int depth + 2.10 if-chain, not
a crush walker. Sources match s29c md5.

Live RSS (`/usr/bin/time` `%e` / `%M`). `OODA_SRC_ROOT=ooda`.
Smoke-shaped fixtures (ephemeral; removed). 2026-08-12T20:10:50Z.

| Case | emit-c rc / s / RSS KB | check rc / s / RSS KB |
|------|-----------------------:|----------------------:|
| `println(tok)` bare | 1 / 0.01 / 4408 | 1 / 0.00 / 4424 |
| `println((1), tok)` | 1 / 0.01 / 4572 | 1 / 0.00 / 4564 |
| `println(("x") + tok)` | 1 / 0.01 / 4536 | 1 / 0.00 / 4580 |
| `eprintln(tok)` | 1 / 0.02 / 4536 | 1 / 0.00 / 4436 |
| public `println(1)` | 0 / 0.01 / 4560 | 0 / 0.01 / 4912 |

All refuse `ERR secret`. Grouped **tracks** bare / eprintln (~4.4–4.6 MB).
No extra join buffer. `s29_println_guard_smoke.sh` **ALL OK** (exit 0).

Malformed `fn main()` without `pub`/semis → `malloc(): unaligned tcache`
SIGABRT (~4.3 MB). **Not crush-new** (honesty wave did not touch parse
or alloc). Named residual only. Smoke rails stay the close bar.

**H2: PASS** (sibling class). Time residual unchanged.

---

## H3 — Mechanism (no new extract)

| Artifact | What it says / does |
|----------|---------------------|
| WT walkers | Int `depth` L109 + L191; Int `d` L19 — **s29c pin** |
| F14 / F31 / F32 | restatement; **no** modules / walkers invented |
| `crush_hygiene.oot` | **0** product `.oo` / `.c` edited |
| `crush_peer2.oot` | check-path prove; **no** product edit |
| `c_secret_emit_guard` | still **not** called from check / stmt println |
| I5 / I9 | still **FORBIDDEN** |

| Recipe | Heap | Memory |
|--------|------|--------|
| Docs + debris delete (this wave) | 0 | **PASS** |
| Relink same walker sources | 0 source delta | **PASS** |
| I5 `joined = joined + ", " + arg` | new prefix-chain | **FAIL** if landed |
| I9 fabricated `args` into emit_guard | new buffer or FN | **FAIL** if landed |

**H3: PASS.**

---

## Scorecard

| ID | Sev | Site | Finding | Status |
|----|-----|------|---------|--------|
| CRUSH-1 | HIGH | `oodac/c_emit_{secret,print,secret_guard}.oo` | Product walker changed this wave | **ABSENT** |
| CRUSH-2 | HIGH | crush-owned docs/hygiene/prove | New per-char / per-arg concat | **ABSENT** |
| CRUSH-3 | MED | 10:04 ELF | Relink; walker md5 = s29c | **PASS** |
| CRUSH-4 | MED | grouped vs eprintln RSS | same ~4.5 MB class | **PASS** |
| CRUSH-5 | LOW | `s29_println_guard_smoke` | ALL OK | **PASS** |
| CRUSH-6 | — | `check_mod.oo` L57 | P7 `names = names +` | **OPEN adjacent** |
| CRUSH-7 | — | `c_emit_call.oo` L40 | free-call join | **OPEN adjacent** |
| CRUSH-8 | LOW | malformed fixture SIGABRT | parse/alloc residual | **named** |
| CRUSH-9 | — | I5 / I9 | new join / fake `args` | **FORBIDDEN** |
| CRUSH-10 | HIGH | `oo_char_at` / lex | Θ(n²) time | **RESIDUAL** (not crush) |

## Close bar (memory)

Path-A **may close crush memory as PASS** if process accepts:

1. No product walker source change (proven; **must stay true**).
2. No new concat in crush-owned land (proven).
3. stmt / check println RSS stays in the eprintln class on this ELF
   (re-proved).
4. P7 join, `char_at` time, malformed SIGABRT stay **named**, not
   silent crush FAILs.

Re-open **only** if a crush land grows a per-char `OoStr` rebuild, a
**new** `args = args + …` in a SECRET walk, or a walker file md5
drifts from the s29c pin **without** a Memory re-prove.

This wave needed **no** heap change. Memory will not block honesty
close. Memory **will** still block I5/I9.

## Evidence paths

| Item | Path |
|------|------|
| Check / scan_call depth | `ooda/oodac/c_emit_secret.oo` L109–141, L191–211 |
| Stmt depth | `ooda/oodac/c_emit_print.oo` L19–40 |
| Guard (2.10, not crush) | `ooda/oodac/c_emit_secret_guard.oo` |
| P7 adjacent concat | `ooda/oodac/check_mod.oo` L57 |
| Honesty ledgers | `audit/f14_phantom_net_close.oot` · `f31_domain_b.oot` · `f32_rfc002_honesty.oot` |
| Siblings | `audit/crush_hygiene.oot` · `crush_peer2.oot` |
| Prior walker floor | `audit/s29c_memory.oot` |
| Smoke | `ooda/scripts/s29_println_guard_smoke.sh` |

## Notes (return)

1. Did **not** edit product `.oo`. Audit + ephemeral fixtures only
   (removed).
2. Did **not** rebuild or overwrite `ooda/oodac/oodac`.
3. Did **not** flip SPRINT checkboxes (coordinator).
4. Hygiene / Peer2 / Domain A+B claims of “no product edit” were
   independently re-checked (walker md5 + `find` + concat hunt).
5. RULES §1.9: did not stamp on parent “no new concat” alone.

**Crush honesty-wave path-A memory floor: PASS**
(no product walker changes; no new concat).

end of crush_memory.oot
