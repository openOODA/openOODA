# crush_peer2_honesty.oot
# Peer Verifier 2 — claimed-close honesty (zero-trust)
# Date: 2026-08-12T20:25:46Z
# Role: Independent Verifier. RULES. No push. No SPRINT. No product edit.
# Tree: /home/jeryd/Projects/openOODA
# Tips: super eaa555d / ooda 52cc4b5 / openOODA 81f56fc
# OWN: ACCEPT or REJECT claimed closes F19, F39, F34/F36, F15–F18.
# WRITE: this file only.

========================================================================
TABLE
========================================================================

| ID | VERDICT | one-line proof |
|----|---------|----------------|
| F19 | ACCEPT | RFC-003 L15 floors **not** usable; L17/L18 `Process.Args`/`FS.Walk` **NOT SHIPPED**; `grep completions` empty |
| F39 | ACCEPT | `ooda/std/hash/{md5,sha1}.oo` say non-crypto + collisions exist; `grep residual: none` exit 1 |
| F34/F36 | ACCEPT | PM R5 status=`partial` **Not** 250 real libs; §3.1 has no `wireguard.oo` claim (`grep wireguard.oo` exit 1) |
| F15–F18 | ACCEPT | `ls` ENOENT named `abs_out.c` `check_caps_out.c` `load_import_temp.oo` `make_load_import.oo` `issue_2.10.patch` `tmp_*.oo` |

**OVERALL: 4/4 ACCEPT.** Honesty closes hold. No peer fix. No push.

========================================================================
SUMMARY (10 lines)
========================================================================

1. F19 restated: RFC-003 no longer says Args/Walk or "completions" are usable.
2. Mentions of `Process.Args` / `FS.Walk` are **NOT SHIPPED** / do not exist — allowed.
3. F39 headers: non-crypto checksum; residual is collisions, not "none".
4. Functions still call CHS internals; that is leftover, not a doc lie.
5. F34: R5 is `partial`, not unqualified `done` for 250 libs (~31 vs ~1,030).
6. F36: §3.1 lists product `ooda/std/src/net/dns.oo` only; does not claim `wireguard.oo`.
7. Product `ooda/std/src/net/wireguard.oo` is ENOENT; org `std/src/net/wireguard.oo` exists.
8. F15–F18 named clutter is gone under super / ooda / openOODA (`ls` ENOENT).
9. Kept on purpose: `issue_2_10.patch` 3888 B (not the empty F17 `issue_2.10.patch`).
10. Named-gap Args/Walk still unshipped. Do not import. Do not treat F39 as crypto.

========================================================================
METHOD
========================================================================

Read live files. `ls` named paths. `grep` reject phrases. No SPRINT edit.
Reject triggers from charter (any one fails the ID):

- F19: RFC-003 still says Args/Walk usable, or "completions … are usable"
- F39: md5.oo/sha1.oo omit non-crypto/collisions, or say `residual: none`
- F34: R5 unqualified `done` for 250 libs
- F36: §3.1 claims `wireguard.oo` while product file absent
- F15–F18: any named debris path still present

========================================================================
F19 — RFC-003
========================================================================

File: `openOODA/RFC-003-StdLib_Audit.oot` (24 lines).

Live L15: "Those floors are **not** usable."
Live L17: **NOT SHIPPED.** No `Process.Args` module.
Live L18: **NOT SHIPPED.** No `FS.Walk` module.
L3/L22: do not import Args/Walk. Modules do not exist on `ooda/std` or org `std/`.

```
grep -n completions RFC-003-StdLib_Audit.oot   # exit 1
grep -n 'are usable' RFC-003-StdLib_Audit.oot  # exit 1
find ooda/std std -iname '*args*' -o -iname '*walk*'   # empty
```

Prior `crush_peer1.oot` REJECT (old L15 "completions … usable") is **stale**.

========================================================================
F39 — md5.oo / sha1.oo
========================================================================

Product files only copies of those names under `ooda/std/hash/`:

```
// md5.oo L2-L3
// job: MD5 hex non-crypto checksum (RFC 1321 legacy digest, not a security function)
// residual: collisions exist; do not use for integrity or passwords

// sha1.oo L2-L3
// job: SHA-1 hex non-crypto checksum (RFC 3174 legacy digest, not a security function)
// residual: collisions exist; do not use for integrity or passwords
```

`grep -e 'residual: none' ooda/std/hash/md5.oo ooda/std/hash/sha1.oo` → exit 1.

Org `std/hash/md5.oo` is a different fail-closed facade ("NOT real MD5"); no `sha1.oo` there. Not the F39 pair.

========================================================================
F34 / F36 — PM.oot
========================================================================

R5 (L45): status cell **`partial`**. Note: "**Not** 250 real libs (F20/F34: ~31 … vs … ~1,030)."
Live `find ooda/std -name '*.oo' | wc -l` = **31**. Not unqualified `done`.

§3.1 (L98): live residual `ooda/std/src/net/dns.oo` only. Phrase "wireguard not on disk".
`grep wireguard.oo PM.oot` → exit 1. Does **not** claim the file.

Disk 2026-08-12T20:25:46Z:

```
ENOENT  ooda/std/src/net/wireguard.oo
PRESENT ooda/std/src/net/dns.oo          (1229 B)
PRESENT std/src/net/wireguard.oo         (2244 B, org residual; fail-closed)
```

`ooda/std/src/` tree = `net/dns.oo` only. Product-path claim holds.

Residual (not a reject): org `std/src/net/wireguard.oo` is on disk.
PM "not on disk" is product-scoped. Charter reject is claiming `wireguard.oo`
when the product file is absent — that claim is gone.

========================================================================
F15–F18 — named clutter
========================================================================

Ledger names (`audit_2026-08-12_run.oot` + `crush_hygiene.oot`):

| ID | Named path | `ls` |
|----|------------|------|
| F15 | `/home/jeryd/Projects/openOODA/abs_out.c` | ENOENT |
| F15 | `/home/jeryd/Projects/openOODA/check_caps_out.c` | ENOENT |
| F16 | `/home/jeryd/Projects/openOODA/load_import_temp.oo` | ENOENT |
| F16 | `/home/jeryd/Projects/openOODA/make_load_import.oo` | ENOENT |
| F17 | `/home/jeryd/Projects/openOODA/issue_2.10.patch` | ENOENT |
| F17 | `/home/jeryd/Projects/openOODA/ooda/issue2.10.patch` | ENOENT |
| F18 | `$ROOT/tmp_*.oo` (maxdepth 2) | empty |

Same five names also ENOENT under `ooda/` and `openOODA/`.

Not debris (kept): `issue_2_10.patch` 3888 B (underscore; real 2.10 hunk).
Not F18: tracked `tmp_test/` fixture.

========================================================================
RESIDUAL / NOT THIS PEER
========================================================================

- RFC-002 named-gap still **open** (no Args/Walk modules).
- F20 full inventory still open. F26/F27 still CRIT.
- F39 verify is length-only. Not a security hash.
- Org wireguard residual is not product `ooda/std`.
- SPRINT not edited (Docs). Push: **none**.

end of crush_peer2_honesty.oot
