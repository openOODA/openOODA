# f14_domain_a.oot — Domain Expert A / F14 CRIT
# Residual: Phantom stdlib net paths (http_server/dns_server/mqtt/websocket/quic)
# Date: 2026-08-12
# Role: Domain Expert A of 12-agent crush SPRINT CRITs
# Tree: /home/jeryd/Projects/openOODA
# OWN: confirm on disk; fix remaining FALSE claims (CAPS already honest);
#      if std_caps_batch_2.oot deleted, write f14_phantom_net_close.oot
#      (ABSENT + only dns.oo / wireguard residual). Do not invent modules.
#      When closed: tell Docs Writer to REMOVE F14 from SPRINT.oot.
#      No push.

========================================================================
VERDICT
========================================================================

**CLOSED** (honesty). No product `.oo` this turn. No modules invented.

The five named net files **do not exist**. `audit/std_caps_batch_2.oot`
is **deleted**. Live net residual is **only** `ooda/std/src/net/dns.oo`
and `std/src/net/wireguard.oo`. Product facades (`os/net.oo`) are real
and are **not** those five names.

CAPS.oot already said batch_2 was phantom. Remaining live false claims
were PM §3.1 ("5 net libs … verified per batch_2") and RFC-003 citing
the deleted file. Both fixed. Canonical close:
`audit/f14_phantom_net_close.oot`.

========================================================================
ON-DISK PROVE (2026-08-12T20:08Z)
========================================================================

```bash
cd /home/jeryd/Projects/openOODA
test ! -e openOODA/audit/std_caps_batch_2.oot && echo ABSENT_batch_2
# observed: ABSENT_batch_2  (batch_3 also ABSENT)

for f in http_server dns_server mqtt websocket quic; do
  find ooda/std std/src/net -type f -name "${f}.oo"
done
# observed: empty (all five ABSENT under ooda/std and std/src/net)

find ooda/std -path '*/net/*' -name '*.oo' | sort
# observed: ooda/std/src/net/dns.oo

find std/src/net -maxdepth 1 -name '*.oo' | sort
# observed: std/src/net/wireguard.oo

grep -rln --include='*.oo' -E 'fn (http_server|dns_server|mqtt|websocket|quic)\b' \
  ooda/std std/src
# observed: NO_FN
```

| Probe | Result |
|-------|--------|
| `openOODA/audit/std_caps_batch_2.oot` | **ABSENT** (deleted) |
| `std/src/net/{http_server,dns_server,mqtt,websocket,quic}.oo` | **ABSENT** |
| `ooda/std/src/net/` same five | **ABSENT** |
| `fn` names for those five | **NO_FN** |
| `ooda/std/src/net/dns.oo` | **31** lines; `&UdpCap`; residual Err |
| `std/src/net/wireguard.oo` | **68** lines; `&UdpCap`; `process_exit(1)` |
| `ooda/std/os/net.oo` | product facade (not a phantom name) |
| `std/src/net.oo` | domain twin facade |

========================================================================
FALSE CLAIMS FIXED (not already honest in CAPS)
========================================================================

| Doc | Was | Now |
|-----|-----|-----|
| `PM.oot` §3.1 | "5 net libs granular … verified per `std_caps_batch_2.oot`" | ABSENT five; residual dns + wireguard; close pointer |
| `RFC-003-StdLib_Audit.oot` | "partially migrated (see `std_caps_batch_2.oot`)" | ABSENT five; residual dns + wireguard; close pointer |
| `audit/audit_2026-08-12_run.oot` F14 | open residual "rewrite batch_2" | **CLOSED** + close pointer |

**Left honest (no edit):** `CAPS.oot`, `CAPS_MATRIX.oot`,
`cap_g6_dns_batch2_honesty.oot`, peer/security audits that already
say **absent / phantom**.

**Not this OWN:** F19 broader RFC-002/003 (Process.Args / FS.Walk /
String regex). F31 crypto phantoms. Do not invent net servers.

========================================================================
FILES TOUCHED
========================================================================

| File | Change |
|------|--------|
| `openOODA/PM.oot` | §3.1 honesty |
| `openOODA/RFC-003-StdLib_Audit.oot` | F14 residual cite |
| `openOODA/audit/audit_2026-08-12_run.oot` | F14 CLOSED |
| `openOODA/audit/f14_phantom_net_close.oot` | **new** canonical close |
| `openOODA/audit/f14_domain_a.oot` | this evidence |

**Not touched:** any `http_server`/`dns_server`/`mqtt`/`websocket`/`quic`
source (none to touch). `SPRINT.oot`. Remotes (no push).

========================================================================
DOCS WRITER
========================================================================

F14 is **truly closed**. **REMOVE F14 from SPRINT.oot.**

Do not re-open F14 by minting the five files. F19 stays HIGH for
RFC-002/003 named-gap honesty that is **not** these net paths.

========================================================================
RESIDUAL HONESTY (do not claim closed)
========================================================================

1. Real UDP DNS product — still residual (`dns.oo` localhost + Err).
2. Real WireGuard — still residual (`process_exit(1)`).
3. Product HTTP/TCP/UDP/Bind facades — exist; **not** F14's five names.
4. F19 HIGH roll-up — RFC-002 Process.Args / FS.Walk / String still open.
5. F20/F34 std inventory — not this close.
6. SysCap bulk residual — not this close.

========================================================================
STATUS FOR SPRINT/PM
========================================================================

| Item | Status |
|------|--------|
| Five phantom net paths | **ABSENT** (confirmed) |
| `std_caps_batch_2.oot` | **DELETED** |
| Live net residual | **dns.oo + wireguard.oo only** |
| PM / RFC-003 false cites | **FIXED** |
| Canonical close | `audit/f14_phantom_net_close.oot` |
| SPRINT F14 row | **Docs Writer: REMOVE** |
| Push | **none** |

end of f14_domain_a.oot
