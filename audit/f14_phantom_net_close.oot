# f14_phantom_net_close.oot — F14 CRIT close
# Date: 2026-08-12
# Role: Domain Expert A (crush SPRINT CRITs)
# OWN: phantom std net paths. No invent. No push.

========================================================================
PLAIN ENGLISH (RULES 1.10)
========================================================================

**What is true**
Five named std net files are **not on disk**. An old note
(`audit/std_caps_batch_2.oot`) said they were migrated. That note is
**deleted**. Citing it as land evidence is false.

**What exists (net residual only)**
- `ooda/std/src/net/dns.oo` — DNS residual (`&UdpCap`; no real UDP DNS)
- `std/src/net/wireguard.oo` — WireGuard residual (fail-closed)

**What also exists (not the five phantoms)**
Product facades `ooda/std/os/net.oo` + twin `std/src/net.oo`
(Http/Tcp/Udp/Bind). Sec-net (jwt/mtls/waf) is a different tree.

**What is not true**
`http_server` / `dns_server` / `mqtt` / `websocket` / `quic` landed.
Do **not** create those files to paper over F14.

========================================================================
VERDICT
========================================================================

| Path / claim | Status |
|--------------|--------|
| `audit/std_caps_batch_2.oot` | **DELETED** |
| `std/src/net/http_server.oo` | **ABSENT** |
| `std/src/net/dns_server.oo` | **ABSENT** |
| `std/src/net/mqtt.oo` | **ABSENT** |
| `std/src/net/websocket.oo` | **ABSENT** |
| `std/src/net/quic.oo` | **ABSENT** |
| same five under `ooda/std/src/net/` | **ABSENT** |
| `fn http_server` / `fn dns_server` / `fn mqtt` / `fn websocket` / `fn quic` | **NO_FN** |
| `ooda/std/src/net/dns.oo` | **PRESENT** residual (31 lines) |
| `std/src/net/wireguard.oo` | **PRESENT** residual (68 lines) |

**F14 CLOSED.** Honesty close only. No modules invented.

CAPS.oot + CAPS_MATRIX.oot already named the phantom. This file
replaces the deleted batch_2 note.

========================================================================
LIVE TREE (2026-08-12T20:08Z)
========================================================================

```
ABSENT  openOODA/audit/std_caps_batch_2.oot
ABSENT  http_server.oo dns_server.oo mqtt.oo websocket.oo quic.oo
PRESENT ooda/std/src/net/dns.oo
PRESENT std/src/net/wireguard.oo
NO_FN   (ooda/std + std/src)
```

`ooda/std` `*/net/*` = `dns.oo` only.
`std/src/net/` (maxdepth 1) = `wireguard.oo` only.

========================================================================
DOCS FIXED THIS PASS
========================================================================

| Doc | Change |
|-----|--------|
| `openOODA/PM.oot` §3.1 | Removed "5 net libs … batch_2" false verify |
| `openOODA/RFC-003-StdLib_Audit.oot` | Dropped deleted batch_2 cite; ABSENT + residual |
| `openOODA/audit/audit_2026-08-12_run.oot` F14 | Status CLOSED |
| This file | Canonical close |

**Not edited:** `CAPS.oot` (already honest). `SPRINT.oot` (Docs Writer).

========================================================================
DOCS WRITER
========================================================================

**REMOVE F14 from SPRINT.oot.** Finding closed. Do not re-list as open CRIT.

F19 (HIGH) still open: broader RFC-002/003 honesty (Process.Args /
FS.Walk / String) — **not** these five net paths.

========================================================================
DO NOT
========================================================================

- Invent `http_server.oo` / `dns_server.oo` / `mqtt.oo` /
  `websocket.oo` / `quic.oo`.
- Cite deleted `std_caps_batch_2.oot` as CAP-G6 land.
- Claim product DNS or product WireGuard.
- Push.

end of f14_phantom_net_close.oot
