# f1_design_cap.oot — F1 MED DESIGN 284 > 256
# Date: 2026-08-12
# Role: Docs F1. OWN: formalize north-star exception. **No push.**

========================================================================
PLAIN ENGLISH (RULES 1.10)
========================================================================

**What changed**
`DESIGN.oot` is 284 lines. RULES §1.7 says split at 256. The appendix
already self-flagged, but the count was a lie (311 before appendix)
and it hedged "exception or split later." That is now one formal
exception: north star may exceed 256; do not peel the appendix.

**Why it matters**
DESIGN is the single north star + folded spec. Splitting would
duplicate law. A hedge is not a close. F1 is closed by exception,
not by apoptosis.

**What is not done**
File is still 284 > 256. Product `.oo` cap (F27) is unchanged.
No push.

========================================================================
BEFORE / AFTER (DESIGN L146)
========================================================================

BEFORE (stale + hedge):
> This file is 311 lines before appendix — combined >256, exceeds
> RULES §1.7. Keep as exception for north star, or split appendix
> to `DESIGN_SPEC_APPENDIX.oot` later.

AFTER (formal exception):
> **RULES §1.7 exception:** This north-star file may exceed 256
> lines (`wc -l` = 284). Do not split the appendix.

========================================================================
SESSION
========================================================================

| Field | Value |
|-------|-------|
| ROOT | `/home/jeryd/Projects/openOODA` |
| HAS_DESIGN | yes (`openOODA/DESIGN.oot`) |
| Path | exception (preferred), not split |
| Hygiene | `wc -l` DESIGN=284; this ledger ≤256 |
| Push | **none** |
| SPRINT.oot | F1 MED bullet **removed** (exception formal) |

========================================================================
REPRO
========================================================================

```
wc -l openOODA/DESIGN.oot
# 284
grep -n 'RULES §1.7 exception' openOODA/DESIGN.oot
# 146:**RULES §1.7 exception:** ... (`wc -l` = 284). Do not split
grep -n '311 lines before appendix' openOODA/DESIGN.oot
# (no matches)
```

========================================================================
ANTI-GREENWASH
========================================================================

| Claim | Truth |
|-------|-------|
| "DESIGN is now ≤256" | **FALSE** — still 284 |
| "Appendix was split" | **FALSE** — exception, not peel |
| "311 lines before appendix" | **FALSE** — stale; file is 284 total |
| "F27 / product cap closed" | **FALSE** — F1 is docs only |
| "beta / RULES 1.7 repealed" | **FALSE** — one-file north-star exception |

**OVERALL: F1 CLOSED** by formal north-star exception.

end of f1_design_cap.oot
