# f26_bool_err.oot — Domain Expert typecheck. No push. No SPRINT. No product edit.
# Date: 2026-08-12. OWN was check_scan.oo + tc return-lit. STOP: not 1-file without breaking Bool.
# 10-line summary
1. RESEARCH: `true` is NOT String. f26e+seed `tokens` → KW_TRUE; `return true`/`return false`/`let b: Bool = true` OK; isolated `check_scan.oo` OK.
2. 36:5 is modular `strip_import_lines` of `check_cap_util.oo` (1 import). Stripped L36 = `    return sealed_net_kind_of(name) != "";` in `is_sealed_net` → Bool.
3. Product `typecheck_return_and_assign_calls` types `return f(` as f's ret only (ignores `!= ""`). `sealed_net_kind_of` → String ⇒ "return type String does not match declared Bool". Seed check of same replica OK (weaker).
4. Same class next: util L55/L81 `is_sealed_fs`/`is_sealed_sys`; later `c_binop_prec(...) > 0`, `c_env_get(...) == "1"` (secret — not owned), `val_to_int(v) != 0`.
5. Casts that BREAK Bool: `return 1 == 1` → Int (ann_check_return first token); `return "a" == "a"` → same String-vs-Bool. Do not land.
6. Cast seed+product both emit: `if kind_of(x) != "" { return true; } return false;` — both check OK + emit-c. Not OWN (check_cap_util). One file cannot clear all sites.
7. tc_ann_return first-token walk is not this emitter. tc_call_ret SEMI-only (skip binop after call) would close the class after rebuild — 2nd file, not return-lit, not this host.
8. No edit. F26e check of main.oo stays this ERR until multi-site if/true rewrite or rebuilt TC. Residual: return-call vs binop; first-token return-lit.
