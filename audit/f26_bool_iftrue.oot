# f26_bool_iftrue.oot — first 36:5 class in check_cap_util
# Date: 2026-08-12. No push. Product ELF unchanged.

36:5 on modular check is `is_sealed_net`: `return sealed_net_kind_of(name) != ""`.
TC uses only callee ret (String), ignores `!=`.

Rewrote is_sealed_net/fs/sys to if / return true / return false.
File 222 ≤256.

Replica on product 3fb94504:
- `return kind_of(name) != ""` → ERR String vs Bool
- if/true/false → OK

Full main.oo check not re-run (57s / 2.4GiB). Next error may be the same class elsewhere.

end of f26_bool_iftrue.oot
