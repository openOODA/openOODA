# f26_cheap_scan.oot — parent Domain work (F26)
# Date: 2026-08-12
# Role: Coordinator + Domain Expert (cheap name scan)
# No push. F26 NOT closed until p7_check_main_smoke exits 0.

## Darwin

| Path | Result |
|------|--------|
| A tokenize_lines per module | LOST — 1.8GiB RSS / 120s still in first loop (f26_p7_integration.oot) |
| B char-walk + skip // and strings | WINNER in source — not yet in host |

## Source land

| File | Lines | Change |
|------|-------|--------|
| oodac/check_scan.oo | 149 | NEW pub scan_fn_names + names_to_depth0_binds |
| oodac/check_collect.oo | 151 | import check_scan.oo; tokenize scan removed |

grep tokenize_lines in check_scan.oo: none (comment only).
Immune: scan_skip_line_comment + scan_skip_string so `"fn leak"` / `// fn x` are not binds.

## Rebuild

Isolated snapshot /tmp/oodac_f26src (161 modules) + PURE_SKIP_CHECK=1
(old host check of main is the hang — cannot use check-gate to build the fix).
Out: /tmp/oodac_f26 (in flight).

## Residual

150× run_check_core_binds still tokenizes for real check.
Do not close F26 until smoke 0 on a host that contains check_scan.

end of f26_cheap_scan.oot
