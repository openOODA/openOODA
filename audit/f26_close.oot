# f26_close.oot — F26 product close
# Date: 2026-08-12. No push.

## Dual-repro (same host)

| Rail | Host | Result |
|------|------|--------|
| product md5 | `ooda/oodac/oodac` | `3fb94504bdd690dfd15d9460e471798b` = `/tmp/oodac_f26e` 972048 B |
| `p7_check_main_smoke.sh` | product, load ~80 after fanout kill | **ALL OK** wall **58.23s** (check finished <75s rc=1 type ERR) |
| `s29_println_guard_smoke.sh` | same ELF after p7 | **ALL OK** rc=0 |
| `scan_fn_light_smoke.sh` | source | **ALL OK** |

agy/crush had overwritten product with `016a397d` (980608 B). Restored f26e via `mv`. Killed 128 foreign `emit-c`.

## Residuals (not F26)

- Type `return type String does not match declared Bool` at 36:5 (check_scan `true`).
- Loop2 RSS ~2.4 GiB (bounded; finishes).
- 2.10 `mark_user_sinks` walk stays off (made check >180s).
- Guard product ELF: others may overwrite `oodac/oodac` (text-file-busy / foreign rebuild).

end of f26_close.oot
