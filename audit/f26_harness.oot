# f26_harness.oot — F26/P7 Test Harness Writer
# Date: 2026-08-12T20:21:50Z
# Worker: Grok Build (Test Harness Writer — crush phalanx F26)
# Policy: no push. No SPRINT.oot edit. No check_*.oo edit.
# Own: p7 FAIL-string honesty + scan_fn_light_smoke.sh + this ledger.

## Verdict

| Item | Result | Exit | Wall | Notes |
|------|--------|------|------|-------|
| **p7 FAIL string** | **FIXED** | — | — | stale "needs rebuilt host with modular check" → honest "modular path still over 45s (scan/check too slow)" |
| **p7 inner timeout** | **LEFT 45s** | — | — | not raised; this worker did not measure `oodac check oodac/main.oo` finishing |
| **`p7_check_main_smoke.sh`** (`timeout 50`) | **FAIL** | **1** | **45.64s** | tiny OK; large `main.oo` rc=124 at 45s |
| **`scan_fn_light_smoke.sh`** | **PASS** | **0** | <1s | source-level dual-repro; no host |
| **F26** | **OPEN** | — | — | p7 did not exit 0 |

**F26 stays OPEN.** p7 exit 1. Do not close F26. Do not drop deadlock from SPRINT.

---

## TIP SHA / binary

```
parent   git rev-parse --short HEAD → eaa555d
ooda     git rev-parse --short HEAD → 52cc4b5
openOODA git rev-parse --short HEAD → 81f56fc
date -u                             → 2026-08-12T20:20:58Z (pre-p7)
p7 prove                            → 2026-08-12T20:21:50Z
```

| Item | Value |
|------|--------|
| Host | `ooda/oodac/oodac` **963080** B @ 2026-08-12 10:04 |
| md5 host | `35505fc520e997a9421089f11bdffd92` |
| p7 script md5 | `7a7fa15a85e7095002eda1e920bc1667` |
| light smoke md5 | `a8dadabf832876398129501ef1343210` |
| p7 L32 | `timeout 45` **unchanged** |
| p7 L36 | `modular path still over 45s (scan/check too slow)` |

---

## 1) Assigned p7 prove

Command (exact):

```
cd /home/jeryd/Projects/openOODA
timeout 50 bash ooda/scripts/p7_check_main_smoke.sh
```

`/usr/bin/time` + outer `timeout 50` (wait cap 55s). Did **not** wait longer.

```
OK tiny main.oo check <8s
FAIL oodac/main.oo check timeout 45s (slow tree — R4/R5; modular path still over 45s (scan/check too slow))
p7_check_main_smoke: FAILED
# wall_s=45.64  outer_rc=1
```

Tiny rail PASS. Large rail **timeout 45s** (script maps rc=124 → FAIL). Honest string is what printed.

Did **not** run a longer `oodac check oodac/main.oo` this turn. Prior integration prove (`audit/f26_p7_integration.oot`) already showed 120s still rc=124 / 1.8 GiB RSS. No new finish-time to write. **45s stays.**

---

## 2) New rail: `ooda/scripts/scan_fn_light_smoke.sh`

Source-level dual-repro. Does not need a rebuilt host. Does not exec `oodac`.

| Rail | What | This tree |
|------|------|-----------|
| no tokenize | `scan_fn_names` body in `check_scan.oo` / `check_collect.oo` (if def) must **not** contain `tokenize_lines` | **OK** — cheap char-walk in `check_scan.oo`; `check_collect.oo` imports, no def |
| skip | comment-skip or string-skip near scan (`scan_skip_line_comment` / `scan_skip_string` / `"//"` / `"\""`) | **OK** — both skip helpers + `"\""` / `//` walk |

```
OK bash -n scan_fn_light_smoke.sh
OK oodac/check_scan.oo: scan_fn_names has no tokenize_lines
OK oodac/check_scan.oo: comment-skip or string-skip present near scan
OK oodac/check_collect.oo: no scan_fn_names def (ok if imported)
scan_fn_light_smoke: ALL OK
```

Immune: putting `tokenize_lines` back inside `scan_fn_names` fails this smoke **before** another 1.8GiB hang. Product `check_*.oo` not edited this turn (parent owns cheap scan). Later: tiny `.oo` fixture can unit-test the walker on a rebuilt host.

---

## 3) Residual

- **F26 CRIT** until `p7_check_main_smoke.sh` exits 0 (large `oodac/main.oo` **finishes** <45s, any rc).
- Cheap `scan_fn_names` is in source (`check_scan.oo`); shipping ELF may still be the tokenize-era name-scan. Light smoke is source-only; it cannot green p7.
- Do not raise p7's 45s unless a worker measures `oodac check oodac/main.oo` finishing and writes the wall number here.

No SPRINT edit. No `check_*.oo` edit. No push.

end of f26_harness.oot
