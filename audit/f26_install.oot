# f26_install.oot — product ELF swapped; p7 not closed
# Date: 2026-08-12. No push.

## Install

| Item | Value |
|------|--------|
| Wanted | `/tmp/oodac_f26e` md5 `3fb94504` 972048 B |
| First `cp` | FAIL `Text file busy` (old ELF mapped) |
| Atomic `mv` | OK — product now `3fb94504` 972048 B |
| s29 on product | **0** ALL OK |
| Isolated p7 `OODAC_BIN=/tmp/oodac_f26e` | **0** wall 58.70s (before install) |
| Product p7 after install | **1** timeout 75s (twice; second after killing a peer check) |

## Honest close rule

Need p7=0 AND s29=0 on the **product path**. s29 is 0. p7 is not.
Do not remove F26. Tree/load now exceeds the 75s bound that matched the 56.86s isolate.

end of f26_install.oot
