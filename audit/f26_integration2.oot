# f26_integration2.oot — Integration Tester (crush phalanx, pass 2)
# Date: 2026-08-12T20:59:54Z
# Worker: Grok Build (Integration Tester)
# Cwd: /home/jeryd/Projects/openOODA
# Host under test: /tmp/oodac_f26d (HEAD+scan + live 2.9/2.10)
# Policy: no push. No SPRINT edit. No oodac_pure_build_fixed.sh. No second rebuild.
# Install product only if p7 AND s29 both exit 0 on THIS host.

## Verdict (honest)

| Rail | Result | Exit | Wall | Notes |
|------|--------|------|------|-------|
| **scan_fn_light_smoke.sh** | **PASS** | **0** | 0.16s | source-only; no tokenize_lines in scan_fn_names |
| **p7_check_main_smoke.sh** | **FAIL** | **1** | 45.13s | tiny OK; `main.oo` timeout 45s |
| **s29_println_guard_smoke.sh** | **PASS** | **0** | 0.30s | dual-path refuse + no-FP + eprintln; 2.9 LIVE |
| **p6_c_runtime_qa_smoke.sh** | **PASS** | **0** | 15.43s | execvp / env / FFI / writedir + T3 |
| **product install** | **NOT DONE** | — | — | p7 != 0; left `35505fc5` |
| **Push** | **NOT DONE** | — | — | human-gated |

**F26 closable? NO.** Close rule is p7 exit 0 AND s29 exit 0 on the SAME host.
This host has s29=0 and p7=1. Do **not** drop F26 from SPRINT.

Did **not** raise the 45s inside p7: no isolated `check oodac/main.oo` **finish**
was measured on f26d (90s isolated and 180s competing both rc=124, 0 bytes out).

---

## Hosts

```
parent   git rev-parse --short HEAD → eaa555d
ooda     git rev-parse --short HEAD → 52cc4b5
openOODA git rev-parse --short HEAD → 81f56fc
date -u                             → 2026-08-12T20:59:54Z
```

| Item | f26 (prior isolated) | f26d (this pass) | product (untouched) |
|------|----------------------|------------------|---------------------|
| Path | `/tmp/oodac_f26` | `/tmp/oodac_f26d` | `ooda/oodac/oodac` |
| Size | 967784 B | 972048 B | 963080 B |
| md5 | `8c315422` | `b4f942ae` | `35505fc5` |
| sha256 | (prior) | `eb01e6d78c566805…ad77fa9f` | `4e7046e26589ac71…22a8ce73` |
| BuildID | — | `fac396c3c97371dedf64af0e13e61d117af971e2` | `abf4360f…` |
| mtime | 10:35 HST | 10:54 HST | 10:04 HST |
| cheap scan_skip_* | YES | YES | **NO** (tokenize scan) |
| c_secret_is_user_sink | **NO** | YES | **NO** (old 2.9) |
| s29 | FAIL grouped SECRET | **PASS** | PASS (prior p7b prove) |
| p7 | FAIL 45s (finish 56.72s type ERR) | **FAIL 45s** (no finish seen) | FAIL 45s / 1.8GiB |

f26d src snapshot `/tmp/oodac_f26d_src` (160 .oo, 151 modules, unique_fns=459)
md5-matches live WT for check_scan + c_emit_print/secret/secret_guard/secret_alias.

Official log `/tmp/oodac_f26d_build.log` first gcc: `chs_rt_sys.c:438` syntax
error (file was mid-edit; mtime 10:51), `BUILD elapsed=61.94 rc=1`. ELF still
appeared at 10:54 as a valid not-stripped executable (tiny check OK; symbols
present). Second writer did not append `OK_PURE_MULTI` to that log. Host is
**usable**, provenance of the successful link is **not** the rc=1 log line.

---

## Assigned smokes (OODAC_BIN=/tmp/oodac_f26d)

Order as tasked. Cwd `/home/jeryd/Projects/openOODA`.

```
timeout 20 bash ooda/scripts/scan_fn_light_smoke.sh   → 0  wall=0.16
timeout 80 bash ooda/scripts/p7_check_main_smoke.sh    → 1  wall=45.13
timeout 30 bash ooda/scripts/s29_println_guard_smoke.sh → 0  wall=0.30
timeout 40 bash ooda/scripts/p6_c_runtime_qa_smoke.sh  → 0  wall=15.43
```

Script md5: scan `a8dadabf` / p7 `7a7fa15a` / s29 `2ad0ee95` / p6 `9390faf2`.

### scan (20:54:57Z, load 67)

```
OK oodac/check_scan.oo: scan_fn_names has no tokenize_lines
OK oodac/check_scan.oo: comment-skip or string-skip present near scan
OK oodac/check_collect.oo: no scan_fn_names def (ok if imported)
scan_fn_light_smoke: ALL OK
```

### p7 (20:56:22Z, load 41.57; competing peer1 180s check)

```
OK tiny main.oo check <8s
FAIL oodac/main.oo check timeout 45s (slow tree — R4/R5; modular path still over 45s)
p7_check_main_smoke: FAILED
P7 wall=45.13 rss_max_kb=220728 rc=1
```

Tiny rail proves the ELF runs. Large rail is a hang-or-too-slow, **not** a
finished type ERR. Smoke would accept a finished non-zero. We never got there.

### s29 (20:57:28Z, load 27.65)

All 12 dual-path rails OK (bare IDENT, string-lit no-FP, H-later `(1), tok`,
H-same `("x")+tok`, grouped public no-FP, eprintln IDENT). **2.9 is live in
this ELF** — the miss that made s29 RED on `/tmp/oodac_f26`.

### p6 (20:57:28Z)

Static fail-closed + `runtime_zt_t3_smoke` 0. Residual: not ASAN/fuzzer.

---

## Extra check timings on the SAME ELF (not a smoke raise)

| Run | Timeout | Wall | rc | rss_max | stdout | Load / notes |
|-----|---------|------|----|---------|--------|--------------|
| isolated (other agent) | 90s | 90.10 | 124 | 185144 KB | 0 B | load 50–73; CPU ~18% starved |
| this p7 | 45s | 45.13 | 124 | 220728 KB | — | load 41; competed with peer1 |
| peer1 direct | 180s | ~180 | 124 | ~1.99 GiB | 0 B | died ~10:58:56 @ 1986488 KB |
| other 180s | 180s | 180.53 | 124 | **2095532 KB (2.00 GiB)** | 0 B | load ~21; grew 1.1→2.0 GiB |

Contrast: isolated `/tmp/oodac_f26 check oodac/main.oo` **finished 56.72s rc=1**
type ERR (f26_isolated_prove.oot). f26d never printed a byte in 45/90/180s
this pass. Under CPU the RSS curve is the old 1.8GiB tokenize climb — cheap
name-scan is **in** the ELF (`scan_fn_names` @ 0x4142c0, `scan_skip_line_comment`
@ 0x414120, `scan_skip_string` @ 0x4141e0) but **loop 2** in `check_mod.oo`
still calls `run_check_core_binds` per module (tokenize + secret + typecheck).
`run_check_module_body` still tokenizes (dead; no callers).

Did **not** start a third isolated f26d check after the 180s pair died
(load still 21; other agents then pointed p7 at product). No 45s edit.

---

## Product

`ooda/oodac/oodac` remains **35505fc5** / 963080 B / 10:04 HST.
`nm` has `scan_fn_names` + `c_secret_emit_guard` + `c_secret_refuse_println_ident`
and **lacks** `scan_skip_*` and `c_secret_is_user_sink` (tokenize scan + older 2.9).
No copy f26d → product (p7 != 0).

---

## Residual (not this worker)

1. F26 stays CRIT until `p7_check_main_smoke.sh` exits 0 on a host that also
   keeps s29=0. f26d is the first SAME-host split (s29 green, p7 red).
2. Cheap scan won the 1.8GiB **name-scan** Darwin in source; 150×
   `run_check_core_binds` still tokenizes. That is the live time/RSS killer.
3. Do not raise p7's 45s until someone measures an isolated **finish** on
   that host. f26 finished 56.72s — 45s was already short for that ELF.
4. Official f26d_build.log is rc=1; do not cite it as OK_PURE_MULTI.
5. Walker→`c_secret_emit_guard` unify still Domain Expert; s29 only claims
   refuse/no-FP.

No SPRINT edit. No push. No product overwrite.

end of f26_integration2.oot
