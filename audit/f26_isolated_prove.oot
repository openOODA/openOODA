# f26_isolated_prove.oot — isolated host /tmp/oodac_f26
# Date: 2026-08-12
# No push. Do not install over product (s29 regression).

## Host

| Item | Value |
|------|--------|
| Path | `/tmp/oodac_f26` |
| Size | 967784 B |
| md5 | `8c315422495e07ef4b9436d53faff06f` |
| Build | seed emit, PURE_SKIP_CHECK=1, 329.9s, OK_PURE_MULTI |
| Snapshot | HEAD oodac + check_scan + check_collect + check_mod + check_drive |
| Missed | live 2.9 `c_emit_print` / `c_emit_secret*` |

## Prove

| Rail | Result |
|------|--------|
| scan_fn_light_smoke | ALL OK |
| symbols | `scan_fn_names`, `scan_has_name`, `run_check_core_binds` present |
| p7 45s | FAIL timeout (honest — check needs 56.7s) |
| isolated check 90s | **finish 56.72s rc=1** type ERR Bool/String at 36:5 — **not a hang** |
| s29 | FAIL grouped SECRET (2.9 missing in this ELF) |
| p6 | ALL OK |

## Darwin

Cheap scan WON vs 1.8GiB tokenize hang.
45s smoke is shorter than measured finish. Raise only after a host that keeps s29.

Next: `/tmp/oodac_f26b` from **live** tree.

end of f26_isolated_prove.oot
