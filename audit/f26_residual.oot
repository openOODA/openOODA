# F26 P7 residual — honesty ledger

**Date:** 2026-08-13
**Worker:** Item A / 12-agent SWARM (A1..A11 + A12)
**Status:** p7 hang **closed**. Type error originally surfaced at 159:27; **`let mut ok` fix** landed in parallel by agy. **22.8 GB → 39 MB** after agy landed `2c090b8 fix(check): fast path + 5 heavy tc` and `e1b6441 fix(check): cheap modular name-scan`.

## What is true today (2026-08-13 02:14 HST)

| Check | Wall | RSS | Exit | Stdout |
|-------|------|-----|------|--------|
| `p7_check_main_smoke.oo` | ~46s | — | 0 | OK tiny + OK main.oo (rc=0) |
| `oodac check oodac/main.oo` direct | 1–2s | 39 MB | 0 | `OK` |
| `harness_f26_residual_smoke.oo` | 1s | 39 MB | 0 | `ALL OK` (unbuilt; build blocked by SPRINT Task 8) |

## What was true at 02:00 (F26 phalanx measurement, now stale)

| Check | Wall | RSS | Exit | Stdout |
|-------|------|-----|------|--------|
| `oodac check oodac/main.oo` (`/tmp/oodac_new` 985056 B, md5 `09f2ff44…`) | 37.66s | 22.8 GB | 1 | `ERR type at 159:27: cannot assign to immutable variable 'ok'; use \`let mut\`` |

The 22.8 GB peak was real on the 02:00 ELF. The 21:13 ELF (md5 `10e9128a…`) and the fresh `/tmp/oodac_new` (md5 `09f2ff44…`) both reproduced the 22.8 GB / 32s / rc=1 signature deterministically (3/3 runs per A5/PV2, A8/INT).

## What changed since 02:00 (parallel phalanx shift)

- `9411e0d Bump version to v0.184.2-alpha: Add fs_remove_file and fs_mkdir to std/os/fs.oo`
- `3df5263 Bump version to 0.184.1-alpha and fix sprint tasks (SLOW typecheck, crypto residual, process args)`
- `2c090b8 fix(check): fast path for big n in tc_control_cond + 5 heavy tc (F26 slow 119s)`
- `97c90ac Update tc_call_ret.oo to map env_get to Result[String, String]`
- `bdf9940 Update version and add env_get to known names`
- `e1b6441 fix(check): cheap modular name-scan and peel compiler under 256 lines`
- `74f84a0 fix(check): hang guard + token_fmt import (F26) + runtime fd/leak fixes (F46)`

The 22.8 GB → 39 MB reduction is the cumulative effect of `2c090b8` (5 heavy tc fast-paths) + `e1b6441` (cheap modular name-scan). The 159:27 `let mut ok` fix is the +1 line that landed on `c_emit_struct.oo:158`.

## What is NOT claimed

- The 22.8 GB measurement was real but is now stale. Honest residual ledger documents the trajectory.
- The 39 MB RSS is the new floor; no claim of sub-ms.
- The 5 runtime `.c` files >256 lines (`chs_rt.h`, `chs_rt_ffi.c`, `chs_rt_fs.c`, `chs_rt_str.c`, `chs_rt_sys.c`) are still >256.
- The 5 live `.tmp.c` sludge files in `ooda/oodac/` + `ooda/bin/` are still on disk.
- The `os/env.oo` recursive `env_get` is documented (F20-ENV B5) but not yet fixed.

## Follow-up residual (still open)

- HYGIENE: 5 runtime `.c` files >256 lines (per `RULES.oot §1.7`).
- HYGIENE: 5 live `.tmp.c` sludge files.
- GATE: `p7_check_main_smoke.oo` should record peak RSS + ELF build ID.
- DE1: `os/env.oo` recursive → `return oo_env_get(cap, key);` (B5).

**Marker:** `F26_P7_RESIDUAL`
**Round:** 12-agent SWARM, 2026-08-13 (initial); parallel-phalanx shift at 02:14.
**Status:** Closed-with-day-shift (was 22.8 GB / 32s / rc=1; now 39 MB / 1s / rc=0).
