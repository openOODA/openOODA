# f26_runtime.oot — Runtime auditor. 2026-08-12 10:49 HST. No push / no SPRINT / no rebuild.
WATCH /tmp/oodac_f26d_build.log: died 61.94s rc=1 after seed emit-c (unique_fns=459/151). No /tmp/oodac_f26d ELF.
OVERLAY: f26d HAS both c_emit_secret.oo (9814 B md5 08949c71 = f26c overlay) AND c_emit_secret_alias.oo (9241 B md5 8a06c8db = live tree). f26c alias was stale 5958 B / 157 L — no sink helpers.
PROOF overlay closed: unique_fns 457→459; cache oodac_pure_1324637/all.c defines c_secret_is_user_sink @18814 and c_secret_mark_user_sinks @18831. main.oo L105–106 imports both.
FIRST gcc error: ooda/runtime/chs_rt_sys.c:438 expected identifier or '(' before 'return' (then L439 before '}'). Extra '}' at L437 after oo_fetch. Uncommitted WIP (sys_args + fetch write-loop). NOT a .oo.
MAP: no .oo. Live chs_rt_sys.c mtime 10:42. Same first error as f26c; f26c then also hit implicit-decl (now gone).
SMALLEST FIX: delete the stray `}` at chs_rt_sys.c:437 (1 line). Not a 5-line missing import — not applied.
scan_has_name: /tmp/oodac_f26 T @0x413c00 (also scan_charset_has, scan_fn_names). check_scan.oo md5 0ad05bbe identical f26src/f26c/f26d. Char-walk; `.contains` only in L8 comment.
f26d all.c also emitted scan_has_name (proto L175, body L3558). Seed emit of scan path OK; gcc never reached all.c.
BLOCKER for next snapshot: runtime brace, not secret overlay.
