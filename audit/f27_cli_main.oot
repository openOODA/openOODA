# f27_cli_main.oot -- Hygiene F27 leftover cli/main.oo (262). No push. No SPRINT.
# Date: 2026-08-12T21:07Z  Role: Hygiene. OWN ooda/cli/*.oo only.
# Cut: build flag parse vs dispatch. Mirrors cli_run_args. One helper.
# NEW ooda/cli/build_args.oo (81). main.oo 262->191 + import "build_args.oo".
# pub fn once: cli_build_args (build_args.oo:6). Dispatch + product_sh stay in main.
# PROVE: wc -l 191 / 81 (both <=256). timeout 20 oodac check both -> OK rc=0.
# Untouched: SPRINT.oot PM.oot oodac/* product_sh.oo pkg.oo. Host not rebuilt.
# Residual: no CLI dual-repro/rebuild; F27 oodac stamp unchanged (already wc-empty).
