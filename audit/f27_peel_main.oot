# f27_peel_main.oot -- F27 peel main.oo CLI front-end (main now <=256)
# Date: 2026-08-12  Role: Domain Expert F27 last file. No push. No SPRINT edit.
# Cut: closed CLI helper parse_cli_argv (argv + path/size + HITL re-exec) vs dispatch.
# NEW ooda/oodac/cli_parse.oo (112). main.oo 308->233 + one import. barrel kept (120).
# pub fn once: parse_cli_argv | main stays in main.oo. No import lines dropped.
# PROVE: wc -l 233 / 112 (both <=256). grep fn parse_cli_argv = 1 (cli_parse.oo:7).
# timeout 20 ./oodac/oodac check oodac/cli_parse.oo -> FINISH rc=1 shell_sq (~0.19s)
# timeout 20 ./oodac/oodac check oodac/main.oo -> rc=124 (pre-existing F26 graph hang)
# F27: main.oo <=256. Host not rebuilt. No CLI dual-repro. Dispatch left in main.
# Untouched: SPRINT.oot, check_*, c_emit_*, load_import*, lex*, cli_build/emit.
# Residual: product check of main graph still F26-slow (1.8GiB); isolated helper finishes.
# Collect: starts at main.oo; cli_parse wired via import; barrel not moved.
