# f27_peel_secprop.oot — F27 peel c_emit_secret_prop (NOT F27 FIXED)
# Date: 2026-08-12  Role: Domain Expert  No push. No SPRINT.oot edit.
# Cut: c_secret_prop_from_rhs_scan (nested LPAREN/brace scan, now pub).
# NEW ooda/oodac/c_emit_secret_prop_scan.oo (70). prop 262→198 + one import.
# Stayed: c_secret_mark_returning_fns, c_secret_prop_from_rhs. main.oo unchanged.
# PROVE: wc -l 198 / 70 (both ≤256). scan fn once (only helper). import once L7.
# timeout 20 ./oodac/oodac check oodac/c_emit_secret_prop.oo → rc=1 field_at (~6s)
# timeout 20 ./oodac/oodac check oodac/c_emit_secret_prop_scan.oo → rc=1 field_at (~0.2s)
# F27 still OPEN: main.oo 308. Untouched: check_*.oo c_emit_secret.oo let_ext lex SPRINT.
# Residual: host not rebuilt; isolated check type-fails field_at (builtin); no emit-c dual-repro.
