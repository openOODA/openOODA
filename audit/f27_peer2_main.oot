# f27_peer2_main.oot -- Peer Verifier 2 F27 main.oo peel. ACCEPT. No push. No SPRINT.
# Date: 2026-08-12T20:50Z  Role: Peer Verifier 2. Zero-trust. Own this audit only.
# wc: ooda/oodac/main.oo 233 <=256 (HEAD 307; SPRINT/peel said 308 after skip_grant import).
# import still on main: lex.oo:5 check_collect.oo:23 check_mod.oo:24 load_import.oo:25.
# collect starts at main.oo (120 import lines). cli_parse.oo has 0 imports; barrel not moved.
# check_scan pulled: main -> check_collect.oo:3 import "check_scan.oo". Not a direct main import.
# pub fn main once in main.oo:128. parse_cli_argv once in cli_parse.oo:7 (was inline in HEAD main).
# Other oodac/*.oo >256: only tc_names.oo 264. At cap: bc_emit_expr.oo 256, c_emit_skip.oo 256.
# REJECT test fail: required imports not moved off collect root. SPRINT untouched (still lists 308).
# Residual: F27 CRIT still open vs SPRINT until stamp; host not rebuilt; product check main still F26-slow.
