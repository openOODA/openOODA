# f44_md_grep.oot — F44 HIGH phantom .md greps in cap smokes
# Date: 2026-08-12. OWN: 3 cap smokes only. No SPRINT. No push. No cap logic.
# Changed: STATIC_CAPS/CAP_FFI/CAPS_MATRIX/BIOMETRIC_CAPS greps .md → .oot.
# Files: ooda/scripts/cap_forge_path_a_smoke.oo, cap_ffi_residual_smoke.oo, biometric_caps_residual_smoke.oo
# Dual-repro: all three PASSED rc=0 twice (forge also refuses bare dlopen E_CAP).
# Grep finds: bootstrap/{STATIC_CAPS,CAP_FFI,CAPS_MATRIX,BIOMETRIC_CAPS}.oot present; .md absent.
# Residual: other residual_* smokes still pin .md (not F44). product_floor may still cascade on F46.
# Status: F44 CLOSED.
