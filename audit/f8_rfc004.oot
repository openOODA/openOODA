# f8_rfc004.oot — F8 RFC-004 stub close
# Date: 2026-08-12T20:25Z
# Role: Docs / Command — F8 LOW
# OWN: RFC-004 close by supersede. Do **not** activate a second inventory.
#      Do **not** edit SPRINT.oot. **No push.**

========================================================================
PLAIN ENGLISH (RULES 1.10)
========================================================================

**What changed**
RFC-004 used to say a swarm should burn 884 stubs. It had ten empty
boxes and no file list. The RFC now says it is **CLOSED /
superseded** by the SPRINT audit umbrella and
`audit/audit_2026-08-12_run.oot`.

**Why it matters**
A second purge RFC that repeats F26 / F27 / F20 is Pride (SINS #3)
and Sloth (SINS #4): a plan with no inventory. 884 is unproven.

**What is not done**
F26 still FAIL. F27 still over cap. F20 still needs classify.
No stubs burned. No product `.oo` this turn. SPRINT F8 row is
Docs Writer, not this OWN.

========================================================================
DARWIN
========================================================================

| Path | What | Result |
|------|------|--------|
| A | Activate RFC-004 ≤256 lines, point at F26/F27/F20 | **LOST** — copies SPRINT §3 umbrella |
| B | CLOSED / superseded by SPRINT + audit run | **WIN** |

Hunt for unique debt **not** on SPRINT: **none**. 884 has no
inventory. The only other "884-stub" hit is a stale line in
`audit/grok_work_up_verify.oot` (not a live owner).

========================================================================
SESSION
========================================================================

| Field | Value |
|-------|-------|
| ROOT | `/home/jeryd/Projects/openOODA` |
| HAS_DESIGN | yes (`openOODA/DESIGN.oot`) |
| SWARM plan | solo — RFC-004 + this ledger; no product `.oo` |
| Hygiene | `wc -l` on owned files; cap 256 |
| Smokes | none — INFO stub; absence of inventory is the proof |
| Push | **none** |
| SPRINT.oot | **not edited** |

========================================================================
SUMMARY
========================================================================

| # | Claim | Expected if old RFC-004 true | Actual | VERDICT |
|---|--------|------------------------------|--------|---------|
| 1 | 884 stubs identified | file list or count repro | **no list**; 884 only in this RFC + stale verify line | **UNPROVEN** |
| 2 | burndown 1–884 | some boxes checked + owners | all boxes were empty | **STUB** |
| 3 | unique debt off SPRINT | item not in F26/F27/F20 umbrella | **none found** | Path B |
| 4 | RFC restated | CLOSED / superseded | honesty banner + §Status | **HONEST** |
| 5 | Product this turn | none | no stubs; no product | **PASS** |
| 6 | Push / SPRINT | none / no edit | none / no edit | **PASS** |

**OVERALL: HONEST CLOSE PASS** — F8 closed by supersede, **not** by
a purge. Debt **still open** on SPRINT.

**Do not claim:** 884 stubs burned, 884 stubs exist, debt gone,
F26/F27/F20 closed, beta.

========================================================================
REPRO (disk, this turn 2026-08-12T20:25Z)
========================================================================

```
wc -l openOODA/RFC-004-Technical_Debt_Purge.oot
# before: 22

grep -rn --include='*.oot' --include='*.oo' '884' .
# RFC-004 (three lines) + audit/grok_work_up_verify.oot:145 only

find ooda/std -type f -name '*.oo' | wc -l
# 33  (SPRINT F20 still says ~31; F20 owns the classify)

wc -l ooda/oodac/*.oo | awk '$1>256'
# main.oo 308, load_import.oo 287, lex.oo 277,
# c_emit_let_ext.oo 263, c_emit_secret_prop.oo 262
# (c_emit_skip.oo 256 = at cap). F27 owns peel.
```

Live pointers (do **not** duplicate here):
- F26: `audit/f26_cheap_scan.oot`
- F27: SPRINT §3 CRIT row
- F20: SPRINT §3 HIGH row
- Umbrella: `audit/audit_2026-08-12_run.oot`

========================================================================
ANTI-GREENWASH
========================================================================

| Claim | Truth |
|-------|-------|
| "884 stubs identified" | **FALSE** — no inventory |
| "RFC-004 is a live purge plan" | **FALSE** — closed / superseded |
| "F8 closed means debt is gone" | **FALSE** — F26/F27/F20 still open |
| "Path A inventory is needed" | **FALSE** — would copy SPRINT |
| "beta / clean-code fixed" | **FALSE** — alpha; F27 still over cap |

========================================================================
HYGIENE
========================================================================

Owned files must stay ≤256 lines. No product `.oo`. No push.
No SPRINT.oot edit. No secrets. No temps.

========================================================================
NEXT
========================================================================

1. Peer: RFC-004 banner must say CLOSED / superseded, not "swarm
   ready." 884 must be UNPROVEN.
2. Docs Writer: mark F8 done on SPRINT.oot when they accept this.
3. Product: F26 smoke 0, then F27 peel, then F20 classify. Not here.
