# i17_drop_lex_probe.oot — Darwin: drop duplicate lex_all in check
# Date: 2026-08-12. No push. Source only (next rebuild picks it up).

tokenize_lines (token_fmt.oo) already calls lex_all and process_exit on ERR.
check_drive.run_check_core_binds had a second lex_all before tokenize_lines.
Removed the probe. Lex fail-closed still happens once, inside tokenize_lines.

Not in product ELF 3fb94504 until next rebuild. F26 stays closed on that host.

end of i17_drop_lex_probe.oot
