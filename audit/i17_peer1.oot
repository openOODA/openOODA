# i17_peer1.oot — Peer1 zero-trust. No push. No SPRINT.
1. ACCEPT I17. check_drive.oo has zero lex_all calls (L27–28 comments only; L29 tokenize_lines).
2. token_fmt.oo tokenize_lines still lex_all + ERR\tlex + process_exit(1) (L4–14). File clean vs HEAD.
3. Lex fail-closed not gone: lex.oo L233–236 FAIL CLOSED return Err; dump_tokens still probes.
4. WT vs ooda 52cc4b5: probe block deleted. Host ELF 3fb94504 stale (source-only; not rebuilt).
5. Residual not reject: collect_tokens stops on lex_step ERR without exit — tokenize probe is load-bearing.
6. Extra WT dirt (binds/secret + lex peel) is out of I17; fail-closed still present.
