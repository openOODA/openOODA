# meta_phalanx_hygiene.oot — Dependency / Hygiene Enforcer
# Date: 2026-08-11 (UTC 2026-08-12T04:57Z)
# Role: Dependency/Hygiene Enforcer (meta phalanx)
# Tree: /home/jeryd/Projects/openOODA  (ooda/ + openOODA/)
# Tip (ooda): f41c8ec
# OWN: metamorphic pack line-lock ≤256; /tmp purge on pack; bash -n smokes;
#      no ooda-root sludge from this work
# WRITE: this audit only (plus missing doc-claimed fixtures restore)

========================================================================
SUMMARY
========================================================================

| Check | VERDICT | Notes |
|-------|---------|-------|
| All metamorphic pack files ≤256 lines | **PASS** | max owned source = 106 (`c_emit_meta.oo`) |
| `c_emit_meta.oo` >256 → split to `c_emit_meta_decoy.oo` | **N/A** | 106 ≤ 256; no split; `main.oo` import unchanged |
| `/tmp` refs in meta pack committed surface | **PASS** | ZERO hits across pack (smokes use `$HOME/.cache/ooda-tmp`) |
| `bash -n` all metamorphic scripts | **PASS** | 5/5 syntax clean |
| No ooda-root sludge from this work | **PASS** | no `c_emit_meta_decoy.oo` / hygiene dumps under `ooda/` root |
| Claims match tree (doc fixtures) | **PASS** | restored missing shuffle + epoch fixtures claimed by `METAMORPHIC.md` |

OVERALL: **PASS**

========================================================================
LINE COUNTS TABLE
========================================================================

Measured live under `ooda/` at tip `f41c8ec` (hygiene pass).

| File | Lines | ≤256 | /tmp |
|------|------:|:----:|:----:|
| `oodac/c_emit_meta.oo` | 106 | PASS | none |
| `runtime/chs_rt_meta.c` | 46 | PASS | none |
| `scripts/metamorphic_path_a_smoke.sh` | 80 | PASS | none |
| `scripts/metamorphic_residual_smoke.sh` | 42 | PASS | none |
| `scripts/meta_vs_det_residual_smoke.sh` | 42 | PASS | none |
| `scripts/bare_metal_residual_smoke.sh` | 42 | PASS | none |
| `scripts/residual_path_a_floor_smoke.sh` | 106 | PASS | none |
| `fixtures/metamorphic_path_a.oo` | 18 | PASS | none |
| `fixtures/metamorphic_path_a_shuffle.oo` | 19 | PASS | none |
| `fixtures/metamorphic_epoch.oo` | 10 | PASS | none |
| `fixtures/metamorphic_marker.oo` | 9 | PASS | none |
| `fixtures/meta_vs_det_marker.oo` | 6 | PASS | none |
| `fixtures/baremetal_marker.oo` | 6 | PASS | none |
| `fixtures/residual_metamorphic_fail.oo` | 1 | PASS | none |
| `fixtures/residual_meta_vs_det_fail.oo` | 1 | PASS | none |
| `fixtures/residual_bare_metal_fail.oo` | 1 | PASS | none |
| `bootstrap/METAMORPHIC.md` | 80 | PASS | none |
| `bootstrap/META_VS_DET.md` | 39 | PASS | none |
| `bootstrap/BARE_METAL.md` | 39 | PASS | none |

Headroom: largest owned emit unit `c_emit_meta.oo` = **106 / 256** (150 lines free before split gate).

========================================================================
SPLIT GATE — c_emit_meta.oo
========================================================================

Rule: if `oodac/c_emit_meta.oo` > 256 lines → split decoy emitter to
`oodac/c_emit_meta_decoy.oo` and fix `oodac/main.oo` imports only.

Live:
  lines(c_emit_meta.oo) = 106
  split_needed          = NO
  c_emit_meta_decoy.oo  = not created (correct)
  main.oo L104          = `import "c_emit_meta.oo";` only (unchanged)

Contents retained in single unit (under pressure bar):
  - `c_parse_metamorphic_path_a`
  - `c_meta_layout_emit_decoys` (multi-pass body-mode gate)
  - `c_emit_meta_decoy`

========================================================================
/tmp PURGE (meta pack)
========================================================================

Scan (grep `/tmp` on full meta pack listed above): **ZERO hits**.

Smokes already prefer non-/tmp temps:
  `export TMPDIR="${TMPDIR:-$HOME/.cache/ooda-tmp}"`
  `mkdir -p "$TMPDIR"`

No committed meta-pack file needed edit for /tmp removal this pass.

Out of OWN (not modified): broader ooda fixtures/scripts that intentionally
use path string `/tmp/...` as cap-sandbox test oracles (e.g. secret_*, corpus
cap fail cases). Those are product rails, not metamorphic sludge.

========================================================================
bash -n (metamorphic scripts)
========================================================================

```
bash -n scripts/metamorphic_path_a_smoke.sh
bash -n scripts/metamorphic_residual_smoke.sh
bash -n scripts/meta_vs_det_residual_smoke.sh
bash -n scripts/bare_metal_residual_smoke.sh
bash -n scripts/residual_path_a_floor_smoke.sh
# all: exit 0
```

ALL_BASHN_OK = true

========================================================================
CLAIMS-MATCH-TREE FIX (this work)
========================================================================

`bootstrap/METAMORPHIC.md` documents:
  - `fixtures/metamorphic_path_a_shuffle.oo`
  - `fixtures/metamorphic_epoch.oo`

Both were **missing** on disk at hygiene start (claims ≠ tree). Restored as
test-only fixtures (no product code change):

| Fixture | Purpose | Lines |
|---------|---------|------:|
| `fixtures/metamorphic_path_a_shuffle.oo` | path-a + named_alpha/beta/gamma (no reorder product) | 19 |
| `fixtures/metamorphic_epoch.oo` | meta_epoch only; **no** `// METAMORPHIC: path-a` | 10 |

========================================================================
OODA ROOT SLUDGE
========================================================================

From **this work**: none.
  - no `tmp_*` created under `ooda/`
  - no split decoy file left dangling
  - audit lives under `openOODA/audit/` only

Pre-existing untracked ooda root cruft (not created here; not deleted here):
  `tmp_boot.log`, `tmp_boot2.log`, `tmp.c`, `tmp_tc_assign_mut.oo.head`,
  `tmp_test.oo` — remain outside meta OWN (untracked; not staged).

========================================================================
IMPORT / WIRE SNAPSHOT
========================================================================

| Site | Status |
|------|--------|
| `oodac/main.oo` → `import "c_emit_meta.oo"` | present |
| `oodac/c_emit.oo` → `c_parse_metamorphic_path_a` + `c_meta_layout_emit_decoys` + `c_emit_meta_decoy` | present |
| `runtime/chs_rt_meta.c` → `oo_meta_epoch` | present |
| `scripts/ci_product.sh` wires metamorphic smokes | peer-verified (peer2) |

========================================================================
HONESTY / RESIDUAL
========================================================================

- Path-A only: layout decoys + `meta_epoch`. Full immune / runtime mutation residual.
- Hygiene does **not** claim live tip `emit-c` product-green for decoys (peer2
  residual: tip may soft-skip rebuild).
- Line lock is live measurement; concurrent peers may grow `c_emit_meta.oo` —
  if it crosses 256, next hygiene pass must split to `c_emit_meta_decoy.oo`
  and add `import "c_emit_meta_decoy.oo"` in `main.oo` only.

========================================================================
FILES TOUCHED (Hygiene OWN)
========================================================================

| Path | Action |
|------|--------|
| `ooda/fixtures/metamorphic_path_a_shuffle.oo` | restore (doc claim) |
| `ooda/fixtures/metamorphic_epoch.oo` | restore (doc claim) |
| `openOODA/audit/meta_phalanx_hygiene.oot` | this report |

No edits to `c_emit_meta.oo` / `main.oo` / smokes this pass (already ≤256, clean).

end of meta_phalanx_hygiene.oot
