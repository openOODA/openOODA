# p7_prove_2026-08-12.oot
# Solo prove. No swarm. Artifacts under /tmp/p7_prove/.
## Official script
`ooda/scripts/p7_check_main_smoke.oo` — tiny <8s; `main.oo` timeout 180.
rc=1 (type/parse) counts as ALL OK. Only 124 is FAILED.

## Runs (live ELF `ooda/oodac/oodac`)
| ELF | wall | main.oo | p7 |
|-----|------|---------|-----|
| after skip_balanced + tc_call_ret SEMI | 86s | rc=1 `if` cond found ERR_BINOP | ALL OK |
| + tc_control_cond skip ERR_BINOP | 117s | rc=1 assign `k2` ERR_BINOP | ALL OK |
| + kidx rename | 181s | rc=124 + `undefined ret_val` | FAILED |
| + mut-param bind (KW_MUT) | 181s | rc=124 empty | FAILED |

## Replicas (pass on last good type-fix ELF)
- `if end == start + 1` was ERR_BINOP (fold has no precedence). After skip: OK.
- `if 1 {` still ERR Int (not fail-open).
- `pub fn foo(mut x: String) { if x == "" }` was undefined `x`. After KW_MUT bind: OK.
- trunc rbrace/rparen still rc=1 ERR.

## Also this turn
`bootstrap/oodac_pure_build` and `_fixed2.oo` were **zeroed** mid-session. Restored from git; re-applied emit-c timeout fail-closed.

## Next
Hang in modular check after early FPs gone. First suspect: `llvm_emit_stmt.oo` (`timeout 8` → 124). Do not raise 180.
