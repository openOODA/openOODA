# phalanx_sprint_wave2_2026-08-12.oot
# 12-agent SWARM wave 2. Orthogonal files. No debugger swarm.
# No 119s main.oo check.

## Ships
INT rebuild ELF; DE1 isolate 111:5 + tc_call_ret SEMI; DE2 skip_balanced unclosed;
DOCS F38b pack; HARN unclosed_body; COORD board.

## Lane results

| Lane | Result | Proof |
|------|--------|-------|
| INT | LANDED | `oodac/oodac` 980648 B. `nm` `T check_function`. First build 25.5s (pre-DE2). Second build 24.7s (DE1+DE2). |
| DE1 | LANDED | 111:5 concat = `lex.oo:33` space. Modular class = `return f(...)` vs Bool. `tc_call_ret.oo` SEMI-after-call only. |
| DE2 | LANDED | `parse_drive.oo` depth!=0 → `ERR parse unclosed brace`. |
| RT | NO WRITE | 21/21 `gcc -c chs_rt*.c` ALL_CC_OK. slist_get still ERR+exit(1). |
| HARN | LANDED | `harness_unclosed_body.oo` (`-> Int { return 1;`). |
| DOCS | LANDED | STDLIB F38b closed. f20 hash row = unused leaf. |
| COORD | LANDED | SPRINT wave-2 assignment; then close 2/5 after prove. |
| PV1 | PASS | check_caps 251L; all list_get gated; check_fn_trunc live. |
| PV2 | FAIL then PASS | First ELF (pre-DE2): rbrace OK, rparen ERR. After second rebuild: 3/3 rc=1+ERR. |
| SEC | MIXED | 7 asked rows PASS. f20 fs_walk row stale. Org-tree second sha256 remains. |
| MEM | PASS | Expanded 111:5 = lex.oo:33. 1MiB bound still in check_drive. |
| HYG | PASS | 0 .md. check_caps 251. SPRINT ≤256. 0 sludge. |

## Prove (second ELF)

```
harness_trunc_rparen.oo  exit=1  ERR check truncated fn header
harness_trunc_rbrace.oo  exit=1  ERR parse unclosed brace
harness_unclosed_body.oo exit=1  ERR parse unclosed brace
```

## Closed this wave
Task 2 (trunc/unclosed). Task 5 (emit-c + nm check_function).

## Not done
- Task 1: p7 timeout 180 not run; 111:5 class fixed in source, not dual-repro on main.oo.
- Task 3/6: f20 fs_walk / process_exit count; org std second sha256.

## Next
p7 ALL OK under 180s (or accept residual). DOCS inventory rows. Do not mint _ops.
