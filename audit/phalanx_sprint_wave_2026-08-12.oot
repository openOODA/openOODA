# phalanx_sprint_wave_2026-08-12.oot
# 12-agent SWARM. Orthogonal files. No debugger swarm.
# Ships: SPRINT honesty, check_caps header OOB, sha256 collision, emit-c timeout, PM honesty.
## Adapter
ROOT: /home/jeryd/Projects/openOODA
SWARM plan: 12-lane phalanx (DE1 DE2 RT PV1 PV2 SEC MEM INT HARN HYG DOCS COORD)

## Lane results
| Lane | Result | Proof |
|------|--------|-------|
| DE1 | LANDED | `check_caps.oo` 251L. `check_fn_trunc` + `pos<n` on PUB/FN/name/LPAREN. `saw_rparen`. Body `i < body_end && i < n`. |
| DE2 | LANDED | One `pub fn sha256` in `ooda/std/core/crypto.oo`. `hash/sha256.oo` unused `process_exit(1)` leaf. |
| RT | NO WRITE | `oo_slist_get` / `oo_ilist_get` already ERR + exit(1). No abort on get-OOB. |
| INT | LANDED | `bootstrap/oodac_pure_build` + `_fixed2.oo`: timeout 124 → ERR_EMIT_TIMEOUT. `emit-c \|\| true` = 0. No rebuild. |
| HARN | LANDED | `bootstrap/corpus/check/fail/harness_trunc_{rbrace,rparen}.oo` |
| DOCS | LANDED | PM Focus F26 open/slow. Open table Args/Walk residual not shipped. |
| COORD | LANDED | Task 1 = SLOW + type 111:5. Tip `dd4671c` / `19ec94e`. COORD sole SPRINT writer. |
| PV1 | STALE RACE | Read check_caps before DE1 write. Re-read after: header guards present. |
| PV2 | FAIL seed | ELF `oodac/oodac` ABSENT. Seed: missing `}` → OK (unexpected); missing `)` → 134 + slist_get OOB. |
| SEC | MIXED | 31 product std `.oo`. Args absent. `fs_walk` exists in `os/fs.oo`. Five `_ops` phantoms absent. Org std still has a second sha256. |
| MEM | PASS source | 1MiB bound + hang guard + one `tokenize_lines` per check. 119s = later `typecheck_*` scans. Did not run main.oo. |
| HYG | PASS | 0 `.md` files. 0 root sludge deletes. |

## Not done (honest)
- F26 type error at 111:5 (still Task 1).
- Task 2 close: needs rebuilt ELF + dual-repro on HARN fixtures.
- Task 5 `nm check_function` after pure build.
- Task 3/6 remainder: gate-shape inventory, org-tree sha256, RFC-002 file missing.
- No moonshots.

## Next rotation
1. INT rebuild `oodac/oodac` from seed.
2. PV2 re-prove trunc fixtures on that ELF.
3. DE1 isolate `ERR type at 111:5` (typecheck, not hang).
