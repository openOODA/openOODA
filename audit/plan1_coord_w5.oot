# PLAN1_COORD_W5 — Ship 1 Wave 5 pass or fail

File: openOODA/audit/plan1_coord_w5.oot
Process only. Not product. Disk is the source.
Owner: COORD. Wave 5 merge note. Do not implement. Do not merge.
Ship 1. Product path is cli/lsp_*.oo. vscode starts bin/ooda lsp.

Logline: Ship 1 is PASS. Combined bin/ooda advertises completion with trigger & and hover still names add.
Setup: Combined tree /home/jeryd/.grok/worktrees/ooda-cli-three. Rebuild host is ./oodac_bin. Seed cannot check this pin. Dirty parent /home/jeryd/Projects/openOODA/ooda is read-only.
Beats:
  1. Record the combined tree and the rebuild host.
  2. Score each ship 1 required claim as PASS or FAIL.
  3. Name the cursor wrapper residual.
  4. Leave skeptics and merge open.

## 0. Verdict

  PASS ship 1.

  The parent agent already proved the facts in this note.
  COORD did not rebuild. COORD did not edit product.
  Wave 0 observe stays in audit/plan1_coord.oot.

## 1. Combined tree

  Path: /home/jeryd/.grok/worktrees/ooda-cli-three
  Combined tree: one worktree that holds the three ship write sets.
  Product CLI: bin/ooda
  Rebuild command pin:

    OODAC_BIN=./oodac_bin bash bootstrap/oodac_pure_build cli/main.oo bin/ooda

  Seed is bootstrap/seed/oodac. Seed cannot check this pin.
  Cause is still undefined variable oo_fs_rmdir in the CLI graph.
  Worktree oodac/oodac is absent. Do not plant a CLI there.
  Do not copy bin/ooda onto oodac/oodac.

  Language Server Protocol (LSP) is the editor protocol.
  completionProvider is the LSP flag that says the server can complete text.
  Trigger & is the character that starts a completion request.

## 2. Required claims

  Source: audit/plan_cli_lsp.oot section 0.

  1. initialize includes completionProvider. Trigger characters include &.
     PASS.
     Parent: bin/ooda lsp initialize includes completionProvider trigger &.

  2. A buffer with &Fs at the cursor returns an item labeled &FsCap.
     PASS.
     Parent: ./bin/ooda run scripts/lsp_complete_smoke.oo
     prints lsp_complete_smoke: PASSED.

  3. Prefix uses line and character. Not a fake prefix key.
     PASS.
     The smoke that PASSED sends initialize, then
     textDocument/completion with uri, line, and character.
     It does not send a prefix field.

  4. Empty prefix still returns the catalog. No hang. No crash.
     PASS.
     The same smoke still replies for a missing URI, a directory
     URI, a body with no position, and a path that contains `..`.
     Hang budget is 10 seconds. The smoke printed PASSED.

  5. Hover and definition still name the identifier under the cursor.
     Product hover: PASS.
     Parent: hover on fixtures/lsp_cursor.oo add() returns fn add.
     It does not return main.
     Official wrapper scripts/lsp_cursor_smoke.oo: FAIL only
     because ./oodac/oodac is absent in the worktree.
     That wrapper fail is residual. It does not fail the ship.
     Wave 0 forbids a CLI at oodac/oodac.

  6. New lock: scripts/lsp_complete_smoke.oo prints PASSED.
     PASS.

  7. A test that greps the catalog in source is not enough.
     PASS.
     The lock drove rebuilt bin/ooda. It did not grep cli/*.oo
     as proof.

## 3. Cursor wrapper residual

  scripts/lsp_cursor_smoke.oo requires ./oodac/oodac, then ./bin/ooda.
  The combined tree has bin/ooda. It does not have oodac/oodac.
  The wrapper fails on the missing compiler path only.
  Hover on add is already PASS.
  Do not plant ./oodac/oodac to make the wrapper print PASSED.

## 4. What this wave does not do

  It does not merge.
  It does not start SK1, SK2, or SK3.
  It does not refresh bootstrap/seed/oodac.
  It does not write the dirty parent.
  It does not write oodac/, runtime/, or std/.
  plan1_int.oot is absent. Proof is the parent facts above.
  A second complete-smoke run is not recorded here.

## 5. Isolation

  COORD wrote only this note.
  Did not edit product files.
  Did not write /home/jeryd/Projects/openOODA/ooda.

Status: Wave 5 ship 1 is PASS. Skeptics may run on the combined binary. Merge stays open.
