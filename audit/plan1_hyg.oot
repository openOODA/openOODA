# PLAN1 HYG — Ship 1 Wave 2 lock

File: openOODA/audit/plan1_hyg.oot
Process only. Not product. Disk is the source.
Ship 1. Product path is cli/lsp_*.oo. vscode starts bin/ooda lsp.
Owner: HYG. Wave 2 lock. Write set only. Did not edit dirty parent.

Logline: The ship 1 write set stays under 256 lines, carries Academy headers, and adds no leftover .py, .md, .sh, .tmp.c, or .bin.
Setup: Worktree /home/jeryd/.grok/worktrees/ooda-lsp-complete. Branch wip/lsp-complete. Dirty parent /home/jeryd/Projects/openOODA/ooda is read-only.
Beats:
  1. Count every write-set .oo file.
  2. Require Title, Logline, Setup, and Beats in the first 25 lines.
  3. Refuse new .py .md .sh and leftover .tmp.c / .bin in cli/, scripts/, fixtures/.
  4. Confirm cli/lsp_drive.oo was not edited.

## 1. Write set

  DE1  cli/lsp_reply.oo                 modified. 224 lines.
  DE2  cli/lsp_complete.oo              new. 179 lines.
  HARN scripts/lsp_complete_smoke.oo    new. 111 lines.
  HARN fixtures/lsp_complete_probe.oo   new. 12 lines.

  DE1  cli/lsp_drive.oo                 not edited. 214 lines.
       git status porcelain empty. git diff empty.
       Default lock: dispatch stays in lsp_reply.oo.

  HYG did not edit the write set. No header or 256 repair was required.
  HYG did not edit hover or definition. HYG did not edit dirty parent.

## 2. Line counts (wc -l, comments count)

  | file | lines | 256 | header | verdict |
  | --- | --- | --- | --- | --- |
  | cli/lsp_complete.oo | 179 | pass | Title Logline Setup Beats | PASS |
  | cli/lsp_reply.oo | 224 | pass | Title Logline Setup Beats | PASS |
  | scripts/lsp_complete_smoke.oo | 111 | pass | Title Logline Setup Beats | PASS |
  | fixtures/lsp_complete_probe.oo | 12 | pass | Title Logline Setup Beats | PASS |
  | cli/lsp_drive.oo | 214 | pass | not in this write | SKIP (unedited) |

  Cap is 256. Closest file is cli/lsp_reply.oo at 32 lines under the cap.
  Each write-set file ends with a newline. last line == wc -l.

## 3. Academy headers

  dinner_lint product check: `// # ` and `// Logline:` in the first 25 lines.
  FORMAT 1.1 also wants `// Setup:` and `// Beats:` (3 to 6 beats).

  cli/lsp_complete.oo
    // # Native LSP completion list
    Logline present tense. Period. 4 beats.

  cli/lsp_reply.oo
    // # Native LSP method replies
    Logline present tense. Period. 3 beats.

  scripts/lsp_complete_smoke.oo
    // # Drive ooda lsp completion
    Logline present tense. Period. 5 beats.

  fixtures/lsp_complete_probe.oo
    // # LSP completion probe
    Logline present tense. Period. 3 beats.
    dinner_lint skips *fixtures* for headers. Ship 1 still requires the header.

  No beat names a line number. No missing field.

## 4. File law

  git status on cli/ scripts/ fixtures/:
    M  cli/lsp_reply.oo
    ?? cli/lsp_complete.oo
    ?? fixtures/lsp_complete_probe.oo
    ?? scripts/lsp_complete_smoke.oo

  No new .py .md .sh in the write set.
  Pre-existing tracked fixtures/pm_launch_tamper.py is not this ship.

  find cli scripts fixtures for *.tmp.c and *.bin: none.
  find worktree for *.tmp.c: none.

## 5. dinner_lint

  Path exists: /home/jeryd/Projects/openOODA/openOODA/scripts/dinner_lint.oo
  The script does not edit boards. It walks openOODA ooda docs from a monorepo root.
  Worktree has no bin/ooda (Wave 0 seed rebuild failed). Cannot exec the .oo linter.
  Full-repo dinner_lint would leave this worktree and score the dirty parent. Out of scope.

  Same rules applied to the write set only. scoped_fails=0.

## 6. Result

  PASS

  cli/lsp_complete.oo              179  PASS
  cli/lsp_reply.oo                 224  PASS
  scripts/lsp_complete_smoke.oo    111  PASS
  fixtures/lsp_complete_probe.oo    12  PASS
  cli/lsp_drive.oo                 214  not edited
