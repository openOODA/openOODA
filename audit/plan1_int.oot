# PLAN1_INT — Combined CLI completion and hover

File: openOODA/audit/plan1_int.oot
Wave 4 INT. Ship 1. Own this note. Do not edit product.
Do not edit the dirty parent. Do not edit oodac/, runtime/, or std/.

Logline: Combined bin/ooda lsp advertises completion, returns &FsCap, and hover still names add.
Setup: Combined tree `/home/jeryd/.grok/worktrees/ooda-cli-three`. Host `./oodac_bin`. Seed fails `oo_fs_rmdir`. `bin/ooda` is present. Dirty parent `/home/jeryd/Projects/openOODA/ooda` is read-only.
Beats:
  1. Record the oodac_bin rebuild. Do not rebuild again.
  2. Record the seed fail on oo_fs_rmdir.
  3. Confirm lsp_complete_smoke prints PASSED.
  4. Confirm hover via ooda lsp returns fn add.

## 0. Verdict

PASS

Rebuild used `OODAC_BIN=./oodac_bin`. Exit 0. `OK_PURE_MULTI`.
Seed still fails `oo_fs_rmdir`.
`lsp_complete_smoke: PASSED`.
Hover via `ooda lsp` returns `fn add`.
This role did not rebuild. `bin/ooda` is present.

## 1. Combined CLI

  Path: `/home/jeryd/.grok/worktrees/ooda-cli-three`
  Branch: `wip/cli-three`
  HEAD: `55ae5580326d6c2cf641e285d7d139ab4c613067`
  Subject: feat(cli): lsp completion, real launch --exec, and timed bench
  Pin start: `64ef8bd`

  Product CLI: `/home/jeryd/.grok/worktrees/ooda-cli-three/bin/ooda`
  sha256: `1c51901daf7451d7163789026e4794947b1d2883227778d0e032fe730d8ca63f`
  Version: `ooda 0.201.0-beta (pure .oo CLI)`
  strings: `pure .oo product CLI` — yes
  strings: `oodac: usage:` — no
  strings: `ooda bench: ok` — no

  Worktree `oodac/oodac` is absent.
  Host `./oodac_bin` sha256 stayed
  `783d8c6d2d8cc90f4b03479f3e49dbcea3eff46a2c7f65ad4eff273215471cda`.
  Host is a compiler (`oodac: usage:`). It is not a product CLI.

## 2. Rebuild

  Combined rebuild is already on disk. This role did not rebuild.

    cd /home/jeryd/.grok/worktrees/ooda-cli-three
    OODAC_BIN=./oodac_bin \
      bash bootstrap/oodac_pure_build cli/main.oo bin/ooda

  Not seed. Not dirty parent `oodac/oodac`.
  `PURE_SKIP_CHECK` unset.
  Log: `/tmp/grok-goal-e3250e659225/implementer/cli_rebuild.log`

    pure_build: input_fp=15f41f10bfdf022e4b806e38d24c0803d9a534b687576354ec0dd92a37c0f458
    pure_build: check host=./oodac_bin nmods=52 timeout=164s
    pure_build: unique_fns=281 from_modules=52
    OK_PURE_MULTI
    EXIT:0

  `bin/ooda` is present. Do not rebuild again.

## 3. Seed fail

  Official seed command still fails this pin.

    OODAC_BIN=./bootstrap/seed/oodac \
      bash bootstrap/oodac_pure_build cli/main.oo bin/ooda

  Combined `cli/cmd_pm_tree.oo` still calls `oo_fs_rmdir`.
  Wave 0 seed rebuilds on this pin printed:

    ERR	type	Type error at 154:58: undefined variable 'oo_fs_rmdir'

  Exit 1. No seed-built `bin/ooda`.
  Seed sha256:
    `077d0f883edb083bc2598c0f1f87469d7a091c53e3db1ffd826f2cf5b046fef2`
  Seed `strings` has no `oo_fs_rmdir`.
  Host for this pin is `./oodac_bin` (Wave 0 option 1 / `plan_merge`).
  Do not refresh seed. Do not plant `oodac/oodac`.

## 4. Complete smoke

  cwd is the combined tree.

    timeout 30 ./bin/ooda run ./scripts/lsp_complete_smoke.oo

  Pass 1: `lsp_complete_smoke: PASSED`. Exit 0.
    Log: `lsp_complete_ship1_raw.txt` at 2026-08-17T19:20:40-04:00
  Pass 2: `lsp_complete_smoke: PASSED`. Exit 0.
    Log: `lsp_complete_run2.log`

  The smoke drives `./bin/ooda lsp`. It does not grep `cli/*.oo`.
  initialize includes `completionProvider` with trigger `&`.
  Completion at line 11 character 16 (`F` of `&Fs`) returns `&FsCap`.
  `"isIncomplete":false`.
  Missing URI, directory URI, no position, and `..` still reply.
  Hang budget is 10s.

  Live request:

    uri `file://fixtures/lsp_complete_probe.oo`
    position line 11 character 16
    no `"prefix"` key

## 5. Hover via ooda lsp

  `scripts/lsp_cursor_smoke.oo` wants `./oodac/oodac`. That file is absent.
  Do not plant it. Drive hover on product `./bin/ooda lsp`.

  Request (`hover_req.json`):
    initialize
    textDocument/hover on `fixtures/lsp_cursor.oo`
    position line 12 character 12 (`add` in `add(1, 2)`)
    textDocument/definition at the same position

  Result (`hover.log`, `lsp_hover.log`):
    initialize advertises `completionProvider` trigger `&`
    hover contents: `fn add`
    definition range start line 16 character 4
  Exit 0.
  No `fn main`. Not first-fn-only.

## 6. Isolation

  This role wrote only this note.
  It did not edit `cli/`, `scripts/`, `fixtures/`, `oodac/`,
  `runtime/`, or `std/`.
  It did not write `/home/jeryd/Projects/openOODA/ooda`.
  It did not copy `bin/ooda` onto `oodac/oodac`.
  It did not rebuild.
  No new `.py`, `.md`, or `.sh`.

## 7. What this role does not do

  It does not merge.
  It does not refresh seed.
  It does not plant `./oodac/oodac` so the cursor wrapper can pass.
  It does not edit hover or definition.

Status: Wave 4 INT is PASS. Combined `bin/ooda` complete smoke
PASSED. Hover via `ooda lsp` returns `fn add`.

C emit plus gcc remains the product path.
