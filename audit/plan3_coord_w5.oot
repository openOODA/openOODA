# PLAN3_COORD — Wave 5 pass/fail ship 3

File: openOODA/audit/plan3_coord_w5.oot
Wave 5 COORD. Process only. Not product. Disk is the source.
Owner: COORD. Do not implement. Do not merge. Do not edit product.

Logline: Ship 3 is PASS. Rebuilt ./bin/ooda prints check milliseconds on hello and never prints ooda bench: ok.
Setup: Worktree `/home/jeryd/.grok/worktrees/ooda-bench`. Drive `./bin/ooda`. Dirty parent `/home/jeryd/Projects/openOODA/ooda` is read-only.
Beats:
  1. Score each done-when claim from `audit/plan_cli_bench.oot`.
  2. Record the live double-run and the refuse cases.
  3. Roll up the twelve role notes.
  4. Name residuals and isolation.

## 0. Verdict

PASS ship 3.

All seven done-when claims are PASS.
All twelve role notes exist. INT is green.
`ooda bench: ok` is absent from `cli/cmd_more.oo`, `cli/main.oo`, and `bin/ooda`.
This role does not merge.

## 1. Live commands

Worktree: `/home/jeryd/.grok/worktrees/ooda-bench`
Binary: `./bin/ooda` (product CLI, INT rebuild).

  `./bin/ooda bench fixtures/hello.oo`
  Pass 1: `ooda bench: check fixtures/hello.oo 27ms` exit 0
  Pass 2: `ooda bench: check fixtures/hello.oo 6ms` exit 0
  Times differ. Both parse as integer milliseconds >= 0.

  `./bin/ooda bench`
  Prints `usage: ooda bench <file.oo> [--run]`. Exit 2.

  `./bin/ooda bench missing_no_such.oo`
  Prints `ERR<TAB>bench<TAB>unreadable file: missing_no_such.oo`.
  Exit 2.

  Never `ooda bench: ok` on those three drives.

INT also double-ran hello (10ms then 5ms) and
`scripts/bench_smoke.oo` (`bench_smoke: PASSED` twice).
Check child may print `OK`. That is the compiler. It is not
`ooda bench: ok`.

## 2. Claims

From `audit/plan_cli_bench.oot` section 0.

| # | claim | status | proof |
| ---: | --- | --- | --- |
| 1 | real `oodac check` via `find_oodac` | PASS | `cli/cmd_more.oo` `run_ooda_bench`; hello exit 0 |
| 2 | wall ms from `now_ms` under TimeCap | PASS | `check fixtures/hello.oo 27ms` then `6ms` |
| 3 | missing file: ERR, exit 2, no ok | PASS | `ERR<TAB>bench<TAB>unreadable file:` exit 2 |
| 4 | check fail: non-zero, no run line | PASS | PV1 + smoke `--run` on `no_cap_now_ms.oo` |
| 5 | `--run` only after check pass | PASS | DE1 gate; PV1 clone; smoke lock |
| 6 | no file: not ok, not exit 0 | PASS | usage line, exit 2 |
| 7 | help names the real command | PASS | `ooda bench <file.oo> [--run]` in `cli/main.oo` |

Signature on disk:

    pub fn main(fs: &FsCap, sys: &SysCap, time: &TimeCap, args: List[String])

Help stayed in `cli/main.oo`. No `cli/cli_help.oo` split.

## 3. Role rollup

| role | file | wave | status |
| --- | --- | ---: | --- |
| COORD | plan3_coord.oot | 0 | observe done |
| RT | plan3_rt.oot | 0 | TimeCap on main YES |
| DOCS | plan3_docs.oot | 0 | honest print named |
| DE1 | plan3_de1.oot | 1 | stub gone |
| DE2 | plan3_de2.oot | 1 | TimeCap + help |
| HARN | plan3_harn.oot | 1 | bench_smoke.oo |
| HYG | plan3_hyg.oot | 2 | PASS |
| PV1 | plan3_pv1.oot | 3 | PASS |
| PV2 | plan3_pv2.oot | 3 | PASS |
| SEC | plan3_sec.oot | 3 | PASS |
| MEM | plan3_mem.oot | 3 | PASS |
| INT | plan3_int.oot | 4 | PASS |

Count: 12 role notes. 0 MISSING. Wave 5 this file: PASS.

## 4. Isolation

  Write set stayed:

    cli/cmd_more.oo
    cli/main.oo
    scripts/bench_smoke.oo

  This role wrote only this note.
  Did not write product files.
  Did not write `/home/jeryd/Projects/openOODA/ooda`.
  Did not write `oodac/`, `runtime/`, `std/`, or the public site.
  Did not rebuild `bin/ooda`. Did not merge.
  No new `.py`, `.md`, or `.sh`.

  Line counts (limit 256): cmd_more.oo 142, main.oo 255,
  bench_smoke.oo 62.

## 5. Residual (not FAIL)

  Seed CLI rebuild still fails on `oo_fs_rmdir`. Official host
  for this pin is worktree `./oodac_bin`. See INT.
  Parent `cmd_more.oo` still prints the stub. That tree is dirty
  and read-only.
  Host `sh -c` still starts `oodac`. The script is not the user file.
  Optional `--run` hello is not a Wave 5 live drive. The gate is
  on disk. Smoke owns check-fail `--run`.
  SK1 / SK3 are a later parent gate. They are not this note.
  SHIPPED.oot waits on skeptic pass.

## 6. What this role does not do

  It does not implement bench.
  It does not edit product.
  It does not merge the three ships.
  It does not copy `bin/ooda` onto `oodac/oodac`.

Status: Wave 5 COORD is PASS. Ship 3 is PASS.

C emit plus gcc remains the product path.
