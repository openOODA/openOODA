# PLAN3_HYG — ship 3 Academy and file-law lock

File: openOODA/audit/plan3_hyg.oot
Wave 2 LOCK. Check only cli/cmd_more.oo, cli/main.oo, scripts/bench_smoke.oo.

Logline: The three ship-3 files stay at or under 256 lines, carry 4-element Academy headers, and cmd_more.oo does not contain `ooda bench: ok`.
Setup: Worktree `/home/jeryd/.grok/worktrees/ooda-bench`. Dirty parent is read-only. C emit plus gcc remains the product path.
Beats:
  1. Count lines. Split print_help only if main.oo is 256 or more.
  2. Enforce Academy headers. Confirm no bench ok stub in cmd_more.oo.
  3. Confirm no new .py, .md, or .sh. Do not change other dispatch.

## 0. Verdict

PASS

`ooda bench: ok` is absent from `cli/cmd_more.oo`.
`cli/main.oo` is 255 lines. No `cli/cli_help.oo` split.
No new `.py`, `.md`, or `.sh`. Dirty parent not edited.

## 1. Line counts

  Counted by newline count on disk. Limit 256.

  | file | lines | 256 |
  | --- | --- | --- |
  | cli/cmd_more.oo | 142 | pass |
  | cli/main.oo | 255 | pass |
  | scripts/bench_smoke.oo | 54 | pass |

  Slack on main.oo: 1 line.
  print_help stays in cli/main.oo.
  cli/cli_help.oo was not created.

## 2. Academy headers

  FORMAT 1.1 four elements on each ship file: title, Logline, Setup, Beats.
  FORMAT 1.2: Logline is one present-tense sentence that ends with a period.
  Beats are 3 to 6 items. No line numbers in beats.

  Wave 2 lock rewrote two loglines in place (no added lines):

    cmd_more.oo Logline: leftover floors print ok, pack writes
    an air-gap tree, and bench times oodac check.
    That line does not say bench prints a success line.

    main.oo Logline: route argv under FsCap, SysCap, and TimeCap
    to native product commands.
    Setup now names bench with the other native commands.

  bench_smoke.oo header was already one sentence and five beats.
  dinner_lint Title + Logline markers are present on all three.

## 3. No `ooda bench: ok` in cmd_more.oo

  `grep` of `cli/cmd_more.oo` for `ooda bench: ok`: no matches.

  `run_ooda_bench` is not a stub. It prints only:

    usage: ooda bench <file.oo> [--run]
    ERR<TAB>bench<TAB>unreadable file: FILE
    ooda bench: check failed FILE
    ooda bench: check FILE Nms
    ooda bench: run FILE Nms

  Other floors still print their own ok lines (em, migrate,
  install, context-pack, emit-health, errdigest). Those stubs
  stay. They are not this ship. pack still forwards to PM.

  scripts/bench_smoke.oo names `ooda bench: ok` only as the
  string that must not appear on stdout or stderr. That is
  a negative assertion. It is not a product print.

## 4. Dispatch and file law

  Other command dispatch in cli/main.oo is unchanged.
  HYG did not edit the bench call, the TimeCap parameter, or
  any non-bench branch. Help still names
  `ooda bench <file.oo> [--run]`.

  Worktree porcelain is only:

    M cli/cmd_more.oo
    M cli/main.oo
    ?? scripts/bench_smoke.oo

  No new `.py`. No new `.md`. No new `.sh`.
  No leftover `*.tmp.c` from this ship.
  Did not write `/home/jeryd/Projects/openOODA/ooda`.
  Did not write oodac/, runtime/, std/, or the public site.

## 5. What this role does not do

  It does not rebuild bin/ooda.
  It does not run bench_smoke (INT owns the binary).
  It does not change host_run.oo or find_compiler.oo.
  It does not keep the old stub to satisfy qa/e2e_spine.oo.

Status: Wave 2 HYG lock is PASS. Ship 3 stays open for PV and INT.

C emit plus gcc remains the product path.
