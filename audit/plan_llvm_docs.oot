# LLVM-DOCS — Board sentences after INT

File: openOODA/audit/plan_llvm_docs.oot
DOCS lane. Draft only. Do not edit SHIPPED.oot or ROADMAP.oot.
COORD applies after INT. C emit + gcc remains the product path.

## 0. Gate

Apply the sentences below if and only if INT proves all five:

  1. Cold LLVM `check fixtures/hello.oo` works with no planted `g_` listing.
  2. LLVM `check oodac/main.oo` is `OK` or honest `ERR`.
  3. LLVM `tokens` / `outline` / `emit-c` / `emit-llvm` / `check` floor works.
  4. The C path still works.
  5. No text sells Production LLVM as the everyday path.

Locks INT must name PASSED:

  `scripts/open68_cold_hello_smoke.oo`
  `scripts/open68_main_check_smoke.oo`
  `scripts/open68_cmds_floor_smoke.oo`

Disk now (HOLD):

  Side ELF `.ooda-cache/ooda-tmp/oodac_llvm` is absent.
  INT PASSED lines are absent.
  Do not copy these sentences onto the boards yet.

## 1. Current leftover lines (ROADMAP section 5 compiler leftovers)

Quote from ROADMAP.oot `## 5. Open` compiler leftovers:

  Compiler leftovers (other agents; text kept):

  [ ] LLVM-linked oodac as the gcc replacement: every command, C path retired. Not this week unless the owner names it.
  [ ] LLVM-linked ELF `check fixtures/hello.oo` with no planted `g_` listing. Today a stale listing can print `ERRcheckempty`. Warm stamp-walk of a valid listing is product.
  [ ] LLVM-linked ELF `check oodac/main.oo`.
  [ ] Raw gcc binary `cmp` without strip + repro flags.

## 2. SHIPPED.oot — compiler floor LLVM (add)

Insert after the stamp-walk bullet. Keep the gcc-replacement residual bullet that follows.

S1 add (IFF cold hello, no planted `g_`):

  - Side LLVM-linked oodac checks `fixtures/hello.oo` after a wipe of `.ooda-cache/check/g_*`. No gcc helper plants a listing. Check prints `OK` twice. A mutated hello does not print OK. Lock: `scripts/open68_cold_hello_smoke.oo`. C emit + gcc remains the product path.

S2 add (IFF main is `OK` — pick this when INT prints `check main OK`):

  - Side LLVM-linked oodac `check oodac/main.oo` prints `OK`. It does not need a planted `g_` listing. Lock: `scripts/open68_main_check_smoke.oo`.

S2b add (IFF main is honest `ERR` — pick this when INT prints `check main fail-closed ERR`):

  - Side LLVM-linked oodac `check oodac/main.oo` prints honest `ERR`. It does not fake `OK`. Lock: `scripts/open68_main_check_smoke.oo`.

S3 add (IFF five-command floor):

  - Side LLVM-linked oodac runs `tokens`, `outline`, `emit-c`, `emit-llvm`, and `check` on `fixtures/hello.oo` twice with the same marks. Lock: `scripts/open68_cmds_floor_smoke.oo`. This is not gcc-parity. Every command and a retired C path stay residual.

Keep (do not delete; condition 4 and 5):

  - C emit through `chs_rt`, then `gcc`. That is the product path.
  - Clang link of full oodac as a gcc replacement is residual. Lock: `scripts/open68_llvm_lock.oo`.

Do not add a sentence that names LLVM as the everyday path.

## 3. ROADMAP leftover rows (check off IFF)

Stay `[ ]` (never check off this cycle):

  [ ] LLVM-linked oodac as the gcc replacement: every command, C path retired. Not this week unless the owner names it.
  [ ] Raw gcc binary `cmp` without strip + repro flags.

R-hello — replace the open hello row with this `[x]` IFF condition 1:

  [x] LLVM-linked ELF `check fixtures/hello.oo` with no planted `g_` listing. Cold check prints `OK` twice. A mutated hello does not print OK. Lock: `scripts/open68_cold_hello_smoke.oo`. Warm stamp-walk of a valid listing stays product.

R-main — replace the open main row with this `[x]` IFF condition 2 and INT prints `check main OK`:

  [x] LLVM-linked ELF `check oodac/main.oo` prints `OK`. Lock: `scripts/open68_main_check_smoke.oo`.

R-main-err — replace the open main row with this `[x]` IFF condition 2 and INT prints `check main fail-closed ERR`:

  [x] LLVM-linked ELF `check oodac/main.oo` prints honest `ERR`. It does not fake `OK`. Lock: `scripts/open68_main_check_smoke.oo`.

Honesty row stays `[ ]` (condition 5):

  [ ] Public text must not sell residual or moonshot as product: load-time layout morph, HITL (the loop is `ooda fix`), device `gpu_launch_shader`, Production LLVM as the everyday path, dummy WASM bodies, WAT emit, or a browser WASM host.

## 4. Do not write

Do not check off gcc replacement.
Do not retire the C path.
Do not say Production LLVM is the everyday path.
Do not apply any sentence before INT names PASSED.
