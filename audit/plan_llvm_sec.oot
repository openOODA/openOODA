# LLVM SEC — shim and cache audit

File: openOODA/audit/plan_llvm_sec.oot
Role: SEC. C emit + gcc remains the product path. No cap re-grant.

## Findings

  1. LOW: `llvm_link_shim.oo` has no `oo_cap_grant_sys`. Stamp smoke greps that.
  2. LOW: `llvm_link_shim.oo` has no `ret i32 1` success stub.
  3. LOW: `llvm_emit_fn.oo` has no `llvm_emit_listing_fresh_stub`.
  4. MED: `check_cache_put_body` writes through `sh -c` and refuses a body that contains `'`. Empty body is not written. Not a CRIT on the LLVM path.
  5. LOW: `read_file` / `write_file` shims pass the caller cap to `oo_*`. No forged grant.

No CRIT. No emit edit this role.
