# PLAN SK3 — Isolation and collision

File: openOODA/audit/plan_sk3.oot
Skeptic. Read-only product. Disk is the source. Did not edit worktrees.
Did not edit dirty parent `/home/jeryd/Projects/openOODA/ooda`.
Did not edit `openOODA.github.io`.

Question: did we touch the locked trees or the dirty main worktree?

Logline: Combined ooda-cli-three keeps oodac, runtime, std, the public site, and the dirty parent free of this write set.
Setup: Dest is `/home/jeryd/.grok/worktrees/ooda-cli-three` on `wip/cli-three` at `55ae558`. Pin is `64ef8bd`. Parent `/home/jeryd/Projects/openOODA/ooda` stays dirty and read-only.
Beats:
  1. Refuse diffs under oodac/, runtime/, and std/ in the combined tree.
  2. Refuse site edits and refuse cli/lsp_complete.oo on the dirty parent.
  3. Refuse new .py, .md, or .sh and any write-set file over 256 lines.

## 0. Verdict

PASS on the SK3 isolation question.

| claim | status | command |
| --- | --- | --- |
| 1 | PASS | combined `oodac/` `runtime/` `std/` match pin `ooda-lsp-complete`; lack parent extras |
| 2 | PASS | site `refs/heads/main` == `origin/main` == `a5623b75` |
| 3 | PASS | `test ! -e` parent `cli/lsp_complete.oo`; parent grep `lsp_complete` empty |
| 4 | PASS | combined write set has no new `.py` `.md` `.sh` |
| 5 | PASS | eleven write-set files, max `cli/main.oo` 255 |

COORD may merge a ship only if SK1, SK2, and SK3 are PASS on that ship.
This file is SK3 only. It does not score live `bin/ooda`.

## 1. Combined locked trees

Worktree: `/home/jeryd/.grok/worktrees/ooda-cli-three`
Branch: `wip/cli-three` from `64ef8bd` then one commit `55ae558`
(`feat(cli): lsp completion, real launch --exec, and timed bench`).

`oodac/` `runtime/` `std/` file names match pin worktree
`/home/jeryd/.grok/worktrees/ooda-lsp-complete` (still at `64ef8bd`).
Combined has no `oodac/oodac`. Combined `std/` is 403 files.
`std.pin` is `ooda-std 0.201.0-beta` on dest and pin.

Dirty parent is not the pin:

  parent `oodac/` adds `c_emit_deref.oo` `c_emit_for.oo` `c_emit_list_ty.oo`
  `c_emit_olist.oo` `c_emit_result_ty.oo` `c_emit_skip_peek.oo`
  `c_emit_sum.oo` `c_emit_walk.oo` plus `oodac/oodac` and `main.oo.bin`
  parent `runtime/` adds `chs_rt_flist.c` `chs_rt_list_set.c`
  `chs_rt_llist.c` `chs_rt_llist_f.c` `chs_rt_llist_s.c`
  parent `std/` is 404 files

Combined lacks those extras. The write did not copy parent compiler
or runtime work into dest. Snapshot
`/tmp/grok-goal-e3250e659225/implementer/isolation.diff.txt`
lists combined diffs only in `cli/cmd_more.oo` `cli/cmd_pm_launch.oo`
`cli/lsp_reply.oo` `cli/main.oo` plus the new write-set paths.
Line `no oodac/runtime/std in combined` is `none`.

`lsp/` stays eight files. No `vscode/` in the worktree.

## 2. Public site

Repo: `/home/jeryd/Projects/openOODA/openOODA.github.io`

  `refs/heads/main`     a5623b75d1f2eb70442e5ef9f8f2f68b0c2a39d1
  `refs/remotes/origin/main`  same
  last log: `docs: research library pass and replace index helpers with .oo`

No `.py` `.md` `.sh` under the site tree.
isolation.diff site porcelain is `## main...origin/main` only.
This role did not write index, guide, or research.

## 3. Dirty parent not a dest

Parent `/home/jeryd/Projects/openOODA/ooda` still holds the old lies:

  `cli/cmd_more.oo` prints `ooda bench: ok`
  `cli/cmd_pm_launch.oo` still `sys_exec(..., "test -e " + sq(path) + "; exit 0")`

Absent on parent:

  `cli/lsp_complete.oo`
  `fixtures/lsp_complete_probe.oo`
  `fixtures/pm_launch_exec/`
  `scripts/lsp_complete_smoke.oo`
  `scripts/pm_launch_exec_smoke.oo`
  `scripts/bench_smoke.oo`

Grep of parent `cli/` for `lsp_complete` is empty.
Grep of parent tree for `lsp_complete` is empty.
Parent `oodac/oodac` exists because that tree is already dirty.
This write set did not add it and did not add `cli/lsp_complete.oo`.

## 4. Write set file law

Copy list is eleven dest paths (plan_merge.oot). Combined counts:

  | file | lines | 256 |
  | --- | ---: | --- |
  | cli/lsp_complete.oo | 179 | pass |
  | cli/lsp_reply.oo | 224 | pass |
  | cli/cmd_pm_launch.oo | 125 | pass |
  | cli/cmd_more.oo | 142 | pass |
  | cli/main.oo | 255 | pass |
  | scripts/lsp_complete_smoke.oo | 164 | pass |
  | scripts/pm_launch_exec_smoke.oo | 62 | pass |
  | scripts/bench_smoke.oo | 62 | pass |
  | fixtures/lsp_complete_probe.oo | 12 | pass |
  | fixtures/pm_launch_exec/runme.oo | 14 | pass |
  | fixtures/pm_launch_exec/manifest | 8 | n/a |

No write-set `.py` `.md` `.sh`.
The only `.py` in dest is pre-existing `fixtures/pm_launch_tamper.py`.

## 5. Residual (not FAIL)

  Combined `scripts/` still holds emit leftovers
  (`*.oo.bin` and `*.oo.bin.tmp.c`) from INT smokes.
  Those are not `.oo` / `.oot` and are not the write set.
  Parent `oodac/oodac` hash moved under another actor.
  This role did not write that binary.
  `vendor/` is on parent only. Dest has no `vendor/`.

## 6. Isolation

  Wrote only this note.
  Did not edit `cli/`, `oodac/`, `runtime/`, `std/`, or the public site.
  Did not write `/home/jeryd/Projects/openOODA/ooda`.
  No new `.py`, `.md`, or `.sh`.

Status: SK3 PASS. Locked trees and dirty parent stay outside the write set.

C emit plus gcc remains the product path.
