# PLAN_W3_COORD — Wave 3 attack rollup

File: openOODA/audit/plan_w3_coord.oot
Process only. Not product. Disk is the source.
Owner: COORD. Wave 3 rollup. Do not implement. Do not merge.

Logline: Wave 3 scores twelve attack notes as PASS. No role is FAIL. No role is MISSING.
Setup: Notes are `audit/plan{1,2,3}_{pv1,pv2,sec,mem}.oot`. Dirty parent `/home/jeryd/Projects/openOODA/ooda` is read-only.
Beats:
  1. Read each Wave 3 note and take its verdict.
  2. Fill ship x role as PASS, FAIL, or MISSING.
  3. Name residuals that do not fail this wave.
  4. Leave Wave 4 INT open.

## 0. Board

Wave 3 roles are PV1, PV2, SEC, and MEM.
Ships are 1 (lsp complete), 2 (launch --exec), and 3 (bench).
`plan2_pv2.oot` is complete on disk. It is not MISSING.

| ship | PV1 | PV2 | SEC | MEM |
| ---: | --- | --- | --- | --- |
| 1 | PASS | PASS | PASS | PASS |
| 2 | PASS | PASS | PASS | PASS |
| 3 | PASS | PASS | PASS | PASS |

Count: 12 PASS. 0 FAIL. 0 MISSING.
No Wave 3 role asked COORD to stop a ship.
No Wave 3 role edited `oodac/`, `runtime/`, `std/`, or the dirty parent.
Wave 4 INT is not scored here.

## 1. Ship 1 — lsp complete

Worktree: `/home/jeryd/.grok/worktrees/ooda-lsp-complete`
Proof: `cli/lsp_complete.oo` plus the completion branch in `cli/lsp_reply.oo`.

  PV1  missing URI, directory URI, no position, and `..` load to empty src. No crash. No hang.
  PV2  hover and definition still name `add` at the cursor. Not first-fn-only. Cursor probe PASSED.
  SEC  `..` is refused. No `process_exec` on the new path. No file body in the reply.
  MEM  no catalog while. 64-item cap is on `lsp_comp_take`. Empty and huge prefix stop.

Residual, not FAIL:
  Hover loader has no `..` gate. `file:///etc/passwd` has no `..` and may read.
  `bin/ooda` is absent. Official smokes were not run. INT owns that.

## 2. Ship 2 — launch --exec

Worktree: `/home/jeryd/.grok/worktrees/ooda-launch-exec`
Proof: `cli/cmd_pm_launch.oo`. Gate file `cli/pm_launch.oo` was not edited.

  PV1  missing name, `..`, yank, and sha drift die before `run_oodac_run`.
  PV2  not `.oo`, no add, ELF, empty, directory, and shell leaf do not `sh` CAS bytes.
  SEC  `test -e` is gone. Caps stay a subset. Writedir is not widened. No CAS body print.
  MEM  one `run_oodac_run` that `process_exit`s. `waitpid` reaps. Smoke hang budget is 10s.

Residual, not FAIL:
  Empty lock sha still matches (W3-P6 / R-SHA0). A `.oo` under a blank pin is later PV.
  Helper `sh` for `pwd` / `ls` remains. It is not the payload child.
  ELF named `*.oo` reaches `oodac run` and fails closed there.

## 3. Ship 3 — bench

Worktree: `/home/jeryd/.grok/worktrees/ooda-bench`
Proof: `cli/cmd_more.oo` `run_ooda_bench` and `cli/main.oo`.

  PV1  check-fail with `--run` exits 1. No `ooda bench: run` line. Gate is on disk.
  PV2  `bench --run` with no file exits 2. Stub `ooda bench: ok` is gone from source and strings.
  SEC  clock is `now_ms` under TimeCap. User file is one `sq` operand. No `date` timer.
  MEM  two `now_ms` per phase. No `list_new` / `list_push`. Hello hang budget is 30s.

Residual, not FAIL:
  Parent `cmd_more.oo` still prints the stub. That tree is dirty and read-only.
  Host `sh -c` still starts `oodac`. The script is not the user file.

## 4. Shared leftover

Seed CLI rebuild is still exit 1 on `oo_fs_rmdir`.
Official `bin/ooda` is absent in all three worktrees.
INT must rebuild and double-run. COORD does not start Wave 4 here.
C emit plus gcc remains the product path.

## 5. Isolation

COORD wrote only this note.
Did not write product files.
Did not write `/home/jeryd/Projects/openOODA/ooda`.
Did not rebuild `bin/ooda`.
Did not merge.

Status: Wave 3 attack board is PASS. All three ships stay open for INT.
