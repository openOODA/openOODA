# ROADMAP_NS_HYG — File Law And 256-Line Survey

File: openOODA/audit/roadmap_ns_hyg.oot
HYG survey only. Do not rewrite ROADMAP.oot. Do not run check_board.oo.
C emit + gcc remains the product path. Ignore oodac/ and org std/ splits.

## 0. Scope

  This report checks file law (RULES 1.13, START §0, NORTHSTAR 2.1)
  and the 256-line cap on the nine boards (RULES 1.7, FORMAT).
  Public trees: `../openOODA.github.io/`, `../registry/`.
  Leftover public wiki: `../wiki/`. Do not write there (WORKFLOW 0.2).
  Production `[ ]` rows must be user-facing or board-facing file-law
  defects. Residual 5.1 facts stay residual.

## 1. Nine Boards (Line Count)

  Counted by last line on disk. All nine stay <= 256.

  | board | lines | 256 |
  | --- | --- | --- |
  | START.oot | 94 | pass |
  | RULES.oot | 120 | pass |
  | NORTHSTAR.oot | 157 | pass |
  | MOONSHOTS.oot | 68 | pass |
  | ROADMAP.oot | 125 | pass |
  | SHIPPED.oot | 80 | pass |
  | FORMAT.oot | 148 | pass |
  | SECURITY.oot | 153 | pass |
  | WORKFLOW.oot | 247 | pass |

  No board split is required. WORKFLOW.oot is 9 lines under the cap.
  Each board has the FORMAT 2.1 title and `File:` line.

## 2. Public Site (`../openOODA.github.io/`)

  No `.md` file. No `.sh` file. No unexpected Markdown.
  Door `index.html` prints `curl -fsSL https://openooda.org/install | oo`.
  Payload `install` is `.oo`. It is not a shell script.
  Guide pages do not name `qa/e2e/*.sh`.

  Stale words live only in `README.oot`:
    table names `install` / `install.sh`
    example uses `curl … | sh`
  Disk has no `install.sh`. WORKFLOW says never `| sh`.
  A stranger on the door sees `| oo`. Install stays paused (5.1).
  That stale README is a docs fix, not a new 5.2 product row.

## 3. Public Store (`../registry/`)

  `README.oot` is the legal store note.
  `README.md` is leftover Markdown on the live store.
  `README.md.msrc` is the sidecar for that leftover file.
  RULES 1.13 and START §0 forbid `.md` documentation.
  NORTHSTAR 2.1 says docs use `.oot`.
  This is a user-facing file-law hole on `registry.openooda.org`.

  Other store files (`index`, `catalog`, `index.minisig`, package
  `manifest`) are store bytes, not process boards. `index.sig` is
  already named leftover HMAC. Do not open a new row for it.

## 4. Leftover Wiki (`../wiki/`)

  WORKFLOW 0.2: leftover site. Guide is `/guide/`.
  `README.md` is forbidden Markdown on that leftover public tree.
  Pages themselves are `.oot`. No `.sh` and no `.py` there.
  File law still applies in every directory (RULES 1.13).

## 5. Allowed And Already Residual

  `../vscode/server/*.js` is the allowed editor glue (START §0).
  ROADMAP 5.1 already names vscode `ooda-lsp.js`. Do not reopen.

  `../qa/e2e/` holds 166 `*.sh` files. ROADMAP 5.1 and SHIPPED
  already name this residual. `qa_master.oo` is product (22 green).
  The public site does not sell those scripts as product.
  `../qa/README.oot` already calls `e2e/*.sh` residual.
  Do not copy this as a new leftover row.

  No `.py` file is on disk in this workspace.
  No `install.sh` is on the public site tree.

## 6. `../ooda/cli/` Sample (Not A Split Row)

  Sample last-line counts. All sampled files stay <= 256.

  | file | lines |
  | --- | --- |
  | main.oo | 254 |
  | host_run.oo | 207 |
  | cmd_pm_ingest.oo | 193 |
  | cmd_pm_io.oo | 189 |
  | fix_drive.oo | 172 |
  | json_check.oo | 164 |
  | pm_extract.oo | 140 |
  | pm_core.oo | 134 |
  | cmd_pm_launch.oo | 122 |
  | cmd_more.oo | 97 |
  | lsp/lsp_server.oo | 88 |
  | cmd_pm.oo | 54 |

  `main.oo` is 2 lines under the cap. Do not open a CLI split row.
  Do not open oodac/ or std/ split rows.

## 7. Board Claims That Are Not New 5.2 Rows

  SHIPPED and ROADMAP 5.1 name `audit/library_status.oot`.
  This workspace has no `audit/library_status*.oot` file.
  `scripts/open51_hygiene_smoke.oo` requires that door.
  That is library-inventory honesty (5.1 / 5.4), not a file-law
  public-tree row. Do not add a std split row.

  Emit `*.tmp.c` under `scripts/` and `../ooda/cli/` is build
  cruft. It is not a public-tree `.md` / `.sh` defect.

  START still says “section 6 residual tracks.” Section 6 on
  disk is the closed stale-word log. DOCS already named this
  as a board-word fix. Not a HYG 5.2 product row.

## 8. Recommended Open Rows

  Only public leftover `.md` fails file law for a stranger.
  Boards pass 256. Site has no unexpected `.md`. vscode JS
  is allowed. `qa/e2e/*.sh` stays in 5.1. C emit + gcc stays
  product. Do not add 5.3 or 5.4 rows.

| id | row | class |
| --- | --- | --- |
| HYG-1 | Delete leftover `../registry/README.md` and `../registry/README.md.msrc`. Keep `../registry/README.oot`. | production (RULES 1.13, public store) |
| HYG-2 | Delete leftover `../wiki/README.md`. Wiki stays leftover. Guide stays `/guide/`. | production (RULES 1.13, leftover public tree) |

  Parent: these are delete-only hygiene rows. They are not
  oodac work. They are not org-std work. They are not moonshots.

C emit + gcc remains product.
