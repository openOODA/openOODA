# s210_hygiene.oot — 2.10 user-sink wrapper. Hygiene reviewer. COMPLETE. No push. No SPRINT.
# Date: 2026-08-12T20:56:10Z  WT: secret.oo 233 / alias.oo 237 / guard.oo 228 (all ≤256). Hard >600: none.
# DECISION: COMPLETE. mark_user_sinks called from check_secret_println_sinks (c_emit_secret.oo L152).
# is_user_sink used L224. Not a dead/half-land hole — do not revert the call.
# Land: c_secret_is_user_sink + c_secret_mark_user_sinks in c_emit_secret_alias.oo (L160–237).
# One-hop: formal IDENT in a sealed-sink call in the same fn body → __sec_sink__<fn>=1.
# Dual-repro host: /tmp/oodac_f26d (972048 B 10:54; nm T mark_user_sinks; md5=WT secret+alias).
# wrap_fail leak(s){println(s)}; leak(tok) → check+emit-c+llvm rc=1 ERR\tsecret\tleak sink refuses SECRET tok
# wrap_safe (formal unused) + wrap_lit ("tok") → check OK emit-c rc=0. Prefix __sec_sink__dump also refuse.
# Product host oodac/oodac 10:04 963080 B stale: no mark symbol; wrap_fail ALLOW (named host lag).
# Clean-cwd f26d: secret_sink_fail/pass + eprintln_fail hold. No peel. No ELF in oodac/. No push.
# STOP.
