# s211_sticky.oot — 2.11 sticky secret clear (honesty). No push. No SPRINT edit.
# Date: 2026-08-12  Role: Security residual  Host: ooda/oodac/oodac 963080 B 10:04 (__secref__ live)
# Path A sequential: y=secret; y="clean"; println(y) ALLOW check/emit-c/llvm (rebind_fail / sticky_residual).
# secref: // SECRET: api_key then let api_key="public"; println(api_key) REFUSE all three (root never wiped).
# Field: b.v=secret; b.v="clean"; println(b.v) REFUSE all (c_secret_prop_field_assign set-only; root sticks).
# HOLE: y=secret; if false { y="clean" }; println(y) → check ALLOW + llvm ALLOW + emit-c REFUSE (y still secret).
# Same FN: while false / else-arm. Cause: check_secret_println_sinks is flat; clean EQ puts __sec__y=0 forever.
# emit-c already refuse-until-later: {; frame + scope_exit drops the "0"; outer 1 remains (over-refuse if-true).
# No IFC join invented. Full clearer = refuse-until-later. Next OWN: nest-brace>fn-body never-clear (tiny trap).
# Residual: field/index/secref over-stick; check/llvm in-block under-stick. Copy-then-clear source (z) still REFUSE.
# Prove: /tmp/s211_sticky_* if_false_clear / while_false_clear / else_clear. Product fixtures not regressed.
# STOP. No product .oo edit. No push.
