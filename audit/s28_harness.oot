# s28_harness.oot — 2.8 macro-body SECRET second pass path-A harness pointer
# Date: 2026-08-12
# Scope: SPRINT 2.8 smoke rails only (no Domain Expert product land)

## Harness

| Item | Path |
|------|------|
| Smoke | `ooda/scripts/s28_macro_secret_smoke.sh` |
| Binary | `OODAC_BIN` or `ooda/oodac/oodac` |
| Fixtures | ephemeral under `$TMPDIR/s28_macro_secret_$$` (not repo root) |
| Product | `ooda/oodac/check_residual.oo` (`macro_expand` / `ast_macro` → `AST_MACROS`) |
| Residual doc | `ooda/bootstrap/MACRO_SECRET_RESIDUAL.oot` (`MACRO_SECRET_RESIDUAL_ALPHA`) |

## Rails

| Kind | Case |
|------|------|
| Probe | `check` accept of `macro_expand` / `ast_macro` / invented `macro` keyword → macros exist |
| FAIL-closed (unsupported) | `check` residual-refuse `macro_expand` / `ast_macro` (`ERR residual` + `AST_MACROS`) **or** parse/type refuse of invented macro syntax |
| FAIL-closed (unsupported) | `emit-c` must not silent-green residual free-name / invented syntax |
| N/A (unsupported) | SECRET-in-expanded-body **not claimed**; residual doc names free-name refuse |
| FAIL-closed (macros exist) | `// SECRET: tok` then expanded body `println(tok)` must `ERR secret` (`check` + `emit-c`) |
| FAIL-closed (both) | `// SECRET: tok` then `println(tok)` first-pass floor still refuses |

Exit 0 only if rails honest.

## Run

```
cd /home/jeryd/Projects/openOODA/ooda
OODAC_BIN=./oodac/oodac bash scripts/s28_macro_secret_smoke.sh
```

## Residual (documented, not a fail)

- **Macro-body SECRET second pass:** unneeded while `AST_MACROS` residual deny holds (no expansion surface).
- **emit-c residual-deny:** check-path only; emit-c unknown-free-call is not expansion.
- **Full IFC / `#[Secret]` / interp `${}`:** path-A ident scan + residual refuse only; not DESIGN taint.

end of s28_harness.oot
