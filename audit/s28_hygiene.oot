# s28_hygiene.oot — Hygiene Enforcer (2.8 macro-body SECRET residual)
# Date: 2026-08-12T19:13:48Z
# Role: Dependency/Hygiene Enforcer — RULES §1.7 (2.8 phalanx)
# Tree: /home/jeryd/Projects/openOODA  (ooda/ + openOODA/)
# Tip (ooda): 52cc4b5 + WIP (`bootstrap/MACRO_SECRET_PASS.oot` staged;
#              `MACRO_SECRET_RESIDUAL.oot` + `MACRO_BODY_SECRET.oot` ??)
# Tip (openOODA): 81f56fc + WIP issue_2_8_macro_secret_residual.oot ??;
#                 SPRINT 2.8 already **FIXED** path-A documented
# OWN: line-lock 2.8 residual docs + `check_residual.oo` (HEAD, no edit);
#      no ELF / scratch in `oodac/`; consistent `audit/s28_*.oot`; no credentials
# WRITE: this audit + hygiene-only scrub of leftover emit dump
# POLICY: RULES §1.7 / PROBE MAX_LINES = 256 (soft); pack watch 300; hard ban >600
#         No mass product peels; force-split only if DE blew 600
#
# ID collision honesty: DESIGN / PM **1.4** = first-class AST macros
# (moonshot residual). This file is SPRINT backlog **2.8** =
# "Macro-body SECRET second pass (or residual doc)".
# Do not mix the two.
## SUMMARY
| Check | VERDICT | Notes |
|-------|---------|-------|
| Hard ban >600 (touched .oo) | **PASS** | 0 OWN / 0 `ooda/oodac/**/*.oo` over 600 |
| Soft lock ≤256 (2.8 OWN) | **PASS** | residual docs 20–29; `check_residual.oo` **93** |
| Pack pressure ≤300 (OWN) | **PASS** | all OWN << 300 |
| Adjacent secret family soft | **WARN** residual | `c_emit_secret.oo` **264**, `c_emit_secret_prop.oo` **262**, `main.oo` **307**, `check_cap_util.oo` **357** — pre-existing / 2.10/7.x; **not** 2.8 blow |
| DE monofile blow >600 | **none** | 2.8 is **residual-doc only**; no product `.oo` land |
| ELF extra / scratch in `oodac/` | **PASS** | no bak/x/fuzz/toys; only product host |
| Product host ELF | **kept** | gitignored `oodac/oodac` **962960** B — not a DE toy |
| Super emit dump | **PASS** (scrubbed) | deleted `oodac_new.tmp.c` (reappeared mid-wave) |
| Credentials | **PASS** | ZERO live secrets/tokens/keys |
| `/tmp` in product `.oo` / 2.8 docs | **PASS** | ZERO |
| Dedicated `s28_*` smoke | **N/A** | residual-doc close; rails are existing residual refuse |
| `bash -n` related residual smokes | **PASS** | ast_macros 42L / residual_path_a 106L / secret_taint 81L |
| `audit/s28_*.oot` names | **WARN** residual | this file is first `s28_*`; rogue `issue_2_8_macro_secret_residual.oot` left |
| Triple residual docs | **WARN** residual | 3 overlapping bootstrap docs (leave — they *are* the 2.8 land) |
| Mass deletes of product | **none** | only untracked/ignored debris |
| Push | **no** | charter |

OVERALL: **PASS**
  (hard monofile green; no extra ELF; emit-dump scratch scrubbed; no
   credentials; soft residual only on pre-existing adjacent / name dupe)

## SCOPE + METHOD
2.8 ACTIVE (SPRINT backlog): MED "Macro-body SECRET second pass
(or residual doc)". Path-A close is **documented unneeded**: AST macros
are default-deny at check (`check_residual.oo` `macro_expand` /
`ast_macro` → `AST_MACROS`), so a second-pass SECRET walker over
expanded bodies has no surface.

OWN units this pass:

| Path | Role in 2.8 | Git |
|------|-------------|-----|
| `bootstrap/MACRO_SECRET_PASS.oot` | residual honesty (staged) | `A` (20L) |
| `bootstrap/MACRO_SECRET_RESIDUAL.oot` | residual honesty (twin) | `??` (23L) |
| `bootstrap/MACRO_BODY_SECRET.oot` | residual honesty (twin) | `??` (29L) |
| `oodac/check_residual.oo` | `AST_MACROS` refuse rail | **clean** (HEAD 93L) |
| `audit/issue_2_8_macro_secret_residual.oot` | security residual (rogue name) | `??` |
| `audit/s28_*.oot` | phalanx evidence | this file |

Adjacent observe (line-lock only if DE had blown 600):

| Path | Lines | Note |
|------|------:|------|
| `oodac/c_emit_secret.oo` | 264 | token-scan SECRET (2.10 / 7.10) |
| `oodac/c_emit_secret_guard.oo` | 228 | emit SECRETSINK (2.10 / 6.1 / 7.1) |
| `oodac/c_emit_secret_prop.oo` | 262 | T1/T2 prop (pre-existing soft) |
| `oodac/c_emit_secret_alias.oo` | 157 | alias resolve |
| `oodac/c_emit_args.oo` | 240 | 7.2 last-arg |
| `oodac/main.oo` | 307 | pre-existing soft/pack residual |
| `bootstrap/AST_MACROS.oot` | 39 | moonshot residual (1.4) |
| `bootstrap/SECRET_TAINT.oot` | 129 | dirty is **6.1** env_set wording, not 2.8 |
| `fixtures/residual_macro_fail.oo` | 1 | `macro_expand("m")` refuse rail |
| `fixtures/ast_macros_marker.oo` | 6 | marker comment only |

No 2.8 product walker. Hygiene does **not** run residual smokes
(integration/harness own exit-0); bar here is `bash -n` + tree locks.

Commands:

  `wc -l` + hard/pack/soft gates on OWN + full `oodac/*.oo` >600 inventory
  repo-wide `.oo` >600 inventory (honesty: 12 `std/src` catalog stubs)
  `git add -n oodac/` + `bootstrap/` before+after scrub
  `bash -n` related residual smokes (no dedicated s28 harness)
  `grep` credential + `/tmp` on OWN + residual docs
  delete extra ELF + emit-dump scratch; **keep** product `oodac/oodac`
  no push; no product logic rewrite; no SPRINT flip

Thresholds:

| Threshold | Meaning |
|-----------|---------|
| **256** | RULES §1.7 / PROBE MAX_LINES — soft lock |
| **300** | Pack monofile pressure (flag for DE peel) |
| **600** | Path-A hard ban — force-split only if blown |

---
LINE LOCK — 2.8-touched / adjacent monofiles (live)
---

Measured under monorepo tip ooda=`52cc4b5`+WIP (2026-08-12T19:13Z).

| File | Lines | ≤256 | ≤300 | ≤600 | HEAD dirty? | Split? |
|------|------:|:----:|:----:|:----:|:-----------:|:------:|
| `bootstrap/MACRO_SECRET_PASS.oot` | **20** | PASS | PASS | PASS | staged NEW | n/a |
| `bootstrap/MACRO_SECRET_RESIDUAL.oot` | **23** | PASS | PASS | PASS | NEW | n/a |
| `bootstrap/MACRO_BODY_SECRET.oot` | **29** | PASS | PASS | PASS | NEW | n/a |
| `oodac/check_residual.oo` | **93** | PASS | PASS | PASS | no | NO |
| `oodac/c_emit_secret.oo` | **264** | OVER | PASS | PASS | 2.10 / 7.10 | residual |
| `oodac/c_emit_secret_guard.oo` | **228** | PASS | PASS | PASS | 2.10 / 6.1 | NO |
| `oodac/c_emit_secret_prop.oo` | **262** | OVER | PASS | PASS | no | residual |
| `oodac/c_emit_secret_alias.oo` | **157** | PASS | PASS | PASS | no | NO |
| `oodac/c_emit_args.oo` | **240** | PASS | PASS | PASS | 7.2 comments | NO |
| `oodac/main.oo` | **307** | OVER | OVER | PASS | no | residual |
| `audit/issue_2_8_macro_secret_residual.oot` | **21** | PASS | PASS | PASS | NEW | n/a |
| Aggregate | Value |
|-----------|------:|
| Touched `.oo` over **600** | **0** |
| `oodac/**/*.oo` over **600** | **0** |
| 2.8 OWN over **256** | **0** |
| Adjacent over **256** | **4** (secret 264 / prop 262 / main 307 / check_cap 357) |
| Out-of-OWN `std/src/**/*.oo` over **600** | **12** (catalog stubs — not 2.8) |

md5 (stable re-read at measure):

| File | md5 |
|------|-----|
| `MACRO_SECRET_PASS.oot` | `a7b136269803cb9851b2e8c5aaafe3be` |
| `MACRO_SECRET_RESIDUAL.oot` | `14b2e893faac7f36e7e453c2a4f78866` |
| `MACRO_BODY_SECRET.oot` | `840dc94e5e5a2aa7e96b56a32e703f7b` |
| `check_residual.oo` | `82360ff1ca1da9d7dacee851cd85426e` |
| `issue_2_8_macro_secret_residual.oot` | `93452b560d95a43d8351bf70da82d079` |
| product `oodac/oodac` | `a44ee35eb344837009697e2c9d4e4662` (962960 B, BuildID `a93cfd09…`) |

Delta honesty:

- **No 2.8 product land this wave.** `check_residual.oo` is HEAD-clean.
  Live refuse still maps `macro_expand` / `ast_macro` → `AST_MACROS`
  (L28–29) and `check_residual_free_calls` walks IDENT+LPAREN.
- Residual docs are **new** (20 / 23 / 29). Three concurrent agents
  wrote the same "unneeded on path-A" close. Leave all three — they
  *are* the SPRINT 2.8 deliverable (residual doc), not LLM toys.
- Concurrent dirt on `c_emit_secret*.oo` / `c_emit_args.oo` /
  `check_drive.oo` / `tc_names.oo` / `SECRET_TAINT.oot` is **2.10 /
  6.1 / 7.x**, not 2.8. Hygiene does not own those diffs.
- **Hard ban holds** for entire `ooda/oodac/*.oo` product set
  (count_over_600 = 0).
- Out-of-OWN: **12** `std/src/ooda/**` files still over 600 (crypto
  stubs + slam/credit/gates). **Not** 2.8. Path-A oodac floor is the lock.
  PM R3 "0 files > 600" is the **oodac** lock, not std catalog.

Live refuse (hygiene re-read, `residual_feature_of`):

```
if name == "macro_expand" { return "AST_MACROS"; }
if name == "ast_macro" { return "AST_MACROS"; }
```

Static: no macro-body SECRET walker added. Residual docs all say
second pass is **unneeded until AST_MACROS is implemented**. Matches
`AST_MACROS.oot` + `RESIDUAL_PATH_A.oot` + fixture
`residual_macro_fail.oo`.


*(Continued in `openOODA/audit/s28_hygiene_part2.oot`)*

end of s28_hygiene.oot (part 1)
