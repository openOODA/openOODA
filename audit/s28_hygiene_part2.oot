# s28_hygiene_part2.oot — (Part 2 of s28_hygiene.oot)
# Continuation of `openOODA/audit/s28_hygiene.oot`.

## SPLIT GATE
Rule: force-split only if OWN unit >600 (path-A hard ban).

| Unit | Over hard? | Action |
|------|:----------:|--------|
| residual docs 20 / 23 / 29 | no | **none** |
| `check_residual.oo` 93 | no | **none** |
| adjacent secret 264 / prop 262 / main 307 / check_cap 357 | no hard | residual — not 2.8 OWN |

Suggested DE (when authorized, not this hygiene pass):

1. Collapse the three bootstrap residual twins into one canonical
   `MACRO_SECRET_RESIDUAL.oot` (Docs/Coordinator — not hygiene delete).
2. Optional peel of `check_cap_util.oo` 357 / `main.oo` 307 if a later
   CAP / CLI pass owns them.
3. Keep fixtures under `$TMPDIR` via existing residual smokes — never
   `oodac/a.oo`.
4. When AST_MACROS leaves refuse, reopen 2.8 as a real walker (SPRINT
   already names this).

## BINARY + SCRATCH SCRUB
Charter: **no ELF / scratch in `oodac/`**. Extra copies + lab toys deleted.
Product host kept (gitignored; harness `OODAC_BIN` default).

### Extra ELF (this pass — already clean)
| Artifact | Size | Pre | Post |
|----------|-----:|-----|------|
| `oodac/oodac.x` / bak / fuzz | — | absent (s71/s72/s61 scrub) | **absent** |
| `oodac/oodac` | 962960 B ELF | product host | **kept** (gitignored) |
| `bin/ooda` | 111624 B ELF | product CLI | **kept** |
| `bootstrap/seed/oodac` | 878392 B ELF | seed pin | **kept** |
| super `oodac/oodac` | 962888 B ELF | **tracked** in super-repo | **kept** (do not `git rm`; residual) |

Tree-wide ELF inventory (skip `.git`): **exactly these 4**. No extra
ELF toys. `file(1)`: all x86-64 LSB dynamically linked.

`git add -n oodac/` after scrub: **product `.oo` only**
(`ast_emit_outline`, `c_emit_args`, `c_emit_secret`,
`c_emit_secret_guard`, `check_drive`, `tc_names`) — no ELF, no scratch
toys. Those `.oo` diffs are concurrent 2.10/7.x, not 2.8.

### Scratch toys (scrubbed this pass)
Super-repo emit dump (gitignored `/*.c`; reappeared mid-wave):

- `oodac_new.tmp.c` — generated C preamble dump; **deleted**

Product tree (`ooda/oodac/`): **no** `a.oo` / `b.oo` / `*.orig` /
`*.rej` / `test_slice.oo` / `*.tmp.c` this pass (s72 seal held).

Not deleted (out of 2.8 oodac charter): historical super-repo
`tmp_*.oo`, `scratch/*.oo` sources, tracked corpus `oodac/test_*.oo`.
The three `bootstrap/MACRO_SECRET_*.oot` files are **2.8 residual** —
leave.

`.gitignore`: no new lines required. `/oodac/*.orig` `*.rej`
`test_slice.oo` already sealed in 7.2. Product `/oodac/oodac` already
ignored. Super `/*.c` already covers `oodac_new.tmp.c`.

---
bash -n + SECRETS + /tmp
---

No dedicated `scripts/s28_*_smoke.oo` (2.8 is residual-doc). Related
rails syntax-checked:

```
OK bash -n scripts/ast_macros_residual_smoke.oo        (42 lines)
OK bash -n scripts/residual_path_a_floor_smoke.oo      (106 lines)
OK bash -n scripts/secret_taint_residual_smoke.oo      (81 lines)
```

Honesty: `ast_macros_residual_smoke.oo` still points at
`bootstrap/AST_MACROS.md` (file is now `.oot`). **WARN residual** on
the AST_MACROS pack rail — pre-existing, **not** 2.8. Path-A refuse
rail that actually matters is `residual_path_a_floor_smoke.oo` +
`fixtures/residual_macro_fail.oo`. Hygiene does **not** rewrite that
smoke.

Credential-class scan (`password=`, `BEGIN *PRIVATE`, AWS/GitHub/Slack
tokens, Bearer) on:

  - `bootstrap/MACRO_SECRET_PASS.oot`
  - `bootstrap/MACRO_SECRET_RESIDUAL.oot`
  - `bootstrap/MACRO_BODY_SECRET.oot`
  - `oodac/check_residual.oo`
  - `audit/issue_2_8_macro_secret_residual.oot`
  - this file

  → **ZERO live credentials.**

`/tmp` in 2.8 OWN docs + `check_residual.oo`: **none**.

VERDICT credentials: **PASS**

---
audit/s28_*.oot NAME CONSISTENCY
---

Pattern: `s28_<role>.oot` under `openOODA/audit/` (same family as
`s61_hygiene.oot` / `s72_hygiene.oot`).

| File | Role | Status |
|------|------|--------|
| `s28_hygiene.oot` | this file | **written** |
| domain / harness / ideas / integration / peer / memory / runtime / security / rollup | optional later: `s28_<role>.oot` | not present this pass |

Rogue name on disk (left; hygiene does not rewrite peer audits):

| File | Problem |
|------|---------|
| `issue_2_8_macro_secret_residual.oot` | Security residual used `issue_2_8_*` instead of `s28_security.oot` / `s28_residual.oot` |

No other rogue names (`s2.8_*.oot`, `28_*.oot`, `macro_secret_hygiene.oot`)
on disk.

Line pressure on 2.8 artifacts (informational; ban is on touched `.oo`):

| File | Lines |
|------|------:|
| `MACRO_SECRET_PASS.oot` | 20 |
| `MACRO_SECRET_RESIDUAL.oot` | 23 |
| `MACRO_BODY_SECRET.oot` | 29 |
| `issue_2_8_macro_secret_residual.oot` | 21 |
| `s28_hygiene.oot` | this |

SPRINT 2.8 row already flipped **FIXED** path-A documented
(`MACRO_SECRET_RESIDUAL.oot`) by Docs/Coordinator. Hygiene does
**not** rewrite SPRINT.

SECRET_TAINT table does **not** mention 2.8 / macro-body second pass.
Dirty line is 6.1 env_set wording. Not a hygiene edit.

## ACTIONS / NON-ACTIONS THIS PASS
| Action | Result |
|--------|--------|
| Measure OWN monofile locks | **done** — hard 0 over 600 |
| Force-split DE >600 | **not needed** |
| Flag adjacent soft residual | secret 264 / prop 262 / main 307 / check_cap 357 |
| Flag std/src catalog >600 | **12** files — out of OWN |
| Scrub extra ELF | **none present** |
| Scrub emit-dump scratch | **done** (`oodac_new.tmp.c`) |
| Keep product `oodac/oodac` | **yes** |
| `bash -n` related residual smokes | **PASS** |
| Product SECRET / residual logic edits | **none** (refuse already HEAD) |
| Delete triple residual docs | **no** (2.8 land; Docs may collapse) |
| Rewrite rogue `issue_2_8_*` name | **no** (peer file) |
| Flip SPRINT 2.8 | **no** (already FIXED) |
| Push | **no** |
| Audit write | **this file** |

## RETURN — COMPACT
```
SCOPE   2.8 macro-body SECRET residual  ooda=52cc4b5+WIP  openOODA=81f56fc+WIP
OWN     MACRO_SECRET_PASS.oot=20 staged; RESIDUAL=23 NEW; BODY=29 NEW
        check_residual.oo=93 HEAD-clean (macro_expand/ast_macro → AST_MACROS)
>600    0 touched / 0 oodac product   PASS hard ban
        12 std/src catalog stubs (not 2.8)
>300    0 OWN
>256    0 OWN; adjacent secret 264 / prop 262 / main 307 / check_cap 357 WARN
BINARY  no extra ELF; product oodac KEPT (gitignored); super tracked residual
        tree ELF count = 4 (oodac host + super copy + seed + bin/ooda)
SCRUB   oodac_new.tmp.c DELETED
CREDS   PASS  no live secrets
/tmp    PASS product + 2.8 docs
BASHN   ast_macros / residual_path_a / secret_taint OK
        no dedicated s28 smoke (residual-doc N/A)
NAMES   s28_hygiene written; rogue issue_2_8_macro_secret_residual.oot left
        triple bootstrap residual twins left (Docs may collapse)
PUSH    no
OVERALL PASS
```

end of s28_hygiene.oot

end of s28_hygiene_part2.oot
