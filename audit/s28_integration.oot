# s28_integration.oot — Compiler Integration Tester (SPRINT 2.8)
# Date: 2026-08-12T19:16:30Z
# Worker: Grok Build (Compiler Integration Tester — 2.8)
# Cwd: /home/jeryd/Projects/openOODA/ooda
# Scope: live tip oodac secret rails; SPRINT 2.8 macro-body SECRET second pass
#        (or residual doc) re-prove
# Policy: no push; no seed_pure_multi; run listed smokes (90s each); run any s28_*
## Verdict (honest)
| Layer | Result | Exit | Notes |
|-------|--------|------|-------|
| **`secret_t1_return_smoke.oo`** | **PASS** | **0** | return / alias / if-taint refuse+allow + CI wire |
| **`secret_t2_depth_smoke.oo`** | **PASS** | **0** | field/method/args/dlopen/sys_exec argv/comment-skip + CI wire |
| **`s61_env_set_secretsink_smoke.oo`** (6.1 regression) | **FAIL** | **1** | emit-c refuse+no-FP **GREEN**; `check` type-first (`undefined variable 'env_set'`) — no `ERR secret` |
| **`s71_guard_scan_smoke.oo`** (7.1 regression) | **PASS** | **0** | println IDENT refuse + write_file payload refuse + string-lit no-FP |
| **`s72_sys_exec_last_arg_smoke.oo`** (7.2 regression) | **PASS** | **0** | last-arg IDENT refuse + quoted commas/SECRET-spelling no-FP |
| **`s710_parse_secret_env_smoke.oo`** (7.10 regression) | **PASS** | **0** | println IDENT + two-name + empty-name fail-closed |
| **`s28_macro_secret_smoke.oo`** (Harness extra; landed mid-pass) | **PASS** | **0** | residual-refuse + invented-syntax fail-closed + first-pass SECRET + N/A residual doc |
| **2.8 extra: `macro_expand`/`ast_macro` check refuse** | **PASS** | **1** | `ERR residual` `AST_MACROS` (path-A default-deny) |
| **2.8 extra: emit-c of those names** | **PASS/residual** | **1** | `ERR c_emit unknown free call` — **no** expansion, **no** `ERR residual` on emit |
| **2.8 extra: first-pass SECRET still live** | **PASS** | **1** | `ERR secret println sink refuses SECRET tok` (check + emit-c) |
| **Push** | **NOT DONE** | — | human-gated |

**2.8 path-A claim:** Macro-body SECRET **second pass is unneeded** on Path A because AST macros are **not expanded**. SPRINT 2.8 is **residual-doc** (`or residual doc`), not a new walker. Residual packs are on disk (`MACRO_SECRET_RESIDUAL.oot` cited by SPRINT; plus `MACRO_SECRET_PASS.oot` / `MACRO_BODY_SECRET.oot`). Live `oodac check` default-denies `macro_expand` / `ast_macro` (`ERR residual` / `AST_MACROS`). Live `emit-c` does **not** expand those names — it dies `unknown free call`. First-pass token SECRET (`check_secret_println_sinks`) still refuses visible `println(tok)` under `// SECRET: tok`. There is **no** `c_secret_macro` / second-pass function in `c_emit*`. Dedicated harness `s28_macro_secret_smoke.oo` **GREEN** (exit 0) — residual N/A rail honest.

**Assigned script battery:** **RED** (1 of 7 listed+landed). RED is **6.1 dual-path `check`**, not a 2.8 expander miss. T1/T2 + 7.1/7.2/7.10 + s28 remain **GREEN**. First `find` this pass had **no** `s28_*`; harness landed mid-pass and was re-run.

**Overall 2.8 integration status: GREEN** (residual-doc + `s28_macro_secret_smoke` + live AST_MACROS refuse + first-pass SECRET) / **RED** (`s61_*` `check` vs this host ELF) / **not a second-pass implementation**

Do **not** greenwash: no macro-body SECRET walker landed. `emit-c` does **not** run `check_residual_free_calls` (check-only). Dirty `check_drive.oo` moves SECRET **before** typecheck (6.1 dual-path intent) but the **07:49 host ELF was not rebuilt**, so live `check` still type-firsts `env_set`. Full IFC / `#[Secret]` / `${}` not claimed. `ci_product.oo` wires T1/T2 + `ast_macros_residual_smoke.oo` — **not** s61/s71/s72/s710/s28.

---

## TIP SHA / binary
```
ooda     git rev-parse --short HEAD → 52cc4b5
ooda     git log -1 --oneline       → 52cc4b5 fix(r2): multi-import typecheck path-A + digit-0 import names
openOODA git rev-parse --short HEAD → 81f56fc
parent   git rev-parse --short HEAD → eaa555d
date -u                             → 2026-08-12T19:16:30Z
```

| Item | Value |
|------|--------|
| Product tip | `ooda/oodac/oodac` **962960** bytes @ 2026-08-12 07:49 local |
| md5 | `a44ee35eb344837009697e2c9d4e4662` |
| BuildID | `a93cfd09506ba87497379f66b6c7047dee02573d` |
| Side / older | `oodac/oodac` 962888 (07:43) — **not** used |
| Seed | `bootstrap/seed/oodac` present — pure multi **not** re-run this pass |
| Host used | `OODAC_BIN=/home/jeryd/Projects/openOODA/ooda/oodac/oodac` |
| Host `strings` | `macro_expand`, `ast_macro`, `AST_MACROS`, `env_set`, `setenv` present |
| `nm` | **no** `oo_env_set` / `macro_expand` / `ast_macro` text symbols |
| Working tree (ooda) | dirty: `c_emit_secret*.oo` + `c_emit_args.oo` + `check_drive.oo` + staged `bootstrap/MACRO_SECRET_PASS.oot`; untracked `MACRO_BODY_SECRET.oot`, `MACRO_SECRET_RESIDUAL.oot`, `scripts/s28_macro_secret_smoke.oo`, `s61_*`, `s71_*`, `s72_*`, `s710_*` |

Host ELF is the **same** 07:49 binary used by s61/s71/s72/s710 integration. Dirty hunks this wave are **not** a 2.8 second-pass rewrite: `c_emit_secret.oo` is 7.10 `str_slice` + 2.10 `__sec_sink__`; `check_drive.oo` is 6.1 secret-before-type (source only).

---

## 0) DE / Harness landing observed
| Path | Role |
|------|------|
| `ooda/bootstrap/MACRO_SECRET_RESIDUAL.oot` (23) | SPRINT-cited 2.8 residual; marker `MACRO_SECRET_RESIDUAL_ALPHA`; **untracked** |
| `ooda/bootstrap/MACRO_SECRET_PASS.oot` (20) | sibling residual; marker `MACRO_SECRET_PASS_RESIDUAL`; **staged** |
| `ooda/bootstrap/MACRO_BODY_SECRET.oot` (29) | sibling residual; marker `MACRO_BODY_SECRET_RESIDUAL_ALPHA`; **untracked** |
| `openOODA/audit/issue_2_8_macro_secret_residual.oot` (21) | Security Auditor 2.8 residual (same claim) |
| `openOODA/audit/s28_harness.oot` | harness pointer (landed mid-pass) |
| `ooda/scripts/s28_macro_secret_smoke.oo` (305) | Harness extra (untracked; landed mid-pass); ephemeral TMPDIR fixtures |
| `ooda/bootstrap/AST_MACROS.oot` | M153 path-A: check default-deny of `macro_expand` / `ast_macro` |
| `ooda/bootstrap/RESIDUAL_PATH_A.oot` L20 | table row AST_MACROS |
| `ooda/oodac/check_residual.oo` L28–29 | `macro_expand` / `ast_macro` → `"AST_MACROS"` |
| `ooda/oodac/check_drive.oo` L109 | `check_residual_free_calls` (check path only) |
| `ooda/oodac/c_emit.oo` L77–86 | emit SECRET first-pass; **no** residual-free-call walk |
| `ooda/oodac/c_emit_secret.oo` | first-pass `c_parse_secret_env` + `check_secret_println_sinks` — **no** macro expand |
| `ooda/fixtures/residual_macro_fail.oo` | `pub fn main() { let r = macro_expand("m"); }` |
| `ooda/scripts/ast_macros_residual_smoke.oo` | **doc** smoke; looks for `bootstrap/AST_MACROS.md` (**absent**; tree has `.oot`) |
| `ooda/scripts/secret_{t1,t2}_*.oo` | listed T1/T2 integration rails |
| `ooda/scripts/s61_env_set_secretsink_smoke.oo` | 6.1 regression (untracked; now **dual-path** emit-c **and** check) |
| `ooda/scripts/s71_guard_scan_smoke.oo` | 7.1 regression |
| `ooda/scripts/s72_sys_exec_last_arg_smoke.oo` | 7.2 regression |
| `ooda/scripts/s710_parse_secret_env_smoke.oo` | 7.10 regression |

**Only** `scripts/s28_*` is `s28_macro_secret_smoke.oo`. Mid-pass also landed `audit/s28_{harness,hygiene,ideas,memory}.oot`. Integration did **not** invent the smoke.

Source (tip, live) — AST_MACROS refuse is name-only at check:

```
if name == "macro_expand" { return "AST_MACROS"; }
if name == "ast_macro" { return "AST_MACROS"; }
```

`c_emit.oo` / `c_emit_secret*.oo` have **zero** `macro_expand` / `ast_macro` / `second pass` hits. Emit of those free calls is the generic unknown-call residual (`ERR c_emit unknown free call needs annotation`).

Dirty `check_drive.oo` vs `52cc4b5`: SECRET walk **moved before** typecheck (6.1 comment). **Not in this host ELF.** Live `check` of `env_set(...)` is still type-first.

---


*(Continued in `openOODA/audit/s28_integration_part2.oot`)*

end of s28_integration.oot (part 1)
