# s28_peer1.oot
# Peer Verifier 1 (adversarial QA) — SPRINT 2.8 path-A close
# Date: 2026-08-12
# Role: Peer Verifier 1 (QA) — red-team expand-then-print SECRET
#        / macros fail-closed / T1–T2 / s61 / s71 / s72 / s710
# Tree: /home/jeryd/Projects/openOODA/ooda
# OWN: this audit + live probes only (no product feature implementation)
# Host: ooda/oodac/oodac
#   size=962960 B  mtime=2026-08-12 07:49:59 -0700
#   md5=a44ee35eb344837009697e2c9d4e4662
#   nm: `T residual_feature_of` @ 0x411c60
#   nm: `T residual_deny` @ 0x412270
#   nm: `T check_residual_free_calls` @ 0x4123f0
#   nm: `T c_secret_emit_guard` @ 0x461080
#   nm: `T c_secret_scan_call` @ 0x45e020
#   strings: `macro_expand` + `ast_macro` + `AST_MACROS` +
#            `Residual product refuse` + `unknown free call needs annotation`
# Tip (ooda): 52cc4b5 fix(r2): multi-import typecheck path-A + digit-0 import names
# Source pin: oodac/check_residual.oo mtime 2026-08-12 04:55 (IN host)
#             bootstrap/MACRO_SECRET_RESIDUAL.oot mtime 2026-08-12 11:50
#             bootstrap/MACRO_BODY_SECRET.oot     mtime 2026-08-12 11:50
#             bootstrap/MACRO_SECRET_PASS.oot     mtime 2026-08-12 11:50 (staged)
#             oodac/check_drive.oo     WT newer (secret moved before typecheck)
#             oodac/c_emit_secret*.oo  WT newer than host (6.1/7.10; not 2.8)
# Host ELF is not a post-11:50 rebuild. 2.8 is residual-doc + refuse rails
# already in the live ELF. Do not claim a new walker land.

========================================================================
VERDICT
========================================================================

## **ACCEPT path-A close of 2.8** (macro-body SECRET second pass residual-doc)

SPRINT **2.8** (`Macro-body SECRET second pass (or residual doc)`) is an
honest path-A **residual-doc** close on the live product host:

1. First-class AST macros are **not implemented**. There is no expander
   in `oodac/*.oo` or `runtime/*.c`. `c_emit_lower` copies the call as
   `t + "(" + args + ")"` (L99). No runtime `macro_expand` / `ast_macro`.
2. Direct `macro_expand(...)` / `ast_macro(...)` are **default-deny at
   check** (`check_residual.oo` L28–29, L73–92; `check_drive.oo` L109).
   Official `fixtures/residual_macro_fail.oo` → `ERR\tresidual` +
   `--json-errors` **E_RESIDUAL**. `residual_path_a_floor_smoke` **exit 0**
   including `OK refuse residual_macro_fail (AST_MACROS)`.
3. Assigned red-team **found no fixture that expands then prints SECRET**.
   The attack `// SECRET: api_key` + `macro_expand("println(api_key)")`
   keeps the sink **inside a string literal**. emit-c / emit-llvm / native
   build / `ooda run` never inject `oo_print_str(api_key)` from that
   string. gcc/`oodac build`/`ooda run` die on undeclared `macro_expand`.
4. First-pass SECRET still sees sinks that **are** in the token stream
   (user-fn body `println(api_key)`, import-concat, `comptime { println }`).
   A second pass is unneeded until a real expander exists.
5. Regression: `secret_t1_return_smoke`, `secret_t2_depth_smoke`,
   `s71_guard_scan_smoke`, `s72_sys_exec_last_arg_smoke`,
   `s710_parse_secret_env_smoke` all **exit 0**. s61 **emit-c** rails
   still refuse; official s61 smoke **exit 1** on this stale host for
   check type-first (same residual `s61_peer1.oot` already named). **Not
   a 2.8 FN.**

Do **not** over-claim: no macro-body SECRET walker is implemented.
emit-c / emit-llvm do **not** run `check_residual_free_calls` (statement
`macro_expand(...)` emit-c **rc=0**, C still has no expansion). Alias
`let f = macro_expand` bypasses IDENT+LPAREN residual at check. `${}`
is 5.8 (prints the literal). User-sink wrapper `expand(api_key)` that
does `println(s)` is **2.10** (P15 ran and printed `s3cret_value`) —
not 2.8 expansion.

========================================================================
CLAIM UNDER PRESSURE
========================================================================

| Slice | Path-A claim | Peer gate |
|-------|--------------|-----------|
| Macro-body SECRET 2nd pass | unneeded (no expander) | no probe injects sink tokens from a string/body |
| `macro_expand` / `ast_macro` | default-deny at check | `ERR\tresidual` + `E_RESIDUAL` + rc≠0 |
| Expand then `println(SECRET)` | must not execute | emit C keeps string; gcc/`ooda run` fail; no `s3cret_value` on stdout |
| Residual doc | named, fail-closed wording | `MACRO_SECRET_RESIDUAL.oot` + `AST_MACROS.oot` + `RESIDUAL_PATH_A.oot` |
| T1 / T2 / s71 / s72 / s710 | stay green | official smokes exit 0 |
| s61 | stay green | emit-c refuse still green; official check dual-path red on stale host |

Walker honesty:
- `check_residual_free_calls` is **IDENT + next LPAREN** only. No alias.
- Residual runs on **check** only (`check_drive.oo` L109). emit-c L86 is
  `check_secret_println_sinks` then lower — **no** residual pass.
- let-bind `let r = macro_expand(...)` hits emit `unknown free call`
  (`c_emit_let_ext.oo` L190). Statement-position does not.
- String interiors are not IDENTs. That is why the expand-string attack
  does not trip `ERR\tsecret` — and also why it cannot run as code.

========================================================================
SOURCE REVIEW (2.8 — no product edit)
========================================================================

`ooda/oodac/check_residual.oo` (93 lines):

```
residual_feature_of:
  macro_expand → AST_MACROS
  ast_macro    → AST_MACROS
check_residual_free_calls:
  IDENT && next==LPAREN → residual_deny → process_exit(1)
```

`ooda/oodac/check_drive.oo`:
- L62 WT: `check_secret_println_sinks` **before** typecheck (6.1 reorder;
  **not** in the 07:49 host — host still type-then-secret).
- L109: `check_residual_free_calls` after typecheck (host + WT).

`ooda/oodac/c_emit.oo` L86: secret scan; **no** residual refuse.

`ooda/oodac/c_emit_lower.oo` L90–99: secret guard on the **unexpanded**
call, then `call = t + "(" + args + ")"`. No AST rewrite.

`ooda/oodac/cli_build.oo`: `oodac build` / `ooda run` emit-preamble +
protos + bodies + gcc. **No** `check` first. Fail-closed is gcc
`implicit declaration of function 'macro_expand'` (proved live).

Docs (path-A residual-doc close):
- `bootstrap/MACRO_SECRET_RESIDUAL.oot` (`MACRO_SECRET_RESIDUAL_ALPHA`)
- `bootstrap/MACRO_BODY_SECRET.oot` (`MACRO_BODY_SECRET_RESIDUAL_ALPHA`)
- `bootstrap/MACRO_SECRET_PASS.oot` (`MACRO_SECRET_PASS_RESIDUAL`, staged)
- `bootstrap/AST_MACROS.oot` / `RESIDUAL_PATH_A.oot` table row

========================================================================
PROBE TABLE (live product host)
========================================================================

Host: `OODAC=/home/jeryd/Projects/openOODA/ooda/oodac/oodac`
Probes: `/tmp/s28_peer1/probes/*.oo` (ephemeral; **not** landed)
Re-prove cwd: `/tmp/s28_peer1/clean` (no `.ooda-cache`).

Expect refuse-macro = check rc≠0 + `ERR\tresidual` + `AST_MACROS`.
Expect no-expand = emit C does **not** turn `"println(api_key)"` into
a sink call. Expect no-run = gcc / `ooda run` rc≠0, no `s3cret_value`.

### Assigned red-team (gate)

| # | Probe | Shape | Expect | check | emit-c | Verdict |
|---|-------|-------|--------|-------|--------|---------|
| P01 | `P01_macro_expand_println_secret.oo` | `macro_expand("println(api_key)")` + SECRET | no expand; refuse or fail-closed | rc=1 `AST_MACROS` | rc=0 C=`macro_expand(oo_str_lit("println(api_key)"))` **no injected sink** | **PASS** no-expand |
| P02 | `P02_ast_macro_println_secret.oo` | `ast_macro("println(api_key)")` | same | rc=1 `AST_MACROS` | rc=0 C=`ast_macro(...)` no inject | **PASS** no-expand |
| F1 | `fixtures/residual_macro_fail.oo` | official `let r = macro_expand("m")` | residual | rc=1 residual + `E_RESIDUAL` | rc=1 `unknown free call … macro_expand` | **PASS** |
| R1 | `scripts/secret_t1_return_smoke.sh` | T1 return / alias / if | exit 0 | — | — | **PASS** |
| R2 | `scripts/secret_t2_depth_smoke.sh` | T2 field / method / args | exit 0 | — | — | **PASS** |
| R3 | `scripts/s71_guard_scan_smoke.sh` | 7.1 ident scan | exit 0 | — | — | **PASS** |
| R4 | `scripts/s72_sys_exec_last_arg_smoke.sh` | 7.2 last-arg | exit 0 | — | — | **PASS** |
| R5 | `scripts/s710_parse_secret_env_smoke.sh` | 7.10 parse-env | exit 0 | — | — | **PASS** |
| H1 | `scripts/residual_path_a_floor_smoke.sh` | official residual floor | exit 0 + AST_MACROS | — | — | **PASS** |

**P01 execute prove (fail-closed, not just emit text):**

| Path | rc | Signal | SECRET printed? |
|------|----|--------|-----------------|
| gcc of emit-c C + `chs_rt.c` | 1 | `implicit declaration of function 'macro_expand'` | **no** (no bin) |
| `$OODAC build --backend c P01` | 1 | `ERR\tbuild\tgcc linking failed` same undeclared | **no** |
| `./bin/ooda run P01` from `ooda/` | 1 | `ERR\trun\tnative build failed` same undeclared | **no** |
| `emit-llvm P01` | 0 | `%t2 = call i64 @macro_expand(%OoStr %t1)` + string const `"println(api_key)"` | **no run**; no injected print |

**Gate: 9/9 PASS** (assigned expand-then-print + official residual + T1/T2/s71/s72/s710).

### Bonus / control (not required to ACCEPT)

| # | Probe | Shape | Expect | Result | Verdict |
|---|-------|-------|--------|--------|---------|
| P03 | let-bind `let r = macro_expand("api_key"); println(r)` | refuse | check residual; emit unknown-call | **PASS** |
| P06 | user fn body `println(api_key)` | first-pass SECRET | emit `println sink refuses SECRET api_key` | **PASS** first-pass |
| P07 | `println("macro_expand(api_key)")` | no residual FP | check OK emit OK | **PASS** no-FP |
| P08 | `// macro_expand(...)` comment | no residual FP | check OK emit OK | **PASS** no-FP |
| P10 | unused SECRET + string `"macro_expand"` | no residual/secret FP | check OK emit OK | **PASS** |
| P11 | official shape replay | residual + unknown-call | match F1 | **PASS** |
| P12 | `comptime { println(api_key) }` | first-pass still sees sink | dual `ERR\tsecret` | **PASS** (not an expander) |
| P13 | C `#define PRINTS println(api_key)` | lex refuse | `ERR\tlex` `#` | **PASS** no cpp |
| P14 | `import "lib.oo"` lib prints SECRET | first-pass on concat | dual `ERR\tsecret` | **PASS** import ≠ macro |
| P16 | `x.macro_expand(...)` | residual IDENT+LPAREN | check residual; emit invalid C method, gcc fail | **PASS** |
| P17 | newline between name and `(` | still residual | check residual; emit same as P01 | **PASS** |
| P05 | `println("${api_key}")` | 5.8 no interp | emit `oo_print_str(oo_str_lit("${api_key}"))` | **PASS** as 5.8 residual |
| H2 | `scripts/s61_env_set_secretsink_smoke.sh` | 6.1 stay green | emit-c 4/4 OK; check 3 FAIL type-first | **emit green**; smoke exit 1 (see residual) |

### Adversarial miss that is **not** a 2.8 FN

| # | Probe | What happened | Classification |
|---|-------|---------------|----------------|
| P04 | `let f = macro_expand; f("println(api_key)")` | check **OK** (no IDENT+LPAREN on the residual name). emit-c rc=0: `long long f = macro_expand; f(...)`. gcc rc=1 undeclared + `f` not a function. **No expansion. No print.** | Residual-refuse completeness (alias). Build still fail-closed. **Not** a SECRET FN. |
| P09 | `let x = ast_macro` (no call) | check OK; emit `long long x = ast_macro`; no expand | Residual is call-shaped only. Fine. |
| P15 | `fn expand(s){ println(s) }; expand(api_key)` | check OK, emit OK, **gcc+run printed `s3cret_value`** | **SPRINT 2.10** user-sink wrapper. Sink is in the token stream. Not a macro expander. T2 `secret_call_arg_fail` is return-prop, not callee-println. Do **not** hang this on 2.8. |
| s61 smoke | official script exit 1 | check `undefined variable 'env_set'` (host type-first). emit-c still `ERR\tsecret`. WT `check_drive.oo` moved secret before typecheck; host 07:49 does not have it. Script is untracked. | Pre-existing 6.1 check dual-path residual (`s61_peer1.oot`). **Not** a 2.8 regression. |
| ast_macros residual smoke | exit 1 `FAIL missing bootstrap/AST_MACROS.md` | file is `AST_MACROS.oot` | Honesty-pack suffix lag. Real refuse rail is `residual_path_a_floor_smoke` (green). |

========================================================================
COMMAND (repro)
========================================================================

```
OODAC=/home/jeryd/Projects/openOODA/ooda/oodac/oodac
cd /tmp/s28_peer1/clean
$OODAC check /tmp/s28_peer1/probes/P01_macro_expand_println_secret.oo
# rc=1  ERR<tab>residual<tab>… free call 'macro_expand' … AST_MACROS
$OODAC emit-c /tmp/s28_peer1/probes/P01_macro_expand_println_secret.oo
# rc=0  C contains: macro_expand(oo_str_lit("println(api_key)"));
#        does NOT contain oo_print_str(api_key) from expansion
$OODAC check /home/jeryd/Projects/openOODA/ooda/fixtures/residual_macro_fail.oo --json-errors
# rc=1  code=E_RESIDUAL
cd /home/jeryd/Projects/openOODA/ooda
$OODAC build --backend c /tmp/s28_peer1/probes/P01_macro_expand_println_secret.oo /tmp/s28_peer1/P01.bin
# rc=1  implicit declaration of function 'macro_expand'
./bin/ooda run /tmp/s28_peer1/probes/P01_macro_expand_println_secret.oo
# rc=1  ERR<tab>run<tab>native build failed   (no s3cret_value)
OODAC=./oodac/oodac bash scripts/secret_t1_return_smoke.sh          # exit 0
OODAC=./oodac/oodac bash scripts/secret_t2_depth_smoke.sh           # exit 0
OODAC=./oodac/oodac bash scripts/s71_guard_scan_smoke.sh            # exit 0
OODAC=./oodac/oodac bash scripts/s72_sys_exec_last_arg_smoke.sh     # exit 0
OODAC=./oodac/oodac bash scripts/s710_parse_secret_env_smoke.sh     # exit 0
OODAC=./oodac/oodac bash scripts/residual_path_a_floor_smoke.sh     # exit 0
OODAC=./oodac/oodac bash scripts/s61_env_set_secretsink_smoke.sh    # exit 1 host check type-first
```

========================================================================
FP / FN NOTES
========================================================================

### False positives (residual/secret refuse when should allow)

| Probe | Result |
|-------|--------|
| String `"macro_expand(api_key)"` | **no residual FP** |
| Comment `// macro_expand(...)` | **no residual FP** |
| Unused SECRET + public println | **no secret FP** |
| `${api_key}` as string text | **no secret FP** (prints literal; 5.8) |

**Assigned residual FP count: 0.**

### False negatives (expand-then-print SECRET)

| Probe | Result |
|-------|--------|
| `macro_expand("println(api_key)")` | **no expand, no print** |
| `ast_macro("println(api_key)")` | **no expand, no print** |
| Alias `f = macro_expand; f(...)` | **no expand**; gcc fail-closed |
| let-bind expand result | residual + unknown-call |
| `#define` / `comptime` | lex / first-pass secret |

**Assigned expand-then-print FN count: 0.**

emit-c statement residual-not-refused is **not** counted as a SECRET FN:
the C call is not an expander and cannot link.

========================================================================
RESIDUALS (honest, out of 2.8 gate)
========================================================================

1. **No emit-c / emit-llvm residual pass.** Statement `macro_expand(...)`
   emit-c rc=0. Fail-closed at gcc / `ooda run`, not at emit. If a runtime
   stub is ever added, this becomes a 2.8 reopen.
2. **Residual IDENT+LPAREN only.** `let f = macro_expand` check OK (P04).
3. **`${}` interp** — 5.8. No expansion today (literal print).
4. **User-sink wrappers** — 2.10. P15 printed SECRET via `println(s)`.
5. **s61 official smoke exit 1** on this host (check type-first). emit-c
   still refuse. WT `check_drive.oo` reorder not in ELF. Not 2.8.
6. **`ast_macros_residual_smoke.sh` looks for `AST_MACROS.md`** (missing;
   file is `.oot`). Doc-pack suffix. Refuse rail is residual_path_a (green).
7. **2.8 residual docs split / hygiene:** `MACRO_SECRET_PASS.oot` staged;
   `MACRO_BODY_SECRET.oot` + `MACRO_SECRET_RESIDUAL.oot` untracked. Leave.
8. Full IFC / `#[Secret]` / real AST macros — reopen 2.8 when macros ship.

========================================================================
PEER OWN
========================================================================

No product compiler edit. No fixture land. No smoke change.
Write: `openOODA/audit/s28_peer1.oot` only.
Solo under SWARM (single audit file; live host probes only).

========================================================================
CLOSE TABLE
========================================================================

| Item | Status |
|------|--------|
| Expand-then-print SECRET fixture | **none found** |
| `macro_expand` / `ast_macro` check refuse | **PASS** live + E_RESIDUAL |
| `ooda run` / gcc cannot execute expand-string | **PASS** undeclared, no print |
| Residual doc present + fail-closed wording | **PASS** |
| First-pass still catches in-source sinks | **PASS** (P06/P12/P14) |
| T1 smoke | **PASS** exit 0 |
| T2 smoke | **PASS** exit 0 |
| s71 / s72 / s710 smokes | **PASS** exit 0 |
| s61 emit-c refuse | **PASS** (official smoke check dual-path OPEN) |
| residual_path_a AST_MACROS | **PASS** exit 0 |
| Gate matrix | **9/9 PASS** |
| Path-A close 2.8 | **ACCEPT** |
| emit residual / alias / 2.10 wrapper / `${}` / s61 check | **OPEN residual** (not gate) |

end of s28_peer1.oot
