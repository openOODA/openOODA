# s28_peer2.oot
# QA Peer Verifier 2 — SPRINT 2.8 doc vs tree honesty (greenwash)
# Date: 2026-08-12T19:17Z
# Role: Peer Verifier 2 (orthogonal to Peer 1 / integration)
#       OWN = MACRO_SECRET_RESIDUAL.oot exists + claims match tree
# Tree: /home/jeryd/Projects/openOODA
# Write: this audit only. No product/std/oodac edits. No push.

========================================================================
VERDICT
========================================================================

## **ACCEPT** 2.8 residual-doc honesty

SPRINT **2.8** ("Macro-body SECRET second pass **(or residual doc)** —
**FIXED** path-A documented (`MACRO_SECRET_RESIDUAL.oot`)") is an
honest residual-doc close, not a walker land:

1. **File exists** at `ooda/bootstrap/MACRO_SECRET_RESIDUAL.oot`
   (23 L, 1508 B, md5 `14b2e893…`, marker `MACRO_SECRET_RESIDUAL_ALPHA`).
2. **Claims match the tree.** AST macros are not expanded. Check
   default-denies free calls `macro_expand` / `ast_macro` → `AST_MACROS`
   (`check_residual.oo` L28–29, wired `check_drive.oo` L109). No
   `c_secret_macro` / second-pass walker in `c_emit*`.
3. **No greenwash.** Doc explicitly does **not** claim SECRET tracking
   through expansions. Dual-claim scan of "second pass shipped /
   product-green" is empty. SPRINT **FIXED** is the allowed residual
   close, not an implemented pass.

**Do not claim:** second-pass walker In; SECRET-through-macros;
AST_MACROS product; emit-c / emit-llvm residual-coded refuse;
`${}` (5.8); git-tracked residual (SPRINT-named file is `??`).

**Do not REJECT 2.8** solely because the SPRINT-named file is
untracked, twins exist, or emit-llvm is dual-path fail-open on the
free name. Those are named residuals. None is expansion.

========================================================================
SUMMARY TABLE
========================================================================

| # | Probe | Expected | Actual | VERDICT |
|---|--------|----------|--------|---------|
| 1 | `MACRO_SECRET_RESIDUAL.oot` exists | SPRINT-named path | `ooda/bootstrap/` 23 L / 1508 B | **PASS** |
| 2 | Marker + "do not claim" | residual honesty | `MACRO_SECRET_RESIDUAL_ALPHA`; L16–18 no walker | **PASS** |
| 3 | Rails named in doc | `check_residual.oo` + `RESIDUAL_PATH_A.oot` | both on disk; L28–29 + table AST_MACROS | **PASS** |
| 4 | `residual_feature_of` | `macro_expand`/`ast_macro` → AST_MACROS | source + host `strings` | **PASS** |
| 5 | `oodac check` residual_macro_fail | rc≠0 + `ERR residual` AST_MACROS | dual rc=1 | **PASS** |
| 6 | `oodac check` ast_macro | same | rc=1 AST_MACROS | **PASS** |
| 7 | product `ooda check` | same refuse | rc=1 AST_MACROS | **PASS** |
| 8 | seed `bootstrap/seed/oodac check` | same refuse | rc=1 AST_MACROS | **PASS** |
| 9 | tokens still `IDENT macro_expand` | no rewrite | `IDENT125macro_expand` | **PASS** |
| 10 | emit-c residual_macro_fail | no expand; fail-closed | dual rc=1 `unknown free call macro_expand` | **PASS** (named) |
| 11 | emit-llvm residual_macro_fail | no expand | dual **rc=0** `call i64 @macro_expand` — **no body rewrite** | **OPEN** (not 2.8) |
| 12 | IDENT without `(` | refuse is IDENT+LPAREN | `let f = macro_expand` check **OK**; still no expand | **OPEN** (1.4) |
| 13 | No second-pass fn | zero `c_secret_macro` / expand walker | grep `oodac/*.oo` empty | **PASS** |
| 14 | First-pass SECRET still live | `println(tok)` refuse | `ERR secret` rc=1 | **PASS** |
| 15 | String body is not a sink | no expand → residual not secret | `macro_expand("println(token)")` → AST_MACROS | **PASS** |
| 16 | `${}` not this close | 5.8 residual | `println("${x}")` check **OK** (literal) | **PASS** named |
| 17 | Dual-claim / greenwash | no shipped second pass | zero hits on residual + twins | **PASS** |
| 18 | `residual_path_a_floor_smoke` | refuse residual_macro_fail | **PASSED** (AST_MACROS row) | **PASS** |
| 19 | `s28_macro_secret_smoke.sh` | doc exists + refuse + no greenwash | dual **ALL OK** | **PASS** |
| 20 | SPRINT **FIXED** wording | residual-doc close only | "path-A documented (MACRO_SECRET_RESIDUAL.oot)" | **PASS** |
| 21 | Git of SPRINT-named file | documented if untracked | `??` (PASS twin is `A`) | **PASS** (named) |

**OVERALL: ACCEPT** 2.8 (doc exists; claims match tree; no greenwash).

========================================================================
METHOD
========================================================================

1. Static read of live WT `MACRO_SECRET_RESIDUAL.oot` vs
   `check_residual.oo`, `RESIDUAL_PATH_A.oot`, `AST_MACROS.oot`,
   `SECRET_TAINT.oot`, `c_emit_secret.oo`, `check_drive.oo`, SPRINT 2.8.
2. Dual-claim / greenwash grep on residual + twins (`MACRO_SECRET_PASS`,
   `MACRO_BODY_SECRET`).
3. Live product host only: `ooda/oodac/oodac` check / emit-c / emit-llvm
   / tokens. Dual-repro on refuse + llvm + s28 smoke.
4. Seed `bootstrap/seed/oodac` + product `bin/ooda check`.
5. Ephemeral fixtures `/tmp/s28_peer2/` (not committed).
6. No product rebuild. No push. No trust of sibling s28 summaries
   without re-prove (RULES §1.9).

========================================================================
HOST / TIP / WT
========================================================================

| Item | Value |
|------|--------|
| When (UTC) | 2026-08-12T19:17Z |
| ooda tip | `52cc4b5` — `fix(r2): multi-import typecheck path-A + digit-0 import names` |
| openOODA tip | `81f56fc` + WIP SPRINT 2.8 **FIXED** line |
| Product host | `ooda/oodac/oodac` **962960** B @ 2026-08-12 07:49:59 -0700 |
| Host md5 | `a44ee35eb344837009697e2c9d4e4662` |
| Surface md5 | `MACRO_SECRET_RESIDUAL.oot` `14b2e893faac7f36e7e453c2a4f78866` |
| Host symbols | `T residual_feature_of` @ `0x411c60`; `T check_residual_free_calls` @ `0x4123f0` |
| Host strings | `macro_expand` / `ast_macro` / `AST_MACROS` |
| Git residual | `?? MACRO_SECRET_RESIDUAL.oot` + `?? MACRO_BODY_SECRET.oot`; `A MACRO_SECRET_PASS.oot` |
| Peer OWN | audit only |

Host ELF predates the residual docs (07:49 vs 11:50). Refuse rail is
HEAD-clean `check_residual.oo` — not a new walker. Concurrent dirt on
`c_emit_secret*.oo` / `check_drive.oo` is 2.10 / 6.1 / 7.x (no macro
/ expand / second-pass hunks).

========================================================================
DOC vs TREE (claim map)
========================================================================

Residual (`MACRO_SECRET_RESIDUAL.oot`):

| Claim | Tree |
|-------|------|
| Status Unneeded (Path A Floor) | No expand walker; AST_MACROS is path-A refuse floor |
| Macros not evaluated/expanded at rt/ct | tokens keep IDENT; emit-c unknown-call; llvm emits `@macro_expand` call **without rewrite** |
| `check_residual.oo` default-deny free calls | L28–29 + `check_residual_free_calls` IDENT+LPAREN; `check_drive.oo` L109 |
| `RESIDUAL_PATH_A` names AST_MACROS | table row `macro_expand`, `ast_macro` |
| Do not claim SECRET-through-macros | `c_emit_secret.oo` has **zero** expand/macro hits |
| Reopen when AST_MACROS promoted | matches `AST_MACROS.oot` + DESIGN 1.4 + PM 1.4 "Names refused" |

`load_import` concat is **file text**, not AST rewrite. Emitted C
`#define` (HITL / MAX_CYCLES) is compiler-internal, not user macros.

========================================================================
GREENWASH
========================================================================

| Risk | Finding |
|------|---------|
| Claim second pass implemented | **absent** — residual + twins deny it |
| SPRINT **FIXED** = walker In | **no** — qualifier is "path-A documented" + issue allows residual doc |
| SECRET_TAINT lists 2.8 as In | **no** — table omits 2.8 (honest: unneeded, not a taint product) |
| t2_security still lists 2.8 residual | **consistent** — walker itself remains residual |
| Triple docs overclaim | **no** — same unneeded thesis; three markers (sludge, not green) |
| `ast_macros_residual_smoke.sh` looks for `.md` | pre-existing pack drift; **not** 2.8. Live rail is floor smoke + s28 smoke |

========================================================================
NAMED RESIDUALS (not REJECT)
========================================================================

R1. SPRINT-named file is **untracked**. Twin `MACRO_SECRET_PASS.oot` is
    staged. Hygiene already left all three. Close is WT-true.
R2. emit-c does **not** call `check_residual_free_calls`. Fail-closed
    via `unknown free call` (`c_emit_let_ext.oo` L190). Doc says
    default-deny **at check** — accurate.
R3. emit-llvm **rc=0** emits `call i64 @macro_expand` (dual). **Not**
    expansion. AST_MACROS dual-path residual (1.4), not a SECRET FN.
R4. Residual walk is IDENT+LPAREN only. `let f = macro_expand` check
    **OK**. Still no expand. 1.4 refuse completeness.
R5. `${}` interp is **5.8**. Check accepts the literal. Residual doc
    does not close it.
R6. Check secret runs **before** residual (`check_drive.oo` L62 then
    L109). `println(r)` after `r = macro_expand(key)` may `ERR secret`
    first. Still fail-closed; not a second pass.

========================================================================
LIVE (product host)
========================================================================

```
oodac check fixtures/residual_macro_fail.oo     # rc=1 ERR residual AST_MACROS ×2
oodac check /tmp/s28_peer2/ast_macro_fail.oo    # rc=1 ERR residual AST_MACROS
bin/ooda check residual_macro_fail.oo           # rc=1 ERR residual AST_MACROS
bootstrap/seed/oodac check residual_macro_fail  # rc=1 ERR residual AST_MACROS
oodac emit-c residual_macro_fail.oo             # rc=1 unknown free call ×2
oodac emit-llvm residual_macro_fail.oo          # rc=0 call i64 @macro_expand ×2
oodac tokens residual_macro_fail.oo             # IDENT macro_expand
scripts/residual_path_a_floor_smoke.sh          # PASSED (macro row AST_MACROS)
scripts/s28_macro_secret_smoke.sh               # ALL OK ×2
```

========================================================================
FILES
========================================================================

| Path | Role |
|------|------|
| `openOODA/audit/s28_peer2.oot` | this report (OWN) |
| `/tmp/s28_peer2/` | ephemeral probes (not committed) |

No product / std / oodac / SPRINT edits. No push.

========================================================================
FINAL
========================================================================

**ACCEPT** 2.8. `MACRO_SECRET_RESIDUAL.oot` exists, matches the tree,
and does not greenwash a walker. Second pass stays residual until
`AST_MACROS` is product.

end of s28_peer2.oot
