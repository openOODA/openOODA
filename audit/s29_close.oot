# 2.9 close — grouped println SECRET (2026-08-12)
**Host:** `ooda/oodac/oodac` 963032 B, md5 `67f7fe463708ac0feff304263a5c569b`, mtime 09:55.

**Smoke:** `scripts/s29_println_guard_smoke.oo` **ALL OK** (bare, grouped later `(1), tok`, same-arg `("x")+tok`, eprintln, string no-FP). Dual-path check + emit-c.

**MUST (clean cwd):**
| Shape | check | emit-c |
|-------|-------|--------|
| `println((n), tok)` | ERR secret | ERR secret |
| `println(("x")+tok)` | ERR secret | ERR secret |
| `eprintln(id(1), tok)` | ERR secret | ERR secret |
| `println(n, tok)` | ERR secret | ERR secret |

**Honesty:** helper unify (`c_secret_emit_guard` from stmt println) is **not** claimed. Close is **depth-aware IDENT walk**. Full IFC / `${}` residual.

**P7:** modular large-tree check is in source (`check_mod.oo` >12 files). Shipping this ELF may predate that land. `p7_check_main_smoke` still needs a rebuild that includes `check_mod` change.
