# s29_domain_b.oot — Domain Expert B / SPRINT 2.9 println walker
# Residual: MED 2.9 — orthogonal: eprintln/print_err/log vs println;
#           dual-path check vs emit; string-lit no-FP
# Date: 2026-08-12T19:26:16Z
# Role: Domain Expert B of 12-agent 2.9 phalanx (orthogonal to Domain A unify)
# Tree: /home/jeryd/Projects/openOODA
# Tip (ooda): 52cc4b5 fix(r2): multi-import typecheck path-A + digit-0 import names
# Host: ooda/oodac/oodac (product, 962960 B, md5 a44ee35eb344837009697e2c9d4e4662)
# OWN: prove print-family split (eprintln / print_err / log vs println);
#      prove check vs emit (and llvm) dual-path honestly; prove string-lit
#      SECRET spelling is no-FP. Do not rewrite 7.x. No moonshots;
#      no auto-push; monofile ≤600.
#
# ID: SPRINT backlog **2.9** = println walker → full `c_secret_emit_guard`.
# Historical DE2.9 row (`c_emit_secret.oo:175-186`) is the check-path
# token IDENT walk, **not** a close. B does not flip SPRINT.
## VERDICT
**PASS-with-residual** (honesty). No source change this turn. 7.x not rewritten.

2.9 unify is **OPEN**. Live product host (clean-cwd probes; see cache note):

- **println / eprintln** (product names): SECRET IDENT is **dual-path
  secret** — check + emit-c + emit-llvm all `ERR\tsecret`. String lit
  `"tok"` is **no-FP** on all three. Alias `let f = println|eprintln;
  f(tok)` same dual-path secret.
- **print_err / log / fprintf / syslog** (guard list, **not** check
  walker): emit-c **SECRET** via `c_secret_emit_guard` →
  `c_guard_scan_idents`. check = **type** `undefined variable` (no
  `ERR\tsecret`). emit-llvm **rc=0** `call @print_err` / `@log` / …
  — **llvm fail-open**. String lit: emit-c **no** `ERR\tsecret` (no-FP);
  check type (no secret).
- **`print`** (LLVM println-shape, not a C sink): emit-c raw `print(tok);`
  **no secret**; check type; llvm **`oo_print_str` + `oo_println`** no
  secret. Not a typed product API.
- **Drop-in trap:** replacing L175–192 with
  `c_secret_emit_guard(env, sid, "", "", "", "")` would **FN** the
  check/llvm front door (empty `args` → scanner no-ops). Unify must
  feed lowered arg text **or** keep the IDENT token walk and grow the
  sid list.
- **7.x:** not rewritten
- **Monofile:** print **87**; ident **228**; guard **228**; secret **264** (≤600)

Domain A owns walker → `c_secret_emit_guard` (stmt println and/or
check-path L175–192). This file owns **family split / dual-path /
no-FP**.

---
SCOPE (orthogonal to Domain A)
---

| In | Out |
|----|-----|
| eprintln / print_err / log vs println rails | Replacing L175–192 with the guard (A) |
| check vs emit vs llvm dual-path honesty | 7.1 `c_guard_scan_idents` rewrite |
| String-lit / comment `"tok"` no-FP | 7.2 `c_sys_exec_last_arg` rewrite |
| Isolated tmp probes (not committed fixtures) | 7.10 `c_parse_secret_env` rewrite |
| Drop-in empty-arg FN warning | 2.10 `__sec_sink__`; SPRINT flip; remotes |

---
WHY THE FAMILY SPLIT MATTERS (2.9 attack class)
---

Three rails, one name-set that **does not match**:

| Rail | Names that get **secret** IDENT refuse |
|------|----------------------------------------|
| check / llvm front-door (`check_secret_println_sinks` L175–192) | **`println` `eprintln` only** (alias-resolved sid) |
| stmt emit (`c_emit_ident_stmt` → `c_emit_println`) | **`println` only** (`__sec__` IDENT; **no** alias resolve) |
| free-call / method (`c_emit_lower` / `c_emit_call` → guard) | `println` `eprintln` **`print_err` `log` `fprintf` `syslog`** (+ other sealed) |

`eprintln` is a **product** sibling (`tc_names`, LLVM builtin,
`SECRET_TAINT` In). `print_err` / `log` / `fprintf` / `syslog` are
**emit-guard only**. `print` is LLVM-only shape. 2.9 is the unify of
those rails — **not** a new refuse for println (already dual-path).

---
STATIC (tip source; B did not edit)
---

| Site | Fact |
|------|------|
| `c_emit_secret.oo:175–192` | `sid == "println" \|\| sid == "eprintln"`; optional LPAREN; IDENT → `c_secret_refuse_sink_resolved`. **No** `c_secret_emit_guard`. **No** print_err/log/fprintf/syslog |
| `c_emit_print.oo:9–85` | `c_emit_println`: token IDENT + `c_secret_refuse_println_ident` (`__sec__` only). Zero guard calls |
| `c_emit_ident.oo:129–131` | stmt `id == "println"` only → `c_emit_println` (early return) |
| `c_emit_lower.oo:90` | free-call (eprintln / print_err / log / …) → `c_secret_emit_guard` |
| `c_emit_call.oo:82` | method → same guard |
| `c_emit_secret_guard.oo:223–225` | full `args` scan lists println/eprintln/**print_err/log/fprintf/syslog** |
| `c_emit_secret_alias.oo:9–10` | same print-family in `c_secret_is_sink_name` |
| `tc_names.oo:5` | known: **println + eprintln only** (not print_err/log/print) |
| `llvm_emit_expr.oo:132–133` | builtin **`println` `print` `eprintln`** — no print_err; no walker |
| `c_emit.oo:86` / `llvm_emit.oo:79` | `check_secret_println_sinks` **before** lower / llvm body |
| `check_drive.oo` | **HEAD ELF:** secret **after** type/cap. **WT dirty:** secret **before** type (6.1; **not in this ELF**) |
| `SECRET_TAINT.oot` | println/eprintln **In**; “logs / non-println” **residual** |

Dirty hunks on `c_emit_secret*.oo` / `c_emit_args.oo` / `check_drive.oo`
are **7.10** slice + **2.10** `__sec_sink__` + **6.1** reorder + **7.2**
comments — **not** a 2.9 walker land. `c_emit_print.oo` / `c_emit_ident.oo`
HEAD-clean. 7.1 walker / 7.2 last-arg / 7.10 parse-env **untouched by B**.

WT secret-first would still **not** dual-path-secret `print_err`/`log`:
the L175 list does not name them. 6.1 reorder ≠ 2.9 family close.

---
LIVE MATRIX — product fixtures (clean cwd)
---

Dir: `$TMPDIR/s29_b_probes` (not committed). Cwd **that dir** so
workspace-root `.ooda-cache/check` cannot false-OK (s71 R9). Host
`oodac` check / emit-c / emit-llvm.

Refuse (expected `rc≠0` + `ERR\tsecret` on **all three**):

| Fixture | Sink | Result |
|---------|------|--------|
| `secret_sink_fail` | println | **PASS** dual-path `println sink refuses SECRET x` |
| `secret_eprintln_fail` | eprintln | **PASS** dual-path `eprintln sink refuses SECRET tok` |
| `secret_t2_args_println_secret_fail` | println | **PASS** dual-path |
| `secret_t2_real_arg_fail` | println | **PASS** dual-path `api_key` |
| `secret_multi_arg_println_fail` | println | **PASS** dual-path `s` |
| `secret_t1_alias_println` | println alias | **PASS** dual-path `secret_tok` |

No-FP / pass (expected `rc=0`, **no** `ERR\tsecret`):

| Fixture | Claim | Result |
|---------|-------|--------|
| `secret_t2_string_lit_arg_fp` | `println("api_key")` | **PASS** no-FP triple |
| `secret_t2_string_fp` | `let msg = "api_key"; println(msg)` | **PASS** no-FP triple |
| `secret_t2_string_contains_name_fp` | longer string contains name | **PASS** |
| `secret_t2_nested_call_fp` | println string text only | **PASS** |
| `secret_t2_args_println_ok_pass` | `println("no secret")` + other SECRET | **PASS** |
| `secret_t2_comment_fp` / `_blockish_fp` | `// println(api_key)` | **PASS** |
| `secret_midline_safe` | mid-line `// SECRET:` not a tag | **PASS** |
| `secret_sink_pass` / `secret_eprintln_pass` | public print | **PASS** |

**Product fixture FN=0, FP=0** (clean cwd).

Workspace-root `.ooda-cache/check` **did** false-OK `secret_sink_fail`
(`OK` / rc=0) until cwd changed — content-hash cache of an older OK.
Not a walker FN. Named residual (process).


*(Continued in `openOODA/audit/s29_domain_b_part2.oot`)*

end of s29_domain_b.oot (part 1)
