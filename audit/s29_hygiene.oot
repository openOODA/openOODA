# s29_hygiene.oot — Hygiene Enforcer (2.9 println walker → c_secret_emit_guard)
# Date: 2026-08-12T19:23:25Z
# Role: Dependency/Hygiene Enforcer — RULES §1.7 (2.9 phalanx)
# Tree: /home/jeryd/Projects/openOODA  (ooda/ + openOODA/)
# Tip (ooda): 52cc4b5 + WIP (`scripts/s29_println_guard_smoke.oo` ??;
#              `c_emit_secret*.oo` dirt is 7.10 slice + 2.10 `__sec_sink__`)
# Tip (openOODA): 81f56fc + WIP `audit/s29_harness.oot` ??; SPRINT 2.9 still OPEN
# OWN: line-lock 2.9-touched `.oo`; bash -n `s29_println_guard_smoke.oo`;
#      no ELF / scratch in `oodac/`; consistent `audit/s29_*.oot`; no credentials
# WRITE: this audit + hygiene-only scrub of leftover emit dump
# POLICY: RULES §1.7 / PROBE MAX_LINES = 256 (soft); pack watch 300; hard ban >600
#         No mass product peels; force-split only if DE blew 600
#
# ID: SPRINT backlog **2.9** = "println walker → full `c_secret_emit_guard`".
# Historical SPRINT "What got fixed" DE2.9 row is check-path token refuse
# (`c_emit_secret.oo` IDENT walk), **not** stmt-println unify. Do not
# treat that table as a 2.9 close.
## SUMMARY
| Check | VERDICT | Notes |
|-------|---------|-------|
| Hard ban >600 (touched .oo) | **PASS** | 0 OWN / 0 `ooda/oodac/**/*.oo` over 600 |
| Soft lock ≤256 (2.9 OWN) | **PASS** | print **87** / ident **228** / guard **228** / lower **241** |
| Pack pressure ≤300 (OWN) | **PASS** | all 2.9 OWN << 300 |
| Adjacent secret family soft | **WARN** residual | `c_emit_secret.oo` **264**, `c_emit_secret_prop.oo` **262**, `main.oo` **307**, `check_cap_util.oo` **357** — pre-existing / 2.10 / 7.10; **not** 2.9 blow |
| DE monofile blow >600 | **none** | **no 2.9 product land**; print/ident/lower HEAD-clean |
| ELF extra / scratch in `oodac/` | **PASS** | no bak/x/fuzz/toys; only product host |
| Product host ELF | **kept** | gitignored `oodac/oodac` **962960** B — required by smoke; not a DE toy |
| Super emit dump | **PASS** (scrubbed) | deleted `oodac_new.tmp.c` (reappeared mid-wave) |
| Credentials | **PASS** | ZERO live secrets/tokens/keys |
| `/tmp` in product `.oo` | **PASS** | ZERO in 2.9 OWN |
| Smoke `/tmp` | **PASS** | harness uses `$TMPDIR/s29_println_guard_$$` + `trap` |
| `bash -n` s29 smoke | **PASS** | `scripts/s29_println_guard_smoke.oo` 157L syntax clean |
| `audit/s29_*.oot` names | **PASS** | `s29_harness` + this file; no rogue `issue_2_9_*` |
| Mass deletes of product | **none** | only untracked/ignored debris |
| Push | **no** | charter |

OVERALL: **PASS**
  (hard monofile green; no extra ELF; emit-dump scratch scrubbed; names
   consistent; no credentials; 2.9 unify still OPEN — no DE blow to split)

## SCOPE + METHOD
2.9 ACTIVE (SPRINT backlog): MED unify stmt `println` walker onto full
`c_secret_emit_guard` (same rail as free-call / eprintln).

Live dual path (hygiene re-read; **not** a product edit):

| Path | File | What it does |
|------|------|--------------|
| stmt `println` | `c_emit_ident.oo` L129–131 → `c_emit_print.oo` | token IDENT + `c_secret_refuse_println_ident` (`__sec__` only). **No** `c_secret_emit_guard` / `c_guard_scan_idents` |
| check / LLVM front-door | `c_emit_secret.oo` L175–193 | token IDENT walk + `c_secret_refuse_sink_resolved` (alias). **No** `c_secret_emit_guard` |
| free-call lower | `c_emit_lower.oo` L90 | **already** `c_secret_emit_guard(...)`; guard L223–225 lists `println`/`eprintln`/`print_err`/`log`/`fprintf`/`syslog` → `c_guard_scan_idents` |
| method | `c_emit_call.oo` L82 | same guard (2.2) |

OWN units this pass:

| Path | Role in 2.9 | Git |
|------|-------------|-----|
| `oodac/c_emit_print.oo` | stmt `c_emit_println` token rail | **clean** (HEAD 87L) |
| `oodac/c_emit_ident.oo` | `id == "println"` dispatch | **clean** (HEAD 228L) |
| `oodac/c_emit_lower.oo` | free-call already calls guard | **clean** (HEAD 241L) |
| `oodac/c_emit_secret.oo` | check-path println token walk | dirty (**7.10** `str_slice` + **2.10** `__sec_sink__`) |
| `oodac/c_emit_secret_guard.oo` | `c_secret_emit_guard` + walker | dirty (**2.10** `__sec_sink__` only) |
| `scripts/s29_println_guard_smoke.oo` | harness rails (TMPDIR fixtures) | `??` untracked (157L) |
| `audit/s29_*.oot` | phalanx evidence | `s29_harness` `??` / this file |

Adjacent observe (line-lock only if DE had blown 600):

| Path | Lines | Note |
|------|------:|------|
| `oodac/c_emit_secret_prop.oo` | 262 | T1/T2 prop (pre-existing soft) |
| `oodac/c_emit_secret_alias.oo` | 157 | alias resolve |
| `oodac/c_emit_args.oo` | 240 | 7.2 last-arg (already sliced) |
| `oodac/c_emit_call.oo` | 141 | method guard (2.2) |
| `oodac/main.oo` | 307 | pre-existing soft/pack residual |
| `oodac/check_cap_util.oo` | 357 | pre-existing pack residual |
| `bootstrap/SECRET_TAINT.oot` | 129 | path-A println/eprintln **In**; unify not named |
| `scripts/secret_eprintln_smoke.oo` | 47 | existing eprintln dual rail |

No 2.9 product walker land. Hygiene does **not** run the smoke
(integration/harness own exit-0); bar here is `bash -n` + tree locks.

Commands:

  `wc -l` + hard/pack/soft gates on OWN + full `oodac/*.oo` >600 inventory
  repo-wide `.oo` >600 inventory (honesty: 12 `std/src` catalog stubs)
  `git add -n oodac/` after scrub
  `bash -n` s29 smoke + related secret smokes
  `grep` credential + `/tmp` on OWN + smoke + s29 audits
  delete extra ELF + emit-dump scratch; **keep** product `oodac/oodac`
  no push; no product logic rewrite; no SPRINT flip

Thresholds:

| Threshold | Meaning |
|-----------|---------|
| **256** | RULES §1.7 / PROBE MAX_LINES — soft lock |
| **300** | Pack monofile pressure (flag for DE peel) |
| **600** | Path-A hard ban — force-split only if blown |

---
LINE LOCK — 2.9-touched / adjacent monofiles (live)
---

Measured under monorepo tip ooda=`52cc4b5`+WIP (2026-08-12T19:23Z).

| File | Lines | ≤256 | ≤300 | ≤600 | HEAD dirty? | Split? |
|------|------:|:----:|:----:|:----:|:-----------:|:------:|
| `oodac/c_emit_print.oo` | **87** | PASS | PASS | PASS | no | NO |
| `oodac/c_emit_ident.oo` | **228** | PASS | PASS | PASS | no | NO |
| `oodac/c_emit_lower.oo` | **241** | PASS | PASS | PASS | no | NO |
| `oodac/c_emit_secret_guard.oo` | **228** | PASS | PASS | PASS | 2.10 `__sec_sink__` | NO |
| `oodac/c_emit_secret.oo` | **264** | OVER | PASS | PASS | 7.10 + 2.10 | residual |
| `oodac/c_emit_secret_prop.oo` | **262** | OVER | PASS | PASS | no | residual |
| `oodac/c_emit_secret_alias.oo` | **157** | PASS | PASS | PASS | no | NO |
| `oodac/c_emit_call.oo` | **141** | PASS | PASS | PASS | no | NO |
| `oodac/c_emit_args.oo` | **240** | PASS | PASS | PASS | 7.2 comments | NO |
| `oodac/main.oo` | **307** | OVER | OVER | PASS | no | residual |
| `oodac/check_cap_util.oo` | **357** | OVER | OVER | PASS | no | residual |
| `scripts/s29_println_guard_smoke.oo` | **157** | PASS | PASS | PASS | NEW | n/a |
| `audit/s29_harness.oot` | **37** | PASS | PASS | PASS | NEW | n/a |
| Aggregate | Value |
|-----------|------:|
| Touched `.oo` over **600** | **0** |
| `oodac/**/*.oo` over **600** | **0** |
| 2.9 OWN over **256** | **0** (print/ident/lower/guard) |
| Shared secret over **256** | **1** (`c_emit_secret.oo` = 264 — 7.10/2.10; not 2.9) |
| Adjacent over **256** | **3** (prop 262 / main 307 / check_cap 357 — pre-existing) |
| Out-of-OWN `std/src/**/*.oo` over **600** | **12** (catalog stubs — not 2.9) |

oodac pack-watch (≥256, all product — honesty, not 2.9 OWN):

| File | Lines |
|------|------:|
| `bc_emit_expr.oo` | 256 |
| `c_emit_secret_prop.oo` | 262 |
| `c_emit_let_ext.oo` | 263 |
| `c_emit_secret.oo` | 264 |
| `lex.oo` | 267 |
| `load_import.oo` | 289 |
| `main.oo` | 307 |
| `c_emit_skip.oo` | 329 |
| `check_cap_util.oo` | 357 |

md5 (stable re-read at measure):

| File | md5 |
|------|-----|
| `c_emit_print.oo` | `b3b53cbe97c77cbd1dbe4f6744b69162` |
| `c_emit_ident.oo` | `ce738529e56a16c06d636e5817d95064` |
| `c_emit_lower.oo` | `16d3971c99cdbd06fe41f74958288dd8` |
| `c_emit_secret.oo` | `a1e63ca5a51b2602f39a651013c4ed9b` |
| `c_emit_secret_guard.oo` | `ef9029995324d31aeb26748afe21e75b` |
| `s29_println_guard_smoke.oo` | `664dbdd90f4f847718d022969861c41c` |
| `s29_harness.oot` | `f84bce61206c5d63505e42df5c96eddf` |
| product `oodac/oodac` | `a44ee35eb344837009697e2c9d4e4662` (962960 B, BuildID `a93cfd09…`) |

Delta honesty:

- **No 2.9 product land this wave.** `c_emit_print.oo` / `c_emit_ident.oo`
  / `c_emit_lower.oo` are HEAD-clean. Stmt `println` still token-walks
  and calls `c_secret_refuse_println_ident` (L26–28 of print).
- Guard WORK **228** vs HEAD **200**. Diff is concurrent **2.10**
  `__sec_sink__` a0/a1/a2 + args-scan branch. `println`/`eprintln` were
  **already** in the walker list at HEAD. Not a 2.9 unify.
- Secret WORK **264** vs HEAD **259**. Net +5 is 2.10 `__sec_sink__`
  scan; parse-env rewrite is **7.10** `str_slice` (line-count neutral
  inside `c_parse_secret_env`). Not 2.9.
- **Hard ban holds** for entire `ooda/oodac/*.oo` product set
  (count_over_600 = 0).
- Out-of-OWN: **12** `std/src/ooda/**` files still over 600 (crypto
  stubs + slam/credit/gates). **Not** 2.9. Path-A oodac floor is the lock.
  PM R3 "0 files > 600" is the **oodac** lock, not std catalog.

Live stmt rail (hygiene re-read, `c_emit_println`):

```
if sk == "IDENT" {
    let sn: String = field_at(list_get(toks, sp), 3);
    if c_env_get(env, "__sec__" + sn) == "1" {
        c_secret_refuse_println_ident(env, sn);
    }
}
```

Live free-call (hygiene re-read, `c_emit_lower`):

```
c_secret_emit_guard(env, t, a0, a1, a2g, args);
```

Static: `c_secret_emit_guard(` **absent** in `c_emit_print.oo`. That
absence **is** SPRINT 2.9. Hygiene does not fill it.


*(Continued in `openOODA/audit/s29_hygiene_part2.oot`)*

end of s29_hygiene.oot (part 1)
