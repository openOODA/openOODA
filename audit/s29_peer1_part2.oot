# s29_peer1_part2.oot — (Part 2 of s29_peer1.oot)
# Continuation of `openOODA/audit/s29_peer1.oot`.

---

Host: `OODAC=/home/jeryd/Projects/openOODA/ooda/oodac/oodac`
Probes: `/tmp/s29_peer1/probes/*.oo` (ephemeral; **not** landed)
Re-prove cwd: `/tmp/s29_peer1/clean` (no `.ooda-cache` poison).

Expect refuse = rc≠0 + `ERR\tsecret` on **emit-c and check**.
Expect no-FP = rc=0 and **no** `ERR\tsecret` on both.

### Assigned red-team (gate)
| # | Probe | Shape | Expect | emit-c | check | Verdict |
|---|-------|-------|--------|--------|-------|---------|
| P01 | `P01_println_secret.oo` | `// SECRET: tok` + `println(tok)` | refuse | rc=1 `println sink refuses SECRET tok` | rc=1 same | **PASS** |
| P02 | `P02_println_string_fp.oo` | `println("tok")` | no FP | rc=0 none | rc=0 `OK` | **PASS** |
| P02b | `P02b_println_secret_word_fp.oo` | `println("secret")` + `// SECRET: secret` | no FP | rc=0 none | rc=0 `OK` | **PASS** |
| P03 | `P03_multi_arg_second.oo` | `println(n, tok)` | refuse | rc=1 SECRET `tok` | rc=1 | **PASS** |
| P03b | `P03b_multi_arg_first.oo` | `println(tok, n)` | refuse | rc=1 SECRET `tok` | rc=1 | **PASS** |
| P03c | `P03c_multi_arg_lit_then_secret.oo` | `println("pub", tok)` | refuse | rc=1 SECRET `tok` | rc=1 | **PASS** |
| P03d | `P03d_multi_arg_concat.oo` | `println("literal " + tok)` | refuse | rc=1 SECRET `tok` | rc=1 | **PASS** |
| P04 | `P04_alias_println.oo` | `let f = println; f(tok)` | refuse | rc=1 SECRET `tok` | rc=1 | **PASS** |
| P04b | `P04b_alias_println_multi.oo` | `let f = println; f(1, tok)` | refuse | rc=1 SECRET `tok` | rc=1 | **PASS** |
| R1 | `scripts/secret_t1_return_smoke.oo` | T1 return / alias / if | exit 0 | — | — | **PASS** |
| R2 | `scripts/secret_t2_depth_smoke.oo` | T2 field / method / args | exit 0 | — | — | **PASS** |
| R3 | `scripts/s28_macro_secret_smoke.oo` | 2.8 residual-doc | exit 0 | — | — | **PASS** |
| R4 | `scripts/s61_env_set_secretsink_smoke.oo` | 6.1 stay green | exit 0 | emit 4/4 OK | check 3 type-first | **emit PASS**; official **exit 1** (not 2.9) |
| R5 | `scripts/s71_guard_scan_smoke.oo` | 7.1 ident scan | exit 0 | — | — | **PASS** |

**Assigned behavioral gate: 13/13 PASS** (s61 counted as emit-c refuse
still live; official script exit 1 is the pre-existing check dual-path).

**2.9 unify gate: FAIL** (no product call site).

### Official fixtures / harness (bonus, still green)
| # | Probe | Shape | emit-c | Verdict |
|---|-------|-------|--------|---------|
| F1 | `fixtures/secret_sink_fail.oo` | `println(x)` | rc=1 SECRET `x` | **PASS** |
| F2 | `fixtures/secret_t2_string_lit_arg_fp.oo` | string lit | rc=0 | **PASS** |
| F3 | `fixtures/secret_multi_arg_println_fail.oo` | `println(n, s)` | rc=1 SECRET `s` | **PASS** |
| F4 | `fixtures/secret_t1_alias_println.oo` | `let f = println; f(secret_tok)` | rc=1 SECRET `secret_tok` | **PASS** |
| H1 | `scripts/s29_println_guard_smoke.oo` | println + eprintln + string | exit 0 | **PASS** (refuse/no-FP; **not** helper) |
| P05 | `P05_eprintln.oo` | `eprintln(tok)` | rc=1 `eprintln sink refuses SECRET tok` dual | **PASS** (this path **does** hit the guard via lower) |
| P06 | `P06_unused_secret_clean_print.oo` | tag `other`, print `"no secret"` | rc=0 dual | **PASS** no-FP |

### Adversarial miss that is **not** a 2.9 FN
| # | Probe | What happened | Classification |
|---|-------|---------------|----------------|
| P04c | `let f = println; println("ok")` (no `f(...)`) | check rc=0, no `ERR\tsecret`. emit-c **rc=134** `malloc(): unaligned tcache chunk detected` after preamble. **No SECRET print.** | Host emit heap abort on bare sink-alias bind. **Not** a SECRET FN. Not assigned gate. Separate OWN. |
| s61 smoke | official script exit 1 | check `undefined variable 'env_set'` (host type-first). emit-c still `ERR\tsecret`. WT `check_drive.oo` reorder not in ELF. | Pre-existing 6.1 check dual-path (`s61_peer1.oot`). **Not** a 2.9 regression. |
| DE2.9 table | claimed L175–186 → `c_secret_emit_guard` | source is still the inline walker | False close. This REJECT. |

---
COMMAND (repro)
---

```
OODAC=/home/jeryd/Projects/openOODA/ooda/oodac/oodac
cd /tmp/s29_peer1/clean
$OODAC check /tmp/s29_peer1/probes/P01_println_secret.oo
# rc=1  ERR<tab>secret<tab>println sink refuses SECRET tok
$OODAC emit-c /tmp/s29_peer1/probes/P01_println_secret.oo
# rc=1  same ERR secret
$OODAC check /tmp/s29_peer1/probes/P02b_println_secret_word_fp.oo
# rc=0  OK   (no ERR secret)
$OODAC emit-c /tmp/s29_peer1/probes/P03_multi_arg_second.oo
# rc=1  ERR<tab>secret<tab>println sink refuses SECRET tok
$OODAC emit-c /tmp/s29_peer1/probes/P04_alias_println.oo
# rc=1  ERR<tab>secret<tab>println sink refuses SECRET tok
$OODAC emit-c /home/jeryd/Projects/openOODA/ooda/fixtures/secret_t1_alias_println.oo
# rc=1  ERR<tab>secret<tab>println sink refuses SECRET secret_tok
cd /home/jeryd/Projects/openOODA/ooda
OODAC=./oodac/oodac bash scripts/secret_t1_return_smoke.oo          # exit 0
OODAC=./oodac/oodac bash scripts/secret_t2_depth_smoke.oo           # exit 0
OODAC=./oodac/oodac bash scripts/s28_macro_secret_smoke.oo          # exit 0
OODAC=./oodac/oodac bash scripts/s71_guard_scan_smoke.oo            # exit 0
OODAC=./oodac/oodac bash scripts/s29_println_guard_smoke.oo         # exit 0
OODAC=./oodac/oodac bash scripts/s61_env_set_secretsink_smoke.oo    # exit 1 host check type-first
# unify prove (must stay empty until a land):
grep -n c_secret_emit_guard oodac/c_emit_print.oo oodac/c_emit_secret.oo
# (no matches)
```

## FP / FN NOTES
### False positives (secret refuse when should allow)
| Probe | Result |
|-------|--------|
| `println("tok")` with `// SECRET: tok` | **no secret FP** |
| `println("secret")` with `// SECRET: secret` | **no secret FP** |
| unused SECRET + `println("no secret")` | **no secret FP** |
| official `secret_t2_string_lit_arg_fp` | **no secret FP** |

**Assigned residual FP count: 0.**

### False negatives (SECRET reaches println)
| Probe | Result |
|-------|--------|
| `println(tok)` | **refused** dual-path |
| `println(n, tok)` / `println(tok, n)` / `"pub", tok` / concat | **refused** |
| `let f = println; f(tok)` and `f(1, tok)` | **refused** |
| official sink / multi-arg / alias fixtures | **refused** |

**Assigned println-SECRET FN count: 0.**

Unify-not-called is **not** counted as a SECRET FN: token + check rails
still BLOCK the assigned shapes.

---
RESIDUALS (honest, out of assigned red-team)
---

1. **2.9 unify OPEN.** Stmt `println` does not call `c_secret_emit_guard`.
   Real land is `c_emit_println` after args are lowered (or drop the
   ident special-case so stmt println hits `c_emit_lower`). Check-path
   cannot call the guard without C arg text.
2. **DE2.9 table is false.** Do not treat SPRINT “What got fixed” as a
   close. Hygiene already said this.
3. **Stmt vs eprintln split.** `eprintln(SECRET)` hits the guard via
   lower. Stmt `println` does not. That is the ticket.
4. **Check walker LPAREN optional.** Accidental forward scan + true
   alias resolve both refuse P04. Unify should keep **true** alias
   (`resolve` then scan the call’s args), not the file-forward scan.
5. **`c_emit_println` arg refuse is `__sec__` only** (no alias resolve
   on the printed name). emit-c is saved by check-first. If check is
   ever skipped, alias-of-data at stmt println is weaker than the guard.
6. **s61 official smoke exit 1** on this host (check type-first). emit-c
   still refuse. WT reorder not in ELF. Not 2.9.
7. **P04c emit-c abort** (`malloc(): unaligned tcache chunk`) on
   `let f = println` without a call. Check OK. Host emit residual.
   Not a SECRET hole.
8. **`${}` interp** — 5.8. String text is correctly not IDENT.
9. **User-sink wrappers** — 2.10. Not this OWN.
10. Full IFC / `#[Secret]` / interproc beyond `__fr_secret__` — named
    in `SECRET_TAINT.oot`. Do not greenwash.

## PEER OWN
No product compiler edit. No fixture land. No smoke change.
Write: `openOODA/audit/s29_peer1.oot` only.
Solo under SWARM (single audit file; live host probes only).

## CLOSE TABLE
| Item | Status |
|------|--------|
| `println(SECRET)` refuse dual-path | **PASS** live |
| `println("secret")` / `println("tok")` no FP | **PASS** live |
| multi-arg (2nd / 1st / lit+ident / concat) | **PASS** live |
| `let f = println; f(SECRET)` | **PASS** live |
| Official sink / multi / alias fixtures | **PASS** |
| `s29_println_guard_smoke` | **PASS** exit 0 (refuse/no-FP, not helper) |
| T1 smoke | **PASS** exit 0 |
| T2 smoke | **PASS** exit 0 |
| s28 smoke | **PASS** exit 0 |
| s71 smoke | **PASS** exit 0 |
| s61 emit-c refuse | **PASS** (official smoke check dual-path OPEN) |
| stmt println → `c_secret_emit_guard` | **FAIL** (no call site) |
| DE2.9 claimed land | **FALSE** |
| Assigned behavioral matrix | **13/13 PASS** |
| Path-A close 2.9 | **REJECT** |
| Unify / DE2.9 / s61 check / P04c abort / `${}` / 2.10 | **OPEN residual** |

end of s29_peer1.oot

end of s29_peer1_part2.oot
