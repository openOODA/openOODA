# s29_product.oot — product host still has 2.9 grouped SECRET refuse. No push. No SPRINT. No ELF overwrite.
# Date: 2026-08-12  Host: ooda/oodac/oodac 963080 B @ 10:04 md5 35505fc520e997a9421089f11bdffd92 (untouched)
# Prove: cd ooda && timeout 30 bash scripts/s29_println_guard_smoke.oo → rc=0  ALL OK (default OODAC_BIN)
# Rails: FAIL-closed bare + grouped_later println((1),tok) + groupplus (("x")+tok) + eprintln; no-FP string-lit + grouped public
# Source 2.9 live: oodac/c_emit_print.oo L19–40  `let mut d = 0` + LPAREN/RPAREN depth; refuse only if d<=0 break + IDENT `__sec__`
# Contrast: /tmp/oodac_f26 (967784 B md5 8c315422…) missed live 2.9 c_emit_print — s29 FAIL there is isolated-ELF only
# VERDICT: PASS. 2.9 still on product. NOT a regression. F26 isolated ELF is the one that lacked 2.9.
# Residual (named, not FAIL): walker→c_secret_emit_guard unify; full IFC / #[Secret] / ${}. Product ELF not rebuilt. No push.
