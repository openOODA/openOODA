# s29b_integration.oot — Compiler Integration Tester (SPRINT 2.9 wave-2)
# Date: 2026-08-12T19:31:53Z
# Worker: Grok Build (Compiler Integration Tester — 2.9 wave-2)
# Cwd: /home/jeryd/Projects/openOODA/ooda
# Scope: re-prove live tip oodac secret rails after 2.9 wave-1 phalanx.
#        Assigned battery only: s29_println_guard_smoke + T1 + T2 + s28 + s71.
# Policy: no push; no seed_pure_multi; timeout 90s each; no product rewrite.
## Verdict (honest)
| Layer | Result | Exit | Notes |
|-------|--------|------|-------|
| **`s29_println_guard_smoke.oo`** (2.9 own) | **PASS** | **0** | dual-path println IDENT refuse + string-lit no-FP + eprintln IDENT refuse |
| **`secret_t1_return_smoke.oo`** (T1 regression) | **PASS** | **0** | return / alias / if-taint refuse+allow + CI wire |
| **`secret_t2_depth_smoke.oo`** (T2 regression) | **PASS** | **0** | field/method/args/dlopen/sys_exec argv/comment-skip + CI wire |
| **`s28_macro_secret_smoke.oo`** (2.8 regression) | **PASS** | **0** | residual-refuse + invented-syntax fail-closed + first-pass SECRET + N/A residual doc |
| **`s71_guard_scan_smoke.oo`** (7.1 regression) | **PASS** | **0** | println IDENT refuse + write_file payload refuse + string-lit no-FP |
| **2.9 walker → `c_secret_emit_guard`** | **OPEN** | — | stmt `println` still `c_emit_println` + token IDENT walker; **not** `c_secret_emit_guard` |
| **Push** | **NOT DONE** | — | human-gated |

**2.9 path-A claim (wave-2):** **NOT CLOSED as helper unify.** Same as wave-1 (`audit/s29_integration.oot`). SPRINT 2.9 / DE2.9 wants the println walker at `c_emit_secret.oo` L173–200 to become `c_secret_emit_guard(env, id, ...)`. Live source is still the **token IDENT loop** calling `c_secret_refuse_sink_resolved`. Stmt `println` is special-cased in `c_emit_ident_stmt` → `c_emit_println` → `c_secret_refuse_println_ident` (zero `c_secret_emit_guard` hits in `c_emit_print.oo` / `c_emit_ident.oo` / `c_emit_secret.oo`). Dual-path IDENT refuse + string-lit no-FP **still GREEN** on this host. Dedicated harness **honestly claims refuse/no-FP only**, not the helper unify.

**Assigned script battery:** **GREEN** (5 of 5 listed). No SKIP. Wave-2 list does **not** include s61/s72/s710 (those were wave-1 extras; s61 was RED there on `check` type-first — **not re-run** this pass).

**Overall 2.9 wave-2 integration status: GREEN** (refuse/no-FP rails + T1/T2/s28/s71 floors) / **OPEN** (walker→guard unify)

Do **not** greenwash: stmt `println` does **not** call `c_secret_emit_guard`. Dual-path IDENT refuse is the **token walker** (`check_secret_println_sinks`) plus emit belt (`c_emit_println`) — **not fail-open**, but **not the requested unify**. Host ELF **07:49 was not rebuilt**. Full IFC / `#[Secret]` / `${}` not claimed. `ci_product.oo` wires T1/T2 only — **not** s28/s29/s71.

---

## TIP SHA / binary
```
ooda     git rev-parse --short HEAD → 52cc4b5
ooda     git log -1 --oneline       → 52cc4b5 fix(r2): multi-import typecheck path-A + digit-0 import names
openOODA git rev-parse --short HEAD → 81f56fc
parent   git rev-parse --short HEAD → eaa555d
date -u                             → 2026-08-12T19:31:53Z
```

| Item | Value |
|------|--------|
| Product tip | `ooda/oodac/oodac` **962960** bytes @ 2026-08-12 07:49:59 -0700 |
| md5 | `a44ee35eb344837009697e2c9d4e4662` |
| BuildID | `a93cfd09506ba87497379f66b6c7047dee02573d` |
| Side / older | `oodac/oodac` 962888 (07:43) — **not** used |
| Seed | `bootstrap/seed/oodac` present — pure multi **not** re-run this pass |
| Host used | `OODAC_BIN=/home/jeryd/Projects/openOODA/ooda/oodac/oodac` |
| Host `nm -S` | `c_emit_println` @ `0x4651b0` (0xa17); `c_secret_emit_guard` @ `0x461080` (0x1282); `c_guard_scan_idents` @ `0x45fb90`; `check_secret_println_sinks` @ `0x475c50`; `c_secret_refuse_println_ident` @ `0x45de80` |
| Working tree (ooda) | dirty: `c_emit_secret*.oo` + `c_emit_args.oo` + `check_drive.oo` + staged `bootstrap/MACRO_SECRET_PASS.oot`; untracked `MACRO_BODY_SECRET.oot`, `MACRO_SECRET_RESIDUAL.oot`, `scripts/s28_*`, `s29_*`, `s61_*`, `s71_*`, `s72_*`, `s710_*` |

Host ELF is the **same** 07:49 binary used by wave-1 s29 / s28 / s61 / s71 / s72 / s710 integration. Dirty hunks this wave are **not** a 2.9 walker rewrite: `c_emit_secret.oo` (219 L, mtime 12:28) is 7.10 `str_slice` + 2.10 `__sec_sink__`; `check_drive.oo` is 6.1 secret-before-type (source only — **not in this ELF**). `c_emit_print.oo` (87 L, 11:43) / `c_emit_ident.oo` (228 L, 03:39) **clean vs HEAD**.

Wave-1 note: `s29_integration.oot` counted `c_emit_secret.oo` as 264 L. Live file is **219 L** (helpers split; walker body **unchanged**). Not a 2.9 land.

---

## 0) DE / Harness landing observed (wave-2)
| Path | Role |
|------|------|
| `ooda/oodac/c_emit_secret.oo` (219) | `check_secret_println_sinks` L173–200 — **still token IDENT walker**, not `c_secret_emit_guard` |
| `ooda/oodac/c_emit_print.oo` (87) | `c_emit_println` L26–28 — `c_secret_refuse_println_ident` only |
| `ooda/oodac/c_emit_ident.oo` L129–131 | stmt `id == "println"` → `c_emit_println` (early return; no emit-guard) |
| `ooda/oodac/c_emit_secret_guard.oo` (228) | `c_secret_emit_guard` L223–225 lists println/eprintln/print_err/log for `c_guard_scan_idents` |
| `ooda/oodac/c_emit_lower.oo` L90 | free-call → `c_secret_emit_guard` (`eprintln` / `log` / other sinks) |
| `ooda/oodac/c_emit_call.oo` L82 | method → `c_secret_emit_guard` |
| `ooda/oodac/c_emit.oo` L86 | emit front-door: `check_secret_println_sinks` **before** lower |
| `ooda/oodac/check_drive.oo` L62 | WT: secret **before** typecheck (6.1). **Not in this 07:49 ELF.** |
| `ooda/oodac/llvm_emit.oo` L79 | LLVM front-door also calls `check_secret_println_sinks` |
| `openOODA/audit/s29_harness.oot` | harness pointer (wave-1) |
| `openOODA/audit/s29_integration.oot` | wave-1 integration (broader battery; same host; same OPEN unify) |
| `openOODA/audit/s29_domain_a.oot` | DE-A: ALREADY_FIXED dual refuse; **no product hunk** |
| `ooda/scripts/s29_println_guard_smoke.oo` (157) | assigned 2.9 smoke; ephemeral TMPDIR fixtures |
| `ooda/scripts/secret_{t1,t2}_*.oo` | assigned T1/T2 regression |
| `ooda/scripts/s28_macro_secret_smoke.oo` (224) | assigned 2.8 regression |
| `ooda/scripts/s71_guard_scan_smoke.oo` (123) | assigned 7.1 regression |

**No** `s29b_*` audits existed at prove time except this file. Wave-2 integration did **not** invent a new smoke. `grep c_secret_emit_guard` on `c_emit_print.oo` / `c_emit_ident.oo` / `c_emit_secret.oo` → **none**.

Source (tip, live) — DE2.9 site still token walk:

```
if sid == "println" || sid == "eprintln" {
    ...
    } else if tk == "IDENT" {
        let nm: String = field_at(list_get(toks, p), 3);
        c_secret_refuse_sink_resolved(env, sid, nm);
```

Stmt emit belt (`c_emit_print.oo`) still:

```
if c_env_get(env, "__sec__" + sn) == "1" {
    c_secret_refuse_println_ident(env, sn);
}
```

---


*(Continued in `openOODA/audit/s29b_integration_part2.oot`)*

end of s29b_integration.oot (part 1)
