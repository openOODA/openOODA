# s29b_integration_part2.oot — (Part 2 of s29b_integration.oot)
# Continuation of `openOODA/audit/s29b_integration.oot`.

## 1) Assigned smokes — exit codes
All runs: `cd ooda && OODAC_BIN=/home/jeryd/Projects/openOODA/ooda/oodac/oodac` ; `timeout 90s`. Logs under `/tmp/s29b_integration/`.

`bash -n` **OK** on all five scripts.

### 1a) s29_println_guard_smoke (2.9 own) — exit 0
`bash -n` **OK** (157 lines).

```
OK fail-closed emit-c: secret_tok_println_ident
OK fail-closed check: secret_tok_println_ident
OK no-FP emit-c: secret_tok_println_string_lit
OK no-FP check: secret_tok_println_string_lit
OK fail-closed emit-c: secret_tok_eprintln_ident
OK fail-closed check: secret_tok_eprintln_ident
s29_println_guard_smoke: ALL OK
RESIDUAL full IFC / #[Secret] attr / string-interp ${} deep taint — not claimed closed
RESIDUAL 2.9 walker→c_secret_emit_guard unify is Domain Expert product; this smoke proves refuse/no-FP
```

Log: `/tmp/s29b_integration/s29_println_guard_smoke.out`

Classification: dual-path IDENT refuse + string-lit no-FP **GREEN**. Smoke header and `s29_harness.oot` **explicitly do not claim** the helper unify. Matches wave-1 1h.

### 1b) secret_t1_return_smoke (T1) — exit 0
`bash -n` **OK** (81 lines).

```
OK secret_function_return_fail emit refuses (rc=1)
OK secret_function_return_pass emit OK
OK secret_alias_rebind_pass emit refuses (rc=1)
OK secret_alias_rebind_fail emit OK (sticky cleared)
OK secret_if_taint_fail emit refuses (rc=1)
OK secret_if_taint_pass emit OK
OK ci_product wires secret_t1_return_smoke
OK secret_t1_return_smoke
```

Log: `/tmp/s29b_integration/secret_t1_return_smoke.out`

No T1 regression under the 2.9 refuse rails.

### 1c) secret_t2_depth_smoke (T2) — exit 0
`bash -n` **OK** (96 lines).

```
OK secret_field_assign_fail emit refuses (rc=1)
OK secret_field_assign_pass emit OK
OK secret_method_return_fail emit refuses (rc=1)
OK secret_method_return_pass emit OK
OK secret_call_arg_fail emit refuses (rc=1)
OK secret_multi_arg_println_fail emit refuses (rc=1)
OK secret_multi_name_fail emit refuses (rc=1)
OK secret_multi_name_pass emit OK
OK secret_dlopen_arg_fail emit refuses (rc=1)
OK secret_dlopen_arg_pass emit OK
OK secret_sys_exec_argv_fail emit refuses (rc=1)
OK secret_sys_exec_argv_pass emit OK
OK secret_midline_safe emit OK
OK comment-skip fixture shape (no line-start SECRET directive)
OK ci_product wires secret_t2_depth_smoke
OK secret_t2_depth_smoke
```

Log: `/tmp/s29b_integration/secret_t2_depth_smoke.out`

Includes `secret_multi_arg_println_fail` — T2 still covers multi-arg println SECRET. No T2 regression.

### 1d) s28_macro_secret_smoke (2.8 regression) — exit 0
`bash -n` **OK** (224 lines).

```
OK probe: check refused macro_expand (rc=1)
OK probe: check refused ast_macro (rc=1)
OK probe: check refused macro_kw (rc=1)
OK SECRET floor refuse: secret_tok_println
OK residual-refuse check macro_expand: residual_macro_expand
OK residual-refuse check ast_macro: residual_ast_macro
OK fail-closed check: macro_kw_syntax
OK fail-closed emit-c: emit_macro_expand
OK fail-closed emit-c: emit_ast_macro
OK fail-closed emit-c: emit_macro_kw
OK N/A rail documented: MACRO_SECRET_RESIDUAL.oot
OK macros unsupported: residual N/A rail honest
OK static: residual names only in check_residual.oo
OK string-body not secret (residual only; no expand)
OK emit-llvm no-expand (named residual: rc=0 call @macro_expand)
s28_macro_secret_smoke: ALL OK
```

Log: `/tmp/s29b_integration/s28_macro_secret_smoke.out`

Not a 2.9 walker change. First-pass SECRET floor still refuses visible `println(tok)` (`SECRET floor refuse: secret_tok_println`). AST_MACROS residual deny still holds. No 2.8 reopen.

### 1e) s71_guard_scan_smoke (7.1 regression) — exit 0
`bash -n` **OK** (123 lines).

```
OK fail-closed: secret_tok_println_ident
OK fail-closed: secret_tok_write_file_payload
OK no-FP: secret_tok_println_string_lit
s71_guard_scan_smoke: ALL OK
RESIDUAL full IFC / #[Secret] attr / string-interp ${} deep taint — not claimed closed
RESIDUAL 7.10: c_parse_secret_env still char-by-char name concat (not this OWN)
RESIDUAL interproc SECRET beyond path-A __fr_secret__ (cross-file return taint)
```

Log: `/tmp/s29b_integration/s71_guard_scan_smoke.out`

Honesty: working-tree `c_parse_secret_env` is already `str_slice(src, start, k)` (no `name = name + c`). Host ELF was **not** rebuilt after that slice. Smoke residual banner is **stale** vs working-tree source; parse/refuse rails still hold either way. 7.1 ident-scan floor **GREEN**.

---

## 2) Exit-code scoreboard
| Script | Timeout | Exit | Color |
|--------|---------|------|-------|
| `ooda/scripts/s29_println_guard_smoke.oo` | 90s | **0** | GREEN |
| `ooda/scripts/secret_t1_return_smoke.oo` | 90s | **0** | GREEN |
| `ooda/scripts/secret_t2_depth_smoke.oo` | 90s | **0** | GREEN |
| `ooda/scripts/s28_macro_secret_smoke.oo` | 90s | **0** | GREEN |
| `ooda/scripts/s71_guard_scan_smoke.oo` | 90s | **0** | GREEN |

Assigned 5: **5 GREEN / 0 RED / 0 SKIP**.

Not in this wave-2 list (named only): `s61_env_set_secretsink_smoke.oo` (wave-1 RED — `check` type-first on this ELF), `s72_sys_exec_last_arg_smoke.oo`, `s710_parse_secret_env_smoke.oo`. Do **not** treat their absence as a 2.9 pass.

---

## 3) What GREEN is (and is not)
| Claim | Status |
|-------|--------|
| Dual-path `println(SECRET)` refuse | **YES** (s29 emit-c + check) |
| Dual-path `println("tok")` no-FP | **YES** |
| Dual-path `eprintln(SECRET)` refuse | **YES** (product names eprintln) |
| T1 return / alias / if taint | **YES** (exit 0) |
| T2 field/method/args/dlopen/sys_exec/comment-skip | **YES** (exit 0) |
| 2.8 residual-doc + AST_MACROS refuse + first-pass SECRET | **YES** (s28 exit 0) |
| 7.1 ident-scan regression | **YES** (s71 exit 0) |
| Dedicated `s29_*` smoke | **YES** — exit 0 |
| Stmt `println` calls `c_secret_emit_guard` | **NO** — `c_emit_println` + token walker |
| `check_secret_println_sinks` L173–200 is `c_secret_emit_guard(...)` | **NO** — still IDENT token loop |
| `c_secret_emit_guard` lists println and is reachable from free-call lower | **YES** (source; not exercised by this battery beyond eprintln front-door) |
| Front-door println/eprintln SECRET IDENT reaches emit-guard | **NO** — token walker exits first |
| Host ELF rebuilt for 2.9 / 7.10 / 6.1 | **NO** — same 07:49 962960 B |
| `ci_product.oo` wires s28/s29/s71 | **NO** — T1/T2 only (`ci_product.oo` L157–158) |
| Full IFC / `#[Secret]` / `${}` | **residual** |
| Wave-1 s61 `check` type-first RED | **not re-proved** this pass |

---

## 4) Delta vs wave-1 `s29_integration.oot`
| Item | Wave-1 (19:24Z) | Wave-2 (19:31Z) |
|------|-----------------|-----------------|
| Host ELF | 962960 / `a44ee35e…` / 07:49 | **same** |
| Tip SHA | ooda `52cc4b5` / openOODA `81f56fc` | **same** |
| s29 smoke | GREEN 0 | GREEN 0 |
| T1 / T2 | GREEN 0 | GREEN 0 |
| s28 / s71 | GREEN 0 | GREEN 0 |
| s61 / s72 / s710 | s61 RED; s72/s710 GREEN | **not assigned** |
| Walker → emit-guard | OPEN | **still OPEN** |
| Product hunk for 2.9 | none | **none** |

Wave-2 is a **re-prove**, not a new land. Refuse rails did not regress.

---

## 5) Follow-ups (not this worker)
1. Domain Expert: replace `c_emit_secret.oo` L173–200 token walker with a real `c_secret_emit_guard` call **or** stop special-casing stmt `println` so it lowers like `eprintln` (`c_emit_expr_c` → emit-guard). Do **not** claim 2.9 closed until that land is in **source and host ELF**.
2. Optional belt: `c_emit_println` should call `c_secret_refuse_sink_resolved` (or emit-guard on joined arg text) so the emit belt matches alias resolve. Front-door currently carries alias.
3. Refresh s71 residual banner: working-tree `c_parse_secret_env` is `str_slice`; host ELF still 07:49.
4. Wire s29 (and s28/s71) into `ci_product.oo` if product wants those rails in CI. Harness scripts are **untracked** until human commit.
5. s61 dual-path `check` vs this ELF remains a **named wave-1 residual**. Rebuild from dirty `check_drive.oo` is **not** a 2.9 unify.

---

## Evidence paths
| Item | Path |
|------|------|
| This audit | `/home/jeryd/Projects/openOODA/openOODA/audit/s29b_integration.oot` |
| Wave-1 integration | `/home/jeryd/Projects/openOODA/openOODA/audit/s29_integration.oot` |
| Harness pointer | `/home/jeryd/Projects/openOODA/openOODA/audit/s29_harness.oot` |
| Harness smoke | `/home/jeryd/Projects/openOODA/ooda/scripts/s29_println_guard_smoke.oo` |
| DE2.9 walker | `/home/jeryd/Projects/openOODA/ooda/oodac/c_emit_secret.oo` L173–200 |
| Stmt println belt | `/home/jeryd/Projects/openOODA/ooda/oodac/c_emit_print.oo` |
| Ident stmt dispatch | `/home/jeryd/Projects/openOODA/ooda/oodac/c_emit_ident.oo` L129–131 |
| Emit guard | `/home/jeryd/Projects/openOODA/ooda/oodac/c_emit_secret_guard.oo` |
| Free-call site | `/home/jeryd/Projects/openOODA/ooda/oodac/c_emit_lower.oo` L90 |
| Check drive | `/home/jeryd/Projects/openOODA/ooda/oodac/check_drive.oo` |
| Smoke logs | `/tmp/s29b_integration/*.out` + `*.rc` |
| Sprint row | `openOODA/SPRINT.oot` ID **2.9** MED |
| Prior 2.8 integration | `openOODA/audit/s28_integration.oot` |
| Prior 7.1 integration | `openOODA/audit/s71_integration.oot` |
| DE-A dual-refuse | `openOODA/audit/s29_domain_a.oot` |

end of s29b_integration.oot

end of s29b_integration_part2.oot
