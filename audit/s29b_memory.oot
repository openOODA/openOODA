# s29b_memory.oot — Memory Auditor / 2.9 wave-2 depth walk
# Date: 2026-08-12T19:36:50Z
# Role: Memory Auditor — prove-not-leak; **no new concat in the depth walk**
# Tip: openOODA `81f56fc` / ooda `52cc4b5`
# Binary: `/home/jeryd/Projects/openOODA/ooda/oodac/oodac` (962960 B, mtime 2026-08-12 07:49, md5 `a44ee35eb344837009697e2c9d4e4662`)
# Source (wave-2):
#   `c_emit_secret.oo` (9142 B, 219 L, 12:28, md5 `e8d9ff2dbe66940b8d4c7dbce2c7443b`) — check + `c_secret_scan_call` Int `depth`
#   `c_emit_print.oo` (3339 B, 97 L, 12:35, md5 `4a993a5519c13b08a400c088810468e1`) — stmt Int `d` (landed mid-audit)
# Adjacent dirty (not 2.9b): `c_emit_secret_guard.oo` (9779 B, 228 L, 11:46) — **2.10** `__sec_sink__` only
# Wave-1 pointer: `audit/s29_memory.oot` (no-unify concat bar; walker was first-`RPAREN`)
# OWN: audit only (no product code changes this pass)
## Scope
Prove or reject SPRINT **2.9 wave-2** memory floor:

> Hunt: **no new concat in the depth walk**

Wave-2 product hunks (uncommitted vs `52cc4b5`):

1. `check_secret_println_sinks` println/eprintln token walk — Int paren `depth` (12:28).
2. Sibling `c_secret_scan_call` — same Int `depth` (same file, same hunk).
3. `c_emit_println` per-arg IDENT scan — Int `d` so inner `RPAREN`/`COMMA` no longer stop the scan (12:35, mid this audit).

That is **not** `c_secret_emit_guard` unify (still OPEN). That is **not** a joined `args` string.

Hunts:

| # | Hunt | Expected if 2.9b memory-closed |
|---|------|--------------------------------|
| H1 | New per-char / per-arg `OoStr` concat added for the depth walk | **absent** — `depth`/`d` are `Int`; tokens stay `field_at` |
| H2 | Grouped / nested walk heap vs wave-1 sibling class | same RSS class as bare println / eprintln; no extra join buffer |
| H3 | Mechanism honesty (Int depth vs new extract / fake `args`) | reuse token walk + `c_secret_scan_call` depth pattern; **not** a third builder |

Out of scope (named residual only): helper unify (`s29_memory.oot` H3 / I5/I9 still **FORBIDDEN**), host-ELF lag (depth **not** in 07:49), 7.1/7.2/7.10 walkers (PASS `s71`/`s72`/`s710_memory.oot`), 6.1 `env_set` (`s61_memory.oot`), **2.10** `__sec_sink__` slice, 2.8 residual-doc (`s28_memory.oot`), `c_args_strip_cr` (8.6), T1/T2 taint completeness, `#[Secret]` / `${}` (5.8), runtime HITL 7.4–7.8, check-cache false-OK (s71 R9).

ID collision: none. This file is SPRINT **2.9 wave-2** = paren-depth on the existing token walks. Wave-1 `s29_memory.oot` stays the no-unify / no-join bar.

## Verdict — path-A memory floor
| Result | Meaning |
|--------|---------|
| **PASS** | Wave-2 added **Int `depth` / Int `d` + index `+ 1`**. Zero `name = name + c`. Zero `args = args + …`. Zero new `str_slice` in any of the three depth walks. IDENT text is still `field_at(..., 3)` (token already paid). `c_secret_scan_call` and stmt `c_emit_println` reuse the same Int pattern. No third extract. No fake `args`. |

**Not claimed:** host ELF contains the walks (it does **not** — 07:49). Dual-path grouped refuse on this binary (check **FN**; `s29_println_guard_smoke` **exit 1**). Helper unify. O(n) lex time (`char_at` still Θ(n²)).

| Slice | Result |
|-------|--------|
| H1 no new concat in the depth walk | **PASS** (WT source; every new `+` is Int) |
| H2 grouped / nest heap vs sibling | **PASS class** on this ELF (stmt rescan / eprintln guard); **new-walk RSS not re-proved** |
| H2 long-ident / nest **time** | **RESIDUAL** Θ(n²) `char_at` / lex (not 2.9b-new) |
| H3 mechanism | **PASS** — Int depth on existing token walks; no third extract |
| Functional rails (`s29_println_guard_smoke` wave-2) | **HONEST FAIL** on this ELF (`check` accepts `(1), tok`) |
| Product unify (stmt println → emit_guard) | **OPEN** (not a memory FAIL) |
| Stmt `c_emit_println` depth | **PASS source** (Int `d` at L19–32; not in ELF) |

---

## H1 — No new concat (the 2.9b close bar)
### What wave-2 actually landed
| Site | Form | Alloc? |
|------|------|--------|
| `c_emit_secret.oo` L179–190 | `let mut depth = 1` then `depth ± 1` / `p + 1` on LPAREN/RPAREN | **none** (Int) |
| `c_emit_secret.oo` L191–195 | IDENT → `field_at(..., 3)` + existing `c_secret_refuse_sink_resolved` | token field (pre-existing) |
| `c_emit_secret.oo` L97–129 | `c_secret_scan_call` same Int depth (COMMA only at `depth == 1`) | **none** (Int) |
| `c_emit_print.oo` L19–32 | `let mut d = 0`; LPAREN `d+1`; RPAREN/COMMA/SEMI break only if `d <= 0` | **none** (Int) |
| `c_emit_print.oo` L34–38 | IDENT → `"__sec__" + sn` ( **existing** env key) | once-per-ident (not new) |
| `scripts/s29_println_guard_smoke.oo` | + grouped rail `println((1), tok)` | none (script) |
| `audit/s29b_*.oot` | phalanx evidence | none (doc) |

Check-walk diff vs `52cc4b5` (only the depth hunk; if-chain collapse / 7.10 / 2.10 sit in the same file):

```
                 let mut in_args = true;
+                let mut depth = 1;
                 while in_args && p < n {
-                    if field_at(list_get(toks, p), 0) == "RPAREN" {
-                        in_args = false;
-                    } else if field_at(list_get(toks, p), 0) == "IDENT" {
+                    let tk = field_at(list_get(toks, p), 0);
+                    if tk == "LPAREN" { depth = depth + 1; p = p + 1; }
+                    else if tk == "RPAREN" {
+                        depth = depth - 1;
+                        if depth <= 0 { in_args = false; }
+                        p = p + 1;
+                    } else if tk == "IDENT" {
```

Stmt-walk diff vs `52cc4b5` (mid-audit land):

```
         let mut sp = p;
+        let mut d = 0;
         while sp < n {
             let sk: String = field_at(list_get(toks, sp), 0);
-            if sk == "COMMA" || sk == "RPAREN" || sk == "SEMI" {
-                break;
+            if sk == "LPAREN" { d = d + 1; }
+            else if sk == "RPAREN" {
+                if d <= 0 { break; }
+                d = d - 1;
+            } else if sk == "COMMA" || sk == "SEMI" {
+                if d <= 0 { break; }
             }
```

Forbidden patterns **absent** from all three depth walks (`check_secret_println_sinks` L173–200, `c_secret_scan_call` L89–130, `c_emit_println` L19–41):

| Pattern | In 2.9b depth walk? |
|---------|---------------------|
| `name = name + c` | **no** |
| `out = out + c` / `out + char_at` | **no** |
| `let mut name = ""` then accumulate | **no** |
| `args = args + ", " + a` / `joined = joined +` | **no** |
| new `oo_str_concat` | **no** |
| new `str_slice` inside the IDENT walks | **no** (`str_slice` at secret L53 is **7.10**; L210 is **2.10** prefix) |
| check / stmt walker calling `c_secret_emit_guard` | **no** (must stay no from check — wrong signature; I9) |

Every **new** `+` is **Int** cursor / depth (`p + 1`, `sp + 1`, `depth + 1`, `d + 1`, `argi + 1`).

Existing (not new, not 2.9b-owned) string `+`:

| Site | Expression | Class |
|------|------------|-------|
| `c_emit_println` L36 | `"__sec__" + sn` | once-per-ident **env key** (existing) |
| `c_emit_println` L77–83 | `"  oo_print_*(" + arg + ");"` | once-per-arg **emit text** (product lower) |
| `c_secret_refuse_println_ident` | `"ERR\tsecret\tprintln …" + name` | once-per-refuse diagnostic |
| `c_secret_refuse_sink_ident` | `"ERR\tsecret\t" + sink + "…"` | once-per-refuse |
| `c_emit_call_args` L40 | `args = args + ", " + a` | **adjacent join** (eprintln / free-call only) |
| `c_args_strip_cr` | `out = out + c` | **8.6** (not 2.9b) |

`field_at` (`token_fmt.oo` L75–114) still `char_at` + **one `str_slice` per field**. That cost is the **token table**, paid by every walker since before 2.9. Wave-2 does not rebuild the ident; it only **continues** the index after a nested `)`.

**H1: PASS.** The depth walk added **no** concat (check + scan_call + stmt).

---


*(Continued in `openOODA/audit/s29b_memory_part2.oot`)*

end of s29b_memory.oot (part 1)
