# s29c_hygiene.oot — Hygiene Enforcer (2.9 wave-3 H-later / H-same)
# Date: 2026-08-12T19:46:29Z
# Role: Dependency/Hygiene Enforcer — RULES §1.7 (2.9 wave-3 phalanx)
# Tree: /home/jeryd/Projects/openOODA  (ooda/ + openOODA/)
# Tip (ooda): 52cc4b5 + WIP (print depth + secret depth/7.10/2.10;
#              `scripts/s29_println_guard_smoke.oo` ?? 194L)
# Tip (openOODA): 81f56fc + WIP `audit/s29c_*.oot`; SPRINT 2.9 still OPEN
# OWN: line-lock 2.9 wave-3 touched `.oo`; bash -n smoke; no ELF/scratch
#      in `oodac/`; **no ELF commit**; consistent `audit/s29c_*.oot`;
#      no credentials
# WRITE: this audit + hygiene-only scrub of gdb scratch
# POLICY: RULES §1.7 / PROBE MAX_LINES = 256 (soft); pack watch 300;
#         hard ban >600. No mass product peels; force-split only if DE blew 600
#
# ID: SPRINT backlog **2.9** wave-3 = ship / prove the wave-2 depth
#     hunk (H-later + H-same dual-path refuse). Wave-1 unify
#     (stmt println walker → full `c_secret_emit_guard`) remains OPEN.
# Wave-1 ledger: `audit/s29_hygiene.oot` (19:23Z).
# Wave-2 ledger: `audit/s29b_hygiene.oot` (19:36Z). Do not overwrite them.
## SUMMARY
| Check | VERDICT | Notes |
|-------|---------|-------|
| Hard ban >600 (touched .oo) | **PASS** | 0 OWN / 0 `ooda/oodac/**/*.oo` over 600 |
| Soft lock ≤256 (2.9 OWN) | **PASS** | print **97** / ident **228** / secret **231** / guard **228** / lower **241** |
| Pack pressure ≤300 (OWN) | **PASS** | all 2.9 OWN << 300 |
| Adjacent secret family soft | **WARN** residual | prop **262**, `main.oo` **307**, `check_cap_util.oo` **357**, `lex.oo` **277**, `load_import.oo` **287** — pre-existing / concurrent; **not** wave-3 blow |
| DE monofile blow >600 | **none** | print still 97 (same md5 as s29b); secret **231** (+12 vs s29b 219) still <<600 |
| ELF extra / scratch in `oodac/` | **PASS** | no bak/x/fuzz/toys; only product host |
| **No ELF commit** | **PASS** | 0 ELF staged in ooda / openOODA; `git add -n oodac/` is `.oo` only |
| Product host ELF | **kept** (not committed) | gitignored `oodac/oodac` **962960** B — **stale vs WT** (04:49 -1000 / 14:49Z) |
| Super tracked ELF | **residual** | `oodac/oodac` already in super index — **not** a wave-3 add; do not `git rm` |
| Seed ELF pin | **kept** | tracked `bootstrap/seed/oodac` (ignore-listed; already in index) |
| gdb scratch | **PASS** (scrubbed) | deleted `ooda/gdb_output.txt` + `ooda/gdb_script` |
| Credentials | **PASS** | ZERO live secrets/tokens/keys |
| `/tmp` in product `.oo` | **PASS** | ZERO in 2.9 OWN |
| Smoke `/tmp` | **PASS** | `$TMPDIR/s29_println_guard_$$` + `trap` |
| `bash -n` s29 smoke | **PASS** | `scripts/s29_println_guard_smoke.oo` **194** L syntax clean |
| Smoke sludge | **PASS** | wave-2 duplicate `cat` of `println_grouped_later.oo` **gone** |
| Host vs source | **WARN** residual | WT has paren depth; ELF does not. H-later check FN; H-same emit-c leak |
| `audit/s29c_*.oot` names | **PASS** | consistent `s29c_<role>.oot`; no rogue `issue_2_9_*` |
| Mass deletes of product | **none** | only untracked gdb scratch |
| Push | **no** | charter |
| Unify `c_secret_emit_guard` | **OPEN** | still absent from print/ident/secret |

OVERALL: **PASS**
  (hard monofile green; no extra ELF; **no ELF commit**; gdb scratch
   scrubbed; names consistent; no credentials; wave-3 OWN ≤256; unify
   still OPEN; host ELF stale named residual — not a hygiene fail)

## SCOPE + METHOD
2.9 wave-3 ACTIVE: machine-code close of the wave-2 depth hunk.
A SECRET IDENT after an inner `RPAREN` still inside `println` args
must dual-path refuse (`check` + `emit-c`) on **H-later**
`println((1), tok)` and **H-same** `println(("x") + tok)`.
Wave-1 unify remains OPEN. Wave-2 source depth remains **unshipped**.

Live dual path (hygiene re-read; **not** a product edit):

| Path | File | What it does now (WT) |
|------|------|------------------------|
| stmt `println` | `c_emit_ident.oo` → `c_emit_print.oo` | IDENT scan **with paren depth `d`** + `c_secret_refuse_println_ident` (`__sec__` only). **No** `c_secret_emit_guard` |
| check / LLVM / emit-c front-door | `c_emit_secret.oo` L185–212 | token IDENT walk **with `depth`** + `c_secret_refuse_sink_resolved`. **No** `c_secret_emit_guard` |
| `c_secret_scan_call` | `c_emit_secret.oo` L101–142 | LPAREN-required + `depth`; COMMA at `depth==1`. Used by other sinks, **not** stmt/check println |
| free-call lower | `c_emit_lower.oo` L90 | **already** `c_secret_emit_guard(...)` |
| method | `c_emit_call.oo` L82 | same guard (2.2) |

OWN units this pass:

| Path | Role in 2.9 wave-3 | Git |
|------|--------------------|-----|
| `oodac/c_emit_print.oo` | stmt IDENT scan + depth `d` | **dirty** (HEAD 87→**97**; same as s29b) |
| `oodac/c_emit_ident.oo` | `id == "println"` dispatch | **clean** (HEAD 228L) |
| `oodac/c_emit_lower.oo` | free-call already calls guard | **clean** (HEAD 241L) |
| `oodac/c_emit_secret.oo` | check walker + depth; 7.10/2.10 | dirty (HEAD 259→**231**) |
| `oodac/c_emit_secret_guard.oo` | `c_secret_emit_guard` + walker | dirty (**2.10** `__sec_sink__` only) |
| `scripts/s29_println_guard_smoke.oo` | one smoke; H-later + H-same rails | `??` untracked (**194** L) |
| `audit/s29c_*.oot` | wave-3 phalanx evidence | this family |

Adjacent observe (line-lock only if DE had blown 600):

| Path | Lines | Note |
|------|------:|------|
| `oodac/c_emit_secret_prop.oo` | 262 | T1/T2 prop (pre-existing soft) |
| `oodac/c_emit_secret_alias.oo` | 157 | alias resolve |
| `oodac/c_emit_args.oo` | 240 | 7.2 last-arg (already sliced) |
| `oodac/c_emit_call.oo` | 141 | method guard (2.2) |
| `oodac/main.oo` | 307 | pre-existing soft/pack residual |
| `oodac/check_cap_util.oo` | 357 | pre-existing pack residual |
| `oodac/load_import.oo` | 287 | R2 peel; **not** 2.9 |
| `oodac/lex.oo` | 277 | concurrent (was 267 in s29b); **not** 2.9 |
| `bootstrap/SECRET_TAINT.oot` | 129 | path-A println/eprintln **In**; unify not named |

Hygiene does **not** run the official smoke (harness/integration own
exit-0). Bar here is `bash -n` + tree locks + one isolated honesty
probe of the wave-3 shapes on the **shipping** ELF.

Commands:

  `wc -l` + hard/pack/soft gates on OWN + full `oodac/*.oo` >600
  repo-wide `.oo` >600 inventory (honesty: 12 `std/src` catalog stubs)
  `git add -n oodac/` — must not list ELF
  `bash -n` s29 smoke + related secret smokes
  `grep` credential + `/tmp` on OWN + smoke + s29c audits
  isolated `println((1), tok)` + `println(("x") + tok)` on product host
  delete extra ELF + gdb scratch; **keep** product `oodac/oodac`
  **do not** `git add` / commit any ELF
  no push; no product logic rewrite; no SPRINT flip; no rebuild

Thresholds:

| Threshold | Meaning |
|-----------|---------|
| **256** | RULES §1.7 / PROBE MAX_LINES — soft lock |
| **300** | Pack monofile pressure (flag for DE peel) |
| **600** | Path-A hard ban — force-split only if blown |

---
LINE LOCK — 2.9 wave-3 touched / adjacent (live)
---

Measured under monorepo tip ooda=`52cc4b5`+WIP (2026-08-12T19:46:29Z).

| File | Lines | ≤256 | ≤300 | ≤600 | HEAD dirty? | Split? |
|------|------:|:----:|:----:|:----:|:-----------:|:------:|
| `oodac/c_emit_print.oo` | **97** | PASS | PASS | PASS | **yes** (+10 depth vs HEAD) | NO |
| `oodac/c_emit_ident.oo` | **228** | PASS | PASS | PASS | no | NO |
| `oodac/c_emit_lower.oo` | **241** | PASS | PASS | PASS | no | NO |
| `oodac/c_emit_secret_guard.oo` | **228** | PASS | PASS | PASS | 2.10 `__sec_sink__` | NO |
| `oodac/c_emit_secret.oo` | **231** | PASS | PASS | PASS | 7.10 + 2.10 + depth | NO |
| `oodac/c_emit_secret_prop.oo` | **262** | OVER | PASS | PASS | no | residual |
| `oodac/c_emit_secret_alias.oo` | **157** | PASS | PASS | PASS | no | NO |
| `oodac/c_emit_call.oo` | **141** | PASS | PASS | PASS | no | NO |
| `oodac/c_emit_args.oo` | **240** | PASS | PASS | PASS | 7.2 comments | NO |
| `oodac/main.oo` | **307** | OVER | OVER | PASS | no | residual |
| `oodac/check_cap_util.oo` | **357** | OVER | OVER | PASS | no | residual |
| `oodac/load_import.oo` | **287** | OVER | PASS | PASS | R2; not 2.9 | residual |
| `oodac/lex.oo` | **277** | OVER | PASS | PASS | concurrent; not 2.9 | residual |
| `scripts/s29_println_guard_smoke.oo` | **194** | PASS | PASS | PASS | NEW (H-later + H-same) | n/a |
| `audit/s29c_harness.oot` | **64** | PASS | PASS | PASS | NEW | n/a |
| Aggregate | Value |
|-----------|------:|
| Touched `.oo` over **600** | **0** |
| `oodac/**/*.oo` over **600** | **0** (159 product files) |
| 2.9 OWN over **256** | **0** (print/ident/secret/guard/lower) |
| Adjacent over **256** | **5** (prop 262 / lex 277 / load 287 / main 307 / check_cap 357) |
| Out-of-OWN `std/src/**/*.oo` over **600** | **12** (catalog stubs — not 2.9) |

oodac pack-watch (≥256, all product — honesty, not 2.9 OWN):

| File | Lines |
|------|------:|
| `bc_emit_expr.oo` | 256 |
| `c_emit_secret_prop.oo` | 262 |
| `c_emit_let_ext.oo` | 263 |
| `lex.oo` | 277 |
| `load_import.oo` | 287 |
| `main.oo` | 307 |
| `c_emit_skip.oo` | 329 |
| `check_cap_util.oo` | 357 |

md5 (stable re-read at measure):

| File | md5 |
|------|-----|
| `c_emit_print.oo` | `4a993a5519c13b08a400c088810468e1` |
| `c_emit_ident.oo` | `ce738529e56a16c06d636e5817d95064` |
| `c_emit_lower.oo` | `16d3971c99cdbd06fe41f74958288dd8` |
| `c_emit_secret.oo` | `b96bfc0ead0543120c71e34a6d9f372b` |
| `c_emit_secret_guard.oo` | `ef9029995324d31aeb26748afe21e75b` |
| `s29_println_guard_smoke.oo` | `2ad0ee9592709771015e8fc55298a81e` |
| `s29c_harness.oot` | `085e8bbd45b7b8941f2b4b693c2bb27e` |
| product `oodac/oodac` | `a44ee35eb344837009697e2c9d4e4662` (962960 B, BuildID `a93cfd09…`) |

Delta vs wave-2 `s29b_hygiene.oot` (19:36Z):

- **print 97, same md5.** No further stmt land this wave.
- **secret 219 → 231** (+12). Still depth + 7.10 `str_slice` + 2.10
  `__sec_sink__` + sink-list compress. **Still** inline check walker
  (I11), **not** `c_secret_scan_call` for println (I1 unfinished).
  231 << 256. Not a monofile blow.
- **smoke 206 → 194.** Duplicate `cat` / second `expect_refuse` on
  `println_grouped_later.oo` **removed**. H-later + H-same + public
  group still present once each. Sludge WARN from s29b **cleared**.
- **Hard ban holds** for entire `ooda/oodac/*.oo` (count_over_600 = 0).
- Out-of-OWN: **12** `std/src/ooda/**` still over 600. **Not** 2.9.
  PM R3 "0 files > 600" is the **oodac** lock, not std catalog.

Catalog stubs still over 600 (honesty; out of OWN):

| File | Lines |
|------|------:|
| `std/src/ooda/sec/crypto/sha3.oo` | 652 |
| `std/src/ooda/robot/slam.oo` | 660 |
| `std/src/ooda/sec/crypto/double_ratchet.oo` | 677 |
| `std/src/ooda/sec/crypto/dilithium.oo` | 684 |
| `std/src/ooda/sec/crypto/groth16.oo` | 709 |
| `std/src/ooda/fin/credit.oo` | 714 |
| `std/src/ooda/sec/crypto/ed25519.oo` | 723 |
| `std/src/ooda/quantum/gates.oo` | 831 |
| `std/src/ooda/sec/crypto/kmac.oo` | 852 |
| `std/src/ooda/sec/crypto/zkstark.oo` | 854 |
| `std/src/ooda/sec/crypto/tpm.oo` | 931 |
| `std/src/ooda/sec/crypto/tss.oo` | 964 |

Static: `c_secret_emit_guard(` **absent** in `c_emit_print.oo`,
`c_emit_ident.oo`, `c_emit_secret.oo`. That absence **is** SPRINT 2.9
unify. Hygiene does not fill it. Zero `args = args +` in `c_emit_println`.

## SPLIT GATE
Rule: force-split only if OWN unit >600 (path-A hard ban).

| Unit | Over hard? | Action |
|------|:----------:|--------|
| `c_emit_print.oo` 97 | no | **none** |
| `c_emit_ident.oo` 228 | no | **none** |
| `c_emit_lower.oo` 241 | no | **none** |
| `c_emit_secret_guard.oo` 228 | no | **none** |
| `c_emit_secret.oo` 231 | no | **none** |
| adjacent prop 262 / lex 277 / load 287 / main 307 / check_cap 357 | no hard | residual — not 2.9 OWN |

Suggested DE (when authorized, not this hygiene pass):

1. Do **not** grow `c_emit_secret.oo` to call `c_secret_emit_guard`
   (wrong signature: C texts, not toks). Unify, if ever, stays in
   `c_emit_print.oo` (97 → still <<256) or drop the stmt special-case.
2. Keep 2.9 fixtures under `$TMPDIR` via smoke — never `oodac/a.oo`.
3. Rebuild host ELF **before** claiming wave-3 dual-path. Hygiene
   does **not** rebuild.
4. Optional: switch check println loop to `c_secret_scan_call` (I1)
   instead of a second inline depth walker. Line-neutral or smaller.


*(Continued in `openOODA/audit/s29c_hygiene_part2.oot`)*

end of s29c_hygiene.oot (part 1)
