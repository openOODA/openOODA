# s29c_hygiene_part2.oot — (Part 2 of s29c_hygiene.oot)
# Continuation of `openOODA/audit/s29c_hygiene.oot`.

---
HOST vs SOURCE (independent probe)
---

Product host is the **same** 04:49 / 07:49 ELF as wave-1 / wave-2.
WT print/secret depth is **not** in that ELF. `s29c_runtime.oot`
disasm: check walker LPAREN ×1 (optional skip only); scan_call 4
token eqs (no loop LPAREN). **FAIL host / PASS source.**

Isolated hygiene probe (ephemeral `$TMPDIR/s29c_hyg_*`, trap-cleaned):

```
// SECRET: tok
println((1), tok);          // H-later
println(("x") + tok);       // H-same (harness exact shape)
println(tok);               // wave-1 floor
```

| Door | Live host | Notes |
|------|-----------|-------|
| H-later `emit-c` | rc=1 `ERR secret` after `oo_print_int((1LL));` | stmt belt (per-arg), not check walker |
| H-later `check` | rc=0 `OK` | **FN** — ELF walker still first-`RPAREN` stop |
| H-same `emit-c` | rc=0 **leak** `oo_print_str(oo_str_concat((oo_str_lit("x")), tok))` | stmt inner scan stops at grouping `)` |
| H-same `check` | rc=0 `OK` | **FN** |
| bare `emit-c` / `check` | rc=1 `ERR secret` both | wave-1 floor **holds** |

`s29c_harness.oot` live-prove table matches this. `s29c_peer1.oot`
**REJECT** (3 of 4 MUST miss) is consistent. Hygiene does **not**
treat WT depth as shipped. Peer1 extra-wrap `#2` vs harness exact
`("x") + tok` is a **shape-doc residual**, not a hygiene fail.

## BINARY + SCRATCH SCRUB + NO ELF COMMIT
Charter: **no ELF / scratch in `oodac/`**. **No ELF commit.**
Extra copies + lab toys deleted. Product host kept (gitignored;
harness `OODAC_BIN` default). Super tracked copy **not** `git rm`.

### Extra ELF (this pass — already clean)
| Artifact | Size | Pre | Post |
|----------|-----:|-----|------|
| `oodac/oodac.x` / bak / fuzz | — | absent | **absent** |
| `ooda/oodac/oodac` | 962960 B ELF | product host | **kept** (gitignored; **not** staged) |
| `ooda/bin/ooda` | 111624 B ELF | product CLI | **kept** |
| `ooda/bootstrap/seed/oodac` | 878392 B ELF | seed pin (already tracked) | **kept** (no new blob) |
| super `oodac/oodac` | 962888 B ELF | **already tracked** in super-repo | **kept** (do not `git rm`; not a wave-3 add) |

Tree-wide ELF inventory (skip `.git`): **exactly these 4**.

BuildIDs:

| Binary | BuildID (sha1) | git |
|--------|----------------|-----|
| `ooda/oodac/oodac` | `a93cfd09506ba87497379f66b6c7047dee02573d` | **ignored** (`/oodac/oodac`) |
| super `oodac/oodac` | `81ce36d07631fd1f8706832d63cb970d76795b46` | **already in super index** (residual) |
| `ooda/bin/ooda` | `630f7a734cd5e9c36c61d7add50a4e221bffa4fb` | ignored `/bin/` |
| `ooda/bootstrap/seed/oodac` | `7dcbc1551ff1c4fa61099a0fcd72469bf1efa63f` | tracked pin + ignore-listed |

Host ELF is **stale vs seed** and **stale vs WT depth**. Hygiene does
**not** rebuild.

### No ELF commit (gate)
| Check | Result |
|-------|--------|
| `git -C ooda diff --cached` ELF | **none** (only `MACRO_SECRET_PASS.oot` staged — 2.8, not ELF) |
| `git -C openOODA diff --cached` ELF | **none** (empty) |
| super `git diff --cached` ELF | **none** |
| `git -C ooda add -n oodac/` | product `.oo` only — **no** `oodac/oodac` |
| Hygiene `git add` of any ELF | **not done** |
| Hygiene commit / push | **not done** |

`git add -n oodac/` after scrub: product `.oo` only
(`ast_emit_outline`, `c_emit_args`, `c_emit_print`, `c_emit_secret`,
`c_emit_secret_guard`, `check_drive`, `lex`, `load_import`,
`parse_stmt_dump`, `parse_stmt_nav`, `tc_names`) — no ELF, no scratch
toys. Mix of 2.9 wave-2/3 (print/secret) + concurrent 2.10 / 7.10 /
6.1 / R2 / ebnf. Not all of that is 2.9 OWN.

### Scratch toys (scrubbed this pass)
- `ooda/gdb_output.txt` (364 B, debuginfod noise) — **deleted**
- `ooda/gdb_script` (44 B, `b tok_line` toy) — **deleted**

Neither was tracked. No new `.gitignore` line (one-off; not recurring
oodac debris). `ooda/oodac_new.tmp.c` **absent** this measure (s29b
had to delete it twice).

Product tree (`ooda/oodac/`): **no** `a.oo` / `b.oo` / `*.orig` /
`*.rej` / `test_slice.oo` / `*.tmp.c` / extra `oodac.*`.

Not deleted (out of 2.9 oodac charter): historical super-repo
`tmp_*.oo`, `scratch/a.oo`, tracked corpus `oodac/test_*.oo`,
ignored super `test.c` / `test.c.tmp.c` / `abs_out.c` /
`check_caps_out.c`. Concurrent `/tmp/s29c_*` probe dirs belong to
sibling agents — **leave**.

`.gitignore`: no new lines. 7.2 `/oodac/*.orig` `*.rej` `test_slice.oo`
already in ooda WT. Product `/oodac/oodac` already ignored.

---
bash -n + SECRETS + /tmp
---

```
OK bash -n scripts/s29_println_guard_smoke.oo          (194 lines)
OK bash -n scripts/secret_eprintln_smoke.oo            (47 lines)
OK bash -n scripts/secret_sink_enforce_smoke.oo        (264 lines)
OK bash -n scripts/secret_taint_residual_smoke.oo      (81 lines)
OK bash -n scripts/secret_t1_return_smoke.oo           (81 lines)
OK bash -n scripts/secret_t2_depth_smoke.oo            (96 lines)
OK bash -n scripts/secret_product_floor_smoke.oo       (49 lines)
OK bash -n scripts/s28_macro_secret_smoke.oo           (224 lines)
OK bash -n scripts/s71_guard_scan_smoke.oo             (123 lines)
ABSENT scripts/s29c_println_guard_smoke.oo             (wave-3 reuses s29 smoke)
```

Smoke uses `$TMPDIR/s29_println_guard_$$` + `trap 'rm -rf "$FIX"' EXIT`
— fixtures never land in-repo. **No** hard-coded `/tmp`. Default
TMPDIR fallback is `$HOME/.cache/ooda-tmp`.

`ci_product.oo` does **not** list `s29_println_guard_smoke.oo`
(named residual; not a hygiene fail).

Credential-class scan (`password=`, `BEGIN *PRIVATE`, AWS/GitHub/Slack
tokens, Bearer, `sk-`) on:

  - `c_emit_print.oo`
  - `c_emit_ident.oo`
  - `c_emit_secret.oo`
  - `c_emit_secret_guard.oo`
  - `c_emit_lower.oo`
  - `c_emit_call.oo`
  - `s29_println_guard_smoke.oo`
  - `s29c_harness.oot`
  - this file

  → **ZERO live credentials.**

Dummy fixture string `"s3cret"` in smoke heredoc = theater payload, not
a host secret.

`/tmp` in 2.9 OWN `.oo` + s29 smoke: **none**.
`s29c_harness.oot` names `/tmp/s29c_harness_prove_*` only as a
prove-cwd note (not a product path).

VERDICT credentials: **PASS**

---
audit/s29c_*.oot NAME CONSISTENCY
---

Pattern: `s29c_<role>.oot` under `openOODA/audit/` (wave-3 sibling of
`s29_*.oot` / `s29b_*.oot`). Wave-1 / wave-2 ledgers **kept**.

Present at this measure (re-list after sibling land):

| File | Role | Lines | Status |
|------|------|------:|--------|
| `s29c_harness.oot` | Test Harness Writer | 64 | present (H-later + H-same; ELF FN named) |
| `s29c_peer1.oot` | Peer Verifier 1 | 230 | present (**REJECT** 3/4 MUST) |
| `s29c_runtime.oot` | Machine Code / Runtime | 383 | present (no new ELF; first-`)`) |
| `s29c_hygiene.oot` | this file | this | **written** |
| ideas / integration / peer2 / memory / security / domain / rollup | optional later: `s29c_<role>.oot` | — | not present this measure |

No rogue names (`s2.9c_*.oot`, `29c_*.oot`, `issue_2_9_*.oot`,
`s29_wave3_*.oot`, `s29c_wave*`) on disk.

Line pressure on 2.9 wave-3 artifacts (informational; ban is on
touched `.oo`):

| File | Lines |
|------|------:|
| `s29c_harness.oot` | 64 |
| `s29c_peer1.oot` | 230 |
| `s29c_runtime.oot` | 383 |
| `s29_println_guard_smoke.oo` | 194 |
| `s29c_hygiene.oot` | this |

Honesty: `s29b_harness.oot` still names only H-later as the wave-2
rail. Wave-3 pointer is `s29c_harness.oot` (adds exact H-same
`("x") + tok`). Hygiene does **not** rewrite wave-1 / wave-2
harness. SPRINT 2.9 row remains **OPEN** until Docs/Coordinator
flip after a **rebuilt** host + Peer1 ACCEPT. This wave's WT depth
is still **not** the shipping ELF.

SECRET_TAINT table lists println/eprintln path-A **In**. It does **not**
claim stmt-println == `c_secret_emit_guard`. Not a hygiene edit.

## ACTIONS / NON-ACTIONS THIS PASS
| Action | Result |
|--------|--------|
| Measure OWN monofile locks | **done** — hard 0 over 600 |
| Force-split DE >600 | **not needed** |
| Flag adjacent soft residual | prop 262 / lex 277 / load 287 / main 307 / check_cap 357 |
| Flag std/src catalog >600 | **12** files — out of OWN |
| Scrub extra ELF | **none present** |
| Scrub gdb scratch | **done** (`gdb_output.txt` + `gdb_script`) |
| Keep product `oodac/oodac` | **yes** (smoke host; stale named; **not** committed) |
| Stage / commit ELF | **no** |
| `bash -n` s29 smoke | **PASS** (194) |
| Isolated H-later / H-same probe | emit-c H-later refuse / H-same leak; check FN both |
| Product println / guard unify edits | **none** (unify still OPEN) |
| Flip SPRINT 2.9 | **no** (peers/coordinator; still OPEN) |
| Rebuild host | **no** |
| Push | **no** |
| Audit write | **this file** |

## RETURN — COMPACT
```
SCOPE   2.9 wave-3 H-later + H-same (unify still OPEN; host unshipped)
        ooda=52cc4b5+WIP  openOODA=81f56fc+WIP
OWN     c_emit_print.oo=97 DIRTY (same md5 as s29b; +10 paren depth; no emit_guard)
        c_emit_ident.oo=228 HEAD-clean (id==println dispatch)
        c_emit_lower.oo=241 HEAD-clean (already calls guard)
        c_emit_secret_guard.oo=228 (println already in walker; +28 is 2.10)
        c_emit_secret.oo=231 (s29b 219; +12; depth + 7.10 + 2.10; inline walk)
        smoke=194  harness=64 NEW
>600    0 touched / 0 oodac product   PASS hard ban
        12 std/src catalog stubs (not 2.9)
>300    0 OWN
>256    0 OWN print/ident/secret/guard/lower
        adjacent prop 262 / lex 277 / load 287 / main 307 / check_cap 357 WARN
BINARY  no extra ELF; product oodac KEPT (gitignored); super tracked residual
        tree ELF count = 4 (oodac host + super copy + seed + bin/ooda)
        host STALE vs WT depth (04:49 -1000)
NOCOMMIT 0 ELF staged; add -n oodac/ is .oo only; hygiene did not git add ELF
SCRUB   gdb_output.txt + gdb_script DELETED
CREDS   PASS  no live secrets; dummy "s3cret" is fixture only
/tmp    PASS product + smoke (TMPDIR+trap)
BASHN   s29_println_guard_smoke.oo OK (194)
        secret_eprintln / sink_enforce / taint / t1 / t2 / product_floor
        s28 / s71 OK
PROBE   H-later  emit-c ERR secret / check OK   (ELF FN)
        H-same   emit-c leak concat(tok) / check OK
        bare     dual ERR secret (wave-1 floor)
NAMES   s29c_harness / peer1 / runtime / hygiene
        (ideas / integration / peer2 / memory / security / domain / rollup
         not present this measure)
UNIFY   still OPEN — stmt println does not call c_secret_emit_guard
PUSH    no
OVERALL PASS
```

end of s29c_hygiene.oot

end of s29c_hygiene_part2.oot
