# s29c_phalanx_rollup.oot — 2.9 wave-3 RPAREN hole (12-agent phalanx)
# Date: 2026-08-12
# Role: Documentation Writer / Close rollup
# Decision: **OPEN** — **not FIXED**. Peer1 **REJECT**. Host **not rebuilt**.
# Policy: no push; no moonshots; do not flip SPRINT 2.9
# Wave-1: `audit/s29_*.oot` (bare dual refuse; unify OPEN)
# Wave-2: `audit/s29b_*.oot` (source depth; ELF first-`)`; Peer1 REJECT)
## Close criteria
| # | Criterion | Result |
|---|-----------|--------|
| 1 | Peer1 ACCEPT on rebuilt host (`check` **and** `emit-c` `ERR	secret`) | **FAIL** — `audit/s29c_peer1.oot` **REJECT** (3 of 4 MUST miss); ELF is **not** new |
| 2 | Dedicated smoke green (`s29_println_guard_smoke.oo`) | **FAIL** exit 1 (Docs Writer re-prove) |
| 3 | T1/T2 secret emit floors still green | **PASS** exit 0 |
| 4 | 2.8 / 7.1 regression | **PASS** exit 0 (`s28` + `s71`) |
| 5 | Host ELF implements paren depth | **FAIL** — same 04:49/-1000 ELF (`a44ee35e…`); WT depth **unshipped** |
| 6 | Rebuild product (or prove) host from WT | **FAIL** — `bootstrap/oodac_pure_build` **ERR_EMIT** `check_cap_util.oo` |
| 7 | Harness rails defined | **PASS** — `audit/s29c_harness.oot` + smoke (honest fail-closed) |
| 8 | Honesty residuals named (not greenwashed) | **YES** — WT ≠ ELF; unify OPEN; H-same leak |
| 9 | Push after green | **NO** (no close; no push) |

## Decision honesty
**SPRINT 2.9 stays OPEN.** Wave-3 does **not** close 2.9.

Standing order this turn: mark 2.9 FIXED **only** if Peer1
**ACCEPT**s on a **rebuilt** host. Peer1 did **not**. The shipping
binary is byte-identical to wave-1 / wave-2 (`962960` B, md5
`a44ee35eb344837009697e2c9d4e4662`, BuildID `a93cfd09…`). Docs Writer
independently re-proved the same four MUST probes. Dual-path
`ERR	secret` still fails on three grouping / nest shapes.

Wave-3 question (not wave-1 bare refuse; not DE2.9 helper unify):
ship the wave-2 **depth-aware IDENT walk** so a SECRET IDENT after an
inner `RPAREN` still inside `println` / `eprintln` args is refused on
**both** `oodac check` and `oodac emit-c`.

| Fact | Evidence |
|------|----------|
| Wave-1 floor | bare `println(tok)` / string no-FP / `eprintln(tok)` / `println(n, tok)` still **dual SECRET** |
| Live hole | `println((n), tok)` check **OK**; `println((("x")+tok))` emit-c **prints `tok`**; `eprintln(id(1), tok)` check **OK** |
| WT source | `c_emit_secret.oo` Int `depth` (check + `c_secret_scan_call`); `c_emit_print.oo` Int `d` (stmt scan) |
| Shipping ELF | `ooda/oodac/oodac` **962960** B @ 04:49:59 -1000 (= 07:49 -0700) — **HEAD first-`RPAREN` stop** |
| Rebuild this wave | `/tmp/s29c_rebuild` + integration: `ERR_EMIT check_cap_util.oo` **EXIT:1**. Product host **untouched** |
| Unify | stmt `println` still does **not** call `c_secret_emit_guard`. Wave-3 is **depth-in-ELF**, not that helper |
| DE2.9 table | still **false** — walker is a token walk, not `c_secret_emit_guard(env, id, …)` |

**Still not claimed (do not greenwash):**

| Residual | Status |
|----------|--------|
| Peer1 RPAREN ACCEPT | **REJECT** — gate for FIXED |
| Host rebuilt with WT depth | **no** — same `a44ee35e…` ELF |
| `s29_println_guard_smoke` | **exit 1** — check FN on `(1), tok`; emit-c FN on `("x")+tok` |
| `println((("x")+tok))` / `("x")+tok` emit-c leak | **live** (`oo_print_*` of concat(`tok`)) |
| LLVM / wasm / bc on hole shapes | **FN / DROP / leak / no walker** (named; not the Peer1 bar) |
| Helper unify (`c_secret_emit_guard` from stmt/check) | **OPEN** |
| Full IFC / `#[Secret]` / `${}` / 2.10 wrappers | residual |
| Formal `s29c_domain_*.oot` / peer2 / security / memory | mid-land at rollup time — **do not override Peer1** |

Path-A 2.9 wave-3 ≠ “bare dual refuse already holds.”
Path-A 2.9 wave-3 ≠ source-only `depth` with a stale host.
Path-A 2.9 wave-3 ≠ a failed `/tmp` rebuild.
**FIXED is forbidden until Peer1 ACCEPT on a rebuilt host.**

Working-tree `SPRINT.oot` dropped the numeric 2.9 row this day. HEAD
still lists `2.9` MED as **open**. Docs Writer **did not** mark FIXED.

## Prove (Documentation Writer host re-prove)
| Gate | Result |
|------|--------|
| Host | `ooda/oodac/oodac` **962960** B · md5 `a44ee35eb344837009697e2c9d4e4662` |
| BuildID | `a93cfd09506ba87497379f66b6c7047dee02573d` |
| ooda tip | `52cc4b5` `fix(r2): multi-import typecheck path-A + digit-0 import names` |
| Host mtime | 2026-08-12 04:49:59 -1000 — **same bytes** as wave-1 / wave-2 |
| WT check walker | `c_emit_secret.oo` **231** L md5 `b96bfc0e…` — `depth` at L109 + L191 (**unshipped**) |
| WT stmt belt | `c_emit_print.oo` **97** L md5 `4a993a55…` — `d` at L19–33 (**unshipped**; ELF is 87-L HEAD) |
| Guard call in print / secret | **none** |
| `scripts/s29_println_guard_smoke.oo` | exit **1** (194 L; md5 `2ad0ee95…`) |
| `scripts/secret_t1_return_smoke.oo` | exit **0** |
| `scripts/secret_t2_depth_smoke.oo` | exit **0** |
| `scripts/s28_macro_secret_smoke.oo` | exit **0** |
| `scripts/s71_guard_scan_smoke.oo` | exit **0** |

### Peer1 MUST rails (Docs Writer re-prove — `/tmp/s29c_docs_must`)
Expect refuse = rc≠0 + `ERR	secret` on **check and emit-c**.

| # | Shape | check | emit-c | Dual SECRET? |
|---|-------|-------|--------|--------------|
| 1 | `println((n), tok)` | rc=0 `OK` | rc=1 `ERR	secret` after `oo_print_int((n));` | **NO** |
| 2 | `println((("x")+tok))` | rc=1 `ERR	type` arity (no `ERR	secret`) | rc=0 **leak** `oo_print_*` of concat(`tok`) | **NO** |
| 3 | `eprintln(id(1), tok)` | rc=0 `OK` | rc=1 `ERR	secret` eprintln | **NO** |
| 4 | `println(n, tok)` | rc=1 `ERR	secret` | rc=1 same | **YES** |

Matches `audit/s29c_peer1.oot`. `ERR	type` is **not** a SECRET seal.
Peer1 also names alt `#2b` `println(("x")+tok)`: check `OK` + emit-c leak.

### Extra Docs Writer matrix (same host)
| Shape | check | emit-c |
|-------|-------|--------|
| `println(("x") + tok)` | `ERR	type` or `OK` (wrap) | **leak** `oo_print_str(oo_str_concat(…, tok))` |
| `println(id(1), tok)` | `OK` | emit-c SECRET (stmt later-arg) |
| `println(f(g(), tok))` | `OK` | rc=0 **leak** |
| `"x".len(), tok` | `OK` | rc=0 **DROP** (prints `"x"` only) |
| `eprintln((1), tok)` | `OK` | emit-c SECRET (guard belt) |
| `println(tok)` / `println("tok")` | dual SECRET / dual ALLOW | wave-1 floor |

### Smoke rails (current script)
| Kind | Case | Result on this ELF |
|------|------|--------------------|
| FAIL-closed | `println(tok)` | **OK** dual |
| NO-FP | `println("tok")` | **OK** dual |
| FAIL-closed | `eprintln(tok)` | **OK** dual |
| FAIL-closed H-later | `println((1), tok)` | emit-c **OK**; **check accepts** |
| FAIL-closed H-same | `println(("x") + tok)` | **emit-c accepted** (leak) |
| NO-FP | `println((1), 2)` unused SECRET | **OK** |

## 12-agent phalanx summary
Composition per `SWARM.oot` §6. Wave-3 owns **depth-in-ELF**. Formal
Domain / Peer2 / Security / Memory / Integration `.oot` still landing;
product host + Peer1 + Docs Writer re-prove hold the gate.

### I. Engineering Core (3)
| Agent | Evidence | Verdict |
|-------|----------|---------|
| Domain Expert A/B | WT depth already landed wave-2; no new ELF | **source only** — rebuild **not** in product host |
| Machine Code / Runtime | `audit/s29c_runtime.oot` | **FAIL host** — no new ELF; check walker first-`)` |

### II. Adversarial QA Wing (4)
| Agent | Evidence | Verdict |
|-------|----------|---------|
| Peer1 (MUST matrix) | `audit/s29c_peer1.oot` | **REJECT** — 3/4 miss dual `ERR	secret` |
| Peer2 (parity) | ephemeral `/tmp/s29c_peer2_stale_*` (`.oot` mid-land) | stmt `println` H-same still **leaks** vs eprintln **BLOCK** |
| Security Auditor | `/tmp/s29c_sec` (`.oot` mid-land) | Z2 still fail-open on this ELF (Peer1 + Docs matrix) |
| Memory Auditor | `/tmp/s29c_mem_host` in-flight `oodac build` | **not** a Peer1 ACCEPT; product host unchanged |

### III. Integration Wing (3)
| Agent | Evidence | Verdict |
|-------|----------|---------|
| Compiler Integration | `/tmp/s29c_integration/rebuild.out` | **ERR_EMIT** `check_cap_util.oo`; pre-rebuild smoke **exit 1** |
| Test Harness | `audit/s29c_harness.oot` + smoke | **PASS rails**; live prove **exit 1** (honest) |
| Hygiene Enforcer | `audit/s29c_hygiene.oot` | **PASS** monofile; **no ELF commit**; host stale named |

### IV. Command Wing (2)
| Agent | Evidence | Verdict |
|-------|----------|---------|
| Idea Fairy | `audit/s29c_ideas.oot` | I1+I2 **winner** = seed isolate + prove side ELF; **I11 doc-FIXED REJECT** |
| Documentation Writer | this rollup | **OPEN** — no SPRINT flip |

### Support / ephemeral
| Artifact | Role |
|----------|------|
| `/tmp/s29c_docs_must/` | Docs Writer Peer1 MUST re-prove |
| `/tmp/s29c_docs_floors/` | T1/T2/s28/s71 floor logs |
| `/tmp/s29c_peer1_1251061/` | Peer1 dual-repro |
| `/tmp/s29c_runtime_prove/` | ELF disasm + run matrix |
| `/tmp/s29c_rebuild/` | failed pure rebuild (`ERR_EMIT check_cap_util.oo`) |
| `/tmp/s29c_integration/` | integration rebuild + seed emit dumps |
| `/tmp/s29c_harness_prove_*` | harness live rails |
| `/tmp/s29c_mem_host/` | memory auditor scratch build (not product) |

## Peer / support audits (landed)
| Audit | Role | Verdict |
|-------|------|---------|
| `s29c_peer1.oot` | RPAREN MUST gate | **REJECT** |
| `s29c_runtime.oot` | ELF vs WT depth | **FAIL host — no new ELF** |
| `s29c_harness.oot` | H-later + H-same rails | **PASS rails**; ELF red |
| `s29c_hygiene.oot` | line-lock / no ELF commit | **PASS** |
| `s29c_ideas.oot` | rebuild-fail ship path | I1+I2 winner; I9/I11 **REJECT** |
| Wave-2 `s29b_peer1.oot` | same ELF 3/4 miss | still true |
| Wave-1 `s29_peer1.oot` | bare FN=0; unify REJECT | still true for **assigned** wave-1 shapes |

## Product land (this wave — not a close)
| Repo | Surface | What |
|------|---------|------|
| **ooda** | `oodac/c_emit_secret.oo` | WT paren `depth` (wave-2 hunk; **231** L). **Not** in ELF |
| **ooda** | `oodac/c_emit_print.oo` | WT Int `d` (wave-2; 87→97). **Not** in ELF |
| **ooda** | `scripts/s29_println_guard_smoke.oo` | H-later + H-same rails (194 L) |
| **openOODA** | `audit/s29c_*.oot` | wave-3 evidence |
| **openOODA** | `SPRINT.oot` | Docs Writer **did not** flip 2.9 FIXED |
| **ooda** | host `oodac/oodac` | **not rebuilt** (rebuild **failed**) |

No remotes. Depth in source is **not** product close.

## Cap map (N/A) / secret-scan map (wave-3 truth)
| Surface | This ELF | WT source |
|---------|----------|-----------|
| `check_secret_println_sinks` | first `RPAREN` stops | Int `depth` |
| `c_secret_scan_call` | first `RPAREN` returns | Int `depth`; COMMA at `depth==1` |
| `c_emit_println` IDENT scan | first COMMA/RPAREN/SEMI | Int `d`; break only at `d<=0` |
| `c_secret_emit_guard` | eprintln / free-call only | same — **not** stmt/check |
| LLVM / wasm refuse | check front-door only | same (need ELF rebuild) |
| `chs_rt` print | raw `fwrite` | not a SECRET seal |

Two FN subclasses (do not collapse):

| ID | Shape | ELF today |
|----|-------|-----------|
| **H-later** | `println((n), tok)` / `eprintln(id(1), tok)` | check/llvm/wasm FN; stmt emit-c BLOCK |
| **H-same** | `println(("x")+tok)` / `f(g(), tok)` | check FN or TYPE; stmt emit-c **leaks `tok`**; eprintln emit-c BLOCK (guard) |

## Residual (not a 2.9 FIXED)
| Item | Status |
|------|--------|
| Peer1 RPAREN ACCEPT | **open** — rebuild host + dual `ERR	secret` on all four MUST |
| WT depth in ELF | **open** — rebuild **failed** this wave (`ERR_EMIT check_cap_util.oo`) |
| H-same emit-c leak | **open** on this ELF (stmt depth unshipped) |
| LLVM / wasm / bc hole shapes | **open** (bc has no walker) |
| 2.9 unify → `c_secret_emit_guard` | **open** — not this wave’s machine |
| DE2.9 “walker calls emit_guard” | **false** — drop that sentence when 2.9 does close |
| `print` / `print_err` / `log` family | leftover (`s29_domain_b.oot`) |
| 6.1 check type-first | not 2.9 |
| Formal domain / peer2 / security / memory `.oot` | may land after this rollup — **does not override Peer1** |

## Next (not this writer)
1. Make a **new** product `ooda/oodac/oodac` from WT depth (check + stmt).
   Do **not** claim FIXED from source grep or a failed `/tmp` pure build.
2. Re-run Peer1 four MUST probes **on that ELF**. ACCEPT only if **both**
   `check` and `emit-c` print `ERR	secret` on all four. `ERR	type` is FAIL.
3. `s29_println_guard_smoke.oo` exit 0 on **that** ELF (virgin TMPDIR cwd).
4. Then — and only then — a later rollup may flip SPRINT 2.9. Wording:
   **depth-aware IDENT walk (RPAREN class) + prove**. Not “walker →
   `c_secret_emit_guard`.” Not wave-1 “dual refuse” alone.

## Reproduce
```bash
cd /home/jeryd/Projects/openOODA/ooda
export OODAC_BIN=./oodac/oodac
# static: WT has depth; ELF does not implement it until rebuild
grep -n 'depth\|let mut d' oodac/c_emit_secret.oo oodac/c_emit_print.oo
md5sum oodac/oodac
# live host (04:49 -1000) still FN on grouped later-arg:
#   check println((1), tok) → OK
#   emit-c println((("x")+tok)) → oo_print_* of tok
bash scripts/s29_println_guard_smoke.oo
# expect: FAILED (exit 1) until host matches WT and Peer1 ACCEPT
```

end of s29c_phalanx_rollup.oot
