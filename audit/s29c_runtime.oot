# s29c_runtime.oot — Machine Code / Runtime Auditor (SPRINT 2.9 wave-3)
# Date: 2026-08-12T19:44Z
# Role: Machine Code / Runtime — nm/objdump gate on the check walker
# Tree: /home/jeryd/Projects/openOODA/ooda (+ openOODA/audit)
# Tip (ooda): 52cc4b5 fix(r2): multi-import typecheck path-A + digit-0 import names
# Tip (openOODA): 81f56fc + WIP s29 / s29b / s29c audits
# Host: ooda/oodac/oodac  962960 B  mtime 2026-08-12 04:49:59 -1000
#   (= 07:49 PDT / 14:49Z — same instant as s29 / s29b runtime)
#   md5 a44ee35eb344837009697e2c9d4e4662
#   BuildID a93cfd09506ba87497379f66b6c7047dee02573d
#   file: ELF 64-bit LSB executable, x86-64, not stripped
# OWN: prove whether a **new** shipping ELF walks LPAREN with a
#      paren-**depth** loop inside `check_secret_println_sinks` /
#      `c_secret_scan_call`, or is still first-`RPAREN`-only.
#      Comment-only. No product rebuild. No push.
#      Tmp prove: `/tmp/s29c_runtime_prove`, `/tmp/s29c_smoke_tmp`.
#
# ID: SPRINT **2.9 wave-3** = machine-code close of the wave-2 depth
#     hunk. Not 2.8 (macro), not 2.10 (`__sec_sink__`), not 7.10
#     (`str_slice`), not DE2.9 “walker → `c_secret_emit_guard`”.
# Wave-1: `audit/s29_runtime.oot` (unify OPEN).
# Wave-2: `audit/s29b_runtime.oot` (source depth / host first-`)`).
## SUMMARY
| Claim | Expected if wave-3 shipped | Actual | VERDICT |
|-------|----------------------------|--------|---------|
| New ELF this wave | new size / md5 / BuildID / mtime after WT | **same** 962960 B / `a44ee35e…` / `a93cfd09…` / 04:49 -1000 | **FAIL** — **no new ELF** |
| Check walker LPAREN **loop** | LPAREN immediates **≥ 2** (skip **and** loop); depth local | `check_secret_println_sinks`: LPAREN **×1** @ `0x476608` (optional skip only). Loop @ `0x47666a` is RPAREN then IDENT. `jmp 0x47666a` — **never** LPAREN | **FAIL** (first-RPAREN) |
| `c_secret_scan_call` depth | **5** token `oo_str_eq` (LPAREN-require + LPAREN-loop + RPAREN + COMMA + IDENT) | **4** (`0x45e050` / `0x45e12e` / `0x45e190` / `0x45e1d8`). Equal-RPAREN → restore + `jmp oo_str_release` (return) | **FAIL** |
| Stmt belt LPAREN in IDENT scan | `d++` on loop LPAREN (`c_emit_print.oo` WT) | IDENT scan: COMMA / RPAREN / SEMI **break**; LPAREN only @ `0x465b2c` (post-args skip) | **FAIL** (flat) |
| WT source has depth | `let mut depth = 1` + LPAREN++ / RPAREN-- | `c_emit_secret.oo` L109 + L191; `c_emit_print.oo` L19 `d` | **PASS** (source) |
| Bare `println(tok)` still refuse | dual-path `ERR\tsecret` | check / emit-c / llvm rc=1 `SECRET tok` | **PASS** (wave-1 floor) |
| `println((1), tok)` dual refuse | check **and** emit-c `ERR\tsecret` | emit-c rc=1 after `oo_print_int((1LL));`; **check `OK`**; llvm rc=0 `@oo_print_int(i64 1)` + `c"s3cret"`; **`oodac run` prints `1`** | **FAIL** (host FN) |
| `println(("x") + tok)` refuse | dual `ERR\tsecret` | check `OK`; emit-c rc=0 **leaks** `oo_print_str(oo_str_concat((oo_str_lit("x")), tok))` | **FAIL** |
| Smoke green on this host | `s29_println_guard_smoke` exit 0 | exit **1**: grouped check accepted; groupplus emit-c accepted | **FAIL** (honest fail-closed) |
| Product symbols `T` not `U` | walkers defined | all listed `T`; **zero** `U c_secret*` | **PASS** (missing is a **rebuild**) |
| Stmt println calls `c_secret_emit_guard` | DE2.9 | **0** calls in WT `c_emit_secret.oo` / `c_emit_print.oo` / `c_emit_ident.oo`; ELF callers still `c_emit_primary_c` + `c_emit_method` | **FAIL** (unify OPEN) |
| `chs_rt` refuses SECRET | **no** | `oo_println` = newline; **zero** `__sec__` in `runtime/*` | **PASS** (not the home) |

**OVERALL: PASS source / host STALE / live FN** — wave-3 asked for a
**new ELF** whose check walker has a **LPAREN loop / depth**, not
first-`)`. **No such ELF exists.** Shipping machine code is still the
HEAD first-`RPAREN` stop that `s29b_runtime.oot` already named.

Do **not** greenwash:

- Same fingerprint as `s29_runtime.oot` / `s29b_runtime.oot` /
  `s710_runtime.oot`. `nm` / `objdump` of this file describe the
  **old** walker. There is **nothing new to bless**.
- WT `c_emit_secret.oo` (231 L, 09:39 -1000, md5 `b96bfc0e…`) **does**
  have `depth`. That is source, not text.
- WT `c_emit_print.oo` (97 L, 09:35 -1000, md5 `4a993a55…`) **does**
  have stmt `d`. ELF `c_emit_println` (2583 B) does **not**.
- DE2.9 unify is still **false** on HEAD, WT, and this ELF. Wave-3
  is **depth-in-ELF**, not unify.
- `s29_println_guard_smoke.oo` exit 0 on wave-1 rails does **not**
  prove depth. Current script (**194** L, md5 `2ad0ee95…`) **exit 1**.

## METHOD
1. Static: HEAD vs WT `ooda/oodac/c_emit_secret.oo` (`git diff`);
   `c_emit_print.oo` IDENT scan; `check_drive.oo` L62 (WT
   secret-before-type — **not** this ELF); `c_emit.oo` L86;
   `llvm_emit.oo` L79; `main.oo` `run` / `emit-bc` / `emit-wasm`.
2. Product host `ooda/oodac/oodac`: `nm -S`, `readelf -n` / `-l`,
   `strings`, file-off→VMA (`VMA − 0x400000`), `objdump -d` of
   `c_secret_scan_call` (`0x45e020`–`0x45e32f`),
   `check_secret_println_sinks` (`0x475c50`–`0x477114`),
   `c_emit_println` (`0x4651b0`–`0x465bc7`),
   `run_check_core` (`0x477120`–`0x4776dc`).
3. Live: clean cwd `/tmp/s29c_runtime_prove/clean` (no `.ooda-cache`);
   check / emit-c / emit-llvm / emit-wasm / emit-bc / run / tokens;
   official `scripts/s29_println_guard_smoke.oo` with `OODAC_BIN` =
   this ELF, `TMPDIR=/tmp/s29c_smoke_tmp`.
4. No `chs_rt` / emit logic edits. No host rebuild. Side binary
   `oodac/oodac` 962888 B @ 04:43 **not** used. Seed
   `bootstrap/seed/oodac` 878392 B @ 2026-08-11 **not** the proof host.

---
0. Host vs source honesty (the wave-3 runtime question)
---

| Surface | mtime / pin | What the check walker does |
|---------|-------------|----------------------------|
| HEAD `c_emit_secret.oo` | 52cc4b5 (259 L, md5 `86ad26c0…`) | **no** `depth`. After optional one `LPAREN`, `if RPAREN { in_args = false }` |
| WT `c_emit_secret.oo` | **2026-08-12 09:39 -1000** (231 L, md5 `b96bfc0e…`) | `let mut depth = 1`; loop `LPAREN` → `depth+1`; `RPAREN` → `depth-1`, stop only if `depth <= 0`. `c_secret_scan_call` COMMA only at `depth == 1` |
| HEAD `c_emit_print.oo` | 87 L | IDENT scan `break` on first `COMMA`/`RPAREN`/`SEMI` |
| WT `c_emit_print.oo` | **09:35 -1000** (97 L, md5 `4a993a55…`) | `let mut d = 0`; LPAREN `d+1`; RPAREN/COMMA/SEMI break only if `d <= 0` |
| Product `ooda/oodac/oodac` | **04:49 -1000** (predates WT by ~5 h) | disasm = **HEAD** walker (§1). **0** `__sec_sink__` strings |
| `scripts/s29_println_guard_smoke.oo` | **194** L, md5 `2ad0ee95…` | grouped + groupplus + public-group rails |
| Seed `bootstrap/seed/oodac` | 2026-08-11 / 878392 B | also pre-depth |

Source is **newer than the host**. Rebuild did **not** happen between
the depth land (`s29b`) and this audit. Wave-3’s gate cannot pass
without a new link.

A **new** ELF that closes wave-3 in machine code **must** show:

1. New `stat` / `md5` / BuildID (not `a44ee35e…` / `a93cfd09…`).
2. `check_secret_println_sinks` LPAREN immediates **≥ 2** (skip **and**
   loop). Today: **1**.
3. Loop body compares `LPAREN` (inc) **and** `RPAREN` (dec); RPAREN
   match is **not** `jne 0x475f30` / restore+`ret`.
4. `c_secret_scan_call` **5** token `oo_str_eq` (LPAREN-require +
   LPAREN-loop + RPAREN + COMMA + IDENT). Today: **4**.
5. Live: `oodac check` of `println((1), tok)` → `ERR\tsecret` rc=1;
   `oodac run` of the same must **not** print `1`.
6. `s29_println_guard_smoke.oo` exit 0 (grouped **and** groupplus).
7. (stmt belt, not the check-walker gate) `c_emit_println` IDENT scan
   contains a **loop** LPAREN. Today: **0**.

This auditor did **not** rebuild (OWN: document honesty, nm/objdump
**the ELF that exists**).

---
1. Q1 — Does a new ELF implement the LPAREN / depth walker?

*(Continued in `openOODA/audit/s29c_runtime_part2.oot`)*

end of s29c_runtime.oot (part 1)
