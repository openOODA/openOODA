# s29c_runtime_part2.oot — (Part 2 of s29c_runtime.oot)
# Continuation of `openOODA/audit/s29c_runtime.oot`.

---

**No. There is no new ELF. The shipping walker is first-`RPAREN`.**

### Product host (`nm -S` / `readelf`)
| Symbol | `nm -S` | Size |
|--------|---------|-----:|
| `check_secret_println_sinks` | `T` @ `0x475c50` | 5316 |
| `c_secret_scan_call` | `T` @ `0x45e020` | 783 |
| `c_emit_println` | `T` @ `0x4651b0` | 2583 |
| `c_emit_ident_stmt` | `T` @ `0x469290` | 5787 |
| `c_secret_emit_guard` | `T` @ `0x461080` | 4738 |
| `c_guard_scan_idents` | `T` @ `0x45fb90` | 5354 |
| `c_secret_refuse_println_ident` | `T` @ `0x45de80` | 175 |
| `c_secret_refuse_sink_ident` | `T` @ `0x45df30` | 237 |
| `c_secret_refuse_sink_resolved` | `T` @ `0x45fb30` | 89 |
| `c_secret_resolve_alias` | `T` @ `0x45c7a0` | 237 |
| `run_check_core` | `T` @ `0x477120` | 1468 |
| `c_parse_secret_env` | `T` @ `0x45d140` | 3378 |

Sizes **unchanged** vs `s29b_runtime.oot`. A depth land would grow
`check_secret_println_sinks` / `c_secret_scan_call` / `c_emit_println`.

`nm -u`: **no** `U c_secret*` / `U check_secret*` / `U c_emit_println`.
Missing is a **rebuild**, not a symbol.

Rodata (file-off = VMA − `0x400000`):

| VMA | C string |
|-----|----------|
| `0x4c43ed` | `LPAREN` |
| `0x4c43f4` | `RPAREN` |
| `0x4c43d0` | `COMMA` |
| `0x4c4321` | `IDENT` |
| `0x4c43cb` | `SEMI` |
| `0x4c4d11` | `println` |
| `0x4c4d10` | `eprintln` |

`strings` of the ELF: **0** `__sec_sink__`. WT source has **1**.

Callers (`objdump` xrefs):

| Callee | Unique callers |
|--------|----------------|
| `check_secret_println_sinks` | `c_emit_stream_mode`, `llvm_emit_stream`, `run_check_core` |
| `c_secret_scan_call` | `check_secret_println_sinks` |
| `c_emit_println` | `c_emit_ident_stmt` |
| `c_secret_refuse_println_ident` | `c_emit_println` (**1**) |
| `c_secret_emit_guard` | `c_emit_primary_c`, `c_emit_method` |

`run_check_core` still calls `check_secret_println_sinks` **after**
typecheck (`0x477505`). WT `check_drive.oo` L62 is secret-**before**
type (6.1) — **not in this ELF**.

### `check_secret_println_sinks` — LPAREN×1, first RPAREN exits
Token-kind counts inside the function: `LPAREN` **1**, `RPAREN` **1**,
`IDENT` **3**, `COMMA` **0**, `println` **1**, `eprintln` **1**.

Walker after `c_secret_resolve_alias` + sid `println`/`eprintln`:

```
0x476608  lit LPAREN
0x47660d  add  $2, %rbx          ; p = i+1, then maybe skip
0x47665f  oo_str_eq LPAREN
0x476666  cmove %rbp, %rbx       ; if NOT LPAREN, do not skip
0x47666a  loop head
0x476675  lit RPAREN
0x4766c8  oo_str_eq RPAREN
0x4766cf  jne  0x475f30          ; EQUAL → leave walker (next sink ifs)
0x4766d5  lit IDENT
0x47672c  oo_str_eq IDENT
0x476733  jne  0x476db0          ; EQUAL → resolve + refuse
0x476739  mov  %rbp, %rbx        ; else p+1
0x47673c  jmp  0x47666a          ; back to RPAREN — never LPAREN
```

`0x475f30` is `write_file` / `fetch` / … (`mov $0x4c5734` =
`write_file`). HEAD sequential `if sid ==`, not WT `else if` /
`__sec_sink__`.

A depth walker would load `LPAREN` **inside** `0x47666a…0x47673c`,
keep an integer local, and take `0x475f30` only when depth hits 0.
There is **no** such local and **no** loop `LPAREN`.

### `c_secret_scan_call` — 4 compares, first-RPAREN return
Immediates: `LPAREN` @ `0x45e050` (require, **before** the loop),
`RPAREN` @ `0x45e12e`, `COMMA` @ `0x45e190`, `IDENT` @ `0x45e1d8`.
**No** loop `LPAREN`.

```
0x45e14a  oo_str_eq  RPAREN
0x45e151  je   0x45e190     ; not equal → try COMMA
          … restore regs …
0x45e187  jmp  oo_str_release ; EQUAL → return (scan over)
0x45e1b3  add  $1, %r14      ; COMMA → argi++  (no depth==1 test)
```

WT would compare `LPAREN` again inside the loop and only stop `RPAREN`
when `depth <= 0`. This body cannot do that.

### `c_emit_println` — still flat (ELF ≠ WT)
Stmt belt **is** a wave-3 source hunk (`c_emit_print.oo` +10 L `d`).
ELF string compares: `LPAREN`×1 (`0x465b2c`, after the arg loop),
`RPAREN`×3, `COMMA`×2, `SEMI`×2 (`0x4c43cb`), `IDENT`×1.
IDENT scan @ `0x465278`:

```
0x4652af  lit COMMA   → jne 0x4653f8  ; break
0x4652d8  lit RPAREN  → jne 0x4653f8  ; break
0x4652fb  lit SEMI    → jne 0x4653f8  ; break
0x46531e  lit IDENT   → refuse __sec__
0x465260  add $1      ; next tok — never LPAREN
```

Consequence (same class as s29b, plus the new groupplus rail):

- `println((1), tok)` — first arg is `(1)` (no SECRET IDENT); after
  `c_emit_expr_c` the COMMA is seen; second arg `tok` refused. emit-c
  **BLOCK** without depth. Front-door still **FN**.
- `println(("x") + tok)` — IDENT scan stops at the grouping `RPAREN`;
  never sees `tok`; `c_emit_expr_c` then lowers the **whole** concat.
  emit-c **leaks**. Front-door **FN**.

---
2. Q2 — Live host (clean cwd; this ELF)

*(Continued in `openOODA/audit/s29c_runtime_part3.oot`)*

end of s29c_runtime_part2.oot (part 2)
