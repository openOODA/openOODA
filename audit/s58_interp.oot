# s58_interp.oot — 5.8 ${} lex residual honesty
# Date: 2026-08-12. OWN: this ledger only. No token_scan_string.oo. No SPRINT. No push.
VERDICT: STOP. Unimplemented fail-closed. No lex hole. No smallest refuse.
LEX: token_scan_string.oo 57 lines; "..." only; no $ arm / INTERP / splice (grep empty).
OUTSIDE: $ / ${tok} → check rc=1 Unexpected character '$' (lex_all + lex_step).
INSIDE: // SECRET: tok; println("${tok}") check+emit-c rc=0; emit oo_str_lit("${tok}").
CONTROL: println(tok) emit-c rc=1 ERR secret — IDENT refuse still live; skip is T2 no-FP.
WHY NO REFUSE: product .oo embeds shell ${} (cli_build.oo, main.oo, cli/product_sh.oo).
REOPEN: interp must land with ${IDENT} taint at sinks in the same change (not lex-only).
NAMED: SECRET_TAINT String-interp ${} deep taint residual; PM 3.5 later; DESIGN residual.
PROOF: host oodac 2026-08-12; s28_security Z4 I1 same ALLOW-literal; no crash.
DO NOT CLAIM: interp shipped, taint through ${}, 5.8 closed, template tokens.
