# s61_harness.oot — 6.1 env_set SECRETSINK path-A harness pointer
# Date: 2026-08-12
# Scope: SPRINT 6.1 smoke rails only (no Domain Expert product land)
## Harness
| Item | Path |
|------|------|
| Smoke | `ooda/scripts/s61_env_set_secretsink_smoke.oo` |
| Binary | `OODAC_BIN` or `ooda/oodac/oodac` |
| Fixtures | ephemeral under `$TMPDIR/s61_env_set_secretsink_$$` (not repo root) |
| Product | `ooda/oodac/c_emit_secret_guard.oo` (`env_set` / `setenv` SECRETSINK — do not rewrite) |

## Rails
| Kind | Case |
|------|------|
| FAIL-closed | `// SECRET: tok` then `env_set(envc, "KEY", tok)` (bare IDENT) |
| FAIL-closed | `// SECRET: tok` then `setenv(envc, "KEY", tok)` **if product** names `setenv` |
| PASS no-FP | `env_set(envc, "KEY", "tok")` string literal when `tok` is SECRET name (no `ERR secret`; unknown-call lower residual allowed) |

Exit 0 only if all rails hold.

## Run
```
cd /home/jeryd/Projects/openOODA/ooda
OODAC_BIN=./oodac/oodac bash scripts/s61_env_set_secretsink_smoke.oo
```

## Residual (documented, not a fail)
- **Full IFC / `#[Secret]` / interp `${}`:** path-A ident scan only; not DESIGN taint.
- **Product lower:** `env_set` may remain emit-name / link-fail (CAPS residual); harness proves refuse/no-FP, not runtime setenv.
- **7.10:** `c_parse_secret_env` still char-by-char name concat (separate OWN).

end of s61_harness.oot
