# s61_hygiene.oot — Hygiene Enforcer (6.1 env_set SECRETSINK phalanx)
# Date: 2026-08-12T19:03:30Z
# Role: Dependency/Hygiene Enforcer — RULES §1.7 (6.1 phalanx)
# Tree: /home/jeryd/Projects/openOODA  (ooda/ + openOODA/)
# Tip (ooda): 52cc4b5 + WIP (`c_emit_secret*.oo` 2.10 `__sec_sink__`;
#              `scripts/s61_env_set_secretsink_smoke.oo` ??)
# Tip (openOODA): 81f56fc + WIP s61_* audits; SPRINT 6.1 still OPEN (Docs)
# OWN: line-lock 6.1-touched `.oo`; bash -n `s61_env_set_secretsink_smoke.oo`;
#      no ELF / scratch in `oodac/`; consistent `audit/s61_*.oot`; no credentials
# WRITE: this audit + hygiene-only scrub of leftover emit dump
# POLICY: RULES §1.7 / PROBE MAX_LINES = 256 (soft); pack watch 300; hard ban >600
#         No mass product peels; force-split only if DE blew 600
#
# ID collision honesty: DESIGN / PM **6.1** = metamorphic vs deterministic
# (policy residual). This file is SPRINT backlog **6.1** =
# "Confirm env_set in SECRETSINK (partially landed with 2.3 — re-prove)".
# Do not mix the two.
## SUMMARY
| Check | VERDICT | Notes |
|-------|---------|-------|
| Hard ban >600 (touched .oo) | **PASS** | 0 OWN / 0 `ooda/oodac/**/*.oo` over 600 |
| Soft lock ≤256 (6.1 OWN) | **PASS** | `c_emit_secret_guard.oo` **228** |
| Pack pressure ≤300 (OWN) | **PASS** | 228 < 300 |
| Adjacent secret family soft | **WARN** residual | `c_emit_secret.oo` **264**, `c_emit_secret_prop.oo` **262**, `main.oo` **307**, `check_cap_util.oo` **357** — pre-existing / 2.10; **not** 6.1 blow |
| DE monofile blow >600 | **none** | `env_set` already in HEAD SECRETSINK; no 6.1 product land |
| ELF extra / scratch in `oodac/` | **PASS** | no bak/x/fuzz/toys; only product host |
| Product host ELF | **kept** | gitignored `oodac/oodac` **962960** B — required by smoke; not a DE toy |
| Super emit dump | **PASS** (scrubbed) | deleted `oodac_new.tmp.c` (reappeared mid-wave) |
| Credentials | **PASS** | ZERO live secrets/tokens/keys |
| `/tmp` in product `.oo` | **PASS** | ZERO in 6.1 OWN |
| Smoke `/tmp` | **PASS** | harness uses `$TMPDIR/s61_env_set_secretsink_$$` + `trap` |
| `bash -n` s61 smoke | **PASS** | `scripts/s61_env_set_secretsink_smoke.oo` 150L syntax clean |
| `audit/s61_*.oot` names | **PASS** | consistent `s61_<role>.oot` set |
| Mass deletes of product | **none** | only untracked/ignored debris |
| Push | **no** | charter |

OVERALL: **PASS**
  (hard monofile green; no extra ELF; emit-dump scratch scrubbed; names
   consistent; no credentials; soft residual only on pre-existing adjacent)

## SCOPE + METHOD
6.1 ACTIVE (SPRINT backlog): HIGH confirm `env_set` in SECRETSINK
(partially landed with 2.3 — re-prove). Product: `c_emit_secret_guard.oo`
(+ check-path `c_emit_secret.oo` + alias list `c_emit_secret_alias.oo`).

OWN units this pass:

| Path | Role in 6.1 | Git |
|------|-------------|-----|
| `oodac/c_emit_secret_guard.oo` | emit SECRETSINK `env_set` / `setenv` | dirty (+28 is **2.10** `__sec_sink__`; env_set already HEAD) |
| `oodac/c_emit_secret.oo` | check-path `sid == "env_set"` scan | dirty (+5 is 2.10; env_set already HEAD) |
| `oodac/c_emit_secret_alias.oo` | sink-name set includes `env_set` | clean (HEAD) |
| `scripts/s61_env_set_secretsink_smoke.oo` | harness rails (TMPDIR fixtures) | `??` untracked |
| `audit/s61_*.oot` | phalanx evidence | `??` / this file |

Adjacent observe (line-lock only if DE had blown 600):

| Path | Lines | Note |
|------|------:|------|
| `oodac/c_emit_secret_prop.oo` | 262 | T1/T2 prop (pre-existing soft) |
| `oodac/c_emit_args.oo` | 240 | 7.2 last-arg (already sliced) |
| `oodac/check_cap_util.oo` | 357 | `env_set` sealed Env op (pre-existing pack) |
| `oodac/main.oo` | 307 | pre-existing soft/pack residual |
| `bootstrap/SECRET_TAINT.oot` | 129 | named In (partial; re-prove rails) |

HEAD already lists `env_set` at guard L117 / L157 / L187 and secret L238–239.
This wave is a **re-prove**, not a new sink land. Hygiene does **not** run
the smoke (integration/harness own exit-0); bar here is `bash -n` + tree locks.

Commands:

  `wc -l` + hard/pack/soft gates on OWN + full `oodac/*.oo` >600 inventory
  `git add -n oodac/` before+after scrub
  `bash -n` s61 smoke
  `grep` credential + `/tmp` on OWN + smoke + s61 audits
  delete extra ELF + emit-dump scratch; **keep** product `oodac/oodac`
  no push; no product logic rewrite; no SPRINT flip

Thresholds:

| Threshold | Meaning |
|-----------|---------|
| **256** | RULES §1.7 / PROBE MAX_LINES — soft lock |
| **300** | Pack monofile pressure (flag for DE peel) |
| **600** | Path-A hard ban — force-split only if blown |

---
LINE LOCK — 6.1-touched / adjacent monofiles (live)
---

Measured under monorepo tip ooda=`52cc4b5`+WIP (2026-08-12T19:03Z).

| File | Lines | ≤256 | ≤300 | ≤600 | HEAD dirty? | Split? |
|------|------:|:----:|:----:|:----:|:-----------:|:------:|
| `oodac/c_emit_secret_guard.oo` | **228** | PASS | PASS | PASS | 2.10 `__sec_sink__` | NO |
| `oodac/c_emit_secret.oo` | **264** | OVER | PASS | PASS | 2.10 + 7.10 slice | residual |
| `oodac/c_emit_secret_alias.oo` | **157** | PASS | PASS | PASS | no | NO |
| `oodac/c_emit_secret_prop.oo` | **262** | OVER | PASS | PASS | no | residual |
| `oodac/c_emit_args.oo` | **240** | PASS | PASS | PASS | 7.2 comments | NO |
| `oodac/check_cap_util.oo` | **357** | OVER | OVER | PASS | no | residual |
| `oodac/main.oo` | **307** | OVER | OVER | PASS | no | residual |
| `scripts/s61_env_set_secretsink_smoke.oo` | **150** | PASS | PASS | PASS | NEW | n/a |
| Aggregate | Value |
|-----------|------:|
| Touched `.oo` over **600** | **0** |
| `oodac/**/*.oo` over **600** | **0** |
| 6.1 OWN over **256** | **1** (`c_emit_secret.oo` = 264 — 2.10/7.10; not 6.1) |
| 6.1 OWN `c_emit_secret_guard.oo` | **228** (soft PASS) |
| Adjacent over **256** | **3** (prop 262 / main 307 / check_cap 357 — pre-existing) |

md5 (stable re-read at measure):

| File | md5 |
|------|-----|
| `c_emit_secret_guard.oo` | `ef9029995324d31aeb26748afe21e75b` |
| product `oodac/oodac` | `a44ee35eb344837009697e2c9d4e4662` (962960 B, BuildID `a93cfd09…`) |

Delta honesty:

- **No 6.1 product land this wave.** HEAD already has `env_set` in emit
  guard (L117 a1-slot + L156–166 2.3/6.1 OS-sink block + L214–215 args
  scan), check-path `sid == "env_set"` (L238–242), and alias sink set
  (L16). Re-prove only.
- Guard WORK **228** vs HEAD **200**. Diff is concurrent **2.10**
  `__sec_sink__` prefix + duplicate a0/a1/a2 refuse — **not** env_set
  growth. Still under soft 256.
- Secret WORK **264** vs HEAD **259**. Net +5 is 2.10 `__sec_sink__`
  scan (s710 already measured 264). Parse-env slice is 7.10 OWN.
- **Hard ban holds** for entire `ooda/oodac/*.oo` product set
  (count_over_600 = 0).
- Out-of-OWN: **12** `std/src/ooda/**` files still over 600 (crypto
  stubs + slam/credit/gates). **Not** 6.1. Path-A oodac floor is the lock.

Live SECRETSINK (hygiene re-read, `c_secret_emit_guard`):

```
if sink == "setenv" || sink == "env_set" || sink == "unsetenv" || sink == "mmap"
    || sink == "mprotect" || sink == "shmat" || sink == "pwrite" || sink == "sendfile"
    || sink == "splice" || sink == "tee" { … a1/a2 refuse … }
```

Static: `env_set` present in guard + secret + alias. No new sink name added.


*(Continued in `openOODA/audit/s61_hygiene_part2.oot`)*

end of s61_hygiene.oot (part 1)
