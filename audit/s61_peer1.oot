# s61_peer1.oot
# Peer Verifier 1 (adversarial QA) — SPRINT 6.1 path-A close
# Date: 2026-08-12
# Role: Peer Verifier 1 (QA) — red-team SECRET env_set refuse / string-lit
#        no FP / T1–T2 / s71–s72–s710 regression
# Tree: /home/jeryd/Projects/openOODA/ooda
# OWN: this audit + live probes only (no product feature implementation)
# Host: ooda/oodac/oodac
#   size=962960 B  mtime=2026-08-12 07:49:59 -0700
#   md5=a44ee35eb344837009697e2c9d4e4662
#   strings: `env_set` + `setenv` + `c_secret_emit_guard` present
#   nm: `T c_secret_emit_guard` @ 0x461080
#   nm: `T c_secret_scan_call` @ 0x45e020
# Tip (ooda): 52cc4b5 fix(r2): multi-import typecheck path-A + digit-0 import names
# Source pin: oodac/c_emit_secret_guard.oo mtime 2026-08-12 11:46
#             oodac/c_emit_secret.oo     mtime 2026-08-12 11:54
#   WT source newer than host; env_set is still in the live ELF (strings +
#   emit-c refuse). Do not claim a post-11:46 rebuild.
# ID collision: DESIGN/PM 6.1 = metamorphic vs deterministic (different OWN).
# This file is SPRINT backlog 6.1 — Confirm env_set in SECRETSINK.

========================================================================
VERDICT
========================================================================

## **ACCEPT path-A close of 6.1** (emit SECRETSINK re-prove)

SPRINT **6.1** (`env_set` in SECRETSINK; partially landed with 2.3 — re-prove)
is a real path-A **emit** floor on the live product host:

1. `env_set` is **named** in emit guard, check walk, and alias set
   (`c_emit_secret_guard.oo` L117 / L157 / L215; `c_emit_secret.oo` L238–242;
   `c_emit_secret_alias.oo` L16). Host ELF contains the `env_set` string
   and the secret-guard symbols.
2. Assigned red-team is **green on emit-c** from a **clean cwd**
   (`/tmp/s61_peer1/clean`, no `.ooda-cache` poison):
   - `// SECRET: tok` then `env_set(envc, "KEY", tok)` **refuse**
     (`ERR\tsecret\tenv_set sink refuses SECRET tok`)
   - `env_set(envc, "KEY", "tok")` string literal **no secret FP**
     (no `ERR\tsecret`; dies later as unknown free call)
3. Official harness `scripts/s61_env_set_secretsink_smoke.sh` **exit 0**.
4. Regression: `secret_t1_return_smoke`, `secret_t2_depth_smoke`,
   `s71_guard_scan_smoke`, `s72_sys_exec_last_arg_smoke`,
   `s710_parse_secret_env_smoke` all **exit 0**.

**Check dual-path is residual, not a 6.1 FN.** `oodac check` runs
`typecheck_undefined_vars` **before** `check_secret_println_sinks`
(`check_drive.oo` L64 then L106). `env_set` is **not** in `is_known_name`
(`tc_names.oo` has `env_get` only) → `ERR\ttype\tundefined variable 'env_set'`
and the secret walk never reports. ELF still references `env_set` inside
`check_secret_println_sinks` (runtime auditor). Control `env_get` **is**
dual-path green.

Do **not** over-claim: no product `oo_env_set` / no `c_emit_lower` branch.
Clean `env_set` is fail-closed as `unknown free call needs annotation`
(CAPS_MATRIX unlowered residual). Full IFC / `#[Secret]` / `${}` stay
residual. `s61` is **not** wired in `ci_product.sh` (harness residual).

========================================================================
CLAIM UNDER PRESSURE
========================================================================

| Slice | Path-A claim | Peer gate |
|-------|--------------|-----------|
| `env_set` in SECRETSINK | named emit + check + alias | source 3 guard sites + check sid + `c_secret_is_sink_name` |
| `// SECRET: tok` then `env_set(..., tok)` | fail-closed | `ERR\tsecret` + rc≠0 on **emit-c** |
| String literal `"tok"` at env_set | must **not** FP | no `ERR\tsecret` (unknown-call residual OK) |
| Host vs source | rebuilt **or** emit-c prove | host stale; **emit-c prove documented** |
| T1 / T2 / s71 / s72 / s710 | stay green | all five smokes exit 0 |

Walker honesty:
- Guard runs **before** lower (`c_emit_lower.oo:90`). SECRET IDENT
  refuse is independent of `oo_env_set` existing.
- `c_guard_scan_idents` skips `"..."` interiors (7.1). That is why
  `"tok"` does not trip `ERR\tsecret`.
- Ticket is **re-prove SECRETSINK**, not “ship `oo_env_set`.”

========================================================================
SOURCE REVIEW (6.1 — no product edit)
========================================================================

`ooda/oodac/c_emit_secret_guard.oo` (228 lines, under monofile ratchet):

```
c_secret_emit_guard:
  sink = c_secret_resolve_alias(env, t)
  env_get || env_set || …        → refuse a1
  setenv || env_set || unsetenv  → refuse a1 and a2   // CRIT 2.3 / HIGH 6.1
  then c_guard_scan_idents(env, sink, args) if env_set in sealed list
```

`ooda/oodac/c_emit_secret.oo` check walk (264 lines, soft 256 already over):

```
if sid == "setenv" || sid == "env_set" || sid == "unsetenv" || mmap-family
    c_secret_scan_call(..., want=1, eq_only=false, sid)
    // refuse any IDENT at argi >= 1 (cap-first)
```

`c_emit_secret_alias.oo:16` — `env_set` is a known sink name (alias rebind).

`c_emit_lower.oo` — **no** `env_set` / `setenv` branch (only `env_get` →
`oo_env_get`). After a clean guard, unknown-call annotation fires.

`tc_names.oo` — `env_get` known; **`env_set` unknown** → check type-first.

========================================================================
PROBE TABLE (live product host)
========================================================================

Host: `OODAC=/home/jeryd/Projects/openOODA/ooda/oodac/oodac`  
Probes: `/tmp/s61_peer1/probes/*.oo` (ephemeral; **not** landed)  
Re-prove cwd: `/tmp/s61_peer1/clean` (no `.ooda-cache`).

Expect refuse = emit-c rc≠0 + `ERR\tsecret`.  
Expect string no-FP = **no** `ERR\tsecret` (unknown-call residual allowed).

### Assigned red-team (gate)

| # | Probe | Shape | Expect | emit-c | check | Verdict |
|---|-------|-------|--------|--------|-------|---------|
| P01 | `P01_env_set_value.oo` | `env_set(envc, "KEY", tok)` SECRET `tok` | refuse | rc=1 `env_set sink refuses SECRET tok` | rc=1 type `undefined env_set` | **PASS** emit |
| P03 | `P03_string_lit.oo` | `env_set(envc, "KEY", "tok")` | no secret FP | rc=1 `unknown free call … env_set` **no** `ERR\tsecret` | rc=1 type undefined | **PASS** no-FP |
| R1 | `scripts/secret_t1_return_smoke.sh` | T1 return / alias / if-taint | exit 0 | — | — | **PASS** |
| R2 | `scripts/secret_t2_depth_smoke.sh` | T2 field / method / args / midline | exit 0 | — | — | **PASS** |
| R3 | `scripts/s71_guard_scan_smoke.sh` | 7.1 ident scan | exit 0 | — | — | **PASS** |
| R4 | `scripts/s72_sys_exec_last_arg_smoke.sh` | 7.2 last-arg | exit 0 | — | — | **PASS** |
| R5 | `scripts/s710_parse_secret_env_smoke.sh` | 7.10 parse-env | exit 0 | — | — | **PASS** |
| H1 | `scripts/s61_env_set_secretsink_smoke.sh` | official 6.1 rails | exit 0 | refuse + setenv + string no-FP | — | **PASS** |

**Gate: 8/8 PASS** (assigned + official smoke).

### Bonus / control (not required to ACCEPT)

| # | Probe | Shape | Expect | emit-c | check | Verdict |
|---|-------|-------|--------|--------|-------|---------|
| P02 | `P02_env_set_key.oo` | `env_set(envc, tok, "public")` | refuse | rc=1 SECRET `tok` | type-first | **PASS** emit |
| P04 | `P04_string_contains.oo` | `"header tok placeholder"` | no secret FP | unknown-call, no secret | type-first | **PASS** |
| P05 | `P05_comment.oo` | `// env_set(..., tok)` + public args | no secret FP | unknown-call, no secret | type-first | **PASS** |
| P07 | `P07_setenv.oo` | `setenv(envc, "KEY", tok)` | refuse (2.3 sibling) | rc=1 `setenv sink refuses SECRET tok` | type `undefined setenv` | **PASS** emit |
| P08 | `P08_env_get.oo` | `env_get(envc, tok)` control | refuse dual-path | rc=1 SECRET `tok` | rc=1 SECRET `tok` | **PASS** dual |
| P09 | `P09_unused_secret.oo` | unused `tok`; public key/val | no secret FP | unknown-call, no secret | type-first | **PASS** |
| P10 | `P10_escaped.oo` | `"foo \"tok\" bar"` | no secret FP | unknown-call, no secret | type-first | **PASS** |
| P11 | `P11_substr.oo` | IDENT `tok_public` vs tag `tok` | no secret FP | unknown-call, no secret | type-first | **PASS** |
| P12 | `P12_multiarg.oo` | extra arg IDENT `tok` | refuse (args scan) | rc=1 SECRET `tok` | type-first | **PASS** emit |
| F1 | `fixtures/secret_setenv_fail.oo` | product setenv | refuse | rc=1 SECRET `token` | type-first | **PASS** |
| F2 | `fixtures/secret_setenv_pass.oo` | public setenv | no secret | unknown-call, no secret | type-first | **PASS** no-FP |
| F3 | `fixtures/secret_env_get_fail.oo` | product env_get | refuse dual | rc=1 | rc=1 | **PASS** |
| F4 | `fixtures/secret_env_get_pass.oo` | public env_get | allow | rc=0 | rc=0 | **PASS** |

### Adversarial miss that is **not** a 6.1 FN

| # | Probe | What happened | Classification |
|---|-------|---------------|----------------|
| P06 | `let f = env_set; f(envc, "KEY", tok)` | emit-c **abort** rc=134 (`unaligned tcache chunk` / malloc) | Host crash on alias-of-unlowered name. Path-A **does** list `env_set` as alias sink, but the assigned shape is a **direct** `env_set(...)` call (P01 green). Crash is fail-closed-ugly, not a SECRET allow. Residual: alias×unlowered. **Not** a 6.1 SECRETSINK FN. |

========================================================================
COMMAND (repro)
========================================================================

```
OODAC=/home/jeryd/Projects/openOODA/ooda/oodac/oodac
cd /tmp/s61_peer1/clean
$OODAC emit-c /tmp/s61_peer1/probes/P01_env_set_value.oo
# rc=1  ERR<tab>secret<tab>env_set sink refuses SECRET tok
$OODAC emit-c /tmp/s61_peer1/probes/P03_string_lit.oo
# rc=1  ERR<tab>c_emit<tab>unknown free call … env_set   (no ERR secret)
$OODAC check /tmp/s61_peer1/probes/P01_env_set_value.oo
# rc=1  ERR<tab>type<tab>undefined variable 'env_set'   (type-first residual)
cd /home/jeryd/Projects/openOODA/ooda
OODAC=./oodac/oodac bash scripts/s61_env_set_secretsink_smoke.sh   # exit 0
OODAC=./oodac/oodac bash scripts/secret_t1_return_smoke.sh        # exit 0
OODAC=./oodac/oodac bash scripts/secret_t2_depth_smoke.sh         # exit 0
OODAC=./oodac/oodac bash scripts/s71_guard_scan_smoke.sh          # exit 0
OODAC=./oodac/oodac bash scripts/s72_sys_exec_last_arg_smoke.sh   # exit 0
OODAC=./oodac/oodac bash scripts/s710_parse_secret_env_smoke.sh   # exit 0
```

========================================================================
FP / FN NOTES
========================================================================

### False positives (secret-refuse when should allow)

| Probe | Result |
|-------|--------|
| `"tok"` as env_set value literal | **no secret FP** |
| Longer string containing `tok` | **no secret FP** |
| Escaped `"foo \"tok\" bar"` | **no secret FP** |
| `// env_set(..., tok)` comment | **no secret FP** |
| Longer IDENT `tok_public` vs tag `tok` | **no secret FP** |
| Unused SECRET + public args | **no secret FP** |

**Assigned string-lit FP count: 0.**

### False negatives (allow when should refuse)

| Probe | Result |
|-------|--------|
| `env_set(..., tok)` value IDENT | **no FN** emit-c |
| `env_set(tok, ...)` key IDENT | **no FN** emit-c |
| Extra-arg IDENT `tok` | **no FN** emit-c (full args scan) |
| `setenv(..., tok)` sibling | **no FN** emit-c |
| `env_get(..., tok)` control | **no FN** dual-path |

**Assigned env_set IDENT FN count: 0** (emit-c).

Check-path secret for `env_set` is **unreachable** (type-first). That is
**not** counted as an emit FN.

========================================================================
RESIDUALS (honest, out of 6.1 gate)
========================================================================

1. **No product `oo_env_set`.** CAPS_MATRIX: sealed but not lowered.
   Clean calls fail-closed as unknown free call. Not this re-prove.
2. **Check dual-path** — type-undefined before secret. Source + ELF have
   the sid test; CLI never prints `ERR\tsecret` for `env_set`.
3. **`let f = env_set` abort** (P06) — host malloc smash on alias of an
   unlowered sink. Assigned direct-call rail is green. Do not close as
   alias prove.
4. **`s61` not in `ci_product.sh`.** Harness residual.
5. **No in-tree `secret_env_set_*.oo`.** Ephemeral + official smoke only.
6. **`diag_json` help omits `env_set`** (runtime auditor). Doc-only.
7. Full IFC / `#[Secret]` / `${}` — SECRET_TAINT residual.
8. DESIGN/PM **6.1** metamorphic-vs-deterministic — different ID.

========================================================================
PEER OWN
========================================================================

No product compiler edit. No fixture land. No smoke change.  
Write: `openOODA/audit/s61_peer1.oot` only.

========================================================================
CLOSE TABLE
========================================================================

| Item | Status |
|------|--------|
| `env_set` named in SECRETSINK (source + host) | **PASS** |
| SECRET IDENT at `env_set` fail-closed | **PASS** live emit-c |
| String literal secret name no FP | **PASS** live emit-c |
| Official `s61_env_set_secretsink_smoke` | **PASS** exit 0 |
| T1 smoke | **PASS** exit 0 |
| T2 smoke | **PASS** exit 0 |
| s71 / s72 / s710 smokes | **PASS** exit 0 |
| Gate matrix | **8/8 PASS** |
| Path-A close 6.1 | **ACCEPT** |
| check dual-path / `oo_env_set` / P06 abort / ci wire | **OPEN residual** (not gate) |

end of s61_peer1.oot
