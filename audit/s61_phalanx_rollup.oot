# s61_phalanx_rollup.oot — 6.1 `env_set` SECRETSINK (12-agent phalanx)
# Date: 2026-08-12
# Role: Documentation Writer / Close rollup
# Decision: **FIXED path-A** — **ALREADY_FIXED** (2.3 land; this close is re-prove)
# Policy: no push; no moonshots
## Close criteria
| # | Criterion | Result |
|---|-----------|--------|
| 1 | `env_set` is a named SECRETSINK (guard + check walk + alias set) | **PASS** — source + host ELF (`s61_runtime.oot`) |
| 2 | Dedicated smoke green (`s61_env_set_secretsink_smoke.oo`) | **PASS** exit 0 (Docs Writer re-prove) |
| 3 | T1/T2 secret emit floors still green | **PASS** exit 0 (`secret_t1_return` + `secret_t2_depth`) |
| 4 | 7.1 / 7.2 / 7.10 regression | **PASS** exit 0 (`s71` + `s72` + `s710`) |
| 5 | R2 multi-import TC still green | **PASS** exit 0 (`r2_multi_import_tc_smoke`) |
| 6 | Harness rails defined | **PASS** — `audit/s61_harness.oot` + smoke |
| 7 | Runtime layer split honest (`chs_rt` ≠ 6.1) | **PASS** — `audit/s61_runtime.oot` |
| 8 | Honesty residuals named (not greenwashed) | **YES** — no `oo_env_set`; check type-first; alias abort |
| 9 | Push after green | **NO** (human-gated; no push this close) |

## Decision honesty
**FIXED path-A for HIGH 6.1 (confirm `env_set` in SECRETSINK):**

`env_set` is already a named SECRET sink on the 2.3 / DE2.3 land.
This close is **re-prove**, not a product rewrite.

| Surface | `env_set` present |
|---------|-------------------|
| `oodac/c_emit_secret_guard.oo` L117 | cap-first family scan **a1** |
| `oodac/c_emit_secret_guard.oo` L157–165 | `CRIT 2.3 / HIGH 6.1` scan **a1 and a2** |
| `oodac/c_emit_secret_guard.oo` L214–225 | full `args` `c_guard_scan_idents` |
| `oodac/c_emit_secret.oo` L238–242 | `check_secret_println_sinks` `sid` list, argi ≥ 1 |
| `oodac/c_secret_is_sink_name` | `c_emit_secret_alias.oo` L16 |
| Product host ELF | unique `env_set\0` @ VMA `0x4c454d`; refs from sink-name / guard / check walk |

Live prove: `scripts/s61_env_set_secretsink_smoke.oo` **ALL OK**
(fail-closed SECRET IDENT at `env_set`; sibling `setenv` refuse; string
literal no-FP — unknown-call lower residual allowed). T1/T2 secret
smokes remain exit 0. 7.1 / 7.2 / 7.10 dedicated smokes remain exit 0
(not reopened). R2 multi-import TC remains exit 0.

**ALREADY_FIXED:** Domain Experts did **not** rewrite SECRETSINK this
wave. Host ELF **already** contains `env_set` (same 07:49 product
bytes as 7.1/7.2/7.10 close).

**ID collision honesty:** DESIGN / PM **6.1** is metamorphic vs
deterministic (policy residual). This rollup is **SPRINT backlog 6.1**
only. Do not mix the two.

**Still not claimed (do not greenwash):**

| Residual | Status |
|----------|--------|
| No product `oo_env_set` / no `c_emit_lower` `env_set` branch | **named** — CAPS_MATRIX unlowered alias; clean call = `unknown free call` |
| `oodac check` type-undefined `env_set` before `ERR secret` | **named** — dual-path SECRET is green for `env_get`, not `env_set` |
| `let f = env_set; f(...)` host abort (malloc tcache) | **named** — alias residual; not a dedicated-smoke rail |
| `setenv` / `unsetenv` / `getenv` / `putenv` product emit | same unlowered family |
| Full IFC / `#[Secret]` / interp `${}` | residual (named in harness) |
| `ci_product.oo` does not wire `s61_env_set_secretsink_smoke` | hygiene residual — `secret_sink_enforce` only (setenv fixtures, no env_set) |
| `diag_json` SECRET hint lists `env_get` not `env_set` | hygiene residual |
| No in-tree `secret_env_set_*.oo` | harness uses ephemeral TMPDIR fixtures (correct) |
| `s61_harness.oot` residual line still says 7.10 concat | **stale** — 7.10 already FIXED path-A |
| Formal peer / security / memory / hygiene / domain `.oot` still landing | ok — smoke + harness + runtime hold close |

Path-A 6.1 ≠ implement `oo_env_set`.
Cap preferred style: smoke green + named residual = FIXED path-A.
**ALREADY_FIXED** path-A: sink name already listed; this close is prove + board flip.

## Prove (Documentation Writer host re-prove)
| Gate | Result |
|------|--------|
| Host | `ooda/oodac/oodac` **962960** B · md5 `a44ee35eb344837009697e2c9d4e4662` |
| BuildID | `a93cfd09506ba87497379f66b6c7047dee02573d` |
| ooda tip | `52cc4b5` `fix(r2): multi-import typecheck path-A + digit-0 import names` |
| Host mtime | 2026-08-12 07:49 local — **same host bytes** as 7.1/7.2/7.10 close |
| Static sink | `env_set` in guard L117/L157/L215, check walk L239, alias L16 |
| Host ELF | `strings` has `env_set`; runtime auditor: sink-fn immediates present |
| Monofile | `c_emit_secret_guard.oo` **228** lines (≤600; under soft 256) |
| `scripts/s61_env_set_secretsink_smoke.oo` | exit **0** (ALL OK + lower residual named) |
| `scripts/secret_t1_return_smoke.oo` | exit **0** |
| `scripts/secret_t2_depth_smoke.oo` | exit **0** |
| `scripts/s71_guard_scan_smoke.oo` | exit **0** (7.1 regression) |
| `scripts/s72_sys_exec_last_arg_smoke.oo` | exit **0** (7.2 regression) |
| `scripts/s710_parse_secret_env_smoke.oo` | exit **0** (7.10 regression) |
| `scripts/r2_multi_import_tc_smoke.oo` | exit **0** (regression) |

### DE / harness rails (must all hold)
| Kind | Case | Result |
|------|------|--------|
| FAIL-CLOSED | `// SECRET: tok` then `env_set(envc, "KEY", tok)` | OK |
| FAIL-CLOSED | `// SECRET: tok` then `setenv(envc, "KEY", tok)` | OK (product names `setenv`) |
| NO-FP | `env_set(envc, "KEY", "tok")` string literal | OK (no `ERR secret`; unknown-call residual) |

Docs Writer extra (ephemeral, not dedicated-smoke rails):

| Kind | Case | Result |
|------|------|--------|
| FAIL-CLOSED | SECRET as `env_set` **key** | PASS (`… refuses SECRET token`) |
| NO-FP | unused SECRET + public `env_set` args | `unknown free call` (no `ERR secret`) |
| Dual-path | `oodac check` on `env_set` fixtures | type-undefined (residual) |
| Control | `secret_env_get_{fail,pass}` | emit+check dual-path PASS |
| Residual | `let f = env_set; f(..., tok)` | host abort (`unaligned tcache`) — not a 6.1 fail |

## 12-agent phalanx summary
Composition per `SWARM.oot` §6. Some peers still landing at rollup time;
rollup uses live smoke + landed audits + ephemeral peer probe artifacts.

### I. Engineering Core (3)
| Agent | Evidence | Verdict |
|-------|----------|---------|
| Domain Expert A | no rewrite this wave | **ALREADY_FIXED** — 2.3/DE2.3 lists `env_set` |
| Domain Expert B | sibling `setenv`/`unsetenv` family | **ALREADY_FIXED** — same 2.3 branch |
| Machine Code / Runtime | `audit/s61_runtime.oot` | **PASS** — emit lists `env_set`; `chs_rt` is not 6.1 |

### II. Adversarial QA Wing (4)
| Agent | Evidence | Verdict |
|-------|----------|---------|
| Peer1 (matrix) | `audit/s61_peer1.oot` | **ACCEPT** — emit refuse + no-FP + regressions; check type-first residual |
| Peer2 (scale / family) | `/tmp/s61_peer2_1103362/` (formal `.oot` optional) | **in-flight** — env_set/setenv/unsetenv + alias abort residual |
| Security Auditor | `/tmp/s61_sec.FKg4Ju/` probes | **in-flight** — putenv / alias / concat / wrap |
| Memory Auditor | `audit/s61_memory.oot` | **PASS** — no new concat; RSS class matches setenv |

### III. Integration Wing (3)
| Agent | Evidence | Verdict |
|-------|----------|---------|
| Compiler Integration | `audit/s61_integration.oot` | **GREEN** T1/T2/s71/s72/s710 + s61 smoke; check/alias residual |
| Test Harness | `audit/s61_harness.oot` + `scripts/s61_env_set_secretsink_smoke.oo` | **PASS** rails defined + exit 0 |
| Hygiene Enforcer | `audit/s61_hygiene.oot` | **PASS** monofile 228; `bash -n` smoke; `ci_product` not wired |

### IV. Command Wing (2)
| Agent | Evidence | Verdict |
|-------|----------|---------|
| Idea Fairy | (optional) | support — do not implement `oo_env_set` this close |
| Documentation Writer | this rollup + SPRINT pin/row | **FIXED path-A** board flip |

### Support / ephemeral (not full peer .oot yet)
| Artifact | Role |
|----------|------|
| `/tmp/s61_docs_reprove/` | Docs Writer re-prove s61 + T1/T2/s71/s72/s710/r2 |
| `/tmp/s61_docs_prove/` | extra key/alias/pass matrix |
| `/tmp/s61_integration/*.out` + `*.rc` | integration T1/T2/s71/s72/s710 logs |
| `/tmp/s61_runtime_prove/` | runtime emit/check matrix |
| `/tmp/s61_peer1/` | peer1 fixture matrix |
| `/tmp/s61_peer2_1103362/` | peer2 family + alias |
| `/tmp/s61_sec.FKg4Ju/` | security extra sinks |

## Peer / support audits (landed)
| Audit | Role | Verdict |
|-------|------|---------|
| `s61_harness.oot` | smoke pointer | **PASS** rails (7.10 residual line **stale**) |
| `s61_runtime.oot` | ELF + layer split | **PASS** (scope) |
| `s61_integration.oot` | live tip smokes | **GREEN** assigned + s61 |
| `s61_hygiene.oot` | line-lock / no extra ELF | **PASS** (board OPEN snapshot **stale** — Docs flipped) |
| `s61_memory.oot` | no new concat | **PASS** |
| `s61_peer1.oot` | refuse / no-FP / regression | **ACCEPT** |
| Peer2 / security / domain formal `.oot` | family / ZT | ephemeral `/tmp/s61_*` if absent |

## Product land (this close)
| Repo | Surface | What |
|------|---------|------|
| **ooda** | `scripts/s61_env_set_secretsink_smoke.oo` | Harness path-A smoke (untracked until human commit) |
| **openOODA** | `SPRINT.oot` | 6.1 → **FIXED** path-A ALREADY_FIXED; pin/focus brief |
| **openOODA** | this rollup | close evidence |
| **ooda** | `bootstrap/SECRET_TAINT.oot` | env_set row: In + smoke; lower residual (no longer “re-prove rails”) |

No host rebuild this close. Residual next leftover concat remains **8.6**
`c_args_strip_cr`. Do **not** grow a new `oo_env_set` product op under 6.1.

## Cap map (adjacent, not 6.1) / SECRETSINK map (path-A truth)
| Surface | Behavior | Note |
|---------|----------|------|
| `c_secret_emit_guard` | `env_set` a1 + a1/a2 + full args scan | **6.1 OWN** |
| `check_secret_println_sinks` | `sid == env_set` argi ≥ 1 | **6.1 OWN** (check CLI type-first residual) |
| `c_secret_is_sink_name` | alias set includes `env_set` | **6.1 OWN** |
| `is_sealed_env` | EnvCap check lists `env_set` | CAP adjacent — not SECRET |
| `c_emit_lower` `env_get` → `oo_env_get` | product GET | control; **no** `env_set` lower |
| `oo_process_policy_getenv` / exec inherit | T3 / DE1.8 | **not** 6.1 |
| `setenv` / `unsetenv` | sibling 2.3 names | same unlowered family |

## Residual (not this close fail)
| Item | Status |
|------|--------|
| Product `oo_env_set` / lower | open — CAPS / new product op; not 6.1 |
| `oodac check` dual-path `ERR secret` for `env_set` | open until name is typed/lowered |
| Alias `let f = env_set` host abort | open — host malloc; not sink-list fail |
| `ci_product.oo` s61 wire | hygiene residual |
| `diag_json` hint omits `env_set` | hygiene residual |
| Full IFC / `#[Secret]` / `${}` | residual |
| DESIGN/PM 6.1 metamorphic vs deterministic | different ID — untouched |
| Formal peer2 / security / domain `.oot` still landing | ok — smoke + harness + runtime + peer1/memory/hygiene/integration hold close |

## Reproduce
```bash
cd /home/jeryd/Projects/openOODA/ooda
export OODAC_BIN=./oodac/oodac
# static: env_set is a named SECRETSINK
grep -n 'env_set' oodac/c_emit_secret_guard.oo oodac/c_emit_secret.oo oodac/c_emit_secret_alias.oo
# expect: guard L117/L157/L215; check L239; alias L16
bash scripts/s61_env_set_secretsink_smoke.oo
# expect: ALL OK + product-lower residual lines; exit 0
```

end of s61_phalanx_rollup.oot
