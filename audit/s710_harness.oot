# s710_harness.oot — 7.10 c_parse_secret_env path-A harness pointer
# Date: 2026-08-12
# Scope: SPRINT 7.10 smoke rails only (no Domain Expert product land)

## Harness

| Item | Path |
|------|------|
| Smoke | `ooda/scripts/s710_parse_secret_env_smoke.sh` |
| Binary | `OODAC_BIN` or `ooda/oodac/oodac` |
| Fixtures | ephemeral under `$TMPDIR/s710_parse_secret_env_$$` (not repo root) |
| Product | `ooda/oodac/c_emit_secret.oo` (`c_parse_secret_env` — do not rewrite) |

## Rails

| Kind | Case |
|------|------|
| FAIL-closed | `// SECRET: tok` then `println(tok)` (bare IDENT) |
| PASS | two SECRET names both refuse (`// SECRET: a` + `// SECRET: b` then `println(a)` **and** `println(b)`) |
| FAIL-closed | `// SECRET:   ` (empty / spaces-only) invalid name |

Exit 0 only if all rails hold.

## Run

```
cd /home/jeryd/Projects/openOODA/ooda
OODAC_BIN=./oodac/oodac bash scripts/s710_parse_secret_env_smoke.sh
```

## Residual (documented, not a fail)

- **Full IFC / `#[Secret]` / interp `${}`:** path-A parse + sink refuse only; not DESIGN taint.
- **Concat hygiene:** product still `name = name + c` per ident char until Domain A slice land. This harness proves tag parse / refuse, not allocator math.
- **Interproc:** SECRET beyond same-file `__fr_secret__` remains residual.

end of s710_harness.oot
