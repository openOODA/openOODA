# s710_peer1.oot
# Peer Verifier 1 (adversarial QA) — SPRINT 7.10 path-A close
# Date: 2026-08-12
# Role: Peer Verifier 1 (QA) — red-team SECRET parse / multi-name / empty
#        fail-closed / T1–T2 / s71–s72 regression
# Tree: /home/jeryd/Projects/openOODA/ooda
# OWN: this audit + live probes only (no product feature implementation)
# Host: ooda/oodac/oodac
#   size=962960 B  mtime=2026-08-12 07:49:59 -0700
#   md5=a44ee35eb344837009697e2c9d4e4662
#   nm: `T c_parse_secret_env` @ 0x45d140
#   strings: `invalid name (need ident after SECRET directive)` present
# Tip (ooda): 52cc4b5 fix(r2): multi-import typecheck path-A + digit-0 import names
# Source pin: oodac/c_emit_secret.oo mtime 2026-08-12 11:54 (NEWER than host)
#   7.10 body is slice in WT source; product binary predates Domain A.
## VERDICT
## **ACCEPT path-A close of 7.10** (source + assigned parse rails)
SPRINT **7.10** (`c_parse_secret_env` no per-char OoStr leak / one
`str_slice` ident) is a real path-A **source** close. Assigned
behavioral red-team is green on the live product host via **emit-c of
fixtures that exercise parse** (plus `check` dual-path from a clean cwd).

1. Parse-env is **defined** and linked (`nm` `T c_parse_secret_env`;
   empty-name string in the binary).
2. WT source (`oodac/c_emit_secret.oo` L7–82) takes **one**
   `str_slice(src, start, k)` (L65). There is **no** `name = name + c`
   in `c_parse_secret_env`. Language has no C stack `char[]`; slice is
   the honest alpha substitute (same class as 7.1 / 7.2).
3. Assigned red-team matrix is **green dual-path** (`emit-c` + `check`)
   from a **clean cwd** (`/tmp/s710_peer1/clean`, no `.ooda-cache`
   poison):
   - `// SECRET: tok` then `println(tok)` **refuse**
   - two SECRET names both refuse (`println(a)` and `println(b)`)
   - empty / spaces-only `// SECRET:` **fail-closed** (`invalid name`)
4. Regression: `secret_t1_return_smoke`, `secret_t2_depth_smoke`,
   `s71_guard_scan_smoke`, `s72_sys_exec_last_arg_smoke` all **exit 0**.
   Harness extra `s710_parse_secret_env_smoke.oo` **exit 0** (bonus).

**Host is stale.** Product `oodac` mtime 07:49 < source 11:54. Peer
rule: *REJECT until rebuilt **or document prove via emit-c of a fixture
that exercises parse**.* Document, do **not** REJECT the assigned
close: emit-c of P01 / P02a / P02b / P03 / P03b + product fixtures
exercises `c_parse_secret_env` on the live host; refuse/allow match the
source contract. Do **not** claim the running binary is sliced (Peer2
objdump: host parse-env still `oo_str_concat`×4 / `oo_str_slice`×0).

Do **not** over-claim: full IFC / `#[Secret]` / `${}` stay residual.
`s710_integration.oot` was written **before** the slice land (still
says L60 concat) — stale; this peer re-read source + re-ran smokes.
`s71`/`s72`/`s710` smoke residual banners still print “7.10 concat”
(harness text lag, not a product FN).

## CLAIM UNDER PRESSURE
| Slice | Path-A claim | Peer gate |
|-------|--------------|-----------|
| 7.10 memory (source) | no per-char OoStr thrash in `c_parse_secret_env` | source: one `str_slice` / directive name; no `name = name + c` |
| `// SECRET: tok` then `println(tok)` | fail-closed | `ERR\tsecret` + rc≠0 on emit **and** check |
| Multiple `// SECRET:` names | both live | `println(a)` and `println(b)` each refuse; unused `c` allows |
| Empty `// SECRET:` (and spaces-only) | fail-closed | `invalid name` + rc≠0 dual-path |
| Host vs source | rebuilt **or** emit-c prove | host stale; **emit-c prove documented** |
| T1 / T2 / s71 / s72 | stay green | all four smokes exit 0 |

Walker honesty (same class as 7.1 / 7.2):
- Ticket says “char-by-char leak.” `.oo` has **no** stack `char buf[N]`.
- Alpha close = **slice, not quadratic concat**. That land is in WT
  source. Live host still concat until next seed/rebuild.

---
SOURCE REVIEW (7.10 — no product edit)
---

`ooda/oodac/c_emit_secret.oo` (264 lines, ≤600; soft 256 already over
pre-7.10; Domain A did not grow the file):

```
pub fn c_parse_secret_env(src: String) -> String
  needle = "// " + "SECRET:"          // line-start only
  skip " " / "\t" after colon
  start = k
  while is_al: k = k + 1              // NO name = name + c
  name = str_slice(src, start, k)     // ONE slice per directive
  if name == "": ERR invalid name; process_exit(1)
  c_env_put __sec__name=1
  c_env_put __secref__name=1
  i = k                               // next line-start SECRET still seen
```

Static (this peer, after Domain A):

| Grep | Result |
|------|--------|
| `name = name + c` in file | **0** |
| `str_slice` | L65 (OWN) + L254 (`__sec_sink__` prefix — **not** 7.10) |
| empty-name fail-closed | L66–69 unchanged |
| multi-name continue | L73 `i = k` then outer loop |

Ident class is still `_A-Za-z0-9` from the **first** char (leading
digits allowed). That is pre-existing, not a 7.10 rewrite.

Dirty-tree also has 2.10 `__sec_sink__` scan at L253–257. **Not** this
OWN; not re-proved here.

---
HOST vs SOURCE (stale binary)
---

| Item | Value |
|------|--------|
| Host | `ooda/oodac/oodac` 962960 B @ 2026-08-12 07:49:59 -0700 |
| md5 | `a44ee35eb344837009697e2c9d4e4662` |
| Source | `oodac/c_emit_secret.oo` 10864 B @ 2026-08-12 11:54:09 -0700 |
| 7.10 body in host | **concat** (Peer2 objdump; this peer does not claim slice-in-binary) |
| 7.10 body in WT | **slice** (L65) |

Gate used: **document prove via emit-c of fixtures that exercise parse**
(see probe table). Rebuild is **not** this peer’s OWN.

`oodac emit-c oodac/c_emit_secret.oo` is **not** a valid prove path
(monofile is not a standalone program; emit dies
`ERR\tc_emit\tunknown free call needs annotation\too_str_eq`). User
fixtures with `// SECRET:` **do** run parse-env.

---
PROBE TABLE (live product host)

*(Continued in `openOODA/audit/s710_peer1_part2.oot`)*

end of s710_peer1.oot (part 1)
