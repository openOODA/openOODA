# s710_runtime.oot — Machine Code / Runtime Auditor (SPRINT 7.10)
# Date: 2026-08-12T18:58Z
# Role: Machine Code / Runtime — emit-time .oo vs C host for `c_parse_secret_env`
# Tree: /home/jeryd/Projects/openOODA/ooda (+ openOODA/audit)
# Tip (ooda): 52cc4b5 fix(r2): multi-import typecheck path-A + digit-0 import names
# Host: ooda/oodac/oodac  962960 B  mtime 2026-08-12 07:49
#   md5 a44ee35eb344837009697e2c9d4e4662
#   BuildID a93cfd09506ba87497379f66b6c7047dee02573d
# OWN: after Domain A lands slice — host may be stale until rebuild;
#      document host vs source honesty; nm/objdump if rebuilt;
#      leftover stack char[N] in this walk; chs_rt not primary.
#      Comment-only audit. No product rebuild this turn. No push.
## SUMMARY
| Claim | Expected | Actual | VERDICT |
|-------|----------|--------|---------|
| Domain A source close is slice, not concat | `str_slice(src, start, k)`; **no** `name = name + c` | `c_emit_secret.oo` L43 `let start = k`; L65 `let name = str_slice(src, start, k)`; grep `name = name +` **0** | **PASS** (source) |
| Product ELF defines `c_parse_secret_env` | `T` text symbol, not `U` | `nm -S`: `T` @ `0x45d140` **3378** B (`0xd32`); `readelf` FUNC GLOBAL | **PASS** |
| ELF extract uses slice, not ident concat | `oo_str_slice`×1; **0** ident-loop `oo_str_concat` | **STALE host:** `oo_str_slice`×**0**; `oo_str_concat`×**4** (needle + **name+c loop** + `__sec__` + `__secref__`) | **FAIL residual** (host stale) |
| No leftover `char[N]` / stack ident buffer | slice / OoStr, not fixed C buffer | .oo: no `char[]`; host disasm: **0** `sprintf`/`strncpy`/`strcpy`/`strcat`; frame `sub $0x68,%rsp` (104 B spills) | **PASS** |
| No implicit-function-declaration | proto or prior def before use | Isolated emit printed `OoStr c_parse_secret_env(OoStr src);` **before** body; `c_emit_let_ext` allowlists name as OoStr; gcc contrast | **PASS** (mechanism) |
| chs_rt is not the home of this symbol | emit-time .oo only | **zero** hits in `ooda/runtime/*`; floor only supplies `oo_char_at` / `oo_str_slice` / `oo_str_concat` / `oo_chars_len` | **PASS** (not primary) |
| Current host can **re-emit** this parser | complete body with slice | Isolated `emit-c` prints proto + `long long start = k;` then dies at untyped `let is_al` → `unknown free call oo_str_eq` | **FAIL residual** (rebuild) |
| Host rebuilt this wave | new ELF matching source | **same** 07:49 ELF as s71/s72 runtime (962960 B / md5 `a44ee35e…` / BuildID `a93cfd09…`) | **STALE** — do not nm/objdump a post-slice ELF |

**OVERALL: PASS source / host STALE** — HIGH 7.10 is **source-closed** (`str_slice` once, no `char[N]`). The **shipping C host has not been rebuilt** and still executes the pre-slice ident loop (`oo_str_concat` at `0x45d3b5` after `add $1` on the class match). Do **not** treat this ELF as 7.10 machine-code proof.

Do **not** greenwash: Domain A (`s710_domain_a.oot`) already named “product binary not rebuilt.” This auditor independently confirms that with `nm`/`objdump`. Semantic T1/T2 rails on the **old** host still parse `// SECRET:` (concat path); they do **not** prove the new slice is linked.

## METHOD
1. Static: `ooda/oodac/c_emit_secret.oo` (`c_parse_secret_env`),
   `c_emit.oo` / `c_emit_stream_mode` call sites, `c_emit_let_ext.oo`
   OoStr allowlist, `runtime/chs_rt_str.c` (`oo_str_slice` / `oo_str_concat` /
   `oo_char_at`), `runtime/chs_rt.h`.
2. Product host `ooda/oodac/oodac` (pre-slice ELF): `nm -S`, `readelf -s`,
   `objdump -d --start-address=0x45d140 --stop-address=0x45de72`.
3. Contrast: seed `ooda/bootstrap/seed/oodac` same symbol (also concat, 0 slice).
4. Live isolated `oodac emit-c` of extracted `c_parse_secret_env` only
   (`/tmp/s710_parse_only.oo` — not a product file). Full-module `emit-c`
   of `c_emit_secret.oo` timed out in preamble (25s, 4096 B) — not used as
   proof.
5. Implicit-decl contrast: C11 `-Werror=implicit-function-declaration`.
6. No product logic change. No host rebuild this auditor.

`chs_rt` inspected only as the floor behind slice/concat/char_at.

---
0. Host vs source honesty (the 7.10 runtime question)
---

| Surface | mtime | What `c_parse_secret_env` does |
|---------|-------|--------------------------------|
| `ooda/oodac/c_emit_secret.oo` | **2026-08-12 11:54** (Domain A land) | `start` + advance `k` + **one** `str_slice`; **no** `name = name + c` |
| Product `ooda/oodac/oodac` | **2026-08-12 07:49** (pre-land) | still `oo_str_concat` in the ident class-match loop; **0** `oo_str_slice` |
| Seed `ooda/bootstrap/seed/oodac` | 2026-08-11 19:27 / 878392 B | `oo_str_concat`×3, `oo_str_slice`×0 (older; 1× `c_env_put`) |

Source is **newer than the host by ~4 h**. Same host fingerprint as
`s71_runtime.oot` / `s72_runtime.oot`. Rebuild did **not** happen between
Domain A land and this audit.

Until a new `oodac` is emitted+linked from this `.oo`:

- `nm`/`objdump` describe the **old** walker.
- A future ELF that closes 7.10 in machine code should show at least:
  `oo_str_slice`×**1** and **no** `oo_str_concat` on the ident-advance
  edge (the `add $0x1` + concat + release pair at `0x45d3a0`). Remaining
  honest concats: needle `"// "+"SECRET:"` (L8) and
  `"__sec__"+name` / `"__secref__"+name` (L70/L72).

This auditor did **not** rebuild (OWN: document honesty, nm/objdump
**if** rebuilt).

---
1. Well-defined symbol (no implicit decl / missing symbol)
---

### Product host — PASS
| Probe | Result |
|-------|--------|
| `strings` | `c_parse_secret_env` present |
| `nm -S --defined-only` | `000000000045d140 0000000000000d32 T c_parse_secret_env` |
| `readelf -s` | FUNC GLOBAL DEFAULT, size **3378** |
| Undefined in host? | **no** `U c_parse_secret_env` |

3378 bytes is a full line-start / needle / class-table walker, not a stub.

### Call sites (ELF + .oo)
| Site | Role |
|------|------|
| `check_secret_println_sinks` @ `0x475c50` | `call c_parse_secret_env` @ `0x475c85` — check-path env |
| `c_emit_stream_mode` @ `0x477b30` | `call c_parse_secret_env` @ `0x477cd2` — emit-path (`c_emit.oo` L78 `sec_env`) |

`.oo` matches: `check_secret_println_sinks` L135; `c_emit.oo` L78.

### Emit-time declaration order — PASS (mechanism)
`c_emit_let_ext.oo` lists `c_parse_secret_env` as **OoStr**. Isolated live
emit printed, before any `{`:

```
OoStr c_parse_secret_env(OoStr src);
```

then `OoStr c_parse_secret_env(OoStr src) { ... }`.

Product `cli_emit.oo` default `emit-c` is `protos` then `bodies`.

### Implicit-decl contrast — PASS (mechanism)
| TU | `gcc -std=c11 -Werror=implicit-function-declaration` |
|----|------------------------------------------------------|
| proto present, take address of `c_parse_secret_env` | **PROBE_COMPILE_OK** |
| take address with **no** proto | **error** (`undeclared`) |

Product TU shape is the first row (protos pass).

---
2. Source slice (Domain A) vs host concat — no leftover `char[N]`

*(Continued in `openOODA/audit/s710_runtime_part2.oot`)*

end of s710_runtime.oot (part 1)
