# s71_harness.oot — 7.1 c_guard_scan_idents path-A harness pointer
# Date: 2026-08-12
# Scope: SPRINT 7.1 smoke rails only (no Domain Expert product land)

## Harness

| Item | Path |
|------|------|
| Smoke | `ooda/scripts/s71_guard_scan_smoke.sh` |
| Binary | `OODAC_BIN` or `ooda/oodac/oodac` |
| Fixtures | ephemeral under `$TMPDIR/s71_guard_scan_$$` (not repo root) |
| Product | `ooda/oodac/c_emit_secret_guard.oo` (`c_guard_scan_idents` — do not rewrite) |

## Rails

| Kind | Case |
|------|------|
| PASS fail-closed | `// SECRET: tok` then `println(tok)` (bare IDENT) |
| PASS fail-closed | `// SECRET: tok` then `write_file` payload `tok` |
| PASS no-FP | `println("tok")` string literal when `tok` is SECRET name |

Exit 0 only if all rails hold.

## Run

```
cd /home/jeryd/Projects/openOODA/ooda
OODAC_BIN=./oodac/oodac bash scripts/s71_guard_scan_smoke.sh
```

## Residual (documented, not a fail)

- **Full IFC / `#[Secret]` / interp `${}`:** path-A ident scan only; not DESIGN taint.
- **7.10:** `c_parse_secret_env` still char-by-char name concat (separate OWN).
- **Interproc:** SECRET beyond same-file `__fr_secret__` remains residual.

end of s71_harness.oot
