# s71_hygiene.oot — Hygiene Enforcer (7.1 c_guard_scan_idents phalanx)
# Date: 2026-08-12T18:37:54Z
# Role: Dependency/Hygiene Enforcer — RULES §1.7 (7.1 phalanx)
# Tree: /home/jeryd/Projects/openOODA  (ooda/ + openOODA/)
# Tip (ooda): 52cc4b5 + WIP (`scripts/s71_guard_scan_smoke.oo` ??)
# Tip (openOODA): 81f56fc + WIP s71_* audits + SCIENCE/SPRINT (M21 / science rows; not 7.1)
# OWN: line-lock 7.1-touched `.oo`; bash -n `s71_guard_scan_smoke.oo`;
#      no ELF / scratch in `oodac/`; consistent `audit/s71_*.oot`; no credentials
# WRITE: this audit + hygiene-only scrub of leftover toys / extra ELF
# POLICY: RULES §1.7 / PROBE MAX_LINES = 256 (soft); pack watch 300; hard ban >600
#         No mass product peels; force-split only if DE blew 600
## SUMMARY
| Check | VERDICT | Notes |
|-------|---------|-------|
| Hard ban >600 (touched .oo) | **PASS** | 0 OWN / 0 `ooda/oodac/**/*.oo` over 600 |
| Soft lock ≤256 (7.1 OWN) | **PASS** | `c_emit_secret_guard.oo` **200** |
| Pack pressure ≤300 (OWN) | **PASS** | 200 < 300 |
| Adjacent secret family soft | **WARN** residual | `c_emit_secret.oo` **259**, `c_emit_secret_prop.oo` **262** — pre-existing; DE A **did not** touch |
| DE monofile blow >600 | **none** | Domain A: **no product edit** (ALREADY_FIXED prove) |
| ELF extra / scratch in `oodac/` | **PASS** (scrubbed) | deleted a.oo/b.oo, `*.tmp.c`, bak/x/fuzz ELF copies |
| Product host ELF | **kept** | gitignored `oodac/oodac` **962960** B — required by smoke; not a DE toy |
| LLM / cyclic scratch | **PASS** (scrubbed) | `oodac/a.oo` + `b.oo` (R2 cyclic pair, reappeared 07:56) |
| Credentials | **PASS** | ZERO live secrets/tokens/keys |
| `/tmp` in product `.oo` | **PASS** | ZERO in 7.1 OWN |
| Smoke `/tmp` dummy path | **WARN** residual | fixture write target never hit if refuse rail holds |
| `bash -n` s71 smoke | **PASS** | `scripts/s71_guard_scan_smoke.oo` 123L syntax clean |
| `audit/s71_*.oot` names | **PASS** | consistent `s71_<role>.oot` set |
| Mass deletes of product | **none** | only untracked/ignored debris |
| Push | **no** | charter |

OVERALL: **PASS**
  (hard monofile green; extra ELF + oodac scratch scrubbed; names consistent;
   no credentials; soft residual only on pre-existing adjacent secret units)

## SCOPE + METHOD
7.1 ACTIVE (SPRINT backlog): CRIT `c_guard_scan_idents` stack buffer /
no per-char OoStr leak — `ooda/oodac/c_emit_secret_guard.oo`.

OWN units this pass:

| Path | Role in 7.1 | Git |
|------|-------------|-----|
| `oodac/c_emit_secret_guard.oo` | walker + `c_secret_emit_guard` | clean (HEAD) |
| `scripts/s71_guard_scan_smoke.oo` | harness rails (TMPDIR fixtures) | `??` untracked |
| `audit/s71_*.oot` | phalanx evidence | `??` / this file |

Adjacent observe (not dirty; line-lock only if DE had blown 600):

| Path | Lines | Note |
|------|------:|------|
| `oodac/c_emit_secret.oo` | 259 | 7.10 `c_parse_secret_env` still OPEN |
| `oodac/c_emit_secret_alias.oo` | 157 | alias resolve |
| `oodac/c_emit_secret_prop.oo` | 262 | T1/T2 prop |
| `oodac/c_emit_args.oo` | 238 | 7.2 last-arg (sibling, not this OWN) |

DE A (`s71_domain_a.oot`): **no source change** — path-A slice already landed
(T1 memory / tip). Integration ran harness smoke **exit 0** (not re-run here;
hygiene bar is `bash -n` + tree locks).

Commands:

  `wc -l` + hard/pack/soft gates on OWN + full `oodac/*.oo` >600 inventory
  `git add -n oodac/` before+after scrub
  `bash -n` s71 smoke
  `grep` credential + `/tmp` on OWN + smoke + s71 audits
  delete extra ELF + scratch toys; **keep** product `oodac/oodac`
  no push; no product logic rewrite; no SPRINT flip

Thresholds:

| Threshold | Meaning |
|-----------|---------|
| **256** | RULES §1.7 / PROBE MAX_LINES — soft lock |
| **300** | Pack monofile pressure (flag for DE peel) |
| **600** | Path-A hard ban — force-split only if blown |

---
LINE LOCK — 7.1-touched / adjacent monofiles (live)
---

Measured under monorepo tip ooda=`52cc4b5`+WIP (2026-08-12T18:37Z).

| File | Lines | ≤256 | ≤300 | ≤600 | HEAD dirty? | Split? |
|------|------:|:----:|:----:|:----:|:-----------:|:------:|
| `oodac/c_emit_secret_guard.oo` | **200** | PASS | PASS | PASS | no | NO |
| `oodac/c_emit_secret.oo` | **259** | OVER | PASS | PASS | no | residual |
| `oodac/c_emit_secret_prop.oo` | **262** | OVER | PASS | PASS | no | residual |
| `oodac/c_emit_secret_alias.oo` | **157** | PASS | PASS | PASS | no | NO |
| `oodac/c_emit_args.oo` | **238** | PASS | PASS | PASS | no | NO |
| `scripts/s71_guard_scan_smoke.oo` | **123** | PASS | PASS | PASS | NEW | n/a |
| Aggregate | Value |
|-----------|------:|
| Touched `.oo` over **600** | **0** |
| `oodac/**/*.oo` over **600** | **0** |
| 7.1 OWN over **256** | **0** (`c_emit_secret_guard.oo` = 200) |
| Adjacent over **256** | **2** (secret 259 / secret_prop 262 — pre-existing) |

md5 (stable re-read at measure):

| File | md5 |
|------|-----|
| `c_emit_secret_guard.oo` | `66fe7377bc3f3e3669352f2dfe21d96e` |
| product `oodac/oodac` | `a44ee35eb344837009697e2c9d4e4662` (962960 B, BuildID `a93cfd09…`) |

Delta honesty:

- **No DE product land this wave.** Domain A proved existing `str_slice`
  walker (L105); did not grow the monofile.
- **Hard ban holds** for entire `ooda/oodac/*.oo` product set (count_over_600 = 0).
- Soft residual on `c_emit_secret*.oo` is **not** a 7.1 blow — do not force-peel.

## SPLIT GATE
Rule: force-split only if OWN unit >600 (path-A hard ban).

| Unit | Over hard? | Action |
|------|:----------:|--------|
| `c_emit_secret_guard.oo` 200 | no | **none** |
| adjacent secret 259 / 262 | no hard | residual — 7.10 OWN may peel later |
| `main.oo` 307 (pre-existing) | no hard | not 7.1-touched |

## BINARY + SCRATCH SCRUB
Charter: **no ELF / scratch in `oodac/`**. Extra copies + lab toys deleted.
Product host kept (gitignored; harness `OODAC_BIN` default).

### Extra ELF (pre-scrub FAIL → post-scrub PASS)
| Artifact | Size | Pre | Post |
|----------|-----:|-----|------|
| `oodac/oodac.x` | 962680 B ELF | present (gitignored) | **deleted** |
| `oodac/oodac.bak.pre_cap_g4` | 958320 B ELF | present | **deleted** |
| `oodac/oodac.bak.pre_r2_domain_a` | 962680 B ELF | present | **deleted** |
| `oodac/oodac_fuzz_copy` | 962888 B ELF | present | **deleted** |
| `oodac/oodac` | 962960 B ELF | product host | **kept** (gitignored) |

`git add -n oodac/` after scrub: **empty** — no ELF, no scratch, no `.oo` add.

### Scratch toys (scrubbed)
Product tree (`ooda/oodac/`):

- `a.oo`, `b.oo` — cyclic `import` pair (R2 lab; **reappeared 07:56** after R2 hygiene)
- `c_emit_preamble.bin.tmp.c`, `main.oo.bin.tmp.c`, `stage1_noarc.tmp.c` (2.0 MB)

Super-repo contemporaneous emit dump (DE/integration window 11:36–11:37):

- `oodac_new.tmp.c` — generated C preamble dump; **deleted**

Not deleted (out of 7.1 oodac charter): historical super-repo `tmp_*.oo`,
`scratch/`, root `oodac/oodac` host copy. Tracked corpus `oodac/test_*.oo`
(`test_expand`, `test_le`, …) are pre-7.1 helpers — leave for DE.

`.gitignore` already covers `/oodac/a.oo`, `/oodac/b.oo`, `/oodac/oodac*`,
`/oodac/*.tmp.c` — no ignore edit required this pass.

---
bash -n + SECRETS + /tmp
---

```
OK bash -n scripts/s71_guard_scan_smoke.oo   (123 lines)
```

Smoke uses `$TMPDIR/s71_guard_scan_$$` + `trap` cleanup — fixtures never
land in-repo.

Credential-class scan (`password=`, `BEGIN *PRIVATE`, AWS/GitHub/Slack
tokens, Bearer) on:

  - `c_emit_secret_guard.oo`
  - `s71_guard_scan_smoke.oo`
  - all live `audit/s71_*.oot`

  → **ZERO live credentials.**

Dummy fixture string `"s3cret"` in smoke heredocs = theater payload, not
a host secret. Ideas table mentions fixture name `api_key` as an ident
under test — not a key material.

`/tmp` in 7.1 OWN `.oo`: **none**.

Smoke fixture `write_file(..., "/tmp/s71_guard_scan_should_not_write", tok)`
is a fail-closed target that must never execute. **WARN residual** (prefer
`$FIX/...` if harness revises); not a secret leak.

VERDICT credentials: **PASS**

---
audit/s71_*.oot NAME CONSISTENCY
---

Pattern: `s71_<role>.oot` under `openOODA/audit/` (same family as
`r2_hygiene.oot` / `r2_domain_a.oot`).

| File | Role | Status |
|------|------|--------|
| `s71_harness.oot` | Test Harness Writer | present |
| `s71_domain_a.oot` | Domain Expert A | present |
| `s71_domain_b.oot` | Domain Expert B | present (landed mid-pass) |
| `s71_ideas.oot` | Idea Fairy residuals | present |
| `s71_integration.oot` | Compiler Integration Tester | present |
| `s71_peer1.oot` | Peer Verifier 1 | present (landed mid-pass) |
| `s71_phalanx_rollup.oot` | Close rollup | present (landed mid-pass) |
| `s71_hygiene.oot` | this file | **written** |
| peer2 / security / memory | optional: `s71_peer2.oot`, `s71_security.oot`, `s71_memory.oot` | not required this pass |

No rogue names (`s7.1_*.oot`, `71_*.oot`, `guard_scan_hygiene.oot`,
`secret_71_*`) on disk.

Line pressure on audits (informational; ban is on touched `.oo`):

| File | Lines |
|------|------:|
| `s71_harness.oot` | 37 |
| `s71_domain_a.oot` | 155 |
| `s71_domain_b.oot` | 230 |
| `s71_ideas.oot` | 237 |
| `s71_integration.oot` | 233 |
| `s71_peer1.oot` | 244 |
| `s71_phalanx_rollup.oot` | 175 |
| `s71_hygiene.oot` | this |

Integration tip claim `eaa555d` is **not** in this `ooda` object store.
Live tip is **`52cc4b5`**. Hygiene does not rewrite peer audits.

## ACTIONS / NON-ACTIONS THIS PASS
| Action | Result |
|--------|--------|
| Measure OWN monofile locks | **done** — hard 0 over 600 |
| Force-split DE >600 | **not needed** |
| Flag adjacent soft residual | secret 259 / secret_prop 262 |
| Scrub extra ELF + oodac scratch | **done** |
| Keep product `oodac/oodac` | **yes** (smoke host) |
| `bash -n` s71 smoke | **PASS** |
| Product guard/emit logic edits | **none** |
| Flip SPRINT 7.1 | **no** (peers/coordinator) |
| Push | **no** |
| Audit write | **this file** |

## RETURN — COMPACT
```
SCOPE   7.1 c_guard_scan_idents  ooda=52cc4b5+WIP  openOODA=81f56fc+WIP
OWN     c_emit_secret_guard.oo=200  (DE A no edit; ALREADY_FIXED prove)
>600    0 touched / 0 oodac product   PASS hard ban
>300    0 OWN
>256    0 OWN; adjacent secret 259 / secret_prop 262 WARN residual
BINARY  extra ELF bak/x/fuzz DELETED; product oodac KEPT (gitignored)
SCRUB   oodac a.oo/b.oo + *.tmp.c + super oodac_new.tmp.c
CREDS   PASS  no live secrets; dummy "s3cret" is fixture only
/tmp    PASS product; WARN smoke dummy write path
BASHN   s71_guard_scan_smoke.oo OK (123)
NAMES   s71_harness / domain_a / domain_b / ideas / integration
        / peer1 / phalanx_rollup / hygiene  consistent
PUSH    no
OVERALL PASS
```

end of s71_hygiene.oot
