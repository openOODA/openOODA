# s72_harness.oot — 7.2 c_sys_exec_last_arg path-A harness pointer
# Date: 2026-08-12
# Scope: SPRINT 7.2 smoke rails only (no Domain Expert product land)

## Harness

| Item | Path |
|------|------|
| Smoke | `ooda/scripts/s72_sys_exec_last_arg_smoke.sh` |
| Binary | `OODAC_BIN` or `ooda/oodac/oodac` |
| Fixtures | ephemeral under `$TMPDIR/s72_sys_exec_last_arg_$$` (not repo root) |
| Product | `ooda/oodac/c_emit_args.oo` (`c_sys_exec_last_arg` — do not rewrite) |

## Rails

| Kind | Case |
|------|------|
| PASS fail-closed | `// SECRET: tok` then `sys_exec(sys, "sh", "-c", tok)` (SECRET last argv IDENT) |
| PASS no-FP | quoted last arg containing commas / secret-looking text: `sys_exec(sys, "sh", "-c", "echo hello, tok, SECRET")` |

Exit 0 only if all rails hold.

## Run

```
cd /home/jeryd/Projects/openOODA/ooda
OODAC_BIN=./oodac/oodac bash scripts/s72_sys_exec_last_arg_smoke.sh
```

## Residual (documented, not a fail)

- **Full IFC / `#[Secret]` / interp `${}`:** path-A last-arg ident refuse only; not DESIGN taint.
- **7.10:** `c_parse_secret_env` still char-by-char name concat (separate OWN).
- **Slice hygiene:** product already `str_slice` last span; this harness proves emit refuse/no-FP, not allocator math.

end of s72_harness.oot
