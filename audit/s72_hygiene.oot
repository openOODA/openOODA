# s72_hygiene.oot — Hygiene Enforcer (7.2 c_sys_exec_last_arg phalanx)
# Date: 2026-08-12T18:50:13Z
# Role: Dependency/Hygiene Enforcer — RULES §1.7 (7.2 phalanx)
# Tree: /home/jeryd/Projects/openOODA  (ooda/ + openOODA/)
# Tip (ooda): 52cc4b5 + WIP (`scripts/s72_sys_exec_last_arg_smoke.oo` ??;
#              `c_emit_args.oo` +2 comment lines from runtime auditor)
# Tip (openOODA): 81f56fc + WIP s72_* audits + SPRINT 7.2 FIXED (Docs/Coordinator)
# OWN: line-lock 7.2-touched `.oo`; bash -n `s72_sys_exec_last_arg_smoke.oo`;
#      no ELF / scratch in `oodac/`; consistent `audit/s72_*.oot`; no credentials
# WRITE: this audit + hygiene-only scrub of leftover toys / extra ELF +
#        `.gitignore` seal for `oodac/*.orig` `*.rej` `test_slice.oo`
# POLICY: RULES §1.7 / PROBE MAX_LINES = 256 (soft); pack watch 300; hard ban >600
#         No mass product peels; force-split only if DE blew 600
## SUMMARY
| Check | VERDICT | Notes |
|-------|---------|-------|
| Hard ban >600 (touched .oo) | **PASS** | 0 OWN / 0 `ooda/oodac/**/*.oo` over 600 |
| Soft lock ≤256 (7.2 OWN) | **PASS** | `c_emit_args.oo` **240** |
| Pack pressure ≤300 (OWN) | **PASS** | 240 < 300 |
| Adjacent secret family soft | **WARN** residual | `c_emit_secret.oo` **264**, `c_emit_secret_prop.oo` **262** — pre-existing / 7.10; DE A **did not** grow last-arg |
| DE monofile blow >600 | **none** | Domain A: **no product edit** (ALREADY_FIXED prove); +2 comments only |
| ELF extra / scratch | **PASS** (scrubbed) | deleted `oodac_new`, super `oodac_bin`/`oodac_bin_new`/`test_pthread`, scratch ELF + emit dumps |
| Product host ELF | **kept** | gitignored `oodac/oodac` **962960** B — required by smoke; not a DE toy |
| LLM / patch toys | **PASS** (scrubbed) | `oodac/*.orig` `*.rej` `*.o9DRCOL` `test_slice.oo` + root patch diffs |
| Credentials | **PASS** | ZERO live secrets/tokens/keys |
| `/tmp` in product `.oo` | **PASS** | ZERO in 7.2 OWN |
| Smoke `/tmp` | **PASS** | harness uses `$TMPDIR/s72_sys_exec_last_arg_$$` + `trap` |
| `bash -n` s72 smoke | **PASS** | `scripts/s72_sys_exec_last_arg_smoke.oo` 111L syntax clean |
| `audit/s72_*.oot` names | **PASS** | consistent `s72_<role>.oot` set |
| Mass deletes of product | **none** | only untracked/ignored debris |
| Push | **no** | charter |

OVERALL: **PASS**
  (hard monofile green; extra ELF + oodac scratch scrubbed; names consistent;
   no credentials; soft residual only on pre-existing adjacent secret units)

## SCOPE + METHOD
7.2 ACTIVE (SPRINT backlog): CRIT `c_sys_exec_last_arg` stack buffer /
no per-char OoStr leak — `ooda/oodac/c_emit_args.oo`.

OWN units this pass:

| Path | Role in 7.2 | Git |
|------|-------------|-----|
| `oodac/c_emit_args.oo` | last-arg walker + `str_slice` | dirty (+2 honesty comments; no logic) |
| `scripts/s72_sys_exec_last_arg_smoke.oo` | harness rails (TMPDIR fixtures) | `??` untracked |
| `audit/s72_*.oot` | phalanx evidence | `??` / this file |

Adjacent observe (not dirty for last-arg logic; line-lock only if DE had blown 600):

| Path | Lines | Note |
|------|------:|------|
| `oodac/c_emit_secret_guard.oo` | 228 | sole caller (`c_secret_emit_guard` sys_exec) |
| `oodac/c_emit_secret.oo` | 264 | 7.10 `c_parse_secret_env` still OPEN |
| `oodac/c_emit_secret_prop.oo` | 262 | T1/T2 prop (pre-existing soft) |
| `oodac/c_emit_secret_alias.oo` | 157 | alias resolve |
| `oodac/main.oo` | 307 | pre-existing soft/pack residual |

DE A (`s72_domain_a.oot`): **no source change** — path-A slice already landed
(`str_slice` L125; `t1_memory.oot` M-SG3). Integration ran harness smoke
**exit 0** (not re-run here; hygiene bar is `bash -n` + tree locks).

Commands:

  `wc -l` + hard/pack/soft gates on OWN + full `oodac/*.oo` >600 inventory
  `git add -n oodac/` before+after scrub
  `bash -n` s72 smoke
  `grep` credential + `/tmp` on OWN + smoke + s72 audits
  delete extra ELF + scratch toys; **keep** product `oodac/oodac`
  no push; no product logic rewrite; no SPRINT flip

Thresholds:

| Threshold | Meaning |
|-----------|---------|
| **256** | RULES §1.7 / PROBE MAX_LINES — soft lock |
| **300** | Pack monofile pressure (flag for DE peel) |
| **600** | Path-A hard ban — force-split only if blown |

---
LINE LOCK — 7.2-touched / adjacent monofiles (live)
---

Measured under monorepo tip ooda=`52cc4b5`+WIP (2026-08-12T18:50Z).

| File | Lines | ≤256 | ≤300 | ≤600 | HEAD dirty? | Split? |
|------|------:|:----:|:----:|:----:|:-----------:|:------:|
| `oodac/c_emit_args.oo` | **240** | PASS | PASS | PASS | +2 comments | NO |
| `oodac/c_emit_secret_guard.oo` | **228** | PASS | PASS | PASS | concurrent 7.1/2.10 | NO |
| `oodac/c_emit_secret.oo` | **264** | OVER | PASS | PASS | concurrent 7.10/2.10 | residual |
| `oodac/c_emit_secret_prop.oo` | **262** | OVER | PASS | PASS | no | residual |
| `oodac/c_emit_secret_alias.oo` | **157** | PASS | PASS | PASS | no | NO |
| `oodac/main.oo` | **307** | OVER | OVER | PASS | no | residual |
| `scripts/s72_sys_exec_last_arg_smoke.oo` | **111** | PASS | PASS | PASS | NEW | n/a |
| Aggregate | Value |
|-----------|------:|
| Touched `.oo` over **600** | **0** |
| `oodac/**/*.oo` over **600** | **0** |
| 7.2 OWN over **256** | **0** (`c_emit_args.oo` = 240) |
| Adjacent over **256** | **3** (secret 264 / secret_prop 262 / main 307 — pre-existing) |

md5 (stable re-read at measure):

| File | md5 |
|------|-----|
| `c_emit_args.oo` | `45716b09bf7366e59831fa8dceef761e` |
| product `oodac/oodac` | `a44ee35eb344837009697e2c9d4e4662` (962960 B, BuildID `a93cfd09…`) |

Delta honesty:

- **No DE product land this wave.** Domain A proved existing `str_slice`
  last-span (L125); did not grow the monofile for logic.
- **Runtime auditor** added two comment lines (240 vs s71-era 238) documenting
  `oo_str_slice×1` / 0 concat. Still under soft 256. Not a peel trigger.
- **Hard ban holds** for entire `ooda/oodac/*.oo` product set (count_over_600 = 0).
- Soft residual on `c_emit_secret*.oo` / `main.oo` is **not** a 7.2 blow —
  do not force-peel.

## SPLIT GATE
Rule: force-split only if OWN unit >600 (path-A hard ban).

| Unit | Over hard? | Action |
|------|:----------:|--------|
| `c_emit_args.oo` 240 | no | **none** |
| adjacent secret 264 / 262 | no hard | residual — 7.10 OWN may peel later |
| `main.oo` 307 (pre-existing) | no hard | not 7.2-touched |


*(Continued in `openOODA/audit/s72_hygiene_part2.oot`)*

end of s72_hygiene.oot (part 1)
