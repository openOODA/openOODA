# s72_integration.oot — Compiler Integration Tester (SPRINT 7.2)
# Date: 2026-08-12T18:47:50Z
# Worker: Grok Build (Compiler Integration Tester — 7.2)
# Cwd: /home/jeryd/Projects/openOODA/ooda
# Scope: live tip oodac last-arg + secret rails after 7.2 `c_sys_exec_last_arg` (str_slice, no per-char OoStr)
# Policy: no push; no seed_pure_multi; run listed smokes (90s each); run Domain/Harness extras if present
## Verdict (honest)
| Layer | Result | Exit | Notes |
|-------|--------|------|-------|
| **`secret_t1_return_smoke.oo`** | **PASS** | **0** | return / alias / if-taint refuse+allow + CI wire |
| **`secret_t2_depth_smoke.oo`** | **PASS** | **0** | field/method/args/dlopen/**sys_exec argv**/comment-skip + CI wire |
| **`s71_guard_scan_smoke.oo`** (7.1 regression) | **PASS** | **0** | println IDENT refuse + write_file payload refuse + string-lit no-FP |
| **`s72_sys_exec_last_arg_smoke.oo`** (Harness extra) | **PASS** | **0** | last-arg IDENT refuse + quoted commas/SECRET-spelling no-FP |
| **sys_exec secret fixtures** (extra emit-c) | **PASS** | **1/0** | `secret_sys_exec_{fail,pass}` + argv pair: refuse/allow as specified |
| **`scripts/s72_*` besides last-arg** | **SKIP** | — | only `s72_sys_exec_last_arg_smoke.oo` present |
| **Push** | **NOT DONE** | — | human-gated |

**7.2 path-A last-arg claim:** `c_sys_exec_last_arg` is live on tip host source (`c_emit_args.oo` L71–129). One `str_slice(args, last_start, n)` for the last top-level span — no `out = out + c` in the last-arg extract. Fail-closed last IDENT + quoted-comma no-FP + T1/T2 secret matrices **GREEN**. 7.1 ident-scan regression **GREEN**.

**Assigned script battery:** **GREEN** (3 of 3 listed + 1 Harness extra + 4 sys_exec secret fixtures). No RED emit refuse/allow on this pass.

**Overall 7.2 integration status: GREEN** (assigned rails) / **not closed** (7.10 parse-env concat; full IFC; CI does not yet wire s72)

Do **not** greenwash: residual 7.10 `c_parse_secret_env` char concat remains OPEN; `c_args_strip_cr` still per-char concat (8.6, **not** last-arg); full IFC / `#[Secret]` / `${}` interp not claimed. `ci_product.oo` wires T1/T2 only — **not** s71/s72 smokes.

---

## TIP SHA / binary
```
git rev-parse --short HEAD → 52cc4b5
git log -1 --oneline       → 52cc4b5 fix(r2): multi-import typecheck path-A + digit-0 import names
date -u                    → 2026-08-12T18:47:50Z
```

| Item | Value |
|------|--------|
| Product tip | `ooda/oodac/oodac` **962960** bytes @ 2026-08-12 07:49 local |
| md5 | `a44ee35eb344837009697e2c9d4e4662` |
| BuildID | `a93cfd09506ba87497379f66b6c7047dee02573d` |
| Side / older | `oodac/oodac` 962888 (07:43); `ooda/oodac_new` 956688 (03:56) — **not** used |
| Seed | `bootstrap/seed/oodac` present — pure multi **not** re-run this pass |
| Host used | `OODAC_BIN=/home/jeryd/Projects/openOODA/ooda/oodac/oodac` |
| Working tree | dirty: `c_emit_secret*.oo` + untracked `scripts/s71_guard_scan_smoke.oo`, `scripts/s72_sys_exec_last_arg_smoke.oo` |

---

## 0) DE / Harness landing observed
| Path | Role |
|------|------|
| `ooda/oodac/c_emit_args.oo` (238) | `c_sys_exec_last_arg` — CRIT 7.2 comment + `str_slice` last span |
| `ooda/oodac/c_emit_secret_guard.oo` (228) | sole last-arg **caller** (`sys_exec` sink L130–140) |
| `ooda/scripts/s72_sys_exec_last_arg_smoke.oo` | Harness extra (landed this session); ephemeral TMPDIR fixtures |
| `openOODA/audit/s72_harness.oot` | harness pointer |
| `openOODA/audit/s72_domain_a.oot` | Domain A **ALREADY_FIXED** prove (no source rewrite) |
| `ooda/scripts/secret_{t1,t2}_*.oo` | listed T1/T2 integration rails |
| `ooda/scripts/s71_guard_scan_smoke.oo` | 7.1 regression |
| `ooda/fixtures/secret_sys_exec_{fail,pass,argv_*}.oo` | product-floor sys_exec secret fixtures |

No other `scripts/s72_*`. Only extra: `scripts/s72_sys_exec_last_arg_smoke.oo`.

Source (tip) — 7.2 slice, not char concat:

```
// CRIT 7.2: one str_slice for the last arg span — no per-char OoStr thrash.
if last_start >= n {
    return args;
}
let out = str_slice(args, last_start, n);
```

Same file L188 `out = out + c` is **`c_args_strip_cr`** (8.6 CR strip), **not** 7.2.

---

## 1) Primary smokes — exit codes
All runs: `cd ooda && OODAC_BIN=./oodac/oodac` ; `timeout 90s`. Logs under `/tmp/s72_integration/`.

### 1a) secret_t1_return_smoke — exit 0
```
OK secret_function_return_fail emit refuses (rc=1)
OK secret_function_return_pass emit OK
OK secret_alias_rebind_pass emit refuses (rc=1)
OK secret_alias_rebind_fail emit OK (sticky cleared)
OK secret_if_taint_fail emit refuses (rc=1)
OK secret_if_taint_pass emit OK
OK ci_product wires secret_t1_return_smoke
OK secret_t1_return_smoke
```

Log: `/tmp/s72_integration/secret_t1_return_smoke.out`

### 1b) secret_t2_depth_smoke — exit 0 (includes sys_exec argv)
```
OK secret_field_assign_fail emit refuses (rc=1)
OK secret_field_assign_pass emit OK
OK secret_method_return_fail emit refuses (rc=1)
OK secret_method_return_pass emit OK
OK secret_call_arg_fail emit refuses (rc=1)
OK secret_multi_arg_println_fail emit refuses (rc=1)
OK secret_multi_name_fail emit refuses (rc=1)
OK secret_multi_name_pass emit OK
OK secret_dlopen_arg_fail emit refuses (rc=1)
OK secret_dlopen_arg_pass emit OK
OK secret_sys_exec_argv_fail emit refuses (rc=1)
OK secret_sys_exec_argv_pass emit OK
OK secret_midline_safe emit OK
OK comment-skip fixture shape (no line-start SECRET directive)
OK ci_product wires secret_t2_depth_smoke
OK secret_t2_depth_smoke
```

Log: `/tmp/s72_integration/secret_t2_depth_smoke.out`

### 1c) s71_guard_scan_smoke (7.1 regression) — exit 0
```
OK fail-closed: secret_tok_println_ident
OK fail-closed: secret_tok_write_file_payload
OK no-FP: secret_tok_println_string_lit
s71_guard_scan_smoke: ALL OK
RESIDUAL full IFC / #[Secret] / string-interp ${}
RESIDUAL 7.10: c_parse_secret_env still char-by-char name concat
RESIDUAL interproc SECRET beyond path-A __fr_secret__
```

Log: `/tmp/s72_integration/s71_guard_scan_smoke.out`

### 1d) s72_sys_exec_last_arg_smoke (Harness extra) — exit 0
`bash -n` **OK** (111 lines).

```
OK fail-closed: secret_tok_sys_exec_last_ident
OK no-FP: secret_tok_sys_exec_last_quoted_commas
s72_sys_exec_last_arg_smoke: ALL OK
RESIDUAL full IFC / #[Secret] attr / string-interp ${} deep taint — not claimed closed
RESIDUAL 7.10: c_parse_secret_env still char-by-char name concat (not this OWN)
RESIDUAL last-arg slice hygiene is product (c_sys_exec_last_arg); this smoke proves emit rails only
```

Log: `/tmp/s72_integration/s72_sys_exec_last_arg_smoke.out`

### 1e) sys_exec secret fixtures (extra emit-c, 90s each) — all match
| Fixture | Want | Exit | `ERR	secret` |
|---------|------|------|--------------|
| `secret_sys_exec_fail.oo` | refuse last IDENT | **1** | **yes** (`sys_exec sink refuses SECRET token`) |
| `secret_sys_exec_pass.oo` | allow quoted last | **0** | no |
| `secret_sys_exec_argv_fail.oo` | refuse mid-argv IDENT | **1** | **yes** |
| `secret_sys_exec_argv_pass.oo` | allow literal argv | **0** | no |

Logs: `/tmp/s72_integration/secret_sys_exec_{fail,pass,argv_fail,argv_pass}.{out,rc}`

Note: refuse path still emits C preamble then `ERR	secret` (same as T1/T2). Smokes grep the diagnostic, not “no C at all.”

---

## 2) Exit-code scoreboard
| Script | Timeout | Exit | Color |
|--------|---------|------|-------|
| `ooda/scripts/secret_t1_return_smoke.oo` | 90s | **0** | GREEN |
| `ooda/scripts/secret_t2_depth_smoke.oo` | 90s | **0** | GREEN |
| `ooda/scripts/s71_guard_scan_smoke.oo` | 90s | **0** | GREEN |
| `ooda/scripts/s72_sys_exec_last_arg_smoke.oo` | 90s | **0** | GREEN |
| extra `secret_sys_exec_fail` emit-c | 90s | **1** (refuse) | GREEN |
| extra `secret_sys_exec_pass` emit-c | 90s | **0** | GREEN |
| extra `secret_sys_exec_argv_fail` emit-c | 90s | **1** (refuse) | GREEN |
| extra `secret_sys_exec_argv_pass` emit-c | 90s | **0** | GREEN |
| other `ooda/scripts/s72_*` | — | **SKIP** | — |

Listed 3 + Harness extra: **4 GREEN / 0 RED / 1 SKIP class**.

---

## 3) What GREEN is (and is not)
| Claim | Status |
|-------|--------|
| 7.2 `c_sys_exec_last_arg` defined on tip | **YES** (`c_emit_args.oo` L71–129) |
| Last-arg extract is `str_slice` (no `out = out + c`) | **YES** (L121–125) |
| Last-arg IDENT refuse (`sys_exec(..., tok)`) | **YES** (s72 fail-closed + `secret_sys_exec_fail`) |
| Quoted last-arg commas / SECRET spelling no-FP | **YES** (s72 no-FP rail) |
| T2 sys_exec argv mid-position refuse | **YES** (T2 + extra fixture) |
| T1 return / alias / if taint | **YES** (exit 0) |
| 7.1 ident-scan regression | **YES** (s71 exit 0) |
| `ci_product.oo` wires s72 last-arg smoke | **NO** — T1/T2 only |
| 7.10 `c_parse_secret_env` char concat | **OPEN** (not this OWN) |
| `c_args_strip_cr` per-char concat | **OPEN** (8.6; not 7.2) |
| Full IFC / `#[Secret]` / `${}` | **residual** |

---

## 4) Follow-ups (not this worker)
1. Wire `s72_sys_exec_last_arg_smoke.oo` (and s71) into `ci_product.oo` if product wants last-arg rails in CI. Harness scripts are **untracked** until human commit.
2. 7.10 `c_parse_secret_env` remains next residual-queue CRIT/HIGH (handoff).
3. Do not treat 8.6 `c_args_strip_cr` concat as a 7.2 last-arg regression.

---

## Evidence paths
| Item | Path |
|------|------|
| This audit | `/home/jeryd/Projects/openOODA/openOODA/audit/s72_integration.oot` |
| Harness pointer | `/home/jeryd/Projects/openOODA/openOODA/audit/s72_harness.oot` |
| Domain A prove | `/home/jeryd/Projects/openOODA/openOODA/audit/s72_domain_a.oot` |
| Last-arg source | `/home/jeryd/Projects/openOODA/ooda/oodac/c_emit_args.oo` |
| Guard caller | `/home/jeryd/Projects/openOODA/ooda/oodac/c_emit_secret_guard.oo` |
| Smoke logs | `/tmp/s72_integration/*.out` + `*.rc` |
| Prior 7.2 memory claim | `openOODA/audit/t1_memory.oot` (M-SG3) |
| Prior 7.1 integration | `openOODA/audit/s71_integration.oot` |

end of s72_integration.oot
