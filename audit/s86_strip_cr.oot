# s86_strip_cr.oot — leftover 8.6 c_args_strip_cr keep-run slice. No push. No SPRINT edit.
# Date: 2026-08-12T20:42Z  Role: Domain Expert leftover 8.6  OWN: oodac/c_emit_args.oo only
# VERDICT: FIXED path-A. Was `out = out + c`. Now keep-run `str_slice` (same family as 7.10).
# File: /home/jeryd/Projects/openOODA/ooda/oodac/c_emit_args.oo 240→254 (≤256). md5 f1b0f2e50a04bd877d57351cac0a36c0
# Filter kept: drop bare CR byte 13 and C `\r` escape. Never `c == "\r"` (Ooda "\r" is letter r).
# Clean argv (no CR): return original — 0 concat. Hits: join slices of kept runs only.
# Residual: run-join `out + str_slice` when CRs exist (filter is not one span). char_at time tax.
# Untouched: 7.2 last-arg / drop-first. Call-in c_emit_lower.oo sys_exec rest (twice). No SPRINT. No push.
# Prove: no `out = out + c`; emit-c uses oo_str_slice join; gcc+run rails OK (true/oo_str_lit/esc/bare CR/CRLF).
# STOP. Host ELF not rebuilt. Lexer ident concat still later residual.
