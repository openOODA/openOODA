# Shutdown handoff — 2026-08-12
## Safe to stop
Swarm agents for CAP-G3 were cancelled after partial close. No remote push was performed.

## Closed this session (path-A)
| ID | Commit / tip | Prove |
|----|--------------|-------|
| T1–T5 | prior session | power-law board closed |
| **CAP-G1** | ooda `c2da895` | `cap_g1_net_granular_smoke` PASS |
| **CAP-G2** | ooda `0cbc4aa` | `cap_g2_fs_granular_smoke` PASS |
| **CAP-G3 inject** | ooda CAP-G3 land | `cap_g3_grant_inject_smoke` PASS (after pure multi install) |

## CAP-G3 honesty
- **Landed:** ambient inject `http/tcp/udp/bind/fsread/fswrite/process` + type-driven rebind for arbitrary main param names; preamble decls for V2 grants.
- **Residual open:** HW/UI tokens have grants but **no product sealed ops**; ProcessCap vs SysCap = CAP-G4; seccomp = CAP-G5; std migration = CAP-G6.

## Floors at handoff
Re-run before next work:

```bash
cd ooda
bash scripts/cap_g1_net_granular_smoke.oo
bash scripts/cap_g2_fs_granular_smoke.oo
bash scripts/cap_g3_grant_inject_smoke.oo
```

All three **PASSED** at handoff on product host after pure multi install (~958k B).

## Dirty trees (do not ship blind)
- **ooda:** many unrelated `oodac/*.oo` modifications (emit/tc/bc WIP) — **not** in CAP-G commits; leave parked.
- **openOODA:** md→oot migration mid-flight; CAPS residual honesty edits may be dirty.
- **std:** ahead origin with honesty batches (T5); push human-gated.

## Next pick (when resuming)
1. CAP-G4 ProcessCap / SysCap honesty
2. CAP-G5 seccomp residual honesty
3. Human push/PR slices per `audit/t5_ship_readiness.oot`
4. Clean pure multi on non-WIP tree if secret T1/T2 smokes red

## Push policy
**Do not auto-push.** Owner decides PR stacks for ooda / std / openOODA.
