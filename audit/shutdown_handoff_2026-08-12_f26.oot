# Shutdown handoff — 2026-08-12 (power)
# No push. Do not claim beta.

## Pin

| Item | Value |
|------|--------|
| Release | v0.184.0-alpha |
| ooda tip | 52cc4b5 (dirty, ~79 paths) |
| boards tip | 81f56fc (dirty, ~177 paths) |
| Product ELF | `ooda/oodac/oodac` **972048 B** md5 **`3fb94504bdd690dfd15d9460e471798b`** = `/tmp/oodac_f26e` |
| Do not use | `016a397d` (agy overwrite; we restored f26e) |

**In plain words:** The compiler on disk is the cheap-scan + 2.9 host. Big-tree check finishes. Secret grouped refuse still works.

## Closed this session (removed from SPRINT)

- **F26** p7=0 (58.2s, check rc=1 type ERR, not hung) + s29=0 + light=0. Ledger: `audit/f26_close.oot`
- **F27** no `oodac/*.oo` >256 (Peer2 ACCEPT)
- F44 `.md`→`.oot` greps dual-green. F45 process.oo restated. F46 extra `}` gone.
- Honesty: F19 RFC-003, F20 inventory, F23 MUST-GATE, F34/F36 PM, F39 md5/sha1, F35 string, F40 AES exit, F8 RFC-004, F1 DESIGN, F43=F12
- Hygiene F15–F18/F28/F29. I17 duplicate `lex_all` dropped in **source** (`check_drive.oo`) — not in this ELF yet

## Residuals (do next)

1. **Guard the ELF.** agy/crush overwrite `oodac/oodac`. Restore from `/tmp/oodac_f26e` if md5 ≠ `3fb94504`.
2. **Bool/String 36:5** — TC ignores `!=` after a String call. Rewrote `is_sealed_net/fs/sys` to if/true/false. Same class may remain. Ledger: `audit/f26_bool_err.oot` + `f26_bool_iftrue.oot`
3. **2.10** user-sink walk stays **off** (made check >180s). Cheap marker may be mid-flight. Residual: `fn leak(s){println(s)}`
4. **Loop2 RSS ~2.4 GiB** (bounded). I17 source cut not in ELF.
5. **RFC-002** Args/Walk still not shipped.
6. Moonshots / SPRINT §4 — do not do.

## Do not

- Do not push unless asked.
- Do not run 100-wide `oodac_pure_build_fixed` (load 80–105). Sequential seed emit only.
- Do not re-add `c_secret_mark_user_sinks` in check.
- Do not close F26 again as a rebuild — it is closed on this ELF.

## Re-prove after boot

```
md5sum ooda/oodac/oodac          # want 3fb94504…
cd ooda && bash scripts/p7_check_main_smoke.sh   # expect ALL OK ~58s
bash scripts/s29_println_guard_smoke.sh          # expect ALL OK
bash scripts/scan_fn_light_smoke.sh
```

end of shutdown_handoff_2026-08-12_f26.oot
