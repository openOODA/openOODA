# SPRINT 12 HIGH — independent re-verify
# Native FS / Env / process args / typestate
Marker: TYPESTATE_FS_AUDIT_REQUEST_2026-08-13
Date: 2026-08-13
Role: grok, audit only. No product edits.
Axiom: RULES 1.9 zero trust. Re-read live files. Do not copy the prior audit.

Scope
  Live tree is /home/jeryd/Projects/openOODA/ooda (not openOODA/ooda).
  Read: std/os/fs.oo env.oo process.oo
        runtime/chs_rt_fs.c chs_rt_sys.c chs_rt_str.c
        oodac/check_typestate*.oo check_drive.oo check_mod.oo
        check_cache.oo main.oo load_import.oo
        bootstrap/TYPE_STATE.oot SHIPPED.oot
  Prior file typestate_fs_env_args_audit.oot is a claim list only.
  Did not rebuild ELF. Did not run typestate_check_smoke.oo.

## Verdict
Native facades still wrap `oo_*` and C re-checks caps. DE1.8 env
prefix is the env seal, not a typestate bypass. Typestate runs on
the check path. The prior audit is stale on three line facts and
wrong on np>12 TYPESTATE prepend.

| Surface | Status | Evidence |
|---------|--------|----------|
| FS facade → `oo_*` | PASS | `/home/jeryd/Projects/openOODA/ooda/std/os/fs.oo:16-99` |
| Env facade → `oo_env_get` | PASS | `std/os/env.oo:14-16` |
| process_args → `oo_sys_args` | PASS | `std/os/process.oo:15-17` |
| Runtime FS/env cap re-check | PASS | `chs_rt_fs.c:42,55,69,75,81,100,118,124,134,144,153` + `chs_rt_sys.c:167-180,111-117` |
| Runtime ProcessCap re-check | PASS | `chs_rt_sys.c:190-196,443` |
| Write bound + writedir + O_NOFOLLOW | PASS | `chs_rt_fs.c:20-39,54-66` `PATH_MAX` `OODA_FS_WRITEDIR` `openat` `O_NOFOLLOW` |
| Write mkdir/remove/link/symlink writedir | PASS | `chs_rt_fs.c:123-159` |
| Write silent PATH_MAX truncate | RESIDUAL | `chs_rt_fs.c:20-23` `to_cpath` clamps, no deny |
| Read/probe/size/dir no `to_cpath` | RESIDUAL | `chs_rt_fs.c:43,70,76,102` raw `path.data` |
| `fs_is_dir` 1024 not PATH_MAX | RESIDUAL | `chs_rt_fs.c:119` |
| `fs_walk` no depth cap | RESIDUAL | `std/os/fs.oo:50-76` recurse |
| mkdir/unlink/link/symlink no O_NOFOLLOW | RESIDUAL | `chs_rt_fs.c:130,140,149,158` |
| No landlock/seccomp | RESIDUAL | `std/os/fs.oo:13` names CAP-G5 |
| args 4096 + drop argv0 | PASS + residual | `chs_rt_sys.c:442-467` |
| DE1.8 `env_get` prefix OODA_/OO_ | PASS | `chs_rt_sys.c:119-127` `chs_rt_fs.c:80-91` |
| DE1.8 exec inherit same prefix | PASS | `chs_rt_sys.c:245-283` |
| exec inherit pointer snapshot | RESIDUAL | `chs_rt_sys.c:269` `newenv[n++]=*src` then `clearenv` |
| SPRINT “typestate bypasses DE1.8 via oo_” | PASS (no bypass) | typestate never calls getenv; `oo_` is C ABI |
| Typestate on check product path | PASS | `check_drive.oo:105` `main.oo:26-32` |
| `check_typestate_call.oo` exists | FAIL (stale claim) | no such file under `oodac/`; calls live in `check_typestate_walk.oo:12-50` |
| np>12 prepends TYPESTATE | FAIL | `check_mod.oo:53-86` no `ts_extract`; no prepend |
| Small-graph imports keep directives | PASS | `load_import.oo:76-79` keeps comment lines; `TYPE_STATE.oot:25` |
| Cache key `t_ts3_` | PASS | `check_cache.oo:69-72` |
| Join / compose / Type#fn / method | PASS | parse `check_typestate_parse.oo:115-128`; walk/join/fn/match |
| Real heap alias | RESIDUAL | `TYPE_STATE.oot:41` `No heap alias` |
| Inter-file lattice on np>12 | RESIDUAL | `TYPE_STATE.oot:41`; large path checks one stripped body |
| Join drops aliases | RESIDUAL | `check_typestate_join.oo:51-95` rebuilds only `__tsv__` |
| `ok_env_get` HOME at check vs run | PASS (seal) | corpus `ok_env_get.oo:11` check; run `HOME` → NULL |

## 1. Paths
User list `ooda/std/os/*.oo` under openOODA/openOODA is empty.
Product files:

  /home/jeryd/Projects/openOODA/ooda/std/os/fs.oo
  /home/jeryd/Projects/openOODA/ooda/std/os/env.oo
  /home/jeryd/Projects/openOODA/ooda/std/os/process.oo
  /home/jeryd/Projects/openOODA/ooda/runtime/chs_rt_fs.c
  /home/jeryd/Projects/openOODA/ooda/runtime/chs_rt_sys.c
  /home/jeryd/Projects/openOODA/ooda/oodac/check_typestate.oo
  /home/jeryd/Projects/openOODA/ooda/oodac/check_typestate_parse.oo
  /home/jeryd/Projects/openOODA/ooda/oodac/check_typestate_tok.oo
  /home/jeryd/Projects/openOODA/ooda/oodac/check_typestate_walk.oo
  /home/jeryd/Projects/openOODA/ooda/oodac/check_typestate_join.oo
  /home/jeryd/Projects/openOODA/ooda/oodac/check_typestate_fn.oo
  /home/jeryd/Projects/openOODA/ooda/oodac/check_typestate_match.oo
  /home/jeryd/Projects/openOODA/ooda/oodac/check_drive.oo
  /home/jeryd/Projects/openOODA/ooda/oodac/check_mod.oo
  /home/jeryd/Projects/openOODA/ooda/oodac/check_cache.oo
  /home/jeryd/Projects/openOODA/ooda/oodac/main.oo

No `check_typestate_call.oo`. No `ts_extract_dir_src` anywhere under ooda/.

## 2. Native FS
`fs.oo` is a thin wrap. Read/probe/walk take `&FsReadCap`. Write/mkdir/remove/link/symlink take `&FsWriteCap`. Each call is one `oo_*`.

C (`chs_rt_fs.c`):

  Every op calls `oo_cap_require_fsread` or `oo_cap_require_fswrite`.
  Those accept exact granular token or full FsCap (`chs_rt_sys.c:165-180`).
  Writes call `to_cpath`, then `oo_process_policy_getenv("OODA_FS_WRITEDIR")`,
  then `path_under_writedir`. `/` as writedir is refused (`:26`).
  File write opens via `openat` + `O_CREAT|O_TRUNC|O_CLOEXEC|O_NOFOLLOW` (`:38`).

Read path is weaker than write path:

  `oo_read_file` `fopen(path.data, "rb")` (`:43`) — no `to_cpath`, no PATH_MAX.
  `oo_path_exists` same (`:70`).
  `oo_file_size` same (`:76`).
  `oo_fs_read_dir` `opendir(path.data)` (`:102-103`).
  Owned `OoStr` from `oo_str_alloc_payload` does write `data[len]=0`
  (`chs_rt_str.c:9`). Product strings are C strings today.
  Embedded NUL still shortens the fopen path. NULL `path.data` is UB.
  That is the named residual. Not closed.

`oo_fs_is_dir` uses `to_cpath(..., 1024)` not `PATH_MAX` (`:119`).
Long dir paths truncate.

`to_cpath` on write: if `p.len >= max` it copies `max-1` and does not
fail (`:20-22`). Two long paths that share a prefix can collide.

`fs_mkdir` / `fs_remove_file` / `fs_hardlink` / `fs_symlink` check
writedir then call `mkdir`/`unlink`/`link`/`symlink` on the C path.
No `O_NOFOLLOW`. Last-component TOCTOU is residual. `fs.oo:13`
already says no landlock/seccomp.

`fs_walk` is OODA, not C. Recurse with `fs_is_dir` + `fs_read_dir`.
No depth cap (`fs.oo:50-76`). Deep tree can blow the stack.

SHIPPED lists `fs_walk(FsReadCap)` and the sealed file ops. Match.

## 3. Env and DE1.8
`env_get` does not recurse. It returns `oo_env_get(cap, key)` (`env.oo:15`).

`oo_env_get` (`chs_rt_fs.c:80-91`):
  1. `oo_cap_require_env`
  2. `oo_process_policy_getenv(key.data ? key.data : "")`

`oo_process_policy_getenv` (`chs_rt_sys.c:121-127`):
  empty key → NULL
  key must start `OODA_` (5) or `OO_` (3)
  else NULL
  then `getenv(key)`

`OODA_` is a subset of `OO_`. Dual test is redundant. Filter is real.

`oo_sys_exec` child env (`chs_rt_sys.c:245-283`):
  Walk parent `environ`. Keep entries whose key prefix is `OODA_` or `OO_`.
  Snapshot is pointer copy, not `strdup` (`:269`).
  Then `clearenv` (glibc/Apple) or `environ[0]=NULL`.
  Then `environ = newenv` and `setenv("PATH", "/usr/bin:/bin", 1)`.
  Prefix seal holds. Pointer snapshot is residual: glibc `clearenv`
  can free `setenv`-allocated strings; those pointers can dangle.

SPRINT line 107 says verify “typestate is correctly bypassing DE1.8
filtering via `oo_` prefixes”. Live fact: no bypass.

  Typestate reads source comments and tokens. It does not read env.
  `oo_env_get` is the C symbol so the OODA wrap does not recurse and
  so emit lowers to the sealed op. The env key filter is still applied.
  `ok_env_get.oo:11` calls `env_get(env, "HOME")`. Check can pass
  (EnvCap in scope). At run, `HOME` is not `OODA_`/`OO_`, Result is Err.
  That is the seal.

`oo_hide.c:17` uses raw `getenv("OODA_HIDE")`. Internal, not product
`env_get`. Key would pass the prefix anyway.

`std/sh.oo:104-108` `sh_env_get` is `process_exec(..., "printenv "+key)`.
Child env is already DE1.8-filtered, so `printenv HOME` is empty.
Not a native env bypass. Residual: shell concat on `key`.


*(Continued in `openOODA/audit/sprint12_typestate_fs_verify_part2.oot`)*

end of sprint12_typestate_fs_verify.oot (part 1)
