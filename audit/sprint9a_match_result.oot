# sprint9a_match_result.oot — SPRINT 9a leftover. DE2. 2026-08-13.
# RULES 1.3 Honesty, 1.6 Proof, 1.9 zero trust. No product patch. No SPRINT edit.
# No seed/oodac replace. Language items deferred.
OWN: 9a leftover — "seed emit match Result still residual".
GATE: patch oodac/c_emit_match_stmt.oo ONLY if live emit-c of match Result is broken in product source.
PROOF: read + emit-c live and seed. Tiny fixture. Did not rebuild seed. Did not run ci_product.oo.

## VERDICT
**source OK.** Live product source already lowers match Result to OoResS / OoResV.
Live emit-c of the fixture is rc=0 and prints `OoResS __m = r; if (__m.ok)`.
This seed pin does the same. **No c_emit_match_stmt.oo patch.**

9a is a **SEED BINARY residual** (stale board / older seed / seed rebuild graph),
not a live source bug.

## WHAT I READ
| Path | What |
|------|------|
| ooda/oodac/c_emit_match_stmt.oo (116 lines) | stmt match: env V → OoResV + .err; else OoResS + .val |
| ooda/oodac/c_emit_let.oo:131-168 | Ok/Err → OoResS (R); write_file → OoResV (V) |
| ooda/oodac/c_emit_skip.oo:72-82 | Result[Void,…] → OoResV; else Result → OoResS |
| ooda/oodac/c_emit_match_let.oo | let/assign match uses __ml; same R/V split (not patched) |
| ooda/bootstrap/corpus/emit-c/pass/match_result_stmt.oo | existing stmt rail |
| ooda/bootstrap/corpus/emit-c/pass/match_result_let.oo | existing let rail |
| ooda/fixtures/bc_match_result.oo | VM match helpers (not used as emit rail) |

## FIXTURE
Wrote ooda/fixtures/match_result_seed.oo (18 lines, academy header).
No write_file in the product fixture (cap injection skipped).

    pub fn main() {
        let r: Result[String, String] = Ok("ok");
        match r {
            Ok(x) => println(x),
            Err(e) => println(e),
        }
    }

## COMMANDS + RC
which oodac → not on PATH.

Live ELF: ooda/oodac/oodac
  1036240 B  sha256 cf48b35ee679828af385b013a9477cdc587eaac5c65897ff7cee73f0ee9904d0

Seed ELF: ooda/bootstrap/seed/oodac
  878392 B   sha256 37a99a97c0dce4dd4c6b3a89a55387b3689d568b9cf8f811494ededb92cf47aa
  (did not replace)

Cwd: /home/jeryd/Projects/openOODA/ooda
Each emit timeboxed 60s. None hit the cap.

1. ./oodac/oodac
   rc=0  usage includes emit-c

2. ./bootstrap/seed/oodac
   rc=0  usage includes emit-c

3. timeout 60 ./oodac/oodac emit-c fixtures/match_result_seed.oo
   rc=0  wall=0.00s  rss=6456 KB  stderr empty  stdout 9632 B

4. timeout 60 ./bootstrap/seed/oodac emit-c fixtures/match_result_seed.oo
   rc=0  wall=0.00s  rss=5092 KB  stderr empty  stdout 8133 B

5. timeout 60 ./oodac/oodac emit-c bootstrap/corpus/emit-c/pass/match_result_let.oo
   rc=0  wall=0.01s  `OoResS __ml = ok_r;`

6. timeout 60 ./bootstrap/seed/oodac emit-c bootstrap/corpus/emit-c/pass/match_result_let.oo
   rc=0  wall=0.01s  same `__ml` shape

---
EMITTED C (live fixture, 5 lines)
---

  OoResS r = ((OoResS){1, oo_str_lit("ok")});
  {
  OoResS __m = r;
  if (__m.ok) {
    OoStr x = __m.val;

Seed printed the same five lines. Err arm is `OoStr e = __m.val;` then print.
That is the source in c_emit_match_stmt.oo:47-51.

---
EXTRA PROBE (not a source patch)
---

/tmp write_file match (Result[Void, String], not committed):
  live rc=0  `OoResV r = oo_write_file(...);` / `OoResV __m = r;` / `__m.err`
  seed rc=0  same V shape on this pin

/tmp process_exec match-let (not committed):
  live and seed rc=0 emit `OoResS __ml = process_exec(process, oo_str_lit("true"));`
  process_exec is an .oo facade, not a C symbol. Invalid C if gcc'd.
  That is a **lower/helper** residual, not match-stmt emit. Deferred.

## RESIDUAL
- Live source + live ELF already emit match Result (OoResS / OoResV).
- This seed pin already emits the same for stmt, let, and write_file V.
- SPRINT 9a leftover is still true as a **seed-binary** claim: an older
  seed, or seed emit of a larger graph (oodac/main.oo rebuild), was not
  re-proven here. Did not run bootstrap/oodac_pure_build.
- process_exec as match scrutinee is unlowered on live and seed. Not
  c_emit_match_stmt.oo. Language item deferred.
- Did not patch c_emit_match_stmt.oo. Academy header stays. 116 lines.

Marker: SPRINT9A_MATCH_RESULT_2026-08-13
