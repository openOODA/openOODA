# sprint_de1_mkdir_leftover.oot — leftover sh_mkdir_p(p / sh_copy(p, hunt
# Date: 2026-08-14T13:35:58Z
# Role: DE1. This file only. No product edit. Did not touch std/sh.oo,
#       std/os/fs.oo, SPRINT.oot, or oodac/*.oo.
# Scope: /home/jeryd/Projects/openOODA/ooda **/*.oo except std/sh.oo
# Method: grep --include='*.oo' -R for leftover old-API first-arg `p`.

ZERO leftovers.

`sh_mkdir_p(p` = 0 hits (also `sh_mkdir_p(\s*p`).
`sh_copy(p,` = 0 hits (also `sh_copy(\s*p\s*,` and `sh_copy(p`).

Live callers already use the FsWriteCap / FsReadCap signatures:

- `sh_mkdir_p(fw, …)` — 153 call sites
- `sh_copy(fs, fw, …)` — 8 call sites (m162_residual_deepen_smoke.oo,
  install_dress_rehearsal.oo)
- first arg not `fw`/`fr`/`fs` — 0 (std/sh.oo defs excluded)

No leftover files to patch. No `fw: &FsWriteCap` added.
