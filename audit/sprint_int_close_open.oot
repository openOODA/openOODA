# sprint_int_close_open.oot — INT live prove (close / open)
# Date: 2026-08-14T03:12:06-10:00
# Worker: INT. Cwd: /home/jeryd/Projects/openOODA/ooda
# Write set: this file only. No product .oo. No SPRINT.oot. Seed not replaced.
# No bootstrap/oodac_pure_build. No ci_product.oo. No bootstrap.
# Solo under SWARM: one audit file, live rails only.
Host
  git:     56fcb21 (56fcb214bbcc86660a2fbfa1356b044b818867ae)  main ahead origin/main +1
           dirty tree (academy headers + live std/str.oo parse_int + std/sh.oo + tc_call_let.oo)
  PATH oodac: absent (`command -v oodac` empty)
  ELF:     ./oodac/oodac
  size:    1036240 B  mtime 2026-08-13 07:59:09 -1000
  md5:     4b9099e4c5014e3c74ef4562a8440160
  BuildID: 472f46f45e1dcdec59b9fee81ace31280ce4bd81
  seed:    ./bootstrap/seed/oodac  878392 B  md5 dedee1d87513411fd8eeb2d460e76ff1
           BuildID 7dcbc1551ff1c4fa61099a0fcd72469bf1efa63f  (read-only; not replaced)
  race:    no live bootstrap/oodac_pure_build / ci_product.oo at start

Writers
  Waited 45s then retried missing twins once (2026-08-14T03:11:30-10:00).
  `qa/probe_honesty_tests.oo` **absent** (retry).
  `qa/probe_lifecycle_tests.oo` **absent** (retry).
  `qa/probe_boundary_tests.oo` **absent** (retry).
  `qa/probe_chaos_tests.oo` **absent** (retry).
  `test_collect.oo` **absent** (retry).
  `.oo` twins still on disk (qa 2026-08-12 18:32; test_collect.oo 2026-08-13 04:54).

## Prove table
| # | command | rc | wall | residual |
|---|---------|----|------|----------|
| 1 | `/usr/bin/time -v ./oodac/oodac check oodac/main.oo` | **0** | **26.75s** (user 21.06 sys 5.49) | stdout `OK` (3 B, `4f 4b 0a`). Product stderr empty. Max RSS **12853868 kB = 12.26 GiB**. Same ELF as sprint_int_open_prove.oot (26.30s / 12.26 GiB) and sprint_int_prove.oot (26.05s / 12.25 GiB). Not 1–2s / 39 MB. B8 cost still live. |
| 2 | `/usr/bin/time -v ./oodac/oodac check scripts/ci_product.oo` | **1** | **0.44s** (user 0.41 sys 0.02) | stdout `ERRparseunclosed brace` (25 B). Max RSS 43300 kB. File present (47 lines). Live `when` / `and` / `then` blocks (not comments). Imports `std/scenario.oo` + `std/sh.oo`. Same academy parse class as prior `oodac_check_capture.oo`. This is **check**, not `ci_product.oo`. |
| 3 | `/usr/bin/time -v ./oodac/oodac check std/str.oo` | **0** | **0.09s** (user 0.06 sys 0.02) | stdout `OK` (3 B). Max RSS **58192 kB**. Live dirty `std/str.oo` (131 lines) includes `pub fn parse_int` at line 104. HEAD has no `parse_int` (`+31` vs HEAD). Isolated check **OK**. Not a seed rebuild. |
| 4 | `./oodac/oodac check qa/probe_honesty_tests.oo qa/probe_lifecycle_tests.oo qa/probe_boundary_tests.oo qa/probe_chaos_tests.oo` | **absent** | — | Missing after wait + one retry. `.oo` twins remain. Not run. |
| 5 | `./oodac/oodac check test_collect.oo` | **absent** | — | Missing after wait + one retry. `test_collect.oo` remains (38 lines). Not run. |

ci_product.oo / bootstrap/oodac_pure_build / bootstrap: **not run** (instructed). Seed ELF not touched.

## 1 — compiler proof (THE rail)
```
Command being timed: "./oodac/oodac check oodac/main.oo"
User time (seconds): 21.06
System time (seconds): 5.49
Elapsed (wall clock) time (h:mm:ss or m:ss): 0:26.75
Maximum resident set size (kbytes): 12853868
Exit status: 0
```

stdout last line: `OK`
stderr: empty (GNU time on stderr; product stderr empty)

Verdict: **PASS**. Live check of `oodac/main.oo` finishes, rc=0, 26.75s, 12.26 GiB RSS.
F26 hang stays closed. Residual named: B8 RSS not baseline.

Do not say the check is cheap. Do not say 39 MB.

## 2 — ci_product.oo check (not ci_product.oo)
`scripts/ci_product.oo` is on disk (47 lines, 2733 B, mtime 2026-08-13 05:33).
It is an academy wrapper: `when`/`and`/`then` still live (F26 harness dodge of commenting them is **not** applied here).
`oodac check` → rc=1 `ERRparseunclosed brace` in 0.44s.

This does **not** prove:
- the `ci_product.oo` product rail (not run; long rail / race)
- bootstrap_no_cargo / fixed_point / cargo-shadow
- that every listed smoke exits 0

Parse fail is a file residual, not a THE-rail fail.

## 3 — std/str.oo + parse_int
Live `std/str.oo` checks **OK** (0.09s, 57 MB RSS).
`parse_int` is in the **working tree** (lines 104–130): signed decimal fold, no overflow check, does not recurse into itself.
HEAD `std/str.oo` has no `parse_int`. Uncommitted `+31`.

This closes the prior INT residual “`parse_int` still not in `std/str.oo`” for the **live tree check only**.
It does not commit, does not rebuild the seed, and does not make THE rail cheap.

## 4 / 5 — probe + test_collect .oo
Absent. Retry policy honored (wait 45s, retry once).

On disk only:
- `qa/probe_honesty_tests.oo` (3563 B, 107 lines)
- `qa/probe_lifecycle_tests.oo` (3771 B, 117 lines)
- `qa/probe_boundary_tests.oo` (3578 B, 127 lines)
- `qa/probe_chaos_tests.oo` (3241 B, 117 lines)
- `test_collect.oo` (1299 B, 38 lines)

This turn does not claim those migrations closed. `.oo` copies stay.

## Close / open
| item | this turn | stay |
|------|-----------|------|
| F26 hang (check finishes) | **closed as live** — rc=0 26.75s | cost is not hang |
| B8 12 GiB / ~26s | **re-proved, not closed** | 12.26 GiB / 26.75s. Not 39 MB / 1–2s. Over F26 8 GiB WARN. Under 32 GiB FAIL. |
| seed rebuild | **not run** | compiler-graph rebuild / older seed still unproven. Seed ELF untouched. |
| `parse_int` in live `std/str.oo` | **check OK** (dirty +31 vs HEAD) | uncommitted; no overflow check; not a seed pin |
| `ci_product.oo` typecheck | **FAIL** parse | academy `when`/`and`/`then` live |
| `ci_product.oo` | **not run** | long rail / race residual honest |
| probe `*.oo` + `test_collect.oo` | **absent** after retry | `.oo` twins remain |

## Verdict
| rail | verdict |
|------|---------|
| 1 check `oodac/main.oo` | **PASS** rc=0 26.75s Max RSS 12.26 GiB |
| 2 check `scripts/ci_product.oo` | **FAIL** rc=1 0.44s `ERRparseunclosed brace` |
| 3 check `std/str.oo` | **PASS** rc=0 0.09s Max RSS 57 MB (`parse_int` live dirty) |
| 4 check `qa/probe_*_tests.oo` | **absent** after retry |
| 5 check `test_collect.oo` | **absent** after retry |
| `ci_product.oo` | **not run** (instructed) |
| `bootstrap/oodac_pure_build` | **not run** (instructed) |
| seed | **untouched** |

Open residuals (named, not this-turn THE FAIL):
- B8: 12.26 GiB RSS / 26.75s wall (not 39 MB / 1–2s). Stays open. No new cheap-check proof.
- Seed rebuild: not executed. Stays open. No new compiler-graph seed proof.
- `ci_product.oo` not re-run.
- `ci_product.oo` still `ERRparseunclosed brace` (live academy DSL).
- Probe + `test_collect` `.oo` twins not landed.
- `parse_int` live-check OK but uncommitted vs HEAD.

Do not delete `.oo` until seed match-Result on the compiler graph is proved.
Do not say the check is cheap.
Do not say 12 GiB or seed rebuild is closed.

Marker: `SPRINT_INT_CLOSE_OPEN_2026-08-14`
end of sprint_int_close_open.oot
