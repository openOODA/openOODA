# sprint_int_open_prove.oot — INT live prove (residual / open wave)
# Date: 2026-08-14T02:58:46-10:00
# Worker: INT. Cwd: /home/jeryd/Projects/openOODA/ooda
# Write set: this file only. No product .oo. No SPRINT.oot. Seed not replaced.
# No oodac_pure_build.sh. No ci_product.sh (long rail / race on oodac/oodac).

Host
  git:     56fcb21 (56fcb214bbcc86660a2fbfa1356b044b818867ae)  main ahead origin/main +1
  PATH oodac: absent (`command -v oodac` empty)
  ELF:     ./oodac/oodac
  size:    1036240 B  mtime 2026-08-13 07:59:09 -1000
  md5:     4b9099e4c5014e3c74ef4562a8440160
  BuildID: 472f46f45e1dcdec59b9fee81ace31280ce4bd81
  seed:    ./bootstrap/seed/oodac  878392 B  md5 dedee1d87513411fd8eeb2d460e76ff1
           BuildID 7dcbc1551ff1c4fa61099a0fcd72469bf1efa63f  (read-only; not replaced)
  race:    no live oodac_pure_build.sh / ci_product.sh at start
           one foreign `oodac check std/src/ooda/3d/lights_types.oo` finished before THE rail

Writers
  Waited 45s then retried missing twins once. Retried again after THE rail.
  `scripts/install_dress_rehearsal.oo` **absent** (both retries).
  `scripts/release.oo` **absent** (both retries).
  `.sh` twins still on disk (2026-08-13 17:53): install_dress_rehearsal.sh 4486 B, release.sh 3805 B.
  `scripts/harness_f26_residual_smoke.oo` landed mid-wave (02:55:54 → 02:57:01 → 02:57:45).
  Proved snapshot: 3237 B, 110 lines, mtime 2026-08-14 02:57:45 -1000,
  md5 b1e02d009007ce98237628b2b7e80664.

## Prove table

| # | command | rc | wall | residual |
|---|---------|----|------|----------|
| 1 | `/usr/bin/time -v ./oodac/oodac check oodac/main.oo` | **0** | **26.30s** (user 20.81 sys 5.35) | stdout `OK` (3 B). stderr empty. Max RSS **12854108 kB = 12.26 GiB**. Same ELF as sprint_int_prove.oot (26.05s / 12.25 GiB). Not 1–2s / 39 MB. B8 cost still live. |
| 2 | `/usr/bin/time -v ./oodac/oodac check scripts/harness_f26_residual_smoke.oo` | **0** | **1.00s** (user 0.80 sys 0.18) | stdout `OK` (3 B). stderr empty. Max RSS **392836 kB = 384 MB**. This is **check**, not a harness **run**. Does not re-time main.oo. |
| 3 | `./oodac/oodac check scripts/install_dress_rehearsal.oo` | **absent** | — | Missing after wait + two retries. `.sh` twin remains. Item-10 leftover not closed. |
| 4 | `./oodac/oodac check scripts/release.oo` | **absent** | — | Missing after wait + two retries. `.sh` twin remains. Item-10 leftover not closed. |

ci_product.sh / oodac_pure_build.sh: **not run** (instructed). Seed ELF not touched.

## 1 — compiler proof (THE rail)

```
Command being timed: "./oodac/oodac check oodac/main.oo"
User time (seconds): 20.81
System time (seconds): 5.35
Elapsed (wall clock) time (h:mm:ss or m:ss): 0:26.30
Maximum resident set size (kbytes): 12854108
Exit status: 0
```

stdout last line: `OK`
stderr: empty (GNU time on stderr; product stderr empty)

Verdict: **PASS**. Live check of `oodac/main.oo` finishes, rc=0, 26.30s, 12.26 GiB RSS.
Matches SPRINT pin class (OK ~26s / 12 GB). Residual named: B8 RSS not baseline.

Do not say the check is cheap. Do not say 39 MB.

## 2 — F26 harness check (not run)

Writer rewrite vs prior ERRparse residual:
- live `when` / `and` / `then` blocks are now comments (same dodge as p7_check_main_smoke.oo)
- no `parse_int` (still absent from std/str.oo); local `parse_digits` + `digit_val`
- still `import "std/scenario.oo"` + `import "std/sh.oo"`
- 32 GiB fail-close via `expect_lt` + `process_exit(1)` is in source
- 8 GiB path is `println("WARN …")` only

`oodac check` **OK** on the 02:57:45 snapshot. That proves the academy script typechecks.

It does **not** prove:
- the harness **run** rail (would re-exec THE 12 GiB check)
- `.oo` fail-close under a live 32 GiB RSS (not observed)
- that 12.26 GiB would not WARN (it would; 12.26 GiB > 8 GiB)
- that `expect_lt` Result is consumed on the WARN path (WARN is a print; FAIL uses `is_err`)

`.sh` twin (`scripts/harness_f26_residual_smoke.sh`) still has the live 32 GiB fail-close. Not re-run this turn.

## 3 / 4 — dress + release .oo

Absent. Retry policy honored (wait 45s, retry; retry again after row 1).

On disk only:
- `scripts/install_dress_rehearsal.sh` (4486 B, 2026-08-13 17:53)
- `scripts/release.sh` (3805 B, 2026-08-13 17:53)

SPRINT item 10 residual stands: those two `.sh` files still have no `.oo` twin.
This turn does not claim the migration close. `.sh` copies stay.

## Verdict

| rail | verdict |
|------|---------|
| 1 check `oodac/main.oo` | **PASS** rc=0 26.30s Max RSS 12.26 GiB |
| 2 check `harness_f26_residual_smoke.oo` | **PASS** rc=0 1.00s Max RSS 384 MB (check only) |
| 3 check `install_dress_rehearsal.oo` | **absent** after retry |
| 4 check `release.oo` | **absent** after retry |
| `ci_product.sh` | **not run** (instructed) |
| `oodac_pure_build.sh` | **not run** (instructed) |
| seed | **untouched** |

Open residuals (named, not this-turn FAIL):
- B8: 12.26 GiB RSS / 26.30s wall (not 39 MB / 1–2s). Over F26 8 GiB WARN. Under 32 GiB FAIL.
- F26 harness **run** not executed. `.oo` WARN/FAIL paths unproven at runtime.
- Item 10: `install_dress_rehearsal.oo` and `release.oo` not landed.
- 9a: compiler-graph seed rebuild / older seed still unproven. `.sh` copies stay.
- DE10: `sh_arg_ok` is in live `std/sh.oo` (251 lines); git/tar/chmod still shell. No native fs_* (signature freeze).
- `parse_int` still not in `std/str.oo` (harness local `parse_digits` only).
- ci_product not re-run.

Do not delete `.sh` until seed match-Result on the compiler graph is proved.
Do not say the check is cheap.
Do not say dress/release migration is closed.

Marker: `SPRINT_INT_OPEN_PROVE_2026-08-14`
end of sprint_int_open_prove.oot
