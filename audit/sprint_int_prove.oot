# sprint_int_prove.oot — INT live prove (phalanx)
# Date: 2026-08-13T20:21:24-10:00
# Worker: INT. Cwd: /home/jeryd/Projects/openOODA/ooda
# Write set: this file only. No product .oo. No SPRINT.oot. Seed not replaced.
# No bootstrap/oodac_pure_build. No ci_product.oo (long rail / race on oodac/oodac).
Host
  git:     56fcb21 (56fcb214bbcc86660a2fbfa1356b044b818867ae)  main ahead origin/main +1
  PATH oodac: absent (`command -v oodac` empty)
  ELF:     ./oodac/oodac
  size:    1036240 B  mtime 2026-08-13 07:59:09 -1000
  md5:     4b9099e4c5014e3c74ef4562a8440160
  BuildID: 472f46f45e1dcdec59b9fee81ace31280ce4bd81
  seed:    ./bootstrap/seed/oodac  878392 B  md5 dedee1d87513411fd8eeb2d460e76ff1
           BuildID 7dcbc1551ff1c4fa61099a0fcd72469bf1efa63f  (read-only; not replaced)
  race:    no live bootstrap/oodac_pure_build / ci_product.oo at start

## Prove table
| # | command | rc | wall | residual |
|---|---------|----|------|----------|
| 1 | `/usr/bin/time -v ./oodac/oodac check oodac/main.oo` | **0** | **26.05s** (user 20.56 sys 5.37) | stdout `OK` (3 B). stderr empty. Max RSS **12846000 kB = 12.25 GiB**. Not 1–2s / 39 MB baseline. B8 cost still live. |
| 2 | `./scripts/ci_product.oo` | **not run** | — | **ci_product not re-run: long rail / race risk**. 250-line script: cargo-shadow + `bootstrap_no_cargo.oo` (rebuilds `oodac/oodac`) + **131** `*_smoke.oo` rails + `fixed_point.oo`. Not a <3 min rail. Would race any pure rebuild. |
| 3 | `./oodac/oodac emit-c /tmp/sprint_int_prove/match_result.oo` | **0** | **0.0081s** | DE2 did **not** leave `fixtures/match_result_seed.oo`. Invented `/tmp` fixture (not in repo). Live emit 9875 B / 165 lines. Lowers both let-match and stmt-match to `OoResS` + `__ml.ok` / `__m.ok`. RSS 6968 kB. |
| 3b | `./bootstrap/seed/oodac emit-c /tmp/sprint_int_prove/match_result.oo` | **0** | **0.0065s** | Optional seed emit, 60s cap. Same tiny fixture also lowers `OoResS` (8375 B / 139 lines). RSS 5540 kB. This does **not** close SPRINT 9a for the compiler graph / `fs_write_file` seed residual — only this fixture. |
| 4 | `./oodac/oodac check std/sh.oo` | **0** | **0.1123s** | DE1 did **not** land `sh_arg_ok` (tree grep zero; `std/sh.oo` clean vs HEAD). Isolated check **OK** (RSS 52852 kB). Not a product fail. Sibling-import fail did not appear because `sh_arg_ok` is absent. |
| 4b | `./oodac/oodac check scripts/oodac_check_capture.oo` | **1** | **0.25s** | Optional importer of `std/sh.oo`. Last line: `ERRparseunclosed brace`. **Not** a `sh_arg_ok` product fail. Same class: `scripts/std_split_residual_smoke.oo` rc=1 `ERRparseunexpected IDENT` in 0.2529s. Both also import `std/scenario.oo`. |

## 1 — compiler proof (THE rail)
```
Command being timed: "./oodac/oodac check oodac/main.oo"
User time (seconds): 20.56
System time (seconds): 5.37
Elapsed (wall clock) time (h:mm:ss or m:ss): 0:26.05
Maximum resident set size (kbytes): 12846000
Exit status: 0
```

stdout last line: `OK`
stderr: empty
wall date-delta: 26s (matches GNU time 26.05s)

Verdict: **PASS**. Live check of `oodac/main.oo` finishes, rc=0, ~26s, ~12.25 GiB RSS.
Matches SPRINT pin language (OK ~27s / 12 GB). Residual named: B8 RSS not baseline.

## 2 — ci_product
Read `scripts/ci_product.oo` first. It is the local product rail, not a 3-minute smoke:

- purity greps + cargo/rustc PATH shadow
- `bootstrap_no_cargo.oo` under `SEED_OODAC` (writes `oodac/oodac` + `bin/ooda`)
- 131 smoke rails (chs / wasm / llvm / caps / secret / residual packs / …)
- `fixed_point.oo` (refuses `OK_HOST`)

**ci_product not re-run: long rail / race risk.**

Residual stays honest: this turn does not claim the full product rail.

## 3 — match Result emit
DE2 leftover `fixtures/match_result_seed.oo`: **absent**.
Existing in-tree cousins (not used as the prove fixture): `fixtures/bc_match_result.oo`, `bootstrap/corpus/emit-c/pass/match_result_let.oo`, `match_result_stmt.oo`.

Invented fixture (not in repo): `/tmp/sprint_int_prove/match_result.oo`

```
pub fn main() {
    let r: Result[String, String] = Ok("hi");
    let s = match r { Ok(v) => v, Err(e) => e, };
    println(s);
    let e: Result[String, String] = Err("no");
    match e { Ok(v) => println(v), Err(x) => println(x), }
}
```

Live `./oodac/oodac emit-c` rc=0. Lowered C (tail of `main`):

- `OoResS r = ((OoResS){1, oo_str_lit("hi")});` then `__ml.ok` bind `s`
- `OoResS e = ((OoResS){0, oo_str_lit("no")});` then `__m.ok` stmt print

Seed emit of the **same** `/tmp` fixture: also rc=0, same `OoResS` shape (seed preamble lacks some later cap grants; match arms present).

Residual: SPRINT item 9a (“seed emit match Result still residual”) is **not reproduced** on this tiny let+stmt fixture. Still unproven: seed emit of the compiler graph, and seed `fs_write_file` / `OoResS` vs `OoResV` (item 8). No seed ELF write this turn.

## 4 — sh_arg_ok / std/sh.oo
`sh_arg_ok`: **not landed**. `grep -RIn sh_arg_ok` over `*.oo *.oot *.oo` → zero hits. `git diff --stat -- std/sh.oo` empty.

`std/sh.oo` is still thin `process_exec` wrappers (`import "std/os/process.oo"`). Isolated `oodac check std/sh.oo` → **OK** 0.1123s. Do not treat as product fail (and it did not fail).

Optional importer checks (60s cap) of scripts that already `import "std/sh.oo"`:

- `scripts/oodac_check_capture.oo` → rc=1 `ERRparseunclosed brace` (0.25s)
- `scripts/std_split_residual_smoke.oo` → rc=1 `ERRparseunexpected IDENT` (0.2529s)

Those are parse residuals on academy smokes that also pull `std/scenario.oo`. Not evidence for or against `sh_arg_ok`. Isolated `sh.oo` already proved the module checks.

## Verdict
| rail | verdict |
|------|---------|
| 1 check `oodac/main.oo` | **PASS** rc=0 26.05s Max RSS 12.25 GiB |
| 2 `ci_product.oo` | **skipped** (long rail / race risk) — residual honest |
| 3 live emit-c match Result | **PASS** rc=0 0.0081s (`/tmp` fixture) |
| 3b seed emit-c same fixture | **PASS** rc=0 0.0065s (tiny fixture only; 9a graph still open) |
| 4 `sh_arg_ok` | **not landed**; isolated `std/sh.oo` check **OK** |
| 4b sh importers | parse ERR (not product fail) |

Open residuals (named, not this-turn FAIL):
- B8: 12.25 GiB RSS / ~26s wall (not 39 MB / 1–2s)
- ci_product not re-run
- 9a seed emit of compiler graph / write-file Result still unproven (tiny fixture is green)
- DE1 `sh_arg_ok` absent
- DE2 `fixtures/match_result_seed.oo` absent
- academy smokes that import `sh.oo`+`scenario.oo` still `ERRparse*`

end of sprint_int_prove.oot
