# sprint_pv1_close_open.oot
# PV1 hostile. Read-only product. This file only.
# Date: 2026-08-14
# Tree: /home/jeryd/Projects/openOODA
# Did not run oodac check oodac/main.oo (INT).
# Did not trust SPRINT.oot. Grep + read only.

Pre-write snapshot (first read)
  ooda/scripts/ci_product.oo:17-24 live when/and/then. No rail roster.
  ooda/std/str.oo:104-130 parse_int digit fold. No self-call.
  ooda/oodac/tc_call_let.oo ended at :113. cap_builtin_ret only after COLON.
  ooda/qa/*.oo absent. ooda/test_collect.oo absent. .sh live.
Re-read once (writers still moving)
  ci_product.oo unchanged. parse_int unchanged.
  tc_call_let.oo grew an else-if EQ arm that calls cap_builtin_ret and drops gty.
  qa twins and test_collect.oo still absent.

## Score

| Item | Question | Disk | Verdict |
|------|----------|------|---------|
| 1 | ci_product.oo: no live when/and/then? rail list present? check would parse? | live tokens; no roster; parse walk only | FAIL |
| 2 | std/str.oo has parse_int? body does not recurse? | yes :104; no self-call | PASS |
| 3 | tc_call_let.oo hits cap_builtin_ret on unannotated let x = call()? | yes :112; result unused | PARTIAL |
| 4 | new qa/*.oo and test_collect.oo exist? .sh still present? | no new twins; all .sh live | FAIL |

Wave: PARTIAL. Not PASS. parse_int closed. Unannotated hit is a dead store. ci_product story tokens and rail roster never landed. QA twins never landed.

## 1 — ci_product.oo — FAIL

Q: no live when/and/then? rail list present? check would parse?

Live tokens. No rail list. Check parse walk would not ERRparse. Full check OK unrun and would type-fail.

Proof live when/and/then
- ooda/scripts/ci_product.oo:17 `when "product scripts clean of host toolchain" {`
- :18-22 five live `and  "..."` blocks.
- :23 `then "ci_product PASSED" {`
- :24 `} } } } } }`
- Grep `^\s*(when|and|then)\b` = 7 hits. Not comments.
- Contrast ooda/scripts/harness_f26_residual_smoke.oo:26-28 and release.oo:32-36 (`// when` / `// and` / `// then`).
- SCRIPTING.oot:22: when/and/then are story, not grammar. ooda.ebnf has no when/and/then.
- ooda/oodac/lex_comment.oo:12-27 only eats `//` to newline. These lines are IDENT STRING LBRACE.

Proof no rail list
- Twin ooda/scripts/ci_product.sh:73-205 `for rail in` names 132 scripts (product_pure_dispatch_smoke.sh … pure_build_fp_smoke.sh) then :216 runs each.
- ci_product.oo has zero of those names. No `for rail`, no `list_push`, no roster const.
- Beats :6 say "every listed rail passes". Nothing is listed.
- .oo runs bootstrap_no_cargo.sh (:41) and fixed_point.sh (:43) only. The 132-rail loop is missing.
- .sh:70-71 export OODA + OODAC_BIN. .oo never does.

Proof check would parse (source only; not run)
- ooda/oodac/check_drive.oo:57-67 top-level walker: KW_PUB / KW_FN / KW_TYPE / KW_IMPORT else `ERR\tparse\tunexpected` + exit.
- After comments this file's top-level is two imports + `pub fn main`. That walk should not ERRparse.
- Live when/and/then sit inside main. check_caps.oo:141-145 skip_balanced the body, then :155-158 sealed walk only on IDENT LPAREN. `when` is IDENT STRING. Not a top-level ERRparse.
- ooda/oodac/tc_names_known.oo:73-77 keywords omit when/and/then. In-body `when` is an undefined IDENT (tc_names.oo:160-166). Full `oodac check` would not print OK.
- I did not run `oodac check scripts/ci_product.oo`. Parse is reasoned, not executed.

Holes
1. Same dodge as the F26 harness was supposed to take. Writers left story tokens live.
2. :26 `release.sh + | grep` — stray `+` before the pipe. Even a green parse would ship a broken host-toolchain grep.
3. "every listed rail exits 0" is theater. The .oo cannot fail a missing rail.
4. .sh stays the real product rail (SPRINT residual: ci_product.sh not re-run). This twin is not a migrate.

First snapshot = re-read. No land.

## 2 — std/str.oo parse_int — PASS

Q: has parse_int? body does not recurse?

Yes. Digit fold. No self-call.

Proof present
- ooda/std/str.oo:11 beat 4 names parse_int.
- :15 residual: decimal ASCII only; no overflow check.
- :103-104 `pub fn parse_int(s: String) -> Int` with comment "do not call parse_int (recursion)".
- :105-129: chars_len, optional leading "-", char_at ladder 0-9 as `v = v * 10 + n`, `i = i + 1`.
- :130 `return v * sign;`
- Grep parse_int in this file: comment :11, comment :103, def :104. Zero calls.

Proof no recurse
- Body never writes `parse_int(`. Same shape as ooda/oodac/tc_parse_int.oo:12-38 (compiler-local copy). That is a fold, not a wrapper.
- Contrast str_sub :47-49 (`return str_slice(...)`) and env.oo `return oo_env_get(...)` — those rename to avoid self-recursion. parse_int does not wrap a builtin of the same name.

Holes (not a FAIL)
1. ooda/std/core/str.oo has no parse_int. Callers of core/str.oo do not see this fn.
2. Non-digits are skipped, not Err. Garbage text can stay 0 or a prefix fold.
3. No overflow check (header residual). Same as the harness parse_digits hole.
4. SPRINT.oot:132 still lists sh-parse_int as if open. Disk closed it. Board lag, not a code miss.

First read already had the fn. Re-read same.

## 3 — tc_call_let.oo unannotated hit — PARTIAL

Q: hits cap_builtin_ret on unannotated `let x = call()`?

Yes on disk after re-read. The type is thrown away. ret_table still built.

Proof In (re-read)
- ooda/oodac/tc_call_let.oo:8-9 beats now say annotated and unannotated; hit cap_builtin_ret first.
- :58-105 still the annotated `let [mut] name : Ann = IDENT LPAREN` path. :71-73 cap_builtin_ret then :74-82 call_ret_type_at.
- :106-118 new arm: after IDENT, `else if` EQ, then IDENT LPAREN, `let gty: String = cap_builtin_ret(field_at(..., p, 3))`.
- That is `let x = env_get(...)` / `let mut x = env_get(...)`. The call happens.
- Helper still ooda/oodac/tc_call_ret.oo:219-247. env_get / oo_env_get → "Result".
- ooda/oodac/main.oo:60 imports tc_call_ret.oo before :67 tc_call_let.oo. Product concat sees the name.
- ooda/oodac/check_drive.oo:111 still calls typecheck_let_ann_call_init.

Proof still unpaid
- :112-114 `gty` is never read. No resolve_type_alias. No types_compatible. No bind into env. No call_ret_type_at fallback when cap_builtin_ret returns "".
- :20 `let ret_table: String = build_fn_ret_table(toks);` every entry. :12 residual admits it.
- Isolated `oodac check oodac/tc_call_let.oo` still has no local cap_builtin_ret (no import of tc_call_ret.oo).
- tc_ann_let.oo:99-107 still resolve_type_alias(ret_table, cname) for zero-arg `()` only. No cap_builtin_ret.
- cap_builtin_ret still returns bare "Result" / "List" / "Bool" / "Int".
- Split tables: tc_names.oo:187-188 get_builtin_ret_type maps oo_file_size to Bool. cap_builtin_ret :236-239 maps it to Int.

Honest holes
1. First snapshot had no EQ arm. Re-read landed a dead store. "Hits" is true as a call site and false as a type rail.
2. Non-cap `let x = foo()` on this arm skips call_ret_type_at entirely. Annotated path still pays the table scan.
3. "Cheap" is unproven. Did not time. Did not run check main.oo.

First snapshot vs re-read: writers paid the name hit. They did not type the binding.

## 4 — qa/*.oo + test_collect.oo — FAIL

Q: new qa/*.oo and test_collect.oo exist? .sh still present?

New twins do not exist. Every named .sh is still present.

Proof exist (no)
- ooda/qa/ on disk: probe_boundary_tests.sh, probe_chaos_tests.sh, probe_honesty_tests.sh, probe_lifecycle_tests.sh, repro_crit1_fd_starvation_product_run.sh, repro_crit1_fd_starvation_root.sh. Six .sh. Zero .oo.
- Read of ooda/qa/probe_boundary_tests.oo, probe_honesty_tests.oo, probe_chaos_tests.oo: missing. Parent lists the .sh only.
- ooda/test_collect.sh exists (38 lines; DFS import collect; echo MODS). ooda/test_collect.oo: missing (parent lists test_collect.sh).
- /home/jeryd/Projects/openOODA/qa/test_collect.oo: missing.
- /home/jeryd/Projects/openOODA/qa/qa_phase1_4.oo: missing (parent lists qa_phase1_4.sh).

Proof .sh still present
- All six ooda/qa/*.sh shebang + job headers live.
- ooda/test_collect.sh:1-38 live.
- Repo-root qa/qa_runner.sh, qa_phase1_4.sh, qa_phase5a.sh, qa_phase5b.sh, qa_phase6_8.sh, e2e_runner.sh, qa_harness_common.sh live.

Not this close
- qa/qa_runner.oo exists. It is a SHA-256 / json_parse_internal counter, not a twin of qa_runner.sh and not test_collect.
- qa/tests/**/*.oo and qa/tests_*/*.oo are old fixtures, not new probe twins.

Holes
1. Item 10 [x] ("most scripts have a .oo twin") still false for ooda/qa and test_collect.
2. test_collect.sh is the import DFS the seed/build path still uses. No .oo collect means that rail stays bash.
3. Re-read did not land these files.

## What this pass did not do

- No oodac check oodac/main.oo.
- No oodac check of ci_product.oo or std/str.oo.
- No ELF strings. No live RSS / wall clock.
- No edit of SPRINT.oot.

Marker: `SPRINT_PV1_CLOSE_OPEN_2026-08-14`
end of sprint_pv1_close_open.oot
