# sprint_pv2_audits_part2.oot — (Part 2 of sprint_pv2_audits.oot)
# Continuation of `openOODA/audit/sprint_pv2_audits.oot`.

## np>12 TYPESTATE prepend — FAIL (claim). Audit of that claim: PASS
No `ts_extract` under `ooda/`. No `check_typestate_call.oo`.
`check_mod.oo:53-86` when `np > 12`:

  collect paths
  scan fn names into depth-0 binds
  `body = strip_import_lines(raw2)`
  `run_check_core_binds(sys, body, binds)`
  print OK

`strip_import_lines` (`check_collect.oo:141-172`) drops import lines only.
Same-file `// TYPESTATE` stays. There is no extract. There is no prepend.

`check_typestate.oo:16-21`: parse src; if no `__tsf__` fn, return.
A large-graph consumer with no own TYPESTATE lines is unchecked.
`TYPE_STATE.oot:25` already says small graphs. `:41` no inter-file lattice.
`SHIPPED.oot:49` `Imports keep directives.` is wider than the large path.

`load_import.oo:76-139` keeps non-import chunks (comments included) on
np<=12. sprint12 cited `:76-79` — those lines start the import-chunk skip;
the keep is the complement + tail `:138-139`. Imprecise, not a hole.

`check_drive.oo:105` still `check_typestate(src, toks)`.
`check_cache.oo:70` still `t_ts3_`.
`main.oo` typestate imports are **:28-34**, not sprint12's `:26-32`.
`:26-27` are `check_residual.oo` / `check_hitl.oo`. Set of files is right.
No `check_typestate_call.oo`. Line map drifted.

Calls live in `check_typestate_walk.oo:12-50` (`ts_apply_call`). True.

**Item 3: PASS** on live facades + cap re-check + residual path.data.
np>12 prepend: **FAIL as a fact** (does not happen). sprint12: **PASS**
on that FAIL, **PARTIAL** on `main.oo:26-32` line map.

---
4. RULES 1.13 / 1.14
---

`RULES.oot:68-88` heading `1.13 Scripts`.
  New tools/proofs/scripts are `.oo`. Run with oodac.
  Do not add a new `.oo` / `.oo` / `.js` when `.oo` can do the job.
  Host script only for gcc / npm / seed bootstrap, with a why-not-.oo line.
  Old `*_smoke.oo` may stay until the `.oo` run path is the product path.

`RULES.oot:90` `1.14 Scripts: see 1.13`

SPRINT wave `[x] rules-merge` (`SPRINT.oot:132`) matches live RULES.

**Item 4: PASS**

---
5. harness_f20_inventory_smoke.oo
---

Live `ooda/scripts/harness_f20_inventory_smoke.oo`:

  `:16` `const EXPECTED: Int = 38;`
  `:17-27` ALLOW includes
      `ooda/std/sh.oo`
      `ooda/std/scenario.oo`
      `ooda/std/net/http.oo`
      `ooda/std/_template.oo`
  38 paths. Matches the tree.

Line samples (re-read last line on disk):

  core/crypto.oo 45   (`:38` expect; file ends `:45`)
  os/fs.oo       100  (`fs.oo:100`)
  hash/sha256.oo 17   (full file 17)
  os/env.oo      16   (`env.oo:16`)
  os/cmd.oo      62   (`cmd.oo:62`)
  os/process.oo  62   (`process.oo:62`)
  str.oo         100  (`str.oo:100`)
  test.oo        67   (`test.oo:67`)

`.oo` twin `harness_f20_inventory_smoke.oo:8-13,21` same 38 + same four
names + same line samples.

sprint9 `:249-261` said EXPECTED 34 and ALLOW omitted the four extras.
That page is stale. Live harness would **pass** on this tree, not fail.

Wave `[x] F20-count` (`SPRINT.oot:127`) says 38 + sh + scenario.
True, understated (http + `_template` also in ALLOW).

**Item 5: PASS**

---
FALSE / STALE [x] ON SPRINT
---

Did not treat "audit closed" as a product lie. A [x] is false when the
checked sentence is false on disk.

| SPRINT line | Box | Verdict |
|-------------|-----|---------|
| :17 B2 token cap 1048576 | [x] | holds (`token_stream.oo:22-23` `token_fmt.oo:26-27`) |
| :29 B4 peak RSS + build-id | [x] | holds (p7 .oo/.oo). sprint9 Open/no-RSS is stale |
| :33 B6 cap_builtin_ret | [x] | holds (`tc_call_ret.oo:219-247`) |
| :37 B7 8/32 GiB | [x] | holds (`harness_f26_residual_smoke.oo:18-19`) |
| :41 B8 50s/30GB → 28.5s/12GB | [x] | holds as board text; not re-timed here |
| :49 B5 env fix | [x] | **fix is real; prose is false** |
| :67 Task 10 migrate all .oo | [x] | **overclaim** |
| :77 Task 11 B8 audit | [x] | audit exists; request text has false names |
| :100 Task 12 typestate | [x] | holds vs live + sprint12 |
| :127 F20 harness 38 | [x] | holds |
| :132 rules-merge 1.13/1.14 | [x] | holds |
| :109 Closed `34 files` | (closed, no box) | **false** |

### False / stale text next to a box
**B5 [x] `SPRINT.oot:50-53` — false live sentences**
  `:50` `env.oo:7` — call is `:15`.
  `:51` `The .oo body still recurses if resolved.` — body is `oo_env_get`.
  `:53` close `env.oo:6` — `:6` is a comment.
  `:53` `c_emit_helpers.oo:235-236` — those lines are `return true;` /
        start of `c_emit_builtin`. Real lower: `c_emit_lower.oo:153-158`.
  The **close** (no more self-call) is true. The **surrounding [x] text** is not.

**Task 11 [x] `SPRINT.oot:85` — false mechanism**
  `(2) Drop_depth: list_filter at tc_scope.oo:28-43`
  Live: no `list_filter`. Function is `:36-51`. `str_ends_with` + while.
  Box means "audit done" (sprint11 PASS + NOTE). Frozen request is still wrong.

**Task 11 [x] `SPRINT.oot:95` — stale open**
  `(6) ... B6 + B7 remain open.` Both are [x] on the same file (`:33`, `:37`).

**Task 10 [x] `SPRINT.oot:67-75` — false "all"**
  Title: migrate **all** `.oo` to `.oo` (closed).
  Close: all ~200 `ooda/scripts/` `.oo` have a `.oo` counterpart.
  Live holes: `scripts/install_dress_rehearsal.oo` (no `.oo`),
  `scripts/release.oo` (no `.oo`). Several `ooda_*.oo` have no `.oo` twin.
  RULES 1.13 allows a host script for seed/gcc. That is not "all migrated".
  Body still says `386` then `97/203`. Inconsistent with "closed / all ~200".

**Closed-this-week `SPRINT.oot:109`**
  `3 std honesty 34 files, inventory matches find` — live 38. False closed claim.

**Closed-this-week `SPRINT.oot:106`**
  `Leftover is B2 B3 B4-gate` — those three are [x]. Stale leftover list.
  Does not re-claim 39 MB.

**Not a false [x]**
  Pin + B8 + Task 9 on 39 MB / 38 / `oo_env_get`.
  Task 12 one-liner matches live + sprint12.
  Wave F20 38 + rules-merge.

---
PRIOR-AUDIT DRIFT (do not reuse)
---

sprint9_f26_f20_verify.oot
  KEEP: 38 files; env `:15` `oo_env_get`; 39 MB other ELF; F20 ledger 34;
        fs.oo 100; 3/34 old table match (byte/python/test) not re-wc'd here
        except test.oo 67 still.
  DROP: harness EXPECTED 34 / ALLOW old 34 / would fail (live 38, would pass).
  DROP: p7 no RSS / no build-id / B4 Open (live B4 [x], p7 prints both).
  DROP: F20 smoke "43 L" (live harness `.oo` is 45 lines).

sprint11_b8_binds_verify.oot
  KEEP: seven `list_push` sites; no `binds = binds +` in the two files;
        `str_ends_with`; `name@d`; env red herring; 50s/30GB → 28.5s/12GB
        not 39 MB; check_scan leftover.
  KEEP: list_filter is wording, not a call.
  NOTE: SPRINT + RELEASE_NOTES + b8_root_cause still say `list_filter`.

sprint12_typestate_fs_verify.oot
  KEEP: facade → `oo_*`; C require lines; path.data residuals; 1024 is_dir;
        silent to_cpath; no O_NOFOLLOW on mkdir/unlink/link/symlink;
        DE1.8; args 4096 drop argv0; no ts_extract; no call file;
        check_drive `:105`; cache `t_ts3_`; SHIPPED wider than np>12.
  DROP: `main.oo:26-32` (live typestate imports `:28-34`).

## VERDICT
| Item | Live | Prior audit |
|------|------|-------------|
| 1 F20 count 38 | **PASS** | sprint9 PASS; harness page stale |
| 1 env.oo → oo_env_get | **PASS** | sprint9 PASS (`:15`) |
| 1 39 MB corrected | **PASS** | sprint9 PASS; B4 39 MB sentence gone |
| 2 binds list_push | **PASS** | sprint11 PASS |
| 2 drop_depth list_filter | **FAIL** (no such call) | sprint11 NOTE — accept |
| 3 facades + C cap re-check | **PASS** | sprint12 PASS |
| 3 residual path.data | **PASS** (still open) | sprint12 PASS |
| 3 np>12 TYPESTATE prepend | **FAIL** (does not exist) | sprint12 FAIL — accept |
| 4 RULES 1.13 / 1.14→1.13 | **PASS** | SPRINT [x] 132 holds |
| 5 harness 38 + 4 extras | **PASS** | sprint9 FAIL/stale on this page |

False [x] / false [x] prose:
  B5 surrounding text (`:50-53`)
  Task 10 "all .oo migrated" (`:67-75`)
  Task 11 request `list_filter` + "B6+B7 remain open" (`:85`, `:95`)
  Closed-week "34 files" (`:109`)

Do not say std is 34. Do not say env.oo recurses. Do not say 39 MB is the
floor. Do not say drop_depth is `list_filter`. Do not say np>12 prepends
TYPESTATE. Do not say every `.oo` has an `.oo` twin.

**Marker:** `SPRINT_PV2_AUDITS_2026-08-13`
end of sprint_pv2_audits.oot

end of sprint_pv2_audits_part2.oot
