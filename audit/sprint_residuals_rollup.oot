# sprint_residuals_rollup.oot — DOCS
# Date: 2026-08-13
# Role: DOCS. This file only. Did not edit SPRINT.oot. Did not edit product code.
# Axiom: RULES 1.9 check peers. 1.10 short words.
# Method: read SPRINT + RULES + sprint*.oot. Grep live tree. No oodac check this turn.
PLAIN ENGLISH
  What changed: last wave put caps, gates, audits, and the 38-file inventory on disk.
  Why it matters: the board [x] rows are real source, not talk — except cheap check and shell safety.
  What is not done: DE10 still shells out. 9a is only a tiny emit. write_file is already void-result in live source. Proofs are ahead of the product.
  What to do next: land sh_arg_ok or native fs_*. Prove seed emit on the compiler graph. Keep the .oo copies. Do not say memory is 39 MB.

---
PREVIOUS WAVE — one line each (files on disk, or unproven)
---

B2  CLOSED as source. token_stream.oo:22 and token_fmt.oo:26 print ERR lex too many tokens and exit at 1048576 rows. No flood fixture. Wrap cap is dead (collect already exits). Peer: sprint_pv1_b2b4b6b7.oot PASS + holes; sprint_mem_caps.oot.

B4  CLOSED as print gate. p7_check_main_smoke.oo and .oo print peak RSS + ELF build-id. rc 134/137/139/143 is FAIL. RSS is not a fail line. 12 GB still ALL OK. Peer: sprint_pv1 PASS + holes.

B6  CLOSED as helper only. cap_builtin_ret lives in tc_call_ret.oo:219 and maps env_get / write_file / fetch. let x = env_get(...) does not use it. Full ret_table still built. Cheap is unproven. Peer: sprint_pv1 PARTIAL.

B7  CLOSED as numbers on disk. harness_f26_residual_smoke.oo fail-closes at 32 GiB and warns at 8 GiB. The .oo prints WARN and drops the FAIL Result. 12 GB still WARNs. Peer: sprint_pv1 PARTIAL; sprint_mem_caps.oot.

8   CLOSED as live ABI. runtime chs_rt.h / chs_rt_fs.c and c_emit_preamble.oo:92 are OoResV oo_write_file (void result: ok or error text). oodac/*.oo has zero OoResS oo_write_file. Seed pin and product ELF already string OoResV. Build sed still rewrites leftovers. ci_product.oo not re-run. Peer: sprint8_writefile_emit.oot.

9   CLOSED as audit. sprint9_f26_f20_verify.oot is on disk. 39 MB was another ELF. Live pin is ~28 s / 12 GB. env.oo:15 is oo_env_get. 9a leftover named below.

11  CLOSED as audit. sprint11_b8_binds_verify.oot PASS. Live tc_names.oo uses list_push (grep binds = binds + is empty). drop_depth uses str_ends_with. Honest delta 50 s / 30 GB → 28.5 s / 12 GB. Not 39 MB.

12  CLOSED as audit. sprint12_typestate_fs_verify.oot PASS on facades + cap re-check. Residuals stay: raw path.data on some reads; no np>12 TYPESTATE prepend.

F20 CLOSED as harness. Live find ooda/std -name '*.oo' is 38. harness_f20_inventory_smoke.oo/.oo expect 38 and current line samples (fs.oo 100, env.oo 16, …). f20_std_inventory.oot still says 34 — stale ledger, not the harness.

RULES CLOSED as text. RULES 1.10 is Plain English. 1.13 is Scripts (.oo proofs; old .oo may stay until the .oo run path is the product path). 1.14 is "see 1.13".

## THIS RESIDUAL WAVE
DE10 sh.oo
  Still 20 process_exec wrappers. 19 glue a String into sh -c. Only sh_sleep takes an Int.
  sh_arg_ok is not in std/sh.oo. std/sh_arg.oo is absent.
  scripts/sh_arg_ok_smoke.oo is on disk and calls sh_arg_ok — proof is ahead of the product.
  Peer: sprint_sec_sh.oot (pre-DE1). Not secure. A char gate will not fix git / tar / chmod.
  INT: isolated oodac check std/sh.oo OK. Peer audit of a landed gate: not on disk yet.

9a match Result
  Product emit exists: c_emit_match_stmt.oo / c_emit_match_let.oo.
  fixtures/match_result_seed.oo is on disk (stmt match, no write_file).
  Peer sprint9a_match_result.oot: live + this seed pin emit-c the fixture rc=0 (OoResS __m). Let rail and a /tmp write_file V match also rc=0. No source patch.
  9a is a seed-binary / compiler-graph leftover, not a live source bug. bootstrap/oodac_pure_build not re-run.
  process_exec as a match target is still unlowered (peer). Not this file.

Task 8 write_file ABI
  No preamble patch this wave. Live source already prints OoResV.
  Residual is an old seed, tokens.txt dump, and the build sed belt.
  Peer: sprint8_writefile_emit.oot. This DOCS grep matches that file.

Proofs
  On disk: match_result_seed.oo, sh_arg_ok_smoke.oo, corpus match_result_stmt/let, p7 + F20 + F26 smokes.
  INT clock (peer, not re-timed here): oodac check oodac/main.oo rc=0, 26.05 s, 12.25 GiB RSS.
  ci_product.oo not re-run (long rail / race).
  HARN run / PV2 peer audit: not on disk yet.
  HYG: sprint_hyg.oot line lock PASS on the wave-touch set. That file said match_result_seed.oo and sh_arg_ok_smoke.oo were absent; both are present now.

## STILL OPEN
- DE10: 19 shell-injection sinks. No native fs_* / argv. sh_arg_ok not landed. Smoke will not compile until it is.
- 9a: tiny match emit is green on this seed pin. Compiler-graph seed rebuild still unproven. .oo copies stay.
- Task 8: old-seed OoResS belt. ci_product not re-run.
- B8 leftover: ~26 s / 12 GB. Not 1–2 s / 39 MB. Still over F26 8 GiB WARN. Under 32 GiB FAIL.
- B6 cheap: helper exists; let-path and ret_table cost remain.
- B7 .oo FAIL: expect_lt Result is dropped. Only the .oo fail-closes on 32 GiB.
- B2: no product flood smoke. tokenize_lines post-cap is dead.
- parse_int: not in std/str.oo. COOKBOOK is the .oo-replace plan, not a parse_int sample.
- Task 12 residuals: raw read paths; no landlock; fs_walk no depth cap.
- F20 ledger f20_std_inventory.oot still says 34.

Do not delete .oo until seed match-Result on the compiler graph is proved.
Do not say the check is cheap.
Do not say sh.oo is safe.

---
PEER AUDITS THIS WAVE (sprint*.oot)
---

On disk
  sprint8_writefile_emit.oot     RT   live write_file is already OoResV
  sprint9a_match_result.oot      DE2  tiny match emit OK on live + this seed; no source patch
  sprint_hyg.oot                 HYG  line lock; F20 still 38; snapshot stale on two new files
  sprint_mem_caps.oot            MEM  1048576 is rows vs chars; 12 GB still > 8 GiB WARN
  sprint_int_prove.oot           INT  check main OK 26 s / 12.25 GiB; tiny match emit OK; ci_product skipped
  sprint_pv1_b2b4b6b7.oot        PV1  B2/B4 PASS with holes; B6/B7 PARTIAL
  sprint_pv2_audits.oot          PV2  F20=38 / env_get / 39 MB board PASS; list_filter wording FAIL; facades PASS
  sprint_sec_sh.oot              SEC  DE1 not landed; 19 sinks live
  sprint9_f26_f20_verify.oot     prior wave
  sprint11_b8_binds_verify.oot   prior wave
  sprint12_typestate_fs_verify.oot prior wave

Not on disk yet
  HARN live-run, DE1 land note, COORD board edit.

Marker: `SPRINT_RESIDUALS_ROLLUP_2026-08-13`
end of sprint_residuals_rollup.oot
