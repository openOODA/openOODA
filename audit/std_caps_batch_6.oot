# Audit Batch 6 — SWARM/PROBE/CAPS — 15 files (wave6 + wave7 partial)

**Date:** 2026-08-12
**SWARM:** fan-out 5+5 (wave6-ui 5 WindowCap/FrameCap vs media pure, wave6-vision 5 image decode, wave7-agent 5 pure planning)
**PROBE:** C+L+K, B2 missing cap fail-closed
**CAPS:** 27 tokens — is_cap_type 27, sealed WindowCap/FrameCap still residual (no branch in sealed_kind_of)

## Results

### UI 5 — WindowCap/FrameCap
- std/ui 3 + std/media 2 — WindowCap (GUI window), FrameCap (framebuffer), AudioCap/GpuCap/CameraCap per CAPS §4-5 — is_cap_type accepts 27, but sealed_kind_of has no Window/Frame/Audio/Camera branch (only Fs/Net/Sys...), so oodac check cannot yet enforce — residual per CAPS_MATRIX. Currently cap-free stubs, correctly not requiring caps.

### Vision 5 — image decode cap-free
- std/vision → std/image 5 — pure image decode, oodac check OK, proxy CAPS_MATRIX confirm seal table, correctly cap-free

### Agent 5 — pure planning cap-free
- std/agent/context.oo, memory.oo, tool_call.oo, swarm.oo, planner.oo — 0 sealed Fs/Net, no Cap needed, oodac check OK per CAPS granular

## Evidence
- wave6-ui (c2a37fd4 WindowCap no enforce), wave6-vision (image decode), wave7-agent (5836870c agent 5 PASS)
