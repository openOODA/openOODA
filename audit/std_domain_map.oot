# std domain map — one climb, then path freeze

File: openOODA/audit/std_domain_map.oot
THIS FILE IS GENERATED — climb protocol for std/. Do not invent extra parents.

## 0. E-M law for this climb

Cruise is Delta_path = 0 AFTER this climb. Folders name domains.
Law/theory/principle is a file role, not a nested directory.
Do not move trunk. Prove keepers live in qa/; canaries/ is gone.
A library is an imported module. Subtract nothing until GREEN importers exist.

## 1. Do not move (trunk, support, not-libraries)

Top-level files stay: result.oo option.oo str.oo byte.oo math.oo and other std/*.oo.
Folders stay as roots: core collections math text encoding io fs net os
  sync actor hash crypto sec markup archive time diag test simd
  principles formal_proofs prove proof_verifiers cli compression
Attractors (move INTO, do not rename): math crypto sec robot econ ai chem bio net collections encoding

## 2. New parents (create once)

hw/ compile/ phys/ data/ ui/

## 3. Role vs domain

Physical laws live in phys/ (role: law). Math laws live in math/.
Cross-domain bounds live in principles/ (Landauer, dimensions, fail-closed, one CODATA).
std/theory/ is a junk drawer: drain into math/, crypto/, principles/, econ/, then remove empty theory/.

## 4. Agent exclusive sources

See spawn prompts. No two agents git-mv the same source folder.
Import string rewrite is the parent after all nine moves complete.

## 5. Climb result (this turn)

Top-level std dirs: 197 -> 36.
Trunk roots stayed. Tail nested under hw compile phys chem bio robot econ data ai ui.
std/theory/ drained and removed. conservation_laws.oo lives in phys/.
algebraic_invariants.oo lives in math/. Double-run of its two verifiers: PASSED.
Import strings rewritten (longest prefix first). Path freeze now.
