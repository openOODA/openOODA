# t2_phalanx_rollup.oot — Swarm Coordinator rollup (Command Wing)
# Date: 2026-08-12T10:43:41Z
# Wait: ~5m for peers + DE settle, then merge openOODA/audit/t2_*.oot + live re-prove
# Scope: SPRINT T2 — secret scan depth (IDs 2.5, 2.6, 2.7, 5.4, 5.5, 5.6, 6.2)

## Inputs merged

| Peer audit | Role | Verdict (self) |
|------------|------|----------------|
| `audit/t2_hygiene.oot` | Hygiene Enforcer | **PASS** (line-lock; bash -n; no /tmp) — early snapshot |
| `audit/t2_peer1_args.oot` | Peer Verifier 1 (args sinks) | **PASS** 5/5 write_file/tcp_write/println |
| `audit/t2_peer2_field.oot` | Peer Verifier 2 (field/method/index) | **PASS** 7/7 primary+invent |
| `audit/t2_peer3_t1_regression.oot` | Peer Verifier 3 (T1 non-reg) | **PASS** smoke + 25/25 on pre-T2 host |
| `audit/t2_security.oot` | Security Auditor (string/comment FP) | **PASS** FP=0 core; dlopen dual-path; ffi_call residual |
| `audit/t2_integration.oot` | Compiler Integration Tester | **PASS** pure multi + install + matrix 29/29 |

Coordinator note: several QA peers ran against intermediate product host **937592** B (pre-final T2 install). Integration + coordinator live re-prove use **post-install** product host only (**950936** B).

## Coordinator live re-prove (post-wait, post-rebuild)

| Item | Value |
|------|--------|
| Tip commit | `ad53efe` (T2 land; parent `1f67f4d`) |
| Product binary | `ooda/oodac/oodac` **950936** B @ 2026-08-12 03:43:09 |
| md5 | `431d4cc25ef7405ce72a11be73a5f099` |
| BuildID | `0d39e7b370e974e4f6d417e49fd9e38571bb2bbc` |
| pure multi | unique_fns=**428** modules=**149** (integration Attempt C) |
| T2 depth smoke | **PASS** exit 0 |
| T1 return smoke regression | **PASS** exit 0 |
| Coordinator primary matrix | **pass=29 fail=0** |

### ID gate (2.5 / 2.6 / 2.7 / 5.4–5.6 / 6.2)

| ID | Claim | Live fixtures / evidence | Result |
|----|-------|--------------------------|--------|
| **2.5** | full `args` string scan on sealed sinks | `secret_t2_args_{write_file,tcp_write,println_*}` + multi-arg smoke | **FIXED** |
| **2.6** | nested RHS walker | DE3 prop scan; `secret_t2_nested_{rhs,call}_fail` refuse `SECRET y`; depth≤64 residual | **FIXED** (path-A) |
| **2.7** | skip string/comment false positives | `secret_t2_string_*` / `comment_*` ALLOW; `secret_midline_safe` | **FIXED** |
| **5.4–5.6** | field / index / method return taint | `secret_field_assign_*`, `secret_method_return_*`, invent list_get/`xs[i]` (peer2) | **FIXED** (path-A model) |
| **6.2** | FFI / payload args | `secret_t2_dlopen_path_*`, `secret_dlopen_arg_*`, tcp_write payload; ffi_call free residual | **FIXED** (named sinks; ffi_call residual) |

### Full matrix (coordinator, post-install host)

```
PASS refuse args write_file / tcp_write / println secret / println concat
PASS ok println clean with SECRET: other
PASS allow string FP / lit-arg FP / contains-name FP / nested string FP
PASS allow comment FP / blockish comment FP / dlopen public path
PASS refuse real_arg / concat prop / dlopen path / dlopen flag
PASS refuse field_assign_fail / method_return_fail
PASS ok field_assign_pass / method_return_pass
PASS refuse call_arg / multi_arg_println / multi_name_fail / dlopen_arg / sys_exec_argv
PASS ok multi_name_pass / dlopen_arg_pass / sys_exec_argv_pass / midline_safe
PASS refuse nested_rhs / nested_call (compound RHS → sink)
TOTAL primary pass=29 fail=0  (+ nested probes green; smokes T1+T2 green)
```

## Cohort outcomes

| # | Agent | Mapped this turn | Outcome | Notes |
|---|-------|------------------|---------|-------|
| 1 | DE1 guard full-args + comments | `c_emit_secret_guard.oo` (200) | **SHIP** | 2.5 + 2.7 |
| 2 | DE2 field/index/method | `c_emit_secret.oo` + `_prop` + `c_emit_ident.oo` | **SHIP** (in tree) | 5.4–5.6 path-A; agent may still be running at rollup |
| 3 | DE3 nested RHS | `c_emit_secret_prop.oo` (262) | **SHIP** | 2.6 path-A ≤64 |
| 4 | Peer1 args | `t2_peer1_args.oot` | **PASS** | 5/5 |
| 5 | Peer2 field/method | `t2_peer2_field.oot` | **PASS** | 7/7 |
| 6 | Peer3 T1 regression | `t2_peer3_t1_regression.oot` | **PASS** | pre-final host; re-confirmed live |
| 7 | Security | `t2_security.oot` | **PASS** + residual | FP=0; ffi_call dual-path residual |
| 8 | Integration | `t2_integration.oot` | **PASS** | pure multi + 29/29 + install |
| 9 | Test harness | `secret_t2_depth_smoke.sh` + ci wire | **SHIP** | exit 0 |
| 10 | Hygiene | `t2_hygiene.oot` | **PASS** early; tip prop 262/300 | under line-lock post-settle |
| 11 | Docs | `bootstrap/SECRET_TAINT.md` | **PASS** | T2 path-A In; full IFC residual |
| 12 | Swarm Coordinator | this rollup | **SHIP** | T2 closed on fixture gate |

**Cohort summary:** Engineering SHIP · Adversarial PASS · Integration PASS · Hygiene PASS · Command SHIP.

## Mechanism (tip tree)

| Piece | Path | Role |
|-------|------|------|
| Full args emit scan | `oodac/c_emit_secret_guard.oo` | tail `c_guard_scan_idents` on all sealed sinks; `//` + `/* */` skip |
| Nested RHS prop | `oodac/c_emit_secret_prop.oo` | LPAREN-depth scan; field-path `__sec__root.f`; method chain |
| Check-path sinks | `oodac/c_emit_secret.oo` | write_file/tcp_write/println/dlopen IDENT walks; LHS assign hook |
| Field assign emit | `oodac/c_emit_ident.oo` | field-assign coordination with secret env |
| Rails | `scripts/secret_t2_depth_smoke.sh` + `ci_product.sh` | hard-req + CI wire |
| Honesty | `bootstrap/SECRET_TAINT.md` | T2 path-A In table; full IFC residual marker |

## FIXED IDs (this rollup)

`2.5` `2.6` `2.7` `5.4` `5.5` `5.6` `6.2` — **FIXED** (T2 closed on live product host post-rebuild).

SPRINT: mark **T2 [x]**. Do not re-open unless a prove fails.

## Residuals (honest — do not block T2)

| Residual | Sev | Notes |
|----------|-----|-------|
| `secret_t2_ffi_call_fail` | MED | `ffi_call` not a product free; emit can abort (rc=134) instead of clean secret refuse — not in depth smoke |
| Nested RHS depth lattice | LOW | path-A ≤64 LPAREN; not full points-to / full IFC |
| Field base-object taint | MED | `println(b)` after `b.v = secret` may not refuse base `b` (field-name path-A model) |
| Index-assign form | LOW | `xs[i] = secret` residual honesty (get/list_push path-A green) |
| `secret_file_size_pass` free-name | HIGH | non-T2 emit residual; same as T1 PARTIAL |
| Full IFC / `#[Secret]` | — | still residual; SECRET_TAINT honesty smoke PASS |
| DE2 agent process | process | may still show running at rollup; tree already contains field work |

## Evidence paths

| Item | Path |
|------|------|
| This rollup | `/home/jeryd/Projects/openOODA/openOODA/audit/t2_phalanx_rollup.oot` |
| Integration | `audit/t2_integration.oot` |
| Peer1 / Peer2 / Peer3 | `audit/t2_peer1_args.oot`, `t2_peer2_field.oot`, `t2_peer3_t1_regression.oot` |
| Security / hygiene | `audit/t2_security.oot`, `audit/t2_hygiene.oot` |
| Sources | `ooda/oodac/c_emit_secret{,_prop,_alias,_guard}.oo`, `c_emit_ident.oo` |
| Fixtures | `ooda/fixtures/secret_t2_*.oo` + field/method/dlopen smokes |
| Smoke / CI | `ooda/scripts/secret_t2_depth_smoke.sh`, `ci_product.sh` |
| Product host | `ooda/oodac/oodac` 950936 B |

## Close decision

| Question | Answer |
|----------|--------|
| T2 fixtures prove green on rebuilt product host? | **YES** (29/29 coordinator; integration PASS; T1+T2 smokes exit 0) |
| Mark T2 [x] in SPRINT? | **YES** |
| FIXED IDs | **2.5, 2.6, 2.7, 5.4, 5.5, 5.6, 6.2** |
| Commit secret + audit/SPRINT? | **YES** (local only, no push) |

**T2 closed: yes**

end of t2_phalanx_rollup.oot
