# t3_hygiene.oot — Hygiene Enforcer (T3 runtime pack)
# Date: 2026-08-12T10:48:43Z
# Role: Hygiene Enforcer
# Tree: /home/jeryd/Projects/openOODA  (ooda/ + openOODA/)
# Tip (ooda): ad53efe (ad53efeae0d6bc03017bcdfb5d569bdb8c43c885) — T2 tip; T3 work is WIP dirty
# Tip (openOODA): 7a9170a (docs/t2 close)
# OWN: chs_rt_sys.c / chs_rt_ffi.c / chs_rt_fs.c line-lock ≤350 (flag monofile);
#      no secret material or /tmp hardcodes in new T3 scripts; bash -n T3/runtime smokes;
#      warn if agents dirtied unrelated huge surfaces
# WRITE: this audit only (no product edits required this pass)
# POLICY: measure-first; no mass deletes; no push

========================================================================
SUMMARY
========================================================================

| Check | VERDICT | Notes |
|-------|---------|-------|
| `chs_rt_sys.c` ≤350 lines | **PASS** | 295 / 350 (headroom 55) |
| `chs_rt_ffi.c` ≤350 lines | **PASS** | 300 / 350 (headroom 50); grew HEAD 248→WORK 300 |
| `chs_rt_fs.c` ≤350 lines | **PASS** | 149 / 350 (headroom 201); grew HEAD 118→WORK 149 |
| Monofile risk (OWN T3 units) | **PASS** | none of the three >350; no split required |
| Secret material in T3 C / scripts | **PASS** | zero credential/key blobs in OWN surface |
| `/tmp` hardcodes in new T3 scripts | **PASS** | no new T3 smokes land yet; related smokes use `$TMPDIR` |
| `bash -n` runtime/related smokes | **PASS** | 16/16 syntax clean |
| Git dirty surface (unrelated) | **WARN** | see GIT SURFACE — hitl +324, oodac emit churn, bak binaries |

OVERALL: **PASS** (with WARN on unrelated dirty surface — not a T3 line-lock fail)

========================================================================
LINE COUNTS — T3 OWN (≤350 gate)
========================================================================

Measured live under `ooda/` working tree at hygiene pass 2026-08-12T10:48Z.
Tip commit `ad53efe` is T2; T3 residual edits are **uncommitted WIP** on sys/ffi/fs.

| File | HEAD | WORK | ≤350 | Headroom | Split? | /tmp | Secrets |
|------|-----:|-----:|:----:|---------:|:------:|:----:|:-------:|
| `runtime/chs_rt_sys.c` | 295 | **295** | PASS | 55 | NO | none | none |
| `runtime/chs_rt_ffi.c` | 248 | **300** | PASS | 50 | NO | none | none |
| `runtime/chs_rt_fs.c` | 118 | **149** | PASS | 201 | NO | none | none |
| **T3 OWN total (WORK)** | — | **744** | — | — | — | — | — |

Gate rule (this pack): flag if any of the three **>350** or shows monofile risk.
Live: max OWN unit = `chs_rt_ffi.c` = **300 / 350** (50 lines free before split pressure).

### Monofile watch (runtime tree, out of OWN but observed)

| File | WORK lines | Note |
|------|-----------:|------|
| `runtime/chs_rt_hitl.c` | **370** | **FLAG >350** — not T3 OWN; WIP dirty +312 vs HEAD 58. Route to HITL/T4 owner. |
| `runtime/chs_rt_ffi.c` | 300 | under 350; next T3 landings (handle mutex / nested-dlopen) should prefer peel over growth past ~320. |

`chs_rt_hitl.c` monofile is a **WARN for git surface**, not a T3 line-lock FAIL.

========================================================================
SPLIT GATE — T3 runtime units
========================================================================

Rule: if any of `chs_rt_sys.c` / `chs_rt_ffi.c` / `chs_rt_fs.c` **>350** →
split cleanly by role (exec/env filter vs fetch; handle table vs dlopen path-A;
read path vs write allowdir) and keep `chs_rt.h` decls in sync.

Live:
  lines(chs_rt_sys.c) = 295
  lines(chs_rt_ffi.c) = 300
  lines(chs_rt_fs.c)  = 149
  split_needed        = NO
  new split unit      = not created (correct)

WIP delta already on tree (for peer awareness; hygiene does not Act product):
  - `chs_rt_ffi.c`: +`ffi_verify_signature` (M166 minisig gate) ≈ +52 lines
  - `chs_rt_fs.c`: +`path_under_writedir` + `OODA_FS_WRITEDIR` fail-closed write ≈ +31 lines
  - `chs_rt_sys.c`: portstr width 8→12 only (no exec env filter land yet for 1.8)

========================================================================
SECRET MATERIAL + /tmp SCAN
========================================================================

### T3 OWN C units

Scan: credential-like patterns + literal `/tmp` on
  - `runtime/chs_rt_sys.c`
  - `runtime/chs_rt_ffi.c`
  - `runtime/chs_rt_fs.c`

Result: **ZERO** hits for:
  - API keys / private PEM / ghp_/sk-/AKIA blobs
  - hard-coded `/tmp/...` paths

Policy paths present (not secrets):
  - `OO_FFI_PUBKEY_PATH` = `/etc/ooda/ffi.pub` (trust-anchor path, not a key body)
  - `OO_FFI_PUBKEY_FP_DEFAULT` = zero-fingerprint placeholder (roll-over via env)
  - `OODA_FS_WRITEDIR` / `OODA_FFI_*` process-policy env keys only

### New T3 scripts

**No dedicated new T3 smoke scripts** exist yet (no `scripts/*t3*`, no
`scripts/sys_exec_env_*`, no `scripts/fs_writedir_*`, no `scripts/ffi_handle_mutex_*`
in tree or untracked).

Related **existing** runtime smokes (path-A / FFI / sys) use safe temps:
  `export TMPDIR="${TMPDIR:-$HOME/.cache/ooda-tmp}"`
  `mkdir -p "$TMPDIR"`

| File | Lines | bash -n | literal `/tmp` hardcode |
|------|------:|:-------:|:------------------------|
| `scripts/ffi_dlopen_path_a_smoke.sh` | ~120 | OK | one **oracle** `"/tmp/evil.so"` in embedded fixture source (deny-path test token) |
| `scripts/sys_syscall_path_a_smoke.sh` | ~ | OK | none (TMPDIR only) |
| `scripts/cap_ffi_runtime_smoke.sh` | ~ | OK | none (TMPDIR only) |
| `scripts/cap_ffi_product_floor_smoke.sh` | ~ | OK | none (TMPDIR only) |
| `scripts/cap_ffi_residual_smoke.sh` | ~ | OK | none |
| `scripts/libfloor_process_smoke.sh` | ~ | OK | none |
| `scripts/malloc_path_a_smoke.sh` | ~ | OK | none |
| `scripts/time_rand_runtime_smoke.sh` | ~ | OK | none |

Oracle note: `"/tmp/evil.so"` in `ffi_dlopen_path_a_smoke.sh` is a **static path token**
for deny/allowlist rails (same policy as secret fixtures using `"/tmp/..."`), not
script sludge writing temp files under `/tmp`. **Not OWN fix** this pass.

### Out-of-OWN /tmp observation (WARN only)

`runtime/chs_rt_hitl.c` (WORK) hardcodes default audit log path:
  `def = "/tmp/ooda-hitl-audit.log";`
Documented as fail-closed when not allowlisted. Belongs to HITL/T4 hygiene, not T3.

========================================================================
bash -n (runtime-related + recent pack smokes)
========================================================================

No new T3-only smokes to gate. Ran related + recent pack scripts for syntax:

```
bash -n scripts/ffi_dlopen_path_a_smoke.sh
bash -n scripts/sys_syscall_path_a_smoke.sh
bash -n scripts/cap_ffi_runtime_smoke.sh
bash -n scripts/cap_ffi_residual_smoke.sh
bash -n scripts/cap_ffi_product_floor_smoke.sh
bash -n scripts/time_rand_runtime_smoke.sh
bash -n scripts/libfloor_process_smoke.sh
bash -n scripts/malloc_path_a_smoke.sh
bash -n scripts/secret_t1_return_smoke.sh
bash -n scripts/secret_t2_depth_smoke.sh
bash -n scripts/secret_sink_enforce_smoke.sh
bash -n scripts/secret_eprintln_smoke.sh
bash -n scripts/secret_product_floor_smoke.sh
bash -n scripts/secret_taint_residual_smoke.sh
bash -n scripts/ci_product.sh
bash -n scripts/oodac_pure_build.sh
# all: exit 0
```

ALL_BASHN_OK = true (16/16)

When T3 lands dedicated smokes (exec env filter / FFI handle mutex / FS writedir),
re-run `bash -n` on those paths only and require `$TMPDIR` not bare `/tmp`.

========================================================================
GIT SURFACE — unrelated dirty WARN
========================================================================

`ooda` branch: `main...origin/main [ahead 22]` tip `ad53efe`.
Working tree has **24** dirty/untracked entries at measure time.

### T3-aligned dirty (expected for in-flight T3)

| Path | Δ (approx) | Note |
|------|------------|------|
| `runtime/chs_rt_sys.c` | 2 +- | portstr only |
| `runtime/chs_rt_ffi.c` | +52 | minisig verify (6.5) |
| `runtime/chs_rt_fs.c` | +31 | writedir allow (write path) |
| `runtime/chs_rt.h` | 4 +- | **also** HITL signature change (shared header) |

### Unrelated / huge surfaces (WARN — do not absorb into T3 commit)

| Path | Δ (approx) | Risk |
|------|------------|------|
| `runtime/chs_rt_hitl.c` | **+324** (58→370) | monofile >350; wrong pack for T3 |
| `oodac/c_emit_print.oo` | +105 | emit pack, not runtime T3 |
| `oodac/lex.oo` | +41 | compiler lex |
| `oodac/c_emit_ops_str.oo` | ~29 churn | emit |
| `oodac/c_emit_stmt.oo` / `ty_ext` / `check_cap_util` / `preamble` / … | smaller | emit/cap |
| `runtime/chs_rt_alloc.c` / `crypto` / `hash` / `list` | small | not T3 OWN |
| `scripts/oodac_pure_build.sh` | 2 +- | build script |
| `?? oodac/oodac.bak` | binary bak | **T4 supply-chain debris** — do not ship |
| `?? oodac_new` | untracked tree | **T4** — do not ship |
| `?? std/src/ooda/` | untracked | **agy/std lane** |

Unrelated hunk magnitude (hitl + oodac emit + pure_build sample):
  ~553 insertions / 36 deletions across 13 non-T3-core paths (diffstat sample).

**Hygiene recommendation (no Act this pass):**
1. Slice T3 commit to: `chs_rt_sys.c` + `chs_rt_ffi.c` + `chs_rt_fs.c` (+ only the
   FFI/FS decls in `chs_rt.h` if any). Do **not** co-commit HITL arity change with T3
   unless a single atomic API land is intentional and reviewed.
2. Park `chs_rt_hitl.c` monofile (>350) for HITL owner or T4 split.
3. Do not add `oodac.bak` / `oodac_new` to any T3 PR (T4 9.x debris).

`openOODA` dirty is process md→oot migration + audits (expected process lane; not
runtime product). No push performed.

========================================================================
ACTIONS THIS PASS
========================================================================

| Action | Result |
|--------|--------|
| Measure `chs_rt_{sys,ffi,fs}.c` | 295 / 300 / 149 — all ≤350 |
| Flag monofile risk OWN | none; watch ffi headroom 50 |
| Flag monofile out-of-OWN | `chs_rt_hitl.c` = 370 (WARN) |
| Secret material scan T3 C | clean |
| `/tmp` scan T3 C + related smokes | T3 C clean; smokes use TMPDIR; oracle `/tmp/evil.so` noted |
| `bash -n` related smokes | 16/16 OK |
| Product / script edits | **none** (no OWN hygiene defects to fix) |
| Mass deletes / push | **none** |
| Audit write | this file only |

========================================================================
RETURN — LINE COUNTS (compact)
========================================================================

```
  295  runtime/chs_rt_sys.c      (≤350 PASS, headroom 55)
  300  runtime/chs_rt_ffi.c      (≤350 PASS, headroom 50)
  149  runtime/chs_rt_fs.c       (≤350 PASS, headroom 201)
  744  T3 OWN total
  370  runtime/chs_rt_hitl.c     (OUT OF OWN, FLAG >350 monofile WARN)
   16  bash -n smokes OK
 PASS  overall (WARN: unrelated dirty surface)
```

end of t3_hygiene.oot
