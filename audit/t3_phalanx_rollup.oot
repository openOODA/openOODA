# t3_phalanx_rollup.oot — Close Coordinator rollup (SPRINT T3)
# Date: 2026-08-12T11:03:00Z
# Role: Close Coordinator — SPRINT T3
# Wait: ~8m for peers + DE settle; merge openOODA/audit/t3_*.oot + live re-prove
# Scope: SPRINT T3 — runtime remaining ZT (IDs 1.8, 1.10, 4.5, 6.4; 6.5 optional)

## Inputs merged

| Peer audit | Role | Verdict (self) | Notes |
|------------|------|----------------|-------|
| `audit/t3_hygiene.oot` | Hygiene Enforcer | **PASS** (WARN unrelated dirty) | Early snapshot: sys/ffi/fs ≤350 at 295/300/149; tip later ffi **359** monofile WARN residual |
| `audit/t3_peer1_exec_env.oot` | Peer Verifier 1 (exec env) | **PASS** 19/19 live matrix | DE1.8 prefix-before-clearenv; env_cap rename |
| `audit/t3_peer2_ffi.oot` | Peer Verifier 2 (FFI mutex/nested) | **1.10 PASS / 4.5 FAIL** | **STALE** re 4.5 — written before DE nested land; live tip has `g_ffi_dlopen_depth` |
| `audit/t3_peer3_regression.oot` | Peer Verifier 3 (path-A regression) | **PASS** | T1/T2 secret green; FFI residual/floor green; soft minisig OS-load |
| `audit/t3_security.oot` | Security Auditor | **PASS** path-A seal + residuals | Child env gadgets BLOCK; nested refuse BLOCK; WRITEDIR fail-closed; hardlink FN residual |
| `audit/t3_integration.oot` | Integration Engineer | **PASS** | pure multi 428; host 956688 B; T3+T1+T2 smokes exit 0 |

Coordinator note: peer2 **4.5 FAIL** is an intermediate snapshot (pre-depth land). Post-land static markers, security live probe, integration, and coordinator smoke all show nested refuse present. **Do not leave T3 open solely on stale peer2.**

## Coordinator live re-prove (post-wait)

| Item | Value |
|------|--------|
| Tip commit (T3 land) | `344defe` (parent `ad53efe` T2) |
| Product binary | `ooda/oodac/oodac` **956688** B |
| md5 | `25fbea3fb79bc76edd9f380da8084070` |
| BuildID (integration) | `49c59bcf3e1a5f248c486d0810e2bc3062f00c35` |
| pure multi | unique_fns=**428** modules=**149** |
| `runtime_zt_t3_smoke.sh` | **PASS** exit 0 (coordinator re-run) |
| T1 return smoke regression | **PASS** exit 0 |
| T2 depth smoke regression | **PASS** exit 0 |
| `sys_exec_env_filter_smoke` (integration) | **PASS** |
| `fs_writedir_smoke` (integration) | **PASS** |

### Coordinator smoke transcript (hard gates)

```
OK 1.8 static: DE1.8 + OODA_/OO_ prefix filter markers
OK 1.8 product: no dangerous env / ENV_CLEAN (runtime harness)
OK 1.10 static: g_ffi_handles_mu
OK 4.5 static: nested dlopen refused + g_ffi_dlopen_depth
OK 6.4 static: OODA_FS_WRITEDIR fail-closed
OK 6.4 product: unset deny; inside OK / outside refuse
OK ci_product wires runtime_zt_t3_smoke
runtime_zt_t3_smoke: PASSED  exit=0
```

Soft (not hard-fail): `ffi_dlopen_path_a_smoke` OS-load rows under optional **6.5** minisig.

### ID gate (1.8 / 1.10 / 4.5 / 6.4)

| ID | Claim | Live mechanism / evidence | Result |
|----|-------|---------------------------|--------|
| **1.8** | filtered env on `sys_exec` | Child snapshots `OODA_`/`OO_` keys **before** `clearenv`; forced `PATH=/usr/bin:/bin`; peer1 19/19; smoke ENV clean | **FIXED** |
| **1.10** | lock FFI handle table | `g_ffi_handles_mu` on register/lookup/take; unlock matrix clean (peer2) | **FIXED** |
| **4.5** | reject nested dlopen | `__thread g_ffi_dlopen_depth` + process `g_ffi_dlopen_mu`; residual `"nested dlopen refused"` before non-recursive lock | **FIXED** (path-A) |
| **6.4** | write allowdir | `OODA_FS_WRITEDIR` fail-closed + `path_under_writedir` + `openat(O_NOFOLLOW)` | **FIXED** (path-A) |
| **6.5** (opt) | minisig before dlopen | `ffi_verify_signature` + minisign child env scrub (security OWN) | **LANDED fail-closed** — soft residual vs unsigned libc smokes |

## Cohort outcomes

| # | Agent | Outcome | Notes |
|---|-------|---------|-------|
| 1 | DE1.8 exec env | **SHIP** | prefix-before-clearenv; env_cap |
| 2 | DE FFI 1.10/4.5 | **SHIP** | mutex + TLS depth + dlopen_mu |
| 3 | DE FS 6.4 | **SHIP** | writedir + O_NOFOLLOW open |
| 4 | Peer1 exec | **PASS** | 19/19 |
| 5 | Peer2 FFI | **PARTIAL→superseded** | 1.10 PASS; 4.5 FAIL stale |
| 6 | Peer3 regression | **PASS** | T1/T2 green |
| 7 | Security | **PASS** + residuals | hardlink HIGH residual named |
| 8 | Integration | **PASS** | pure multi + smokes |
| 9 | Hygiene | **PASS** early; monofile WARN post-growth | ffi 359 >350 residual for T4/split |
| 10 | Test harness | **SHIP** | `runtime_zt_t3_smoke` + dedicated 1.8/6.4 + ci wire |
| 11 | Close Coordinator | **SHIP** | this rollup; T3 closed on live gate |

**Cohort summary:** Engineering SHIP · Adversarial PASS (w/ residuals) · Integration PASS · Hygiene PASS (monofile WARN) · Command SHIP.

## Mechanism (tip tree)

| Piece | Path | Role |
|-------|------|------|
| Exec env filter | `runtime/chs_rt_sys.c` | DE1.8 child filter + policy getenv |
| Handle mutex + nested refuse | `runtime/chs_rt_ffi.c` | 1.10 / 4.5; optional 6.5 minisig |
| Write allowdir | `runtime/chs_rt_fs.c` | 6.4 OODA_FS_WRITEDIR |
| Rollup smoke | `scripts/runtime_zt_t3_smoke.sh` | hard static+product gates |
| Dedicated rails | `scripts/sys_exec_env_filter_smoke.sh`, `fs_writedir_smoke.sh` | 1.8 / 6.4 matrices |
| CI wire | `scripts/ci_product.sh` | hard-req `runtime_zt_t3_smoke` |

## FIXED IDs (this rollup)

`1.8` `1.10` `4.5` `6.4` — **FIXED** (T3 closed on live product host + peer/integration/security green).

SPRINT: mark **T3 [x]**. Do not re-open unless a prove fails.

**Do not re-open T2** (already FIXED). T1 remains FIXED.

## Residuals (honest — do not block T3)

| Residual | Sev | Notes |
|----------|-----|-------|
| Write **hardlink** escape (W7) | HIGH | outside file hardlinked into writedir can be overwritten — not landlock multi-tenant seal |
| Minisig TOCTOU verify→dlopen | HIGH | no fdlopen / verify-by-fd |
| Signed `.so` constructor TCB | HIGH | nested `oo_dlopen` refused; raw `dlopen(3)` / RCE still possible inside ctor |
| Handle refcount / UAF | MED | concurrent dlsym after dlclose |
| Policy `OODA_*` passthrough to exec child | MED | intentional nested-ooda surface |
| `ffi_dlopen_path_a_smoke` unsigned OS load | MED | 6.5 fail-closed breaks prior “load libc” rows until signed fixtures |
| `chs_rt_ffi.c` monofile 359 >350 | LOW/MED | hygiene early PASS; post-land split pressure — T4 / peel residual |
| Unrelated dirty (HITL, emit, bak) | process | **not** in T3 commit slice |

## Evidence paths

| Item | Path |
|------|------|
| This rollup | `/home/jeryd/Projects/openOODA/openOODA/audit/t3_phalanx_rollup.oot` |
| Integration | `audit/t3_integration.oot` |
| Peer1 / Peer2 / Peer3 | `audit/t3_peer1_exec_env.oot`, `t3_peer2_ffi.oot`, `t3_peer3_regression.oot` |
| Security / hygiene | `audit/t3_security.oot`, `audit/t3_hygiene.oot` |
| Sources | `ooda/runtime/chs_rt_{sys,ffi,fs}.c` |
| Smoke / CI | `ooda/scripts/runtime_zt_t3_smoke.sh`, `sys_exec_env_filter_smoke.sh`, `fs_writedir_smoke.sh`, `ci_product.sh` |
| Product host | `ooda/oodac/oodac` 956688 B md5 `25fbea3f…` |

## Close decision

| Question | Answer |
|----------|--------|
| Live proves green (peers + integration + smokes)? | **YES** (peer2 4.5 superseded by post-land live) |
| Mark T3 [x] in SPRINT? | **YES** |
| FIXED IDs | **1.8, 1.10, 4.5, 6.4** |
| Commit runtime T3 + audit/SPRINT? | **YES** (local only, no push) |
| Re-open T2? | **NO** |

**T3 closed: yes**

end of t3_phalanx_rollup.oot
