# t4_hygiene.oot — Hygiene Enforcer / Docs Cartographer (T4 honesty)
# Date: 2026-08-12T11:10:00Z
# Role: Docs Cartographer — T4 Hygiene Honesty
# Tree: /home/jeryd/Projects/openOODA  (ooda/ + openOODA/)
# Tip (ooda): 344defe (T3 land) + WIP residual docs / gitignore
# OWN: residual + audit honesty only; no SPRINT close; no push
# WRITE: this audit + bootstrap residual pointers (AUDIT_RESIDUAL / CAP_FFI / SIGNING / DEBT)

========================================================================
SUMMARY
========================================================================

| Topic | VERDICT | Notes |
|-------|---------|-------|
| Bak binary ignore policy | **DOCUMENTED** | gitignore WIP patterns; debris may remain on disk ignored |
| DEBUG diagnostic vs leak (8.1) | **STALE / reclass** | live `tc_control_cond.oo` = product `ERR\ttype` only; no DEBUG token |
| minisign tool path residual | **OPEN residual** | no `ooda/tools/`; PATH or local binary or loud unsigned escape |
| Monofile pressure ffi >350 | **OPEN residual** | `chs_rt_ffi.c` = **359**; hitl 370; pack watch + MAX_LINES=256 |
| Nested `oo_dlopen` claim honesty | **FIXED in residual docs** | hard refuse via TLS depth (was wrongly “not hard ban”) |
| SPRINT T4 close | **NOT done** | residual ledger only; owner prove required |

OVERALL: **HONEST RESIDUAL PASS** (docs match tree; T4 not claimed closed)

========================================================================
LIVE MEASURES
========================================================================

| Path | Lines / state |
|------|----------------|
| `runtime/chs_rt_ffi.c` | **359** (>350 pack; >256 global) |
| `runtime/chs_rt_hitl.c` | **370** |
| `runtime/chs_rt_sys.c` | 329 |
| `runtime/chs_rt_fs.c` | 225 |
| `oodac/tc_control_cond.oo` | 0× `DEBUG`; println = `ERR\ttype\t…` only |
| `tools/minisign` | **absent** (no `ooda/tools/`) |
| PATH `minisign` | often absent on this host |
| `git check-ignore` bak / oodac_new | **ignored** via WIP `.gitignore` |
| On-disk bak | `oodac.bak`, `oodac.bak.t3pre`, `oodac_new` may still exist |

========================================================================
DOC UPDATES THIS PASS
========================================================================

| Doc | Change |
|-----|--------|
| `ooda/bootstrap/AUDIT_RESIDUAL.md` | §T4 hygiene honesty: bak, 8.1, minisign, monofile, scripts; line-lock O=0 stale |
| `ooda/bootstrap/CAP_FFI.md` | Nested policy = hard refuse; monofile 359 residual |
| `ooda/bootstrap/seed/SIGNING.oot` | `tools/minisign` policy (prior/WIP) — residual host verifier unchecked |
| `ooda/bootstrap/DEBT_HANDOFF.md` | Process items 19–22 T4 pointers |
| `ooda/bootstrap/CAPS_MATRIX.md` | T4 residual one-liner + nested hard refuse |
| `ooda/.gitignore` | bak/side binary patterns (WIP; policy = ignore not ship) |
| This audit | honesty ledger only |

========================================================================
POLICY SNAPSHOT (copy for peers)
========================================================================

1. **Bak:** ignore local rebuild debris; never commit; optional local delete.
2. **8.1:** product typecheck diagnostics ≠ DEBUG leak until a live non-ERR hit.
3. **minisign:** PATH preferred; optional operator `tools/minisign`; never invent ELF; unsigned = loud WARN only.
4. **ffi monofile:** 359 is residual peel pressure — not silent Lock green.

Floor regression (peer3): `audit/t4_peer3_regression.oot` — T1/T2/T3 smokes PASS.

========================================================================
ACTIONS / NON-ACTIONS
========================================================================

| Action | Result |
|--------|--------|
| Residual docs honesty | **updated** |
| Delete bak binaries | **not** (policy = ignore; operator may rm) |
| Install/vendor minisign | **not** |
| Peel `chs_rt_ffi.c` | **not** (docs residual only) |
| Mark SPRINT T4 [x] | **no** |
| Push | **no** |

end of t4_hygiene.oot
