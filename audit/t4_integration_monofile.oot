# t4_integration_monofile.oot — Integration 2 notes for T4 rollup
# Date: 2026-08-12T11:08:00Z
# Role: Integration 2 — Monofile Pressure
# Companion residual: `audit/t4_hygiene.oot` (full measure + split gate)
# Tip: 344defe · host oodac/oodac 956688 B · no push
## Verdict
**PASS with residual WARN** — no product C change; soft monofile pressure documented.

## Measures
| Unit | Lines | Soft ≤350 | Hard ≤600 |
|------|------:|:---------:|:---------:|
| `chs_rt_ffi.c` | 359 | WARN (−9) | PASS |
| `chs_rt_hitl.c` | 370 | WARN (−20) | PASS |
| `chs_rt_sys.c` | 329 | PASS | PASS |
| `chs_rt_fs.c` | 225 | PASS | PASS |

Runtime monofiles **>600 = 0** (floor holds).

## Act / non-act
| Decision | Why |
|----------|-----|
| **No FFI split** | +9 only; minisig peel is security-critical (T3 6.5), not pure; path peel would need include + pure multi prove mid concurrent T4; prefer residual |
| **No HITL thrash** | 370 lines, HMAC/atfork/crypto ordering; residual for later owner |
| **No pure multi rebuild** | no code change; T3 integration + peer3 floors already green |
| **No push** | policy |

## Prove (this agent)
```
gcc -c -Werror -O0 -I runtime runtime/chs_rt_ffi.c   → rc=0
gcc -c -Werror -O0 -I runtime runtime/chs_rt_hitl.c  → rc=0
bash scripts/hygiene_t4_smoke.oo                     → PASSED
```

## Later peel (not this turn)
1. `path_under_allowdir` + `path_under_sys_lib` → include-from-ffi unit (~−25 → under soft)
2. Optional: `ffi_verify_signature` → separate unit **after** security re-prove
3. Prove standalone gcc -c + `runtime_zt_t3_smoke` + side pure multi

## Rollup one-liner
> I2 monofile: ffi 359 / hitl 370 soft WARN; hard >600=0; **no split** (residual); hygiene_t4_smoke PASS; code left intact.

end of t4_integration_monofile.oot
