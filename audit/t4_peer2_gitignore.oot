# t4_peer2_gitignore.oot
# QA Peer 2 — Gitignore / Untracked Binary Coverage (T4)
# Date: 2026-08-12T11:07Z
# Role: QA Peer Verifier 2 (Gitignore / Untracked Binary Coverage)
# Tree: /home/jeryd/Projects/openOODA/ooda (+ monorepo root residual note)
# OWN: this audit; fix **only** critical ignore gaps if DE2 missed bak/tmp product patterns
# Tip (ooda HEAD): `344defe` — T3 runtime ZT (1.8/1.10/4.5/6.4)
# No push. No product feature work.

========================================================================
SUMMARY
========================================================================

| Claim | Expected | Actual | VERDICT |
|-------|----------|--------|---------|
| Product host `oodac/oodac` ignored | not ship as untracked | **IGNORED** (`.gitignore:53`) | **PASS** |
| Bak binaries `oodac/oodac.bak*` ignored or removed | no `??` ship risk | on disk **IGNORED** via DE2 `/oodac/oodac.bak*` | **PASS** |
| Side install `oodac/oodac.new` + root `oodac_new` ignored | no `??` ship risk | **IGNORED** (patterns present; new/alias probed) | **PASS** |
| Emit/tmp `*.tmp.c` / `oodac*.tmp*` ignored | no `??` ship risk | **IGNORED** (`*.c` + DE2 `/oodac/*.tmp.c`) | **PASS** |
| `git status -u` free of bak/tmp product binaries | empty of that class | **NO** bak/tmp debris in porcelain | **PASS** |
| DE2 critical pattern miss requiring peer fix | none | **none** — DE2 hunk sufficient | **PASS** (no edit) |

**OVERALL: PASS** — bak/tmp product binaries are **ignored** (not removed; local keep is OK).
Peer product/gitignore fix this pass: **none**.

SPRINT surface: T4 IDs **9.x** (bak/host binary debris / supply-chain). Seed
`bootstrap/seed/oodac` remains **TRACKED** by design (CI/bootstrap pin).

========================================================================
METHOD
========================================================================

1. `git status --porcelain=v1 -u` on `ooda` (product) + monorepo root residual.
2. `git check-ignore -v` matrix for every bak/side/tmp product path.
3. On-disk ELF inventory (`find` depth≤3, executable ELF).
4. Diff `.gitignore` for DE2 land vs tip `344defe`.
5. Touch-probe `oodac/oodac.new` + `oodac/oodac.alias` (create/check-ignore/rm).
6. Confirm `git status -u` no longer lists bak/tmp after DE2 patterns.
7. Fix gate: edit `.gitignore` **only** if a critical bak/tmp product path is still
   `??` / NOT_IGNORED. Threshold not met → audit only.

========================================================================
DE2 LAND (gitignore) — PROVED
========================================================================

Uncommitted WT hunk on `ooda/.gitignore` (index `677d3316` → working tree):

```
# Product bak / side binaries + emit temps (local rebuild debris)
/oodac/oodac.bak*
/oodac/oodac.new
/oodac/oodac.alias
/oodac/oodac*.tmp*
/oodac/*.tmp.c
/oodac_new
```

Placed after existing `/oodac/oodac` + `/oodac/oodac2` product-host ignores.
Also present (pre-T4, still green): `/bin/`, `/oodac_tmp/`, stage names,
`*.c` (with `!runtime/chs_rt*.c` exceptions), `.ooda-cache/`.

Related DE2 surface (not OWN to re-author here): `?? scripts/hygiene_t4_smoke.sh`
(+ `M scripts/ci_product.sh`) — T4 hygiene gate; peer2 scope is ignore coverage.

========================================================================
PROVE MATRIX — bak / tmp / product binaries
========================================================================

| Path | Disk | Size B | git | Rule / note |
|------|------|--------|-----|-------------|
| `oodac/oodac` | PRESENT | 956688 | **IGNORED** | `.gitignore:53` `/oodac/oodac` |
| `oodac/oodac.bak` | PRESENT | 937592 | **IGNORED** | DE2 `/oodac/oodac.bak*` |
| `oodac/oodac.bak.t3pre` | PRESENT | 955392 | **IGNORED** | DE2 `/oodac/oodac.bak*` |
| `oodac/oodac.new` | ABSENT | — | **IGNORED** (touch-probe) | DE2 `/oodac/oodac.new` |
| `oodac/oodac.alias` | ABSENT | — | **IGNORED** (touch-probe) | DE2 `/oodac/oodac.alias` |
| `oodac_new` | PRESENT | 956688 | **IGNORED** | DE2 `/oodac_new` |
| `oodac/oodac.tmp.c` | PRESENT | 990693 | **IGNORED** | `*.c` + `/oodac/*.tmp.c` |
| `oodac/main.oo.bin.tmp.c` | PRESENT | 16149 | **IGNORED** | `*.c` + `/oodac/*.tmp.c` |
| `oodac/c_emit_preamble.bin.tmp.c` | PRESENT | 24008 | **IGNORED** | `*.c` + `/oodac/*.tmp.c` |
| `oodac/oodac2` | ABSENT | — | **IGNORED** | pre-existing `/oodac/oodac2` |
| `oodac_tmp/` | ABSENT | — | n/a | pre-existing `/oodac_tmp/` |
| `bin/ooda` | PRESENT | 109968 | **IGNORED** | `/bin/` |
| `bootstrap/seed/oodac` | PRESENT | 878392 | **TRACKED** | intentional seed pin |
| `runtime/chs_rt_v2.c` | ABSENT | — | n/a | **9.1** alt runtime already gone |

Live command evidence:

```
$ git check-ignore -v oodac/oodac oodac/oodac.bak oodac/oodac.bak.t3pre oodac_new
.gitignore:53:/oodac/oodac          oodac/oodac
.gitignore:56:/oodac/oodac.bak*     oodac/oodac.bak
.gitignore:56:/oodac/oodac.bak*     oodac/oodac.bak.t3pre
.gitignore:61:/oodac_new            oodac_new

$ git status -u --porcelain | grep -E 'bak|oodac_new|oodac\.new|tmp\.c' || echo none
none
```

**Conclusion:** bak/tmp product binaries are **ignored** (kept on disk for local
regression / install rollback). No peer removal required. No DE2 critical miss.

========================================================================
GIT STATUS CLASSIFICATION — full ooda porcelain
========================================================================

Measured: `cd /home/jeryd/Projects/openOODA/ooda && git status --porcelain=v1 -u`
Branch: `main` ahead of `origin/main` by **23** (no push).

### A) DE2 / T4 hygiene surface (expected this wave)

| Path | Class | Ship risk | Note |
|------|-------|-----------|------|
| `M .gitignore` | **T4 OWN** | low (desired) | DE2 bak/side/tmp patterns — **PASS** under peer2 |
| `?? scripts/hygiene_t4_smoke.sh` | **T4 OWN** | process | DE2 smoke gate (8.1/9.x); not a binary |
| `M scripts/ci_product.sh` | **T4 OWN** | process | likely wire hygiene smoke; out of peer2 fix OWN |

### B) Bak / side / tmp product binaries

| Path | Class | Ship risk | Status |
|------|-------|-----------|--------|
| (none in porcelain) | bak/tmp | **mitigated** | present on disk but **IGNORED** — see prove matrix |

### C) Unrelated tracked dirt (not binary debris; do not co-ship as T4 ignore PR noise)

| Path group | Class | Note |
|------------|-------|------|
| `M oodac/c_emit_*.oo` + `lex.oo` + `check_cap_util.oo` | emit/cap WIP | prior lanes; not ignore debris |
| `M runtime/chs_rt_{h,alloc,crypto,hash,hitl,list}.*` | runtime WIP | HITL monofile etc.; not bak |
| `M bootstrap/*RESIDUAL*.md` + CAPS*.md | bootstrap docs | process |
| `M scripts/{caps_matrix,ffi_dlopen_path_a,oodac_pure_build}.sh` | smoke/build | prior residual |

### D) Non-binary untracked

| Path | Class | Ship risk | Note |
|------|-------|-----------|------|
| `?? std/src/ooda/bio/dna.oo` | **source** (std/agy) | process | **not** product binary; out of peer2 ignore OWN |

### E) Intentionally tracked binary (not debris)

| Path | Class | Note |
|------|-------|------|
| `bootstrap/seed/oodac` | seed product pin | TRACKED ELF 878392 B — correct; must not gitignore |

========================================================================
MONOREPO ROOT RESIDUAL (out of ooda product; WARN only)
========================================================================

`/home/jeryd/Projects/openOODA` super-repo shows untracked host ELFs:

| Path | Disk | git (super) | Class |
|------|------|-------------|-------|
| `oodac_bin` | PRESENT ELF | `??` | workspace host binary |
| `oodac_bin_new` | PRESENT ELF | `??` | workspace host binary |
| `test_pthread` | PRESENT ELF | `??` | lab binary |

These are **not** under `ooda/` product ignore policy (nested product owns
`.gitignore`). Super-repo root `.gitignore` covers lab `/*.c` etc. but not
these names. **Not a DE2 critical miss** of ooda bak/tmp patterns; residual
for monorepo hygiene owner / later T4 extend. Peer2 did **not** edit super
`.gitignore` (scope = product bak/tmp coverage).

========================================================================
CRITICAL-MISS SCAN (DE2)
========================================================================

| Gap candidate | Miss? | Action |
|---------------|-------|--------|
| `oodac/oodac.bak` untracked | **no** — IGNORED | none |
| `oodac/oodac.bak.t3pre` untracked | **no** — IGNORED | none |
| `oodac_new` untracked | **no** — IGNORED | none |
| `oodac/oodac.new` install temp | **no** — pattern | none |
| emit `*.tmp.c` under oodac/ | **no** — IGNORED | none |
| product `oodac/oodac` | **no** — pre-existing | none |
| `chs_rt_v2.c` alternate runtime | **no** — ABSENT | none |
| Broader `oodac/oodac.*` catch-all | optional harden only | not critical; DE2 explicit list sufficient for known install names |

**Peer fix applied: 0 files.**

========================================================================
VERDICT
========================================================================

| Item | Status |
|------|--------|
| bak product binaries ignored or removed | **IGNORED** (PASS) |
| tmp emit artifacts ignored | **IGNORED** (PASS) |
| `git status -u` free of bak/tmp product class | **PASS** |
| DE2 critical pattern miss | **none** |
| Peer `.gitignore` edit | **none** |
| Push | **none** |
| **OVERALL** | **PASS** |

### Leave-off (≤15 lines)

- DE2 landed `/oodac/oodac.bak*`, `/oodac/oodac.new`, `/oodac/oodac.alias`,
  `/oodac/oodac*.tmp*`, `/oodac/*.tmp.c`, `/oodac_new` on `ooda/.gitignore`.
- Live prove: bak + `oodac_new` + tmp.c all `git check-ignore`; porcelain has
  zero bak/tmp product `??` entries.
- Seed `bootstrap/seed/oodac` stays tracked (correct).
- Monorepo root `oodac_bin*` / `test_pthread` remain super-repo residual WARN.
- No peer fix required; audit only. No push.

end of t4_peer2_gitignore.oot
