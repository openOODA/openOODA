# t4_phalanx_rollup.oot — Close Coordinator rollup (SPRINT T4)
# Date: 2026-08-12T11:14:00Z
# Role: Close Coordinator — SPRINT T4
# Wait: peers + security + hygiene settle; merge openOODA/audit/t4_*.oot + live re-prove
# Scope: SPRINT T4 — hygiene / supply-chain debris (IDs 8.1, 9.1–9.8)

## Inputs merged

| Peer audit | Role | Verdict (self) | Notes |
|------------|------|----------------|-------|
| `audit/t4_peer1_debug.oot` | Peer Verifier 1 (DEBUG residual) | **PASS** 7/7 hunt matrix | 0× DEBUG in oodac/**/*.oo; `tc_control_cond` ERR\ttype only |
| `audit/t4_peer2_gitignore.oot` | Peer Verifier 2 (gitignore bak/tmp) | **PASS** | bak* / oodac_new / tmp.c IGNORED; porcelain free of bak class |
| `audit/t4_peer3_regression.oot` | Peer Verifier 3 (T1–T3 floor) | **PASS** | T1+T2+T3 smokes exit 0 on tip host |
| `audit/t4_security.oot` | Security Auditor (supply-chain) | **PASS** path-A + OWN mit | minisign body pin + abs verifier + env -i; CRIT residual tracked seed named |
| `audit/t4_hygiene.oot` | Hygiene Enforcer / residual honesty | **PASS** honesty ledger | bak policy documented; monofile soft WARN residual |
| `audit/t4_integration_monofile.oot` | Integration 2 (monofile pressure) | **PASS** + soft WARN | ffi 359 / hitl 370; hard >600=0; no split |

Coordinator note: hygiene residual ledger explicitly does **not** close SPRINT alone — close is this rollup on **peers + smokes + security path-A**. Tracked-seed policy contradiction (security B3/G1) is an honest residual, not a T4 debris reopen (same class as T3 hardlink residual).

## Coordinator live re-prove (post-wait)

| Item | Value |
|------|--------|
| Tip commit (product feature parent) | `344defe` (T3 land) |
| T4 hygiene land (ooda) | `0bfd752` — `chore(t4): hygiene / supply-chain debris` |
| Product binary | `ooda/oodac/oodac` **956688** B |
| md5 (live host at close) | `0208bab43c9cd49718d2b859a84ef94c` |
| BuildID | `d01dbdc2cb79dbe12389efa91c5d54034edc09c2` |
| `hygiene_t4_smoke.sh` | **PASS** exit 0 (coordinator re-run) |
| T1 return smoke regression | **PASS** exit 0 |
| T2 depth smoke regression | **PASS** exit 0 |
| T3 runtime ZT smoke regression | **PASS** exit 0 |

### Coordinator smoke transcript (hard gates)

```
OK bash -n hygiene_t4_smoke.sh
OK oodac.bak* present and gitignored (2 file(s))
OK no patch_awk / boot_dbg / cap-strip scripts
OK tc_control_cond.oo: no DBG_/DEBUG println (ERR type OK)
hygiene_t4_smoke: PASSED  exit=0

OK secret_t1_return_smoke  exit=0
OK secret_t2_depth_smoke   exit=0
OK runtime_zt_t3_smoke     exit=0  (soft ffi_dlopen_path_a exit=1 residual; not hard)
```

Live matrix extras:
- `grep -rni DEBUG oodac --include='*.oo'` → **0**
- `git check-ignore` bak* + `oodac_new` → **IGNORED**
- `find` patch_awk / boot_dbg / cap_strip → **empty**
- `chs_rt_v2.c` → **ABSENT**

### ID gate (8.1, 9.1–9.8)

| ID | Claim | Live mechanism / evidence | Result |
|----|-------|---------------------------|--------|
| **8.1** | strip DEBUG println leak | peer1 7/7; smoke no DBG_/DEBUG; only product `ERR\ttype` in `tc_control_cond.oo` | **FIXED** |
| **9.1** | `chs_rt_v2.c` alternate runtime | ABSENT under `runtime/`; peer2 + security | **FIXED** (absent) |
| **9.2** | bak host binaries not ship | `/oodac/oodac.bak*` gitignored; smoke PASS; may remain on disk | **FIXED** (ignore policy) |
| **9.3** | side install `oodac_new` / `.new` / `.alias` | gitignore patterns; peer2 touch-probe | **FIXED** |
| **9.4** | emit/tmp `*.tmp.c` / `oodac*.tmp*` | ignored (pre + DE2) | **FIXED** |
| **9.5** | no `patch_awk` / boot_dbg / cap-strip | find empty; smoke PASS | **FIXED** |
| **9.6** | minisign trust path (script) | OWN: `SEED_PUBKEY_B64` body pin; abs `/usr/bin`/`/bin` first; `env -i` verifier | **FIXED** (path-A) |
| **9.7** | secret-key / tools minisign ignore | `*.sec`, `*.secret.key`, `**/minisign.key`, `/tools/minisign` | **FIXED** |
| **9.8** | host ELF sludge (workspace) | super-repo ignore `oodac_bin*` / `test_pthread`; product bak class sealed | **FIXED** (path-A) |

## Cohort outcomes

| # | Agent | Outcome | Notes |
|---|-------|---------|-------|
| 1 | DE1 DEBUG strip (8.1) | **SHIP** / already clean | live tree 0× DEBUG; ERR diagnostics only |
| 2 | DE2 gitignore bak/tmp | **SHIP** | bak* / oodac_new / tmp patterns |
| 3 | DE supply-chain / smoke | **SHIP** | `hygiene_t4_smoke` + `ci_product` wire |
| 4 | Security minisign pin | **SHIP** | body pin + abs + env -i on both bootstrap scripts |
| 5 | Peer1 DEBUG | **PASS** | 7/7 |
| 6 | Peer2 gitignore | **PASS** | no critical miss |
| 7 | Peer3 floor regression | **PASS** | T1/T2/T3 green |
| 8 | Security audit | **PASS** + residuals | tracked seed CRIT residual named |
| 9 | Hygiene honesty | **PASS** residual docs | monofile soft WARN |
| 10 | Integration monofile | **PASS** + WARN | no ffi split this turn |
| 11 | Close Coordinator | **SHIP** | this rollup; T4 closed on live gate |

**Cohort summary:** Engineering SHIP · Adversarial PASS (w/ residuals) · Integration PASS · Hygiene PASS · Command SHIP.

## Mechanism (tip tree)

| Piece | Path | Role |
|-------|------|------|
| Bak/side/tmp ignore | `ooda/.gitignore` | DE2 + security secret globs |
| Hygiene gate | `ooda/scripts/hygiene_t4_smoke.sh` | hard absence + ignore + DEBUG gates |
| CI wire | `ooda/scripts/ci_product.sh` | hard-req `hygiene_t4_smoke` |
| Minisign body pin | `scripts/bootstrap_{no_cargo,2stage}.sh` | SEED_PUBKEY_B64 + env -i -V |
| Residual honesty | `bootstrap/AUDIT_RESIDUAL.md` (+ CAP_FFI / SIGNING / DEBT) | monofile + minisign + bak policy |
| Super host ELF ignore | monorepo root `.gitignore` | oodac_bin* / test_pthread |

## FIXED IDs (this rollup)

`8.1` `9.1` `9.2` `9.3` `9.4` `9.5` `9.6` `9.7` `9.8` — **FIXED** (T4 closed on peers + hygiene_t4 + T1–T3 smokes green).

SPRINT: mark **T4 [x]**. Do not re-open unless a prove fails.

**Do not re-open T1–T3** (already FIXED).

## Residuals (honest — do not block T4)

| Residual | Sev | Notes |
|----------|-----|-------|
| Tracked `bootstrap/seed/oodac` vs gitignore claim | **CRIT** | index still has seed blob; SIGNING “gitignored” was false — needs CI owner `git rm --cached` **or** rewrite policy to admit tracked seed + hash pin |
| Host path strings inside seed ELF | MED | opsec smell; rebuild seed in clean path |
| `OODA_SEED_ALLOW_UNSIGNED=1` loud skip | MED | by-design residual escape |
| `tools/minisign` if force-added | HIGH | ignored; still unsigned second TCB if committed |
| Super-repo force-add of ignored ELFs | MED | process control only |
| `chs_rt_ffi.c` 359 / `chs_rt_hitl.c` 370 soft monofile | LOW/MED | hard >600=0 holds; peel later |
| Unrelated dirty (emit WIP, hitl, std untracked) | process | **not** in T4 commit slice |

## Evidence paths

| Item | Path |
|------|------|
| This rollup | `/home/jeryd/Projects/openOODA/openOODA/audit/t4_phalanx_rollup.oot` |
| Peer1 / Peer2 / Peer3 | `audit/t4_peer1_debug.oot`, `t4_peer2_gitignore.oot`, `t4_peer3_regression.oot` |
| Security / hygiene / monofile | `audit/t4_security.oot`, `t4_hygiene.oot`, `t4_integration_monofile.oot` |
| Smoke / CI | `ooda/scripts/hygiene_t4_smoke.sh`, `ci_product.sh` |
| Product host | `ooda/oodac/oodac` 956688 B md5 `0208bab4…` |

## Close decision

| Question | Answer |
|----------|--------|
| Live proves green (peers + hygiene + smokes)? | **YES** |
| Mark T4 [x] in SPRINT? | **YES** |
| FIXED IDs | **8.1, 9.1–9.8** |
| Commit ooda hygiene + openOODA docs? | **YES** (local only, no push) |
| Re-open T1–T3? | **NO** |

**T4 closed: yes**

end of t4_phalanx_rollup.oot
