# t5_ship_readiness.oot — Ship Readiness Cartographer (T5)
# Date: 2026-08-12T11:18:14Z
# Role: Domain Expert 3 — Ship Readiness Cartographer
# Tree: /home/jeryd/Projects/openOODA  (ooda/ + std/ + openOODA/)
# Scope: inventory only — possible push/PR readiness; NEVER push; NEVER open PR
# Policy: human decides; this file is a map not a green light

========================================================================
SUMMARY
========================================================================

| Repo | Branch | Ahead | Behind | Working tree | Secrets | Ship now? |
|------|--------|------:|-------:|--------------|---------|-----------|
| `ooda` | `main` | **24** | **0** | **DIRTY** (32 M + 1 ??) | **PASS** | **NO** — commit WIP or stash; strip HITL/bak/nested std |
| `std` | `main` | **21** | **0** | **DIRTY** (215 M + ~604 ??) | **PASS** | **NO** — massive uncommitted peel/WIP; do not bulk-add |
| `openOODA` | `main` | **28** | **0** | **DIRTY** (md→oot mid-flight) | **PASS** | **NO** — finish migration commit first |

OVERALL: **NOT SHIP-READY as working trees.**  
Committed tips are ahead of local `origin/*` with **no behind**. Possible PRs exist **only after** dirty surfaces are resolved (commit intentionally, or leave uncommitted).

**Fetch caveat:** local `origin/*` refs are stale relative to live GitHub
(`ooda`/`openOODA` remotes last touched ~2026-08-11; `std` ~2026-07-28).
Re-fetch before any real push so ahead/behind is not a lie. This audit did **not** fetch.

**Action policy this pass:** measure + write this audit only. No push. No PR. No force.

========================================================================
SECRETS SCAN (all three)
========================================================================

| Check | ooda | std | openOODA |
|-------|:----:|:---:|:--------:|
| `.keys-local` / `*keys-local*` on disk | none | none | none |
| `*.pem` / `id_rsa` / `id_ed25519` / `*.p12` / `*.pfx` / `*.jks` | none | none | none |
| `BEGIN *PRIVATE KEY` content under tree | none | none | none |
| git ls-files paths: keys-local / pem / secret.key / minisign.key | none | none | none |
| untracked names matching key/env/bak/secret | none | none | none |
| bak binaries present on disk | **yes** (ignored) | none found | none found |
| minisign **public** artifacts only | `bootstrap/seed/oodac.pub` + `.minisig` | — | — |

### Notes (not secrets)

- `ooda/oodac/c_emit_secret.oo` is **product secret-scan emit code** (tracked feature), not a credential.
- `ooda/.gitignore` already pins:
  - `/bootstrap/seed/.keys-local/`
  - `*.secret.key`, `**/minisign.key`
  - `/oodac/oodac.bak*`
  - `.env` / `.env.*` / `.envrc`
- On-disk bak (do **not** ship):
  - `ooda/oodac/oodac.bak` (~938 KB)
  - `ooda/oodac/oodac.bak.t3pre` (~955 KB)
  - `git check-ignore`: both match `/oodac/oodac.bak*` — **PASS ignore**
- `std` / `openOODA` have weaker or no key-ignore surface:
  - `std/.gitignore`: `.env*` only (no bak/keys-local patterns)
  - `openOODA`: **no `.gitignore` at all** — residual hygiene risk if debris appears

**SECRETS VERDICT: PASS** — no `.keys-local`, no private keys, no PEM private material in any of the three trees at audit time.

========================================================================
1) ooda  —  https://github.com/openOODA/ooda.git
========================================================================

### Tracking

| Item | Value |
|------|--------|
| Path | `/home/jeryd/Projects/openOODA/ooda` |
| Branch | `main` @ `0bfd752` |
| Upstream | `origin/main` @ `661466d` |
| Ahead / Behind | **24 / 0** (local origin; re-fetch before push) |
| Staged | empty |
| Dirty | **YES** |

### Tip (HEAD)

```
0bfd752 chore(t4): hygiene / supply-chain debris — ignore bak, smoke, minisign pin
```

### Commits ahead of origin/main (24) — themes

| Bucket | Count | Example tips |
|--------|------:|--------------|
| ZT / secret / runtime fix (T1–T4) | ~8 | `fix(t1)` secret taint; `fix(t2)` scan depth; `fix(t3)` exec/FFI/FS; `chore(t4)` bak/minisign |
| Path-A product / metamorphic | ~5 | phalanx, decoys, FFI dlsym, alloc raise |
| oodac free-name / typecheck | ~6 | String/Result tables, if/while field-chain, M171 SEGV |
| Agent token-min CLI | 2 | context/digest/health/pack |
| Docs / seed / test | ~3 | README residual, seed refresh, Issue #12 rails |

Full shortlog (newest first):

```
0bfd752 chore(t4): hygiene / supply-chain debris — ignore bak, smoke, minisign pin
344defe fix(t3): runtime ZT — exec env filter, FFI mutex/nested refuse, FS writedir
ad53efe fix(t2): secret scan depth — full args, nested RHS, field/method, comment skip
1f67f4d fix(t1): secret return taint, alias rebind, if/match LBRACE prop
4b70f14 fix: zero-trust audit CRITs — caps once/entropy, alloc, secrets, crypto wipe
56f0f19 feat: metamorphic path-A emit drive, decoy styles, runtime mix APIs
9068228 feat: metamorphic path-A phalanx — decoys, shuffle, runtime mix
f41c8ec feat: path-A metamorphic floor — layout decoys + meta_epoch
206af08 ship: path-A product depth — alloc raise cap + load_import honesty
cf5b121 ship: path-A FFI dlsym/dlclose, seed minisign, bitwise residual, HITL smokes
89d6e9b fix(seed): refresh seed compiler to fix CI failures
ffb9a03 fix(oodac): M178 #56 pure dual-green — lex caret + bc ARROW skip
4c208c4 fix(zt): fail-closed EnvCap/FsCap for hitl/list/ffi/tls policy getenv
9e248ab docs: README residual pointer to PM.md (M177 #46)
4317efb fix: migrate disabled; free-name Int mistypes; tc_diag line helper
de59bc0 test: Issue #12 typecheck bug rails (pass/fail/xfail)
ff0187d fix(oodac): typecheck if/while field-chain EQEQ/NEQ conditions (Issue #11)
9e87bc9 fix: free-name String/List table + struct-lit ident via byte_at
adeb3c9 fix: env String annotations + free-name String type table
c554319 fix: emit free-name Result/String types; main Result lets untyped
3c56f35 feat: product token-min CLI — ooda context/digest/health/err-digest/pack
dcdcb6e feat: agent token-minimizer pack — digest, emit_health, err_digest, context
72fbe08 fix: M171 untyped-let SEGV — free-name table, skip mega c_call_result_ty
07ecc93 feat: M170 product residual closeout — struct typedefs, caret, ret_c_ty
```

### Dirty surface (do not push as-is)

| Class | N | Paths |
|-------|--:|-------|
| bootstrap residual docs | 2 | `ARC_M2_RESIDUAL.md`, `STATIC_CAPS.md` |
| oodac emit/check/lex/cli | 21 | `c_emit_*`, `check_*`, `lex.oo`, `main.oo`, `cli_*.oo`, `wasm_emit_mem.oo` |
| runtime | 6 | `chs_rt.h`, `chs_rt_alloc.c`, `chs_rt_crypto.c`, `chs_rt_hash.c`, **`chs_rt_hitl.c`**, `chs_rt_list.c` |
| scripts | 3 | `caps_matrix_smoke.sh`, `ffi_dlopen_path_a_smoke.sh`, `oodac_pure_build.sh` |
| untracked nested | 1 | `std/src/ooda/` → `std/src/ooda/bio/dna.oo` (66 lines) |

Diffstat dirty: **~32 files, +663 / −100** (approx; includes HITL blow-up).

### HITL WIP (NOT ship until intentional)

| File | HEAD lines | WORK lines | Δ |
|------|----------:|----------:|--:|
| `runtime/chs_rt_hitl.c` | 58 | **370** | **+312** (>350 monofile FLAG) |

This is the primary **HITL WIP** gate. Prior hygiene audits already flagged >350. Do **not** silent-include in a “T1–T4 land” PR unless HITL owner proves + peels.

### What NOT to ship (ooda)

1. **`oodac/oodac.bak`, `oodac/oodac.bak.t3pre`** — local rebuild debris; ignored; never `git add -f`.
2. **`bootstrap/seed/.keys-local/`** — must never exist in commits (absent now; keep gitignore).
3. **HITL monofile WIP** (`chs_rt_hitl.c` 370) unless split/proved and titled as HITL work.
4. **Nested `std/src/ooda/bio/dna.oo`** inside **ooda** repo — wrong tree; belongs in `std` or delete/move.
5. Unreviewed oodac emit churn bundled with T1–T4 security commits without separate prove.
6. Any minisign **secret** key if dropped later next to seed (public `.pub`/`.minisig` OK).

### Recommended PR titles (after clean/commit of chosen slices)

| Slice | Recommended title | Includes (committed tip range) | Exclude |
|-------|-------------------|--------------------------------|---------|
| A — ZT security land | `fix(zt): T1–T4 secret taint, scan depth, runtime ZT, bak hygiene` | `4b70f14`…`0bfd752` (or full 24 if CI green) | dirty HITL, bak, nested std |
| B — path-A product | `feat(path-a): metamorphic floor + FFI dlsym + product depth` | `cf5b121`…`56f0f19` | HITL WIP dirty |
| C — oodac free-name | `fix(oodac): free-name types, field-chain conds, M171 SEGV` | `07ecc93`…`ff0187d` cluster | unrelated runtime dirty |
| D — agent CLI | `feat: token-min CLI (context/digest/health/pack)` | `dcdcb6e` + `3c56f35` | — |
| Single fat PR (if human prefers one) | `ship: path-A + ZT T1–T4 + oodac free-name residual (24 commits)` | all 24 ahead | **must clean dirty first** |

### ooda readiness verdict

| Gate | Status |
|------|--------|
| Ahead commits exist | YES (24) |
| Clean tree for push | **NO** |
| Secrets | PASS |
| Bak ignored | PASS |
| HITL WIP isolated | **NO** (dirty, oversized) |
| Nested wrong-tree untracked | **FAIL** (`std/` under ooda) |
| **Ready to push/PR** | **NO** |

========================================================================
2) std  —  https://github.com/openOODA/std.git
========================================================================

### Tracking

| Item | Value |
|------|--------|
| Path | `/home/jeryd/Projects/openOODA/std` |
| Branch | `main` @ `6ed3f86` |
| Upstream | `origin/main` @ `f541c06` |
| Ahead / Behind | **21 / 0** (local origin; **very stale fetch** ~2026-07-28) |
| Staged | empty |
| Dirty | **YES — large** |

### Tip (HEAD)

```
6ed3f86 honesty(sec/net): fail-close wireguard facade + JWT residual notes
```

### Commits ahead of origin/main (21) — themes

| Bucket | Count | Notes |
|--------|------:|-------|
| M172 monofile splits / shims | **14+** | sec/crypto, quantum, edge, fin, pe, landlock, waf, collision/keys/mpc/orca, … |
| Issue #6 / #60 paren field-access | 2–3 | `fix(ooda): SPRINT Issue #6…` |
| residual hash stubs + README.oot | 2 | `hash/sha256`, `hash/blake3`, docs |
| honesty residual (sec/net) | 1 | wireguard facade + JWT notes |

Newest / oldest:

```
6ed3f86 honesty(sec/net): fail-close wireguard facade + JWT residual notes
141f2ad split(std): residual M172 monofile shims — collision keys mpc orca
3d614ad docs(std): replace README.md with README.oot …
486f576 chore(std): land residual top-level hash/sha256 + hash/blake3 stubs
…
899815b fix(ooda): SPRINT Issue #6 — paren field access for &Struct params
```

### Dirty surface (do not push as-is)

| Class | Count | Notes |
|-------|------:|-------|
| Modified tracked | **215** | mostly post-split shims / engine peels; **+2239 / −8711** (order-of-magnitude; live drift OK) |
| Untracked files | **~604** | huge net-new domain surface + root debris |
| Porcelain lines | **~524** | status paths (dirs collapsed in `??`) |

Untracked concentration (top):

| Prefix | ~files | Risk |
|--------|-------:|------|
| `src/ooda/**` | 266 | product domain — review before add |
| `src/net`, `src/sys`, `src/hw`, … | ~100+ | platform surface |
| root `test_*.oo`, `src/test*.oo` | 13+ | **scratch — do not ship** |
| `archive/`, `agent/`, `ai/`, `ui/`, … | many | triage; archive may be intentional modules |

### What NOT to ship (std)

1. **Root / `src/` scratch tests:** `test_field.oo`, `test_point.oo`, `test_prop.oo`, `test_struct.oo`, `src/test.oo`, `src/test2.oo`…`src/test11.oo`, `src/test_string.oo`.
2. **Unreviewed bulk `git add .`** of ~604 untracked files — high monofile / stub / incomplete risk.
3. **Secrets** if any future key material lands under `src/ooda/crypto/keys*` without ignore rules (none present now).
4. **Bak / `*~`** — `*~` is gitignored; no bak found; keep it that way.
5. **Half-peeled modified set** (215 files) mixed with untracked siblings — ship either the **committed 19** after clean, or a deliberate curated add, not the whole dirty tree.

### Recommended PR titles (after clean)

| Slice | Recommended title | Notes |
|-------|-------------------|-------|
| A — monofile land | `refactor(std): M172 monofile splits — sec/crypto/quantum/edge shims (≤256)` | core of the 21 |
| B — Issue #6 close | `fix(std): Issue #6/#60 paren field-access residual` | if separated |
| C — docs + hash stubs | `docs(std): README.oot + residual hash/sha256+blake3 stubs` | small tail |
| Fat (clean tree only) | `ship(std): M172 splits + Issue #6 field-access + README.oot (21 commits)` | **only if working tree clean or intentionally committed** |

### std readiness verdict

| Gate | Status |
|------|--------|
| Ahead commits exist | YES (21) |
| Clean tree for push | **NO** (215 M + ~604 ??) |
| Secrets | PASS |
| Scratch tests excluded | **must enforce** before any add |
| **Ready to push/PR** | **NO** |

========================================================================
3) openOODA  —  https://github.com/openOODA/openOODA.git
========================================================================

### Tracking

| Item | Value |
|------|--------|
| Path | `/home/jeryd/Projects/openOODA/openOODA` |
| Branch | `main` @ `2418a85` |
| Upstream | `origin/main` @ `e160abe` |
| Ahead / Behind | **28 / 0** (local origin; re-fetch before push) |
| Staged | empty |
| Dirty | **YES** — markdown → `.oot` migration mid-flight |

### Tip (HEAD)

```
2418a85 docs(t4): close SPRINT T4 — FIXED 8.1, 9.1–9.8
```

### Commits ahead of origin/main (28) — themes

All **docs / SPRINT / honesty / pin** work (T1–T4 close, residual maps, metamorphic RFCs, monorepo tip pins). No runtime code in this repo’s ahead set.

```
2418a85 docs(t4): close SPRINT T4 — FIXED 8.1, 9.1–9.8
ff37895 docs(t3): close SPRINT T3 — FIXED 1.8, 1.10, 4.5, 6.4
7a9170a docs(t2): close SPRINT T2 — FIXED 2.5–2.7, 5.4–5.6, 6.2
…
3ebef74 docs: SPRINT disposition — strip solved path A; residual pure dual-green
```

### Dirty surface (md → oot migration)

| State | Paths |
|-------|-------|
| Deleted tracked `.md` | `DESIGN.md`, `OODA.md`, `PM.md`, `PROBE.md`, `README.md`, `RULES.md`, `SPRINT.md`, `SWARM.md`, issue templates, rfc `0000–0002` `.md`, … |
| Deleted other | `LANE.oot`, `SPEC.oot`, `scripts/check_oot.oo` |
| Modified tracked | `BOOTSTRAP.oot`, `DESIGN.oot`, `SPEC.md` |
| Untracked replacements | `README.oot`, `OODA.oot`, `PM.oot`, `PROBE.oot`, `RULES.oot`, `SWARM.oot`, `CAPS.oot`, `SCRIPTS.oot`, issue templates `.oot`, rfc `.oot`, RFCs 002–004, scripts (`check_board`, `filter_failures`, `purge_audit`, `verify_all`), `bootstrap/STDLIB_CRYPTO_MATH.oot` |
| Untracked audits | `audit/std_caps_batch_2.oot`, `std_caps_batch_hw_fix.oot`, `t1_peer2_alias.oot`, `t5_peer3_regression.oot` (+ this file when untracked) |

**SPEC inconsistency:** `SPEC.md` still present/modified; `SPEC.oot` deleted; migration incomplete for SPEC.

**SPRINT.oot** is tracked at tip and not in porcelain as dirty (close already committed); working file exists.

### What NOT to ship (openOODA)

1. **Half-migrated pair** — delete `.md` without adding matching `.oot` (or leave orphan deletes). Finish rename/add in one commit.
2. **Incomplete SPEC** — do not ship `D SPEC.oot` + `M SPEC.md` without a single chosen format.
3. **Local-only audit noise** — optional; shipping `audit/t5_*.oot` is fine for honesty, but avoid private operator notes if any appear later.
4. **Secrets / bak** — none found; **add a root `.gitignore`** before scale (currently **missing**).
5. **Claims of product ship** — this repo’s ahead set is docs; do not title as runtime land.

### Recommended PR titles (after migration commit)

| Slice | Recommended title | Notes |
|-------|-------------------|-------|
| A — sprint ledger | `docs: SPRINT T1–T4 close + residual honesty ledger (28 commits)` | committed tip only |
| B — format migration | `docs: migrate root/rfc/templates Markdown → .oot` | **needs clean commit of dirty first** |
| Combined | `docs(openOODA): T1–T4 SPRINT close + .oot root migration` | only after dirty resolved |

### openOODA readiness verdict

| Gate | Status |
|------|--------|
| Ahead commits exist | YES (28) |
| Clean tree for push | **NO** (migration WIP) |
| Secrets | PASS |
| `.gitignore` present | **MISSING** (WARN) |
| **Ready to push/PR** | **NO** |

========================================================================
CROSS-REPO SHIP MATRIX
========================================================================

| Question | Answer |
|----------|--------|
| Any repo clean + ahead → instant PR? | **No** — all three dirty |
| Any secrets blocking? | **No** — secrets PASS all three |
| Any bak at risk of commit? | **ooda only** — present but **gitignore PASS** |
| HITL WIP? | **ooda** `chs_rt_hitl.c` WORK 370 — **hold** |
| Largest uncommitted blast radius | **std** (215 M + ~604 ??) |
| Docs-only ship candidate | **openOODA** tip (28) after md→oot finish |
| Code ship candidate | **ooda** tip (24) after dirty purge; **std** tip (21) after dirty purge |
| Push performed this pass? | **NO** |
| PR opened this pass? | **NO** |

========================================================================
HUMAN CHECKLIST (before any push)
========================================================================

1. `git fetch origin` in each repo; re-check ahead/behind.
2. **ooda**
   - Leave bak ignored; do not `add -f`.
   - Drop or relocate `std/src/ooda/bio/dna.oo` out of ooda.
   - Decide HITL: commit as titled HITL PR after peel, or stash/revert dirty `chs_rt_hitl.c`.
   - Commit or stash remaining oodac/runtime/scripts/bootstrap dirt intentionally.
3. **std**
   - Do **not** `git add -A`.
   - Exclude `test_*.oo` / `src/test*.oo` scratch.
   - Either reset dirty to tip for a tip-only PR, or curated commit of proven modules only.
4. **openOODA**
   - Finish md→oot in one coherent commit (incl. SPEC choice).
   - Add `.gitignore` (env, bak, keys-local, editor junk) — currently absent.
5. Secrets re-scan: `find` for `.keys-local`, `*.pem`, `BEGIN *PRIVATE KEY` before push.
6. Human opens PR titles from tables above; cartographer does not.

========================================================================
RECOMMENDED PR TITLE CARD (copy/paste candidates)
========================================================================

### ooda
- `fix(zt): T1–T4 secret taint, scan depth, runtime ZT, bak hygiene`
- `feat(path-a): metamorphic floor + FFI dlsym + product depth`
- `ship: path-A + ZT T1–T4 + oodac free-name residual (24 commits)`  # only if clean

### std
- `refactor(std): M172 monofile splits — sec/crypto/quantum/edge shims (≤256)`
- `ship(std): M172 splits + Issue #6 field-access + README.oot (21 commits)`  # only if clean

### openOODA
- `docs: SPRINT T1–T4 close + residual honesty ledger (28 commits)`
- `docs: migrate root/rfc/templates Markdown → .oot`  # after dirty resolved

========================================================================
END — t5_ship_readiness.oot
# No push. No PR. Human decides.
========================================================================
