# Task 9 — F26 / F20 / env honesty (grok)
**Date:** 2026-08-13
**Role:** grok audit. Did not edit SPRINT.oot.
**Axiom:** RULES 1.9 / 1.10. Residual honesty is not a ban.

## Clock this turn
`/usr/bin/time` `oodac check oodac/main.oo`:

| ELF | ELAPSED | RSS | stdout |
|-----|--------:|----:|--------|
| before this slice rebuild | 39.29 s | 16377276 KB (~16.0 GiB) | OK |
| after this slice rebuild | 30.15 s | 18341064 KB (~17.5 GiB) | OK |

Not 28 s / 12 GB. Not 1–2 s / 39 MB. Not 1.7 s / 41 MB. Wall moved a bit. RSS did not.

## 1. 22.8 GB at 02:00 then 39 MB at 02:14
22.8 GB at ~02:00 is real (f26_phalanx_rollup, f26_residual, first-wave ~22 239 MiB).

39 MB at 02:14 is **not** an honest close of that path. Same residual file says the F26 harness is unbuilt and also ALL OK. Later boards say 39 MB was a **different ELF**. Live cost after B8 was ~28 s / 12 GB. This turn measured **39 s / 16 GB**.

Do not say memory is closed.

## 2. Inventory
Live `ooda/std/**/*.oo` is **36**, not 34. The extra two are `std/scenario.oo` and `std/sh.oo`.

`std/os/fs.oo` is **95** lines, not 83.

## 3. env.oo
`ooda/std/os/env.oo:6` is `return oo_env_get(cap, key);`. Not a self-call. F20 row that still says recursive stub is stale. B8 later showed env.oo was not the 30 GB cause.

## 4. .oo smokes
`harness_f20_inventory_smoke.oo` still wants 34 files — would fail on disk. F26 `.oo` smoke is still blocked by Task 8 (seed/runtime). `.oo` copies remain. Not run this turn (OpenCode owns scripts).

## Verdict
| Claim | Honest |
|-------|--------|
| 22.8 GB happened | yes |
| 39 MB is the floor | no |
| 34 std files | no (36) |
| env.oo still recurses | no |
| Check is cheap now | no (39 s / 16 GB this ELF) |

**Marker:** `TASK9_F26_F20_2026-08-13`
