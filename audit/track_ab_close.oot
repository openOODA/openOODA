# track_ab_close.oot — Track A rebuild + Track B seventh-CRIT close verify
# Date: 2026-08-12T10:07:10Z
# Worker: grok-build (Track A+B close)
# Method: read track_a_rebuild_meta.oot; live re-read tip sources for CRIT 1.1–1.3, 2.2–2.3, 7.3, 1.4–1.7, 1.9

## Track A rebuild result

Source: `audit/track_a_rebuild_meta.oot` (present; 2026-08-12T10:06:21Z).

| Field | Value |
|-------|--------|
| Tip SHA | `56f0f19` (`56f0f19f55473f8961b5614cf71267b1c96aebe4`) |
| Tip subject | feat: metamorphic path-A emit drive, decoy styles, runtime mix APIs |
| Seed pure multi | **PASS** — cold seed multi rebuild green; side out `~/.cache/ooda-tmp/oodac_from_seed` (908656 B) |
| Product install | **PASS** — `oodac/oodac` ← side (atomic `.new`+mv; prior tip `oodac/oodac.bak` 878392 B) |
| metamorphic_path_a_smoke | **PASS** (incl. live "emit-c path-a produces decoys") |
| Live emit-c decoys | **YES** — `__oo_meta_decoy_0/1/2` in emit of `fixtures/metamorphic_path_a.oo` |
| meta_epoch_smoke | **PASS** |
| **OVERALL** | **PASS** |

Track A does not claim seventh-CRIT code land by itself; it re-greens product tip after concurrent runtime/oodac edits and proves metamorphic path-A live.

## CRIT status table (seventh audit targets)

Workspace: `/home/jeryd/Projects/openOODA/ooda` (working tree dirty ahead of origin; tip commit `56f0f19` + uncommitted Track B fixes).

| # | Sev | Status | Evidence |
|---|-----|--------|----------|
| 1.1 | CRIT | **FIXED** | `runtime/chs_rt_host.c:25-39` — `oo_host_ast_dump`/`oo_host_check`/`oo_host_token_dump`/`oo_chs_build` take `long long cap` and call `oo_cap_require_ffi(cap, …)` at entry. Header `chs_rt.h:189-192` matches. |
| 1.2 | CRIT | **FIXED** | `runtime/chs_rt_fs.c:99-102` — `oo_env_get` uses `oo_process_policy_getenv(key.data ? key.data : "")` (not raw `getenv`). |
| 1.3 | CRIT | **FIXED** | `runtime/chs_rt_hitl.c` rewritten (ZT path-A): no `hitl_audit_open`; no raw `getenv("HOME")`. Remaining env reads use `oo_process_policy_getenv("OODA_HITL_ALLOW")` / `OODA_HITL_AUTO_APPROVE` (lines 19, 33). Gap closed by rewrite. |
| 1.4 | HIGH | **FIXED** | Cap-init sites use `pthread_once` + `PTHREAD_ONCE_INIT`: `chs_rt_sys.c:15`, `chs_rt_ffi.c:15`, `chs_rt_libfloor.c:10`, `chs_rt_time_rand.c:10`, `chs_rt_alloc.c:15` (+ list/meta). No `g_*_ready` in `runtime/*.c`. |
| 1.5 | HIGH | **FIXED** | Token packs use all 8 entropy bytes (b[0]…b[7] << shifts), e.g. `chs_rt_sys.c:35-38`, `chs_rt_alloc.c:38-39`, `chs_rt_ffi.c:34` — not 32-bit-only masks. |
| 1.6 | HIGH | **FIXED** | `chs_rt_alloc.c:97-106` `oo_free_bytes`: if `p > oo_list_ambient_quota` leave quota unchanged (no-op reclaim); only subtract when `p <= quota`. |
| 1.7 | HIGH | **FIXED** | `chs_rt_alloc.c:84-89` — before add, if `n > LLONG_MAX - oo_list_ambient_quota` saturate to `LLONG_MAX`. |
| 1.9 | HIGH | **FIXED** | `oodac/check_cap_util.oo:88-97` `is_sealed_sys` includes `async_join`, `async_join_internal`, `python_embed`, `python_embed_internal`. |
| 2.2 | CRIT | **FIXED** | `oodac/c_emit_call.oo:81-82` — after `c_emit_call_args`, `c_secret_emit_guard(env, mname, a0, a1, a2, margs)` in `c_emit_method`. |
| 2.3 | CRIT | **FIXED** | `oodac/c_emit_secret.oo:230-235` SECRETSINK chain adds `setenv`, `env_set`, `unsetenv`, `mmap`, `mprotect`, `shmat`, `pwrite`, `sendfile`, `splice`, `tee`. |
| 7.3 | CRIT | **FIXED** | `runtime/chs_rt_crypto.c:115-120` — `crypto_hmac_sha256_internal` wipes key stack via `crypto_secure_wipe(k, sizeof(k))` (+ ipad/opad/outer) before return (explicit_bzero-class). |

## Counts

| Status | Count |
|--------|------:|
| **FIXED** | **11** |
| **STALE** | **0** |
| **OPEN** | **0** |

Scoped set: CRIT 1.1, 1.2, 1.3, 2.2, 2.3, 7.3, 1.4, 1.5, 1.6, 1.7, 1.9 — all **FIXED**.

## Notes

- Track A overall rebuild: **PASS** (seed multi green; product tip = side install; live decoys YES).
- Track B code lands are present in the ooda working tree (modified runtime + `c_emit_call.oo` / `c_emit_secret.oo` / `check_cap_util.oo`); not all necessarily in commit `56f0f19`.
- Out of scope still OPEN from seventh audit (not re-scored here): 2.1/2.4 return-taint & alias rebind, 3.x ambient init, 5.x brace taint, 7.1–7.2/7.4–7.8 memory, 8.1 DEBUG println, 9.x hygiene, etc.
- Honest residual from original seventh audit (std untracked, crypto gates) unchanged.

end of track_ab_close.oot
