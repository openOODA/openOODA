# WASM-ARCH — Loop 12 five named const-print folds

File: openOODA/audit/wasm_arch.oot
Architecture Lead. Not a complete WASM compiler. Not 15 loops of 75 locks.

(a) seventeen println(STRING) joined by newline
(b) if (!(INT==INT)) no-else — taken when unequal
(c) if INT!=INT || INT!=INT no-else — taken when either pair unequal
(d) if (INT!=INT && INT!=INT) no-else — taken when both pairs unequal
(e) if INT<INT && INT<INT no-else — taken when both pairs less-than

Hostiles dummy: IDENT seventeenth, if (!(1==x)), if 1!=x || 3!=3, if (1!=x && 3!=4), if 1<x && 3<4.
Dummy end residual. Browser host residual.
Not a complete WASM backend. C+gcc everyday.

Status: BAR. Five folds. Adjacent pipelines off-limits.
