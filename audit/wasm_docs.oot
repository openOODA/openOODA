# WASM-DOCS — If-fold already named; residuals quoted

File: openOODA/audit/wasm_docs.oot
DOCS lane. RULES 1.3. C emit + gcc stays the everyday path.
Boards already name if-true/if-false. No SHIPPED/ROADMAP apply.

Logline: SHIPPED and ROADMAP already name const if-true/if-false locks, and dummy-end, not-complete, browser-host, and C+gcc everyday stay residual quotes.
Setup: Read-only SHIPPED.oot and ROADMAP.oot. Quotes live in /tmp/grok-goal-3afece913704/implementer/wasm_boards.txt.
Beats:
  1. Say whether if-fold is already named.
  2. Quote dummy-end, not-complete, browser-host, and C+gcc everyday.
  3. Keep honesty `[ ]`. Do not edit guide/. Do not sell a complete WASM backend.

## 1. Verdict

Already named. Not PROPOSED. Not APPLIED this turn.

  SHIPPED.oot:56 names `scripts/wasm_if_true_smoke.oo` and
  `scripts/wasm_if_false_smoke.oo`. IDENT if stays dummy
  (`scripts/wasm_if_hostile_smoke.oo`).
  SHIPPED.oot:84, ROADMAP.oot:43, and ROADMAP.oot:112 list
  if-true/if-false after int-lit println as named locks.

  Parent applies nothing. Locks are already on the boards.

## 2. Residual quotes (exact)

Dummy-end:

  Other functions keep dummy `end`.
  Other fns keep dummy `end`.
  Other wasm fns keep dummy `end`.
  Dummy WASM `end` stays on non-print functions.
  dummy WASM `end`, browser WASM host) stay residual, not this-cycle `[ ]` work.

Not-complete:

  Not a complete WASM backend.

Browser-host:

  Not a browser host. Not the everyday path.
  A browser WASM host is residual.
  A browser host is residual.

C+gcc everyday:

  C emit through `chs_rt`, then `gcc`. That is the product path.
  C emit + gcc remains the everyday path.
  C emit + gcc remains the product path.
  Native path: no wasmtime, clang, or special env file for C emit + gcc.
  Backends: C emit + `chs_rt` + gcc is the product path.

Honesty (stays `[ ]`; do not flip):

  [ ] Public text must not sell residual or moonshot as product:
      load-time layout morph, HITL (the loop is `ooda fix`), device
      `gpu_launch_shader`, Production LLVM as the everyday path,
      dummy WASM bodies, WAT emit, or a browser WASM host.

## 3. Do not flip

  ROADMAP honesty row stays `[ ]` dummy/WAT/browser.
  Do not flip leftover `[x]` rows.
  Do not edit guide/.
  Do not sell a complete WASM backend.
  Extra emit-wasm is not the everyday path.

## 4. Isolation

  Wrote:
    openOODA/audit/wasm_docs.oot
    /tmp/grok-goal-3afece913704/implementer/wasm_boards.txt
  Did not edit SHIPPED.oot or ROADMAP.oot.
  Did not write docs_sentences.txt (not a propose turn).
  Did not edit guide/.
  Did not emit. Did not rebuild.

Status: if-fold already named. Residuals quoted. Boards not edited.
