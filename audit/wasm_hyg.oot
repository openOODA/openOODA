# WASM-HYG — Line lock for emit-wasm and wasm runtime

File: openOODA/audit/wasm_hyg.oot
Process only. Not product. Disk is the source.
Owner: HYG. RULES.oot 1.7 and 1.13. FORMAT.oot headers.
C emit plus gcc stays the everyday path.

Logline: Every wasm_emit*.oo file and every chs_rt_wasm*.c file stays at or under 256 lines.
Setup: Count set is ooda/oodac/wasm_emit*.oo and ooda/runtime/chs_rt_wasm*.c. HYG writes this board only.
Beats:
  1. Count lines on disk. Comments count.
  2. Require Title, Logline, Setup, and Beats on each emit .oo file.
  3. Report any file over 256. Do not split in this pass.
  4. Search the count set for leftover /tmp names and unauthorized .py .sh .md.

## 1. Count set

  emit  14  ooda/oodac/wasm_emit*.oo
  rt     2  ooda/runtime/chs_rt_wasm*.c
  board  1  openOODA/audit/wasm_hyg.oot
  total 17

  HYG owns this board. HYG does not edit emit or runtime C this pass.
  HYG does not destub regex. HYG does not edit c_emit, llvm, or x86.

## 2. Line table

  Cap is 256. pass means lines <= 256. fail means lines > 256.

  | file | lines | verdict |
  | --- | --- | --- |
  | ooda/oodac/wasm_emit.oo | 83 | pass |
  | ooda/oodac/wasm_emit_expr.oo | 125 | pass |
  | ooda/oodac/wasm_emit_fn.oo | 226 | pass |
  | ooda/oodac/wasm_emit_fold.oo | 190 | pass |
  | ooda/oodac/wasm_emit_ident.oo | 91 | pass |
  | ooda/oodac/wasm_emit_if.oo | 121 | pass |
  | ooda/oodac/wasm_emit_int.oo | 71 | pass |
  | ooda/oodac/wasm_emit_lit.oo | 83 | pass |
  | ooda/oodac/wasm_emit_mem.oo | 158 | pass |
  | ooda/oodac/wasm_emit_preamble.oo | 210 | pass |
  | ooda/oodac/wasm_emit_primary.oo | 228 | pass |
  | ooda/oodac/wasm_emit_stmt.oo | 247 | pass |
  | ooda/oodac/wasm_emit_strcat.oo | 85 | pass |
  | ooda/oodac/wasm_emit_two.oo | 88 | pass |
  | ooda/runtime/chs_rt_wasm.c | 211 | pass |
  | ooda/runtime/chs_rt_wasm_print.c | 239 | pass |
  | openOODA/audit/wasm_hyg.oot | 86 | pass |

  Closest emit file: ooda/oodac/wasm_emit_stmt.oo at 247.
  Closest C file: ooda/runtime/chs_rt_wasm_print.c at 239.
  HYG does not split those files.

## 3. Over 256

  None. Zero emit files over 256. Zero runtime C files over 256.

## 4. Headers

  dinner_lint wants `// # ` and `// Logline:` in the first 25 lines.
  FORMAT.oot 1.1 also wants `// Setup:` and `// Beats:` with 3 to 6 beats.

  Every present emit .oo file has Title, Logline, Setup, and 3 beats.
  No beat names a line number.
  Each Logline ends with a period.
  Runtime .c files have no Academy header. They are C, not .oo.
  This board has a FORMAT.oot 2.1 title and File path.

## 5. Leftovers

  Grep of the listed emit and runtime files:
    No hardcoded /tmp path.
    No leftover .py .sh .md next to those files.

  HYG did not create leftover files. HYG did not delete adjacent smoke .bin
  or .bin.tmp.c names. Those names are outside this count set.

## 6. Result

  Lines: PASS
  Headers on emit .oo: PASS
  Files over 256: none

  HYG write set:
    openOODA/audit/wasm_hyg.oot          86  PASS
