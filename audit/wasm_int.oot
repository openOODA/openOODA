# WASM_INT — Product emit-wasm integration

File: openOODA/audit/wasm_int.oot
INT role. Independent verifier. Product binary only: ooda/oodac/oodac.
Did not run oodac/oodac.wasm4. Did not rebuild. Did not edit emit.
Did not run emit-c, emit-llvm, or emit-x86. Buf is MEM.
Did not score the new if-fold. Parent rebuilds first.

Logline: Product emit-wasm prints hello, print-hi, ident-hi, three-string n-join, two STRING lines, and int 42 under wasmtime.
Setup: cwd ooda/. Drive ./oodac/oodac. wasmtime at /home/jeryd/.wasmtime/bin/wasmtime. Host 2026-08-18. Scratch /tmp/grok-goal-3afece913704/implementer/.
Beats:
  1. Wait for parent rebuild. Record REBUILD_RC:0. Do not rebuild.
  2. Prove existing print locks from product emit-wasm plus wasmtime stdout.
  3. Record mixed ident+STRING as dummy/empty. Skip const if-fold.

## 0. Verdict

PASS

  1. strings usage PASS
  2. hello wasmtime PASS (`Hello, openOODA Developer!`)
  3. wasm_print_hi wasmtime PASS (`Hello, openOODA!`)
  4. wasm_ident_lit wasmtime PASS (`ident-hi`)
  5. wasm_strcat3_lit wasmtime PASS (`Hello, n-join!`)
  6. wasm_strcat_lit wasmtime PASS (`Hello, strcat!`)
  7. wasm_strcat_n.oo ABSENT (n-join lock is wasm_strcat3_lit.oo)
  8. wasm_two_println wasmtime PASS (`line-a` then `line-b`)
  9. wasm_int_lit wasmtime PASS (`42`)
  10. wasm_strcat_mixed dummy/empty (not `Hello, mixed!`)
  11. if-fold not this bar

## 1. Host

  Path: `/home/jeryd/Projects/openOODA/ooda/oodac/oodac`
  Parent rebuild first. File: `/tmp/grok-goal-3afece913704/implementer/oodac_rebuild.txt`
  input_fp: `a5832cacd3eb99a6f13a8d43e2eec10ee0bc02a0d7950692d4a2807a28358874`
  `OK_PURE_MULTI` then `REBUILD_RC:0`.
  This role did not overwrite that file. Did not start `bootstrap/oodac_pure_build`.
  `oodac/oodac.wasm4` exists. This role did not exec it.
  `oodac/oodac.x86` exists. This role did not exec it.

  wasmtime: `/home/jeryd/.wasmtime/bin/wasmtime` is executable.
  version: `wasmtime 47.0.3 (5554cc1a6 2026-07-31)`

  After rebuild, product smokes used `./oodac/oodac emit-wasm`.
  Logs: `/tmp/grok-goal-3afece913704/implementer/wasm_reg_*.log`
  stdout: `ooda/.ooda-cache/ooda-tmp/wasm_*.out`
  Proof log: `/tmp/grok-goal-3afece913704/implementer/int_now.log`

## 2. strings usage

PASS

  Command (cwd ooda/):

    strings oodac/oodac | grep 'oodac: usage:'

  stdout (one line):

    oodac: usage: [--backend c|wasm|llvm|x86] tokens|ast|check|build|run|emit-c|emit-wasm|emit-llvm|emit-x86|outline|reflect <file>

  EXIT: 0

## 3. fixtures/hello.oo

PASS

  Product smoke `scripts/wasm_hello_smoke.oo` after rebuild: PASSED.
  wasmtime stdout (cat -A; `$` is newline):

    Hello, openOODA Developer!$

  Same text in implementer `int_hello.out` and cache `wasm_hello.out`.
  implementer `int_hello.wasm` is 197 bytes. Magic `00 61 73 6d 01 00 00 00`.

## 4. fixtures/wasm_print_hi.oo

PASS

  Product smoke `scripts/wasm_print_lit_smoke.oo` after rebuild: PASSED.

    Hello, openOODA!$

  implementer `int_print_hi.wasm` is 169 bytes. Magic `\0asm`. Has `fd_write`.

## 5. fixtures/wasm_ident_lit.oo

PASS

  Product smoke `scripts/wasm_ident_lit_smoke.oo` after rebuild: PASSED.

    ident-hi$

  implementer `int_ident.wasm` is 161 bytes. Magic `\0asm`. Has `fd_write`.

## 6. fixtures/wasm_strcat3_lit.oo

PASS (n-join). `fixtures/wasm_strcat_n.oo` is absent.

  Body is only `println("Hello, " + "n" + "-join!");`.
  Product smoke `scripts/wasm_strcat_n_smoke.oo` after rebuild: PASSED.

    Hello, n-join!$

  Also `fixtures/wasm_strcat_lit.oo` prints `Hello, strcat!`.
  implementer `int_strcat3.wasm` is 167 bytes. Magic `\0asm`.

## 7. fixtures/wasm_two_println.oo

PASS

  Body is only `println("line-a"); println("line-b");`.
  Product smoke `scripts/wasm_two_println_smoke.oo` after rebuild: PASSED.

    line-a$
    line-b$

  Order is line-a then line-b. implementer `int_two_println.wasm` is 166 bytes.

## 8. fixtures/wasm_int_lit.oo

PASS

  Body is only `println(42);`.
  Product smoke `scripts/wasm_int_lit_smoke.oo` after rebuild: PASSED.

    42$

  implementer `int_int_lit.wasm` is 155 bytes. Magic `\0asm`. Has `fd_write`.

## 9. Dummy residual (not the named beat)

  `wasm_strcat_mixed.oo` exists. Product smoke PASSED.
  wasmtime stdout is empty. Not `Hello, mixed!`.
  implementer `int_mixed.wasm` is 74 bytes, magic `\0asm`, code `0a 04 01 02 00 0b`.
  Honest dummy. Not a mixed runtime concat.

## 10. Isolation

  This role wrote:

    openOODA/audit/wasm_int.oot
    /tmp/grok-goal-3afece913704/implementer/int_now.log

  Did not edit c_emit*, llvm*, x86*, wasm_emit*, cli, guide, or regex.oo.
  Did not overwrite `oodac/oodac`.
  Did not run `oodac/oodac.wasm4`.
  Did not rebuild.
  Did not score if-true / if-false / if-hostile.

Status: PASS. Product emit-wasm ship bar holds on existing locks after rebuild.
Next: COORD owns the if-fold bar. DOCS may name two-println and int-lit from this INT proof.
