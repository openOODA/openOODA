# WASM-QA — Extra-emit seventeen/paren-not-eq/neq-or/paren-neq-and/lt-and no-else locks

File: openOODA/audit/wasm_qa.oot
QA role. Drive ./oodac/oodac only.
Did not edit c_emit*, llvm*, x86*, aarch64*, cli, guide.

Logline: Five hit fixtures and five IDENT hostiles lock extra-emit const folds.
Setup: cwd for smokes is ooda/. wasmtime is optional. Positional check(p, cmd, n).

## Hits

  wasm_seventeen_println.oo — line-a through line-q. Smoke wasm_seventeen_println_smoke.oo
  wasm_if_paren_not_eq_noelse.oo — if (!(1==2)) prints pnoteq-ne. Smoke wasm_ifpnoteqne_smoke.oo
  wasm_if_neq_or_noelse.oo — if 1!=2 || 3!=3 prints neqor-ne. Smoke wasm_ifneqorne_smoke.oo
  wasm_if_paren_neq_and_noelse.oo — if (1!=2 && 3!=4) prints pneqand-ne. Smoke wasm_ifpneqandne_smoke.oo
  wasm_if_lt_and_noelse.oo — if 1<2 && 3<4 prints ltand-ne. Smoke wasm_ifltandne_smoke.oo

## Hostiles

  IDENT seventeenth: wasm_seventeen_hostile.oo — fold-me-only fails.
  if (!(1==x)): wasm_if_paren_not_eq_noelse_hostile.oo — fold-me-only fails.
  if 1!=x || 3!=3: wasm_if_neq_or_noelse_hostile.oo — fold-me-only fails.
  if (1!=x && 3!=4): wasm_if_paren_neq_and_noelse_hostile.oo — fold-me-only fails.
  if 1<x && 3<4: wasm_if_lt_and_noelse_hostile.oo — fold-me-only fails.

Dummy empty, ERR, wasmtime fail pass. Invented fold fails.
Every smoke sets let oodac = "./oodac/oodac"; positional check(p, cmd, n).
Magic 0061736d01000000. Not WAT. Not oodac.wasm4.
