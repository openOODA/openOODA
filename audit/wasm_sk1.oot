# WASM-SK1 — False-fold hunt on seventeen / paren-not-eq / neq-or / paren-neq-and / lt-and

File: openOODA/audit/wasm_sk1.oot
SK1. Extra-emit WASM. Did not edit c_emit*, llvm*, x86*, aarch64*, cli, guide.

Logline: IDENT seventeenth, if (!(1==x)), if 1!=x || 3!=3, if (1!=x && 3!=4), and if 1<x && 3<4 miss.
Setup: Token field 0 is kind. Field 3 is text.

## 0. Verdict

HONEST MISS.

## 1. IDENT seventeenth

wasm_fn_seventeen_println requires seventeen STRING then RBRACE. Seventeenth print IDENT misses.

## 2. if (!(1 == x)) no-else

wasm_fn_if_paren_not_eq_noelse requires LPAREN BANG LPAREN INT EQEQ INT RPAREN RPAREN.
IDENT right of EQEQ misses. wasm_fn_if_not_eq_noelse has no outer parens.

## 3. if 1 != x || 3 != 3 no-else

wasm_fn_if_neq_or_noelse requires INT NEQ INT OROR INT NEQ INT. IDENT right misses.

## 4. if (1 != x && 3 != 4) no-else

wasm_fn_if_paren_neq_and_noelse requires LPAREN INT NEQ INT ANDAND INT NEQ INT RPAREN.
IDENT right misses. wasm_fn_if_neq_and_noelse has no parens.

## 5. if 1 < x && 3 < 4 no-else

wasm_fn_if_lt_and_noelse requires INT LT INT ANDAND INT LT INT. IDENT right misses.
