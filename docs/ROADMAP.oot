# ROADMAP — OODAC 9/10 Goal

File: openOODA/docs/ROADMAP.oot

OODAC 9/10 on safety, perf, security — best of rustc/Go/Zig plus OODAC's lattice (20+26 caps, typestate, SECRET, SMT, unique_fns, blackbox).

**Goal:** Safety 9, Perf 9, Security 9 — keep the lattice, steal the best.

**How:**
1. ty as enum — landed `oodac/types/ty_kind.oo` (string tags until unique_fns preamble has TyTag)
2. borrowck &/&mut — landed on `oodac check` (move and `&mut` alias fail closed)
3. clang -O2 + real llvm emit (perf) — default `build` is llvm; unique-bar victims still incomplete
4. net/http over TcpCap — sealed fetch requires TcpCap

**Next:** typed artifact v3 (toks + type env + granted caps + SMT verdict); emit still no re-lex

