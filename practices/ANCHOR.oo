// # Practices Index — Anchor for Agents
//
// Logline: Index of the 3 evolving practice documents. These are methodologies, not laws.
//
// Setup: This file lives in practices/. Resolve every beat path
//        from this file's directory. Do not prefix openOODA/ or
//        practices/. Lowercase per RULES.oot §1.19. Practices
//        evolve; laws (RULES.oot, RFCs) do not.
//
// Beats:
//   1. 8d-red-team.oot — the 8 adversarial dimensions + 8-gate veto (frozen, but a practice not an RFC).
//   2. ooda-workflow.oot — the OODA loop + 5-component handoff + multi-agent protocols.
//   3. release.oot — the versioning + tagging + release workflow (4-step sequence, drift recovery).

import "8d-red-team.oot";
import "ooda-workflow.oot";
import "release.oot";
