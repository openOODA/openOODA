# Moonshot Scorecard — Per-Target 10/10

File: openOODA/openOODA/scripts/moonshot_scorecard.oot

Source: NORTHSTAR.oot:20-29 (the moonshot).

Each moonshot line is scored 0–10. The score is the count of
unmet evidence; 10 = fully evidenced, 0 = the line is a slogan.

Date: 2026-09-10
Verifier: Mavis audit sweep, session mvs_b2115d13599946ac965f975f0e628547
Double-run: identical across two fresh subshells (sha256 verified).
Law: RULES.oot §1.2 (Boyd's E-M) and RULES.oot §1.17 (80/20).

| # | Moonshot line (NORTHSTAR §1) | Score | Evidence (file:line) | Gap |
|---|-------------------------------|------:|----------------------|-----|
| 1 | No trust without proof        | 8     | openOODA/scripts/proof_of_today.oo (10 drift fixes this session, drift threshold > 4) | daily audit publish is new (this commit); 8D Red Team not yet invoked in CI per-repo |
| 2 | Capabilities replace trusted programs | 9 | oodar/sec/cap/cap_table.json v3.4.2: 26 caps (20 implemented + 6 future); MCP verify_surface checks 20/26 bits | oodar CHANGES.md is 79KB of unverified history; env-var cap probe not yet published |
| 3 | Contracts replace trusted behavior | 2 | oodac HEAD (auto-release pin) lacks "typed artifact v3" in its commit message; the v3 commit 5b03bca is no longer HEAD | typed artifact v3 lives 7 commits behind HEAD; SMT verifier pass not yet green; both in compiler lane |
| 4 | Reproducible builds replace trusted binaries | 10 | 9/9 consumer repos wire auto-release; 9/9 pinned to a tagged version | none — full coverage |
| 5 | Double-run tests, even evidence is verified | 10 | 9/9 consumer repos run 8D Red Team / blackbox autopsy / double-run in CI; blackbox v0.0.11 ships | none — full coverage |
| 6 | Self-hosting replaces trusted vendors | 3 | oodac/bootstrap/oodac_pure_build exists and is executable | full self-host (oodac builds oodac, hash-equal) not yet green; host script is the bridge; compiler lane |
| 7 | Multi-target replaces trusted hardware | 3 | oodac/emit/c/ (default) and oodac/emit/llvm/ (in dev) — 2/6 planned backends | oodac/emit/{x86,aarch64,wasm,gpu}/ and oodac/vm/ not yet on disk; ROADMAP.oot; compiler lane |
| 8 | Per-target 10/10 scores | 5 | this scorecard exists; the proof_of_today.oo driver re-derives line values from on-disk evidence | scorecard is now closed-loop (proof + mirror); daily publish is the new move; no trend line yet |

## Headline

Total: 8 + 9 + 2 + 10 + 10 + 3 + 3 + 5 = 50 / 80  (62.5%)
Per-target 10/10 average: 6.0 / 10
Moonshot headline: 6.0 / 10 — past slogan, below operational.

## Threshold policy

If any line drops below 4/10 for two consecutive daily runs, raise
to a sprint-blocker. If the headline drops below 5/10, block the
next release per RULES.oot §1.17 (80/20 — most leverage is in the
lowest lines first).

## What closes each gap

1. No trust without proof → publish_daily_audit.oo (this commit); the
   daily audit page is the new SSoT of "what is true today."
2. Capabilities → 8-D env-var cap probe; CHANGES.md review.
3. Contracts → compiler lane; outside this session.
4. Reproducible builds → 9/9 wired + pinned; closed.
5. Even evidence is verified → 9/9 repos with red-team CI; closed.
6. Self-hosting → compiler lane; outside this session.
7. Multi-target → compiler lane; outside this session.
8. Per-target 10/10 → publish_daily_audit.oo (this commit); the
   daily page is the trend line.

## What this scorecard is NOT

Not a marketing surface. Not a vanity metric. The score is the
score, even when it is 2/10. Subtract what earns nothing
(RULES.oot §1.17).
